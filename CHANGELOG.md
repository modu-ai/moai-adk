# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **[SPEC-TODO-DESTRUCTIVE-GUARD-001](.moai/specs/SPEC-TODO-DESTRUCTIVE-GUARD-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t330). **`moai todo done` no longer discards the card — it archives it, and a new `moai todo undone <n>` restores it byte-identical to its state immediately before the `done`, findings included.** The archive is additive on `BacklogRecord`: one field, `Archived []BacklogArchiveEntry`, each entry carrying the card, every finding that named it, and the position it held — not the two-field split `plan.md` sketched, because a positionless restore would append rather than reinsert, and REQ-TDG-002 asks for byte-identity, not an equivalent-looking queue. The five-field per-item contract, the three-value `BacklogState` enum, and the stored `schema_version` (still `"1"`) are unchanged, so a binary predating the archive keeps opening a database that carries archived rows (verified: `todo list` on such a database still succeeds, sees the live card, does not see the archived one, and can still `add`). Archived rows are invisible to every live-queue reader (`list`, `next`, `why`, `analyze`) and to the duplicate-analysis input, but are **deliberately included** in `moai todo export-json` — the downgrade route — which now discloses on stderr, only when the export actually carries archived rows, that a binary predating the archive will drop them on its first write; live readers stay silent because REQ-TDG-007 governs them and REQ-TDG-015 governs the export in the opposite direction.
  - **`done` gains two opt-in guards, both no-ops until requested.** `--expect <prefix>` refuses when the addressed card's text does not start with the given prefix. `--require-landed` asks a landing predicate (`internal/kanban/prlink_landed.go`, shared with `moai todo pr`) and refuses only on a **positive** "not landed" answer, proceeding when the predicate is inconclusive — because the predicate was measured, not assumed, to answer the wrong question in both its reachable configurations: as shipped (`LandedRef = "origin/main"`) it reads false for every develop-integrated card, and after the obvious ref correction to `origin/develop` it reads true on any mention at all, which would have passed the premature `done` this SPEC was dispatched to catch. The ref was deliberately left uncorrected — changing it would also change `moai todo pr`'s answers — so `--require-landed` ships opt-in, with its help text and the doctrine naming exactly which ref its answer is about. Absent the flag, `done` runs no landing query and adds no subprocess cost; every refusal path (mismatched `--expect`, a positive `--require-landed`, restoring an unarchived id, restoring into a reissued id) leaves the queue record byte-identical, and neither `done` nor `undone` prompts on any path, refusal included.
  - **A restored id cannot collide with a reissued one**: the allocator's high-water mark now clears archived ids on restore, so a card cannot be restored into an id since given to a different card — the restore is refused and the collision named. A second `undone` on an already-restored card is likewise refused, byte-identical; the archive entry is emptied on restore, so each row has exactly one home.
  - **16 acceptance criteria (AC-TDG-001..016), counted against `acceptance.md`: 16 PASS, 0 FAIL** — every criterion verified in an isolated repository (a Go `t.TempDir()` fixture or a `mktemp -d` + `git init` shell repository); no criterion touched this repository's live queue. Coverage: `internal/kanban` 87.0%, `internal/cli` 79.8% at run-phase and 79.9% re-measured on the merged tree (`go test ./internal/cli/ -count=1 -cover`, after `origin/develop` was absorbed) — below the 85% package target either way; not measured against a base-tree figure, so no claim is made about whether this change moved it. The new code itself is fully covered except four arms, two stream-write failures and two SQL failures (`todoWriteLine` 66.7%, `writeArchive` 66.7%, `readArchive` 76.9%, `ArchiveCard` 93.8%). Both `todo.md` doctrine copies (`.claude/skills/moai/workflows/todo.md` and its template mirror) carry the new verb and flags, byte-identical (`cmp` rc=0). Run-phase commits M1 `05d2c234b` (archive storage shape) → M2-M5 `3a0ce021c` (`undone`, `done`'s guards, export disclosure) → M6 `3f1a7b896` (doctrine + template mirror).
  - **Two pre-existing freeze tests were extended to declare this change's additions rather than edited to absorb them** — `TestBacklogEngineSchemaShape` and `TestTodoVerbSurfaceZeroDelta` — so the exact-surface guard both tests provide is preserved rather than loosened. The landed predicate still answers the wrong question in general (documented in the help text and the doctrine); closing that fully is deferred to a follow-up card (t331) that gives it a persisted landing-state field. The archive grows without bound — declared out of scope; a long-lived queue's `backlog.db` and every `export-json` artifact grow monotonically with finished cards.
  - **Independent sync audit: PASS-WITH-DEBT 0.890** harmonic (Functionality 0.95 / Security 0.92 / Craft 0.82 / Consistency 0.88), zero blocking findings, ten recorded-debt findings F1-F10 — report `.moai/reports/t330/sync-audit.md`. The auditor built the pre-archive binary from `812ee01fc` and drove six isolated fixtures rather than reading the claims back: the strict round trip is byte-identical, the old binary opens and writes a database carrying archived rows without dropping them, the id allocator refuses a restore into a reissued id even after `last_seq` is hand-lowered, and the JSON downgrade loss — inferred at run-phase — was measured (the archive does drop from the live queue, though the migration sidecar keeps the original, making the shipped disclosure conservative rather than overstated). Both freeze tests were confirmed extended rather than loosened: the exact-set comparison is intact in each. F1-F3 (a commit SHA `spec.md` v0.2.2 had already corrected, an unattributed coverage figure, and an uncovered-arm miscount) were corrected before integration; F4 (the archive is rewritten in full on every queue write, so the out-of-scope unbounded growth is a per-write cost) and F6 (`--require-landed`'s fail-open is indistinguishable from a pass on stdout, and the predicate's practical discriminating power is currently zero) are follow-up material for t331.
  - This sync commit carries the `spec.md` frontmatter close (`in-progress → completed`; `updated` already read the same day, so it was left unchanged — `plan.md` and `acceptance.md` carry no frontmatter block, so neither took a transition), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` was recorded as a `pending-backfill-sync` placeholder and backfilled in the immediately following commit. Nothing was pushed by this session; the repository-wide CI verdict belongs to whoever integrates the `WT-todo-destructive-guard` branch. 🗿 MoAI
- **[SPEC-CODEX-INIT-001](.moai/specs/SPEC-CODEX-INIT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`moai codex` launch verbs (`cli`/`app`) now pass through an init-offer gate** right before launching: when the launcher's wiring classifier reports the project incompletely wired, the gate reports the state and its remedy, and in an interactive session offers to initialize now. Accepting runs the existing `moai init --agent codex` wiring generator (`internal/codexwiring`) exactly once and then secures the AGENTS.md ↔ CLAUDE.md instruction contract; declining writes nothing and exits 130 (a cancel code, distinct from every error code); a non-interactive session reports the state and exits 1 WITHOUT issuing any prompt — a prompt nobody can answer would hang automation. The gate consumes the launcher's single wiring judgement and never re-derives it from disk, and it takes no `--spawn` parameter, so both launch paths (`internal/cli/codex_launcher.go:256`) cross the same function and a spawn-only bypass cannot exist. A generator or contract failure stops the launch: no contract step runs after a failed generator, and no launch delivers an unwired session. The instruction contract (`internal/cli/codex_contract.go`) is **connection-only, never content**: a missing AGENTS.md/CLAUDE.md is created with a minimal body, an existing file is preserved byte-for-byte with at most one appended link directive (`@AGENTS.md` in CLAUDE.md, `@CLAUDE.local.md` in AGENTS.md only when that file exists), and an already-linked file is left untouched — idempotent, where "already linked" counts executing imports (outside code fences, HTML comments, and blockquotes), not raw occurrences. Every instruction path is containment-guarded BEFORE any read or write: each existing path component must `Lstat` as a plain directory (the leaf, when it exists, a regular file) and the symlink-and-`..`-resolved absolute path must stay inside the project root — judged by structure, not by a snapshot, and a refusal is issued without reading the path. All writes are per-file temp+rename with a stage-all-then-rename-all ordering, so a mid-write failure leaves every target byte-identical. 11 acceptance criteria (AC-CI-001..011), counted against `acceptance.md`: 11 PASS, 0 FAIL — including the 180-cell path-containment matrix (symlink, `..`, and file-kind axes × verb × spawn) and `GOOS=windows` vet rc=0. Run-phase commits M1-M3 `576dbad05` → M4 `70d8bd86d` → M5/M6 `c392b2ff9` → gate-repair `0a518f1d8` → close `f1d80b305`. This sync commit carries the 3-phase close (`in-progress → completed` merged into the sync commit), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` is recorded as a `pending-backfill-sync` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-WORKTREE-BASEREF-001](.moai/specs/SPEC-WORKTREE-BASEREF-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **New optional setting `git_strategy.worktree_base_branch` names the branch new card worktrees are cut from.** Card worktrees were branching from whatever `refs/remotes/origin/HEAD` happened to name — local repository metadata that does not survive a fresh clone — while the Claude Code `worktree.baseRef` setting accepts only `"fresh"` or `"head"` and so cannot name a branch at all. The setting is the reproducible handle on that choice, stored at the root of `.moai/config/sections/git-strategy.yaml` (`internal/config/types.go:174`) and shipped **empty by default**, which reproduces the pre-setting behaviour exactly on both paths — an unset value changes nothing. Two consumers read it: (1) a SessionStart step (`internal/hook/worktree_base_branch.go`, registered as Task 5 of the existing errgroup at `internal/hook/session_start.go:175-182`) points `refs/remotes/origin/HEAD` at the configured branch so natively created worktrees read the corrected symref — it fires **only from the primary checkout** (the `--git-dir` vs `--git-common-dir` discriminant), stays silent when the value is empty or already matches, prints exactly one line when it changes something, and never blocks session start; (2) `moai cc -w <name>` passes the branch to `git worktree add` as the base operand (`internal/cli/session_worktree.go`), honoured from any working tree, with the empty-base argv held byte-identical to the pre-setting invocation. Both consumers decide whether a value is usable through **one shared exported predicate** — `git show-ref --verify refs/remotes/origin/<value>`, `rc == 0` only — so a value naming no remote-tracking branch is refused before either write: the session-start step prints one diagnostic and leaves the symref alone, worktree creation falls back to the no-operand form. Surfaced two ways: a `Worktree Base Branch` diagnostic in `moai doctor` reports the comparison without writing and distinguishes a plain mismatch from an unresolvable value, and the web console gains a free-text field (`internal/settings/schema_sections.go:169`) with 4-locale strings. The typed save path additionally models three previously unmodelled git-flow keys (`develop_branch`, `release_branch_prefix`, `rc_version_format`) purely so a round trip stops dropping them — declarations only, no accessor and no consumer. Template-first: `internal/template/templates/.moai/config/sections/git-strategy.yaml.tmpl` ships the key with an empty value (branch names appear only in its comment), and `.claude/rules/moai/workflow/worktree-integration.md` documents the setting and both consumers, mirrored byte-identically into the template tree. 16 acceptance criteria (AC-WBR-001..016), counted against `acceptance.md`: 16 PASS, 0 FAIL. Run-phase commits M1 `81808d85b` → M2 `cf2955ed5` → M3 `9e1ea4226` → M4 `5658988be` → M5 `04c645a68` → M6 `8c46460ff`. Independent sync audit: **PASS 0.90** harmonic (Functionality 0.95 / Security 1.00 / Craft 0.80 / Consistency 0.90), zero blocking findings, six recorded-debt findings F1-F6 — report `.moai/reports/t313/sync-audit.md`; the auditor additionally drove all three SessionStart states end to end against a throwaway real repository, closing the fake-only-coverage risk the implementer had flagged for the merge decision. This sync commit carries the 3-phase close (`in-progress → implemented → completed` merged into the sync commit), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` recorded as a `pending-backfill-sync` placeholder and backfilled in the immediately following commit. 🗿 MoAI

- **[SPEC-AGENT-EMIT-LINEAGE-001](.moai/specs/SPEC-AGENT-EMIT-LINEAGE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`make build` now stops before compiling when the committed `internal/template/templates/.codex/agents/moai/*.toml` no longer match what the `.md` agent definitions emit, naming every drifted path and telling you to run `make agents-emit`.** Those TOMLs are machine-generated from the markdown agent layer, but nothing in the build ever invoked the generator, so a forgotten regeneration stayed silent locally until CI went red — and every binary built in the meantime shipped the old agent definitions. The new prerequisite closes that window locally, and it is strictly read-only: the build never regenerates. Regeneration stays behind the explicit `make agents-emit` verb, unchanged — a build that quietly overwrote a hand edit would erase the evidence that the hand edit ever happened.
  - **`moai doctor` gains one row, `Agent Emit Embed`**, asking a different question than the build check: it takes an *already-built* binary, has it deploy its own embedded templates into a scratch directory outside the repository, and compares the TOMLs that came out against the committed ones. Nothing source-side can answer this — `go test` recompiles its test binary on every run, so `//go:embed` reads back the same committed bytes the test compares against and the comparison can never fail. A stale binary is visible only by asking the binary. The row reports how many artifacts it compared (a partial extraction fails rather than passing on a subset) and fails when there is no readable binary to judge, so "0 comparisons, therefore fine" is unreachable. **In a project created by `moai init` the row reads `ok — not applicable` and `moai doctor`'s exit status is unchanged**: a deployed project carries no committed emission set, so there is nothing for this check to compare and it says so instead of failing.
  - **Two new make targets**: `agents-emit-check` (the read-only drift check, also runnable on its own) and `embed-check` (the embed-axis judgment, `BIN=<path>` to aim it at an installed binary rather than `bin/moai`). `embed-check` deliberately has no `build` prerequisite and is not wired into CI — a freshly built binary matches by construction, so a check that could only run right after a build would be the tautology it exists to replace.
  - Maintainer procedure is recorded once, in `CLAUDE.local.md` §2.0 [HARD] (the three copies of each agent definition, which one is generated, and which of the three checks catches a miss); `internal/template/CLAUDE.md` now names the build's real prerequisite chain and points there rather than restating it. 7 acceptance criteria (AC-AEL-001..007), counted against `acceptance.md`: 7 PASS, 0 FAIL, 4 of them mutation-established. Run-phase commits M1 `742a9485d` → M2 `6335b731b` → M3 `9b5d5b10a`, plus `f3e5006ce` (the applicability walk anchors on the committed emission set, not on a `.moai/` marker — a package directory can pick one up as test residue and misjudge the repository root as skippable). This sync commit carries the 3-phase close (`in-progress → implemented → completed`), the `progress.md` §E.4 signal, `@MX` annotations on the new check, and this entry; `sync_commit_sha` recorded as a `pending-backfill-sync` placeholder and backfilled in the immediately following commit. The out-of-scope observation recorded at `progress.md` §G-1 — `internal/cli/.moai/` test residue silently redirecting *other* `.moai/`-marker upward walks — remains open and is not closed by this SPEC. 🗿 MoAI

- **[SPEC-FULL-SUITE-DOCTRINE-001](.moai/specs/SPEC-FULL-SUITE-DOCTRINE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`manager-develop` stopped ordering a repository-wide test run at a point where the project's own standing contract forbids one (card t301).** The agent definition told the run-phase agent to execute the complete Go test suite — in its per-iteration loop and again at STEP 5 — while `AGENTS.md` §4 and `kanban-dispatch.md` §Verification load is lane-local both say, as `[HARD]` and on general grounds, to run only the tests a change can affect and let CI on the integration branch render the repository-wide verdict. The contradiction was resolved as drift in the agent definition, not as a standoff between two defensible policies: the scoping rule was already adopted at the always-loaded contract layer, so the agent definition is what had diverged. **The distributed default was not sacrificed to this repository's own circumstances** — all three copies moved together in one commit, `f33bbe39c` (verified an ancestor of the integration branch): the local `.claude/agents/moai/manager-develop.md`, the distributed `internal/template/templates/.claude/agents/moai/manager-develop.md`, and the third distributed copy `internal/template/templates/.codex/agents/moai/manager-develop.toml`, which has no local twin and is a **generated** artifact — it was regenerated via `make agents-emit` rather than hand-edited, after the golden guard was first observed failing RED on the hand-edit-then-skip-regeneration state, so the guard's live capability is measured rather than assumed. The two rule files carrying the batch definition (`agent-common-protocol-reference.md`, `verification-batch-pattern.md`) moved in both their local and template copies, and `internal/template/catalog.yaml` picked up the one-line hash refresh from `make build`.
  - **What replaces the deleted verdict is stated, not left implicit.** STEP 5 now reads "Run the tests the change can affect (memory guard: batches when needed)", and the completion report it produces MUST name the CI run on the project's **integration branch** as the owner of the repository-wide test verdict and state that this verdict is **PENDING at report time** — two canonical tokens fixed verbatim so that silently omitting the open item is distinguishable from reporting it. Surrounding prose deliberately says "repository-wide test verdict" rather than "full-suite verdict": the absence criteria police a lexical family (`(full|complete|entire|whole)[ -](test )?suite`), so the natural phrasing would have turned an absence check red against the very repair it describes. The `LARGE_SCALE` discriminator was removed as a whole item rather than by deleting the token, because its entire content was that discriminator and its consequence — a token-only deletion leaves a sentence that no longer means anything.
  - **15 acceptance criteria (AC-FSD-001..015), counted against `acceptance.md`: 14 PASS, 0 FAIL, 1 deferred.** A full-tree grep for `full suite` / `COMPLETE test` / `LARGE_SCALE` / `go test ./...` returns 0 hits across all three copies. All 15 pre-edit baselines were re-collected on the measured tree before any edit and matched the recorded values exactly, so the "baseline mismatch means the SPEC is rewritten, not passed" clause never fired; the baseline SHA was corrected from the planned `d29b8942e` to the measured branch point `7ed6edb3e` per `plan.md` §C. **AC-FSD-011 is deferred, not passed**: it is a post-hoc regression observation whose window has not opened — it closes when a real run-phase executes STEP 5 after this landing and records that batch's measured duration and command alongside the inherited 49m40s figure, with explicit attribution that the inherited figure is a carried-over claim no session here re-measured (re-measuring it would require the local full-suite run this SPEC exists to forbid). It is therefore not a before/after experiment, and no quantified improvement is claimed. Reasons, closure conditions, and the owning verdict-renderer are recorded in `progress.md` §E.4 so a later reader need not reconstruct them.
  - **The doctrine was hardened over five plan-audit iterations** — FAIL 0.57 → 0.75 → 0.81 → 0.83, with an operator decision to extend the iteration ceiling — which is why the criteria police a lexical family and per-site line ranges rather than a single literal: iteration 1 closed 9 blocking defects, iteration 2 pulled the third distributed copy into scope and shut 3 mutant holes, iteration 3 recognized that copy as generated rather than authored, and the final pass corrected a command-ordering defect. That history is recorded in `spec.md` §HISTORY and `progress.md` §E.1.
  - This sync commit carries the `spec.md` frontmatter transition (`in-progress → implemented`, version `0.5.0 → 0.5.1`) — the `completed` transition is **deliberately deferred** to a follow-up session once the AC-FSD-011 window satisfies, mirroring how SPEC-CI-FLAKE-SERIES-001 (card t278) is currently held — plus the `progress.md` §E.3 `run_commit_sha` backfill, the §E.4 signal, and this entry. Documentation-only: zero Go, template, or hook surface changed at sync. Per the doctrine this SPEC just landed, the repository-wide test verdict for this change belongs to the CI run on the integration branch and is PENDING at report time. `sync_commit_sha` is recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI

- **[SPEC-TODO-SQLITE-001](.moai/specs/SPEC-TODO-SQLITE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The backlog queue that `moai todo` operates on leaves its single growing JSON document for one SQLite database at `.moai/state/todo/backlog.db`, and the state directory it lives in is renamed from `.moai/state/kanban/` (card t306, absorbing card t309 — a schema and path redefinition is the only zero-cost moment for that rename).** The driver is `modernc.org/sqlite` v1.57.0, pure Go and not a preference: this repository's CI GOOS matrix builds with `CGO_ENABLED=0`, which hard-excludes every cgo-backed SQLite driver, so a pure-Go implementation was the only option that compiles on darwin/arm64, darwin/amd64, linux/amd64 and windows/amd64 alike. The connection is configured for the many-writer desktop case it actually sees — WAL journal mode, a 5000 ms busy timeout — and id integrity moves down into the storage layer itself: a UNIQUE constraint on the item id with `last_seq` advanced inside the same write transaction as the insert, so no committed mutation can mint a duplicate or reuse a removed card's id even when a process dies mid-write. Binary cost measured at +5,911,568 B (+5.64 MiB) against the 77,719,874 B pre-driver baseline, inside the +12 MiB budget. Run-phase commits: M1 `3d24cf6df` → M2 `83a1d492a` → M3+M4 `8910c337c` → M5 `ffe33ac09` → M6 `d19187327`, with evidence at `447f517fe`, SHA backfill at `1c5840558` and the verb-surface guard at `a8099e43e`. 18 acceptance criteria (AC-TOSQ-001..018), counted against `acceptance.md`.
  - **The move happens by itself, once, and is built to be interrupted.** An existing `backlog.json` migrates lazily on the first `moai todo` command against that root, single-flight under the lock the queue already had (`backlog.lock`), so concurrent sessions wait rather than race. The order is what makes it safe: the JSON is read in full (a file that will not parse stops there — nothing is repaired, rewritten, or guessed at), the database is written in one transaction, it is read back and compared to the JSON **field by field** across every card, every finding, in order, plus the id high-water mark — and only if that parity comparison passes is the JSON quarantined by rename to `backlog.json.migrated`. Because the comparison precedes the authority flip, a move that would have lost anything leaves the JSON in place and authoritative. Home-side fallback roots (`~/.moai/todo/<key>/…`) migrate the same way on their own first open; no scanner sweeps them.
  - **The rename carries a fallback read rather than a cutover.** Where only the legacy `.moai/state/kanban/` directory exists it is relocated once; where relocation is refused it is served in place, and where both directories exist the current one wins and the legacy one is left untouched. The per-session registry files that share the directory travel with it. The web console's SSE event key stays `"kanban"` — a frontend-visible contract — and only the watched path moves.
  - **A defect the export surfaced, and closed.** A database sitting beside a `backlog.json` is ambiguous: it is either pre-cutover legacy stranded by a crash between the migration commit and the quarantine, or a downgrade export the operator has just deliberately made. Quarantining unconditionally meant `moai todo export-json` followed by *any* `todo` verb renamed the operator's only downgrade artifact to `.migrated` — a loss they would not have discovered until the older binary came up to an empty queue. Closed by recording an in-flight marker in `meta` as the migration's last outstanding step, cleared once the rename lands: only a set marker means "finish this quarantine", and an export never sets one.
  - **`moai todo export-json`** is the one additive verb — it writes the live database out as a valid legacy-format `backlog.json` at the same root, so a downgraded earlier release resumes full-fidelity service. Every pre-existing verb keeps identical flags, stdout rendering and exit codes across the swap, asserted by a frozen-surface guard read from the live cobra tree (not from source text, so it also catches a flag registered elsewhere or a default quietly changed) which requires `export-json` to be the *only* addition. A storage-engine config knob was deliberately rejected: two always-live engines reintroduce the silent-divergence class this SPEC exists to close.
  - Verification: `internal/kanban` 87.7% coverage against an 85% floor, race-clean under `go test -race`, `golangci-lint` 0 issues, `GOOS=windows` and `GOOS=linux` vet clean, and `internal/{kanban,statusline,web,cli}` plus 16 cli sub-packages green on the merged tree. Every safety mechanism in the migration and rename state machines was proven by mutation — the guard was mutated, the test seen RED, the mutation reverted — after two guards in this SPEC were found to be passing vacuously, asserting mechanisms they were not exercising. The downgrade route and every artifact in the queue directory are documented at `.moai/docs/todo-queue-storage.md` (template-mirrored).
  - This sync commit carries the `spec.md` frontmatter close (`in-progress → completed`, version `0.1.0 → 0.1.1`), the `progress.md` §E.4 signal, the docs-site 4-locale correction of the queue path and storage form plus the new `export-json` row, and this entry; `sync_commit_sha` is recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI

- **[SPEC-CODEX-LAUNCHER-001](.moai/specs/SPEC-CODEX-LAUNCHER-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **A `moai codex` launcher joins `cc` / `glm` / `cg` in the launch group: the bare form (or `status`) prints a fixed six-row readiness readout — codex binary, `CODEX_HOME`, auth, project wiring, generated agent TOMLs, harness entry — and never launches anything; launching takes an explicit verb (`cli` runs the Codex CLI at the project root with arguments after `--` passed through verbatim, `app` delegates to `codex app`), and `--spawn` opens the launch in a new tmux window under the same contract as `moai cc --spawn`.** The auth row stops lying: a two-stage ladder reads `<CODEX_HOME>/auth.json` first (`auth_mode` plus a non-empty credential string for that mode — any other JSON type counts as absent, and the deserializing types never retain a credential value) and falls back to a whole-line-grammar command probe, fixing the stream misread that always reported `unknown` — applied once in the shared probe so every consumer (`codex_setup` MCP tool, web console Codex card, this launcher) reports the same answer. Run-phase commits: M1 `77d879b58` → M2 `f0b3826a3` → M3 `0f897ddbb` → M4 `921fff602`. 16 acceptance criteria, counted against `acceptance.md`.
  - Cross-platform by construction: launch is a child process whose exit code propagates — no `syscall.Exec` process replacement, no OS build tags (windows-amd64 compiles clean), and stdio is the parent's own streams so an interactive Codex session keeps the tty.
  - The launcher is read-only — it never mutates `.claude/settings.local.json`, profile state, or anything under `CODEX_HOME` — and its wiring row (file-set judgement: not wired / partial / invalid / wired, with the `moai init --agent codex` action on every incomplete state) is the single definition of Codex wiring completeness, consumed as-is by the follow-up init SPEC rather than restated.
  - This sync commit carries the README 4-locale command-table row, the `spec.md` frontmatter close (the only artifact of this SPEC carrying a frontmatter block), the `progress.md` §E.4 signal, and this entry. 🗿 MoAI

- **[SPEC-STAMP-REACHABILITY-001](.moai/specs/SPEC-STAMP-REACHABILITY-001/spec.md)** — CI pre-merge codemaps provenance reachability guard in the Graph Freshness workflow (`.github/workflows/graph-freshness.yml`): a new step placed after checkout and before build/bootstrap reads the tracked provenance stamp, verifies object presence in the checkout history, and on `pull_request` events enforces that the stamped commit is an ancestor of `GITHUB_BASE_REF` — failing the job with an attributable `::error::` annotation before any freshness verdict can be judged, so an orphan-bound stamp is caught at the merge gate rather than inheriting red into every later PR. A workflow run with no base-ref anchor skips the ancestry check via explicit skip-with-reason semantics; object-presence verification still runs on every event. Independent sync audit: PASS, harmonic mean 0.912 against threshold 0.85 (Functionality 0.95 / Security 0.90 / Craft 0.90 / Consistency 0.90), 10 findings all minor. This sync commit carries the spec frontmatter close (`in-progress → completed`), the progress §E.4 signal, and this entry; `sync_commit_sha` recorded as a `pending-backfill-sync` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **`moai graph stamp codemaps --commit <rev>`** — explicit-commit stamping mode so the operator names a merge-surviving revision instead of the unconditional-HEAD recording behind REQ-GFR-014's orphan-stamp hazard (card t291). The named revision resolves to a full sha and is recorded verbatim as `commit_sha`; unresolvable revisions are rejected resolve-first, and the dirty-tree combination is rejected pre-write (never rewriting provenance from a tree whose content does not match it); the flagless default path is behavior-preserved. The command Long text carries the operator recipe line for squash merges (`git merge-base origin/main HEAD`). 12 acceptance criteria (AC-SR-/AC-SP-001..012), counted against `acceptance.md`: all green under the binding audit above. Regression lock plus the 4-locale cli-reference documentation landed as M3 (card t291). Error-prefix unification fix (`ad33cc9f4`) renders user-visible failures as a uniform `graph stamp:` context instead of doubled wrappers. 🗿 MoAI
- **[SPEC-CI-FLAKE-SERIES-001](.moai/specs/SPEC-CI-FLAKE-SERIES-001/spec.md)** — three CI-only test-flake fixes on the ubuntu 2-vCPU shared runners (card t278, absorbing t270/t271), each landed RED-first with a mutant proof. (1) `TestConcurrentSendPoll` (internal/sessionmsg): the poller stop-rule treated a pre-completion empty observation as terminal — a TOCTOU that reported 97/100 exactly when a closed-channel 0/0 read raced the final delivery; the rule now re-polls once after senders are done, and only that re-poll's 0/0 terminates, with a `beforeStopCheck` handshake gate forcing the CI interleaving deterministically in the reproduction test (`def99739d`). (2) `TestConfigChange_RT005ReloadIntegration` (internal/hook): the single-sample wall-clock assertion (≤100ms) read a ~120ms scheduler preemption as a defect; rebuilt as a 20-sample nearest-rank p95 (19th of 20) with async drainage between samples — production code untouched (`5aedc1cd3`). (3) `TestAssertPairedHealthyEndToEnd` (internal/timing): the paired calibrated arm tripped on CI-load estimator divergence (per-round 2.47x vs ratio-of-medians 1.09x at a true 1.00x); the new pure helper `CheckRatioAnd` fails the calibrated arm only when BOTH the per-round median and the ratio-of-medians exceed MaxUnits, with a falsifier arm asserting the old single-estimator rule still trips on the same distribution (`324883ebb`). Alongside the fixes: `series-analysis.md` extracts the reusable authoring rule (positive condition · contract statistic · mutant proof), and `reproduction-rate.md` fixes the attempt-aware baseline — 537 ci.yml runs / 556 attempts in the 2026-08-10→08-26 window, 4 current-defect occurrences, pooled p̂=4/166≈0.0241 with the (1−p̂)^N power arithmetic ((1−p̂)^40≈0.377 — N≥40 is a floor, not proof). PR #1666 squash-merged `379b310a6` @ 2026-08-26T18:05:57Z (required checks 5/5). Independent sync-audit (this tree): **PASS-WITH-DEBT 0.942** harmonic (Functionality 0.95 / Security 0.95 / Craft 0.92 / Consistency 0.95), report `.moai/reports/t278/sync-audit.md`. 10 acceptance criteria (AC-CFS-001..010), counted against `acceptance.md`: 9 GREEN, 1 PENDING, 0 RED — AC-CFS-007(b) is the time-gated reproduction-rate verdict whose post-merge window opened at the merge timestamp: closure requires earliest 2026-09-02T18:05:57Z AND N≥40 `go_code=true` ci.yml runs with 0 recurrences of the 3 tests (ledger `.moai/reports/t278/reproduction-rate.md` §5; the final verdict must carry the power arithmetic per acceptance.md §D.1/§F — a bare "0 recurrences" is insufficient, and a recurrence ≥1 blocks GREEN and reopens that fix's mutant chain). The `completed` transition is deliberately deferred to a follow-up session after the window satisfies (operator decision, 2026-08-27): this sync commit carries `in-progress → implemented` only. `sync_commit_sha` recorded as `pending-backfill` and backfilled in the immediately following commit. 🗿 MoAI

- **[SPEC-TEAMMATE-REVIVAL-GUARD-001](.moai/specs/SPEC-TEAMMATE-REVIVAL-GUARD-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The stopped-teammate doctrine is now mechanically enforced: a per-session stop registry plus a PreToolUse SendMessage deny guard refuses — with a `STOPPED_TEAMMATE_VIOLATION:` sentinel-prefixed reason that names the stopped teammate and routes coordination through the owning orchestrator — any SendMessage whose recipient matches a live stop entry, closing the one-message-revival vector (`TaskStop` leaves the name addressable; one delivered message resumes the agent from its transcript as an ownerless writer — the t232 incident shape).** Recipient matching covers the bare name, the agent id, and the sanctioned `name [ref]` form (suffix parsed and stripped), and compares both registry fields — pinned from the measured live TaskStop payload shape, which carries the spawn name in the field mapped to `agent_id` with the display name empty. Recording rides the existing matcher-less PostToolUse dispatch (every TaskStop completion upserts `.moai/state/agent-stops/<session-id>.json` idempotently) and the deny rides one new PreToolUse matcher entry `SendMessage|TaskStop` on the existing wrapper — no new hook subcommands, no new wrapper scripts. Deliberate revival stays possible only through an explicit fresh spawn carrying the same name (the entry clears before the spawn proceeds — the sanctioned revival path), and session end removes the session's entries, so name reuse in later sessions is unaffected. Every stop, send, deny, and clear appends exactly one JSONL row to `.moai/logs/agent-stop-audit.jsonl` (UTC RFC3339 timestamp, session_id, kind, name, agent_id, decision) — the trail that makes a revival window provable after the fact; the correlation recipe ships as `.moai/docs/agent-stop-audit-correlation.md`. The deny layer is opt-in via `workflow.agent_stop_guard.enabled` (template default false — a kill switch consistent with BranchGuard/AgentModelGuard; this repository's local config enables it for dogfood): recording and audit run regardless of the flag's state, and the guard fails open on every uncertainty (malformed input, absent recipient, unreadable registry, ambiguous match never deny). The `cross-session-messaging.md` rule twins (local + template mirror, neutral text) gain the mechanism-layer note and an anti-pattern footnote. 11 acceptance criteria (AC-TRG-001..011), counted against `acceptance.md`: 11 PASS, 0 FAIL. Run-phase commits M1 `7759b8130` (observe+record layer) → M2 `f7bd5bdc7` (enforcement + config-cache schema bump) → `70541af5c` (coverage closure) → M3 `aac630f25` + `08c345cb4` + `a1a908802` (doctrine-pointer proposal, correlation recipe, §E evidence); live firing proven end-to-end in fresh PATH-pinned headless sessions (deny from both lead- and teammate-issued sends — the incident vector, same-name respawn clear, post-respawn delivery acknowledged). This sync commit carries the `spec.md` frontmatter close (`in-progress → completed`, merged into the sync commit per the 3-phase close), the `progress.md` §E.4 signal, and this entry — markdown/rules-only, zero Go changes at sync. `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-LEAD-DEBOTTLENECK-001](.moai/specs/SPEC-LEAD-DEBOTTLENECK-001/spec.md)** — lead-session serial-turn bottleneck moved to a background deputy (card t283). The measured bottleneck: all `-k`/`-f` lead coordination routed through manager-lead subagents whose `tools:` allowlist lacked `SendMessage`/`ListAgents`, so every dispatch send, CI watch, and CodeRabbit poll occupied the lead session's own serial turn (31 coordination acts over 2026-08-25~26). **manager-lead deputy charter**: `tools:` gains `SendMessage, ListAgents` (still one CSV string; manager-lead remains the sole `Agent` carrier — depth-seal tests green, `0.550s` vs baseline `3.305s`), and a new "Deputy dispatch surface" section codifies the permission matrix — 5 delegable duties (dispatch sending with delivery-shape verification / bounded CI-watch polls / CodeRabbit two-condition reads, report-only / first-pass evidence reads returning `RECOMMEND:`-prefixed findings / summary reporting) and 6 retained powers under a grep-able `DEPUTY-RETAINED-BY-LEAD` marker (final merge approval `LEAD-MERGE-APPROVED` / final `FINAL VERDICT:` / operator gates / `moai todo` queue mutations / CodeRabbit slot-wait adjudication / cross-session dispute coordination), plus the lost-dispatch protocol (a `routing` object on a send result = lost delivery, re-send to `name [ref]`) and the stopped-teammate revival ban inherited from SPEC-TEAMMATE-REVIVAL-SOLE-WRITER-001. **kanban-dispatch deputy doctrine**: stub gains a "Deputy dispatch surface" section with 2 NEW [HARD] boundary clauses ("The deputy never holds a power of consequence"; "Nothing structural moves with the delegation" — queue-on-disk stays the delegation channel, completion stays evidence-the-lead-read) at 30→32 [HARD] clauses, 0 removed; the detail companion gains the "Deputy mode" section. Distributed surfaces carry `internal/template/templates/` mirrors (agent mirror diverges by exactly the one pre-existing provenance line; rules mirrors identical; `make build` rc=0 with catalog.yaml manager-lead hash regen; template-neutrality greps 0 hits). **Verification**: runtime probe RED→GREEN across two cells including the definition-discovery constraint (old-definition discovery → tools absent → honest blocker report; worktree-discovered new definition → send succeeded with no `routing` object, tree @ `41ed86a95`); before/after lead-turn occupancy 31→0 in the recorded-form scenario (deputy writes confined to `.moai/reports|state/`; retained-power turns preserved: 2 merge approvals ≥ 2 merges, 0 AskUserQuestion/`moai todo`/`FINAL VERDICT:` on the deputy transcript); mutant RED-capability demonstrated per grep criterion (mutant lines caught 1/1, compliant `RECOMMEND:` control not counted); whole-branch Go diff 0 (`internal/`/`pkg/`/`cmd/` untouched — agent+doctrine-layer construct only). 13 acceptance criteria (AC-LDB-001..013): 9 PASS, 0 FAIL, 4 PASS-WITH-DEBT (AC-009/010/012/013 — measured on the recorded-log scenario form; raw-transcript-grade live board-run confirmation deferred to post-merge use, when agent-definition discovery can resolve against the merged primary). Run-phase commits M1 `9ff2e1ac2` → M2 `41ed86a95` → M3 `e7a3b2953`. This sync commit carries the 3-phase close (`in-progress → implemented → completed` merged into the sync commit), the `progress.md` §E.4 signal, the §E.3 `run_commit_sha` backfill, and this entry; `sync_commit_sha` recorded as `pending-backfill-sync` and backfilled in the immediately following commit. 🗿 MoAI

- **[SPEC-DOCS-V313-CATCHUP-001](.moai/specs/SPEC-DOCS-V313-CATCHUP-001/spec.md)** — v3.1.3 release documentation catch-up (card t274, Class C docs-only: zero Go/template/hook surface, AC-DVC-006 diff 0). The 26-item CHANGELOG [3.1.3]-to-docs gap table is fully closed: 11 updated items (moai-feedback, moai-todo, config-sections, model-policy, profile-matrix, multi-model-audit, skill-guide, update, init-wizard, faq, statusline, claude-cloud pages refreshed across ko/en/ja/zh), V1-V8 version-SSOT updates (hugo.toml → v3.1.3 / 2026-08-24, README badges and version tokens aligned — version-sync gate now green), 4 new items landed as the new page `docs-site/content/{ko,en,ja,zh}/advanced/codex-dual-harness.md` with a matching `docs-site/data/menu/main.yaml` nav entry (operator-approved), 3 documented, 8 not-applicable. The 4-locale same-PR obligation is fulfilled (all locale changes shipped in this change set), and README×4 heading parity holds (12 H2 sections each). Verification is the hns-oss-docs-verify 8-axis exit gate (warning-free hugo build, sitemap, URL blacklist, Mermaid direction, 4-locale parity, README parity, body-emoji scan, version sync), all PASS — re-verified on the post-merge tree. 9 acceptance criteria (AC-DVC-001..009): 9 PASS, 0 FAIL. Run-phase commits M1 `47270bd72` → M5 `bed33bbde`, merge `0044c7a83`. This sync commit carries the 3-phase close (`in-progress → completed` merged into the sync commit), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` recorded as `pending-backfill` and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-TEAMMATE-REVIVAL-SOLE-WRITER-001](.moai/specs/SPEC-TEAMMATE-REVIVAL-SOLE-WRITER-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Two always-loaded rules codify the t232 sync-audit F4 process-debt doctrine layer: (1) `cross-session-messaging.md` bans addressing a TaskStop-ped teammate by name — the name stays resolvable after a stop, and one `SendMessage` resumes the agent from its transcript (the t232 rogue-execution vector); stand-down coordination routes through the owning orchestrator/lead instead, and a matching anti-pattern entry makes the shape greppable. (2) `agent-common-protocol.md` §Background Agent Execution extends the concurrency guard to an audit-window sole-writer rule — a worktree under an active audit has exactly one writer, and a prior auditor session's own report/script commits defer until the audit closes — plus a foreign-commit/revival surfacing duty: report to the lead as a process defect, never quietly continue.** Both clauses are authored identical+neutral on the local and template twins (the ACP twins are byte-identical; `cross-session-messaging.md` keeps only its pre-existing deliberate Origin-line diff). Doctrine-only change: no code, CLI behavior, README, or docs-site surface is touched. 6 acceptance criteria (AC-TRSW-001..006), counted against `spec.md` §3 (Tier S inline — no separate `acceptance.md`): 6 PASS, 0 FAIL. Run-phase commits M1 `d12758749` (rule clauses + template twins + `draft → in-progress` transition) → M2 `8e4830bc4` (§E.2/§E.3 evidence). Registry guard stays `ok` (101 entries, digest unchanged). This sync commit carries the `spec.md` frontmatter close (`in-progress → completed`, merged into the sync commit per the 3-phase close), the `progress.md` §E.4 signal, and this entry — markdown-only. `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **`moai graph check` measures, per layer, how far the graph surfaces have drifted behind the
  code they describe — and fails on it.** Three layers are gated, each by its own metric:
  codemaps by a described-source diff against the stamped generation commit (an endpoint diff,
  so churn that reverts counts zero), the @MX index by an inventory content diff, and
  edges.jsonl by source-fingerprint mismatch. Every generated artifact now carries a provenance
  block naming the tree root and commit (or dirty content fingerprint) it describes; an
  artifact without one reports `absent` — unjudgeable, never silently fresh — and absent is a
  distinct, failing verdict, because a fresh worktree holds none of these untracked artifacts
  at all. No filesystem mtime is read anywhere: a fresh checkout resets every mtime, which an
  mtime metric would misread as freshly regenerated. Exit codes are a contract — 0 all fresh,
  1 stale or absent, 2 system error — consumed by a new advisory step in the pre-commit quality
  gate (every posture emits a distinguishable notice: fresh, stale, disabled, unconfigured;
  blocking is opt-in) and by a new `graph-freshness` CI job that bootstraps the untracked
  layers to the PR head before running the check. Thresholds live in gate.yaml
  `graph_freshness` (40 described files / 1 index file by default, calibrated from this
  repository's own churn history). `moai graph stamp codemaps` records the codemaps provenance
  anchor as the last step of a regeneration, and this change regenerates the repository's own
  codemaps to current HEAD and stamps them — the first state the gate judges is fresh.
- **`moai graph build` derives code-level edges on top of the document-level ones.** Call and
  import edges are extracted from the source tree through the existing tree-sitter substrate,
  now consumed outside the navigator for the first time via a dedicated `internal/graph/symbol`
  seam that pulls no navigator-tier dependency. The layers are additive — every document edge
  survives unchanged — and import targets are normalized to repository-local package paths
  (the go.mod module prefix stripped) so code imports and the codemaps import graph speak the
  same package domain. A 16-language grade matrix publishes, as data, how each language's
  calls were resolved (name-based for the six languages with call captures today, `none` for
  the rest — no empty cells, and a build reports any that appear). Where the two layers
  disagree about the same relationship, neither edge is dropped: the doc edge stays with a
  `disagrees_with` marker naming the code layer's contrary observation, and
  `--all-disagreements` revives the suppressed direction — code-found/doc-silent local imports
  — so a decided-not-to-report signal stays retrievable instead of hardening into
  cannot-be-reported.
- **Three code-query MCP tools: `graph_find_code`, `graph_trace_calls`, `graph_file_api`** (the
  self-hosted server grows from 25 to 28 tools, all read-only-hinted). Signature-level answers
  from the code-derived layer — a file's exported declarations with full signatures and no
  bodies, a symbol's call sites with their resolution grades, a call-graph traversal in both
  directions — and every answer names the tree root and commit it was computed from, so a
  worktree session cannot mistake the primary checkout's answer for its own.
- **Citations anchor by content, not line number (mechanical surfaces).** The canonical citation
  form is a verbatim excerpt plus the hash of the cited region, with the line number carried as
  convenience only. Landed in the mechanical writers: @MX tags stamp the hash of their own
  source line — a lookup keyed by file and hash still resolves after lines are inserted above
  it — and `graph.Citation` + `ResolveCitation` implement the canon for code surfaces, resolving
  the same citation in two trees that carry the region at different physical lines (a
  line-anchored resolver returns the same number in both and points at the wrong place in one).
  Codemaps markdown body citations are LLM-written by `/moai codemaps`; the canon, renderer, and
  `moai graph stamp codemaps` are provided for that pipeline, whose adoption rides its next
  regeneration — the md bodies themselves are not yet excerpt-plus-hash.

- **[SPEC-LSEL-DRAIN-STALL-001](.moai/specs/SPEC-LSEL-DRAIN-STALL-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The LSEL lessons-inbox drain has a durable mechanical trigger: a new `session_drain.sh` wrapper (`.claude/skills/hns-lsel-curator/`) runs the drain on every Claude session start — mkdir-based atomic lock (contention skips with exit 0, a stale holder is reaped after 120s), an unconditional archive of any staged `clusters.json` to `clusters-history/` BEFORE the overwrite (drain.sh clobbers candidates on both the drain and the no-op path), the frozen `drain.sh` core invoked unchanged, a one-line status on stderr, and fail-open on every internal error (an EXIT trap forces exit 0 — the hook can never block session start).** The lock is mkdir rather than flock on purpose: a missing flock binary at hook time is indistinguishable from permanent contention and would silently recreate the stall. A fixture test (`session_drain_test.sh`) covers 5 paths plus a mutant probe that proves the verification predicate rejects an offset-only-advance fake (a check whose red has never been seen proves nothing). The 21-day stalled backlog is bulk-drained through the wrapper: offset 629→4229, 3,600 stubs read, 3,131 discarded as noise (86.97%, matching the plan dry-run prediction), 12 candidate clusters emitted; the Aug-4 original (16 candidates) and the bulk result are both preserved in `clusters-history/` before their respective overwrites. Truthfulness fixes: the `/loop` recipe `lsel-drain-loop.js` no longer claims to run the drain (it only ever printed commands — the false header claim masked the 3-week stall; it is now labeled a read-only reminder, and the wrapper is named as the durable trigger), and the dead `§28` anchors in `backlog_check.sh` are replaced by an actual durable-operations section, with the real `CLAUDE.local.md §28` restored as a maintainer-machine local deliverable. The SessionStart wiring itself (settings.local.json hook entries + CLAUDE.local.md §28) is applied on the maintainer machine only and is deliberately NOT carried by this PR — a tracked settings.json entry would be wiped by every `moai update` (REQ-LDS-009); application steps are documented in spec.md §E. 12 acceptance criteria (AC-LDS-001..011 MUST + AC-LDS-012 SHOULD), counted against `acceptance.md`: 12 PASS, 0 FAIL. Run-phase commits M1 `c45b19c54` (wrapper + test + hygiene) → M2 `29156eef7` (bulk drain + local-deliverable application + evidence); PRESERVE 5/5 clean (template tree, frozen drain core, `internal/harness/`, `.moai/config`, inbox head-629 sha invariant). This sync commit carries the `spec.md` frontmatter close, the `progress.md` §E.4 signal + §E.3 SHA backfill, and this entry — zero code changes (sync is markdown-only). 🗿 MoAI
- **[SPEC-V3R6-WORKFLOW-DOCS-001](.moai/specs/SPEC-V3R6-WORKFLOW-DOCS-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **docs-site now documents the operating models that previously existed only in internal rules: a dedicated Factory Mode page (`advanced/factory-mode`) and an integrated SPEC Lifecycle page (`core-concepts/spec-lifecycle`), in all four locales (ko/en/ja/zh); a card-class A/B/C section on the Kanban Mode page; the card-class table in the README 4-file set; and navigation registration in 8 `_meta.yaml` files plus `data/menu/main.yaml` (4-key locale name maps).** Measured content surface: 29 files, +1051/−128 — 8 new pages (ko canonical factory-mode 10,751 B and spec-lifecycle 8,900 B, en/ja/zh derived), kanban-mode.md card-class section ×4 (Class A/B/C protocol tokens parenthesized in ko/ja/zh intros), spec-based-dev.md → spec-lifecycle reverse links ×4, README card-class table ×4, `_meta.yaml` ×8 (each new key adjacent to its anchor in all 4 locales), `main.yaml` entries + 4-key name maps. Run-phase commits M1 `4ffa6ff96` (ko) → M2 `aaad6fcb6` (en) → M3 `e5c79ecad` (ja) / `a6564a24f` (zh) → M4 `197f5a65d` (nav) → `d6927f855` (AC anchor fixes); final tree `d6927f855`. 11 acceptance criteria (AC-WFD-001..011), counted against `acceptance.md`: 11 PASS, 0 FAIL. Regression gates on the final tree (hns-oss-docs-verify FULL recipe): hugo build warning-free (0 WARN/ERROR, sitemap OK), URL blacklist 0, Mermaid LR/RL 0, body-emoji 0 on the 8 new pages, locale parity 152 files per locale ×4 with the ratchet clean (54 pre-existing baseline, no new divergence), README H2 parity 12/12/12/12. This sync commit carries the `spec.md` frontmatter `in-progress → implemented` transition, the `progress.md` §E.4 audit-ready signal, and this entry — zero docs-site content changes (all pages landed in the run-phase commits). The branch is unpushed, so no CI verdict exists yet; the PR is manager-git's. `sync_commit_sha` is recorded as a `pending-backfill-SPEC-V3R6-WORKFLOW-DOCS-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3) and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-ZONE-REGISTRY-HARDEN-001](.moai/specs/SPEC-ZONE-REGISTRY-HARDEN-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The zone-registry sync guard now pins a SHA-256 digest over the sorted `(id, zone, zone_class, canary_gate)` tuple lines of all 101 registry entries, asserted per mirror alongside the existing entry-count pin, so a mutation that preserves the entry count while substituting an ID or a zone/zone_class/canary_gate value no longer escapes the guard; the failure message prints the computed digest and the same-change update procedure (`update wantRegistryEntries and wantTupleDigest in the same change`).** The count-preserving escape was first observed RED on a scratch mutant (ID substitution passing the then-current guard with `DriftCount=0`), then closed by `registryTupleDigest` + four substitution subtests (TDD, RED→GREEN). Alongside the guard, the CONST-V3R5-010 registry clause — previously truncated mid-predicate at a line-wrap boundary so the stored text read a MUST NOT prohibition as a MUST obligation — is re-selected as the self-contained sentence `The orchestrator MUST immediately escalate via AskUserQuestion with the diagnosis report.`, enabled by a formatting-only rewrap of the pinned source paragraph in `ci-autofix-protocol.md` (whitespace-normalized content byte-identical to base `db1362739`; local twin and template mirror `cmp` rc=0). The closed SPEC-ZONE-REGISTRY-RESYNC-001 `plan.md` literal-check paragraph is corrected by erratum to the implemented line-count semantics (`grep -F -c` equivalent — number of LINES containing the clause), replacing the occurrence-semantics `strings.Count` description, with provenance naming this SPEC. All pre-existing guard contracts hold unchanged: validator drift 0, literal buckets once=97 / zero=0 / multi=0, retired_exempt=4, anchor checks 101/101, mirrors byte-identical. 9 acceptance criteria (AC-ZRH-001..009), counted against `acceptance.md`: 9 PASS, 0 FAIL (1 PASS-WITH-DEBT — AC-ZRH-008 gofmt axis, pre-existing baseline skew on untouched files). Run-phase commits M1 `918840f61` (rewrap + clause re-selection) → M2 `9eafd81f4` (tuple digest, TDD) → M3 `c31c3613d` (plan.md erratum, manager-spec); coverage `internal/constitution` 85.8%, `golangci-lint` 0 issues, windows-amd64 `go vet` exit-0. Internal-guard + registry-data change only: no public surface (CLI behavior, README, docs-site) is touched. This sync commit carries the `spec.md` frontmatter close (the only artifact with a frontmatter block), the `progress.md` §E.4 signal, and this entry — zero code changes (sync is markdown-only). `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-VERIFICATION-COMPLETENESS-001](.moai/specs/SPEC-VERIFICATION-COMPLETENESS-001/spec.md)** — new distributed rule `.claude/rules/moai/development/verification-completeness.md` (path-scoped, so it loads only when harness/rule files are edited): verification steps must be run to their completion axis and named verbatim — the command, the observed output, and the scope actually scanned — before a check may be reported as passed. Six harness rules encode the recurring silent-failure shapes (truncated output read as a pass, zero-match selectors reading as green, absence claims without a named scan scope), each with its originating defect pattern inlined neutrally. Ships byte-identical in the template mirror; the always-loaded context budget is unchanged (0-byte delta) because the rule is path-gated. 9 acceptance criteria, counted against `acceptance.md`: 9 PASS, 0 FAIL.
- **[SPEC-PRECOMMIT-PRESERVE-001](.moai/specs/SPEC-PRECOMMIT-PRESERVE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`moai init` / `moai update` no longer silently discard a locally patched pre-commit hook: the installer classifies an existing MoAI-marker hook by provenance (a SHA-256 sidecar at `.git/hooks/.moai-pre-commit.sha256`, written on every install), and a hook the user edited is copied aside to `.git/hooks/pre-commit.bak.<UTC stamp>` before the replacement lands, with the backup path and the replacement notice printed on stderr at both call sites.** The stamp is a colon-free UTC form so the backup is creatable on Windows too, and an occupied backup name gets a distinct sibling suffix rather than clobbering (O_EXCL-guaranteed). Attribution is three-way — installed vs. recorded digest vs. incoming — because a two-way comparison cannot tell a user patch from an upstream version bump and would warn every user on every release that touches the hook; unmodified hooks therefore stay silent on version bumps. A legacy install with no record degrades to the deliberately noisy direction (any difference reads as a user edit). A failed backup write leaves the hook exactly as found with a warning — overwriting without a recoverable copy is the data loss this SPEC exists to prevent — and a failed provenance write is non-fatal, self-healing on the next run. 12 acceptance criteria (AC-PCP-001..010, 013, 014; 011/012/015 retired at plan audit), counted against `acceptance.md`: 12 PASS, 0 FAIL. Run-phase commits M1 `f71f63d1b` (three-way classifier) → M2 `d5c706e25` (backup, disclosure, notice); evidence `ceae6b905`/`d48e17aea`; merge `6d9f584c7`. 35 PreCommit tests PASS (`-count=1`), full `internal/cli` package `ok`, host and `GOOS=windows GOARCH=amd64` builds exit 0, `golangci-lint` `0 issues.` The hook body itself is untouched — `preCommitHookContent` and its template twin stay byte-identical (scope gate `cmp` rc 0), with exactly one §C.4-permitted caller line per call site. This sync commit carries the `spec.md` frontmatter close, the `progress.md` §E.4 signal, and this entry — zero Go changes (sync is markdown-only). `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI

- **[SPEC-CODEX-WIRING-001](.moai/specs/SPEC-CODEX-WIRING-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`moai init --agent claude|codex|both` selects the harness at init time; a new `internal/codexwiring` package generates `.codex/hooks.json` and the `config.toml` wiring (trust guidance, `mcp_servers`, approval mode); `moai hook --harness codex` runs hooks in Codex runtime mode; and `moai doctor` gains a "Codex Wiring" diagnostic** that detects wiring divergence and self-heals a lost sidecar baseline. Run-phase commits: M1 `90439c59c` → M2 `30c1387c4` → M3 `20ec045c3` → M4 `09d34fcc0` → M5 `fdf2a0b96`. 14 acceptance criteria, counted against `acceptance.md`.
  - The default `tui.status_line` ships `model-with-reasoning`, `context-remaining`, `git-branch`, `current-dir`, and `thread-id`. Codex supports only built-in identifier arrays — no command-backed statusline — so MoAI-specific statusline items (goal/todo/SPEC state) are impossible until openai/codex#17827 resolves; this limitation is documented in all four READMEs (AC-CW-014, SHOULD — discharged in this sync phase).
  - An MCP annotation defect surfaced in plan audit is fixed alongside: the 4 audit-family tools (`audit_cache`, `codex_audit`, `glm_audit`, `audit_multi`) lacked the read-only hint their catalog classification declares; they are now correctly read-only-hinted, and the effective approval set drops from 10 to 6 on the base tree.
  - Run-phase e2e discovered two placement defects, both fixed in place with RED→GREEN regression tests: the update-path wiring refresh sat after the `syncSkipped` early return (an "Up to date" update never refreshed wiring), and a force-reinit silently lost the doctor's sidecar baseline (now re-recorded on disk-match).
  - PRESERVE clean: template tree and `internal/codexadapter/` empty diff; `internal/cli/mcp_server.go` took exactly 14 pure insertions (the 4 annotation lines + comments). Coverage 87.2% (`internal/codexwiring`, threshold 85), `golangci-lint` 0 issues, windows-amd64 `go vet` exit-0. This sync commit carries the README 4-locale statusline-limitation sentence, the `spec.md` frontmatter close (the only one of the four artifacts carrying a frontmatter block), the `progress.md` §E.4 signal, and this entry. 🗿 MoAI
- **`moai worktree clean --stale --json`** reports every non-protected registered worktree — path,
  branch, keep reason, and the four predicates behind that reason (dirty, merge, anchor, and
  ignored content) — and removes
  nothing. It overrides `--yes` rather than combining with it: an inventory that could delete on a
  stray flag is not an inventory. The report and the sweep run the same classification, so the
  report can never describe a tree the sweep would treat differently. It also reaches the worktrees
  the PR-merge sweep cannot see at all — that sweep matches a branch prefix, while this one
  enumerates by checkout.

- **Audit-backend model+effort pinning via `workflow.audit`.** The cross-model audit backends
  (`codex_audit`, `glm_audit`, `audit_multi`) never controlled what they ran on: the codex leg
  resolved the SSOT sync-auditor cell, dropped it at the codex-servability prefix filter, and
  shipped no model — leaving `~/.codex/config.toml` to decide; the GLM leg resolved a model id but
  had no field to carry an effort. `workflow.audit.codex` and `workflow.audit.glm` now take
  `{model, effort}` pairs (pin > SSOT cell > legacy fallback), reaching both codex transmission
  seams (`thread/start` model, `turn/start` model+effort) and the z.ai request body — where live
  measurement selected the top-level `reasoning_effort` field as the one the endpoint honors (the
  thinking-budget object measured a true null). The pin is audit-scoped — `codex_task` and
  `glm_task` resolutions are unchanged — and the template ships empty defaults, so a project that
  never writes a pin sees nothing. GLM effort accepts exactly the z.ai state names `low`/`high`/
  `max`; any other value drops the reasoning directive while the model pin still applies.
- **The web console's Audit panel exposes the four pin fields** with typed inputs, a glm-effort
  select limited to `{low, high, max}`, runtime-applied help text, and labels in all four console
  locales.

- Claude and Codex sessions can now message each other directly, through the `moai` MCP server acting as a broker. Four new MCP tools — `session_msg_register`, `session_msg_list`, `session_msg_send`, `session_msg_poll` — let an agent register a mailbox, discover registered peers, deliver a message, and claim its inbox. Messages carry an A2A-aligned envelope (sender, recipient, text, structured `data`, context/task correlation, expiry) and live in a file store under `.moai/state/session-msg/`, so two processes that share nothing but the checkout can still find each other. The core lives in `internal/sessionmsg/`; the tool surface in `internal/cli/mcp_session_msg.go` is a thin wrapper over it, and the tool count in `internal/mcp/catalog.go` grows from 21 to 25. Two caveats worth knowing before relying on it: the new tools only appear after the MCP server is restarted (a running server keeps its old tool list — see `t184`), and on codex-cli 0.147 the `session_msg_send` `data` argument is leniently coerced rather than strictly validated. Sending to an unregistered agent returns a structured error carrying the known-agent list, so the caller can enumerate peers instead of parsing error text. Real-process round-trip between a `moai mcp-server` and a `codex exec` session is covered by an e2e marker test (`session-msg-e2e`).
- `moai mcp-server` now advertises its full build stamp (version, commit, build date) in the `initialize` result instead of the bare semver, and its `instructions` field names the running build, its pid, and the fact that a rebuilt binary does not replace an already-spawned server.
- `moai doctor` gains an **MCP Server Version** check. The server records its build identity to `.moai/state/mcp-server/<pid>.json` while it runs; the check probes those records for liveness and warns when a live server's commit differs from the installed binary's, with reconnect guidance. Dead records are pruned in passing. The check reports OK — never a warning — when no server is running or when the binary carries no commit metadata.

### Changed

- **[SPEC-SYNC-STRATEGY-KEY-001](.moai/specs/SPEC-SYNC-STRATEGY-KEY-001/spec.md)** — sync-phase close, `in-progress → implemented` (card t303). **The sync-delivery workflow's git-strategy detection reads one canonical key, `git_strategy.{mode}.workflow` (mode resolved from `git_strategy.mode`), and no longer primaries the retired `github.spec_git_workflow`.** The dead key survives in exactly one place — a documented, deprecated fallback in `delivery.md` Step 3.0 (removal version v3.3.0) — everywhere else it is gone (`grep -rn 'spec_git_workflow' internal/template/templates` → 1 hit, the fallback sentinel itself; refined baseline was 10). The value axis is cleaned to the two-member domain `{github-flow, git-flow}`, an unmatched branch value now stops delivery explicitly (no PR, no push, no silent default) rather than falling through, and `WT-*` lane branches route through the git-flow no-ff merge procedure instead of the `gh pr` path used by main-direct/feature-branch delivery. REQ-SYK-010 additionally rebinds the project-interview auto-branch question from the dead path to `git_strategy.{mode}.automation.auto_branch` in `tab_schema.json` (3 hits, template↔local mirror byte-identical). 12 acceptance criteria (AC-SYK-001..012): 12 PASS, 0 FAIL. Run-phase commits M1-M5 `c374d9605` → `63b4628a6`, merged to develop as `7ed6edb3e`. Sync-phase re-verification on the merged tree confirmed two pieces of carried debt independently resolved by later cards rather than by this SPEC: card t316 (`6310dbf28`) deleted the two malformed `personal.auto_branch` / `team.auto_branch` bindings this SPEC's carried-debt item OBS-1 named, without touching this SPEC's own rebind (`git_strategy.{mode}.automation.auto_branch` still present, 3 hits); and card t308 (`da301bbe1`) removed `CLAUDE.local.md`'s instruction to reapply the non-canonical `workflow: gitflow` spelling (carried-debt item B5). Two items remain open, reported rather than resolved: the rebound SPEC-branching interview question now overlaps the per-mode `auto_branch` questions in the personal/team batches (OBS-2), and the template-neutrality CI guard triggers on `pull_request: branches: [main]` so it may not fire on CI under this repo's git-flow card branches — it was run locally instead, exit 0 (OBS-3). No docs-site changes: the dead key and the mode-scoped canonical path do not appear in `docs-site/`; existing references to `git_strategy.automation.auto_branch` predate and are unrelated to this SPEC's scope.
- **[SPEC-TABSCHEMA-AUTOBRANCH-001](.moai/specs/SPEC-TABSCHEMA-AUTOBRANCH-001/spec.md)** — the project-interview schema stops asking two questions that bound to nothing (card t316). Batches 3.3 (Personal Core Settings) and 3.6 (Team Core Settings) each carried an auto-branch question wired to `git_strategy.personal.auto_branch` / `git_strategy.team.auto_branch`; neither path exists. `ModeProfile` (`internal/config/types.go`) has no `auto_branch` field — it nests `Automation AutomationConfig`, whose `AutoBranch` carries `yaml:"auto_branch"` — so the real path is batch 3.10's `git_strategy.{mode}.automation.auto_branch`, and the two dead questions are **deleted rather than rebound**: batch 3.10 already asks the canonical question for every non-manual mode, so rebinding would have produced a duplicate. Both copies of `tab_schema.json` (`.claude/skills/moai-workflow-project/schemas/` and its `internal/template/templates/` mirror) are edited and remain byte-identical (`diff` rc 0), and `make build` regenerates the embedded asset — verified by scanning `bin/moai` itself: zero occurrences of either dead path, with the canonical string still present as the control that proves the scan reaches the embedded schema. Question count per mode is now exactly `personal=1 / team=1 / manual=0`, stated as exact integers so the criterion fails on over-deletion as readily as on under-deletion; the total `auto_branch` occurrence count per copy drops from 9 to 3 (batch 3.10's three sites). No Go production code changed; no consumer of the deleted paths exists anywhere in the tree (`internal/`, `pkg/`, `cmd/`, shell hooks, skill bodies, and every other template all return zero hits). Two out-of-scope facts are recorded rather than fixed: the schema's header counters were already wrong and the drift widens arithmetically (`total_settings: 60` vs computed 46), and manual-mode operators are now visibly never asked about auto-branching — behaviour unchanged, since their previous answer went to a dead path. Run-phase commit `914c4edf5`. Independent sync audit: **PASS**, weighted harmonic mean **95.3** against the Tier S threshold **75** (Functionality 98 / Security 96 / Craft 92 / Consistency 92; must-pass firewall cleared on both), 5 findings — 0 blocking, 2 minor closed by this commit, 3 informational; report `.moai/reports/t316/sync-audit.md`. 10 acceptance criteria (AC-TSA-001..008 plus the paired AC-TSA-005b / AC-TSA-007b), all independently re-measured GREEN by the auditor. This sync commit carries the 3-phase close (`in-progress → completed`), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` recorded as `pending-backfill-sync` and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-GLM-FLASH-DEFAULT-001](.moai/specs/SPEC-GLM-FLASH-DEFAULT-001/spec.md)** — the default coding model for GLM-backed sessions is now `glm-5.3-flash` (card t289). `DefaultGLM53Flash` is registered in `internal/config/defaults.go` and every Claude-tier model slot (high/medium/low/fable/haiku/sonnet/opus) resolves to it; `ValidGLMModels()` grows from four to five members, with `glm-5.3` kept explicitly selectable via `DefaultGLM53` so an explicit retarget can never silently drop it. Flash accepts `reasoning_effort: max` only, so the effort overlay (`internal/template/glm_effort_overlay.go`) carries a flash-specific branch on both the wire and display paths: under flash the `max` state is emitted for every Claude effort level, including low — emitting the true low state would send a wire value the model rejects. The statusline context-window table gains an explicit `"glm-5.3-flash": 1_000_000` entry (a divergence guard, not substring inheritance off `glm-5.3`). The web console offers the model in all four locales (`internal/web/assets/i18n.js` labels it "one-million-token context (default)" in en/ko/ja/zh, and the model-select widget carries the five-member set), and the README tier table plus docs-site pages name flash as the default. A boot smoke test (`TestGLMFlashDefaultEnvInjection`) pins the env-level injection. 13 acceptance criteria (AC-001..AC-013): 13 PASS, 0 FAIL. Run-phase commits M1–M6 `a5454a505` → `9e1bb9e3d`. This sync commit carries the 3-phase close (`in-progress → implemented → completed` merged into the sync commit), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` recorded as `pending-backfill` and backfilled in the immediately following commit. 🗿 MoAI
- Resolving a project's `.moai/state` no longer climbs past the project root. `moai state dump`, `moai state show-blocker`, `moai chain`, `moai tokens`, and `moai clean` stop at the directory that owns the project and fail there when it has no state directory, instead of continuing upward and succeeding somewhere else.
- `moai clean` deliberately does **not** read `CLAUDE_PROJECT_DIR`; it anchors on the directory it runs in. The read-and-append commands above do read it. The asymmetry is intentional: the variable does not reliably track a worktree-resident session, and a delete should not inherit its target from the environment — that is the same "a directory the caller never named" hazard arriving through a different door. To clean another project, run the command inside it.
- The chain event ledger has one location: `<project>/.moai/state/chain`. It previously landed in `.moai/chain` when `CLAUDE_PROJECT_DIR` was unset and in `.moai/state/chain` when it was set, so a single project could scatter its events across two files. A ledger sitting at the legacy `.moai/chain` is relocated once, automatically. Where both exist the legacy file is kept untouched and a warning naming its path is printed — the two are never merged. When a relocation cannot be completed, the command keeps using the legacy path and says so on stderr rather than losing events quietly.
- Every command that resolves a project's state now prints the project root it resolved, before acting on it; `moai clean` prints it before listing or deleting anything. Because `clean` anchors on the working directory while the readers honor `CLAUDE_PROJECT_DIR`, the two can point at different projects within one session — inspect one project, clean another. The printed line is what makes that visible.
- A directory holding a bare `.moai` with no `state` inside it now stops resolution and fails there. Previously the walk skipped such a directory and succeeded at an ancestor. This is deliberate: silently skipping a bare `.moai` is the same behavior that let the walk skip past `~/.moai`, and repairing one without the other would leave the hazard in place. The failure message names the directory it stopped at.
- Resolved state paths are returned in symlink-resolved form. On macOS this changes the string a command prints for the same directory — `/private/var/...` rather than `/var/...`.
- `references/authoring.md` section 3 is restructured around eleven semantic roles, each carrying a light and a dark value. The dark column is derived rather than separately maintained: the `ink` and `surface` anchors swap, every other role keeps its distance from its anchor rather than its hex, opacities never change, and `accent` alone shifts one step lighter because the light value's contrast against ivory does not survive against near-black. A role added later inherits the rule instead of needing a judgement. The one-accent focal discipline survives the restructure — it was the thing most likely to be lost, so it is guarded by its own acceptance criterion.

- Eleven always-loaded documents were reduced to stubs, with their rationale, procedure, worked examples, incident records, and long cross-reference tables moved into eight new companion files that load only when the work touches them. Every obligation stayed where it was; what moved was the material explaining why it exists. The always-loaded surface fell by 8,555 tokens.
- A personal `~/.codex/AGENTS.md` joins the same merged chain and is consumed **before** a project's own contract, narrowing what that contract can carry. The shipped documentation now says so, because the effect is otherwise invisible until something is already missing.
- **[SPEC-GLM-EFFORT-MAX-001](.moai/specs/SPEC-GLM-EFFORT-MAX-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The GLM reasoning-effort ceiling is raised to `max`: `CollapseClaudeEffortToGLM` now maps every Claude effort above `low` (`medium` / `high` / `xhigh` / `max`) onto reasoning `max` instead of stopping at `high`, and a GLM session without an explicit effort override now defaults to `max` (`SessionGLMReasoningState` + the `ANTHROPIC_REASONING_EFFORT` env the launcher injects).** `low` is unchanged (`reasoning_effort=low`, thinking enabled), so the collapse removes exactly one state — no Claude effort maps to `high` anymore. The unused `glmReasoningHigh` runtime variable is deleted while the `high` name stays reserved for stored-only config surfaces. The template-mirror `llm.yaml` doc block states the new mapping. The session-default change carries the REQ-GER-004 supersession note (recorded in spec.md §1.3): SPEC-GLM-EFFORT-REBALANCE-001's `high` session default is superseded by this SPEC (lead-ruled 2026-08-22 — a stalled draft, not living doctrine). The AC-MTP-032b residual of SPEC-MODEL-TIER-PLANTYPE-001 is closed by the t175 shim measurement — the z.ai Anthropic-compat shim honors the Anthropic `thinking` parameter (depth scales with budget) and ignores a top-level `reasoning_effort` field, so the env injection is the live reasoning channel this collapse rides on. 8 acceptance criteria (AC-GEM-001..008): 7 PASS, AC-GEM-008 DEFERRED to the lead (fresh-session env check needs a rebuilt binary + a post-change session — unit-level wire evidence `TestGLMReasoningEnvVars_SessionMax` PASS supplied; the lead appends the live-session result to the same §E.2 row at release-binary install time). Run-phase commits M1 `a0340aa22` (RED) → M2 `626b6775a` (GREEN) → M3 `eb11fbda9` (template-mirror doc) → M4 `9685b35ab` (evidence); not pushed (factory lane — integration is the lead's). Coverage per run-phase §E.2: `internal/template` 85.7%. This sync commit carries only the frontmatter `in-progress → completed` transition on `spec.md` (`status:`; `updated:` already current), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero code changes (sync is markdown-only). `sync_commit_sha` populated as a `pending-backfill-SPEC-GLM-EFFORT-MAX-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3) and backfilled in a follow-up commit. 🗿 MoAI
- **`internal/codexadapter` — Codex dual-harness M3 hook adapter, shipped as a library** (`SPEC-CODEX-HOOK-ADAPTER-001`). Groundwork for running MoAI hooks under codex-cli, not a live feature: nothing invokes the package yet — the wiring (the `--agent` config generator) is the follow-up card. What it carries: the complete eleven-event Codex-to-MoAI event table with six events adapted and recognized-but-unadapted ones refused with an error distinct from unknown event names; translation of the three output keys measured inert on codex 0.147.0 (`continue: false` rewritten to the `decision: block` shape with a non-empty default reason, `systemMessage` routed to `additionalContext`), with keys measured working passed through byte-identical — original bytes returned, never re-marshalled; discard diagnostics that record event, key, and length but never content to a sink, keeping stderr silent when the hook exited 2 so its own blocking reason passes through; per-event stderr classification (PreToolUse blocking reason, Stop continuation prompt, unmeasured classes annotated as such); and a config validator that rejects the `version` key in the `--json` failure stream and collects every unknown-key violation at three nesting levels rather than stopping at the first. The mappings encode plan-phase measurements of codex 0.147.0 (`.moai/reports/t83/precondition-measurement.md`, `.moai/reports/t83/precondition-measurement-round3.md`), pinned by golden tests against the captured payload fixtures. `internal/hook` is untouched (REQ-7). All 7 acceptance criteria verified; 38 tests, coverage 87.1%, 0 lint issues, race-clean.
- **[SPEC-SESSION-TELEMETRY-001](.moai/specs/SPEC-SESSION-TELEMETRY-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The statusline's context-usage snapshot is now one record per session, and it carries the session's model and effort.** The producer wrote a single `<project>/.moai/state/context-usage.json`, so two sessions in the same project overwrote each other's observation on every render — the last writer won, and neither session could tell whose numbers it was reading. The record now lands at `<project>/.moai/state/context-usage/<session-id>.json` (`internal/statusline/context_usage.go`: `SessionTelemetryPath`, `contextUsageDirName`), keyed by the identifier the session runtime delivered to that render. Schema version is bumped to `2` and gains `model` / `effort`, both `omitempty` — a record written under the previous schema still reads, with the two fields simply absent, and an empty value is presented as "not recorded" rather than substituted.
  - **The key is refused, not sanitised.** `usableSessionKey` rejects an empty key, `.`, `..`, either path separator on every platform, and an absolute path; a refused key yields `""` and the caller writes nothing while its render still completes. Rewriting `../escape` into `escape` was the alternative, and it produces a file that looks legitimate and belongs to no session. Before the fix, a key of `../escape` wrote `state/escape.json`.
  - **The recorded model is backend-resolved.** A GLM-backed session records the z.ai model the session actually runs (`glm-5.3`), not the Claude display name the runtime supplied and not its `[1m]` suffix.
  - **Model and effort are inside the write throttle, deliberately, and the question was measured rather than assumed** (`plan.md` §F.1). Five renders with unchanged context, model, and effort leave the record's mtime untouched, so the widened payload does not defeat the throttle; a changed model or effort reaches disk on the very next render. Excluding them would have left the record holding a value that is present and wrong until an unrelated context value moved — a state a reader cannot distinguish from a current one.
  - **One exported reader.** `statusline.ReadSessionTelemetry` is the only exported entry point returning the record, and `moai tokens` migrated onto it: `internal/cli/tokens.go` no longer spells the on-disk schema or the path, and its test fixture is marshalled from `statusline.SessionTelemetryRecord` rather than restating the JSON. The duplicate schema spelling outside `internal/statusline` is gone (`grep -rln '"raw_pct"' internal/ | grep -v '^internal/statusline'` → `0`, from a baseline of 2).
  - **Doctrine and published docs moved with it**: both mirror pairs of `context-window-management.md` / `context-window-management-detail.md` (`.claude/rules/moai/workflow/` + the `internal/template/templates/` mirror, `diff -q` clean), and twelve docs-site pages across four locales — verified at sync, not rewritten (`grep -rln "context-usage.json" docs-site/content` → `0`; `context-usage/` → `12`, three per locale).
  - 11 acceptance criteria (AC-ST-001..011), counted against `acceptance.md`: 11 PASS, 0 FAIL, with AC-ST-002's grep half recorded PARTIAL at run-phase and re-authored under its owner in `e6b01f9b2` — the criterion's two halves were mutually unsatisfiable as first written (the fixture it mandates must create the sidecar file, and creating it spells the name the unscoped grep forbids), so the grep is now scoped to non-test source. Run-phase commits M1 `89ef8f8c1` → M2 `16d095eca` → M3 `3f7568abc` → M4 `3a7681104` → M5 `44f297ec9` → M6 `de0bd9674`, plus `3bb8f4393`; not pushed (factory lane — integration is the lead's). Coverage per run-phase §E.3: `internal/statusline` 90.5%, `internal/cli` 78.8%, `internal/spec` 89.1%; lint `0 issues.`; host and `GOOS=windows` builds both exit 0.
  - **Two things this SPEC does NOT claim.** No coverage-regression figure exists — the pre-change package rate was never measured, and the substitute measurement (every function this SPEC adds is at 100%, above the 90.5% package rate) is a different claim, recorded as a gap in `progress.md` §E.4 rather than as a pass. And whether a GLM-backed session's payload actually carries `effort` remains **unobserved**: no GLM session ran on this machine during the run, so nothing is inferred from the Claude-backend path that was observed.
  - This sync commit carries the frontmatter `in-progress → completed` transition on `spec.md` (the only one of the five artifacts carrying a frontmatter block), the `progress.md` §E.4 audit-ready signal, the §G ownership-correction discharge, and this entry — zero code changes (sync is markdown-only). The branch is unpushed, so no CI verdict exists yet. `sync_commit_sha` is a `pending-backfill-SPEC-SESSION-TELEMETRY-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` § SHA placeholder backfill exemption), backfilled in a follow-up commit. 🗿 MoAI
- **[SPEC-WEB-TODO-QUEUE-001](.moai/specs/SPEC-WEB-TODO-QUEUE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The backlog queue-root resolution moves out of the command layer into `internal/kanban`, split into a pure resolver and an adopting one, and the console gains a read-only `/todo` route.** One queue-root resolution existed only inside `internal/cli/todo.go`; a console that needed the same answer would have had to reimplement it, and a second implementation is a second chance to fork the queue — the measured failure being 30 queued cards on the primary checkout against "queue is empty" from a linked worktree. The resolution now lives in `internal/kanban/todo_root.go` and `internal/cli/todo.go` delegates to it (`grep -c 'ResolveGitDirs' internal/cli/todo.go` → `0`, from a measured baseline of `1`).
  - **The split is the point, and it is a split of side effects, not of interfaces.** `kanban.ResolveTodoQueueRoot` is **pure** — no `MkdirAll`, `Rename`, or `WriteFile` on any of its three branches — and it is what `internal/web` imports, so rendering a page can never migrate the operator's backlog. `kanban.ResolveTodoQueueRootAdopting` resolves and then adopts, and is the only entry point from which the adopt-not-shadow migration is reachable; it is what the `moai todo` command path calls, so that command's behaviour is unchanged. `kanban.HomeDirFn` is the package-local home seam the relocated fallback needs — `internal/cli`'s own seam could not travel with the code, so `init()` rewires it by reference and the existing CLI tests still control the fallback root.
  - **Splitting alone would have traded a write hazard for a read divergence, so it did not stop there.** In a non-git launch context the pure resolver would report an empty fallback queue while `moai todo` adopted first and reported N — the console and the command telling different stories about the same queue. The pure resolver therefore **reads through** to the project-local root when the fallback root holds no queue file and a project-local one does exist, still writing nothing. Its predicate is the mirror of the adoption path's own early returns, so the two agree by construction rather than by coincidence.
  - **`/todo` is a top-level route, not a panel on `/kanban`.** It lists **every card in all three states** (`queued` · `picked` · `dropped`) with id, text, a state badge and the SPEC id where one exists — nothing filtered, because the question the screen answers is "where did that card go", which a queued-only working view cannot answer, and a filter is a later addition over a complete list, never the reverse. An absent, empty or malformed queue file renders the empty state at 200 rather than an error page. The rail gains a sixth row (`/` `/kanban` `/specs` `/monitor` `/settings` `/todo`), an `iconAt` `todo` arm, and four locale strings — `grep -c '"nav.todo"' internal/web/assets/i18n.js` → `4` from a baseline of `0`, with no i18n allowlist entry added. No new SSE event: the section sits inside the existing `data-live="kanban"` marker, and `git diff f276b9742..HEAD -- internal/web/events.go internal/web/assets/app.js` is **empty**.
  - **The storage-migration seam is one function, deliberately.** The queue's storage was under operator review at the time of writing (a JSON file today; a sqlite store keyed by repo root was being considered, undecided) — **no sqlite code was written and no abstraction layer was introduced**. Instead the console's entire backlog read is isolated in `readTodoQueue` (`internal/web/todo_queue_read.go`), the only file in `internal/web` naming a backlog-store symbol; `buildTodo` (`internal/web/todo_view.go`) calls it and touches no store symbol itself, and `TestBacklogReadSeamIsSingleFile` (`internal/web/todo_queue_read_test.go`) asserts that single-file property mechanically. A storage change lands in that one function. There is no interface, factory, or plugin point: a single concrete reader needs no seam beyond its own signature.
  - **Read-only posture is asserted, not asserted-about.** `TestConsoleRoutesLeaveBacklogUntouched` exercises all six routes and observes the backlog bytes identical and `backlog.lock`'s mtime unchanged; the mutation-primitive scan over `internal/web` returns exactly the two pre-existing allowlisted lines in `profile_crud.go` and nothing else. One thing this does **not** claim: `kanban.NewBacklogStore` removes a superseded legacy lock artifact (`backlog.json.lock`) as a constructor side effect (`internal/kanban/backlog_store.go`), which the console's render path reaches. It touches neither `backlog.json` nor the live `backlog.lock`, so the criterion passes as written — but REQ-WTQ-001's prose is arguably broader than the criterion, and the store belongs to `SPEC-KANBAN-TODO-CLI-001`. Left as found, recorded rather than papered over.
  - 11 acceptance criteria (AC-WTQ-001..011), counted against `acceptance.md`: 11 PASS, 0 FAIL. Run-phase commits M1 `063a0a5c7` → M2 `d471057de` → M3 `acd529f1d`, plus the seam-isolation refactor `aef1b51f3`; not pushed (factory lane — integration is the lead's). Coverage per run-phase §E.3: `internal/web` 65.8%, `internal/kanban` 84.5%, `internal/cli` 78.8%; lint `0 issues.`; host and `GOOS=windows GOARCH=amd64` builds both exit 0.
  - **Documentation caught up with the sixth row**, which the published pages falsified: `advanced/moai-web-console.md` and `cli-reference/web.md` in all four locales (route table, area count, live-indicator count, both mermaid node labels, and a new section describing the screen), plus the three console mentions in each of the four READMEs. The docs-site build is warning-free.
  - This sync commit carries the frontmatter `in-progress → completed` transition on `spec.md` (the only one of the four artifacts carrying a frontmatter block), the `progress.md` §E.4 audit-ready signal, the documentation updates above, and this entry — zero Go changes (sync touches markdown only). The branch is unpushed, so no CI verdict exists yet. `sync_commit_sha` is a `pending-backfill-SPEC-WEB-TODO-QUEUE-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` § SHA placeholder backfill exemption), backfilled in a follow-up commit. 🗿 MoAI
- **[SPEC-KANBAN-RECORD-SESSION-KEY-001](.moai/specs/SPEC-KANBAN-RECORD-SESSION-KEY-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The kanban state record is now written by the session it describes, and it carries that session's lane number and card identifier.** The launcher wrote it, keyed from the project-wide `.moai/state/current-session-id.txt` slot — one slot the last SessionStart wins — and the launcher runs *before* the session it launches exists, so the identifier it keyed on belonged to a process that had not started. The property this SPEC is written against is therefore **not** "the record was always keyed to the launching session": measured, some sessions did carry a record under their own identifier, by coincidence, when the slot happened to hold it. That is the *worse* case, because a consumer that finds a record cannot tell whether it describes the session it asked about. The property is that **the key was not a function of the session the record describes**. The write moved into the launched session's own SessionStart hook (`internal/hook/session_start_record.go`), which is the first actor holding that session's runtime identifier, so the key is correct by construction rather than by a resolution step that could be got wrong again. `internal/cli/kanban.go` no longer writes a record at all (`grep -rn 'kanban.WriteBestEffort\|kanban.Write(' internal/cli/` → no output, from a measured baseline of one hit).
  - **Two new record fields, both `omitempty`, both meaning "not recorded" when absent** (`internal/kanban/record.go`). `Lane int` is a factory lane's number with `0` meaning "not a lane" — lanes number from 1, so the zero value is unreachable by legitimate data and carries the distinction a pointer would otherwise be needed for. It is data *beside* `Role`, never a role value: `Role` stays `lane` for every lane, and it is set through a separate `WithLane` rather than through `WithRole`, whose drop-unknown guard exists precisely so arbitrary launch-label text never reaches the role field. `CardID string` is the queue card the session is working, distinct from `SpecID` because Class A and Class B cards never acquire a SPEC identifier. A record written under the previous schema still reads, byte-identically on rewrite.
  - **The card identifier is derived from the worktree basename only where that root's parent directory is named `worktrees`.** That containment test is what makes the leave-it-empty clause reachable: without it the derivation always yields *something* inside any checkout, so a session standing in a primary checkout named `moai-adk-go` would file `moai-adk-go` as its card id and the console would render it as one. The test constrains where the value may come from, not whether the card exists. An explicit `MOAI_KANBAN_CARD` override wins, and an empty override is treated as unset so it never blanks a derivable value.
  - **That derivation deliberately avoids a `git rev-parse` subprocess.** `git rev-parse --show-toplevel` names the same directory, and run-phase measured what it costs on a hook with a 5s budget: `TestSessionStart_DeferredScanDoesNotBlockReturn` went from passing to **650-890ms against its 500ms budget**, attributed by swapping the pre-change `session_start.go` in under the identical environment and back. Fixed in `170935d18` — the containment test is a property of the path, so the derivation is pure path arithmetic.
  - **The backend is carried, not inferred.** `MOAI_KANBAN_BACKEND` (`internal/config/envkeys.go`) is new because the backend is the one launch fact a session cannot observe for itself; it previously reached the record only as a literal argument at the launcher's call sites, which is fine while the launcher writes the record and impossible once the session does. `ANTHROPIC_BASE_URL` is set by the GLM path but settable by anyone, so deriving the backend from it would be a guess dressed as a measurement.
  - **Fail-open throughout, and the shape of it is structural.** `writeKanbanSessionRecord` has no return value, so the handler cannot gate on it; an unwritable state directory yields no record and no error. Four gates precede the write, each a case where the correct answer is no record rather than a guessed one: no runtime identifier or no resolvable root, a re-entry source (resume / clear / compact / fork, since the record's orchestrator-written fields land later and a rewrite would discard them), a session that is neither kanban nor factory, and a record already present for this identifier.
  - **One pre-existing criterion of another SPEC was superseded, not deleted.** `SPEC-FACTORY-MODE-001`'s **AC-FM-023a** pinned the launcher-written record; `TestCC_KanbanWritesStateRecord` now asserts that absence instead, in run-phase commit `19da7c6d9`. **The criterion's fail-open half is unchanged and still binding.** A supersede annotation was added to that SPEC's `acceptance.md` in `1d8b56426` on the operator's ruling, so a later reader of either SPEC finds the reason rather than a criterion that silently no longer describes the tree.
  - **A test-environment hazard worth naming**: these tests run inside a factory lane session, whose `MOAI_FACTORY_WORKER` made every case read as that lane. `scrubKanbanEnv` is the fix, and it is load-bearing rather than tidy — the failure reproduces on the machine running the lane and never in CI.
  - 9 acceptance criteria (AC-KRS-001..009, count verified against `acceptance.md`): 9 PASS, 0 FAIL, at run HEAD `19da7c6d9`. Run-phase commits M1 `41666ca5f` → M2 `8838e31ec`, plus four follow-ups — `170935d18` (the latency fix above), `3964ae7c6` (two acceptance greps satisfied literally rather than argued past), `69ecae652` (the factory lane join's third hop pinned), `19da7c6d9` (the superseded criterion); not pushed (factory lane — integration is the lead's). Coverage per run-phase §E.2: `internal/kanban` 84.4%, `internal/hook` 84.5%, `internal/config` 80.6%, `internal/cli` 78.8%; lint `0 issues.` on all four packages; host and `GOOS=windows GOARCH=amd64` builds both exit 0. Exactly one production writer remains (`grep -rn 'kanban.WriteBestEffort\|kanban.Write(' internal/ --include='*.go' | grep -v _test` → `internal/hook/session_start_record.go:94`), and every pre-existing record file on this machine was observed present with unchanged size and mtime before and after.
  - **Documentation review found nothing to change, and that is a finding rather than a skip.** Searched `.claude/rules/`, its `internal/template/templates/` mirror, `docs-site/content/{en,ko,ja,zh}/`, and the four READMEs for a description of the record's writer or of its fields (`kanban.Record`, `state/kanban`, `current-session-id`, the record's JSON key names, and "the launcher writes"). Every hit is a different subject: `state/kanban/backlog.json` is the queue, `current-session-id.txt` appears only in the goal-arm and context-window doctrine (a different consumer, unchanged here), the 13-field table in `advanced/kanban-mode.md` is the Origin-Trail Chain's `WorktreeNode`, and the "launcher writes a transient settings file" line in `multi-llm/kanban-mode.md` is the cross-session inbound settings file. No published page describes the kanban session record at all, so nothing was written — inventing a page this SPEC did not require would be a worse outcome than the silence.
  - This sync commit carries the frontmatter `in-progress → implemented → completed` transition on `spec.md` (the only one of the three artifacts carrying a frontmatter block), the `progress.md` §E.4 audit-ready signal including the cross-SPEC supersede record, and this entry — zero code changes (sync touches markdown only). The branch is unpushed, so no CI verdict exists yet. `sync_commit_sha` is a `pending-backfill-SPEC-KANBAN-RECORD-SESSION-KEY-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` § SHA placeholder backfill exemption), backfilled in a follow-up commit. 🗿 MoAI
- **[SPEC-WEB-CONSOLE-015](.moai/specs/SPEC-WEB-CONSOLE-015/spec.md)** — sync-phase close (3-phase plan→run→sync). **The console's kanban chain now fills its model, effort and context cells from the session's own telemetry record, and gains a per-lane factory progress list beside the chain roles.** The three cells were literal placeholders — `Model: ""`, `Effort: ""`, `ContextPct: -1`, each with a comment deferring them to a later step — and they now come from `SPEC-SESSION-TELEMETRY-001`'s single exported reader, keyed per session (`internal/web/viewmodel_ops.go`: `readTelemetry`, `telemetryCells`; `buildChain` takes the project root for that purpose alone). This package declares neither the record's path nor its schema: one disk format declared in two places is a format that forks. Consumer-only — `internal/statusline`, `internal/kanban`, `internal/session` and `internal/cli` are untouched (`git diff --stat 40f064c6d..HEAD` over those four → empty).
  - **The lane join is guarded on both sides, and that is the whole design.** The join is `workers.json[lane-N].PID → active-sessions entry → kanban record`, and it is non-unique on BOTH ends: the factory registry's loader is fail-open and pruning dead claims is a separate call the console does not make, so one pid can sit on two lanes; and `session.Registry.Register` deduplicates by **session id alone**, so two live entries can carry one pid. `internal/web/factory_lanes.go` counts before it resolves on each side and sends either case to `unresolved` rather than to a winner.
  - **The hazard is a lookup that completes and returns the wrong answer, not one that returns nothing.** `spec.md` §A.4 measured a lane whose join completes on another lane's session and renders that lane's record as if it were this one's. An empty row is visibly empty; a confident wrong row is not. The unresolved marker exists to keep the second one off the page, and it carries its reason — `no-session`, `ambiguous`, `no-record` — because which of the three failed is a different fact to the reader. An unresolved row keeps its lane number and carries no state, no stage and no stage mark at all: asserting a stage for a session that could not be identified is exactly the plausible substitute this console refuses.
  - **Two user-visible strings were corrected rather than deleted.** The kanban note banner and the not-recorded marker's hover text both named `kanban.Record` as the producer that would eventually fill those cells; measurement showed it was never the producer (`spec.md` §A.2). Each now says only what a blank cell means — "this session has no telemetry record yet" — names no producer, and carries a translation key (`kanban.note`, `mark.notRecorded`) resolving in all four locales. Eleven keys were introduced across `en` / `ko` / `ja` / `zh`, each present and distinct from `en` in the three derived locales.
  - 14 acceptance criteria (AC-WC15-001..052, count verified against `acceptance.md` — the 15th token, `AC-WC15-012`, appears once and only as the record of its own deletion), all PASS at run HEAD `f80bad4d0`. Run-phase commits M1 `b35a3d098` (lane view model + registry join) → M2 `e78080458` (telemetry cells) → M3 `f80bad4d0` (lane section render + the two corrected strings), from base `40f064c6d`; factory lane, not pushed — the lead integrates. Files: `internal/web/factory_lanes.go` (new), `factory_lanes_test.go`, `factory_lane_section_test.go` and `session_telemetry_cells_test.go` (new tests), `viewmodel_ops.go`, `screens.templ` and `widgets.templ` with their generated twins, and `assets/i18n.js`. `go test -count=1 ./internal/web/` → `ok`, `coverage: 66.8% of statements`; `golangci-lint run ./internal/web/...` → `0 issues.`; host and `GOOS=windows GOARCH=amd64` builds both exit 0. Transport is untouched by construction — `git diff 40f064c6d..HEAD -- internal/web/events.go internal/web/assets/app.js` is empty.
  - **Three gaps are carried, not closed.** The coverage regression against the pre-change tree was **not measured**: the post-change rate is 66.8%, the pre-change rate was never taken, and what was measured instead is that every function this SPEC adds sits above the package rate (`loadFactoryLanes` 100%, `resolveLane` 95.8%, `unresolvedLane` 100%, `readTelemetry` 85.7%, `telemetryCells` 100%, `buildChain` 94.7%). Measuring the pre-change rate needs a second worktree checkout, and the lead judged that cost above the benefit here. The per-function figures bound the regression; they are not the measurement the Definition of Done asks for. The browser-side render is likewise unobserved — every render assertion reads the server's markup, and nothing covers `applyI18n` resolving `data-i18n-title` in a live browser — and the join is unobserved against live production state, since every fixture is a `t.TempDir()` synthetic tree and §A.4's live measurement was not re-taken.
  - **Documentation changed in four files, one per locale.** `advanced/moai-web-console.md` stated that model, effort and context "are not recorded yet, so they are left blank", in the chain-board paragraph and again as a principle — falsified by this change, and corrected in `en` / `ko` / `ja` / `zh` to say a blank cell means the session has no record carrying that value. The same page listed the chain board and the SPEC pipeline and did not mention the lane list at all, so one paragraph per locale was added, naming the row's fields, the unresolved marker and its reason, and the no-lane case. Left unchanged: the `cli-reference/web.md` route-table line and the README sentence, both one-line summaries that name no cell and no banner — a summary an added section does not falsify. Nothing else describes this screen's contents, and writing a page this SPEC did not require would be a worse outcome than the silence.
  - This sync commit carries the frontmatter `in-progress → implemented → completed` transition on `spec.md` (the only one of the four artifacts carrying a frontmatter block; `design.md` and `research.md` are retained reference material outside the Tier M set and were not touched), the `progress.md` §E.4 audit-ready signal including the three carried gaps, the four documentation corrections above, and this entry — zero Go changes (sync touches markdown only). The branch is unpushed, so no CI verdict exists yet. `sync_commit_sha` is a `pending-backfill-SPEC-WEB-CONSOLE-015` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` § SHA placeholder backfill exemption), backfilled in a follow-up commit. 🗿 MoAI

- **`moai graph query` and `moai mx query` refresh stale mechanical layers before answering,
  and every answer names its tree.** The refresh is content-hash driven — every walked file is
  hashed, only files whose hash moved are re-parsed — so uncommitted working-tree edits are
  reflected in the answer without a rescan. The cache is anchored per tree: an index whose
  provenance names a different tree root is never trusted incrementally and forces a full
  rescan, so two worktrees of one repository never share refresh state — the wrong-tree-answer
  defect family this round produced twice. A measured-cost budget (gate.yaml
  `update_budget_ms`, default 2000ms) warns on overrun and never blocks the answer. `moai mx
  scan` stamps the same provenance on every write, and the curated codemaps layer is
  deliberately never auto-rewritten at query time — its staleness is exactly the drift gate's
  signal.
- **The `--merged-only` and `--stale` base branch now defaults to `origin/main`, not the local
  `main`.** A local `main` behind the remote reports fewer branches as merged, so the two worktree
  sweeps disagreed about the same tree; the base is now the reference the PR-merge sweep already
  compared against. The "checked out on the base branch" guard compared a local branch name against
  the base literally, which the new default would have silently disabled — a second worktree sitting
  on `main` would then have been judged by the merge predicate, which reports `main` as merged into
  `origin/main`. That comparison now also matches the base's trailing segment, and errs toward
  keeping.
- **Every preserve notice from the PR-merge sweep names its cause** — `cause=dirty`,
  `cause=anchored-by-lock`, `cause=anchored-by-registry`, `cause=refusal-class`,
  `cause=undetermined-merge`, `cause=ignored-content`, and the check-failure variants — so two
  worktrees preserved for different reasons are distinguishable from the sweep's output without
  inspecting the trees.
- `/moai project` now ends by issuing one backlog card carrying the project's first feature, derived from that run's `harness-spec.yaml` `goal` and prefixed `[PROJECT] `, and its completion question offers starting that card as a branch. The queue doctrine gains a **standing source** concept to admit this: a workflow the operator authorized once to issue a card when it finishes, bound by five conditions (one card per run, derived not invented, marked, id reported, starting still a separate pick). The Implementation Kickoff Approval gate is unchanged — the start branch reaches it like any other run-phase entry, and the `ac_converge` goal is armed only after it passes.

### Fixed

- **[SPEC-CLI-TEST-CWD-ISOLATION-001](.moai/specs/SPEC-CLI-TEST-CWD-ISOLATION-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t334). **Running the `internal/cli` test suite no longer leaves a `.moai/` state directory inside the repository tree at the package's working directory.** Five tests (`TestCC_FactoryEntryThroughRunCC`, `TestCC_KanbanEnvMutationIsRestored`, `TestCC_KanbanFlagStrippedBeforeLaunch`, `TestGLM_FactoryWorkerEntry`, `TestGLM_KanbanFlagParity`) drove the name-claim registries (todo leads, factory workers) through a root resolver that falls back to `$CLAUDE_PROJECT_DIR` or the process cwd, so with no project dir pinned the registries landed in-tree at `internal/cli/.moai/state/…` — untracked, gitignored, and silently truncating every `.moai`-marker upward walk that started below the repository root (a wrong answer, not an error: the residue had already failed t317's D9 gate that way). Each producing test now pins its root with `t.Setenv(config.EnvClaudeProjectDir, t.TempDir())`, and the package `TestMain` gained a baseline-delta residue guard that fails the whole run when a `.moai` appears at the entry cwd during it. Test-only diff: 4 `*_test.go` files, zero production-code change; full `internal/cli` package green at 396 s with the guard armed. 5 acceptance criteria (AC-001..AC-005), counted against `acceptance.md`: 5 PASS, 0 FAIL. Run-phase commits: plan `9114ddbc3` → M1+M2 `65e7e7672` → M3 `f5ff4d74e` → M4 `3bdedf04b`. This sync commit carries the 3-phase close (`in-progress → completed`), the `progress.md` §E.4 signal, and this entry — markdown-only. `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-INTEGRATION-LOCK-LIVENESS-001](.moai/specs/SPEC-INTEGRATION-LOCK-LIVENESS-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t298, Tier M). **The release-integration window read as abandoned the instant it was taken, because the pid it recorded as the holder was the `moai integration acquire` process's own — and that process exits the moment the command returns.** Every holder therefore probed dead, every window read `reclaimable`, and the serialization the lock exists to provide was not happening at all: three lanes independently hit this on 2026-08-27, and one real eviction occurred without `--force`. The fix anchors the recorded pid to the **owning session** rather than to whichever process writes the record. `AcquireIntegrationLock` (`internal/kanban/integration_lock.go`) no longer fills an unset `PID` with `os.Getpid()` — a window outlives the process that records it, so that default was not a degraded answer but a wrong one, and the doc comment now says so at the call site where a future tidy-up would reintroduce it. Owner resolution moves to the caller: a new exported `session.ResolveOwnerPID()` (`internal/session/session_pid.go`) returns `(pid, resolved)` from the `MOAI_SESSION_PID` stamp or the nearest non-wrapper ancestor, with **no** `os.Getpid()` third step, while the pre-existing `resolveSessionPID` keeps its `os.Getpid()` fallback so the session registry's recorded-pid semantics are not silently changed by this fix — splitting the seam rather than editing it in place is what keeps the two cost profiles separate. An unresolvable owner is recorded as pid 0 and read as **live**, not guessed at: `Stale()` returns false for `PID <= 0`, so the failure direction is "an operator asks the holder to release" rather than "two lanes merge at once", and the asymmetry is argued in the comment rather than assumed. A new additive `pid_source` field (`PIDSourceSessionOwner`) distinguishes an unresolvable-owner record from a pre-anchor legacy one for a human reader; **no read path branches on it**, so a legacy record's dead pid still reads reclaimable exactly as it did before — a marker that documents rather than gates. Regression coverage is genuinely cross-process (`internal/cli/integration_lock_owner_liveness_test.go` builds a binary, runs `acquire`, lets that process exit, and only then probes) because an in-process test could not observe this defect at all; the merge guard's own test (`internal/hook/integration_lock_guard_test.go`) follows the anchored liveness. 13 acceptance criteria (AC-INL-001..013), counted against `acceptance.md`: **13 PASS, 0 FAIL** — the two preserved invariants run-phase left unasserted individually (AC-INL-005 foreign-release refusal, AC-INL-006 takeover reports what it displaced) were closed by name during sync-phase. Run-phase commits: M1 `ac68ef1ec` (RED) → M2+M3 `3f3465369` → M4 `d5006ff25` → M5 `1061b10a4` → §E.3 `f8b7264ba`. Live confirmation, reported by the lanes that observed it rather than self-measured in this close: once the lead replaced the installed binary (tree at `343399d2f`), the first real acquire reported `held` with the session-owning pid, where the pre-fix binary reproduced the whole failure chain in lane-18's isolated A/B — in both cases the holder UUID was recorded correctly and only the pid differed, which is what confirms the diagnosis (the defect was never in identity *interpretation*). **Explicitly not closed:** the acquire path remains an unserialized read-modify-write (`spec.md` §G, RETAINED and out of scope — re-measured at close, no `flock` call site exists in either file), so a recorded hold stays a coordination signal rather than a permission boundary and the lead announcement remains the first serialization layer; a follow-up card is recommended for the serialization work itself. Making the deployment lag visible is re-scoped to card t326. The independent fresh-context `sync-auditor` returned **PASS (clean), harmonic 0.9189** (Functionality 0.95 / Security 0.87 / Craft 0.92 / Consistency 0.94; 4 findings, all optional, 0 blocking) — report at `.moai/reports/t298/sync-audit.md` — so this sync commit carries the 3-phase close (`in-progress → completed`) alongside the `progress.md` §E.4 signal and this entry. Two things are recorded in §E.4 rather than left implicit, because a later reader needs them. **First, the audit path was occupied when the auditor arrived**: `manager-docs` had written its own read to that same path and committed it, the auditor read it before it was removed, and the first derivation of both must-pass dimensions came back numerically identical to it; the orchestrator sent the auditor back to re-derive those two from its own evidence, and both then moved in opposite directions (Functionality 0.94→0.95, Security 0.88→0.87) while the aggregate barely shifted — so the per-dimension derivations, not the harmonic mean, are the evidence, and the same occupancy left the auditor's report carrying a dangling reference to a section that was never in it, corrected before this commit. **Second, the audit surfaced a §G omission** (`internal/session/anchor_pid_windows.go:14`): §G documents the stamp-absent path but not the stamp-present-but-dead one — on Windows `isProcessAlive` returns `true` unconditionally, so a `MOAI_SESSION_PID` naming an exited process passes a vacuous liveness check, is recorded, and then reads reclaimable, the one path tilting against the declared TREAT-AS-LIVE asymmetry. Classified by the audit as a documentation omission and follow-up-card material, explicitly **not** a defect this card should have covered: that probe is the session registry's, it behaved identically before this SPEC, and changing it would touch the surface §D.4 declared out of bounds. `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **A passing `moai gate` run now reports what actually ran, a hung step's timeout kills the
  step's whole process group instead of hanging the gate, and concurrent manual gate runs in one
  checkout serialize instead of stampeding the same toolchain**
  ([SPEC-GATE-THREE-AXES-001](.moai/specs/SPEC-GATE-THREE-AXES-001/spec.md), #1639). A passing run
  previously printed nothing at all — a test step that replayed a build cache and one that genuinely
  executed the suite were byte-identical silence, and the test axis's pass-side output was discarded
  before it could ever reach the terminal. Every configured step now reports its own outcome:
  `executed` with a measured duration, start instant, and the command line that actually ran (the
  resolved argv, not the step's label — the two diverge on two of the three Node test-resolution
  tiers, where an invisible `--passWithNoTests` decides what a pass even means), or its own skip
  reason, distinguishing all five paths by which the gate skips a step; steps a failed predecessor
  prevented from running report `not reached` rather than inheriting a verdict, and existing
  non-blocking notices survive alongside the summary. When a step's deadline expires, Unix
  terminates the step's entire process group (`Setpgid` at spawn, group signal at kill) — a
  descendant holding the step's output pipe previously kept `Wait` blocked past the very deadline
  meant to bound it — and the return is grace-bounded on both platforms; on Windows, where no
  process-group primitive is applied, the reason states that descendants may have survived.
  Concurrent manual `moai gate` invocations in one project serialize on a per-project lock: a
  starting run waits a bounded budget (`gate.timeouts.lock_wait`, default 30s) while naming the
  holder PID it waits on, then degrades to running unserialized with an explicit notice — never
  failing the run, and still returning the gate's own verdict — and a dead holder, an unacquirable
  lock, and a read-only lock directory all take that same fail-open path. Each phase shipped with
  named regression tests whose RED was observed before the implementation existed, and every
  mutant its criterion names was built, run, and killed per `acceptance.md` (the one Windows-only
  dead-holder mutant is carried by the CI matrix).
- **[SPEC-STATUSLINE-PROFILE-RESPECT-001](.moai/specs/SPEC-STATUSLINE-PROFILE-RESPECT-001/spec.md)** — sync-phase close, `in-progress → implemented` (card t293, issue [#1675](https://github.com/modu-ai/moai-adk/issues/1675)). **A statusline opt-out now stops the GitHub refresh child from being spawned, not merely from being drawn.** Both levers — `segments.github: false` and `statusline.forge: none|off|any unrecognized value` — return before the spawn point: the override gate inside `maybeRefreshGitHubCounts` (`internal/statusline/github.go`) and the segment gate at its call site (`internal/statusline/builder.go`). This is the distinction the reported defect turned on: suppressing only the render leaves the gh API polling — the path the operator disabled on 2026-08-17 after it contributed to GitHub 429 rate-limiting — running on every statusline refresh while the display reads clean. Reverting `none → github` recovers within one refresh cycle without deleting the cache, and the absent-config expansion (all segments enabled) is preserved unchanged, so the gate cannot silently disable the segment for users who never opted out. Alongside it, `ResolveLaunchProfileForProject` (`internal/profile/profile.go`) gained a subtree-aware read path: a session running in a directory *below* a registered project — an unregistered worktree, for instance — now resolves to that project's launch profile by walking `filepath.Dir` toward the root and taking the deepest registered ancestor, with exact matches still outranking the walk. The boundary is structural rather than lexical, so `/proj-other` cannot match `/proj`. The forge pair's four display states (`7/3` counts · `0/0` honest-stale · `-/-` unavailable · absent when opted out) are pinned by characterization test rather than changed. Run-phase commits (card t293, lane-2): `62485c918` (M1) → `5a193fa4c` (M2+M3) → `d615bf374` (M4) → `0a92b22f9` (M7); merged to develop as `8ef14f5ae`. 11 acceptance criteria: 10 PASS, 1 DEFERRED (AC-009 write-side ledger normalization, kickoff decision D1, follow-up card t297), 0 FAIL. Independent sync audit (cold start, different session from the run): **PASS-WITH-DEBT, weighted harmonic mean 86.7 against the Tier M threshold 80** (Functionality 88 / Security 92 / Craft 78 / Consistency 88; must-pass firewall cleared on both), 8 findings — 0 blocking; report `.moai/reports/t293/sync-audit.md`. The auditor killed all three gates by mutation, confirming the tests fail when the fix is absent, and counted the `-run` selector matches (3 + 4 and 8) so a zero-match selector could not have printed a vacuous `ok`.
  - Two limits are recorded rather than smoothed over. **The operational effect is deployment-gated**: a worktree session reads `boardRoot` from the *primary* checkout, whose `statusline.yaml` does not yet carry the `forge` key, so the opt-out takes effect once the merge reaches the primary working tree. And **the two levers read different roots** — the segment lever reads the worktree's config, the forge lever the primary's — so an operator editing a worktree copy gets silence rather than an error; a follow-up card is needed to align them or document the asymmetry. The status stays `implemented` rather than `completed` for exactly this reason: the audit returned PASS-WITH-DEBT, and the close contract admits `completed` only on a clean PASS. `sync_commit_sha` recorded as `pending-backfill-sync` and backfilled in the immediately following commit. 🗿 MoAI

- **Config disk-cache schema poisoning (pre-existing defect surfaced and fixed by [SPEC-TEAMMATE-REVIVAL-GUARD-001](.moai/specs/SPEC-TEAMMATE-REVIVAL-GUARD-001/spec.md) run-phase).** A config cache written by an older binary was served as fingerprint-valid to a newer binary: the cache fingerprint keyed on the config-file content, not the struct shape, so after any config-field addition the older cache unmarshalled with zero-values for the new fields — silently overwriting live file values (observed: `enabled:false` served over a file carrying `enabled:true`, which would have shipped the new guard configured as dead code). Fix: `configCacheSchemaVersion` 1→2 in `internal/config/cache.go` — the cache now records its schema version and a mismatch invalidates the entry (field-addition-is-a-compat-break rationale documented on the constant), so every future config-field addition bumps the version instead of inheriting poisoned caches. Isolation evidence: `MOAI_CONFIG_CACHE_DISABLED=1` loaded the true value, confirming the disk cache as the source.
- **[SPEC-V3R6-GRAPH-FRESHNESS-002](.moai/specs/SPEC-V3R6-GRAPH-FRESHNESS-002/spec.md)** — sync-phase close (3-phase plan→run→sync). **The t250 CR round-2 remediation: all 29 adopted findings of the 69-finding review are executed against the graph-freshness surface — test policy, code policy, astx extraction, docs, and the predecessor SPEC's own correction-and-close.** Test policy: the graph code-query tests and the astx CGO-positive suite that depend on tree-sitter now skip (or build-tag out with a `!cgo` fallback test that walks all 16 registered languages) instead of failing under `CGO_ENABLED=0`; every finding-named untested behavior gained a named test — the citation hash-mismatch branch, MCP required-parameter and `..`-path traversal rejection for all three handlers, the all-layers-fresh gate notice, and Symbol/Via match-content assertions; provenance fixtures are machine-serialized (`json.Marshal`, `fmt.Sprintf`) replacing hand-interpolated strings that broke on Windows paths; a shared result-shape helper replaces every unguarded `res.Content[0]` indexing site, so an empty response fails the test instead of panicking it; and the budget-overrun warning observes an injected duration seam (defaulting to wall-clock in production) rather than assuming a real refresh outlasts the budget. Code policy: the check layer's error path returns the failing layer's report instead of contradicting its own doc comment; `sidecarAbsentReason` is an immutable constant; `MXIndexNeedsRefresh` takes its staleness threshold from the caller; one shared fingerprint-comparison helper serves both `EdgesSourcesMoved` and `checkEdges`; graph-build extraction errors wrap with `%w`; `graph stamp` filesystem errors no longer leak absolute local paths (`*os.LinkError`/`*os.PathError` unwrapped to the errno); `graph refresh --edges` evaluates the SELECTED artifact's provenance rather than the default's; the three MCP code tools return the package's `toolJSON`/`toolErr` (success JSON rides StructuredContent) instead of private constructors; and the five `gate.graph_freshness` shipped-config keys are reclassified class R → class W with their production readers cited. astx: Go import paths written as raw string literals are captured as `@code.import` exactly as interpreted literals are (`go.scm` raw-string alternative plus backtick-aware `stripQuotes`) — a real extraction gap closed. Docs: the `dependencies.md` hook summary bullet names `graph` matching the Mermaid edge, the t250 m5-baseline report replaces its developer-local transcript path with a repository-relative label, the t250 entry's cumulative MCP count reads 25 to 28, and docs-site ko `graph.md` reads "오래되었으면". Lifecycle: the predecessor SPEC-V3R6-GRAPH-FRESHNESS-001 is corrected (exit-2 When-clause for not-comparable system errors, AC-GF-008 promoted to the MUST row, the non-Go declaration-set qualifier on AC-GF-020, version 1.2.0) and closed — `status: completed` with `sync_commit_sha` manually backfilled to `2fc4b40a6` ahead of the close so the prose placeholder is not frozen, full-close path exit 0 with no fallback. The #1648 squash merge had also orphaned t250's codemaps stamp commit; M0 restamped against main-reachable `c9eed8ac6`, and that stamp carries to merge unchanged (27 described-file churn against threshold 40). Run-phase commits M0 `52f7ba135` → M1 `371b8799c` → M2 `58bb7d8ba` → M3 `e70d6acde` → M4 amendment `bc9192411` → predecessor close `f32e9a346`. No README or docs-site changes ride this sync commit — every user-facing doc correction landed in M3. `sync_commit_sha` recorded as a `pending-backfill` placeholder and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-CODEX-VERDICT-SYNTH-001](.moai/specs/SPEC-CODEX-VERDICT-SYNTH-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t229 Class B). **`audit_multi` no longer synthesizes an unrecognized codex review body as a pass.** `synthesizeReviewOutput` (`internal/cli/mcp_codex.go`) previously fell through to `verdict := "pass"` whenever a review body matched no known format, so an adversarial body in an unknown shape read as clean; it now collects every verdict signal the body carries (stated verdict label, scored verdict line — the `FAIL 0.75 / PASS 1.00` form, read by the new `codexScoredVerdict` recognizer, and severity-tagged finding bullets) and adopts the most conservative member of the signal set (P-CONS set-max rule in `adoptConservativeVerdict` — order-independent by construction, so a fourth signal cannot reopen the hole). A body matching NO signal takes its verdict from the mode, not from hardcoded optimism: native review mode (`review/start`) keeps `pass` (a bullet-less body is codex observing nothing to block on), while adversarial mode (`turn/start`) and unknown methods read `inconclusive`. When one backend body carries contradictory verdict signals, the divergence is recorded on the output (`SynthesisNote` via `describeSignalDivergence`, only on genuine disagreement) alongside the conservatively adopted verdict. The `(reviewText, method)` signature is load-bearing — card t234 (#1632) builds its Findings population on this seam. Regression coverage: `internal/cli/codex_verdict_regression_test.go` (3 fixed regressions) + the extended `internal/cli/codex_review_rpc_test.go` (native-mode finding-bullet test preserved via explicit method call); mutant (e) independence observed (AC-CVS-006's witness kills a substitution-order-semantics mutant the other 16 tests do not). 6 acceptance criteria (AC-CVS-001..006), counted against `acceptance.md`: 6 PASS, 0 FAIL. Run-phase commits M1 `55b2ca3e1` → M4 `1e1edf6b4`, run close `1a6c0fac0`, merged origin/main as `4561f432c` (targeted `ok internal/cli 1.034s`, 34 cases re-verified on the merged tree; full-suite verdict deferred to PR CI per verification-load discipline). A card↔SPEC scope gap (participant-count axis) was deferred to a new card by operator decision — recorded in `.moai/reports/t229/succession.md`, out of this entry's scope. This sync commit carries the 3-phase close (`in-progress → completed` merged into the sync commit), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` recorded as `pending-backfill` and backfilled in the immediately following commit. 🗿 MoAI
- **[SPEC-ZONE-REGISTRY-RESYNC-001](.moai/specs/SPEC-ZONE-REGISTRY-RESYNC-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Fresh `moai init` projects no longer fail `moai constitution validate` / `moai doctor` on a drifted zone registry.** The registry's clause/anchor data was resynced to the verbatim single-line spans actually present in the cited sources (clause 73 + anchor 18 + file re-point 4; 67-error clause drift → 0), the 4 `[SUPERSEDED …]` retired entries are clause-exempt by design (option C) while keeping their anchor checks, and a new blocking guard test (`internal/constitution/registry_sync_test.go` — validate + anchor six-step slug + literal grep-F checks, run against both the local and template mirrors) rides the ordinary `go test ./...` CI job so the registry cannot silently drift again; the `constitution-check` CI job gained a secondary `validate` step, and the template registry was re-embedded via `make build`. 14 acceptance criteria, counted against `acceptance.md`.
- **The PR-merge sweep read "gh could not answer" as "not merged".** `gh pr view` errors on the
  ordinary case of a merged PR whose head branch was deleted on the remote, and the sweep collapsed
  that error, an absent PR, and malformed JSON into one empty string it treated as a negative — the
  worktree was preserved, permanently, on every future sweep. Merge detection now carries three
  outcomes (merged / not merged / no answer), and only the no-answer case consults `git branch
  --merged origin/main`. A determinate `OPEN`, `CLOSED`, or `DRAFT` ends the decision without the
  fallback, so neither source overrides the other: `gh` sees squash merges the git fallback cannot,
  and the git fallback sees deleted-branch merges `gh` cannot. When neither can decide, the worktree
  is preserved and the notice says which state was undetermined. The fallback's parser now strips
  git's `+` linked-worktree marker as well as the `*` current-branch one, and skips the
  `(HEAD detached at ...)` / `(no branch)` lines: `+` decorates precisely the branches this sweep
  evaluates — every candidate is checked out in a linked worktree — so stripping only `*` left the
  fallback unable to report any live worktree's branch as merged, and the deleted-branch case above
  still reached the preserve path.
- **The session-anchor guard was blind to most live sessions, and git's worktree lock was what
  actually protected them.** The guard read the session registry alone, whose `cwd` is corrected
  only when the relocation hook fires — measured naming 1 of 5 live anchors, against 5 of 5 named by
  the git worktree lock reason. The decision now lives in `internal/session`, reads the lock reason
  from `git worktree list --porcelain` as its primary source, probes any process id that reason
  names for liveness, and unions the result with the registry, which is retained as a supplementary
  input. It fails closed: an unparseable lock line, a lock naming no process id, and an undetermined
  liveness probe all report the tree as anchored. All three sweeps that shared the blind guard now
  share this one — the PR-merge sweep, `worktree clean --stale`, and the `--merged-only` path, where
  it is the only thing standing between the sweep and a live session's tree.
- **`worktree clean --stale --yes` and `worktree clean --merged-only` destroyed gitignored content that nothing regenerates.** Their
  cleanliness check ran `git status --porcelain` without `--ignored`, and a non-forced
  `git worktree remove` disregards ignored files too — so a merged, clean, unanchored tree whose
  only remaining content was `.claude/agent-memory/` reported `dirty=no` and was deleted, exit 0,
  with nothing in the output to read afterwards. `--merged-only` was the more exposed of the two,
  carrying no dirty guard at all. The ignored-content guard built for the PR-merge sweep now covers
  both, and all three removal paths share ONE decision — allowlist included — in `internal/session`,
  rather than a second copy free to drift. `--stale --json` gains a fourth predicate, `ignored`, so
  the inventory and the sweep stay the same evaluation.
- **An unreadable worktree porcelain silently disarmed both `worktree clean` sweeps.** When
  `git worktree list --porcelain` failed, the lock reader returned an empty map that the callers
  could not distinguish from "no tree is locked", so every tree fell back to the session registry
  alone — the source measured to name 1 of 5 live anchors — and the run looked exactly like a
  healthy one. With `--yes` that removed a lock-anchored tree and killed a live session's shell.
  Both sweeps now fail closed as the PR-merge sweep already did: `--merged-only` removes nothing and
  emits `cause=lock-source-unreadable`, `--stale` keeps every tree with that cause as its keep
  reason, and `--stale --json` reports `anchored: "undetermined"` rather than `"no"`.
- **A locked worktree is recognised before removal is attempted**, instead of producing an
  error-shaped failure notice on every sweep for a tree that is behaving correctly. The sweep still
  never unlocks a worktree and never passes `--force`, and a refusal it did not anticipate remains a
  non-blocking notice carrying git's own message as the cause.
- **A merged, clean, unanchored worktree holding gitignored content is preserved** unless every
  ignored entry falls under a short allowlist of regenerable paths — runtime state, runtime-managed
  local config, build output, test residue. `git status --porcelain` and non-forced `git worktree
  remove` agree in disregarding ignored files, so for that class of content nothing else stood
  between the sweep and its destruction; worktree-local agent memory is written per project root and
  has no path back to the primary checkout. Preserving those trees is a stopgap, not the answer —
  draining them is, and is not part of this change.
- **`parseGLMReview` reads the first TEXT content block, not `Content[0]`.** Under a delivered
  reasoning directive z.ai prefixes the response with thinking content blocks whose payload lives
  in `thinking`, not `text` — the blind `Content[0]` read failed open to `inconclusive` while a
  complete review sat in the next block.
- A Kanban or Factory **lead session now appears in the session list under its own name** rather than an unrelated SPEC heading ([#1596](https://github.com/modu-ai/moai-adk/issues/1596)). A session's name and its title are two separate registrations: `--name` (already injected) made the lead addressable, which is why messaging and delegation always worked, but nothing ever set a title — so the `UserPromptSubmit` title policy filled the slot from the most recently modified `spec.md`, and a lead was listed as `SPEC-…: <heading>`. The launcher now publishes the lead's resolved name — the operator's own when they supplied one, the bare or bumped role otherwise — and the hook titles the session with it, above the SPEC branch and below the first-wins guard, so a later `/rename` still wins. The title registers on the first prompt, not at launch, and the bootstrap notice says so in all four locales. Companion and lane sessions are unchanged.

## [3.1.3] - 2026-08-24

### Summary

Two things this release is actually about.

The first is that a codex-cli harness now has something to read. A root `AGENTS.md` carries the
standing contract for any agent harness, the 11 retained agents are published in a second,
codex-consumable form, the skills are mirrored where codex looks for them, and a hook adapter
library sits ready for the wiring card that follows. None of it changes what Claude Code does.

The second is that the audit tooling stopped answering questions about a tree it never read. An
MCP server is a long-lived subprocess, so its working directory cannot follow a worktree switch —
an audit issued from a worktree audited the primary checkout and did not say so. Five MCP tools
now take the caller's own `project_root`; the codex and GLM backends refuse to verdict a diff they
could not collect; a stated `inconclusive` is read as inconclusive rather than synthesized into a
pass; and the convergence verdict is recorded under the tree that was audited. One operational
caveat decides whether a reader gets any of it: a running MCP server keeps the old behaviour until
the binary is replaced *and* the server restarts.

Filing feedback gained a scrubbing contract, the backlog queue gained an off switch, and the
`moai init` answers that were collected and discarded now take effect.

The rest is a sweep of long-standing defects. Every label in every SVG diagram the skill emits now
carries a resolving accessible name, with connector-geometry checks behind three of the rules;
`moai todo` analyses its own queue; design-dna gained diagram profiles that survive `moai update`;
`moai update` and `moai init` say when a skill mirror fell back from symlink to copy; the
recognised `ANTHROPIC_*` environment keys no longer trail the names the runtime actually reads;
the GLM reasoning-effort ceiling moved to `max`; `/moai gate` gained a typecheck axis so a broken
build can no longer pass; `moai update` no longer bricks on a symlink at a managed root; the
state-directory walk that decided what `moai clean` deletes now stops at the project root; and the
web console server no longer dies to a SIGTERM that lands during startup.

### Added

- **A root `AGENTS.md`** — the standing contract for any agent harness, not only Claude's. It is one
  file because codex reads project instructions under a byte cap and drops the overflow with no
  warning and exit 0, so a contract that does not fit reports as complete. A build guard holds it
  under the ceiling. Making room took eleven always-loaded documents down to stubs against eight
  lazy companions; no obligation moved, only the prose explaining it.
- **The 11 retained MoAI agents are published in dual form** — the existing
  `.claude/agents/moai/*.md` plus codex-consumable `.codex/agents/moai/*.toml`, generated
  deterministically from the template markdown by a new `internal/template/agentemit`. Golden,
  embed, and deploy guards pin the TOML against drift from its markdown source.
- **A skill mirror at `.agents/skills`** for codex-cli, derived from the run rather than hand-listed,
  and kept out of user repositories.
- **`internal/codexadapter`** — the Codex hook adapter, shipped as a library. Nothing invokes it yet;
  the `--agent` config generator is the follow-up. It carries the eleven-event Codex-to-MoAI event
  table, translation of the three output keys measured inert on codex 0.147.0, and a config
  validator that collects every unknown-key violation instead of stopping at the first.
- **`/moai feedback` gains a scrubbing contract** between the drafted report and `gh issue create`: a
  new `internal/feedback` package (masking scrubber, vulnerability classifier, mask log, retry
  queue), the `moai feedback scrub` and `moai feedback queue` verbs, a pre-submission confirmation
  gate, and the `feedback.auto_submit` key that decides whether the gate asks. The classifier reads
  the body **before** masking — masking removes the strings a vulnerability report is recognised by,
  so classifying afterwards fails silently on the one path whose cost is a public disclosure. It is a
  convention the skill body follows, not a sandbox: opening an issue by hand bypasses all three.
- **`workflow.todo.enabled`** turns the backlog queue off — the session-start summary, the statusline
  TODO segment, and the skill's inference routing. Absence means on, so no existing project changes.
  `/moai todo` invoked by name still works, and the rule and skill listing still mention it; that
  boundary is intended, not an oversight.
- **`moai todo` analyses its own queue.** Every `add` measures the new card against the ones already
  there, and `moai todo analyze` re-reads the whole queue on demand. The analysis is a record: no
  card is folded, reordered, dropped, or edited as a consequence.
- **An optional `project_root` on `spec_progress`, `spec_audit`, `spec_drift`, `codex_audit`, and
  `audit_multi`.** An agent inside a worktree passes its own `git rev-parse --show-toplevel`; a
  session in the primary checkout passes nothing and gets what it got before.
- **`moai-domain-svg-infographic` checks connector geometry** (`SVG070`-`SVG074`), mechanising three
  of the six connector rules, with 42 bipolar fixtures that declare the exact code set they must
  produce. No dependency added — Node 18+ standard library only.
- **Every SVG the skill emits carries a resolving accessible name** (`role="img"`, `aria-labelledby`,
  `<title>` first, `<desc>`), enforced as `SVG060`-`SVG064`. Before this, every label in every
  diagram the skill produced was inert to a screen reader.
- **`moai-domain-design-dna` gains diagram profiles**, with the active profile marker persisted under
  the project root's `.design-dna/` so it survives `moai update`, plus opt-in mermaid and drawio
  importer references that treat their sources as untrusted input — coordinates, colours, fonts, and
  layout never carry over.
- **`moai update` and `moai init` say when a skill mirror fell back from symlink to copy.** The
  deployer already returned that result and no call site read it.
- **`/moai gate` gains a typecheck axis**, so a broken build can no longer pass the gate
  ([#1592](https://github.com/modu-ai/moai-adk/pull/1592)).

### Changed

- **The per-agent model/effort matrix moves to a judgment-weighted policy.** The spend goes to the rows
  that judge rather than the rows that produce: the auditing/advising rows and the coordinating rows hold
  `high` in the two upper columns, while `manager-spec` and `manager-develop` sit at `medium` in all three.
  `manager-docs` becomes `sonnet/low` everywhere; no row takes `max` any more.
- **`manager-lead` joins the matrix.** It was absent entirely and resolved to the unmapped-agent `inherit`
  sentinel — the Tier L coordinator took whatever model the session happened to be on. It is now a mapped
  row with its own `lead` group, injectable and overridable like every other retained agent.
- **The GLM reasoning-effort ceiling is raised to `max`.** Every Claude effort above `low`
  (`medium` / `high` / `xhigh` / `max`) now maps onto reasoning `max`, and a GLM session without an
  explicit override defaults to `max`. `low` is unchanged; no Claude effort maps to `high` anymore.
- **The `ANTHROPIC_*` environment namespace enumeration is complete** — the recognised-key set no
  longer trails the names the runtime actually reads.

### Fixed

- **A state-reading command run anywhere under the home directory** resolved to `~/.moai/state` and
  reported success instead of failing. `moai clean` deleted `runs/` there, `moai chain` created
  `~/.moai/chain`, and the readers reported on a project nobody named. The lookup now stops at the
  project root.
- **A symlink at a managed root no longer bricks `moai update`.** A dangling link was reported as
  "Skipped (not found)", the deploy step then hit `EEXIST`, and every rerun repeated the loop —
  a managed root gutted but never repopulated. Every entry is now classified with `os.Lstat`
  semantics, with per-form dispositions and a `symlink` progress line.
- **The codex and GLM audit backends stopped verdicting code they never read.** GLM was reviewing
  nothing and saying so with confidence — one live run returned a `fail` citing a repository
  whitelist this codebase does not contain. When no diff can be collected the result is
  `inconclusive` and the backend is not called.
- **A stated `inconclusive` verdict is read as inconclusive**, not synthesized into a pass.
- **The `audit_multi` convergence verdict is recorded under the tree that was audited.** The writer
  resolved its state directory once at process start from the primary checkout while the reader
  resolves per call, so a run inside a worktree left its verdict where that worktree's gate never
  looks — and every worktree's verdict piled into one directory.
- **`project_root` is canonicalized against symlinks** before its boundary is checked, so a symlinked
  path cannot outlive the boundary that was supposed to contain it.
- **The `moai init` answers that were collected and discarded now take effect.** Three wiring chains
  shipped complete — typed options, wizard plumbing, unit tests — with zero production callers: the
  autonomy tier, the four workflow toggles, and the audit / codex-review-gate selections.
- **The web console server no longer dies to a SIGTERM that lands during startup.** `ListenAndServe`
  bound its loopback listener before registering signal handling, so a caller that learns the server
  is up by observing the listener address could deliver SIGTERM inside that window — and until the
  registration runs, SIGTERM keeps its default disposition and kills the process outright, with no
  failure line to read. Registration now precedes the bind, and a regression test pins the ordering
  by parsing the source rather than grepping it.
- **The `agent-authoring` cross-reference in the constitution is restored**, with a repository-wide
  sweep for the same class of dropped reference.

## [3.1.2] - 2026-08-21

### Summary

A statusline change and the documentation sweep it forced.

The slash pair after the repo name used to be git ahead/behind. It now carries open issues over open change requests, and ahead/behind no longer renders. The counts had moved three times in four days — merged onto the repo head as an arrow-linked prefix, removed the same day because `59/0` read as issues/PRs when it was ahead/behind, then restored as a glyph-tagged `🐛N 🔀N` suffix specifically to avoid that misread. The misread recurred anyway. The pair is where a reader looks for issue and PR counts, so that is what it holds.

The pair distinguishes four things a reader needs to tell apart: `1/1` fetched, `0/0` fetched and nothing open, `-/-` unknown but still coming, and an absent pair when no answer was ever going to arrive. That last case has five causes — the segment is off, `statusline.forge` names `none`, the forge value is unrecognised, no forge CLI is installed, or the remote is on no recognised host (a self-hosted GitLab or GitHub Enterprise checkout). Suppression does not latch: the refresh child re-derives it every run, so installing `gh` restores the pair within one TTL with no config change.

Documentation across README and docs-site described a statusline several layouts out of date, in all four locales at once.

### Changed

- The statusline repo segment's slash pair is **open issues / open change requests**, not git ahead/behind. Ahead/behind no longer appears on the statusline; the data is still collected, it simply has no slot on the bar. Anyone reading `↑2` or `0/0` as sync state should know it is gone.
- `statusline.forge: none` (and `off`) now removes the pair entirely instead of leaving `-/-` on the bar permanently. An unrecognised `statusline.forge` value does the same, so a typo surfaces as an absent pair rather than a marker suggesting a broken fetch.
- A checkout with no forge CLI installed, or whose remote is on no recognised host, shows no pair rather than a permanent `-/-`.

### Fixed

- `-/-` is reserved for states that can resolve themselves. It previously appeared for opt-out, missing CLI, and unrecognised-host cases, telling operators an answer was coming when none ever would.
- README and docs-site statusline descriptions corrected in all four locales: the ASCII `|` separator (documented as `│`), the removed branch arrows, the unified `💾` git-status glyph (documented as the retired `📬`/`📫`/`📪`/`📭` quartet), `🔅 v<version>` without the `cc` prefix, the unspaced `TODO: n/m` ratio, and the fourth line's forge counts which had not rendered there for days.
- docs-site advertised four display presets and a `preset: compact` config key, both retired; `settings-json` described Compact/default/full display modes when every mode collapses to one layout. Both predate this release. The Korean pages were already correct — the drift was in the derived locales.
- Stale comments in `internal/statusline`: two claiming the forge counts render nowhere, a format sketch with arrows and a glyph that appears nowhere in the code, two references to the retired mailbox quartet, and a header claiming a 5-line Full layout the same file records as retired. `renderGitBranch`, which had no non-test caller, is deleted — its doc comment was the likely source of the stale arrow prose in the READMEs.
- `moai glm tools enable --force` overwrites a mismatched token instead of re-emitting the error that just advised it ([#1587](https://github.com/modu-ai/moai-adk/issues/1587)). The flag parsed and the command branched on it, but both branches called the same function, whose token check is a deliberate contract. Changing the token needed a hand edit of `~/.claude.json`. The force path is now its own entry point; the checking path still refuses, and a forced overwrite still leaves every other `mcpServers` entry alone.
- The forge counts could render stale for one frame during a refresh. The refresh stamps the cache before fetching so concurrent renders stop spawning children, and it was clearing the suppression flag in that same early write — before it knew whether a forge or its CLI existed. A render landing in that window drew counts for a checkout that had neither. The flag now clears once both are confirmed.
- The statusline docs said a missing forge CLI leaves the cached counts on the bar. The pair is dropped whenever suppression is set, cached or not; the cache survives on disk for the restore after the CLI is installed. The same page already said so two sections earlier, so it contradicted itself. Corrected in all four locales, along with a `segments:` example that labelled `worktree` opt-in when an omitted key resolves to enabled.

## [3.1.1] - 2026-08-20

### Summary

v3.1.1 is a maintenance release on top of v3.1.0, gathered from two integration batches on the `release/v3.1.1` lane. Four themes dominate it.

**`~/.moai` gets a single source of truth, and a way to clean it.** Home-scope path construction — previously three incompatible styles scattered across seven packages — collapses into one stdlib-only `internal/paths` package honoring a new `MOAI_HOME` override, and all 17 home-scope join sites route through it. On top of that foundation, `moai doctor` gains a `Home Disk Usage` advisory check and `moai clean` gains a `--home` flag whose deletion candidates come from an explicit four-category allowlist guarded by a recursive carve-out predicate — unreadable means unverified, and unverified never deletes.

**The always-loaded context surface goes on a measured diet.** 295,044 → 288,975 bytes, guard headroom 1,239 → 2,757 tokens, with the file count and the budget constant both left untouched — the mechanism is `paths:`-scoped companions rather than deletion. Two structural countermeasures land with it: five prompt-cache discipline directives folded into the always-loaded rule set, and a recurrence control that turns silent re-growth into a stated, sized cost.

**Kanban and Factory terminology settles.** The v3.1.0 rename of `manager-lead` to `manager-kanban` is reversed — the agent is `manager-lead` again, now covering both the kanban lead and the factory lead session — and the board drops from six columns to five, with the sync gate absorbing the review verdict rather than a `review` column owning it. Factory Mode gets its own entry flag — `moai cc -f <N>` / `moai glm -f <N>` launches a lead plus N numbered lanes, each lane carrying a whole card through plan → run → sync in-session, while the kanban chain keeps `-k` — companion sessions are named by bare role, and the orchestration mode catalog is renamed onto the spawn-count axis (six modes to four: `direct` / `serial` / `fanout` / `sweep`).

**Correctness fixes across the CLI, hooks, and the test harness.** `moai update` now backs up unmanaged files *before* removing managed roots, so a template redeploy no longer silently drops local-only files. A raw `*exec.ExitError` is rejected at the exit-code seam, where it had been structurally satisfying the `ExitCoder` interface and passing a subprocess's exit code through as the CLI's own. GLM-5.3 is recognized as a genuine 1M-context model on every surface that reports a context window. And the timing test harness pairs its calibration reference with the measured runs so both sides share one load window, removing a class of load-dependent false trips.

### Added

- **[SPEC-V3R6-MOAI-HOME-PATHS-001](.moai/specs/SPEC-V3R6-MOAI-HOME-PATHS-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`~/.moai` path construction now has a single source of truth: a new stdlib-only `internal/paths` package (`Home()` + `MoaiHome()` + 8 typed sub-path accessors) honors a new `MOAI_HOME` environment override, and all 17 home-scope `.moai` join sites across 7 packages route through it — collapsing the three previously incompatible home-resolution styles (inline `os.UserHomeDir()`, the HOME-first `homedir.go` wrapper, and injected seams) into one contract.** Before this, `~/.moai` path construction was scattered across seven packages with no override mechanism, so tests could only redirect home via `t.Setenv("HOME", ...)` and no way existed to relocate the state root. `MOAI_HOME` semantics: a non-empty absolute path is returned verbatim as the root; empty equals unset; relative values are disregarded (XDG semantics). The SPEC supersedes SPEC-V3R6-SESSION-HANDOFF-AUTO-001's recorded home-resolution position ("new code MUST reuse `os.UserHomeDir()` — no abstraction layer exists"; REQ-MHP-012): the abstraction layer now exists and is mandatory for `~/.moai` paths.
  - **M1/M2 — the package + the env registration (`internal/paths/paths.go`, `internal/config/envkeys.go`).** `internal/paths` imports the standard library only (REQ-MHP-003), so the stdlib-only `internal/glmcred` can adopt it without an import cycle. `EnvHome = "MOAI_HOME"` is registered in `envkeys.go`; `internal/paths` references the name through a glmcred-style exported local alias (`paths.EnvHome`) because importing `internal/config` from `internal/paths` would create a cycle — the `"MOAI_HOME"` string literal lives nowhere else (AC-MHP-006 grep: 0 matches outside the two owners). The sub-path accessors (`StateDir`, `CacheDir`, `ReleasesDir`, `WorktreesDir`, `ProfilesDir`, `GlmEnvFile`, `UserSettingsFile`, `UserConfigSectionsDir`) consume segment constants mirroring `internal/defs/dirs.go`, the canonical owner; no accessor re-literals `".moai"`. Every accessor resolves per call — caching is prohibited (REQ-MHP-005) so the 107 `t.Setenv("HOME", ...)` tests keep observing env changes made later in the same process.
  - **M3/M4 — the 17-site migration.** Every home-scope `filepath.Join(<home>, ".moai", ...)` site migrated: statusline (model_cache ×2, usage, task), config resolver ×2, cli (launcher ×2, deps, migrate_profiles), update (local, cache), kanban/status_read, hook (session_start, pre_tool whitelist), glmcred, and the predicate-hidden receiver-prefix site `internal/cli/migrate_agency.go:184` (fed by the `update.go:827` adapter, which also migrates — so an overridden `MOAI_HOME` reaches the `.migrate-tx-*` checkpoint path). The acceptance predicate (the home-scope-anchored grep) returns 0 matches post-migration against a pre-migration baseline of exactly 17. Injected test seams (`HomeDirFn`, `homeDir` parameters) are preserved: seam-carrying sites consume `defs` segment constants while seam callers resolve via `paths`.
  - **M5 — helper collapse + security lockstep.** `internal/cli/homedir.go` is now a thin delegate to `paths.Home()` and the verbatim duplicate in `internal/cli/preference/cmd.go` is deleted — exactly one HOME-first implementation remains (AC-MHP-009 grep: one match). The `internal/hook/pre_tool.go` allowed-external-paths security whitelist and the `internal/core/project/root.go` home special-case resolve through the same accessors in lockstep (REQ-MHP-009), so an overridden `MOAI_HOME` is consistently trusted by the security boundary and the project-root guard, not only by state writes.
  - **Known hazard — the `.env.glm` shell/Go split (spec.md §4).** `status_line.sh` sources `$HOME/.moai/.env.glm` BEFORE the Go binary runs, while Go reads the same file via glmcred — under a non-default `MOAI_HOME` the two would read different locations, splitting one credential file across two roots. Shell-side `MOAI_HOME` support (`status_line.sh` + the 42 `handle-*.sh` files) is deliberately out of scope and recommended as a follow-up backlog card; until it lands, `MOAI_HOME` is effectively Go-process-only.
  - 16 acceptance criteria (AC-MHP-001..016 — the Tier M ceiling), all PASS: 14 verified in the run phase, and the two doc checks discharged by this sync commit (AC-MHP-013 — REQ-MHP-012 supersession note present in spec.md with SPEC-V3R6-SESSION-HANDOFF-AUTO-001 in `related_specs`; AC-MHP-014 — the §4 hazard recorded in this entry). Coverage per run-phase §E.2: `go test -cover ./internal/paths/` → **100.0%**. Run-phase per-milestone commits M1 `6600bfd8e` → M2 `da85d179c` → M3 `51a587bb0` → M4 `2a598a877` → M5 `d50a54ba9` → M6 evidence `d50725c85`; merged into the `release/v3.1.1` integration lane at merge commit `e518fc897` (user-directed lane routing — no per-phase PR; this commit ships with the next lane push). This sync commit carries only the frontmatter `in-progress → completed` transition on `spec.md` (allowed scope: `status:` + `updated:`), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). `sync_commit_sha` populated as a `pending-backfill-SPEC-V3R6-MOAI-HOME-PATHS-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. 🗿 MoAI

- **[SPEC-V3R6-MOAI-CLEAN-HOME-001](.moai/specs/SPEC-V3R6-MOAI-CLEAN-HOME-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`moai doctor` gains a `Home Disk Usage` advisory check and `moai clean` gains a `--home` flag, giving `~/.moai` both visibility and safe cleanup of its regenerable artifacts (aged release binaries, per-profile `debug/` logs, aged backups).** Before this, the existing `moai clean` was project-scoped (`.moai/state/runs/` only) and `moai doctor`'s check lineup had no disk check at all — `~/.moai` accumulated old release binaries, profile debug logs, and aged backup directories with no surface to see them and no safe command to remove them. The home root resolves through `internal/paths.MoaiHome()` (the dependency SPEC-V3R6-MOAI-HOME-PATHS-001 foundation), so an absolute `MOAI_HOME` override redirects both surfaces uniformly (AC-MCH-012 redirect test: aged entries under the override root are deleted while the default-`HOME` fixture stays untouched).
  - **M1 — the doctor check (`internal/cli/doctor_disk.go`, registered in `internal/cli/doctor.go`).** `checkHomeDisk` reports the `~/.moai` top-level breakdown (top 3 by size), per-profile sizes under `claude-profiles/`, cross-profile duplicate clusters, the `releases/` count vs the current version, and a cleanable-bytes estimate. Duplicate clusters use the resolved D1 heuristic — a category name whose (total size, file count) signature is carried identically by two or more profiles — with no content hashing; false positives are accepted by design because the finding is report-only. `plugins/` directories are reported by the cluster detection but never touched by `clean --home` (REQ-MCH-006 single-home protection). The cleanable estimate is computed by the SAME scanner `clean --home` deletes by, under the home-tier retention, so doctor and clean cannot disagree. Above `DefaultHomeDiskWarnBytes` the check emits WARN with a `moai clean --home` pointer. `~/.claude` appears only as a summary line marked `(report-only — moai clean never touches it)`.
  - **M2/M3 — `moai clean --home` + the carve-out invariant (`internal/cli/clean_home.go`, `internal/cli/clean.go`).** Dry-run by default (lists what would be deleted with sizes); `--force` deletes. Scope is `~/.moai` only — `~/.claude` is never mutated. Deletion candidates come exclusively from an explicit four-category allowlist: per-profile `debug/` entries older than retention, `releases/` binaries beyond the current version plus the `DefaultReleaseKeep` (3) newest (sidecar `.sha256` files follow their binary), root `logs/` files older than retention, and `backups/removed-*` directories older than retention — everything not enumerated is invisible to the scanner. The `isCarvedOut` path-predicate guard enforces the REQ-MCH-005 invariant with recursive any-segment semantics: carved-out names (`projects/`, `config/`, `state/`, `credentials*`, `launch.yaml`, `preferences.yaml`, `worktrees/`, `mcp/`, `bin/`, `search/`, `studio/`, `plugins/`) match at ANY depth — root level and per-profile alike — and the carve-out WINS inside allowlisted containers, so a `credentials*`-named file inside an aged `backups/removed-*` directory is never deleted, even under `--force`; an aged backup containing any carved-out entry skips the WHOLE directory (audit residual R1: whole-dir skip, never a partially-deleted restorable snapshot). Walk errors are treated as carved-out — unreadable means unverified, and unverified never deletes. `os.RemoveAll` fires only on scanner-produced candidates (`@MX:WARN`-tagged deletion sites). The project-scope `clean` path (`stateYAMLWrapper` in `clean.go`) is byte-untouched — its SPEC-V3R2-RT-004 contract is pinned by the AC-MCH-009 regression test.
  - **Retention — home tier only (`internal/config/defaults.go`, `clean_home.go`).** `state.home_retention_days` is read from the HOME tier — `~/.moai/config/sections/state.yaml` resolved via `paths.UserConfigSectionsDir()` — never from a project-tier state.yaml (cwd-dependent reads would let two projects clean one home with different windows). An absent key defaults to `DefaultHomeCleanRetentionDays` (30); an explicit `0` disables cleaning. The three thresholds live as constants in `defaults.go` (`DefaultHomeDiskWarnBytes`, `DefaultHomeCleanRetentionDays`, `DefaultReleaseKeep`) with zero inline literals in the check or clean sources (AC-MCH-011 grep: 0 matches). A `*int` pointer distinguishes an explicit `0` from an absent key.
  - 12 acceptance criteria (AC-MCH-001..012), all PASS. Run-phase per-milestone commits M1 `051a2fa94` → M2 `6ec0ad212` → M3 `a42941195` → M4 `46ed09a66` (evidence + `run_commit_sha` backfill `1b9440595`) on the `release/v3.1.1` integration lane (user-directed lane routing — no per-phase PR). Coverage per run-phase §E.2: **87.6% weighted across the two NEW files** (276/315 statements; `go tool cover -func` per-function `clean_home.go` 75.0–92.1%, `doctor_disk.go` 50.0–100.0%). AC-MCH-010 disclosed gap: the unscoped `go test ./internal/cli/` run exits 1 on `TestHandleCodexReviewGate_LiveCodexBlocksInjectionAndKey`, a pre-existing test that shells out to the real codex backend — non-intersecting with this SPEC (the 17-file run diff contains no codex file; reproduced 2/2 in isolation; identical in cause to the pre-run state) and skipped in CI, which lacks a working codex. Everything else in both packages passes. This sync commit carries only the frontmatter `in-progress → completed` transition on `spec.md` (allowed scope: `status:` + `updated:` — already 2026-08-17), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only; no separate doc-check ACs exist for this SPEC — this entry is the docs surface). `sync_commit_sha` populated as a `pending-backfill-SPEC-V3R6-MOAI-CLEAN-HOME-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. 🗿 MoAI

- **[SPEC-ALWAYS-LOADED-DIET-001](.moai/specs/SPEC-ALWAYS-LOADED-DIET-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The always-loaded context surface was put on a measured diet — 295,044 → 288,975 bytes, guard headroom 1,239 → 2,757 tokens — and two structural countermeasures landed with it: five prompt-cache discipline directives folded into the always-loaded rule set, and a recurrence control that turns silent re-growth into a stated, sized cost.** Before this, the mechanical guard (`TestAlwaysLoadedTokenBudget`, budget 75,000 tokens) had 1.65% headroom left (1,239 tokens ≈ 4,956 bytes), and the surface had a demonstrated recurrence pattern — roughly 4x growth in the three months after the previous reduction, about half of it existing files expanding in place. The diet is deliberately conservative: the always-loaded file count stays 14 (files were split and scoped, not deleted), the budget constant is untouched, and zero Go files changed — the mechanism is `paths:`-scoped companions, the pattern the existing rule set already uses.
  - **M1 — `kanban-dispatch.md` split into an always-loaded stub + a paths-scoped companion.** The six lead-only sections (The board; Entry into the board is an operator act; Card classes; The dispatch cycle including Dispatch language; Review lens selection; The `/clear` handoff) moved verbatim to the new `kanban-dispatch-detail.md`, keyed to load only when a kanban-dispatch file, `manager-kanban.md`, or `workflows/todo.md` is touched. The stub keeps every section that binds non-lead sessions, plus the Class B paragraph relocated whole-line into "Completion is read, never trusted" so the one card-class rule a companion session might rely on stays always-loaded. Measured: stub 13,027 B + companion 8,866 B = 21,893 B against the pre-split file (890 B split overhead); HARD-clause census preserved (6 + 3 = 9); headroom 1,239 → 3,233 tokens after M1.
  - **M2 — G3-G7 cache disciplines folded into `cache-aware-execution.md` as directives 6-10, with a cited-numbers reference companion.** Five one-paragraph `[ZONE:Evolvable] [HARD]` directives in the existing 1-5 shape: pass files by `@`-mention rather than name-citation (`/context` as a one-shot audit, not a routine check); keep command output bounded (quiet flags, targeted queries, redirect-to-file with exit code + bounded tail — `BASH_MAX_OUTPUT_LENGTH` is the runtime backstop, not the target); prefer the quiet form of routine commands; weigh session length as a cost axis (a fresh session re-pays the always-loaded prefix at write price); and a mid-session model or effort switch busts the cache. The new `cache-aware-execution-reference.md` (self-keyed to the parent rule) carries the cited cache numbers — explicitly quoted source values, never restated as measurements of this project — plus per-directive rationale and the axis resolution against Per-Spawn Model Injection (main-session cache vs subagent model selection: different axes, neither SSOT revises the other). Growth +1,907 B, inside the plan's [1,000, 2,000] byte window.
  - **M3 — `rule-authoring.md` recurrence control (new, paths-scoped).** A statement duty binding all four guard slots (unscoped rule files, `CLAUDE.md`, output styles, `MEMORY.md` head): creating a new always-loaded file, or a single edit growing one by more than 1,000 bytes, requires stating the size and a cost justification that addresses what sessions that never need the file pay every turn and after every `/clear` — with scope-first (can this live under a `paths:` companion instead?) asked before either duty fires. Threshold calibration table included; deliberately no cumulative-delta secondary trigger (accepted residual risk, stated as such rather than patched). The rule carries its own top-level `paths:` key, so the recurrence control is excluded from the surface it polices — the control must not itself be recurrence.
  - **M4 — all 5 rule files mirrored to templates.** The two modified and three new rule files are mirrored byte-identical to `internal/template/templates/` (sources authored neutral, so no sanitization was needed — scan evidence in progress.md §E.2 M4), `make build` regenerates the embedded catalog, and the three template-neutrality CI targets pass locally in isolation exactly as CI runs them.
  - 16 acceptance criteria (AC-ALD-001..016), all PASS, verified against the run-phase implementation at PR #1577 squash `67466e997d` on origin/main (per-milestone commits M1 `a203a7c3a` → M2 `6d701d26c` → M3 `fcc07d1bc` → M4 `9269a5e56` preserved inside that squash). Final state measured by both the authoritative guard (`go test ./internal/config/ -run TestAlwaysLoaded` green; budget constant unchanged) and the frontmatter-aware auxiliary: `files=14 bytes=288975 tokens=72243 headroom=2757`. Zero Go files changed across the entire run phase. This sync commit carries only the frontmatter `in-progress → completed` terminal transition on `spec.md` (sole YAML-frontmatter artifact — plan.md / acceptance.md / progress.md use the markdown-header convention), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further changes (sync is markdown-only). README / docs-site unchanged — this is internal rule architecture with no user-facing surface (no CLI, no config key, no documented behavior change; the rules govern agent-side context discipline). `sync_commit_sha` populated as a `pending-backfill-*` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **Factory Mode.** `moai cc -f <N>` (or `moai glm -f <N>`) launches a lead session plus N numbered lanes (`lane-1`..`lane-N`); each lane carries one whole card through plan, run, and sync in a single session. `moai cc -f lane-<n>` re-enters an individual lane. The kanban chain keeps `-k`.
- **`moai graph build` and `moai graph query`.** Build a call-graph edge index (`edges.jsonl`) over the repository, then query it for callers and blast radius. `graph build` also reports milestone-to-card cross-check edges and can list milestones with no card via `--milestones-no-card`.
- **`moai memory doctor` and `moai memory archive`.** Diagnose the memory store (broken links, orphaned topic files) and archive superseded entries.
- **`moai tokens record`.** Record token usage into the per-pool ledger.
- **`moai todo` gains `drop`, `undrop`, `edit`, `move`, and `unpick`**, plus a bare `moai todo` list form and natural-language `add`.
- **New MCP tools for GLM delegation and cross-model audit.** `glm_task`, `glm_job_status`, `glm_job_result`, and `glm_job_cancel` delegate a task to the z.ai backend as a synchronous or background job; `glm_audit` and `audit_multi` add single-backend and multi-backend audit convergence.
- **Config section `crosssession.yaml`.** `inbound` (`""` / `accept` / `hold` / `refuse`) controls how this session treats messages from your other Claude Code sessions; `isolate_machines` requires explicit approval before a message leaves the machine; `dialog_expiry` bounds how long a held message waits.
- **Config section `cache.yaml`.** `cacheStrategy.enabled`, `cacheStrategy.session_ttl` (`1h` / `5m` / `off`), and `cacheStrategy.spec_ttl` (`5m` / `off`) control Anthropic prompt-cache breakpoint injection, alongside a minimum cacheable segment size. GLM caches implicitly and is unaffected.
- **`statusline.forge`.** Select the forge the statusline reports items from — `github`, `gitlab`, or `none`.
- **A bare `/loop` now runs the kanban foreman.** Each iteration watches the backlog queue, dispatches the next operator-picked card to an isolated worker, and reports on evidence read rather than on claims.
- **`moai-domain-design-dna` skill.** Reverse-engineer a reference design — screenshot, image set, or URL — into a portable Design DNA JSON, then generate a new self-contained artifact from it.
- **Six ast-grep rule files now ship with the templates** under `.moai/config/astgrep-rules/{go,security}/`, so `moai init` and `moai update` deploy a working baseline ruleset instead of a single example rule.
- **`MOAI_SESSION_PID`** is stamped into launched sessions on POSIX platforms (Windows launches do not set it).
- **`ANTHROPIC_DEFAULT_MODEL`** is now a recognized environment key.

### Changed

- **`gate.ast_grep_gate.rules_dir` now defaults to `.moai/config/astgrep-rules`** instead of `""`, and the shipped config is the sole source of truth — the code-side fallback path is gone. A project that relied on the empty-value fallback must set the key explicitly.
- **`manager-kanban` is `manager-lead` again**, reversing the v3.1.0 rename. The one agent now covers both the kanban lead and the factory lead session. Any local prompt, script, or config naming `manager-kanban` must be updated; the old name no longer resolves.
- **The kanban board has five columns, not six.** The `review` column is gone — the sync gate absorbs the review verdict.
- **Companion sessions are named by bare role** (`plan`, `run`, `sync`, collision-bumped when duplicated), and factory lanes are named `lane-<N>`.
- **GLM-5.3 is recognized as a 1M-context model** on every surface that reports a context window, rather than being treated as a 200K session.
- **The orchestration mode catalog is renamed onto the spawn-count axis**, going from six modes to four: `direct`, `serial`, `fanout`, `sweep`. Configuration referring to the previous names needs updating.

### Fixed

- **`moai update` backs up unmanaged files before removing managed roots.** A template redeploy previously wiped managed roots first, silently dropping local-only files that had no template counterpart.
- **A raw `*exec.ExitError` is rejected at the exit-code seam.** It had been structurally satisfying the `ExitCoder` interface, so a subprocess's exit code passed through as the CLI's own and its output was silently dropped.
- **Installer scripts resolve the latest version without the rate-limited REST API**, using the `/releases/latest` redirect instead, and `install.bat` now ships with CRLF line endings so `cmd.exe` runs it.
- **`moai mcp` is registered on the root command** and is reachable again.
- **Worktree and hook correctness.** `moai worktree done` now surfaces git's stderr and points at the lock when removal fails instead of exiting silently; anchor guards protect the session's own worktree during remove, clean, and post-PR-merge cleanup; and `isolation: worktree` agent spawns no longer fail with zero diagnostics on a non-repository cwd.
- **`/moai gate` resolves the Node test step to a run-form command**, preferring `test:run` over a watch-mode runner and dropping stray flags.
- **An armed goal now disarms** when a turn dies on an unrecoverable API error, instead of leaving the session looping against a condition nothing is advancing.

## [3.1.0] - 2026-08-15

### Summary

v3.1.0 promotes the `v3.1.0-rc.0` → `-rc.1` → `-rc.2` candidate line to a production release. Three themes dominate it.

**Kanban Mode** is the headline addition: a six-column board (`backlog → plan → run → review → sync → done`) driven by a lead session that dispatches cards to operator-launched companion sessions. The mode arrives with a single-origin board state store that never writes into SPEC frontmatter, a `moai todo` CLI for the backlog queue, a lead session name that survives `/clear`, and an operator-language bootstrap notice. The former `manager-lead` agent is renamed `manager-kanban`, and the launcher flag `-f/--factory` is renamed `-k/--kanban`.

**The `moai web` console** completes its redesign: the transitional settings and board layers are retired into the shell, the console chrome is wired and translated across all four locales, and the schema-driven form renders closed-set fields as closed widgets.

**Model policy and hooks** are tightened: the effort matrix is phase-weighted (plan at the top, run/review at high, sync at low), GLM model tiers collapse to one model per Claude slot, the branch-guard scan is scoped to the command being invoked rather than to text it carries as data, and `moai update` derives a merge base so template additions actually reach existing projects.

### Changed

- **The launcher flag `-f` / `--factory` is renamed `-k` / `--kanban`.** The rename is complete rather than aliased — package, environment variables, functions, and file names all move — so a script invoking `moai cc -f` must switch to `moai cc -k`. "Factory" described an earlier design that the six-column board replaced; keeping the old name would have meant the flag and the feature disagreed.
- **Internal documentation staleness rewrite.** Four stale references retired, three re-anchored, five confirmed live.
- **The effort matrix is phase-weighted, and the GLM session reasoning default is lowered.** Effort is now routed by workflow phase rather than applied uniformly: plan runs at the top level (the phase where a wrong decision is most expensive to unwind), run and review at high, and sync steps down to low — sync is largely mechanical, so paying peak reasoning there bought nothing. The GLM session reasoning default drops in the same direction. Model and effort continue to resolve through `moai model profile`; no agent file hardcodes either.
- **GLM model tiers collapse to one model per Claude slot.** The tier table previously allowed several GLM models to share a slot, which made the resolved model depend on ordering rather than on the slot. Each Claude slot (Opus / Sonnet / Haiku / Fable) now maps to exactly one GLM model.
- **The `moai web` console retires its transitional layers.** Settings fold into the shell and the board view folds into `/specs`, removing the two-layer arrangement that survived the console redesign as scaffolding. The console shell chrome is wired and translated, so the navigation, headers, and status surfaces render in the operator's locale rather than falling back to English.

- **`manager-lead` renamed to `manager-kanban`.** The coordination agent now carries two roles over one skill set rather than one: **Role A** is the existing in-session hierarchical fan-out at Tier L (unchanged — same entry threshold, same depth-2 seal, same context-folding procedure), and **Role B** is the new cross-session kanban-lead role that moves a card across the six-column board by instructing operator-launched companion sessions. The rename is mechanical and complete across live wiring: the agent file, `CLAUDE.md` §4, `.moai/config/sections/workflow.yaml`, the harness delegation map and v4 manifest, the agent-lint surface, the depth-seal CI guard (`manager_lead_depth_test.go` → `manager_kanban_depth_test.go`), README (en/ja/zh), and the docs-site page in all four locales (`advanced/manager-lead` → `advanced/manager-kanban`, with permanent redirects added to `docs-site/vercel.json` so existing links keep resolving). Historical records are deliberately NOT rewritten — prior CHANGELOG entries and closed SPEC bodies keep the name that was actually shipped at the time, because rewriting them would misstate what a given release contained.

### Added

- **`moai todo` — the backlog CLI behind `/moai todo`, with a lock-guarded store.** The slash surface needed a non-interactive counterpart so the queue could be read and written from a script, a hook, or a companion session that has no `AskUserQuestion` channel. `moai todo` supplies it: append, list, and remove operate on the same project-local queue, guarded by a lock so two sessions on one checkout cannot interleave a read-modify-write and lose an entry. A malformed store is reported and left untouched rather than reset — operator intent is the one thing here that cannot be regenerated.
- **The six-column kanban board, with a single-origin state store.** Column assignments live in exactly one file that only the `lead` role may write, so a card's position is never a second datum competing with SPEC frontmatter. The store resolves relative to the git common directory (so every worktree of a checkout sees the same board), writes atomically, treats an absent file as the empty board, and clears a stale lock only when the holder is provably dead — an indeterminate liveness result is treated as live, because guessing "dead" is what unlinks a live holder's lock.
- **The kanban lead session is named, so it survives `/clear`.** The lead's run id is derived from the session name, and an operator-supplied name is adopted rather than overridden. A lead that clears its context can therefore still be addressed by companion sessions under the same name, which is what makes the between-phase `/clear` in the dispatch protocol safe to perform.

- **`/moai todo` — the backlog queue, and the kanban dispatch protocol that consumes it.** The six-column board (`backlog → plan → run → review → sync → done`) leaves `backlog` with no owning session by design: nothing dispatches work *into* the board, so admission has to be an operator act — and until now no surface performed it. `/moai todo` is that surface: `"<description>"` appends, a bare invocation lists, `next` presents the queue through `AskUserQuestion` for the operator to pick, and `done <n>` removes. State is a thin project-local queue at `.moai/state/kanban/backlog.json` (atomic writes; a malformed file is reported and left untouched rather than reset, since operator intent is the one thing here that cannot be regenerated). It holds intent only — no priorities, assignees, or due dates, and it is never auto-populated from TODO comments, open issues, or audit findings.
  - **`kanban-dispatch.md` (new always-loaded rule)** specifies what the lead session does with a picked card: dispatch by session name to the companion owning each column, choose review lenses from what the card actually changed (`--security` / `--deep` for reachable-by-untrusted-input surfaces, `--lean` for suspected over-engineering, no flag for a small local diff), and advance the card **only on evidence it read from `progress.md`** — never on a companion's reply, since inter-session replies are claims rather than observations and are not guaranteed to arrive. Between phases the lead asks the operator to `/clear` the named session (a user-typed command that cannot be sent as an instruction), stating what closed, which session to clear, and what happens next. A column whose owning role has no live session is reported as a fault rather than held silently, because a stalled board with no error is the most expensive failure shape to diagnose.
  - Explicitly out of scope, and stated as such rather than implied: persistent six-column board state, per-card worktree lifecycle, WIP limits, and card/frontmatter consistency reconciliation. The lead spawns nothing — it addresses sessions the operator launched — and being inside a dispatch cycle bypasses no approval gate.

- **[SPEC-WEB-CONSOLE-REDESIGN-001](.moai/specs/SPEC-WEB-CONSOLE-REDESIGN-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The `moai web` settings console was redesigned around a schema-driven form whose widgets tell the truth about the values they accept: dead config was removed from the edit surface, the console was restructured from 6 tabs into 9, closed-set fields now render as closed widgets (select/radio) instead of free text, the GLM tab honestly names the applied source of reasoning effort, the profile UI was de-duplicated, and the incomplete `/autonomy/tiers` stub was removed.** Before this, the form rendered whatever the schema declared but lied in three ways: it offered 7 fields nothing consumed (`workflow.token_budget.{plan,run,sync}`, `workflow.auto_clear.{enabled,after_plan,after_run,token_threshold}` — accessors preserved for backward compat, the edit surface dropped); the `git_strategy` section had FieldDefs registered but no render meta, so `mode` and the three per-profile `merge_method` controls had no UI at all; and every bool was a checkbox / every closed-set field was free text, so a user could type a value the server would silently reject. The redesign closes those gaps and records a structural-coverage ceiling as debt rather than padding toward 90% with tests that assert nothing. Run-phase landed on main as PR #1410 squash `b0d3b61f8` (2026-08-08).
  - **M1 — dead config retired + git_strategy render surface restored (REQ-WCR-001..004).** The 7 dead-config `FieldDef`s are removed from the web edit schema. The yaml keys, `internal/config` struct members, and accessors are preserved verbatim — `TestWorkflowBackwardCompat` pins that an existing `workflow.yaml` carrying both retired blocks still loads and the accessors still return its values (no user's config breaks). `git_strategy.mode` and the three per-profile `merge_method` controls are rendered through a new `git-worktree` panel; the section id is registered in `SchemaSectionIDs()` and now has render meta to match.
  - **M2 — tab restructure 6→9 (REQ-WCR-010..013).** `consoleTabs()` returns 9 entries: `identity` / `language` / `launch` (LLM) / `llm` (3rd Party LLM) / `workflow` / `git-worktree` / `audit` / `agentfm` / `report`. The overloaded Workflow tab is split: `workflow.worktree.*` + `branch_guard.enabled` move to the new Git & Worktree tab; `workflow.audit.*` moves to the new Audit tab. The move is render-placement only — `FieldDef.Section` stays `SectionWorkflow` and the persist target stays `workflow.yaml`, so no write route changes. The tab list is now the single source of panel order (the previous hand-written panel sequence had drifted from the tab order — `report` rendered before `agentfm`).
  - **M3 — widget honesty policy (REQ-WCR-020..023).** Every bool field is now a 2-option radio group (used / not-used) instead of a checkbox, changed in one place (`schemaToggleRow`) so it applies to every tab. The hidden companion `<name>__present` input is preserved so a submitted-but-empty bool still parses as "value retained", not "value cleared". Closed-set fields (`workflow.execution_mode`, `workflow.default_mode`, `workflow.audit.model`, `workflow.audit.gates.{claude,codex,glm}`, `harness.*`) render as select/radio with membership validation — a save request carrying an out-of-set value is rejected at the apply layer. Genuinely open fields (`feedback.repository`, `observability.{report_dir,trace_dir}`, GLM API key) remain free text.
  - **M4 — 3rd Party LLM tab (REQ-WCR-030..034).** `llm.glm.models.{high,medium,low,fable}` render as selects over `{glm-5.2, glm-5.1, glm-4.7, glm-4.5-air}` (derived from `DefaultGLM*` constants — zero literal re-declaration). Each tier carries a reasoning-effort select over `{Max, High, None}` (derived from `template.GLMReasoningStateNames()`). Tier labels read Opus / Sonnet / Haiku / Fable (i18n titles); internal form keys `high` / `medium` / `low` / `fable` stay unchanged. **GLM honesty badge (F4a correction):** the reasoning-effort runtime channel is a single session-level env var, so per-tier effort values are *store-only*. The panel renders a badge naming the **applied source** (the session-level `effort_level` collapsed to z.ai's 3-state) rather than implying a tier's effort is applied at runtime. GLM API key never echoes by default (`value=""` always); a reveal toggle fetches plaintext once server-side and is refused for non-loopback origins.
  - **M5 — profile UI dedup (REQ-WCR-040..042).** The standalone `profileManager` card is removed. Add / rename / delete controls sit adjacent to the profile bar; each is its own POST form placed *outside* the main settings form (nested forms are invalid HTML). A new rename handler rejects default profiles, the currently active profile, name collisions, and path-traversal targets, with the reason surfaced as a banner. `profileSwitch` (the select + "selected: X" header) is preserved.
  - **M6 — autonomy stub removed (REQ-WCR-050).** The bare `<a href="/autonomy/tiers">` link injected before `</body>`, the GET-only `handleAutonomyTiers` handler with no form/action, and the `/autonomy/tiers` route are removed. The init-path `ApplyAutonomyTierBundle` writer and the `MOAI_AUTONOMY_TIER` env seam are preserved (console surface only removed). Removal rationale recorded in plan.md §B D3.
  - **`workflow.execution_mode` closed-set correction (M3 residual, §E.2.1).** M3 had declared `config.ValidExecutionModes()` as `{auto, solo, team}` — an inference with no SSOT basis that omitted `cg` (the fourth documented mode in `harness.yaml` `mode_defaults` and `SPEC-V3R2-HRN-001` REQ-HRN-001-014). Because M3 converted the field to a closed widget, the omission was a regression: a documented mode was unselectable. Fixed by splitting `ValidExecutionModePins()` = `{solo, team, cg}` and `ValidExecutionModes()` = `auto` + pins, with the `harness.mode_defaults.*` FieldDef list derived from the same accessor so the two sets cannot drift.
  - **AC-WCR-063 — PASS-WITH-DEBT (user-approved 2026-08-13).** Coverage measured **73.5%** against a 90% bar. 83% of the package's statements (4323/5208) are `templ generate` output; 927 of the uncovered statements (17.8% of the package) are unreachable `if templ_err != nil { return }` / `ctx.Err()` / `NopComponent` boilerplate that templ emits after every write — unreachable when rendering into `strings.Builder` / `httptest` buffers. **Structural ceiling: 80.7%** even with every real branch covered. Hand-written code alone is **91.1%** (already meets a 90% hand-written-code bar). The honest fix is to redefine AC-WCR-063's denominator (exclude `*_templ.go` generated output, or redefine as hand-written-code ≥ 90%) — **that SPEC-body change is manager-spec's domain and is recorded as the DEBT**, to be resolved in a follow-up. Full rationale at `progress.md §E.2.3`.
  - 28 acceptance criteria (AC-WCR-001..AC-WCR-063 — 25 numbered, 23 PASS / 1 FAIL→PASS-WITH-DEBT (AC-WCR-063) / 2 PASS-WITH-DEBT (AC-WCR-023 whitelist, AC-WCR-030 grep-count) / 2 vacuous PASS (AC-WCR-061/062 — no yaml or template edit touched by this SPEC, so the mirror / neutrality gates have no surface to evaluate). Run-phase plan+run work landed on main as PR #1410 squash `b0d3b61f8` (2026-08-08); the per-milestone commits M1 `c9406abf5` → M2 → M3 → execution-mode correction → M4 `62629d7b0` → M5 `e5efa49da` → M6 `a9995c80b` + coverage characterization are preserved inside that squash. Orchestrator trust-but-verify (this tree, this run): `go build ./internal/web/... ./internal/cli/...` exit 0; `go vet ./internal/web/...` exit 0; `go test ./internal/web/...` → `ok 1.728s`. Repo-wide `go test ./...` has 3 PRE-EXISTING failures in `internal/template` (`TestLateBranchTemplateMirror`, `TestRuleTemplateMirrorDrift`, `TestSanitizedPairParity`) unrelated to this SPEC and unchanged by it (the same three fail on the pre-PR baseline). This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — `acceptance.md` / `plan.md` use the markdown-header convention), the `progress.md §E.4` audit-ready signal, the AC-WCR-063 PASS-WITH-DEBT marker in `acceptance.md`, the docs-site 4-locale `moai-web-console.md` + `cli-reference/web.md` refresh (6-tab → 9-tab, profile-card → profile-bar, widget policy, GLM honesty badge), and this CHANGELOG entry — zero further code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CHAIN-CORE-001](.moai/specs/SPEC-CHAIN-CORE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **An append-only JSONL lineage tree at `.moai/state/chain/events.jsonl` now records worktree spawn boundaries, session_id backfills, and completion edges as named fields on a `WorktreeNode` record — structurally solving the depth-2–3 context-loss pain where the maintainer loses track of origin, what just completed, and where to resume after `/clear`.** Before this, every persistence primitive was FLAT: the session registry `Entry` carried `CWD` as its only worktree link (no parent, no depth, no origin chain); the handoff `pending.json` was singular per-project and overwritten on each save (a depth-3 save clobbered the depth-1 record); the worktree `Snapshot` captured only divergence state. The three pain questions become named fields on a structured record — **Origin** → `origin_chain` + `depth` + `parent_node_id`; **What just completed** → `last_completed_milestone` + completion edges; **Where to resume** → `resume_target` + `resume_command`. This is Phase 1 of the Origin-Trail Chain Epic; it is independently shippable with no dependency on kanban mode (`-k`), factory mode (`-f`), or the manager-lead agent (those are later phases P3–P7).
  - **M0 — WorktreeNode struct + append-only JSONL store (`internal/chain/`).** `WorktreeNode` (13 named fields: NodeID, ParentNodeID, Depth, OriginChain, WorktreePath, SessionID, SpecID, Milestone, EnteredAt, ExitedAt, LastCompletedMilestone, ResumeTarget, ResumeCommand — REQ-CHAIN-001) is derived at read time from a flat `ChainEvent` stream (three event types: `node-enter`, `node-update`, `completion-edge`). `Store.Append` opens with `O_APPEND|O_CREATE|O_WRONLY` + `0o600` and NEVER overwrites, truncates, or seeks-replace (REQ-CHAIN-002). `Store.ReadAll` skips corrupt JSONL lines with a diagnostic warning identifying the line number, and a missing file returns an empty slice, not an error (REQ-CHAIN-003). `Store.ResolveByCWD` resolves the current node via the `(worktree_path, session_id)` pair (REQ-CHAIN-004).
  - **M1 — spawn-boundary population + env constant + session_id backfill.** `Populator.CreateNodeAtSpawn` reads `MOAI_CHAIN_NODE_ID` from the spawning context's environment, derives `depth = parent_depth + 1`, builds `origin_chain = parent_chain + [nodeID]`, and appends a skeleton `node-enter` event with `session_id` empty (REQ-CHAIN-005). `EnvChainNodeID = "MOAI_CHAIN_NODE_ID"` is exposed as a constant in `internal/config/envkeys.go` (REQ-CHAIN-006) — every code site that reads the variable references the constant, never the inline string. The REQ-CHAIN-021 two-phase backfill: a skeleton node created at spawn is backfilled with the child's runtime-assigned `session_id` via a `node-update` event when the child's SessionStart fires — the precondition for REQ-CHAIN-013's `(worktree_path, session_id)` re-resolution after `/clear`.
  - **M2 — `moai chain` CLI subcommands (`internal/cli/chain.go`).** Five subcommands registered under a new `chain` parent cobra command: `moai chain status` (current node summary: depth, parent, spec, milestone, resume_target — REQ-CHAIN-007), `moai chain lineage` (root-to-leaf origin chain with each ancestor's path/spec/milestone/entered_at — REQ-CHAIN-008), `moai chain back` (parent node's resume_target + resume_command — REQ-CHAIN-009), `moai chain list` (all nodes with depth, session_id, staleness status — REQ-CHAIN-010), `moai chain prune [--dry-run]` (fold old exited nodes into archive — REQ-CHAIN-011). Staleness classification (`classifyStaleness`) overlays on the session registry's `LastHeartbeat` via a read-only join (threshold 15 min, REQ-CHAIN-015); the registry `Entry` struct is NEVER mutated (REQ-CHAIN-014 overlay-only). Single-host v1 limitation surfaced when a remote CWD is detected (REQ-CHAIN-016).
  - **M3 — SessionStart lineage banner (`internal/hook/chain_banner.go`).** `chainLineageBanner` is wired into the existing `sessionStartHandler.Handle` flow (`session_start.go:361`). When `MOAI_CHAIN_NODE_ID` is absent from the environment (post-`/clear` env loss), the handler resolves the node from the ledger via `(worktree_path, session_id)`, re-injects the node ID, and emits a lineage system-reminder containing depth, an abbreviated parent chain (N0→N1→N2), spec, milestone, last-completed, and resume_target (REQ-CHAIN-013). The handler is time-boxed via `context.WithTimeout` (2 s) and degrades to a no-op banner on timeout — fail-open (REQ-CHAIN-013 time-box + AC-CHAIN-014).
  - **M4 — mechanical completion hook (`internal/hook/chain_event.go` + `.claude/hooks/moai/chain-event.sh`).** `RunChainEvent` reads a SubagentStop hook payload from stdin, resolves the child node, and appends a `completion-edge` event `{parent_node, child_node, completed_milestone, completed_at, next_resume_target}` to the ledger without requiring any leaf-agent cooperation — the ledger stays current even if the spawning session has crashed or been cleared (REQ-CHAIN-012). The hook is fail-open: any error (missing store, unresolvable node, malformed payload) is a silent no-op (exit 0). The shell wrapper `chain-event.sh` resolves the `moai` binary across 3 tiers and always exits 0.
  - **M5 — compaction / prune (`internal/chain/prune.go`).** `Store.Prune` folds exited nodes older than the threshold (default: 30 days OR 10 MB, whichever fires first) into a `node-summary.json` snapshot under `.moai/state/chain/archived/`, backs up the original `events.jsonl` to `archived/events-{timestamp}.jsonl`, and rewrites the active file with only kept events (REQ-CHAIN-011). This is the ONE exception to the append-only invariant — a maintenance compaction invoked only by the explicit user-facing `moai chain prune` command. `--dry-run` is the default (no files modified).
  - **M6 — template-mirror + `make build` + count fixes.** The `chain-event.sh` hook wrapper is mirrored to `internal/template/templates/.claude/hooks/moai/chain-event.sh`, and `make build` regenerates the embedded catalog. The template source passes the neutrality CI guard (no SPEC IDs, no commit SHAs, no macOS-bias paths — REQ-CHAIN-019 / AC-CHAIN-020).
  - **Overlay join, Entry frozen (REQ-CHAIN-014 / AC-CHAIN-024 MUST-PASS).** The chain ledger NEVER mutates the session registry `Entry` struct. Lineage data is overlaid by joining on `SessionID` at read time only, respecting the REQ-COORD-024 schema freeze. `Entry` retains exactly its 8 frozen fields — zero chain-specific fields (ChainNodeID, OriginChain, Depth, ParentNodeID, ResumeTarget) are present.
  - **Flag-agnostic + subagent boundary (REQ-CHAIN-017 / REQ-CHAIN-018).** The chain record, store, CLI, and hook function independently of `-k`, `-f`, and the `manager-lead` agent — `grep` for these tokens in chain scope returns 0 matches (AC-CHAIN-018). The chain CLI and hook do NOT invoke `AskUserQuestion` or `mcp__askuser__*` — the orchestrator-only interaction boundary is preserved (AC-CHAIN-019).
  - 24 acceptance criteria (AC-CHAIN-001..024: 14 MUST + 6 SHOULD + 4 NICE-TO-HAVE), all PASS, verified against the run-phase implementation at HEAD `10988db68` of the worktree (per-milestone commits M0 `b7641cc7f` → M1 `ecb41536c` → M2 `3a4b2ca34` → M3 `2781048e3` → M4 `cd70d72b5` → M5 `82b9b87a6` → M6 `bd0a34f37`; progress evidence at `10988db68`). Coverage: `go test -cover ./internal/chain/` → **87.4%** (≥85% threshold). Orchestrator trust-but-verify: `go build ./...` exit 0; cross-platform build (`GOOS=windows GOARCH=amd64 go build ./...`) exit 0; `go vet ./internal/chain/... ./internal/cli/... ./internal/hook/...` clean. MX tags (sync sub-step): `@MX:ANCHOR [AUTO]` on `WorktreeNode` (REQ-CHAIN-001 13-field invariant contract), `NewStore` (3 cross-package callers), and `Populator.ResolveCurrentNode` (5 cross-package callers) — all carrying `@MX:REASON`. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — `acceptance.md` / `plan.md` use the markdown-header convention), the `progress.md §E.4` audit-ready signal, the three `@MX:ANCHOR` additions in `internal/chain/`, and this CHANGELOG entry — zero further functional code changes (MX tags are comments, not functional changes). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. README / docs-site unchanged — the chain CLI is a specialized worktree-lineage query tool; the README "frequently used" CLI table is a curated subset and adding a niche depth-tracking command requires coordinated 4-locale count updates disproportionate to this single-SPEC sync (deferred to a follow-up docs sweep or the Epic-completion sync). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-WORKTREE-BRANCH-GUARD-DISCRIM-001](.moai/specs/SPEC-WORKTREE-BRANCH-GUARD-DISCRIM-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Corrects the Main-Checkout Branch-State Guard discriminant so a Claude-native worktree is no longer misclassified as the primary checkout.** Before this, the guard compared the project root (resolved via the primary-checkout path) against the git-common-dir, so a PreToolUse Bash event fired inside a worktree still resolved to the primary checkout dir and tripped BRANCH_GUARD_VIOLATION on legitimate worktree `git` operations (a rebase or force-push was refused inside the worktree that owns it). The parent SPEC-WORKTREE-BRANCH-GUARD-001 landed the v1.1.0 mechanical enforcer and SPEC-WORKTREE-BRANCH-GUARD-OPTIN-001 added the opt-in gate + pattern refinement; this SPEC owns the directory-correction seam.
  - **M1 — Seam A discriminant queries `input.CWD`, not the project root (REQ-WBG-D-001 / AC-WBG-D-001..008).** `internal/hook/branch_guard.go` `isPrimaryCheckout` now receives the per-event CWD (`input.CWD`, reliably populated by Claude Code for PreToolUse:Bash) and compares the absolute `git rev-parse --git-dir` resolved AT THAT CWD against the absolute `git rev-parse --git-common-dir`; equal paths classify as the primary checkout, differing paths as a worktree. The audit-log dir stays pinned to the primary checkout (REQ-WBG-D-004) so worktree events still record against the shared audit log. New tests `TestBranchGuard_Worktree_Allow` / `_Primary_Still_Denies` / `_AuditLog_Placement` / `_FailOpen_NonGitCwd` / `_Exempt` / `TestIsPrimaryCheckout_Worktree` prove the discriminant admits worktree operations while the primary checkout still refuses branch-state mutations (`git` + `switch`, `checkout`, `reset --hard`, `rebase`). Existing branch-guard tests (Latency, CheckBranchStateOrigin, DeniesGitSwitchInPrimary, FailOpenOnGitError, ConfigGate_FlagFlips) PASS after the existing-test CWD-contract was updated to match Seam A.
  - **E7 blocker: NONE.** The suspected third seam (a path-resolver preferring `$CLAUDE_PROJECT_DIR` over `input.CWD`) was ruled out: `resolveProjectRootFromInputOrEnv` (path_resolve.go:88) prefers `input.CWD` exactly as plan.md §C claims. `input.CWD` IS reliably populated for PreToolUse:Bash (proven by the passing worktree-allow test).
  - **E8 RED gap (documented).** The verbatim pre-GREEN failing-test output was NOT captured: a transient 429 rate limit terminated manager-develop after it authored `branch_guard_worktree_test.go` but before it ran the test against the pre-fix code; the orchestrator then implemented Seam A directly (GREEN-first). The RED driver is documented in the test doc comment; residual-risk: without a captured RED, test-first falsifiability rests on the test's stated logic rather than an observed pre-fix failure output.
  - 8 acceptance criteria (AC-WBG-D-001..008, all MUST), all PASS, verified against the run-phase implementation at PR #1488 squash `61e2c3bc0` on origin/main. Orchestrator trust-but-verify (this tree, this run): `go test ./internal/hook/ -run 'BranchGuard|IsPrimaryCheckout|CheckBranchState|AppendBranchGuard' -count=1` → `ok github.com/modu-ai/moai-adk/internal/hook 12.961s`; `go build ./...` exit 0; `GOOS={windows,linux} GOARCH=amd64 go build ./...` exit 0; `golangci-lint run --timeout=3m ./internal/hook/...` 0 issues; affected-file coverage (branch_guard.go) checkBranchState 100%, isPrimaryCheckout 94.1%, appendBranchGuardAdvisory 94.4% (≥85% bar met); subagent-boundary grep 0 matches. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on spec.md (sole YAML-frontmatter artifact — acceptance.md / plan.md use the markdown-header convention), the progress.md §E.4 audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). README / docs-site unchanged — the branch-state guard is an internal hook with NO user-facing mention in README.md (`grep -rn 'branch.guard|BRANCH_GUARD' README.md` → no matches), so no doc entry is added. §25 template neutrality N/A (internal/hook Go sources + SPEC artifacts only — no `internal/template/templates/**` surface touched, no mirror step, no `make build` required). `sync_commit_sha` populated as `61e2c3bc0` (PR #1488 squash on origin/main = run-phase close anchor). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI
- **[SPEC-MCP-CONSOLE-001](.moai/specs/SPEC-MCP-CONSOLE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The `moai web` console now exposes the full MCP surface — per-tool enablement for all 17 tools, codex auth state, and GLM key state — in the same schema-driven console the rest of the project settings already use, in the user's own language, with no credential written to any git-tracked file.** Before this, SPEC-A provisioned the `moai` MCP server by default and SPEC-B wired agents to its tools, but the server's `registerMoaiMCPTools` had NO per-tool gating seam (a console toggle written today would be read by nothing), the console had no MCP section at all, and the only way to see which tools were live or whether codex was authenticated was to hand-edit JSON or shell out to a CLI. SPEC-C closes the Epic's user-facing loop: the console is now the single place to see and control the entire MCP surface, and every control writes through seams the runtime actually reads. This is the third and final SPEC of the moai-MCP integration Epic (A: server default-on → B: agent wiring → C: console surface).
  - **M1 gating seam — a control that actually controls (REQ-C-2 / AC-C-004).** A new shared declaration `internal/mcp/catalog.go` (`ToolDef{Name, WriteCapable}` + `MoaiMCPTools()`, @MX:ANCHOR-tagged) is the single source of truth for the 17-tool surface, consumed by BOTH the server (registration) and the settings schema (per-tool fields). `registerMoaiMCPTools(s, projectDir)` now reads per-tool enablement once at construction via `readMCPToolEnablement`, and each `add(name, tool, handler)` is gated on `enabled[name]` — a disabled tool is structurally ABSENT from `tools/list`, not merely marked inactive. The reader is fail-OPEN (all-enabled default per owner decision D2), the inverse posture of the codex gates' fail-CLOSED readers, so a missing or malformed `mcp.yaml` never silently disables the server. `TestMoaiMCPServer_RegistrationMatchesCatalog` asserts set-equality between tools/list and the catalog (REQ-C-1 single-declaration guard — a tool added to registration without a catalog entry fails the build).
  - **M2 console surface — all 17 tools, write-capable distinguished in text (REQ-C-1 / REQ-C-3 / AC-C-001..003 + AC-C-005).** A new `SectionMCP` section ID (`internal/settings/schema.go`) routes through the SAME schema-driven form + `yamlpatch` seam `workflow.branch_guard.enabled` and the audit-selection fields already use (`internal/settings/sectionroute.go` / `sectionwrite.go`); `mcpFields()` generates one `mcp.tools.<name>.enabled` bool per catalog tool — there is no second tool list. The `fieldsetMCP` templ component (`internal/web/fieldsets.templ`) renders one control per tool, and the four write-capable tools (`goal_arm`, `verify_snapshot`, `codex_task`, `codex_job_cancel`) carry a marker carried in TEXT (not color alone), so the blast-radius difference is legible at the point of decision and survives a monochrome or screen-reader rendering. `TestMCPConsoleRendersAllTools` / `_ToolCountMatchesCatalog` / `_WriteCapableTextDistinctinction` PASS.
  - **M3 codex auth — probe state surfaced, NOT recomputed (REQ-C-4 / REQ-C-5 / REQ-C-6 / AC-C-006..009).** `internal/cli/mcp_codex.go` exports `ProbeCodexSetup(ctx)` returning a typed `CodexSetupResult`; `handleCodexSetup` delegates to it. The classifier (`classifyCodexAuth`) is untouched — consumed, not forked. `internal/web/codex_state.go` carries the `CodexStateView` view model + a fail-open default probe for bare test apps; `internal/web/handlers.go` / `app.go` / `server.go` wire it via DI; `internal/cli/web.go` adapts the probe for web consumption without importing `internal/cli`. The `codexAuthBlock` templ component renders installed/binary/version/auth_provider, surfaces the `codex login` command as remediation when unauthenticated (NO in-console OAuth flow, NO browser launch — REQ-C-5), degrades gracefully when codex is absent, and offers the review-gate + write-mode opt-in toggles through the same seam the fail-closed readers (`readCodexReviewGateEnabled` / `readCodexTaskAllowWrite`) consume — one source of truth, no parallel key. Grep guard: `grep -rn 'classifyCodexAuth\|codex login status' internal/web/ --include=*.go | grep -v _test.go` → 0 matches (no second auth-classification implementation in web).
  - **M4 GLM key state — glmkey.go reused byte-identical (REQ-C-7 / AC-C-010 / AC-C-011).** The `glmKeyStateBlock` templ component renders the GLM key STATE only inside the MCP section: a configured/not-configured boolean + the bounded trailing-four hint. It consumes `view.GLMKeyConfigured` / `view.GLMKeyHint` already populated by the pre-existing `populateGLMKeyHint` → `computeGLMKeyHint` path authored by SPEC-GLM-KEY-INPUT-001. No second credential path, no submit, no reveal (those stay in the 3rd Party LLM section). `git diff ed70e4354 -- internal/web/glmkey.go` → 0 lines changed (byte-identical); `TestGLMKeyField_AbsentFromSchema` still PASSes (field absent from `settings.AllFields()`, preserving the C-C-2 structural anti-leak guarantee); `TestGLMKeyHint_TrailingFourOnly` + `_ShortKeyDisclosesNothing` still PASS — disclosure stays bounded on both branches (configured-boolean + final four only; for a key of four characters or fewer, no characters are shown).
  - **M5 secret-hygiene + i18n + no-fork sweep — test-only (REQ-C-8 / REQ-C-9 / REQ-C-10 / AC-C-012..014).** M5 introduces ZERO production-code changes — it is a pure characterization sweep (DDD PRESERVE) of invariants M1-M4 already established. NEW `internal/web/mcp_secret_hygiene_test.go` (4 tests) reflect-walks the view models to prove no credential field hides in `CodexStateView` or `pageView`'s GLM pair, asserts `glm_api_key` stays absent from `settings.AllFields()`, and that no `.templ` source references the `glmcred` package (closing the template-surface bypass). i18n coverage was already complete at M5 entry — M2/M3/M4 each added all 4 locales (en/ko/ja/zh) in the same change (17 + 10 + 5 keys); the full 14-test governance suite (`TestI18nKeySetParity`, `TestI18nKeyCoverageForward`/`Reverse`, `TestI18nUntranslatedValues`, `TestI18nAllowlistNoOrphans`, `TestI18nEndonymInvariants`, …) PASS — no M5 key fills needed. The no-fork guards (`mcp_audit_surface_test.go:47-95`) remain PASS and untouched.
  - 14 acceptance criteria (AC-C-001..014, all MUST), all PASS, verified against the run-phase implementation at HEAD `651293a4e` of the worktree (per-milestone commits M1 `1f0428a46` → M2 `4fb05f930` → M3 `9b0c57768` → M4 `9136c9345` → M5 `651293a4e`). `go build ./...` exit 0; cross-platform build (`GOOS={linux,windows} GOARCH=amd64`) exit 0 (per §E.3); `golangci-lint run` 0 issues (baseline 0 → M5 0). This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — `acceptance.md` / `plan.md` use the markdown-header convention), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). README unchanged — the English README carries only a generic one-line `moai web` table row (`README.md:279`), not MCP-section documentation; a known stale "3 third-party servers" sentence in README.ja/zh and the docs-site `mcp.md` sweep are adjacent docs debt explicitly OUT OF SCOPE for this sync (separate follow-up PR). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CONFIG-MODE-MIGRATE-001](.moai/specs/SPEC-CONFIG-MODE-MIGRATE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **A new `moai config mode-migrate [--apply]` CLI widens permission bits of `.moai/config/**` files back toward `defs.FilePerm` (`0o644`) — a dry-run-first, operator-approval-gated one-time migration that repairs the already-narrowed population the sibling `SPEC-CONFIG-ATOMIC-WRITE-001` (CLOSED) intentionally left alone.** `SPEC-CONFIG-ATOMIC-WRITE-001` shipped a `Stat`-based mode-*preservation* helper, which is correct for every future write but perpetuates the narrowed mode of any file already narrowed (a `Stat`-based fix faithfully preserves whatever mode it finds). This SPEC owns the one-time *widening* migration that the sibling explicitly deferred (parent `SPEC-CONFIG-TIER-PERSIST-001` REQ-CTP-025/026).
  - **Dry-run-first + `--apply` gate (candidate C design lock).** Default invocation is a no-op dry-run: it lists widening candidates (path + current mode → target mode) and modifies nothing. `--apply` performs the widening. The migration is never blind-widen and never announce-then-auto-widen — the operator reviews the dry-run list and explicitly re-runs with `--apply`.
  - **Only-widen predicate (spec.md §D.2).** `IsWideningCandidate(mode)` returns true only when `(cur|target)==target && cur!=target` — every bit set in the file's mode is also set in `defs.FilePerm`, AND the mode is not already equal. A file carrying a bit NOT in `defs.FilePerm` (exec bits, group-write, set-uid/gid, sticky — e.g. `0700`, `0660`, `0666`) is excluded, because setting it to `defs.FilePerm` would DROP that bit (a narrow, forbidden by REQ-MIG-002). The migration only adds bits; it never removes them.
  - **Scoped to `.moai/config/` only + symlink-safe (AC-MIG-004 / AC-MIG-008).** `ScanConfigDir` walks `.moai/config/` recursively; files outside that tree (e.g. `.claude/settings.json` at 0600) are untouched. Symlinks are `Lstat`-detected (NOT `Stat` — the symlink entry itself is inspected, never followed) and skipped, so `os.Chmod` can never land on an out-of-scope target. The symlink-skip AC (AC-MIG-008) was added in the plan-audit iter1 fix batch (D3 edge case).
  - **Idempotent (AC-MIG-005).** A tree already at `defs.FilePerm` yields an empty candidate list; `--apply` on an empty list is a no-op (exit 0, no marker file, no output noise). Re-running on an already-migrated tree changes nothing.
  - **Routes through the shared atomic-write helper (AC-MIG-006).** `ApplyWidening` re-reads each candidate, writes the content back unchanged via `atomicfile.Write` (the sibling SPEC's atomic + mode-preserving helper), then performs the single permitted `os.Chmod(path, defs.FilePerm)` — the AC-MIG-006 scoped exemption, because the atomic helper preserves the pre-existing destination mode and widening REQUIRES this post-write chmod. The chmod references the named constant `defs.FilePerm` (never a numeric literal — CLAUDE.local.md §14); bare `os.WriteFile` is never used. Verified: source contains `atomicfile.Write`; zero bare `os.WriteFile(`; single `os.Chmod(` site.
  - **New `config` parent command.** No prior `moai config` existed (diagnostics live under `moai doctor config`, which is read-only); `mode-migrate` is the first mutating subcommand under a new `config` parent, registered as a sibling of `doctor config`. The `configCmd` parent owns mutating config operations; future config-surface subcommands slot under it.
  - **Implementation path note.** Plan-phase §E matrix estimated `internal/cli/config/mode_migrate.go`; manager-develop found no prior `moai config` parent existed and placed the file at `internal/cli/mode_migrate.go` (the `config` parent is created inside `mode_migrate.go`, not in a separate `cmd.go`). Real deliverable: `internal/cli/mode_migrate.go` + `internal/cli/mode_migrate_test.go`.
  - 8 acceptance criteria (AC-MIG-001..008, all MUST), all PASS, verified against the run-phase implementation at HEAD `5817e9173` of the worktree (M1 single commit — Tier S, API surface + apply path + wiring collapsed into one cohesive unit). Orchestrator trust-but-verify: `go test -run TestModeMigrate ./internal/cli/` → ok; `go build ./...` + `GOOS=windows GOARCH=amd64 go build ./...` exit 0; `golangci-lint run --timeout=3m ./internal/cli/` → 0 issues; predicate enumeration unit `TestIsWideningCandidate_Enumeration` covers the §D.2 table (0600/0640 candidate; 0700/0660/0644/0664/0666/0500 not). TDD evidence: RED failing-test output captured before GREEN (`undefined: runModeMigrate` / `undefined: IsWideningCandidate` build failure). README / docs-site unchanged — `moai config mode-migrate` is an internal operator CLI; the 4 READMEs and docs-site do not document individual `config` subcommands (zero matches for `moai config` / `mode-migrate` across README.md / README.ko.md / README.ja.md / README.zh.md and `docs-site/`), so no doc entry is added. Template-First: NO `internal/template/templates/**` surface touched (pure CLI + runtime Go) — no mirror step and no `make build` required. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — Tier S, no acceptance.md; ACs inline in spec.md §3), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-HOOK-PRETOOL-PERF-001](.moai/specs/SPEC-HOOK-PRETOOL-PERF-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Collapsed the per-invocation fork+exec+full-config cost that produced 33 PreToolUse timeouts across 30 days, by adding a mtime+size-keyed config disk cache and a concurrent-stress profiling harness — and by landing the security regression test that binds any future fast-path to the destructive-primitive scan.** Tier M, P1, 10 ACs (9 MUST + 1 SHOULD deferred, all MUST PASS, AC-PERF-005 SHOULD DEFERRED — see below). Root cause was structural rather than a deterministic hot spot: every `PreToolUse:Bash` invocation forks a fresh `moai` process via `handle-pre-tool.sh` → `moai hook pre-tool`, and that fresh process loads the **full** merged config (~20 per-section YAML reads + parse + merge) even though the PreToolUse handler only consumes a thin slice (security policy, branch-guard flag, gate config). Under system load and/or concurrent hook execution the fork+exec+full-config cost amplified intermittently — rare enough to be invisible to a smoke test, frequent enough to stall real sessions. The 5→10s timeout widening (separate PR #1473) absorbed the spike as immediate mitigation; this SPEC is the ROOT fix that attacks the structure so the timeout can return toward 5s once per-invocation cost drops (REQ-PERF-010 gates the narrowing on production telemetry — it is NOT performed in this SPEC).
  - **M1 — Config disk cache, production-wired via `LoadWithCache` (`internal/config/cache.go` + `cache_test.go`, REQ-PERF-001..004 / REQ-PERF-008 / REQ-PERF-009 / AC-PERF-001..004 / AC-PERF-008 / AC-PERF-009).** On a cache hit (`.moai/state/config-cache.json` present + schema-version match + per-section mtime+size fingerprint matches every section file), the loader returns the cached config WITHOUT opening any `.moai/config/sections/*.yaml` file — verified by `TestCache_HitSkipsSectionParse`. On a cache miss (absent, corrupt, schema-mismatched, or fingerprint-invalidated), the loader falls through to the existing full `Load` path and writes a fresh cache atomically via `atomicfile.Write` (write-temp + rename in the same directory — REQ-PERF-008; concurrent-writer race guarded by `TestCache_AtomicConcurrentWrite`). Deletion of a section file present at cache-write time is an invalidation signal equal in force to an mtime change (REQ-PERF-003 / `TestCache_SectionDeletionInvalidates`). Corrupt or schema-mismatched cache fails open silently — no user-facing error, no non-zero exit — and is overwritten on successful re-merge (REQ-PERF-004 / `TestCache_CorruptFailsOpen` + `TestCache_SchemaVersionMismatchFailsOpen`). The cache lives at `.moai/state/config-cache.json` (fixed name, predictable for gitignore + cleanup — REQ-PERF-009 / `TestCache_LocationUnderStateDir`). Wired into `ConfigManager.Load` so every consumer benefits, not only the PreToolUse handler.
  - **M2 — Lazy config slice, implemented + unit-tested but DEFERRED from production wiring (`internal/config/slice.go` + `slice_test.go`, REQ-PERF-005 / AC-PERF-005, SHOULD — DEFERRED).** `LoadSlice` / `LoadWithCacheSlice` / `PreToolUseSliceSections` are implemented and `TestLoadSlice_StrictSubset` proves the slice reads only a strict subset of the section set. It is **NOT wired into the production `ConfigManager.Load` hot path**, and this is recorded transparently rather than as a silent PASS. Rationale (progress.md §E.2 M2-WIRING): on a cache miss, `LoadWithCacheSlice` would write a PARTIAL-section cache (only the PreToolUse slice's sections), which subsequent cache-HIT reads would then serve to ALL handlers — breaking non-PreToolUse handlers that need the unloaded sections. M1 (the full-load cache via `LoadWithCache`) IS wired and delivers the measured 93% max-tail reduction on its own; the lazy slice is a capability available for a future per-handler config-needs analysis, not a production optimization today.
  - **M3 — Concurrent-stress profiling harness + post-change measurement (`.moai/specs/SPEC-HOOK-PRETOOL-PERF-001/baseline.md` + `postchange.md`, REQ-PERF-006 / AC-PERF-006).** A new lightweight, env-gated timing collector (`internal/hook/perf/timing.go`) records per-phase wall-time (fork-exec / config-load / dispatch / total) and emits a JSON line to stderr when `MOAI_HOOK_PERF_TIMING` is set; when unset, every method is a no-op so the hot path incurs zero production overhead. The harness (`internal/hook/perf/harness_test.go`) spawns 8 parallel `moai hook pre-tool` invocations across 5 batches (40 total) against a fixture project. **Measured outcome (warm cache, post-change, committed in run `3b6ea0677`)**: config-load p50 dropped 1.26ms → 0.81ms, and the external max-tail collapsed from **894.21ms → 83.39ms — approximately a 91% reduction** under concurrent stress, far exceeding the plan's ≥30% p99-tail target (progress.md §E.2 records the campaign headline as ~93%). The residual tail risk (fork+exec amplification under OS scheduling contention, not config-load) is addressed by REQ-PERF-010: a daemon (C-2, explicitly out of scope per §E) would be the follow-up to eliminate the fork+exec cost entirely.
  - **AC-PERF-007 security regression test (`internal/hook/perf/security_regression_test.go`, REQ-PERF-007, make-or-break MUST).** A FORWARD GUARD that lands now even though NO fast-path is implemented in this SPEC: `TestAC_PERF_007_DestructivePrimitivesNotFastPath` enumerates each of the 13 destructive primitives in the Bash Risk-Amplifier Doctrine set (`rm -rf /`, `rm -rf ~`, `rm -rf .git`, `rm -rf node_modules`, `git push --force`, `git push -f`, `git push --no-verify`, `git commit --no-verify`, `git reset --hard`, `DROP TABLE`, `TRUNCATE` ×2, `chmod -R 777` ×2) as a separate case and asserts a hypothetical fast-path does NOT fire for any of them. `TestAC_PERF_007_CompoundSubcommandOverflowNotFastPath` asserts the same for any Bash command whose compound-subcommand count (pipe `|`, `&&`, `||`, `;`, backtick, `$(...)`) exceeds `BASH_SUBCOMMAND_SOFT_CAP` (5). `FastPathEligible` currently returns `false` unconditionally (no fast-path exists); when a future SPEC introduces one, it MUST implement `FastPathEligible` to return `true` ONLY for commands where `IsDestructiveOrComplex` returns `false`, or this test fails.
  - **Plan correction B5-CORR**: `plan.md` B-5 and C-PRE-4 referenced `internal/hook/observer.go` for the `$CLAUDE_PROJECT_DIR`-first path-resolution helper. This file does NOT exist; the actual helper is `internal/hook/path_resolve.go` (`resolveProjectRootFromEnv` / `resolveProjectRootFromInputOrEnv`). M1 reused `path_resolve.go` verbatim; `observer.go` was never searched. Recorded here so an operator reading the SPEC body alongside the implementation is not misled.
  - 10 acceptance criteria (AC-PERF-001..010: 9 MUST + 1 SHOULD deferred), all MUST PASS with attributable evidence. Verified against the run-phase implementation at HEAD `3b6ea0677` of the branch (run commit: `3b6ea0677` M0-M3 single commit; `go build ./...` exit 0; `go test ./internal/config/... ./internal/hook/...` exit 0 across 14 packages; `golangci-lint run` 0 issues; coverage `internal/hook/perf` 96.4%, `internal/config` 80.9% package aggregate — new files `cache.go`/`slice.go` at ~85% function-level). This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (the `draft → in-progress` intermediate was consolidated into the terminal close — run-phase began from `draft`), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). §25 template neutrality N/A (`internal/config` + `internal/hook` Go sources + SPEC artifacts only — `git diff origin/main..HEAD --name-only -- internal/template/templates/` shows no template mirror touched by this SPEC). README / docs-site unchanged — internal `internal/config` + `internal/hook` performance fix with no user-facing command or surface change (the 10s PreToolUse timeout context is already documented in `internal/hook/CLAUDE.md`). `sync_commit_sha` populated as `pending-backfill-sync-SPEC-HOOK-PRETOOL-PERF-001` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit after the sync PR merges. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). **Out of scope (not in this SPEC)**: narrowing the 10s PreToolUse timeout back toward 5s — gated on REQ-PERF-010 production telemetry confirming p99 < 5s, and performed in a separate follow-up PR; a long-lived hook daemon (C-2) — a follow-up ONLY if the profiling milestone shows the cache+lazy approach insufficient. 🗿 MoAI

- **[SPEC-MCP-AGENT-WIRING-001](.moai/specs/SPEC-MCP-AGENT-WIRING-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The moai MCP server's 17 typed tools are now phase-scoped-granted to the 7 retained agents whose phase scope calls for them — each agent's `tools:` frontmatter CSV carries exactly the `mcp__moai__*` entries matching its phase, and nothing outside it.** Before this, only two agents (`plan-auditor.md:7`, `sync-auditor.md:9`) carried a single `mcp__moai__audit_multi` grant; the other ten retained agents had no declared access to the state tools the harness ships, so even with SPEC-A making the server present by default, the tools were unreachable from most agent contexts. This SPEC extends the established two-agent pattern to the full catalog and records the per-agent grant matrix as a deliberate decision (the 5 agents that receive zero tools is a documented outcome, not a gap).
  - **Phase-scoped grant matrix (REQ-B-1, M1).** `manager-spec` → `spec_progress, spec_audit, spec_drift`; `manager-develop` → `verify_snapshot, verify_trend, goal_status`; `manager-docs` → `spec_progress, spec_audit`; `plan-auditor` → `audit_multi, spec_audit, spec_drift, codex_audit` (retains its pre-existing `audit_multi`); `sync-auditor` → `audit_multi, verify_trend, audit_cache, glm_audit` (retains its pre-existing `audit_multi`); `manager-lead` → `session_list, goal_status`; `super-advisor` → `spec_audit, verify_trend`. The five non-granted agents (`manager-git`, `manager-design`, `e2e-tester`, `builder-harness`, plus the Anthropic built-in `Explore`) receive no MCP tool — each omission justified in `plan.md` §B.3.
  - **Exactly one write-capable grant (REQ-B-2 / AC-B-003).** Of the four write-capable tools (`goal_arm`, `verify_snapshot`, `codex_task`, `codex_job_cancel`), only `verify_snapshot` is granted — and exactly once, on `manager-develop` (whose run-phase work writes the verification baseline that later attributes its own claims — a deliberate pairing). `goal_arm`, `codex_task`, `codex_job_cancel` appear zero times across the catalog. `goal_arm` is deliberately omitted from `manager-lead`: arming a goal changes the session termination condition and belongs to the orchestrator downstream of Implementation Kickoff Approval — `plan.md` §B.2 states the reason and names the one-line change if the owner overrides (AC-B-004).
  - **Frontmatter-only, no Go change (REQ-B-5).** This SPEC edits only the `tools:` line of the seven granted agent files. No agent body prose, no tool handler, no schema, no Go code, and no `.mcp.json` is modified. The model/effort SSOT (`ResolveAgentModelEffort`) was measured complete across all four model-invoking tools at base — all four already resolve through it — and the existing structural guard `TestMCPAudit_NoDirectFrontmatterRead` (`internal/cli/mcp_audit_test.go:148`) already scans the entire codex session path (`openCodexSession` / `openCodexSessionOn` / the `threadParams["model"] = me.Model` assignment at `mcp_codex.go:566`), so REQ-B-6 is verification-shaped, not change-shaped: no Go test is added because the path is already structurally guarded (AC-B-008 / AC-B-009).
  - **Template-First mirror + catalog regen (REQ-B-3 / AC-B-010).** Each of the seven edited `.claude/agents/moai/*.md` files is mirrored to `internal/template/templates/.claude/agents/moai/`, and `make build` regenerates the embedded `internal/template/catalog.yaml` in the same change. The mirrored copies carry no SPEC ID, REQ token, commit SHA, internal date, `/Users/` path, or `CLAUDE.local.md` reference — `TestTemplateNeutrality` + `TestInternalContentLeak` PASS (AC-B-011). Existing `tools:` entries are preserved in their original order; the `mcp__moai__*` additions are appended; `tools:` remains a CSV string, never a YAML array (REQ-B-4 / REQ-B-5 / AC-B-006).
  - **`manager-lead` depth-2 seal untouched (AC-B-007).** `manager-lead` is the sole `Agent`-carrying retained agent; adding MCP tools to it does not add `Agent` to its `tools:`. The depth-2 seal guard (`internal/template/manager_lead_depth_test.go`) continues to PASS — no agent other than `manager-lead` carries `Agent` in `tools:`.
  - 11 acceptance criteria (AC-B-001..AC-B-011: 10 MUST + 1 SHOULD), all PASS, verified against the run-phase implementation at HEAD `6644efdd4` of the worktree (PR #1469 squash `6644efdd4`, merged 2026-08-12). Orchestrator trust-but-verify: full `go test ./...` green (no Go change — the three existing SSOT guards at `mcp_audit_test.go:148`, `mcp_glm_test.go:227`, `mcp_convergence_test.go:518-526` PASS unchanged); `go build ./...` + cross-platform build exit 0; template mirror parity confirmed via `diff` on the `tools:` line of all 7 edited files; neutrality guards PASS. **No Go source file changed** — the run is frontmatter + template mirror + catalog regen only. Known acceptance.md doc drift (NOT fixed in this sync — manager-spec's domain, flagged as debt): AC-B-007 cites `TestManagerLeadDepth` but the real tests are `TestManagerLeadIsSoleAgentCarrier` / `TestManagerLeadCarriesAgent`; AC-B-008 line-number citations are off-by-one. Both are acceptance.md body content (forbidden ownership crossing for manager-docs per the frontmatter-schema matrix) and remain for a follow-up manager-spec delegation. README / docs-site unchanged — internal agent-wiring fix, no user-facing command or behavior change. This sync commit carries only the frontmatter `draft → completed` transition on `spec.md` (pragmatic direct close — the `draft → in-progress` intermediate was not recorded as a separate commit; manager-develop merged the run while SPEC-B sat at draft, so this sync commit carries the terminal `completed` transition directly, resolving the `spec-status-sync` non-required CI check's draft-with-merged-run flag), the `progress.md §E.2 / §E.3 / §E.4` signals, and this CHANGELOG entry — zero further code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-MCP-DEFAULT-ON-001](.moai/specs/SPEC-MCP-DEFAULT-ON-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The self-hosted `moai` MCP server (the 17 typed tools registered by `registerMoaiMCPTools` — `spec_audit`, `verify_trend`, `session_list`, etc.) is now provisioned into every new project by default, the distributed template `.mcp.json` is reduced to a single neutral `moai` entry, and `moai update` no longer clobbers a user's own MCP entries.** Before this, the harness's own MCP server was gated behind an opt-in confirm that defaulted to "no" (so most users never received the tools the harness ships), and `moai update` silently overwrote any MCP entry a user had added because `.mcp.json` was omitted from the 3-way merge set on the false grounds that "MoAI no longer ships an MCP template". Both are fixed, and two completed SPECs whose requirements forbade the flip are amended in place.
  - **Provisioning gate flipped to default-on with an honored decline (REQ-A-3, M3).** The wizard question at `internal/cli/wizard/questions.go` is renamed `mcp_tools_opt_in` → `mcp_provision` and its `Default` flipped `"false"` → `"true"`, keeping positive polarity ("Provision the moai MCP server?"). `provisionMCPEntryIfOptedIn` → `provisionMCPEntryUnlessDeclined` (`internal/cli/init.go`); an explicit decline is honored silently (no `.mcp.json`, no stdout/stderr noise), and the write stays best-effort non-fatal (C-A-4).
  - **Template `.mcp.json` reduced to the single `moai` entry; repo-root carries four (REQ-A-1 / REQ-A-2, M2).** `internal/template/templates/.mcp.json` now carries exactly one active entry — `moai` → `{"command":"moai","args":["mcp-server"]}` with no `env` block — while the repo-root `.mcp.json` carries four (the three third-party entries `context7` / `chrome-devtools` / `playwright` plus `moai`). The two files **diverge on purpose**: the repo root is a maintainer working tree using the third-party tooling directly; the template is a neutral distribution artifact. The neutrality guard `internal/template/mcp_template_neutrality_test.go` is inverted (`mcpAllowedActiveKeys = {moai}`, count = 1), not weakened — the forbidden-token scan and the `${VAR}`-literal secret check are preserved byte-unchanged (REQ-A-9).
  - **`.mcp.json` added to the `moai update` 3-way merge set (REQ-A-4, M4).** `collectMergeableFiles` (`internal/cli/update_template_sync.go`) now returns `.mcp.json` alongside `.claude/settings.json` and `.moai/status_line.sh`, so a user's own MCP entries survive `moai update` instead of being clobbered. The false comment ("MoAI no longer ships an MCP template") is corrected. `TestMergeUserFiles_PreservesUserMCPEntry` proves both the shipped `moai` entry and a user-added `my-tool` entry survive the merge.
  - **Two completed SPECs amended in place to match the flipped contract (REQ-A-6 / REQ-A-7 / REQ-A-8, M1).** `SPEC-MOAI-MCP-SERVER-001` (REQ-MCP-002 gate inversion, REQ-MCP-015 opt-out flag, AC-MCP-002 / AC-MCP-006) and `SPEC-TREND-MCP-001` (REQ-TMC-003 default-on moai, REQ-TMC-001 active count 3 → 1, AC-TMC-001 / AC-TMC-004) are amended per owner direction: `status: completed` preserved, `amendment_of` omitted (the canonical transition pairs it with `in-progress`, which the owner held at `completed`), HISTORY `### Amendments` sub-section carries the record instead. Each amendment states its reversal rationale in prose adjacent to the amended criterion. The load-bearing invariants — REQ-TMC-002 secret hygiene, REQ-TMC-004 `$comment`-free clause, §25 neutrality — are preserved verbatim.
  - **Documentation surfaces reconciled (REQ-A-5, M5).** The three wizard locale strings (ko / ja / zh, `internal/cli/wizard/translations.go`) no longer carry "Opt-in default-off" or the `REQ-MCP-002` token; `.claude/rules/moai/core/settings-management.md` + its template mirror and the template `CLAUDE.md` §12 MCP inventory are restated for the single-entry default-on contract. The `internal/template/mcp_doctrine_parity_test.go` CI guard is inverted to pin the new "default-on" contract (REQ-A-5 cascade — a test asserting the old default is itself such a surface).
  - 14 acceptance criteria (AC-A-001..014: 13 MUST + 1 SHOULD), all PASS, verified against the implementation at run-phase completion (HEAD `0c44ef1b8` of the worktree). Run-phase plan+amendment+run work landed on main as PR #1455 squash `ac3e38a0b` (2026-08-12). Cross-platform build (`go build ./...` + `GOOS={linux,windows} GOARCH=amd64`) exit 0; `go test ./internal/cli/... ./internal/template/...` green; the 3 pre-existing baseline FAILs (`internal/template` zone-registry SPEC-ID/date leaks, `internal/astgrep` dogfood stubs) are confirmed present at base `ed70e4354` and untouched by this SPEC. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — `acceptance.md` / `plan.md` use the markdown-header convention), the `progress.md §E.4` audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. README / docs-site unchanged — the English README carries no MCP-defaults section (canonical-locale-first: nothing to derive); a stale "trend MCP tools" paragraph in README.ja.md / README.zh.md and the docs-site `/claude-code/extensibility/mcp.md` "does not provision by default" line are adjacent docs debt, outside REQ-A-5's enumerated surfaces, flagged for a follow-up 4-locale sweep. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CONFIG-KEY-HONESTY-001](.moai/specs/SPEC-CONFIG-KEY-HONESTY-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Every config key shipped to a user in `internal/template/templates/.moai/config/sections/*.yaml` now either does what its name says or is explicitly classified as not — and a guard keeps it that way.** Before this, 287 distinct `yaml:`-tagged field names existed in `internal/config/types.go`, of which 174 had zero production readers (path-resolved measurement), and 161 of those unread keys still shipped to users as YAML — each one a silent promise that editing it would change behavior, when in fact nothing consumed it. The gap was invisible because no measurement bridged the Go struct surface to the shipped-template surface, and no guard prevented drift back after a fix.
  - **M1 triage rule + shipped-key inventory (REQ-CKH-001/002/003).** A new four-class classification rule (`.moai/docs/config-key-triage-rule.md`, dev-internal, NOT shipped to users) sorts each shipped key into **W** (Wire — has/blocks a real reader, route it), **P** (Prose-consumed — referenced by a shipped doc via its fully-qualified dotted path), **R** (Reserved — names a real intended capability, ship with the reserved marker), or **D** (Dead — no reader, no prose, no planned capability, remove from the shipped template). The rule is operationalized by a 952-entry path-resolved inventory at `internal/config/testdata/shipped_key_inventory.yaml` that maps every shipped YAML key to its classification + reader path (or absence).
  - **M2 path-resolved anti-rot guard (REQ-CKH-008 — `TestShippedConfigKeysHaveReaders`, `internal/config/shipped_key_reader_test.go`).** A CI-bound test reads the inventory, resolves every `X.F` selector to its receiver type, and asserts each non-P/R/D key has a production reader on the resolved path. The guard is path-resolved (a read counts only when the receiver is a `types.go`-declared struct), closing the false-positive seam the bare-field-name measurement left open. The P/R/D allowlists are explicit and individually justified — a future key that loses its reader is caught at PR time, not after release.
  - **M4 system.yaml resolution — unblind parity guard + `loadSystemSection` (REQ-CKH-004).** `internal/template/templates/.moai/config/sections/system.yaml` is now loaded by a real reader (`internal/config/loader_system.go::loadSystemSection`), and a parity guard (`internal/config/loader_system_test.go`) asserts every key in the shipped system.yaml has a corresponding `types.go` field — the same honesty contract the M2 guard enforces for the rest of the section set, extended to the one section the loader previously didn't read.
  - **M5 documented-but-unenforced reconciliation (REQ-CKH-009/010).** The template's `workflow.yaml` worktree toggles (`auto_create` / `auto_merge` / `auto_cleanup`) previously shipped as `true` while `internal/config/defaults.go` set the same knobs to `false` — a contradiction between "shipped default" and "compiled default" that silently inverted user intent. The template values are aligned to `defaults.go` (all three `false`), and each toggle's actual reader status is now stated accurately in `.moai/docs/local-dev-settings-intent.md §22.8` (auto_create has an advisory-wording reader; auto_merge / auto_cleanup have no reader; session_name_pattern is a declared-only placeholder).
  - **M6 neutrality-leak removal + E5 handoff (REQ-CKH-011/012/013).** Three internal-citation leaks in template comments (`SPEC-AGENT-ARCH-V2-001` × 2 in `workflow.yaml`, `issue #653` in `llm.yaml`) were removed while preserving the mechanism description verbatim — the comments still explain *what* the surrounding config does, just without naming the internal SPEC/issue that produced it. The pattern-coverage gaps the leaks exposed (no class covers `plan.md §D D6`-shaped artifact citations; `C6` matches `PR #N` but not `issue #N`) are recorded for sibling SPEC-UPDATE-CI-GUARD-001 at `.moai/specs/SPEC-UPDATE-CI-GUARD-001/research-ckh-f6-handoff.md`; the existing `internal/template/internal_content_leak_test.go` guard was NOT edited (per ownership boundary).
  - **Report-once / delete-never posture over the merge engine (REQ-CKH-011 / AC-CKH-014 — PASS-WITH-DEBT).** `TestTemplateRemovedKeySurvivesUserConfig` (`internal/config/template_removed_key_test.go`) proves a template-removed D-class key + its user-set value survive a `backup.MergeYAML3Way` call (delete-never) and that the retained-key advisory names the key (reported-at-least-once). The cross-call report-once sub-clause (a second merge over the same tree emits no further advisory) is deferred to SPEC-UPDATE-YAML-PRESERVE-001, which owns the merge engine — recorded as a deferred gap rather than silently weakening the assertion.
  - **M3 intentionally skipped.** M3 (tier precedence over the config layer) depends on `SPEC-CONFIG-TIER-PERSIST-001` (E3) reaching `status: completed`; E3 is still in-flight, so M3 is held for a follow-up and NOT executed in this SPEC. The active milestone set is M1, M2, M4, M5, M6.
  - 23 acceptance criteria (AC-CKH-001..023: 22 MUST + 1 PASS-WITH-DEBT), all recorded. Verification: `go build ./...` exit 0; full `go test ./...` deferred to the sync-auditor phase per the close contract. Template-First: the three template files modified (`.claude/rules/moai/core/zone-registry.md`, `.moai/config/sections/llm.yaml`, `.moai/config/sections/workflow.yaml` under `internal/template/templates/`) ARE the template source — no separate mirror step required; the new `.moai/docs/config-key-triage-rule.md` and `.moai/docs/local-dev-settings-intent.md` edits are dev-internal (under `.moai/docs/`, explicitly NOT shipped to the template tree per the file's own header and the local-only `docs/` set). README / docs-site unchanged — internal-tooling config-honesty fix, no user-facing command or behavior change. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md`, the `progress.md §E.4` signal, and this CHANGELOG entry — no further code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-after-merge` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot reference its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-NAVIGATOR-SYNC-005](.moai/specs/SPEC-NAVIGATOR-SYNC-005/spec.md)** — sync-phase close (3-phase plan→run→sync). **The BAS Epic M3 Falconer Fix layer now incrementally regenerates ONLY the stale doc subtrees an edit produced, via a split architecture: a deterministic Go CLI emits a draft-request, the orchestrator mediates an AI draft + a 4-option approval gate, and the Go CLI applies the approved draft behind an approval-token hard guard.** Before this, regenerating the Navigator graph after a code edit meant re-running the full `/moai project` chain (M0 graph-join + M4 tiers overlay + audit) — slow, and it touched every doc surface even when only one capability row had drifted. The Fix layer closes the BAS detect→route→fix loop (the CodeWiki `--compare-to <commit>` pattern adapted to a doc-graph): it takes the M2 work-items + M1 detect subtrees + the M0 graph context, computes a diff-scope, and drafts only the stale subtrees — a human approves, and the apply atomic-renames the approved draft into the live doc surfaces. The doc map stays a living document instead of degrading into a stale snapshot.
  - **Split architecture (Go + orchestrator-mediated AI, NOT Go-calls-LLM).** Layer 1 (`Run` in `internal/navigator/fix/request.go`) is the deterministic producer: loads the four read-only inputs (M2 work-items.json + M1 detect JSONL + git-diff + M0 nav-graph.json), computes the diff-scope, emits `.moai/project/navigator/fix-drafts/<draft-id>/request.json` + a stdout JSON signal — the `design.md §A.4` handoff contract the orchestrator consumes to spawn the AI draft (layer 2). The orchestrator then runs the AskUserQuestion 4-option approval gate. Layer 3 (`Apply` in `internal/navigator/fix/apply.go`) is the deterministic post-approval consumer: validates the `approval.json` token and atomic-renames the approved + scope-conformant subtrees into the live doc surfaces. The Go CLI never calls an LLM — `grep -rn 'AskUserQuestion\|mcp__askuser' internal/navigator/fix/*.go internal/cli/navigator_fix.go | grep -v _test.go | grep -v "// "` → 0 matches (AC-NS5-007a).
  - **On-demand Hidden CLI, not a PostToolUse hook.** `moai navigator-fix` is a Hidden cobra subcommand (sibling of `navigator-sync` / `navigator-route` / `navigator-tiers`), invoked from the `/moai project` surface. There is NO PostToolUse hook and NO `handle-navigator-fix.sh` wrapper — M1 already owns the real-time advisory surface; M3 is invoked on-demand after the audit + route chain has run. `--apply <draft-id>` is the post-approval mode (validates the token, then applies).
  - **Diff-scope = UNION(M1 detect subtrees, git-diff stale subtrees) ∩ graph-bound nodes (REQ-NS5-003).** `ComputeScope` in `internal/navigator/fix/scope.go` computes the set of doc subtrees whose graph node is touched by either the M1 detect JSONL or the post-baseline git-diff, filtered against the M0 nav-graph. The UNION semantics unify three earlier-artifact scopes into one diff-scope; the graph-bound filter excludes paths the graph doesn't cover (no spec orphan rows in the diff-scope).
  - **Fail-open across 8 error modes, ALL exit 0 (REQ-NS5-009 / AC-NS5-009).** work-items absent, detect absent, nav-graph absent, baseline unresolvable (non-git + no compareTo + HEAD~1 fails), unparseable JSON, schema-invalid input, empty diff-scope, no-LLM-runtime — each degrades silently to exit 0 with an empty or partial draft-request and one log line. The layer-1 producer NEVER aborts the calling `/moai project` step. (Distinct from the layer-3 token-refusal hard guard, which exits non-zero — AC-NS5-008d is NOT fail-open.)
  - **Approval-token hard guard (REQ-NS5-008 c4 / AC-NS5-008d).** `ComputeApprovalToken` in `internal/navigator/fix/apply.go` derives a deterministic 64-char hex SHA-256 over (draft-id + option + provenance). `Apply` validates the `approval.json` token BEFORE any live-doc mutation: no approval.json → `ErrApprovalTokenMissing`; tampered token → `ErrApprovalTokenInvalid`; live doc SHAs unchanged on both refusal paths. The token is the load-binding contract that makes the approval a real artifact, not "someone typed a command".
  - **Scope-conformance partition (REQ-NS5-013 / AC-NS5-013).** `ConformDraftToScope` in `internal/navigator/fix/scope.go` partitions the draft's subtree IDs against the diff-scope — in-scope subtrees apply, out-of-scope subtrees are excluded and logged to `.moai/logs/navigator-sync.log` with the `scope-conformance` marker. The draft OUTPUT must conform to diff-scope[], not just the Go engine source — the approval gate + apply both depend on this partition.
  - **Atomic-rename + idempotence + applied.json ledger (AC-NS5-008c).** Apply writes via `.tmp` + `os.Rename` (no stale `.tmp` left on failure) and records an `applied.json` ledger with Approver + git-committer-date ApprovalTimestamp + AppliedSubtreeIDs + ResultingLiveDocSHA. A second Apply on the same draft is a no-op (live doc SHA unchanged, ledger does not grow) — DBT-2 idempotence.
  - **≥50% automation rate, mechanically measured (REQ-NS5-010 / AC-NS5-010).** A deterministic in-tree 10-scenario fixture corpus (3 audit-missing × 3 audit-orphan × 4 detect; 3 action strategies; fixed per-scenario approvals) yields `AC-NS5-010 automation rate: 60.0% (6/10 drafts approved unmodified)` — 60.0% ≥ 50.0% floor PASS. No wall-clock, no math/rand (deterministic).
  - **Non-overlap with all predecessors.** M3 consumes M0/M1/M2/M4 + the audit chain READ-ONLY; `internal/navigator/{sync,detect,route,tiers}/`, `internal/hook/navigator_detect*.go`, `internal/mx/`, and the three predecessor Navigator chains are byte-unchanged by the M3 diff (AC-NS5-005a / AC-NS5-006 grep-verified).
  - **Template-first (AC-NS5-012).** M3 ships pure CLI + runtime Go — `git diff --name-only origin/main...HEAD | grep '^internal/template/templates/'` → no matches (no distributed `.claude/` surface, no catalog regen required).
  - 13 REQ (REQ-NS5-001..013) + 20 AC (AC-NS5-001a through AC-NS5-013, including sub-variants a/b/c/d per acceptance.md §B traceability matrix), all PASS, verified against the implementation at run-phase completion (HEAD `066fc7b79` of `worktree-bas-m2-route`). Run-phase landed in six per-milestone Conventional Commits: `0e24dde06`-lineage (M3.1 scope + draft-request schema) + `c960c8f1e` (§F Phase 4 Mode Selection) + `09ae7f5d6` (M3.2 Hidden CLI + draft-request emission) + `ef215bd7a` (M3.3 LLM-client guard test, AC-NS5-007a) + M3.4 scope-conformance + preview (AC-NS5-008b/013) + M3.5 apply + non-overlap (AC-NS5-008a/c/d, AC-NS5-005, AC-NS5-006) + `ff765318d` (M3.6 fail-open 8 modes + ≥50% automation rate). Coverage: `go test -cover ./internal/navigator/fix/` → **87.5%** (≥85% target). Quality gate: full `go test ./...` green (post-#1456 merge resolved pre-existing baseline FAIL); `go vet ./internal/navigator/fix/... ./internal/cli/...` exit 0; `golangci-lint run --timeout=2m ./internal/navigator/fix/... ./internal/cli/...` → 0 issues; `go build ./...` + `GOOS=windows GOARCH=amd64 go build ./...` exit 0; `go test -race ./internal/navigator/fix/` ok. **Coverage decision (accepted-as-is per run-phase residual-risk note):** the automation-rate corpus is an in-tree deterministic fixture (10 scenarios), not live manager-develop output — the 60.0% figure is attributable to the exact `go test … -v` invocation + its stdout; a live measurement after orchestrator integration MAY differ. MX tags (sync sub-step): `@MX:ANCHOR [AUTO]` on `ConformDraftToScope` (REQ-NS5-013 load-bearing partition), `ComputeApprovalToken` (REQ-NS5-008 c4 deterministic derivation), and `Apply` (AC-NS5-008c/008d sole live-doc write surface) — all carrying `@MX:REASON`; `@MX:NOTE [AUTO]` on `Run` (layer-1 entry point / public API boundary) and `ComputeScope` (REQ-NS5-003 UNION-semantics diff-scope) added in this sync commit. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — `acceptance.md` / `plan.md` use the markdown-header convention), the `progress.md` §E.4 audit-ready signal, the two `@MX:NOTE` additions in `internal/navigator/fix/`, and this CHANGELOG entry — zero further code changes. `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CONFIG-ATOMIC-WRITE-001](.moai/specs/SPEC-CONFIG-ATOMIC-WRITE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Every config and settings write is now atomic and mode-preserving through a single shared helper.** Before this, `atomicWrite` in `internal/config/manager.go` used `os.CreateTemp` (which creates the temp at mode 0600) + `os.Rename` with no `os.Chmod`, so every section save silently narrowed a 0644 config file to 0600; and the CLI writers (`harness.go`, `toolpolicy/{tier_render,codegen}.go`, `update*.go`) called bare `os.WriteFile` with a hardcoded `0o644` that ignored the destination's existing mode and left a partial-write window on crash. Both invariants — crash-atomicity and mode preservation — are now fixed at one choke point.
  - **One helper, every writer.** `internal/config/atomicfile.Write(path, data, defaultMode)` (new package) writes via temp-in-destination-dir + `os.Chmod(tmp, existingMode)` + `os.Rename`; existing destinations keep their pre-write mode (read via `os.Stat`), new destinations take the caller-supplied `defaultMode` (`defs.FilePerm` for regular config, `0o600` for a caller-declared secret). Routed through it: `manager.go saveSection` (Defect 1 — mode-narrowing), `harness.go:390` (Defect 2 — `0o644` hardcode + non-atomic), `toolpolicy/{tier_render,codegen}.go` (Defect 3 — `0o644`), and the `update{,_cleanup,_preserve_inventory,_wizard}.go` production writers (literal → `defs.FilePerm` constant, REQ-CAW-005).
  - **AC-CAW-002 is the load-bearing fix.** A table-driven mode round-trip over `{0o600, 0o644, 0o750}` asserts `os.Stat(target).Mode().Perm() == M` after each write — the 0o750 row is the explicit non-default mode that previously got narrowed to 0600 and now survives. RED was reproduced at both the helper and the `saveSection` level before the GREEN rewrite.
  - **Mutation-verified regression guard (AC-CAW-007).** `internal/config/atomicfile/guard_test.go` walks `internal/config/**` + `internal/cli/**` non-test source for bare `os.WriteFile` to config/settings paths and fails naming the offending site. A throwaway bare write inserted into `manager.go` made the guard FAIL with the exact location; reverting it restored PASS — the guard is not decorative. Six legitimate bare-write sites (backup-dir artifacts, an `O_EXCL` lockfile, a probe, a `mode`-var prior-art site, an injectable seam) pass the predicate naturally and are documented as exempt.
  - **Mode-widening migration deferred.** The parent `SPEC-CONFIG-TIER-PERSIST-001` REQ-CTP-025/026 (one-shot widening of already-narrowed `.moai/config/sections/*.yaml` back to `defs.FilePerm`) is out of scope here and deferred to follow-up sibling `SPEC-CONFIG-MODE-MIGRATE-001` — this SPEC ships the write-path invariant only (forward-looking); repairing historical damage is independently shippable on top. Split lineage recorded in the parent's `§L Split Branches` section.
  - 7 REQ (REQ-CAW-001..007) + REQ-CAW-004a, all MUST ACs PASS. Coverage: `internal/config/atomicfile` 81.8% (gap: the 3 error-wrapping branches need filesystem fault injection to reach; reported honestly rather than padded). Quality gate: `go test ./...` green except the pre-existing `internal/template` 2 baseline FAILs (zone-registry.md SPEC-ID/date leak — unrelated to this SPEC, present on baseline HEAD `ed70e4354`). `go vet` exit 0, `GOOS=windows GOARCH=amd64 go build ./...` exit 0. plan-audit FAIL 0.74 → iteration-2 fixes (D1–D5) → PASS. Run-phase landed as `78acfb714`; this sync commit carries only the frontmatter `draft → completed` transition + this CHANGELOG entry. `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3) and backfilled in a follow-up commit. 🗿 MoAI

- **[SPEC-HARNESS-EVO-RUN-REPORT-001](.moai/specs/SPEC-HARNESS-EVO-RUN-REPORT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The harness run is no longer mute about what it learned: a manifest can declare an improvement-signal slot, a Runner returns structured findings, the doctor surfaces a learning readout, and a post-run push step routes those findings to the orchestrator instead of waiting for a pull.** Four wiring points close the execution→learning gap that Epic Harness-Evolution 1/4 (PIPE-REPAIR) left passive — that predecessor repaired the observation→classify→propose pipe but never bridged the harness's own run into it. This SPEC is Epic Harness-Evolution 2/4.
  - **manifest `learning` block (REQ-HRR-001/002, M1).** `internal/harness/v4manifest/{schema,types,validate}.go` gained an optional `Learning` struct (`enabled` / `tier` / `confidence_floor` / `max_findings_per_run`). A manifest declaring it parses to a populated `Manifest.Learning`; a legacy 8-field manifest parses with `Learning == nil` and is NOT rejected (backward-compat preserved — AC-HRR-010). The block is a declaration slot, not a behavior toggle: it tells the harness "I emit improvement signals", and downstream consumers read it to decide whether to look for them.
  - **Runner findings contract + harness_run producer (REQ-HRR-003/004/007, M2).** `internal/harness/harnessrun/{types,proposal}.go` defines the standardized findings array a Runner returns, and `BuildHarnessRunCandidates` is the `harness_run:` producer — proposalgen's reserved-namespace third producer (a delegation-map sibling of `classify:` and `observe:`), mapping findings → `ProposalCandidate` rows for the Tier-4 application gate. The producer reuses the existing `ProposalCandidate` shape; it does not invent a parallel proposal path.
  - **doctor learning axis (REQ-HRR-005, M3).** `internal/cli/harness/doctor.go` carries a new learning readout that reads the manifest's `Learning` block + a Runner file's declared `findings` and reports whether the run→learning seam is wired. Three doctor tests (`doctor_learning_test.go` + the pre-existing doctor test quartet) pin the readout; the doctor is observation-only (no AskUserQuestion in subagent-domain code — C-HRA-008 grep clean).
  - **specialist findings emission + post-run push doctrine (REQ-HRR-006/007/008, M4 — doc-only).** The release-update specialist (`.claude/agents/harness/hns-release-update-specialist.md`) carries a mandatory `Phase 7.5` emission step — a 5-field findings payload (`{surface, kind, summary, confidence, suggested_tier}`) emitted before completion; `kind` reuses `harnessrun/types.go`'s vocabulary (`drift|gap|friction|defect`); `confidence` is grounded in the manifest's `confidence_floor` (0.70) and MUST NOT borrow `learner.go`'s `defaultConfidence` (1.0). The workflow doctrine (`.claude/skills/moai/workflows/harness.md` + template mirror) adds a `### 2.5 post-run push` step: non-empty findings → orchestrator collects → `harness_run:` producer → `ProposalCandidate` → Tier-4 AskUserQuestion gate; empty findings → silent proceed. The asymmetric boundary holds across both new surfaces — specialists and Runners return findings or blocker reports, they never call AskUserQuestion directly.
  - **§25 template neutrality (AC-HRR-009).** `grep -rn "HARNESS-EVO-RUN-REPORT\|REQ-HRR" internal/template/templates/` → 0 matches. The REQ-HRR-008 tag carried by the live `harness.md` is stripped from the template mirror; the dev-only specialist files and `hns-*-run.js` workflows are NOT leaked to the distributed tree. `internal/template/catalog.yaml` hash unchanged (`make build` not required — `harness.md` is not a catalog artifact; M4 verified empty catalog diff).
  - 10 acceptance criteria (AC-HRR-001..010: 8 MUST + AC-HRR-009 §25-neutrality + AC-HRR-010 backward-compat), all PASS, verified against the implementation at run-phase completion (HEAD `e7a6bd496` of `worktree-herr-plan-refresh`). Run-phase landed in four per-milestone Conventional Commits: `870cdd72a` (M1 manifest learning schema) + `cd88275fe` (M2 Runner findings + harness_run producer) + `4f22d97a6` (M3 doctor learning axis) + `e7a6bd496` (M4 specialist emission + post-run push doctrine). M5 was verification-only (no separate commit). **Coverage decision (accepted-as-is per GOOS approval):** `internal/cli/harness` at **80.9%** is below the §D.3 DoD 85% threshold — `doctor.go` (the code M3/M4 touched) is at high coverage; the gap is in non-doctor cmd code tracked as separate debt, NOT a doc-only M4 regression (M4 was 3 markdown files, zero Go delta). This sync commit carries only the frontmatter `in-progress → completed` transition on `spec.md` (the sole YAML-frontmatter artifact — `acceptance.md` + `plan.md` use the markdown-header convention), the `progress.md` §E.3 run_commit_sha backfill + M5 verification block, the `progress.md` §E.4 audit-ready signal, and this CHANGELOG entry — zero further code changes. `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3) and backfilled by orchestrator in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-NAVIGATOR-SYNC-004](.moai/specs/SPEC-NAVIGATOR-SYNC-004/spec.md)** — sync-phase close (3-phase plan→run→sync). **The BAS Epic M2 Falconer Route layer now promotes Navigator audit and detect findings into actionable work items, each owner-bound to a code path instead of a person.** Before this, the 002 audit surfaced `missing`/`orphan` findings as advisory text and M1 Detect surfaced per-edit affected rows as an ephemeral `systemMessage` — neither named an owner, neither persisted as a work item, and neither told the engineer which code path was now their doc responsibility. The Route layer closes that gap: it reads three read-only inputs (audit-report.json + M1 detect JSONL + M0 nav-graph.json), promotes each finding into a 5-field work item (`source_kind`, `source_entry`, `owner_path`, `action`, `confidence`), and writes `.moai/project/navigator/work-items.{md,json}` atomically.
  - **Owner = code path, never a person.** Three resolution paths, one per `source_kind`: `audit-orphan` → `implementation_path` (high confidence); `audit-missing` → `@NAV:SYM` symbol's owning package via the M0 graph, else the design-doc `source.file` (medium or low); `detect` → `changed_path` verbatim (high). A person moves on; a code path stays — the falconer binding principle (design report §6 M2). The Route layer never calls `git blame`, never reads `CODEOWNERS`, and never emits an email or username in `owner_path`.
  - **On-demand Hidden CLI, not a PostToolUse hook.** `moai navigator route` is a Hidden cobra subcommand (sibling of `navigator-sync` / `navigator-tiers`), invoked from the existing `/moai project` surface. M2 does NOT register a PostToolUse hook — the audit itself is on-demand, so a real-time trigger would usually find no audit to promote; M1 already owns the real-time advisory surface. The engine lives in `internal/navigator/route/{promote,owner,write,run}.go`; the CLI registration is in `internal/cli/navigator_route.go` (+4 lines in `internal/cli/root.go`).
  - **Fail-open across every error mode.** Audit absent, detect state absent, nav-graph absent, unparseable JSON, schema-invalid input, owner-resolution error, all-inputs-absent, and timeout — each degrades silently to exit 0 with an empty or partial work-item set and one log line. The Route layer never aborts the calling `/moai project` step.
  - **Atomic-write + idempotence + provenance, no wall-clock.** `work-items.json` carries `route_commit_sha` (`git rev-parse HEAD`) and `captured_at` (committer date of that SHA) — no `time.Now()`-derived field, so two runs on the same HEAD with the same inputs produce byte-identical output. Carries forward M0's `atomicWrite` pattern and 003's `Provenance` contract.
  - **Non-overlap with all predecessors.** M2 consumes M0/M1/M4 + the audit chain READ-ONLY; `internal/navigator/{sync,detect,tiers}/`, `internal/hook/navigator_detect*.go`, `internal/mx/`, and the three predecessor Navigator chains are byte-unchanged by the M2 diff (AC-NS4-005a / AC-NS4-006 grep-verified).
  - **Route accuracy 83.3% (≥ 70% target), mechanically measured.** A fixture corpus of 30 input findings (6 missing + 12 orphan + 12 detect) yields 25 actionable work items (high or medium confidence) = 83.3%; even under the B5 heading-range fallback (3 medium → low), the floor is 22/30 = 73.3% ≥ 70%. The dual-arithmetic proves the ≥ 70% gate survives the fallback with 3.3pp headroom.
  - 15 MUST ACs (AC-NS4-001a..012, including sub-variants), all PASS. Route coverage 85.8%. Quality gate: `go test ./internal/navigator/route/...` 28 tests green, `go vet` exit 0, `golangci-lint` 0 issues, `GOOS=windows GOARCH=amd64 go build ./...` exit 0. Template-first: M2 ships NO distributed `.claude/` surface (pure CLI + runtime Go; AC-NS4-012 confirmed `git diff template` = 0). README / docs-site unchanged — `navigator-route` is Hidden, no user-facing command surface (consistent with NS1/NS2/NS3 predecessor precedent). `sync_commit_sha` populated as `pending-backfill-*` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-EPIC-STATUS-001](.moai/specs/SPEC-EPIC-STATUS-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **A new `moai epic status <prefix>` CLI computes disk-grounded epic progress from SPEC frontmatter + title `(Mx)` markers + an optional design report, printing the epic's milestone map (done/total/pct, each milestone's covering SPEC + status + `sync_commit_sha`) and flagging orphan milestones no SPEC covers.** Before this, the only "epic progress" surface was the Progress Board / Epic Status banner an agent rendered from its own working memory at output time — a `/clear` or session resume dropped the working-memory accumulator, and a sibling session's mid-milestone work was invisible to the orchestrator session. The producer is observation-only (no file writes, no persisted `epic.json`), composes the existing `spec.ListDocs` + `spec.Audit` read path that already powers `internal/web/board.go`, and emits a frozen-shape `--json` document so the Epic Status / Progress Board banner can be fed from disk instead of from agent recall.
  - **Three-strategy epic discovery, with strict precedence.** Stage 1 (`DiscoverEpic` in `internal/epic/discover.go`) glob-matches `.moai/specs/SPEC-<prefix>-*/` via `spec.ListDocs`; Stage 2 (`ExtractMx`) regex-scans each matched SPEC title for a `(<TOKEN> M<N>)` marker and builds the Mx→SPEC-ID map (first marker wins; later markers on the same SPEC fall through to `extra_mx`; the `--marker <token>` flag overrides the inferred token, which otherwise defaults to the mode across the matched set); Stage 3 (`ParseDesignReport` + `DiscoverDesignReport` in `internal/epic/designreport.go`) auto-discovers a `.moai/reports/<basename>-<token-lower>-*.html` design report by naming rule and parses its slice-table for the canonical M0..Mx list. The design-report parser is fail-open per KI-1 — a missing, unreadable, or reformatted report degrades to marker union only, never an error. Canonical Mx not covered by any SPEC are flagged `orphan_mx` (the BAS-Epic M2/M3/M5 case, the SPEC's motivating example).
  - **Per-Mx status join + aggregate counts.** `JoinStatus` + `classifyMilestoneStatus` (`internal/epic/status.go`) classify each milestone as `done` (owning SPEC `status: completed` AND non-empty `sync_commit_sha`), `in-progress` (owning SPEC `status: in-progress` OR `implemented`), `planned` (owning SPEC `status: draft`), or `absent` (canonical but uncovered). `computePct` returns `round(done/total*100)` with `total: 0 → pct: 0` (AC-ES-007 divide-by-zero guard, NOT a NaN and NOT an error exit). `BuildEpicStatus` is the top-level producer entry point that composes the three stages, the join, and the aggregate counts into the final `EpicStatus` view-model, recording the git HEAD SHA as `baseline_attribution` on every invocation (fail-open to `""` in non-git contexts per KI-6).
  - **Frozen `--json` shape (banner-SSOT feed) + Progress Board human rendering.** `RenderJSON` (`internal/epic/render.go`) emits the spec.md §B.1 contract — `epic` / `epic_token` / `milestones[]` (each with `id`/`label`/`status`/`covered`/`spec_id`/`spec_status`/`sync_commit_sha`) / `done` / `total` / `pct` / `orphan_mx` (omitted when no design report is available, per REQ-ES-005 + design.md §5 lock) / `extra_mx` / `untracked_specs` / `design_report` / `baseline_attribution` — with additive-only forward-compat (consumers MUST tolerate unknown extra fields; AC-ES-009). `RenderHuman` mirrors the Progress Board grammar at `.claude/output-styles/moai/moai.md:636-680` — `🎯 <epic> ▓▓░░░░░░░░ done/total (pct%)` header, per-Mx lines with status icons (🟢/🟡/⬜) per the legend at `moai.md:660-668`, the 4-locale translation table (`Epic progress:` / `에픽 진행:` / `エピック進行:` / `史诗进度:`), and the empty-match-set message `🎯 <epic> — no SPECs matched prefix '<epic>'`.
  - **CLI registration + non-interactive contract.** A new `moai epic` parent command group (registered in `internal/cli/epic.go`, surfaced in the `internal/cli/help.go` tools help-row at `{"moai epic", "Epic milestone progress"}`) carries a single `status` subcommand: `moai epic status <prefix>` with flags `--json`, `--design-report <path>`, `--marker <token>`, and `--base-dir <path>`. The CLI is observation-only per REQ-ES-010 / C-HRA-008: zero `AskUserQuestion` invocations, zero `bufio.NewReader` / `fmt.Scanln` / `ReadString` interactive prompts — it reads, prints, and exits (AC-ES-011 grep guard via `TestNewEpicCmd_NoAskUserQuestion`).
  - **Factory-lead notice touchpoint (REQ-ES-012).** The SessionStart factory-lead notice (`factoryLeadNotice` in `internal/hook/session_start_factory.go:82-86`) now carries a one-line pointer `Epic context: run \`moai epic status <prefix>\` for a disk-grounded milestone map.` so a factory session can surface epic context without leaving its current turn. Wiring the notice itself is owned by the Factory / Kanban Bootstrap SPEC family; this SPEC stabilizes only the CLI invocation target so the wiring SPEC has a stable surface to call.
  - 15 acceptance criteria (AC-ES-001..015: 14 MUST + 1 SHOULD), all PASS, verified against the implementation at run-phase completion (HEAD `4aa9178c3` of `feat/factory-bootstrap-guidance`). Run-phase landed in three per-milestone Conventional Commits: `bcee39540` (M0-M3 producer library — discover + designreport + status + render, 11 files + 1736 insertions) + `f0ca6897c` (M4 CLI verb registration — `moai epic status <prefix>`) + `4aa9178c3` (M5 factory-notice touchpoint + baseline attribution). Coverage: `go test -cover ./internal/epic/` → **87.6%** (≥85% threshold met). §25 template neutrality verified — producer Go source is NOT under `internal/template/templates/`; `grep -rnE 'SPEC-EPIC-STATUS-001|REQ-ES-' internal/template/templates/` → 0 matches (no mirror created). This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md`, the `progress.md` §E.4 audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot reference its own SHA until after it lands) and backfilled in a follow-up commit. MX Tag validation (sync sub-step): 0 `@MX:` tags present in `internal/epic/*.go` + `internal/cli/epic.go` — `BuildEpicStatus` / `DiscoverEpic` currently have fan_in = 1 (CLI only), below the `@MX:ANCHOR` threshold of ≥3 callers, so no tags are warranted at this time; deferred to a future `/moai mx` scan once fan_in grows. 🗿 MoAI
- **[SPEC-FACTORY-BOOTSTRAP-001](.moai/specs/SPEC-FACTORY-BOOTSTRAP-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`-f` / `--factory` is redefined from a one-session chain seed into factory membership, and a companion can now join an existing run from a second terminal.** Under the prior-art commit `94025ce0a`, a companion launched as `moai cc -f --name run-abc123` was misclassified as a lead (the dispatch short-circuited to the lead branch on any `-f` present, so a second chain was seeded); and a companion-shape `--name run-abc123` alone (no `-f`) entered companion mode silently. Both are corrected.
  - **`-f` is factory membership; `--name` shape selects the role.** `resolveFactoryBranch` (`internal/cli/factory.go`) is the four-row truth table the dispatch now consults: `-f` alone → lead; `-f --name <role>-<run-id>` → companion; `--name <anything>` alone → no-op. The dispatch in `cc.go` and `glm.go` parses both flags together instead of `else if`-ing past the companion branch, so a companion carrying `-f` reaches the companion branch. A lead sets `MOAI_FACTORY` + `MOAI_FACTORY_ID` + (when given) `MOAI_FACTORY_SPEC` and still seeds the `plan → run → verify → sync` chain (preserving `SPEC-FACTORY-MODE-001`); a companion sets only `MOAI_FACTORY_LABEL` and seeds nothing. **BREAKING from `94025ce0a`**: a companion-shape `--name` alone (no `-f`) is now a no-op rather than companion entry — companion entry requires `-f`.
  - **Inbound cross-session messages auto-accept via a transient `--settings` file.** `prepareFactorySettings` (`internal/cli/factory_settings.go`) writes `{"crossSessionInbound": "accept"}` to a session-private temp file (`os.TempDir()/moai-factory-<pid>-<nanosecond>.json`, `0600`, cleaned up on exit) and appends `--settings <file>` to the backend's argv. The `--settings` flag is documented to take the strictest merge, which is exactly the property needed: the accept/hold/refuse tier ladder is satisfied regardless of the operator's project/local settings, without mutating any persistent file. When the operator supplied their own `--settings`, moai injects nothing and the notice prints a verify-advisory instead (REQ-FB-007). A write failure degrades fail-open to launching without the injected flag (C8/EC-4).
  - **The SessionStart bootstrap notice is revised.** The lead notice (`factoryLeadNotice` in `internal/hook/session_start_factory.go`) now carries, in order: the run id; the four companion launch lines each carrying `-f` (so the operator copies a factory-membership command, not the bare `--name` form the prior-art notice printed); the leader socket path (`MOAI_FACTORY_LEAD_ADDR`, captured by `enterFactoryMode`); a one-line inbound-automation notice stating auto-accept is active via the injected `--settings` (or the verify-advisory when the operator supplied their own); and the SPEC identifier only when `MOAI_FACTORY_SPEC` is set. The companion notice (`factoryCompanionNotice`) is now **role-less** — it acknowledges the join and does not print "as the Y companion", removing the collision with the role-declaration contract the deferred `SPEC-KANBAN-BOOTSTRAP-001` `REQ-KS-006` owns. A malformed label degrades to the empty string (fail-open).
  - **CLI help documents both entry forms.** `moai cc --help` and `moai glm --help` now carry `-f [SPEC-ID]` (lead, seeds the chain) and `-f --name <role>-<run-id>` (companion, joins an existing run) in the `Use:` field and the `Long:` Flags prose, enumerating the four roles `plan`, `run`, `review`, `sync`. Both commands keep `DisableFlagParsing: true` (they forward arbitrary args to the backend), so the documentation lives in `Use:` / `Long:` and NOT in a `cmd.Flags()` registration — a flag registration under disabled parsing is silently inert and misleads readers about the parse site (REQ-FB-014, SHOULD).
  - **New docs-site Factory Mode page in 4 locales.** `docs-site/content/{en,ko,ja,zh}/multi-llm/factory-mode.md` (frontmatter `title` / `weight: 30` / `draft: false` each) + a `factory-mode` sub-entry under the existing `multi-llm` section in `data/menu/main.yaml` (4-locale `name` map + `ref: /multi-llm/factory-mode`). No new docs-site section, no `_meta.yaml` weight, no `menu.html` SVG case (the `multi-llm` icon already exists in the switch).
  - The block-cap raise (`launcher_blockcap_infinite.go`) continues to fire for both branches — `MOAI_FACTORY` (lead) OR `MOAI_FACTORY_LABEL` (companion) — preserving the prior-art unconditional behavior that `SPEC-FACTORY-MODE-001` REQ-FM-023 / `SPEC-INFINITE-GOAL-001` depend on; the two `TestAC003_*` preserve-tests stay green.
  - 27 acceptance criteria (AC-FB-001..027: 25 MUST + 1 SHOULD + 1 meta), all PASS, verified against the implementation files at HEAD `89227add0` of `feat/factory-bootstrap-guidance`. Run-phase landed in per-milestone commits `d3f8ffcda` (M1 settings injection) + `1b02bcf80` (M2 `-f` redefinition + flat dispatch) + `884e8e2dc` (M3 notice revision) + `c346682a2` (M4 CLI help) + `a87e6174b` (M5 docs-site 4-locale) + `89227add0` (M6 verification + §E.2/§E.3 evidence). `94025ce0a` is the prior-art commit this SPEC revises (NOT reverts). §25 template neutrality verified — `grep -rnE 'SPEC-FACTORY-BOOTSTRAP-001|REQ-FB-|94025ce0a' internal/template/templates/` → 0 matches; the docs-site page is NOT a template path and carries no SPEC/REQ/SHA tokens. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on `spec.md`, the `progress.md` §E.4 signal, and this CHANGELOG entry — no further code changes. `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3; Route B squash merge means the final SHA is known only after PR merge) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI
- **[SPEC-CODEX-PHASE2-001](.moai/specs/SPEC-CODEX-PHASE2-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **An MCP host can now hand a coding or investigation task to codex and then watch it, collect it, or stop it.** Before this, the codex app-server client could only run one shape of request — a review — and only for moai's own audit path. Four tools open it up, registered in `registerMoaiMCPTools` (`internal/cli/mcp_server.go`) with a declared JSON Schema and an explicit read-only hint each: `codex_task` returns the finished output when `background` is false and a job id when it is true; `codex_job_status` and `codex_job_result` read that job's record; `codex_job_cancel` stops a running turn. The hints are set by value rather than inherited — true for the two readers, false for the task and cancel tools — and they are *declarations to the host*, not enforcement.
  - **The session is reusable, and the model finally reaches the wire.** `openCodexSession` (`internal/cli/mcp_codex.go`) owns `initialize` → `thread/start` once and hands back a handle that drives many turns on the retained thread. `runCodexReviewRPC` survives as a thin caller, so the Stop-hook review gate and `codex_audit` keep their exact `(ReviewOutput, error)` contract and their fail-open summaries. Separately, the model and effort resolved by `template.ResolveAgentModelEffort` used to be computed and then dropped before transmission; they now ride the params codex actually receives, per-method (a turn carries model + effort, `thread/start` carries model, `review/start` carries neither). A model codex cannot serve is dropped together with its paired effort, so a project that never opts in transmits exactly what it did before.
  - **Jobs are durable, redacted, and bounded.** Each job is one JSON file under `.moai/state/codex-jobs/` (`internal/cli/codex_jobs.go`) written atomically, so a concurrent reader sees the previous record or the next one and never a partial write — no shared index, so two sessions on one checkout do not contend. The request summary is redacted for bearer tokens and secret-shaped key/values *before* it is truncated to 500 characters, because truncating first can split a secret and leave a usable prefix. The record carries no env, no argv, and no binary path. One `codex_task` turn is bounded by a new 600 s default (`internal/config/defaults.go`), deliberately distinct from the review gate's 900 s so tuning one does not silently move the other.
  - **Cancellation targets one process and refuses when it cannot prove ownership.** `codex_job_cancel` (`internal/cli/codex_job_control.go`) sends `turn/interrupt` with the `{threadId, turnId}` the record captured mid-flight, waits a bounded 5 s grace, then terminates the codex process this server spawned for that job — a single kill on a pid read from the live session's own connection, never a name or pattern sweep. Ownership is decided by live-session membership rather than by the pid stored in the record, so a stale or tampered record cannot direct a signal anywhere; a non-terminal record with no live session is refused. A job whose `turn/started` was never observed has no turn id and degrades to process termination instead of sending a malformed interrupt.
  - **Writing the working tree is opt-in and ships off.** New nested config key `workflow.codex.task.allow_write` (`internal/config/types.go`, default `false` in `internal/config/defaults.go`), read fail-closed — a missing file, a parse error, or an absent block all read as not opted in — and inspectable through `codex_setup`'s result, which is why it is a config key rather than an env var. It was deliberately **not** added to the distributed template config, so no user receives a key to flip by accident. Note what the gate does and does not do: it bounds what moai *requests* of codex, not what codex *enforces*. A `sandboxPolicy` is transmitted on every turn including read-only ones, because the protocol makes the field sticky on the thread and an omitted field would inherit a write-enabled policy from an earlier turn.
  - **Evidence, stated at its real strength.** 18 acceptance criteria (AC-CX2-001..018), all MUST, all recorded PASS. Five of the protocol behaviours were confirmed against a live `codex-cli 0.146.1` session: a second turn on an existing thread is accepted; `turn/started` arrives and its `turn.id` is accepted as `turn/interrupt`'s `turnId`, ending the turn `interrupted` in 152 ms; the `{"type":"readOnly"}` sandbox envelope is accepted and its stickiness is real; `review/start` does emit `turn/started`; and the approval-stall fix returns `turn/completed` with the write denied. **Everything else is a reading of codex's generated JSON Schema**, including the `{"decision":"decline"}` payload shape and the choice of `-32601` for unrecognized approval requests. `resume_last` continues the last recorded thread via `thread/resume`, but re-opening a thread from a *new process* is untested. `item/commandExecution/requestApproval` and `item/permissions/requestApproval` fall to the `-32601` arm, which unblocks the turn but is coarser than the `decline` their schemas would accept; whether codex handles that as gracefully is not established.
- **[SPEC-HARNESS-LEARNING-EVO-001](.moai/specs/SPEC-HARNESS-LEARNING-EVO-001/spec.md)** + **[SPEC-HARNESS-LEARNING-EVO-002](.moai/specs/SPEC-HARNESS-LEARNING-EVO-002/spec.md)** — sync-phase close (3-phase plan→run→sync). **The harness can now learn which agents actually handle which `/moai` subcommand, from observation instead of hand-tuned YAML.** MoAI routes each subcommand to a default set of agents through `.moai/config/sections/delegation.yaml`, whose own header declares a learning loop — observe real runs, propose amendments, never auto-apply. The *observe* half had never worked, for two independent reasons, both found by measurement rather than by reading code.
  - **The routing ledger recorded nothing.** `.moai/state/routing-ledger.jsonl` held four rows accumulated over a month, every one of them with an empty delegation list. Two causes: the only path that created a row was an instruction the orchestrator had to remember on each dispatch, and the single store operation available (`Record`) closed the caller's own prior pending row as a re-route. Recording is now mechanical, derived from hook input at three seams — a prompt arriving, a subagent stopping, a session ending — and the store operation is split into create-if-absent and annotate, with `Record` keeping its original re-route semantics for explicit re-dispatch. Every emission path is fail-open, and while either harness observation gate is closed nothing is written at all: no row, no pending file, no ledger append.
  - **The terminal signal that closes a row was never produced.** Across 37,107 telemetry records, the number carrying a test-pass or test-fail classification was zero. The evidence writer's Bash branch was unreachable from the shipped wiring: the `PostToolUse` hook that reaches it is registered for `Write|Edit|MultiEdit` and never matches a `Bash` call. Test outcomes are now observed through the always-on observe channel scoped to Bash, so rows close as `success` or `fail` instead of accumulating unterminated. No `settings.json` or template change was required, and no evidence record is written twice for one tool call.
  - **A new analyzer reads those rows** — `internal/harness/delegationmap`, exposed as `moai harness delegation analyze` with `--json` and `--dry-run`. It aggregates, per subcommand, which agents were actually delegated, and raises two kinds of finding: an agent that clears both an observation-count and a support-ratio threshold but is not designated in the map, and a designated agent observed zero times. Agent identity is compared by exact match against the retained agent catalog, because a *named* spawn writes its name into `agent_type` — a spawn of `plan-auditor` named `audit-hle` is recorded and classified as non-catalog, never mistaken for a designation the map forgot. Delegations carrying no identity are counted separately and reported as an unattributed share, so a reviewer can see how thin the evidence under a finding is. Findings become drafts through the existing proposal writer; runs are deterministic and re-running over unchanged input churns nothing. An absent, empty, oversized, or malformed ledger is not an error — the command exits 0 with an empty result and a machine-readable reason.
  - **Applying a proposal remains a human decision, by design.** The analyzer only proposes. `delegation.yaml` is opened read-only and never written; amending it stays a Tier-4 decision the orchestrator takes with the user at the existing approval gate, and `.claude/lsel/frozen-allowlist.json` is unchanged, including the `^\.moai/config/sections/` pattern that freezes the map. Nothing shipped here can alter routing on its own.
  - **Known limits, stated rather than deferred.** Pass/fail detection reads the runner's output text, because the observe channel carries no structured exit code — a test runner whose output matches none of the known markers yields no signal, and its row closes later as `abort` rather than `fail`. One unreproduced dogfood run also produced two rows for a single session, consistent with a terminal signal arriving before the first delegation; it is filed as an ordering hazard rather than a coding error, and left open deliberately. Both are recorded in the SPECs' residual-risk sections.
  - Verification closed on a live run rather than on fixtures: a real session produced a ledger row carrying its delegation with the correct agent identity and a terminal `success` outcome. 32 acceptance criteria across the two SPECs, all passing, one of them at PASS-WITH-DEBT with the reason recorded in `progress.md`. Route B (PR-mandatory). 🗿 MoAI
- **[SPEC-INFINITE-GOAL-001](.moai/specs/SPEC-INFINITE-GOAL-001/spec.md)** — sync-phase close (3-phase plan→run→sync). The goal loop's 1M number was never a hard cap — it is the auto-compact window. This SPEC removes the THREE caps that actually broke "arm a goal and walk away" and adds REAL bounds, so an armed `/moai goal` now persists across auto-compact boundaries instead of being force-stopped by vestigial ceilings. Third and final P0 of the autonomy-workflow redesign (§3.2 v2 + §1.2 + the C2 finding). **NEW arm flags on `moai goal arm`** (`internal/cli/goal.go`): `--max-turns N` (0 = infinite, auto-compact-driven; default 30 unchanged for full backward compat), `--max-duration <sec>` (wall-clock seconds since arm time — the M-default PRIMARY run-time bound), `--cost-cap <value>` (recorded in `Ceiling`; enforcement is a documented follow-up because `Eval`/`Ceiling` carry no invocation/token accounting today). **Arm-time fail-closed (REQ-INFINITE-GOAL-004 / AC-011)**: `--max-turns 0` with NEITHER `--max-duration` NOR `--cost-cap` is REJECTED at arm time (non-zero exit + stderr naming the missing bound + no goal file written) — no infinite goal runs without a real bound. **Run-time bounds (REQ-INFINITE-GOAL-004 / AC-005 / AC-006)**: wall-clock fires a verdict indistinguishable from the MaxTurns ceiling (5-section Claim/Evidence/Baseline/Gaps/Residual-risk); the stagnation guard is strengthened from "same note 3×" to a mechanical-condition fingerprint (exit + output-hash + bounded file-set SHA derived from the condition commands' paths). **`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP` handling (REQ-INFINITE-GOAL-002 / AC-003)**: the doctrine (`goal-directive.md` + `workflows/goal.md`) surfaces the silent runtime cap (default 8, `min(30, 8) = 8` today) so users understand an infinite goal requires raising it (e.g. to 200); the `moai cc` / `moai cg` launchers inject `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP=200` into the session env when a `--max-turns 0` goal is armed at launch time (best-effort, fail-open; one-line addition via the existing `buildEnvForLaunch` pattern). **Statusline `/clear` suppression (REQ-INFINITE-GOAL-003 / AC-004)**: when a goal is armed for the current session, `internal/statusline/renderer.go` `handoffGuideStage` suppresses the soft/hard `/clear` directive markers (the stage is still computed for informational purposes), letting auto-compact handle context pressure instead of forcing a `/clear`; GoalArmed==false retains the current markers unchanged. **SessionStart(compact) re-inject (REQ-INFINITE-GOAL-005 / AC-007)**: a new `matcher: compact` SessionStart hook reads the live armed goal + the active SPEC's `progress.md` tail and emits the goal condition, SPEC-id, last-verified mechanical state, and single next action — the official "re-inject context after compaction" mechanism (no-op when no goal is armed). **`/clear` auto-rearm (REQ-INFINITE-GOAL-006 / AC-008)**: `moai handoff save` embeds any live armed goal's verbatim condition + arm-time `Ceiling` into the pending handoff record; `internal/hook/handoff_inject.go` `claimAndInject` writes a NEW goal state file under the NEW session-id on `/clear` (mode=auto), preserving session-id keying; the embedded Ceiling is re-validated before writing (an unbounded embed is rejected, the AdditionalContext injection still proceeds). **`ac_converge` doc correction (REQ-INFINITE-GOAL-007 / AC-009)**: `.claude/skills/moai/workflows/run.md` "Max 20 turns" clause is NOT parsed by `parseCondition` (the `trailingExitClause` regex matches only `exits <N>`), so the `ac_converge` goal actually runs at `DefaultMaxTurns=30`; the doc now states the actual 30-turn execution (option (b) doc-only — option (a) adding the regex would have silently changed 30→20 for every user and contradicted AC-002). **deny/ask invariance (AC-010)**: an armed infinite goal does NOT weaken the deny/ask rules — verified by a table-driven regression guard (`TestAC010_DenyAskHoldsUnderArmedInfiniteGoal`) confirming policy decisions are identical across no-goal and armed-infinite-goal cells (policy is pure (cmd, policy); it does not read goal state). Eleven REQ (REQ-INFINITE-GOAL-001..007) / 11 MUST ACs (001..011), all PASS. Quality gate clean: `go build ./...` exit 0, `GOOS=windows GOARCH=amd64 go build ./...` exit 0, `golangci-lint run --timeout=2m ./internal/goal/... ./internal/statusline/... ./internal/hook/... ./internal/cli/... ./internal/config/...` 0 issues on changed packages, `go test -race ./internal/goal/... ./internal/statusline/... ./internal/hook/...` all pass. **Infinite-loop safety trio (load-bearing)**: AC-011 (arm-time fail-closed) + AC-005 (wall-clock run-time bound) + AC-006 (stagnation halt) — all three green; no infinite goal runs without a real bound, and no unbounded arm is accepted. Run-phase landed in per-milestone commits on `worktree-autonomy-epic` (`b3a30c982` M1 arm flags + AC-011 fail-closed + `47277d1e4` M2 block-cap doctrine + launcher inject + `85a791efe` M3 statusline suppression + `040749776` M4 wall-clock + stagnation + `0407ef019` M5 SessionStart compact re-inject + `cfa601e2a` M6 `/clear` auto-rearm + `279831f37`+`1d3bd237e` M7 ac_converge doc-only correction), merged to `main` via PR #1317 (squash `adc867545`). **⚠️ USER-VISIBLE BEHAVIOR CHANGE — RESIDUAL RISK (statusline `/clear` suppression when goal armed)**: when a goal is armed, the `(⚠️/clear)` / `(🛑/clear!)` statusline markers are suppressed; users who relied on the marker to know when to manually `/clear` lose that signal while a goal is active. **Mitigation**: the stage classification is still computed (informational), only the directive text is suppressed; the goal's own per-turn output becomes the replacement signal; and the suppression is scoped to GoalArmed==true only (no-goal sessions retain the markers unchanged). **Cost-cap enforcement is a documented follow-up**: `Ceiling.CostCap` is recorded at arm time (per REQ-004 "recorded in Ceiling alongside MaxTurns") but its enforcement is deferred — the evaluator carries no invocation/token accounting surface today (OQ-2 resolution). An infinite goal is bounded by wall-clock (AC-005) + stagnation (AC-006), sufficient for "arm and walk away" but not cost-sensitive deployments. **Pre-existing baseline note**: `TestTemplateNoInternalContentLeak` flags `quality-gates-quality.md` matching `REQ-004` (from `SPEC-AUDIT-SNAPSHOT-001`, commit `630f0f44f`) — confirmed present at this branch's base before M1, NOT introduced by this SPEC. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on the sole YAML-frontmatter-bearing artifact (`spec.md`), the `progress.md` §E.4 signal, the README ×4 one-line note, and docs-site flag additions (`utility-commands/moai-goal.md` + `cli-reference/goal.md` across 4 locales) — no further code changes. `sync_commit_sha` populated as `pending-backfill-*` placeholder in this commit (self-referential-hazard workaround per spec-frontmatter-schema.md D3) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗺 MoAI
- **[SPEC-STOPCHAIN-TRIM-001](.moai/specs/SPEC-STOPCHAIN-TRIM-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Three stop-chain / per-edit / mode-token items from the autonomy-workflow redesign §3.5 + §3.1 (P0, second of the autonomy epic): **A10** stop-chain shell trim, **A8** per-edit hook consolidation, **A11** mode-aware hooks via the new `MOAI_AUTONOMY_TIER` mode token. **NEW `MOAI_AUTONOMY_TIER` env var** (`internal/config/envkeys.go` `EnvAutonomyTier`, `internal/config/defaults.go` 3-value enum `semi-auto | automatic | fully-autonomous`, `internal/config/autonomy.go` `AutonomyTier()` reader + `IsAutonomyTierCommitGateOff` / `IsAutonomyTierLifecycleDormant` branch-point predicates) — users opt into higher autonomy tiers; unset/empty/invalid falls back to `semi-auto` (zero behavior delta). **A10**: `handle-stop-goal.sh` exec's `moai hook stop-goal` ONLY when a goal state file exists for the current session (goal-absent precondition `exit 0` at the shell layer — a session with no armed goal pays ZERO `moai` cold-starts per turn-end); `sync-phase-quality-gate.sh` exec's its logic ONLY when the HEAD commit subject matches a sync-commit sentinel (non-sync HEAD → no `go vet` / `go build` / lint spawn); the 4 observer/security Stop hooks (`handle-stop.sh`, `handle-stop-failure.sh`, observer Stop, security Stop) transition to `async:true` advisory mode. **A8**: the `develop-pre-implementation` (PreToolUse, 5s) + `develop-post-implementation` (PostToolUse, 10s) per-edit spawns on every Write|Edit are removed from `manager-develop.md` frontmatter; the verification work moves to a per-cycle `develop-completion` Stop hook at milestone/commit boundaries — a TDD cycle of N atomic edits now pays ZERO per-edit spawns. **A11**: `sync-phase-quality-gate.sh` reads `$MOAI_AUTONOMY_TIER` and branches (fully-autonomous → advisory only / no `decision:block`; automatic → block ONLY on `go build` failures, advisory for lint/test/coverage; semi-auto → full blocking unchanged); `internal/hook/pre_tool.go` IsGitCommit gate (~L444) reads the tier via `config.AutonomyTier()` and is OFF at automatic/fully-autonomous (no synchronous vet+lint+test tax on commits); SubagentStop (`develop-completion`), TeammateIdle, and TaskCompleted lifecycle hooks are dormant at fully-autonomous (observe-only — audit-log written, no block / reject / `AskUserQuestion` translation). **⚠️ USER-VISIBLE BEHAVIOR CHANGE — RESIDUAL RISK (handle-stop.sh async transition)**: `handle-stop.sh` is now `async:true` (advisory) — the main Stop handler can no longer block synchronously; its result is delivered as `additionalContext` on the next turn rather than as a synchronous decision. Deployments that relied on synchronous `handle-stop.sh` blocking now get advisory behavior from that hook. **Mitigation**: the blocking Stop hooks (`sync-phase-quality-gate.sh`, `handle-stop-goal.sh`) remain synchronous — only the 4 observer/security Stop hooks transitioned. **deny/ask invariance (REQ-007)**: the destructive-pattern denylist (`git push` to main, secrets, `rm -rf`, deploy, etc.) remains binding at EVERY tier — `semi-auto`, `automatic`, `fully-autonomous`, and unset — verified by a 4×N table-driven regression guard (`TestAC006_DenyInvariantAtEveryTier`). The tier branch in `pre_tool.go` relaxes ONLY the synchronous verification gate, never the deny path; the `checkBashCommand` deny runs UNCONDITIONALLY after the tier branch regardless of tier (K-6 deny-before-tier ordering invariant, now `@MX:WARN`-tagged). Eight REQ (REQ-STOPCHAIN-TRIM-001..007) / 8 MUST ACs (001, 002, 003, 004, 005, 006, 006b, 007), all PASS. Quality gate clean: `go build ./...` exit 0, `go vet ./internal/hook/... ./internal/config/...` exit 0, `golangci-lint run --timeout=3m ./internal/config/... ./internal/hook/...` 0 issues, `go test -race ./internal/config/ ./internal/hook/ -count=1 -run 'TestAC00|TestAutonomyTier'` ok, coverage 80.7% (`internal/config`) / 84.3% (`internal/hook`), `GOOS=windows GOARCH=amd64 go build ./...` exit 0. MX tags: `@MX:ANCHOR [AUTO]` on `AutonomyTier` reader (run-phase M1, fan_in ≥ 3 with `@MX:REASON`); `@MX:WARN [AUTO]` on `pre_tool.go` tier branch (K-6 deny-before-tier ordering invariant, sync-phase addition). **§25 template neutrality**: all run-phase shell-hook + agent-frontmatter + settings edits mirrored to `internal/template/templates/` byte-identical (the local `.claude/settings.json` vs template `.json.tmpl` difference is the pre-existing `command:`-vs-`args:` renderer convention, NOT SPEC drift); `catalog.yaml` regenerated (M3 cascade); no SPEC-ID leakage into the template tree (the 5 mirrored hooks carry `SPEC-STOPCHAIN-TRIM-001` references in comments per the `SPEC-OBSERVE-HYGIENE-001` precedent — the existing sync-phase-quality-gate.sh already carries such references — and the neutrality CI guard did NOT flag them). Run-phase landed in per-milestone commits `7d041992e` (M1 mode token) + `dfa010c21` (M2 stop-chain shell trim, A10) + `8cf9a807a` (M3 per-edit consolidation, A8) + `a695a0b1a` (M4 mode-aware hooks, A11) + `a382666ce` (catalog regen chore) + `e94d357bf` (M5 run-phase evidence, 8/8 ACs PASS); a pre-sync polish commit `d395efb15` added the `@MX:WARN` on the pre_tool.go tier branch and silenced an errcheck on the AC-007 test fixture. **Pre-existing baseline note**: `TestTemplateNoInternalContentLeak` currently flags `quality-gates-quality.md` matching `REQ-004` (2-segment) — this is a `SPEC-AUDIT-SNAPSHOT-001` A4 wiring-note artifact, NOT this SPEC's regression (verified pre-existing). This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on the sole YAML-frontmatter-bearing artifact (`spec.md`), the `progress.md` §E.4 signal, and this CHANGELOG entry — no further code changes. `sync_commit_sha` populated as `pending-backfill-*` placeholder in this commit (self-referential-hazard workaround per spec-frontmatter-schema.md D3) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI
- **[SPEC-AUDIT-SNAPSHOT-001](.moai/specs/SPEC-AUDIT-SNAPSHOT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Four audit fast-wins from the autonomy-workflow redesign (P0): **A1** sticky artifact-hash cache (`internal/runtime/audit_cache.go` — drops the 24h TTL, cache now hash-only-valid; Tier L hash extends the mechanical subject set to `design.md` + `research.md`, `tasks.md` retained for grandfathered V3R4); **A2** skip-eligible threshold aligned to per-tier PASS via `internal/runtime/audit_snapshot.go` `SkipEligibleByScore` (S 0.75 / M 0.80 / L 0.85, replacing the retired flat `≥ 0.90`); **A3** 4-dimension sync-audit workflow verdict is BINDING on the happy path via `internal/runtime/sync_4dim_binding.go` `FourDimVerdict.IsBinding()` (cold sync-auditor spawn suppressed on clean PASS; INCOMPLETE / dim-0 / contested-finding fallback preserved); **A4** shared diagnostic snapshot keyed by HEAD SHA + claim/lock serialization in `internal/verify/claim_lock.go` (in-process per-key mutex + `O_EXCL` cross-process lock; `RecordCheck` race fix that was dropping 3/4 dimensions). **USER-VISIBLE behavior change**: the plan-audit skip policy in `.claude/rules/moai/workflow/spec-workflow.md` is now THREE conditions (PASS verdict + score ≥ per-tier threshold + artifact-hash unchanged), not the former four — the "Within 24h" condition is retired and the flat `≥ 0.90` threshold is replaced by per-tier PASS thresholds. Four REQ (REQ-AUDIT-SNAPSHOT-001..004) / 4 MUST ACs (with 7 sub-variants — 001, 001b, 002, 003, 003b, 004, 004b), all PASS. Quality gate clean: `go vet ./...` exit 0, `golangci-lint run` 0 issues, `go test -race ./internal/runtime/... ./internal/verify/...` ok, coverage 88.0% (`internal/runtime`) / 80.7% (`internal/verify`), `go build ./...` exit 0. MX tags added in the sync commit: `@MX:WARN` on `claim_lock.go` (two-layer concurrency primitive), `@MX:NOTE` on the two redesign-codification anchors (`audit_snapshot.go`, `sync_4dim_binding.go`). Run-phase landed in per-milestone commits `234d6fc02` (M1, A1+A2) + `a369bd05c` (M2, A3) + `630f0f44f` (M3, A4) + `88042d706` (M4 run-phase evidence); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition across spec.md/plan.md/acceptance.md, the `progress.md` §E.4 signal, the MX tag additions, and this CHANGELOG entry — no further code changes. No docs-site change required — the skip-policy threshold change is documented in CHANGELOG (the canonical user-visible record) and the existing SPEC-lifecycle docs already defer to `spec-workflow.md` as the SSOT. `sync_commit_sha` populated as `pending-backfill-*` placeholder in this commit (self-referential-hazard workaround per spec-frontmatter-schema.md D3) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI
- **[SPEC-SESSION-WORKTREE-001](.moai/specs/SPEC-SESSION-WORKTREE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Added opt-in (default-OFF) automatic worktree isolation for `moai init` / `moai profile` / `moai web` sessions, with worktree-scoped git config (per-worktree `safe.directory`, `core.gitconfig` identity, `init.defaultBranch`, and the three opt-in knobs `workflow.worktree.auto_create` / `auto_cleanup` / `auto_merge`) and on-touch PR-merge auto-cleanup wired into `moai session register` / `moai session list`. Twenty-four REQ / 24 AC, all 24 PASS, including seven observed falsification round-trips (fail-back, dirty-preserve, clean-exit-only, advisory-suppression, profile-dir-NOT-isolated, trigger-invariant, safe.directory-idempotency), linux+windows cross-build exit 0, lint clean, and §25 template neutrality verified (zero SPEC-ID / `[WT]` / `session_worktree` leakage into `internal/template/templates/`). The run-phase implementation landed in PR #1305 (squash merge `f9ee7bbd1`); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition, the `progress.md` §E.3/§E.4 signals, and this CHANGELOG entry — no additional code changes. `sync_commit_sha` populated as `pending-backfill-*` placeholder in this commit (self-referential-hazard workaround per spec-frontmatter-schema.md D3) and backfilled in a follow-up commit after this sync PR merges. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-UPDATE-VERSION-FLAG-001](.moai/specs/SPEC-UPDATE-VERSION-FLAG-001/spec.md)** — sync-phase close (3-phase plan→run→sync). `moai update` gained a `--version <tag>` flag that installs a **specific** GitHub release tag (stable / rc / previous version) through the existing checksum-verified download path, replacing the previous "forward-to-latest-only" limitation. Sixteen REQ / 16 AC, all 16 PASS. **Implementation (M1–M6, `internal/cli/update.go` + new `internal/cli/update_version.go`)**: the flag is registered on the existing `moai update` cobra command (REQ-UVF-001); the new code path `runVersionBranch` runs BEFORE the default binary-update + template-sync flow so a non-project cwd does not block a binary-only install (parity with `--binary`). Tag values are validated by `tagCharsetRegexp` (`^v?[0-9A-Za-z.\-]+$`) BEFORE URL construction — a path-traversal defense that rejects `/`, `?`, `#`, `+`, and whitespace, so a malicious tag value cannot inject path or query fragments into the GitHub API URL (`@MX:ANCHOR` [AUTO] per plan §D#4). `normalizeVersionTag` adds a canonical `v` prefix (so `3.0.0` and `v3.0.0` hit the same URL) and rejects dev-branch `go-v*` tags outright (they belong to the dev-branch self-update path, not `--version`). Tag resolution hits `GET /repos/modu-ai/moai-adk/releases/tags/<tag>` on the compiled `githubReleasesURL` api.github.com allowlist — the URL is constructed from compiled constants, never from environment input, and a poisoned `MOAI_UPDATE_URL` is still rejected fail-closed by the existing `validateUpdateURL` allowlist gate BEFORE any `--version` HTTP call. **Reuse, do NOT fork (AP-UVF-001)**: `installVersionTag` reuses `update.NewChecker`, `update.NewUpdater`, and `update.NewRollback` — the same objects `EnsureUpdate` constructs for the default path. The only new logic is tag validation/normalization, the tag-resolution URL shape, and the backup→download→replace sequence that mirrors `orchestratorImpl.Update` steps 2-4 without the `IsUpdateAvailable` semver gate (a `--version` install is explicit, regardless of newer/older). **Checksum is MANDATORY and has no bypass (REQ-UVF-006)**: there is no `--skip-checksum` / `--insecure` flag, and a checksum mismatch discards the downloaded bytes (the updater cleans its temp file on error) and never reaches `Replace`, so the running binary path is byte-identical pre/post — the mismatch error references CWE-345. **Mutual-exclusion matrix (REQ-UVF-007)**: `--version` is mutually exclusive with `--check`, `--templates-only`, `--restore`, and `--dry-run` (each combination exits non-zero with a usage error naming the conflicting pair, before any network call); `--version` + `--binary` / `--force` / `--yes` are permitted. **Defect paths (REQ-UVF-008 / -009 / -010 / -011)**: HTTP 404 (tag not found), no matching binary asset for the running `GOOS/GOARCH`, checksum mismatch, and network failure each exit non-zero with a structured error naming the tag, the phase (`resolution` vs `download`), and the underlying cause; every defect path leaves the filesystem untouched. **Downgrade confirmation (REQ-UVF-013)**: on an interactive TTY, a requested tag older than the running version prompts for confirmation; `--yes` OR non-TTY stdin skips the prompt. **Re-exec (REQ-UVF-014)**: a successful install re-execs into the newly installed binary (setting `MOAI_SKIP_BINARY_UPDATE=1`) unless `--binary`, so subsequent template sync runs against the new binary's embedded templates. **AC-UVF-002 host-confinement**: the captured outbound request satisfies `req.Host == "api.github.com"` AND `req.URL.Path == "/repos/modu-ai/moai-adk/releases/tags/v3.0.0"` on the constructed tag-resolution URL (not only on the env-var rejection path). **Run-phase verification (16/16 ACs PASS)**: full `go test ./...` exit 0, `golangci-lint run` 0 issues, `GOOS=windows GOARCH=amd64 go build ./...` exit 0, and `git diff --name-only origin/main...HEAD` shows no `internal/template/templates/` path (AC-UVF-015 — scope hygiene; `deps.go` was NOT modified by this SPEC). **4-locale docs-site update (M7 / REQ-UVF-016)**: `cli-reference/update.md` and `getting-started/installation.md` updated in all four locales (ko / en / ja / zh); `hns-oss-docs-verify` exit-gate recipe PASS (warning-free `hugo --minify --gc`, sitemap present, URL-blacklist 0 matches, Mermaid LR/RL 0 matches, 4-locale file-existence + section-count parity at 974 `## ` headings each, README 4-file H2 parity at 13 each, no body-emoji in the M7-modified files). The run-phase implementation + tests + docs-site update landed in commits `843be53d9` (M1–M6) and `c7bae3a77` (M7); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition on the sole YAML-frontmatter-bearing artifact (`spec.md`), the `progress.md` §E.4 signal, and this CHANGELOG entry — no additional code, test, or docs changes. §25 template neutrality N/A for this sync commit — `git diff --name-only origin/main...HEAD | grep '^internal/template/templates/'` → NONE for this commit range. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-PHASE-FRONTMATTER-OWNER-001](.moai/specs/SPEC-PHASE-FRONTMATTER-OWNER-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Established the `phase:` frontmatter value contract — who owns post-authoring corrections, the authoring-time guard preventing workflow-stage tokens (`plan` / `run` / `sync` / `mx`) from being written into the release-target field, and the schema-SSOT prose that binds the field to the era-classification H-5 tie-breaker. Three surviving milestones shipped. **M1** (정정 소유자 / AC-PFO-006·007): the canonical owner of a `phase:` correction is `manager-spec`, reached via orchestrator re-delegation — documented as a separate section in `spec.md` §1.6 outside the Status Transition Ownership Matrix (which binds `status:`, not `phase:`). **M2** (값 계약 / AC-PFO-001·002·003): `spec-frontmatter-schema.md` now normatively defines the field as a release-target label, lists the four prohibited lifecycle-stage tokens (`plan` / `run` / `sync` / `mx`) case-insensitively on whole-value match, and records that the field is load-bearing for `internal/spec/era.go` H-5 era tie-breaker (`matchesModernPhase` returns true only for a value carrying a modern release prefix — a lifecycle token there silently degrades a two-signal predicate to one). **M3** (저작 시점 방지 / AC-PFO-004·005): the authoring-time guard lives on two surfaces — the `manager-spec` body's pre-write checklist and the `spec-assembly` skill — so a workflow-stage token cannot be written into `phase:` at authoring time. **M6** (회귀 검증 / AC-PFO-010·014·015): full `moai spec lint` baseline confirmed steady at 62 warnings / 0 errors post-edit, and the local↔template parity was preserved (`0` SPEC-ID / REQ / AC tokens leaked into the distributed template mirror). The run-phase doctrine edits landed in PR #1286 (squash merge `850de684c`); this sync commit carries only the `in-progress → implemented → completed` transition on the sole YAML-frontmatter-bearing artifact (`spec.md`), the `progress.md` §E.4 signal, and this CHANGELOG entry — no additional code or doctrine changes. **Scope-reduction honesty (recorded, not buried)**: this SPEC was Tier M at plan-phase with 16 working ACs across 5 milestones; the parallel SPEC-PHASE-FIELD-VALIDATION-001 (PR #1285, squash merge `998744216`) landed the same lint guard + 9-file correction first, so M4 (the `PhaseValueRule` lint code) and M5 (the 9-artifact correction) were **excluded** from this SPEC — the SPEC was reduced Tier M → **Tier S** as a result, and the 16-AC working set was tightened to 8 surviving ACs (AC-PFO-001/002/003/004/006/007/014/016, inline in `spec.md` §3). M4·M5 run-phase evidence is **preserved verbatim** in `progress.md §E.2` (lines ~269–420) with the boundary explicitly stated — the work was actually performed and all 16 working ACs were observed PASS, but the parallel SPEC landed first so the contribution is NOT carried by this PR. The Tier-S artifact contract also means this SPEC carries **no `acceptance.md`** (ACs are inline in `spec.md §3`), so the 4-artifact frontmatter transition reduces in practice to 1 YAML-frontmatter artifact (`spec.md`); `plan.md` and `progress.md` use the markdown-header convention without frontmatter. **§25 template neutrality N/A for this sync commit** — this commit touches `.moai/specs/` markdown + `CHANGELOG.md` only; the doctrine edits that landed in PR #1286 were mirrored to `internal/template/templates/` in that run-phase commit, not here. Verified on the worktree: `git diff --name-only origin/main HEAD | grep '^internal/template/templates/'` → NONE for this commit range, and the pre-emission duplicate check `grep -c 'SPEC-PHASE-FRONTMATTER-OWNER-001' CHANGELOG.md` returned `0` before this entry was added. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-PHASE-FIELD-VALIDATION-001](.moai/specs/SPEC-PHASE-FIELD-VALIDATION-001/spec.md)** — sync-phase close (3-phase plan→run→sync). A mis-authored `phase:` frontmatter value is now a **hard lint error** instead of passing silently. The field is schema-defined as a release target (`"vX.Y.Z"`), but nothing validated its value shape, no authoring instruction described it, and 31 artifact files across 20 SPECs carried workflow-stage tokens (`plan` / `run` / `sync`) in it instead. Four milestones landed. **M3** corrected the 14 in-scope files to `"v3.0.2"` (9 `spec.md` + 5 siblings), derived from git rather than assumed — every one of them first landed after the `v3.0.1` tag. It landed *before* M1 by necessity: the new code is non-demotable, so the reverse order would have broken repo lint at 9 errors. **M1** added `FrontmatterPhaseInvalid` at `SeverityError` in `internal/spec/lint.go` — a NEW finding code deliberately absent from `eraDemotableCodes`. `applyEraDemotion` switches on the code name, so an unregistered code survives on grandfathered SPECs; reusing the existing `FrontmatterInvalid` would have demoted the guard on ~96% of non-terminal SPECs, namely every SPEC lacking a `sync_commit_sha`, which is every SPEC at authoring time. The predicate is a **denylist on exact equality**, not a version allowlist: measured against 563 files, a strict-semver allowlist yields 301 false positives and substring containment yields 8 (`Run` matching inside `Runtime`), while exact match yields 0. **M2** documented the field for authoring agents in `.claude/agents/moai/manager-spec.md` and its template mirror (`internal/template/templates/.claude/agents/moai/manager-spec.md`), cross-referencing the frontmatter-schema SSOT rather than restating it; the mirror text carries no internal tokens and `internal/template/catalog.yaml` was regenerated. **M4** proved the guard is load-bearing via two falsifiability round-trips in `internal/spec/lint_phase_test.go`, both of which fail as required: removing the call site, and gutting the predicate while leaving the call site intact. The second is the one that matters — reachability alone proves nothing. 16/16 acceptance criteria judged. Verified: `go test ./...` exit 0 across 105 packages, `golangci-lint` 0 issues, repo spec lint steady at 62 findings / 0 errors. Known debt: `AC-PFV-006` cannot fail while the repo carries an empty error set, so it is a weaker check than its siblings. This sync commit carries the frontmatter `in-progress → implemented → completed` transition across all four artifacts, the `progress.md` §E.4 signal, and this CHANGELOG entry — no additional code changes. 🗿 MoAI

- **[SPEC-PROFILE-MEMORY-001](.moai/specs/SPEC-PROFILE-MEMORY-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Fixed three defects in the Claude profile subsystem — the `~/.moai/claude-profiles/<name>` directories that `moai cc | glm | cg -p <name>` hand to Claude Code as an isolated `CLAUDE_CONFIG_DIR`. Tier L, six milestones, 21 acceptance criteria: 19 PASS, 2 PASS-WITH-DEBT, 0 FAIL. **(1) Per-project profile memory.** The launch ledger `launch.yaml` held a single global `last_profile`, so launching a named profile in project B silently overwrote what project A would come back to. It now also carries a `projects:` map keyed by normalized absolute project path (`projectsKey = "projects"` in `internal/profile/profile.go`), written alongside — never in place of — `last_profile`, so an older binary that knows only the legacy key still reads the ledger. Bare-launch resolution walks this project's entry → the global `last_profile` → `default`; an explicit `-p` still wins outright, a recorded profile whose directory has since disappeared is skipped rather than resolved, and `MOAI_NO_PROFILE_FALLBACK=1` disables both lookups. The new project-scoped surface is `ResolveLaunchProfileForProject`, `RecordLaunchProfileForProject`, and `GetCurrentNameForProject`; four of the five pre-existing call sites were left unchanged. **(2) The ledger no longer records profiles that do not exist.** The record call sat before directory creation, so a mistyped name could be written into the ledger and then resolved on the next bare launch, sending the user into a directory that was never created. The call moved to after creation and gained an existence guard, and the ledger write stays atomic (`os.CreateTemp` + `os.Rename`) so a failure leaves no partial state. **(3) A fresh profile now warns before it drops the user on the login screen.** A newly created profile directory has no `.claude.json` — the per-config-dir account and onboarding state — so Claude Code starts at the login prompt, which reads as the profile switch having failed. The launcher now prints a stderr notice first, emitted exactly once per launch. Nothing is copied or seeded into the new directory: the credential carrier differs per platform, and quietly cloning account state across an isolation boundary is the wrong default. Touched `internal/profile/`, `internal/cli/launcher.go`, `internal/cli/web.go`, one closure in `internal/web/app.go`, plus tests; `internal/profile` and `internal/web` tests were additionally pinned so they can no longer reach the real home directory. Verified on the final tree: `go build ./...` exit 0, `go vet ./...` exit 0, `GOOS=windows GOARCH=amd64 go build ./...` exit 0, `golangci-lint run ./internal/profile/... ./internal/cli/... ./internal/web/...` → `0 issues.`; `internal/profile` coverage 81.2% → 82.9%. Carried debt is recorded in `progress.md` §E.2/§E.3 rather than buried: AC-PM-014(3) and AC-PM-021(3) each asked for a falsification round-trip (remove the sandbox guard, observe a FAIL, restore) that was **not executed**, so those two guards are asserted but not proven falsifiable; the `os.Rename` failure recipe behind AC-PM-020 is confirmed on macOS only; and `internal/cli` coverage was not measured (a full package run takes 271s). §25 template neutrality N/A — `internal/template/templates/` was untouched by every commit in this SPEC (`git diff --name-only 237fe01a4^..b4ae291e6 | grep -c 'internal/template/templates/'` → `0`). 4-locale documentation (`docs-site/content/{ko,en,ja,zh}/cli-reference/profile.md` and `.../web.md`, plus three screenshots under `docs-site/static/images/profile/`) landed in `ff2f728a9` and states in each locale that this behavior is on `main` but **not yet in a release** — nothing here ships to users until the next tag. Implementation commits `237fe01a4`, `6d5b3696d`, `dab895cfd`, `90dca4f4c`, `525b3c723`, `fa0d5898a`, `d6d2eac40`, `b4ae291e6`, `903fb4a67`; this sync commit carries the frontmatter `in-progress → implemented → completed` transition, the `progress.md` §E.4 signal, and this entry — no additional code changes. **Amendment (in-place):** closed the AC-PM-014(3)/AC-PM-021(3) sandbox-guard falsification-roundtrip verification debt recorded above — both guards now proven falsifiable (disable → FAIL observed → restore → PASS observed), promoting the two PASS-WITH-DEBT rows to PASS (21/21 PASS). No user-visible change; production code byte-identical to the prior close. Not pushed. 🗿 MoAI

- **[SPEC-REF-SEO-ABSORB-001](.moai/specs/SPEC-REF-SEO-ABSORB-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Added `moai-ref-seo`, a search-visibility and crawlability reference skill covering canonical-URL discipline, per-page unique title and meta description, robots.txt / sitemap.xml as host-derived artifacts, JSON-LD structured data with entity consistency, a single response-header chokepoint, and the four SEO-causal document-semantics items (single H1 with unskipped levels, image `alt`, descriptive anchor text, live in-page fragment targets). Shipped as a single 207-line `SKILL.md` in both the local tree (`.claude/skills/moai-ref-seo/SKILL.md`) and the distributed template (`internal/template/templates/.claude/skills/moai-ref-seo/SKILL.md`), byte-identical. Keyboard operability, visible focus, and form-control labeling are delegated to the accessibility surface, and generative-engine optimization is deliberately excluded — both stated in the `NOT for:` clause rather than in a trailing body line, so the invariant triad (`Common Rationalizations` / `Red Flags` / `Verification`) still terminates the body. The second deliverable is a **reusable clean-room re-authoring protocol**: the source third-party document ships with no license, NOTICE, or COPYING file, so structure and concept inventory were studied while every sentence was newly written, the source was pinned by `sha256` digest, and an 8-gram overlap check plus a longest-common-substring bound were run against the delivered artifact. Because both checks are lexical-contiguity measures that a synonym-substituted copy would pass, the protocol additionally requires a recorded structural-divergence verdict, and the overlap check carries a self-trip demonstration (source compared against itself, observed to FAIL) proving the check is not vacuous. Registration landed on the same 15-item surface set `moai-ref-secops` actually occupies, measured rather than assumed: `internal/template/catalog.yaml` (`tier: optional-pack:frontend`, `make build`-generated hash), the three Go count constants (`expectedSkillCount` 32 in `catalog_tier_audit_test.go`, `expectedTotal` 42 in `catalog_loader_test.go`, `wantTotal` 42 in `embed_catalog_test.go`) incremented in the same change as the markdown, `delegation.yaml` under the `frontend` domain key in both local and template copies, the two workflow bodies `review.md` and `sync/quality-gates-quality.md` with their two template mirrors, `internal/template/skills_manifest_test.go`, and the docs-site skill-guide table in all four locales (en / ko / ja / zh). The skill-routing rule is deliberately **not** part of the set — `moai-ref-secops` does not appear there either. 24/24 acceptance criteria PASS. The run-phase implementation landed in PR #1274 (squash merge `1594cf60e`); this sync commit carries the frontmatter `in-progress → implemented → completed` transition, the `progress.md` §E.4 signal, and this CHANGELOG entry — no additional code changes. Route B (PR-mandatory). 🗿 MoAI

- **[SPEC-UPDATE-DATA-SURVIVAL-001](.moai/specs/SPEC-UPDATE-DATA-SURVIVAL-001/spec.md)** — sync-phase close (3-phase plan→run→sync). `moai update` now takes an on-disk backup before every destructive step, ships a failure contract with an escape from the marker-gate lockout, and replaces two vacuous safety guards with falsifiable ones. Six milestones, 20/20 ACs PASS. **M1** added the failure contract: a stage runner (`internal/cli/update_recovery_manifest.go`) that marks the destructive region on entry and, on a failure inside it, writes a `recovery-manifest.txt` naming the failed stage, the error, the backup directory, and the exact `moai update --restore <dir>` command — deliberately reporting rather than auto-rolling-back (the rollback path would be the least-tested code running in the worst state). The new `--restore` flag (`internal/cli/update_restore.go` + `internal/cli/update/backup/restore_entry.go`) is handled **before** the project-marker gate, because a destroyed `.moai/config/sections/system.yaml` is exactly the damage restore exists to repair; the exemption is scoped to that one branch, a directory lacking `backup_metadata.json` is refused rather than copied in, and a second merge pass makes repeated application byte-idempotent. **M2** added `internal/cli/update_destructive_registry.go` — 11 (file, function) rows covering 18 `os.RemoveAll`/`os.Rename` sites, each carrying either a protection or an exemption — validated by a guard that enumerates sites by statically parsing Go source rather than reading the table, so the comparison cannot degenerate into `count == count`; rows are keyed on (File, Function, Sites), never line numbers. `.moai/memory/` is now copied into a run-scoped backup before the both-exist branch removes it, and a failed copy aborts the removal. **M3** added `internal/cli/update_disk_backup.go`: `.claude/settings.json`, `.moai/status_line.sh`, and `.gitignore` previously existed nowhere but the process heap between removal and merge-back, so a crash, ENOSPC, or Ctrl+C in that window lost them permanently — both execution paths now route their first destructive step through one shared guard that writes on-disk copies first and **aborts before** the destructive call when the write fails. **M4** routed `ensureGlobalSettingsEnv` through the injectable `userHomeDirFn` seam and pinned the HOME deletion radius to a single named target. **M5** replaced a fake-`simulateMoaiUpdate` user-area guard with one that drives the real entry point (registry-function coverage moved off `0.0%`). **M6** made `mergeBackPreserveInventory` report `restored N/M before failure` on every error path (coverage 64.3% → 94.1%). Three milestones (M4, M5, M6) carry a demonstrated falsification — the guard was observed FAILING against a mutation or against pre-change production code — so their non-vacuity is evidence rather than assertion. M1–M4 landed in PR #1269 (squash merge `48f9f6c5b`); M5–M6 land with this sync commit, which additionally carries the `in-progress → implemented → completed` frontmatter transition and the `progress.md` §E.4 signal. Verified on the final tree: `go build ./...` exit 0, `GOOS=windows GOARCH=amd64 go build ./...` exit 0, `go test ./...` exit 0 with zero FAIL lines, `golangci-lint run` → `0 issues.`, `go vet ./...` exit 0. Carried debt is recorded in `progress.md` §E.3 rather than buried: the M5 guard drives one of the four registry functions, AC-UDS-013's real-HOME diff is trivially satisfied on a machine without `~/.claude/hooks`, three `userHomeDir()` call sites outside `ensureGlobalSettingsEnv` remain unrouted, and the M6 stat branch is unverified on Windows. §25 template neutrality N/A (`internal/cli` + `internal/defs` Go only, no template mirror; verified `0` changed files under `internal/template/templates/`). Route B (PR-mandatory). 🗿 MoAI
- **[SPEC-UPDATE-GUARD-EFFICACY-001](.moai/specs/SPEC-UPDATE-GUARD-EFFICACY-001/spec.md)** — sync-phase close (3-phase plan→run→sync). The safety guards `moai update` gained in the previous release now demonstrably cover what they claimed, and the partial-restore stat branch became verifiable on Windows. This closes the four limitations that release recorded against itself. **The preservation guard now drives four destructive surfaces instead of one** (`internal/cli/update_preserve_reach_test.go`): a path injected into the deprecated-path table, backup-directory rotation, rollback, and the rename step were each mutated in turn and the guard was observed failing on every one — so its reach is evidence, not assertion. **The partial-restore branch became testable without contrived filesystem conditions**: `internal/cli/update_preserve_inventory.go` gained a package-level `os.Stat` seam, so the "some files restored, then a failure" path — previously reachable only by provoking a real stat error on disk — is now exercised directly, and the branch builds and vets clean for Windows. **HOME resolution across the update subsystem now runs through one seam**: three call sites in `internal/cli/update_clean_install.go` and `internal/cli/update_template_sync.go` were routed through the existing injection point (`internal/cli/update_home_seam_test.go`), so update tests no longer read the operator's real home directory — which also makes the previous release's HOME-radius check machine-independent rather than trivially satisfied on a machine that happens to lack the directory. No user-facing behaviour changed: no CLI flag, output, config key, or command semantics were touched, and README and the docs site were deliberately left unedited. Twenty acceptance criteria, all PASS, seven of them backed by an observed falsification. Verified on the final tree: `go build ./...` exit 0, `GOOS=windows GOARCH=amd64 go build ./...` exit 0, `go vet ./...` exit 0, `golangci-lint run --timeout=3m` → `0 issues.`, `go test ./...` exit 0 with zero FAIL lines. Carried debt is recorded in `progress.md` §E.4 rather than buried: Windows *runtime* execution is still cited only from cross-compilation (`go build` / `go vet`), the GNU `xargs` behaviour that motivated one fix was never reproduced on this BSD machine (the adopted fix removes `xargs` entirely, so the judgment is safe either way), and the line-anchored comment filter cannot rule out block-comment or string-literal false positives without a Go parser. §25 template neutrality N/A (`internal/cli` Go only, no template mirror; verified `0` changed files under `internal/template/templates/`). Route B (PR-mandatory). 🗿 MoAI

- **[SPEC-GLM-KEY-INPUT-001](.moai/specs/SPEC-GLM-KEY-INPUT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Added a GLM API key input to the `moai web` Settings console (3rd Party LLM section) that writes through a single shared credential writer, so the console becomes a complete configuration surface for the GLM backend. M1 extracted `internal/glmcred` (new stdlib-only leaf package: `Path` / `Save` / `Load` / `EscapeValue` / `UnescapeValue`) and delegated `internal/cli/glm.go` credential helpers to it (existing tests still redirect to `t.TempDir()` via the `HomeDirFn` seam). **Latent defect closed**: explicit `os.Chmod(envPath, 0600)` after `WriteFile` so a pre-existing `~/.moai/.env.glm` at `0644` is tightened to `0600` on the next save from **both** the console and `moai glm` (REQ-GKI-006-002 / D-3). M2–M5 wired the console field as an out-of-schema hand-built control (NOT a `settings.FieldDef` — never enters `AllFields()`, so no generic schema loop can leak it; `type="password"` + `autocomplete="off"` + unconditional empty `value`; validation trims/rejects line-breaks; presence indicator discloses at most the final four characters). M6 added a 22-AC anti-leak regression battery (`internal/web/glmkey_test.go`) with a sandboxed `glmcred.HomeDirFn` + differential window scan. The run-phase code + tests landed in PR #1227 (merge commit `5832f0671`, 22/22 ACs GREEN, linux+windows cross-platform build exit 0); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (internal/cli + internal/web Go only, no template mirror). Route B (PR-mandatory). 🗿 MoAI
- **[SPEC-I18N-GOVERNANCE-001](.moai/specs/SPEC-I18N-GOVERNANCE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Added value-level i18n governance to `internal/web/assets/i18n.js`: an explicit allowlist of intentionally-untranslated keys (closed reason taxonomy `technical-identifier` / `proper-noun` / `acronym` as a Go enum so an invented reason fails to compile), a pure-function endonym detector, structural bidirectional key-coverage invariants, and a named owning surface. No runtime behavior change — all artifacts are consumed by tests only. 23 identity-set members allowlisted with taxonomy reasons; 3 members translated (no taxonomy reason fits); the `lang.opt.*` endonym family is handled structurally (bidirectional invariant). Deliverables: `internal/web/i18n_untranslated_allowlist_test.go` (typed allowlist + governance contract) + `internal/web/i18n_governance_test.go` (parser + detector + bidirectional invariants) + 9 translation fixes in `i18n.js`. The run-phase code + tests landed in PR #1224 (merge commit `9db42d122`, 9/9 governance ACs GREEN, cross-platform build exit 0); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (no template mirror edits). Route B (PR-mandatory). 🗿 MoAI
- **[SPEC-TDD-ANTICHEAT-001](.moai/specs/SPEC-TDD-ANTICHEAT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Promoted the TDD skill's advisory Red Flags / Verification content into enforced invariants so **test-first becomes falsifiable in the completion matrix** — a test-after run can no longer produce an identical clean self-verification matrix. Added a new HARD **Test-First Anti-Cheat** section to `moai-workflow-tdd/SKILL.md` (verbatim RED failure output is mandatory completion evidence; implementation written before its failing test is deleted and re-derived test-first), added `manager-develop-prompt-template.md` §E **E8. RED Failure Output** item (E1-E7 byte-preserved, additive-only), updated the `manager-develop` agent (`Forbidden` += "Writing implementation before its failing test"; STEP 2 RED phase += RED-evidence + delete-pre-test-code invariant), and regenerated `internal/template/catalog.yaml`. The run-phase code + mirror sync landed in PR #1216 (merge commit `5704e3c91`, M1 single commit, 8/8 ACs GREEN); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality verified (3 mirror pairs byte-identical; `TestTemplateNoInternalContentLeak` PASS). Route B (PR-mandatory). 🗿 MoAI
- **[SPEC-TREND-MCP-001](.moai/specs/SPEC-TREND-MCP-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Trend MCP tooling — first bundled `.mcp.json` template + generic `moai mcp add|remove|list` CLI + recipe catalogue.** M1 ships the distributed `.mcp.json` template (mirrored byte-identical to repo root) with exactly **3 active bundled servers** (`context7` library docs · `chrome-devtools` browser automation · `playwright` end-to-end testing) — all three secret-free stdio servers, no `$comment` form, no runnable `moai` entry in the distributed default (the self-hosted `moai mcp-server` from SPEC-MOAI-MCP-SERVER-001 stays opt-in via `moai init`/`moai web`). Template-First neutrality verified by a new `TestMCPNeutralityTemplateShape` guard plus the existing `TestTemplateNoInternalContentLeak` (no SPEC-ID/SHA/macOS-path leak). M2 adds the **generic `moai mcp add|remove|list` CLI** — a secret-free entry manager that reuses the existing atomic-RMW helpers (`mutateClaudeJSONAtomic` at `glm_tools.go:541`) without redefining them, registers `${VAR}` env-reference tokens (positional secret values are rejected with a structured error), produces exactly one `.claude.json.bak-*` per mutation, and asks zero questions (no `AskUserQuestion` in `mcp.go`; the orchestrator-subagent boundary holds). M3 reconciles the `MOAI-MCP-SERVER REQ-MCP-002` doctrine — "no third-party" ⇒ "no secret-bearing third-party" — at `.claude/rules/moai/core/settings-management.md` (+ template mirror), and ships a **12-section recipe catalogue** at `.moai/docs/mcp-recipes.md` (ast-grep default-disabled with recorded skip rationale; Semgrep / GitHub / Postgres / Sentry / Codecov opt-in; "Supply, do not redefine" canon 7 occurrences). Run-phase verified: 16/16 AC green, `go test ./internal/cli/...` clean, `go test -race` clean, `GOOS=windows GOARCH=amd64 go build ./...` green, `golangci-lint run` 0 issues, `make build` green. This sync commit carries only the spec.md frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality re-verified on the sync surface (3 mirror pairs still byte-identical). Route B (PR-mandatory). 🗿 MoAI

### Removed

- **The GLM team-mode settings writers and `enableTeamMode`.** Agent Teams static orchestration is retired (`--team` resolves to `MODE_TEAM_UNAVAILABLE` and falls back to sub-agents), which left the team-mode settings writers with no caller and `enableTeamMode` with no reachable path. Both are deleted along with their test-only surface. Native `moai cg` and `moai worktree --team` are unaffected — they never routed through this code.
- **The `research.yaml` config section and `state.state_dir`, re-removed.** Both had been removed once and reintroduced by a template resync. `research.yaml` had zero read sites, and `state.state_dir` was write-only — every runtime `.moai/state` path is resolved elsewhere. Orphaned files in existing projects are swept on the next `moai update`.

- **`moai init --harness-profile` flag** — removed in #1322 along with its dead writer (`writeHarnessProfileYAML`) and the wizard `harness_profile` question. The flag had been accepted-but-inert since the wizard restructure: it parsed a value, wrote nothing any runtime consumer read, and its `HarnessProfile` field had zero readers. **Deployed-user impact:** the flag shipped in `v3.0.1`, so a script pinned to `moai init <project> --non-interactive --harness-profile default` now fails with `Error: unknown flag: --harness-profile` (cobra's default for an unregistered flag — no deprecation cycle was staged). Drop the flag from any such invocation; there is no replacement, because it never had an effect to replace. Harness depth is configured through `.moai/config/sections/harness.yaml`. **Security note:** the deleted writer interpolated `opts.HarnessProfile` raw into YAML and wholesale-overwrote the deployed `harness.yaml`; removing it permanently forecloses the injection hole that `validateInitFlags` had already closed for `--project-mode`. 🗿 MoAI
- **[SPEC-CONFIG-DEAD-SWEEP-001](.moai/specs/SPEC-CONFIG-DEAD-SWEEP-001/spec.md)** — removed three dead configuration surfaces after verifying each had zero runtime consumers. **`.moai/config/sections/cache.yaml`**: `LoadCacheConfig` / `CacheConfig` had no caller anywhere in the tree, and no `cache_control` injection exists in Go — the file configured a code path that was never built. The closed set of `session_ttl` values survives as the `config.ValidSessionTTLs()` accessor, which the `internal/settings` schema seam still consumes. **`.moai/config/sections/research.yaml`**: `ResearchConfig` had definitions and defaults but zero read sites; the loader chain, wrapper, defaults, and strict-unmarshal case are removed together. **`state.state_dir`**: a write-only field whose value nothing read — every runtime `.moai/state` path is a hardcoded literal elsewhere (`internal/cli/state.go`, `internal/worktree/state_guard.go`); `StateConfig.RetentionDays` is retained. **Deployed-user impact:** orphaned `cache.yaml` / `research.yaml` in existing projects are swept on the next `moai update`, which cleans `.moai/config/` wholesale — no manual action needed. The four docs-site locales that documented `cache.yaml` as a live user-editable surface are corrected in the same change. 🗿 MoAI

### Fixed

- **`moai update` now derives a merge base, so template additions reach existing projects.** Without a merge base the update compared against the wrong reference and concluded that a newly added template file was already accounted for, so files added to the template since a project's last update never landed. Existing projects that missed template additions pick them up on the next `moai update`.
- **The branch-guard scan is scoped to the command being invoked, not to text it carries as data.** Quoted spans are collapsed before matching, so `moai todo add "… git switch …"` is no longer denied for containing a branch-state command inside a string, while `git switch main` and `git checkout -b "feat/x"` still deny. The exemption doctrine is corrected in the same change: neither the `manager-git` identity axis nor the `MOAI_BRANCH_GUARD_EXEMPT` sentinel is reachable from inside a tool-spawned subagent — the hook runs as a separate process before the guarded command executes, so exporting the sentinel inside that command never reaches it. A subagent that must mutate branch state works in a worktree instead.
- **The permission decision now survives the hook handler merge.** A decision produced by one handler could be dropped when results were merged, so a deny computed upstream did not always reach the runtime. The decision is carried through the merge.
- **The kanban bootstrap notice is delivered to the operator, in their language, and only on a genuinely new session.** Three defects in one surface: the notice was emitted as internal text the operator never saw, it rendered in English regardless of `conversation_language`, and it re-fired on session resumption — so a resumed session announced a bootstrap that had already happened.
- **Kanban board lock release errors are joined in both directions.** A failure releasing the lock could mask a failure from the operation it guarded, or the reverse, depending on which path returned first. Both errors are now joined, so neither is silently discarded.
- **Statusline usage fetch is bounded against an unbounded `Retry-After`.** A server-supplied `Retry-After` was honored verbatim, so a large value stalled the usage fetch phase for as long as the server asked. The phase is now bounded independently of what the header requests (issue #1467).
- **Tests no longer write to the real home directory.** Several tests resolved paths against the actual `$HOME` rather than a temp directory, mutating the developer's own configuration as a side effect of running the suite.

- **[SPEC-V3R6-LIFECYCLE-CLOSE-THREEPHASE-001](.moai/specs/SPEC-V3R6-LIFECYCLE-CLOSE-THREEPHASE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **`moai spec close` no longer blocks 3-phase SPECs from closing.** The schema (`spec-frontmatter-schema.md`) retired the `§E.5 Mx-phase` section (folded into `§E.4`) as part of SPEC-V3R6-LIFECYCLE-REDESIGN-001, but `internal/spec/closer.go` was missed by that migration: Precondition 2 (`closer.go:696-699`) still hard-required `state.HasMxSection` (a `§E.5` section) to be present. The result was a structural contradiction — the schema forbids authoring new `§E.5` sections, but the closer required `§E.5` present — so any 3-phase SPEC honoring the schema by omitting `§E.5` was blocked from closing. Real-world impact: 4 SPECs in the mo.ai.kr checkout were stuck at `status: implemented`, blocked from close by this defect (SPEC-DB-BACKLOG-001, SPEC-DB-CLEANUP-001, SPEC-DESIGN-HANDOFF-NEWS-001, SPEC-ID-SEC-MEDIUM-001); `moai spec close SPEC-DB-CLEANUP-001 --dry-run` reported `"missing §E.5 Mx-phase audit-ready signal"`. **Fix**: Precondition 2 is relaxed to an additive OR modeled on the dual-acceptance pattern already shipped in `transitions.go:79-92` (`closeInfixMatch` accepts both the new `3-phase close` and legacy `4-phase close` infixes). The legacy `§E.5`-present path stays accepted (backward-compat preserved for grandfather-era SPECs — AC-LSG-002) AND a new 3-phase path accepts `§E.4` sync marker + non-empty `sync_commit_sha` (AC-LSG-001). The OR is additive, not a replacement. Three stale `4-phase` references in `closer.go` (L109 doc comment, L352 commit-subject template, L685 doc comment) plus the L91 sentinel-error comment are canonicalized to the 3-phase convention (REQ-LSG-004/005, AC-LSG-004). Scope discipline: `era.go` (`§E.5` literal-heading recognition intentionally preserved for legacy era classification — 9 matches unchanged), `audit.go`, `transitions.go`, and `spec-frontmatter-schema.md` are NOT touched (AC-LSG-003 scope guard). TDD: `TestClose_PreconditionMissingMx` (asserted the `§E.5`-absent case FAILS) is converted to `TestClose_ThreePhasePreconditionPass` (3-phase PASS) + `TestClose_LegacyMxSectionStillAccepted` (legacy PASS); RED captured before GREEN (the 3-phase test failed against unmodified `closer.go` with `missing §E.5 Mx-phase audit-ready signal`). 7 acceptance criteria (AC-LSG-001..007: 6 MUST-PASS + 1 DEFERRED; AC-LSG-007 is the post-merge mo.ai.kr `moai spec close SPEC-DB-CLEANUP-001 --dry-run` validation, recorded as command+expected-output only — requires `make build` reinstall + checkout switch). [The canonical grep `grep -oE 'AC-([A-Z0-9]+-)*[0-9]+' acceptance.md | sort -u | wc -l` returns 9, not 7, because AC-LSG-005's verification text names AC-LSG-014 (atomic-rollback) and AC-LSG-018 (no-op) as parent-SPEC (LIFECYCLE-REDESIGN-001) regression references — they are NOT this SPEC's own ACs; this SPEC's AC matrix is AC-LSG-001..007.] Run-phase landed as M1 `1199e3b58` (Precondition 2 relaxation + TDD pivot) + M2 `617a8418c` (4-phase→3-phase prose canonicalization). Orchestrator trust-but-verify (this tree, this run, HEAD `617a8418c`): `go test ./internal/spec/ -run 'TestClose_ThreePhasePreconditionPass|TestClose_LegacyMxSectionStillAccepted' -v -count=1` → 2/2 PASS; `go test -cover ./internal/spec/...` → **89.1%** (≥85%); `golangci-lint run --timeout=2m ./internal/spec/...` → 0 issues; `grep -c '4-phase' internal/spec/closer.go` → 0; scope guards intact. This sync commit carries only the frontmatter `draft → in-progress → implemented → completed` transition on `spec.md` (sole YAML-frontmatter artifact — `acceptance.md` / `plan.md` use the markdown-header convention), the `progress.md` §E.4 audit-ready signal, and this CHANGELOG entry — zero further code changes (sync is markdown-only). **Gap (recorded, not back-dated)**: the `draft → in-progress` frontmatter step was missed on M1 (spec.md stayed `draft` through the run phase); the M1 commit subject `fix(SPEC-V3R6-LIFECYCLE-CLOSE-THREEPHASE-001): M1 ...` DOES match the canonical `draft → in-progress` owner pattern, so the OwnershipTransitionRule subject-prefix check is satisfied — the gap is the missing frontmatter status advance, not a wrong owner. This sync commit carries the full transition atomically. README / docs-site unchanged — `grep -rn '§E.5|E.5 Mx|moai spec close' README*.md docs/` → no matches, so no user-facing doc was actively wrong (the `moai spec close` surface is an internal lifecycle tool; the fix restores intended behavior, not a documented user contract). §25 template neutrality N/A (`internal/spec/` Go sources + SPEC artifacts only — `git diff origin/main..HEAD -- internal/template/templates/` shows no template-mirror surface touched by this SPEC, no `make build` required). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3 — a commit cannot know its own SHA until after it lands; Route B squash merge means the final SHA is the merged-to-main SHA, known only after PR merge) and backfilled in a follow-up commit after the sync PR merges. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-GATE-ASTGREP-REPAIR-001](.moai/specs/SPEC-GATE-ASTGREP-REPAIR-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Three combined defects behind `moai gate`'s 22,293-finding false-positive firehose (Tier L, P1, 12/13 ACs PASS + AC-GAR-007 SHOULD deferred, 0 uncovered REQ). **D1 — ast-grep rule `go-error-not-wrapped` over-matched `return $ERR`**: the unconstrained metavariable bound any single AST node, so `return nil`, `return true`, `return 0`, and `return f()` were all flagged as unwrapped errors (16,124 of 21,990 findings = 73% of total noise), and the autofix would have rewritten them into `fmt.Errorf("TODO: operation: %w", nil)` — catastrophic if applied. Refined with a name constraint (`constraints.ERR.regex: ^err(e?|s)$`) in `.moai/config/astgrep-rules/go/error-handling.yml`; non-error returns are no longer matched, and the autofix fires under the SAME guard so it is automatically gated. False-positive count on the dogfood tree: 16,124 → 357 (97.8% reduction). The `fix:` field is preserved (AP-GAR-002). **Dogfood-tree only** (B1 decision): the distributed template baseline `internal/template/templates/.moai/config/astgrep-rules/` carries no error-wrapping rule and is NOT touched — the refinement propagates to dogfood users only. **D2 — whole-repo scan including worktrees / vendor / testdata / `*_test.go`**: ast-grep 0.40.5 does NOT support a `globs:` exclusion field in `sgconfig.yml` config-mode (empirically verified — adding any `globs` entry silently breaks the scan, returning 0 matches for every rule). The exclusion lives at the gate layer instead — new `filterExcludedPaths` in `internal/hook/quality/astgrep_gate.go` drops findings whose paths fall under `.claude/worktrees/`, `/vendor/`, `/testdata/`, or end in `_test.go`. The filter is authoritative regardless of `.gitignore` state (`.claude/worktrees/` was already gitignored, so the scan from the main checkout reported 0 there; the filter makes the exclusion explicit and covers `vendor/` + testdata + test files, which `.gitignore` does not cover). Applied at the gate entry point so both the PreToolUse path and the standalone `moai gate` CLI share one filter. **D3 — `moai gate` ignored `gate.yaml` and hardcoded blocking defaults**: `internal/cli/gate.go` `runGate` constructed `quality.DefaultGateConfig()` directly and never consulted `.moai/config/sections/gate.yaml`, so a project setting `ast_grep_gate: { warn_only_mode: true, block_on_error: false }` (advisory intent) still got blocking behavior — a config/behavior inconsistency where the PreToolUse path read the config but the standalone CLI did not. Fixed by routing `runGate` through a new `loadGateCfgForCLI(projectDir)` helper that calls the shared `config.NewLoader().Load()` chain (the same loader that includes `loadGateSection` from SPEC-CONFIG-AUDIT-REPAIR-001 M2), mapping `config.GateConfig → quality.GateConfig` verbatim after the `pre_tool.go loadGateConfig` pattern (including the `disabled_steps` verbatim copy for issue #1265 and the `RulesDir` default). Fall-back (AP-GAR-004): on missing/unparseable `gate.yaml`, the helper returns `quality.DefaultGateConfig()` with `ProjectDir` + `GoBuildTags` applied — the gate still runs and does NOT silently hard-block. **Smoke verified**: `./bin/moai gate` against an isolated temp project with a genuine `return err` violation + `gate.yaml warn_only_mode: true` exits 0 (advisory honored end-to-end). The run-phase code + tests landed across 4 commits (`df21e689e` M0+M1, `6122b0e31` M2, `e7f020763` M3, `65d06e5e8` M4 progress); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry + `@MX:NOTE` annotations on the two new code surfaces — no additional code changes. §25 template neutrality N/A (dogfood-tree config + `internal/` Go sources only; distributed baseline untouched per B1 — `git diff origin/main..HEAD -- internal/template/templates/` shows no new changes from this SPEC). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). `sync_commit_sha` populated as `pending-backfill-*` placeholder in this commit (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3) and backfilled in a follow-up commit after the sync PR merges. 🗿 MoAI

- **[SPEC-UPDATE-TEMPLATE-BASE-SNAPSHOT-001](.moai/specs/SPEC-UPDATE-TEMPLATE-BASE-SNAPSHOT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). `moai update`'s 3-way YAML config merge now derives its BASE from a rendered install-time template snapshot (`.moai/cache/template-snapshot/`), instead of the raw NEW embedded template — closing the **D5 provenance defect** deferred by `SPEC-UPDATE-YAML-PRESERVE-001` (#1243, Decision D5). Tier L, P1 high, plan-audit iter-2 PASS 0.90, 23/23 ACs PASS, sync-auditor PASS (harmonic mean 0.952), AC-TBS-010 non-vacuity independently reproduced. **Root cause**: the prior `SaveTemplateDefaults` / `BackupMoaiConfig` base-derivation treated the raw embedded template (with `{{.Version}}` placeholders) as the BASE for the 3-way merge, so a template-blessed value change between installs (a version bump like `3.0.1 → 3.0.2`, or a default change) was computed as user-vs-new `diff` rather than base-vs-new `diff` — the merge then "preserved" the stale install-time value as if it were a user edit, silently blocking blessed updates from reaching the user's config. The prior SPEC hardened the merge *representation* (comments / order / quoting via the `*yaml.Node` tree) and explicitly deferred this provenance defect because (a) it is a different defect class (provenance, not representation), (b) it requires a new persisted artifact, and (c) it carries its own backward-compatibility surface. **Mechanism (`internal/cli/update/backup`)**: `snapshot.go` (new) renders the template section files to their install-time byte form via the existing `template.Renderer` (resolving `{{.Version}}` etc. to concrete values) and persists them under `.moai/cache/template-snapshot/` — a rendered snapshot, NOT a raw template copy. `base_loader.go` (new) resolves the BASE for `BackupMoaiConfig`: prefer the rendered snapshot when present (REQ-TBS-006), fall back to the prior raw-template behavior when absent (REQ-TBS-007). The snapshot is written at the end of `moai init` (REQ-TBS-001) and at the end of every `moai update` restore phase (REQ-TBS-002), wired via the new `update_snapshot_hook.go` thin hook called from the three restore-completion sites (`update_template_sync.go`, `update_clean_install.go`, `update_restore.go`) plus `init.go`. The snapshot lives under `.moai/cache/` (already gitignored at `.gitignore:273`) and survives the update clean step (`update_cleanup.go` deletes `.moai/config/` but not `.moai/cache/`). **Backward-compatible**: installs without a snapshot (pre-existing installs on first run after upgrade) fall back to the prior raw-template BASE on the first update (REQ-TBS-007), then create one; no migration step, no config rewrite, no user action required. **No representation regression**: the `*yaml.Node` merge from `SPEC-UPDATE-YAML-PRESERVE-001` is untouched — this SPEC swaps only the BASE source, not the merge engine. The run-phase code + tests landed in commit `0852eacf9` (`fix(SPEC-UPDATE-TEMPLATE-BASE-SNAPSHOT-001): use rendered template snapshot as 3-way merge BASE`, 23/23 ACs PASS, run_commit_sha backfilled in `b66f28b93`); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 population + this CHANGELOG entry — no additional code changes. `sync_commit_sha` populated as `pending-backfill-after-merge` placeholder in this commit (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3; Route B squash merge means the final SHA is the merged-to-main SHA, known only after PR merge) and backfilled in a follow-up commit after the sync PR merges. README / docs-site unchanged — this is an internal merge-mechanism correctness fix, not a user-facing feature (no doc was actively wrong; docs-site documents no `moai update` merge/preservation behavior). §25 template neutrality N/A (`internal/cli/update/backup` Go sources + SPEC artifacts only — `git show --name-only 0852eacf9 | grep -c '^internal/template/templates/'` → 0, no template mirror touched). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-WORKTREE-SQUASH-MERGE-001](.moai/specs/SPEC-WORKTREE-SQUASH-MERGE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). `moai worktree clean --stale` / `--merged-only` now reclaim worktrees whose branches were squash-merged into the base branch. The merge predicate previously ran only `git branch --merged <base>` — a reachability test that lists a branch solely when its tip is an ancestor of `base`. A squash merge collapses N branch commits into one new commit on `base`, so the original commits never become ancestors; every squash-merged branch read as unmerged forever and its worktree was silently kept (no error was raised, so the failure was invisible). Because this repository auto-merges every PR with squash, the blind spot covered essentially every normally-merged branch. **Fix**: `internal/core/git/worktree.go` `IsBranchMerged` is now a composed predicate — reachability (preserved verbatim) OR empty-diff OR (rebase-merge history signal AND state reproduction) OR (squash-merge history signal AND state reproduction). The state-reproduction conjunct enumerates every path the branch touched since its merge-base and asks whether base HEAD holds identical content, guarded against the rename-fold, output-encoding, pathspec-interpretation, textconv, and submodule-ignore axes discovered during plan-phase. **No interface change**: the `WorktreeProvider.IsBranchMerged` signature is unchanged, both call sites in `internal/cli/worktree/clean.go` are untouched, and the `clean` command's help text is unchanged (the fix makes behaviour converge to the existing "merged into the base branch" wording rather than diverge from it).

- **Docs follow-up to `#1280`** — worktree redesign retired-flag sweep (4-locale). The `/moai plan --worktree` flag was retired by the worktree redesign (`#1278`); `#1280` swept three user-visible surfaces that still advertised it (the `quickstart.md` and `moai-plan.md` pages in all four locales, and the `moai init` / `moai update` `--help` text) but missed a fourth: the **subcommand table on the core-concepts overview page**. `docs-site/content/{ko,en,ja,zh}/core-concepts/what-is-moai-adk.md` listed `--worktree` as a live `plan` flag, so a reader copying the table into `/moai plan "..." --worktree` would hit an unknown-flag error. Removed `--worktree` from the flag column in all four locales, leaving `--branch`, `--resume SPEC-XXX` — matching the `moai-plan.md` reference table exactly, where worktree isolation moved to the enter-first pattern (`moai cc -w <name>` then `/moai plan`) that is now the only entry path. Recorded here so the retired flag is not re-introduced by a future 4-locale sync. Documentation-only; no code, config, or behavior change. 🗿 MoAI

- **[SPEC-OBS-ENABLED-GATE-001](.moai/specs/SPEC-OBS-ENABLED-GATE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Closed the observability `enabled`-key contract gap exposed by SPEC-HOOK-TRACE-FLUSH-001 (Tier S, P2/Medium, plan-audit iter-2 PASS 0.81, 7/7 ACs GREEN). `internal/cli/deps.go:259` `enableObservabilityIfConfigured(reg, cwd)` gated `TraceWriter` creation on **file existence only** (`os.Stat`), never reading the `enabled` key documented in `.moai/config/sections/observability.yaml` — so any user setting `enabled: false` (the documented opt-out) still had a `TraceWriter` created and trace files written. The contract violation was silent while the `TraceWriter` buffered in memory; SPEC-HOOK-TRACE-FLUSH-001 made the writer actually flush, which is what made the violation observable. **Fix**: the gate now runs a two-step check — `os.Stat` (preserved, REQ-OEG-005 skip-on-missing intact and the heavier parse skipped when the file is absent) THEN `hook.IsObservabilityEnabled()` (REQ-OEG-007 SSOT reuse, consulted only when the file exists). The SSOT returns false on absent key, false on `enabled: false`, and false on parse error (safe-defaults), so file-present + `enabled: false` and file-present + key-absent both short-circuit before `EnableObservability` is called and before the log directory is created (REQ-OEG-002 / REQ-OEG-004). **Cohabitation invariant preserved**: the 3 protected yaml-key read paths (`observabilityOptIn`, `hookOptInEnabled`, `IsObservabilityEnabled`) documented in `internal/hook/observability.go` / `observability_master.go` are untouched — deps.go becomes a consumer of the existing reader, NOT a new independent reader; `AC-HOI-007` 4-quadrant cohabitation test remains green. **Tests**: `internal/cli/deps_coverage_test.go` adds `TestEnableObservabilityIfConfigured_DisabledByEnabledKey` (AC-OEG-001) and `TestEnableObservabilityIfConfigured_AbsentKeyDefaultsDisabled` (AC-OEG-003), both using the `hook.SetObservabilityMasterForTesting(false)` + non-parallel + `t.Cleanup(hook.ResetObservabilityMasterForTesting)` seam pattern (per `internal/hook/notification_test.go:87` precedent — the only honest way to control the `IsObservabilityEnabled()` decision from a tempdir test, because the SSOT resolves cwd via `CLAUDE_PROJECT_DIR` → `os.Getwd()` and cannot read a tempdir's key). **AC-OEG-007 sabotage round-trip**: the RED leg is reliable precisely because the master-toggle seam bypasses the cwd/env resolution that would otherwise mask the sabotage — under the sabotaged `os.Stat`-only gate, `SetObservabilityMasterForTesting(false)` is ignored and `EnableObservability` is called anyway, failing the stub call-count assertion; the restored implementation passes the scoped run (`ok  github.com/modu-ai/moai-adk/internal/cli  0.850s`). **Static verification (AC-OEG-006)**: `grep -c 'IsObservabilityEnabled' internal/cli/deps.go` → `4`; companion grep for `hook.observability_events` / `hook.opt_in.enabled` in the same file excluding comments → `0` matches (REQ-OEG-006 — no system.yaml key reads introduced). **Coverage**: `enableObservabilityIfConfigured` 100% function-level (all four branches exercised); `./internal/cli/` package 76.4%. The run-phase code + tests landed in commit `468eb584a` (single Tier S M1 milestone); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (`internal/cli` Go sources + SPEC artifacts only — 0 template files touched). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-HOOK-TRACE-FLUSH-001](.moai/specs/SPEC-HOOK-TRACE-FLUSH-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Stopped hook trace entries from being lost at process exit, so trace counts become usable as evidence of hook invocation (Tier M, priority High, 16/16 ACs PASS, 0 uncovered REQ). **Root cause**: `internal/hook/trace.TraceWriter` is asynchronous — `Write` enqueues to a 100-slot buffered channel and returns while a background goroutine writes to disk — and `Close()` was its only flush barrier, with **zero production callers** (the only `.Close()` matches in `internal/`/`cmd/`/`pkg/` were a gzip writer in `internal/harness/retention.go`). Because `moai hook <event>` is a one-shot process that reads stdin, dispatches, writes stdout, and exits, the background goroutine lost the race against process teardown. **Measured, not inferred**: `moai hook session-start` invoked 10 times against a scratch project produced 8 of 30 expected entries, and the last handler of each dispatch contributed **0 of 20** — the loss is structurally biased toward the final handler, i.e. the entry carrying the blocking decision, because earlier handlers' entries get the following handlers' execution time to flush while the last one always races the exit. A positive control (`moai hook pre-tool` × 121) produced a **created but 0-byte** trace file, proving the pipeline reached `os.OpenFile` and excluding "observability was simply off" as the explanation. **Fix**: `TraceWriter.CloseWithTimeout(d)` is a bounded flush barrier returning `nil` on a completed drain and an error matching the new sentinel `ErrFlushTimeout` when the budget elapses first; the budget lives in `internal/config/defaults.go` as `DefaultTraceFlushTimeout = 200 * time.Millisecond` rather than as a call-site literal. `registry.Shutdown()` takes the writer reference under the mutex and **releases it before waiting** (the drain path and `writeTrace` share that mutex, so holding it across the wait would risk a deadlock), and both production dispatch sites — `runHookEvent` and `runAgentHook` in `internal/cli/hook.go` — now `defer` it, reached through an anonymous `interface{ Shutdown() }` assertion so `hook.Registry` stays exactly 3 methods wide. `Close()` keeps its unbounded semantics and its existing contract test. **The budget is a ceiling, not a cost**: `CloseWithTimeout` returns the instant the drain completes. Measured drain on a local SSD over 200 repetitions per bucket is p99 140µs for the 3-entry case a real dispatch produces and p99 3.39ms / max 5.30ms with the channel at its 100-entry capacity, so 200ms leaves roughly 40x headroom over the worst measured case while remaining ~4% of the tightest configured hook timeout. **Residual loss is measured, not silenced**: an unconditional "flush everything" guarantee would contradict the budget, so when the budget is exhausted the abandoned count is emitted as the structured field `undrained_entries` — loss cannot be driven to zero under an async design, but it must not be silent. **Three regression guards, all falsified before being trusted**: `TestRegistryShutdownFlushesLastHandlerEntry` (`internal/hook/shutdown_test.go`) asserts the **last** handler's entry is present in the file — asserting mere file existence would be vacuous, since the positive control above created an empty file; `TestHookCommandFlushesLastHandlerEntry` (`internal/cli/hook_flush_test.go`) builds the binary and crosses the process boundary, because `internal/hook` does not import `internal/cli` and therefore cannot execute the CLI wiring at all, leaving `REQ-HTF-002` with no behavioral judge otherwise; and `TestFlushBarrierHasProductionCaller` guards against the caller count returning to zero. The sabotage round-trip reproduced the production failure exactly — the E2E guard failed with a created-but-empty trace file, and only 1 of 5 runs won the race, which is why it runs under `-count`. **@MX correction**: the `@MX:ANCHOR` above `Close` claimed `fan_in=24` and "all hook handler teardown paths call Close"; measured production fan_in was 0, and that false assurance is part of why the missing barrier went unnoticed. Demoted to `@MX:NOTE` per the protocol's below-3 rule; the goroutine-lifetime `@MX:WARN` on `NewTraceWriter` is preserved because that hazard is unchanged. The `goleak.IgnoreTopFunction` for `TraceWriter.run` in `internal/hook/main_test.go` was **removed rather than re-justified** — its sole cause was a test detaching a writer without closing it, that test now calls `Shutdown`, and the package runs with zero exemptions. Verified: `go test ./internal/hook/ -count=3` ok with no goleak exemptions, `go build ./...` and `go vet ./internal/hook/... ./internal/cli/...` exit 0, `moai spec lint` no findings. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no code changes. No user-facing surface changed: this is internal runtime behavior, so no README or docs-site update applies. §25 template neutrality N/A (`git status --porcelain internal/template/` empty across the run phase — no template mirror touched). Known scope exclusions recorded as follow-ups, not fixed here: `enableObservabilityIfConfigured` in `internal/cli/deps.go` gates on the **existence** of `.moai/config/sections/observability.yaml` and never reads its `enabled` key, so `enabled: false` does not disable tracing; the 4 unverified hook families (`security-*`, sync-gate, the 5 workflow hooks, ci-watch) are only now worth re-investigating; the flush-budget measurement covers local SSD on macOS only, with no network-filesystem, Windows, or loaded-disk figure — `undrained_entries` is the production surface for that residual; and `TestFlushBarrierHasProductionCaller` asserts a floor of 3 distinct handlers, not an exact count derived from production wiring. Also observed but **not this SPEC's regression**: `internal/cli/launcher.go` now reports one `errcheck` finding (`Error return value of fmt.Fprintf is not checked`), introduced by a parallel session's `os.Stderr` → `launcherStderr` rename, which took the call out of errcheck's default exclusion — 0 occurrences of `launcherStderr` at the pre-run baseline `c907db541` versus 5 now, and none of this SPEC's 8 commits touched that file. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CI-LOOP-DEVONLY-001](.moai/specs/SPEC-CI-LOOP-DEVONLY-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Stopped distributing a CI watch / auto-fix workflow whose executable never shipped (Tier M, P1/HIGH, plan-audit PASS 0.84 after 3 iterations, 19/19 ACs GREEN, 0 uncovered REQ). **Root cause**: the template tree referenced `scripts/ci-watch/` and `scripts/ci-autofix/` shell scripts that exist only in the development repository, so every downstream user received documentation, a `moai-workflow-ci-loop` skill, and Frozen constitutional clauses mandating a workflow they had no executable for. **Load-bearing refutation** — `moai pr watch` does NOT perform the watch loop: its default mode prints an instruction to run a shell script and exits 0, `os.Exit(2)` does not exist in the CLI, and the polling loop, 30-second interval, 30-minute timeout, and `exit 2` all live in `scripts/ci-watch/run.sh`; the `--help` `Long` text described a contract `RunE` never implemented. An earlier revision of this SPEC concluded the opposite by treating help prose as evidence of implementation and was corrected by executing the command — **help text is not evidence of implementation**. That refutation is why the two protocol files are treated differently. **Measured outcomes**: template-tree script-path references 27 → 0; files carrying the skill identifier (template + `catalog.yaml`) 9 → 0; `moai pr watch` mentions in the distributed rule tree 5 → 0; constitution findings 77 / 18 ci-attributed → 59 / 0 with `validate-exit=1`; `canary_gate: true` 73 → 65 in each of the two zone registries; `.github/required-checks.yml` prerequisites in the template 4 → 0; dev-repo mirror references 9 → 0; user-visible binary strings instructing an absent script 3 → 0; and the 9 dev-only scripts + the skill directory + both mirror protocol files preserved unchanged. **Intentional template/mirror asymmetry**: `ci-autofix-protocol.md` was rewritten in the template to drop its script dependency but preserved verbatim in the dev-repo mirror, while `ci-watch-protocol.md` stopped shipping entirely — one principle covers both, *a rule belongs only where the thing it governs exists*. **Two new guards, both mutation-verified**: `TestEmbeddedTemplatesExcludeCILoopSkill` (`internal/template/embed_ci_loop_guard_test.go`) walks the compiled `//go:embed` FS — the deployment artifact, not the source tree — and correctly reported 3 files when retargeted at a still-shipped skill; `TestArchiveSkill_PreservesUserContent` (`internal/cli/update_archive_test.go`) byte-compares archived content and reported "archive 0 files, source 2" when the copy was disabled, with a copy→rename regression caught by its source-still-present assertion. **Deferred-decision outcome**: the `.github/required-checks.yml` prerequisite was deferred rather than fixed, on the reasoning that every reference to it sat inside this SPEC's removal scope and would vanish on completion — measured 4 → 0, so the deferral was correct and no follow-up SPEC is needed. Two judgments were promoted from deferred debt at run-phase entry by user decision (AC-CLD-018 `moai pr watch` behavior / flag / exit-code invariance under the string-only edit, AC-CLD-019 archive-not-delete), taking uncovered REQ from 1 to 0. docs-site updated across all 4 locales (ko/en/ja/zh): `guides/ci-autonomy.md`, `advanced/skill-guide.md`, `workflow-commands/moai-sync.md`. **Known gap**: `internal/cli` package coverage is 75.9%, below the 85% target — a pre-existing baseline not caused by this SPEC (which only added tests), but not met. §25 template neutrality verified (`TestTemplateNoInternalContentLeak` + `TestSplitHarnessNamespaceNoLeak` PASS; 0 SPEC-ID occurrences in the template tree). Route B (PR-mandatory). 🗿 MoAI

- **[SPEC-UPDATE-LEGACY-SKILL-LIST-001](.moai/specs/SPEC-UPDATE-LEGACY-SKILL-LIST-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Stopped `[ARCHIVE_DRIFT]` from re-firing on every `moai update`, on every project, with no path to convergence (Tier M, P1, plan-audit PASS 0.89 after 3 iterations, 0 MUST-FIX). **Root cause**: `legacySkillIDs` in `internal/cli/update_archive.go` listed 16 skills as "removed in BC-V3R3-007", but three of them — `moai-domain-backend`, `moai-domain-frontend`, `moai-domain-database` — are live, currently-shipped template skills. Every `moai update` wipes `.claude/skills/moai*` (`update/deploy/deploy.go:51-55`, glob target) and redeploys from the embedded template, then runs the archive step (`update.go:449-457`, post-sync); `.moai/archive/` is not a clean target, so the archive survives. Because those three sources are recreated on every run, `archiveSkill` never reached its source-absent short-circuit and `checkArchiveDrift` compared a fresh redeploy against a frozen snapshot — a warning that could never converge. Reproduced on two projects. Provenance: `74bae50f4` deleted all 16 from both trees and `ec0e9e257` authored the list the same day; `697a6e2c7` (PR #709, "Wave 1 Bundle — Skill Consolidation Stage 1") revived three as consolidation targets the next day, and `git log -S"moai-domain-backend" -- internal/cli/update_archive.go` returns exactly one commit — the list was never edited after the day it was written. The template package DID notice (`internal/template/skills_removal_test.go` was narrowed 16 → 9 with an in-source note naming these three as still present in the template tree); the parallel correction to the CLI package never happened, and `SPEC-V3R6-UPDATE-ARCHIVE-CONTRACT-001` met the identical symptom, diagnosed it as unwired `--force`, and declared the list explicitly out of scope. **M1**: `legacySkillIDs` 16 → 13, with a doc comment explaining why the three are absent and pointing at the guard. **M2**: new `TestLegacySkillIDsNotEmbedded` (`internal/cli/update_archive_guard_test.go`) asserts the list stays disjoint from `template.EmbeddedMoaiSkillNames()` — the first check comparing the list against the real embedded manifest rather than against itself (all six pre-existing tests touching `legacySkillIDs` seed synthetic fixtures FROM the list and assert against it, so they pass whatever it contains). Falsified before being trusted (REQ-LSL-008): under a `go test -overlay` re-adding one live ID the guard produced exactly 1 anchored `--- FAIL` naming the injected ID, with the real source verifiably untouched; a pure `classifyManifest` function factors the comparison so the degradation branches stay reachable, and manifest read-error / empty-set both `t.Skip` rather than pass, because a silent pass on an unavailable manifest is a vacuous green. **M3**: untracked the three wrongly-committed `v2.16` archive `SKILL.md` files — snapshots of the post-revival consolidated skills, NOT the deleted legacy bodies (the real legacy content had `references/` and `modules/`, recoverable only from `74bae50f4^`); the four genuine entries (`moai-framework-electron`, `moai-platform-auth`, `moai-platform-chrome-extension`, `moai-platform-deployment`) are preserved byte-unchanged, and no `.gitignore` rule was added (deliberately out of scope). **M4**: `archiveLegacySkills` now accumulates per-entry failures and returns `errors.Join(...)` instead of returning at the first error — all three former in-loop `return archived, fmt.Errorf(...)` sites append and `continue`, where the `continue` is load-bearing rather than incidental, because the two drift-backup sites sit ABOVE `archiveSkill` in the loop body and a bare append would fall through into `os.Rename` for an entry whose backup parent could not be created; the `total:` summary is now unconditional, `archived` still counts successes only, and the `archive: ` / `total: %d skills archived` output literals are unchanged. **Also fixed**: `TestDryRunArchive` (`internal/cli/update_dry_run_test.go`), a 7th consumer the plan-phase inventory missed because it hard-coded the three IDs as string literals instead of referencing `legacySkillIDs` — invisible to an identifier grep, and therefore also missed by `AC-LSL-006`'s `-run` selector; it broke on the first implementation commit and was fixed by deriving the fixture from `legacySkillIDs[:3]`, removing the duplication that hid it. Six further literal uses in `migrate_restore_skill_test.go` / `update_archive_test.go` are NOT defects (`archiveSkill` / `restoreSkill` take the ID as a parameter and never consult the list) and were left untouched per scope discipline. **AC corrected in-flight against measurement**: `AC-LSL-006(a-i)` expected 19 parent PASS lines; the measured value is 20, because M4 adds `TestArchiveLegacySkills_ContinuesAfterFailure`, whose name matches the AC's own `TestArchiveLegacySkills_` selector prefix — verified as an addition, not a substitution, by enumerating all 19 pre-existing parent tests by name in the run. The run-phase code + tests landed in PR #1260 (merge commit `005d800af`, 16/16 ACs PASS, CI 26 checks = 20 SUCCESS / 6 SKIPPED / 0 FAILURE, `go test ./internal/... ./pkg/... ./cmd/...` exit 0 with 104 ok and 0 build-failed, `golangci-lint run ./internal/cli/...` 0 findings, `gofmt -l` clean, `go build` exit 0 on darwin + `GOOS=windows GOARCH=amd64` + `GOOS=linux GOARCH=amd64`); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (`internal/cli` Go sources, SPEC artifacts, and archive deletions only — `git show --name-only 005d800af | grep -c '^internal/template/templates/'` → 0, no template mirror touched). Known follow-ups, out of scope here: (a) `moai migrate restore-skill --force` would overwrite a live skill with the stale archive (`migrate_restore_skill.go:68-83` removes the target then copies the archive over it); (b) wrong archives already created in downstream user projects are not cleaned up; (c) `moai-meta-harness` retirement — its `SKILL.md` self-declares DEPRECATED while rules and tests still reference it (unrelated, surfaced during the same investigation). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CLIFIX-LINTER-STALE-001](.moai/specs/SPEC-CLIFIX-LINTER-STALE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Un-broke four stale agentlint / taskledger checks so the quality gates audit the real system again (Tier M, P3, plan-audit PASS 0.85). **M1 / REQ-LINT-001-001 (LR-04 inert)**: replaced the hand-rolled line-by-line frontmatter parser (whose `skills` / `hooks` setField cases were TODO stubs populating nothing, permanently disabling dead-hook detection) with `yaml.v3.Unmarshal`; added an `agentSandbox` type tolerating both scalar (`sandbox: none`) and mapping forms so the stricter parser does not abort on either. **M2 / REQ-LINT-001-002 (writeHeavyAgents roster)**: dropped the 4 archived names (`expert-backend`, `expert-frontend`, `expert-refactoring`, `researcher`) from the slice — `manager-develop` is the canonical retained write-heavy agent; drift guard `TestWriteHeavyAgents_NoArchivedNames` fails if an archived name returns. **M3 / REQ-LINT-001-003 (LR-07 dedupe)**: live↔mirror path pairing so a live agent + its template mirror both carrying the Skeptical-Evaluator Mandate block no longer double-report (genuine same-name duplicates across two unrelated live paths still fire). **M4a / REQ-LINT-001-005 (help.go)**: removed the phantom `{"moai brain", ...}` entry and gated help-row inclusion on actually-registered cobra commands (`registeredRootSubcommands`), data-driven from the command tree. **M4b / REQ-LINT-001-006 (ClaimTask)**: validate the task EXISTS and is PENDING before producing a successful claim (former code pre-assigned `targetTaskID = taskID` before the pending search, so nonexistent/completed IDs wrote phantom CLAIMED rows). **REQ-LINT-001-007**: each of the 4 previously-dead checks now has a firing + non-firing regression case. The run-phase code + tests landed in PR #1232 (merge commit `0fe62d544`, 6/6 ACs PASS, agentlint coverage 86.7% / taskledger 92.7%, linux+windows cross-build exit 0, golangci-lint 0 issues); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (internal/cli Go only, no template mirror). Route B (PR-mandatory). 🗿 MoAI

- **[SPEC-UPDATE-REINSTALL-LOOP-002](.moai/specs/SPEC-UPDATE-REINSTALL-LOOP-002/spec.md)** — sync-phase close (3-phase plan→run→sync). Ended the release-user `moai update` clean-reinstall loop reported in issue #1243, where a v3 project re-entered the destructive v2→v3 reinstall on every run and never converged (Tier M, P1 high, plan-audit iter3 PASS 0.88, 20/20 ACs PASS). Five distinct defects were fixed as one behaviour-coherent chain. **Defect 1 — literal version test (M1, `internal/cli/v2_detection.go`)**: `probeVersionSignal` classified the version signal with `strings.HasPrefix(v, "v3.")`, so a released `3.0.1` / `3.0.1-rc13` / `v3.0.0+build.5` string never matched the v3 override and the project read as v2. A new `normalizeVersionMajor` helper now trims whitespace, strips at most one leading `v`/`V`, and parses the leading digit run as the major — `major >= 3` is the v3 negative-override, `major == 2` **with a lowercase `v` prefix** is Signal 1 positive, and everything else is Signal 1 negative with no override. Prerelease and build suffixes classify by major alone. The lowercase-only prefix scope is deliberate: bare `2.x` and uppercase `V2.x` did not match the pre-change literal either, and making either positive would flip a residue-free project from `IsV2=false` to `IsV2=true` — the widening NFR-RIL2-001 forbids without exception. A file-level parse error stays Signal 1 positive (a malformed `system.yaml` is more likely a partial v2 migration), but a well-formed file carrying an unrecognized version string is the separate negative case, so a digit-parse failure never widens the destructive path. **Defect 2 — PRESERVE ∩ deprecated net-zero removal (M2, `internal/cli/update_preserve_inventory.go`)**: `preserveInventoryRoots` (`.moai/specs`, `.moai/project`, `.claude/commands`) intersects `defs.DeprecatedPaths` in 9 entries (8 `.claude/commands/agency/*.md` + `.moai/project/brand`). `runCleanReinstall` snapshotted those paths into the PRESERVE inventory (Step 2), deleted them (Step 4), then restored them (Step 6) — a net-zero removal that left the "deprecated path present" v2 signal armed and re-armed the next `moai update`, while the log still reported a positive removal count because the post-REMOVE re-scan runs before Step 6 undoes it. `buildPreserveInventory` now drops every entry equal to or nested under a `DeprecatedPaths` entry via a new `isUnderDeprecatedPath` predicate; matching is slash-normalized (windows parity) and requires an explicit separator boundary, so `.moai/project/brandX` is NOT excluded by `.moai/project/brand` — the bug a bare `strings.HasPrefix` would introduce. The filter is applied AFTER both inventory sources are merged, so it covers the user-owned scan as well as the preserve-root walk. **Defect 5 — deletion without backup (M2, `internal/cli/update_clean_install.go`, same commit as Defect 2 by necessity)**: Step 4 called `os.RemoveAll` with no backup, contradicting the file header. Removing the Step 6 resurrection without adding a backup would have converted a net-zero no-op into unbacked-up deletion of user-authored `.claude/commands/agency/*.md`, so both halves land together. Step 4 now backs up every path it is about to delete, all-or-nothing, aborting before ANY removal on failure with a grep-able `DEPRECATED_BACKUP_FAILED` sentinel naming the unprotected path; a new `expandDeprecatedBackupTargets` expands directory entries (`.agency`, `.moai/project/brand`) into contained files, because `scanDeprecatedPaths` returns directories while `backupDeprecatedPaths` copies file contents and errors on a directory. The manifest manager is hoisted from Step 5 to Step 4 (the backup classifies against the deployed hash), so a manifest load failure now aborts while the tree is still intact. Removal count remains derived from the filesystem diff, never from the planned-list length. **Defect 3 — residue never cleaned on v3-confirmed projects (M3, new `internal/cli/update_residue_cleanup.go`)**: the v3 override short-circuits Signals 2/3 unconditionally, so a tree carrying full v2 residue but a v3 version string resolves `IsV2=false` and is never cleaned — and M1's normalization made `V3VersionConfirmed` true for the *entire released population*, turning that override from a rare edge into the default path and converting #1243's "loops forever" into "residue is never cleaned". The override's **consequence** is narrowed rather than the override itself: a v3-confirmed project still must not enter the destructive full reinstall, but one carrying residue is routed to `runV3ResidueCleanup` — back up, remove, and let the existing `.agency/` migration pre-step run. It takes no `template.Deployer` at all: no PRESERVE snapshot, no tree wipe, no forced redeploy. The sweep operates on the **pre-migration** residue snapshot, because the migration copies `.agency/` to `.agency.archived/` and leaves both in place while `.agency.archived` is itself a `DeprecatedPaths` entry — a sweep scanning afterwards would delete the archive the migration had just written. The `isMoAIProject` marker gate is checked inside the helper rather than only at the call site, so it holds for every caller, and removal is confirmed by post-`RemoveAll` `Lstat` absence rather than by the planned list. The `update.go` block is EXTENDED, not replaced — `runAgencyMigrationAdapter` keeps firing under its existing conditions and is not gated behind the sweep's outcome. **Defect 4 — `--dry-run` unreachable (M4, `internal/cli/update.go`)**: `runUpdate` returned from its `--dry-run` branch before the v2-detection block, so `runCleanReinstall`'s dry-run renderer and `runV3ResidueCleanup`'s `dryRun` branch were both dead from the CLI — `moai update --dry-run` on a v2 tree showed only the legacy-skill archive summary and no plan at all. A new `emitDryRunReinstallPlan` computes the v2 fingerprint inside the dry-run branch and dispatches to the renderers that already existed (reaching them rather than writing parallel ones); a detection failure degrades to a warning and no plan, matching the non-dry-run block, because a dry run must never fail the command. The early return itself does **not** move — it stays above the deny-rule migration whose `stripRetiredV2DenyEntries` call rewrites `.claude/settings.json`, since relocating it past that block would make a dry run write to disk. **Known behaviour, accepted rather than silently fixed**: for a tree carrying `.agency/`, the sweep converges in more than one run (`.agency.archived` is itself a deprecated entry and the migration copies rather than moves) — bounded and terminating, unlike the #1243 net-zero loop, and recorded in the REQ text with its cause rather than left as a surprise. **In-flight AC correction**: `AC-RIL2-014`'s first command was satisfiable by a *comment alone* — the literal grep matched anywhere in the file, so a fixture whose `permissions.deny` was empty and whose only occurrence sat in a comment passed; a comment-stripping filter was added as the floor and a runtime assertion on the parsed `settings.json` as the ceiling, with the residual trailing-comment gap documented rather than hidden. The run-phase code + tests landed in PR #1261 (squash-merge commit `beeb0ebc2`, 20/20 ACs PASS, `go test ./...` 106 ok / 0 FAIL, `golangci-lint run` `0 issues.` identical to baseline, darwin + `GOOS=windows GOARCH=amd64` builds exit 0, `internal/cli` coverage 75.8%); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (`internal/cli` Go sources + SPEC artifacts only — `git show --name-only beeb0ebc2 | grep -c '^internal/template/templates/'` → 0). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-UPDATE-YAML-PRESERVE-001](.moai/specs/SPEC-UPDATE-YAML-PRESERVE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Preserved YAML comments, key order, scalar style, and indent across `moai update`'s 3-way config merge — the **third independent defect** isolated by issue **#1243** (distinct from `SPEC-UPDATE-REINSTALL-LOOP-002` above, which closed the clean-reinstall-loop defect and also cites #1243; this entry closes the separate comment/order/quoting-preservation defect, not the loop). Tier M, P1 high, plan-audit iter-2 PASS 1.00 (skip-eligible), 23/23 ACs PASS, coverage 89.6% (≥ 88.9% baseline), cross-platform build clean (darwin + `GOOS=windows GOARCH=amd64` exit 0), `golangci-lint run` 0 issues. **Mechanism (`internal/cli/update/backup`)**: the prior `MergeYAML3Way` / `DeepMerge3Way` decoded YAML into `map[string]any`, merged the maps, and re-emitted via `yaml.Marshal` — a round-trip that destroyed everything but values (comments dropped, keys alphabetized, scalar style reflowed `"1h"` → `1h`, indent 2 → 4). The merge was rebuilt on the `*yaml.Node` tree (`node_merge.go`, new): `DeepMerge3Way` now returns `*yaml.Node`, `deepMerge3WayTo` walks the node tree and preserves `HeadComment` / `LineComment` / `FootComment`, key insertion order, scalar `Style` (quoted vs plain), and the encoder pins `enc.SetIndent(2)` (REQ-UYP-004) so emitted files keep the project's 2-space indent. **Old-only-key policy reversal**: a user-added key absent from the new template was previously dropped silently; it is now preserved AND reported on stderr as a retained-key advisory (REQ-UYP-007), so users see what was kept instead of losing customizations without trace. **Indent reflow is intentional**: 22 of 30 section templates were already 2-space so they re-emit byte-identical; the remaining 8 (previously 4-space in the template) are reflowed to the pinned 2-space on next `moai update` — this is the REQ-UYP-004 contract, not a regression. **Decision D5 deferred**: `SaveTemplateDefaults` base-derivation provenance is NOT fixed inline and is the next session's plan-phase target under `SPEC-UPDATE-TEMPLATE-BASE-SNAPSHOT-001` (directory not yet filed — recorded as a known gap in `progress.md` §E.4, not a failure; `acceptance.md` §D.4 forward-looking check marks it NOT-yet-filed). The run-phase code + tests landed in commit `ddd86602b` (`fix(SPEC-UPDATE-YAML-PRESERVE-001): preserve YAML comments/order/quoting via node-tree merge (#1243)`, 23/23 ACs PASS, TDD RED-then-GREEN falsifiability captured, subagent-boundary grep clean, §25 template neutrality verified — `grep -rn 'SPEC-UPDATE-YAML-PRESERVE\|REQ-UYP-' internal/template/templates/` → 0 matches); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 population + this CHANGELOG entry — no additional code changes. `sync_commit_sha` populated as `pending-backfill-*` placeholder in this commit (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3; Route B squash merge means the final SHA is the merged-to-main SHA, known only after PR merge) and backfilled in a follow-up commit after the single run+sync+close PR merges. README / docs-site unchanged — this is an internal merge-mechanism correctness fix, not a user-facing feature (no doc was actively wrong). Route B (PR-mandatory, single-PR strategy: run+sync+close carried by one PR, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

### Changed

- **SPEC lifecycle hygiene — 2 closes + 1 supersession** (documentation-only; no source, template, or behaviour change). Three `.moai/specs/` records are brought into agreement with the state of the tree. **[SPEC-KANBAN-RENAME-001](.moai/specs/SPEC-KANBAN-RENAME-001/spec.md)** `in-progress → completed`: the Factory→Kanban rename landed at `7f61332ef` (PR #1513) and the SPEC record had not followed; verified in-tree by `internal/factory` being absent and `internal/kanban/` present, with §E.3's 28/28 AC PASS carried forward rather than re-executed. **[SPEC-AGENT-MODEL-ENFORCE-001](.moai/specs/SPEC-AGENT-MODEL-ENFORCE-001/spec.md)** `in-progress → completed`: per-agent model enforcement landed at `b0d3b61f8` (PR #1410, issue #1376), verified in-tree at four sites (`internal/hook/agent_model_guard.go` + test, `internal/config/types.go` `AgentModelGuardConfig`, its `defaults.go` default block, and the `h.checkAgentModel(input)` call in `pre_tool.go`). **This SPEC closes PASS-WITH-DEBT, not clean** — §E.4 carries the §E.3 verdict verbatim and names all three open items: `AC-AME-053` FAIL (`internal/hook` coverage 84.0% against a 90% bar — a pre-existing package-wide gap this SPEC moved +0.2pp, not one it created), and `AC-AME-041`/`042` PASS-WITH-DEBT (their commands measure a standalone stub file that orchestrator decision D4(b) never created, having inserted the content into `agent-common-protocol.md` instead; measured against the added section the requirement intent is met, but re-pointing the AC text is a manager-spec amendment, not a docs edit). Closing the SPEC does not close the debt. **[SPEC-CONFIG-TIER-PERSIST-001](.moai/specs/SPEC-CONFIG-TIER-PERSIST-001/spec.md)** `draft → superseded` (v0.3.0): the 2026-08-12 split left zero resident requirements — all 35 `REQ-CTP-*` were reassigned to `SPEC-CONFIG-TIER-RESOLVE-001` (a, draft), `SPEC-CONFIG-ATOMIC-WRITE-001` (b, completed), `SPEC-CONFIG-SECTION-INTEGRITY-001` (c, draft), and `SPEC-CONFIG-MODE-MIGRATE-001` (d, completed), each status verified by reading its own frontmatter. `superseded_by:` names the lead successor only, per the scalar repo convention; the full 1→4 fan-out is enumerated in the HISTORY entry. The directory is **retained, not deleted** — it remains the lineage carrier and the §G/§K provenance the children cite. §L's now-false assertion that "the parent's `status:` remains `draft`" is corrected in place rather than left standing beside the new frontmatter. Verified: `moai spec lint` clean over all three, `moai spec audit` MUST-FIX drift 0, `go build ./...` and both cross-platform builds exit 0. 🗿 MoAI

- **[SPEC-HARNESS-TOKEN-OPT-001](.moai/specs/SPEC-HARNESS-TOKEN-OPT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Internal harness-rule optimization (no user-facing behaviour change). Trims roughly 40 KB from the always-loaded rules tax per session start by adding `paths:` frontmatter restrictions to niche-scope rule files (`verification-batch-pattern.md`, `nav-tokens.md`), splitting `goal-directive.md` into a thin always-loaded stub plus a lazy-loaded companion, relocating `session-handoff.md`'s Diet Constraints / V0 Abort Gate / Anti-Patterns blocks to the lazy `session-handoff-examples.md` sidecar, and consolidating the `CLAUDE.local.md` stub tail. **Inverts the A9 attributable diff-check default to consume-path** (`.claude/rules/moai/core/agent-common-protocol.md` § Parallel Execution → Attributable diff-check doctrinal switch): when snapshot key, command, and §E-observed output all match, the orchestrator now reuses the manager-develop §E evidence instead of re-executing the verification dimension; the fallback-to-re-execution contract is preserved verbatim on any mismatch (any of snapshot-key drift / command drift / missing-§E / output drift restores re-execution), so the `verification-claim-integrity.md` §1.1 invariant holds on every path. **Consolidates Implementation Kickoff Approval restatements**: 1 redundant duplicate was cut; 41 load-bearing local restatements were preserved under the user-confirmed default-to-preserve policy. **All 7 do_not_touch safety invariants preserved verbatim** — VCI §1-3 (no unobserved-claim), the AskUserQuestion channel monopoly, the Implementation Kickoff Approval gate semantics, the A9 fallback contract, the sync-auditor's harmonic-mean weighting, the §E attribution triple, and the 6-block handoff format. Run-phase verified independently by the orchestrator (17/18 AC PASS + 1 PASS-WITH-DEBT on AC-HTO-009, the cross-ref-pointer ambitious-target residue expected under default-to-preserve; 44 IK count preserved at the ≤45 floor; template-mirror parity byte-identical; `make build` exit 0; §25 neutrality 0 SPEC/REQ/AC tokens leaked). The run-phase rule + template edits landed across M0-M6 (6 local rule files + 1 new rule file + 2 `.moai/docs/` companions + `CLAUDE.local.md` + 3 template drift closures); this sync commit carries only the spec.md frontmatter `draft → implemented` transition + `progress.md` §E.4 population + this CHANGELOG entry — no additional rule, template, or body-content edits (plan.md and acceptance.md carry no frontmatter and were not touched). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-PIPELINE-FANOUT-ACTIVATION-001](.moai/specs/SPEC-PIPELINE-FANOUT-ACTIVATION-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Activated the ten plan/run/sync pipeline fan-out sites that were written but never reachable in practice, and resolved a re-audit-scope contradiction inside `plan-auditor` (Tier M, 10 REQ / 15 AC, all 15 PASS). Documentation-only: zero Go source files modified. **The load-bearing finding** is that a fan-out site is unreachable when BOTH (a) it is phrased as a discretionary `MAY`, and (b) it lives in a sub-skill the orchestrator only `Read`s *after* it has already entered the phase serially — by the time the site is visible, the serial path is the one already running. Fixing only (a) leaves seven of the ten still invisible; fixing only (b) leaves them visible but optional. Both halves therefore land together. **M1.1 — Fan-Out Index** (`.claude/skills/moai/workflows/plan.md`, `run.md`, `sync.md` + their template mirrors): each of the three phase routers gains a `## Fan-Out Index` table listing every fan-out in its phase with a stable ID (`FO-PLAN-1..2`, `FO-RUN-1..4`, `FO-SYNC-1..4`), its trigger condition, the sub-skill file that owns it, and what is parallelised — so the orchestrator learns a fan-out exists from the always-read router rather than from a file it has not opened yet. **M1.2 — discretionary → conditional** (the six sub-skill / router sites listed below): all ten sites move from `the orchestrator MAY …` to `the orchestrator shall …` under a bolded GEARS modifier (`**Where**` / `**While**` / `**When**`) on the same line, so the obligation is never condition-free — every promoted site remains gated on its own trigger, and each site's fail-open fallback ("the scan runs as one pass with identical output — no error, no warning") is preserved. Sites: `plan.md` FO-PLAN-1 (research fan-out), `plan/spec-assembly.md` FO-PLAN-2 (Phase 11 review lenses), `run/phase-execution.md` FO-RUN-1 (Phase 1.8 MX context-map shards), `run/task-decomposition.md` FO-RUN-2 / FO-RUN-3 / FO-RUN-4 (RED-stage drafter pool, Phase 13/16/17 quality band, Phase 18 MX scan shards), `sync.md` FO-SYNC-1 (Phase 7 quality check), `sync/quality-gates-quality.md` FO-SYNC-2 / FO-SYNC-3 (Phase 9 MX shards, Phase 10 test drafting), `sync/doc-execution.md` FO-SYNC-4 (Phase 12 five-drafter / one-applier structure). **Scope addition, disclosed rather than absorbed silently**: FO-SYNC-2 in `quality-gates-quality.md` had *no* fallback clause at all before this change — promoting it to `shall` without one would have created the first unconditional obligation in the set, so a fallback was ADDED there. That is one clause beyond the original M1.2 statement, and it is recorded here for the same reason it is recorded in the SPEC: an addition that makes a promotion safe is still an addition. **The read/write boundary is unchanged by every promotion** — each fan-out remains read-only (drafters and shards return text and write nothing), tag and document *application* stays single-writer, blocking P1/P2 determinations are still made on the merged result so no shard can clear a gate alone, and the binding verdicts stay with `plan-auditor` / `sync-auditor` rather than with any fanned-out judge. **M1.3 — re-audit-scope contradiction** (`.claude/agents/moai/plan-auditor.md`): the retry-loop section said iteration 2+ is a "full audit PLUS regression check", while the same file's earlier clause and `run/phase-execution.md` both describe a defect-delta re-audit. Iteration 2+ is now stated as a defect-delta re-audit plus a regression check over the prior iteration's defects, agreeing with both. **Verdict authority is explicitly unchanged** — the new text states that the delta scope reduces re-audit cost only and never permits an orchestrator self-assessment to substitute for an auditor verdict. **M1.4 — per-tier REQ/AC budget** (`.claude/rules/moai/workflow/spec-workflow.md` § SPEC Complexity Tier): the tier table gains requirement and acceptance-criterion ceilings — S 8, M 16, L 25 — applying **independently** to each count rather than to their sum, so a Tier M SPEC may carry up to 16 requirements AND up to 16 acceptance criteria. Exceeding a ceiling is stated as a signal to tier up or split, not to relax the budget. **M1.5 — cascade**: the `plan-auditor` catalog hash was regenerated (`internal/template/catalog.yaml`), and `run.md` hit its hard 200-line entry-router ceiling (`TestEntryRouterLOCCeiling`) at 208 once its four-row Fan-Out Index landed — a constraint the plan's risk table did not anticipate. Resolved by relocating the two Phase 4 operational-entry paragraphs (Mode 4 research fan-out, Mode 6 launch procedure) **verbatim** into `run/phase-execution.md`, the sub-skill that already owns Phase 4; both files were inside the SPEC's declared envelope. **The relocation was not loss-free, and the earlier claim that it was is corrected here**: the two Mode paragraphs did move verbatim, but the index table's one-sentence rationale — that the run-phase fan-out sites live in sub-skills read on demand, which is why they are enumerated in the always-read router — did not survive the trim (`grep -c "on demand"` now returns 1 in `plan.md`, 1 in `sync.md`, 0 in `run.md`). The table itself, which is what REQ-PFA-001 mandates and what actually resolves the reachability problem, is present in all three routers, and no acceptance criterion covers the rationale sentence; the residue is that `run.md` alone states the index without stating why. Restoring it requires first freeing a line, since `run.md` now sits at exactly 200 of its 200-line ceiling — recorded as a follow-up rather than done here, because trimming an unrelated line is outside this SPEC's envelope. **§25 template neutrality**: all ten local edits are mirrored under `internal/template/templates/`, and the four pre-existing intentional local-vs-template divergences were verified unchanged (`1/0` on each) — a blind copy would have flattened them. Two adjacent defects were deliberately left alone and named rather than fixed opportunistically: the stale `exit 2` hook description in the local `sync.md`, and a pre-existing neutrality leak in the mirrored `plan-auditor.md`. The run-phase edits landed in seven commits (`bf46ff163..a95369b63`, 23 files, +217/−42, 15/15 ACs PASS, `go build ./...` and `go vet ./...` exit 0, `go test ./...` zero FAIL, `moai spec lint` clean, `TestEntryRouterLOCCeiling` PASS); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 population + this CHANGELOG entry — no additional doctrine or template edits. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-ENVKEY-ANTHROPIC-SSOT-001](.moai/specs/SPEC-ENVKEY-ANTHROPIC-SSOT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Single-sourced the `ANTHROPIC_*` environment-variable **name** family through `internal/config/envkeys.go`, closing the follow-up named in SPEC-CLIFIX-HYGIENE-001's own CHANGELOG entry (Tier M, P2, plan-audit iteration 2 after an initial FAIL 0.72). No runtime behaviour changes: this is a mechanical name-indirection refactor, and every constant's value is byte-identical to the literal it replaces. **The load-bearing finding (§A.2)** is that the pre-existing gate `TestNoBareGLMEnvVarLiteralsInCLIProduction` (`internal/cli/glm_env_parity_test.go:66`) carried two independent defects making it structurally incapable of covering this family: its `banned` slice held only three `CLAUDE_CODE_*` names, and its walk root was `os.Getwd()`, which under `go test` resolves to the **package directory** `internal/cli` rather than the repository root — so of the 83 bare literals measured at base `76d9a8f3b`, only 46 were reachable and **37 (45%) were structurally invisible**. Naively reusing that pattern would have shipped a guard that silently passes on nearly half the surface, so guard repair was treated as a first-class deliverable rather than a by-product. **M1 — SSOT constants**: `envkeys.go` goes 6 → 9 ANTHROPIC constants, adding `EnvAnthropicAPIKey` and `EnvAnthropicDefaultFableModel` (two names in active production use that had **no constant at all**) plus `EnvAnthropicPrefix` for the bare namespace prefix `ANTHROPIC_`, doc-commented as a prefix rather than a variable name; the guard matches on exact literal equality (`bannedSet[val]`), not substring containment, so banning `"ANTHROPIC_"` does not collide with `"ANTHROPIC_API_KEY"`. **M2 — new repo-root-scoped guard**: `internal/config/anthropic_env_ssot_test.go` / `TestNoBareAnthropicEnvVarLiteralsInProduction`, hosted beside the SSOT it enforces, resolving its walk root via `findProjectRoot(t)` and scanning `internal/` + `pkg/` + `cmd/` while excluding `_test.go` files and `internal/config/envkeys.go` itself (a new hazard created by the widening — a repo-root walk otherwise flags the very constant definitions it exists to enforce). Per OQ-4 the existing CLI-scoped test is left intact rather than widened: its identifier asserts CLI scope, it is owned by an already-closed SPEC, and a separate test gives this family an independent RED/GREEN signal. **M3–M5 — transition of all 83 literals to 0**, in a measured RED-window trajectory (83 → 37 after `internal/cli` → 8 after `internal/hook` → 0): `internal/cli/glm.go` 32, `internal/hook/session_end.go` 12, `internal/hook/glm_tmux.go` 9, `internal/hook/session_start.go` 8, `internal/cli/launcher.go` 7, `internal/cli/settings.go` 6, `internal/tmux/cg_detect.go` 4, `internal/statusline/metrics.go` 3, `internal/sandbox/env.go` 1, `internal/cli/worktree/tmux_integration.go` 1 (the prefix site). Behaviour preservation rests on a **forward attestation** rather than an assertion: applying the same value-preserving mapping to the base source of all 10 files and gofmt-comparing against the working tree reports `IDENTICAL` for all 10, so no wrong-but-compiling constant was substituted. **M6 — falsifiability proof**, because a guard that has never been observed to fail is not evidence: all **9/9** banned names were planted in turn and each produced a non-zero guard exit, including the three with zero natural offenders (`ANTHROPIC_`, `ANTHROPIC_DEFAULT_FABLE_MODEL`, `ANTHROPIC_REASONING_EFFORT`) that no other AC exercises; one plant per scan root (`internal/sandbox/env.go`, `cmd/moai/main.go`, `pkg/version/version.go`) proves each root independently, since `pkg/` and `cmd/` hold zero natural literals and their coverage would otherwise be asserted and never proven; and a sibling plant in `internal/config/log.go` was detected, proving the `envkeys.go` exclusion is an exact-path match rather than a package-wide exemption. Total residual diff after every plant-and-revert loop: 0 lines. **Out of scope, recorded so a later reader does not "fix" it by mistake**: the 291 `ANTHROPIC_*` occurrences in `_test.go` files (a declared hardcoding-allowed zone per `CLAUDE.local.md §14`, verified unchanged at 291) and the 12 references under `internal/template/templates/` (governed by template neutrality; the SPEC's merge commit changed 0 template files). **Known residual gap (follow-up candidate):** the three `CLAUDE_CODE_*` names guarded by the CLI-scoped test remain unguarded outside `internal/cli/` — deliberately excluded here to keep this SPEC's blast radius to one env-var family. The run-phase code + guard + SPEC artifacts landed in PR #1254 (merge commit `a89e875e2`, 15/15 ACs PASS, 26 CI rollup entries: 20 SUCCESS / 6 SKIPPED / 0 FAILURE, linux+windows cross-build exit 0, `go vet` exit 0, `golangci-lint` `0 issues.` identical to the pre-M3 baseline); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. §25 template neutrality N/A (Go sources under `internal/` only; the SPEC merge commit changed 0 template files). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CONTEXT-ENGINE-RIGHTSIZE-001](.moai/specs/SPEC-CONTEXT-ENGINE-RIGHTSIZE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Aligned the `.claude/rules/mo/` instruction corpus with Anthropic's 2026-07-24 "new rules of context engineering" guidance, applying the conservative **"보수(B-group only)"** scope decision: transition only the 2 clearly-stylistic absolute rules to judgement-delegating language; preserve all mechanical guardrails (C-group) and safety invariants (A-group Frozen + 5 anchor doctrines) untouched (Tier M, rules-edit SPEC, plan-auditor PASS 0.86). **M1 — code_comments expressive transition** (`moai-constitution.md` TRUST 5 Readable): the absolute `Clear naming, English comments` → `Clear naming; comments match the surrounding code's language and density (per code_comments setting in language.yaml, default English)`. **M2 — Tool Selection consolidation** (`moai-constitution.md § Tool Selection Priority`): the 5-bullet `"Use X instead of Y"` absolute block collapses to a single-line informational SSOT pointer to `agent-common-protocol.md § Tool Selection by Task` (M1.1 dedup + M2.2 informational reframe combined). **M3 — Template mirror sync**: same M1+M2 edits mirrored to `internal/template/templates/.claude/rules/moai/core/moai-constitution.md` with §25 neutralization verified (0 SPEC IDs / 0 REQ tokens / 0 audit citations leaked). **Preserved (A-group + C-group)**: 66 `[ZONE:Frozen]` markers across 13 files byte-identical to baseline (per-file distribution diff-empty); the 5 anchor doctrines (AskUserQuestion-Only, Subagent Prohibitions, Deferred Tool Preload, Branch Guard, Verification-Claim Integrity, Native `/goal` Prohibition) all observable; C-group mechanical guardrails (Multi-File Decomposition + Reproduction-First Bug Fix) unchanged with `[HARD]` markers intact. The run-phase rule edits + template mirror landed in PR #1238 (merge commit `752dc6794`, 10/10 ACs PASS including AC-CER-005 Frozen-count Critical); this sync commit carries only the frontmatter `in-progress → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional rule or template edits. §25 template neutrality: template mirror was synced in the run-phase commit (M3) and verified clean; no template edits in this sync commit. Route B (PR-mandatory). 🗿 MoAI

- **[SPEC-WORKTREE-BRANCH-GUARD-OPTIN-001](.moai/specs/SPEC-WORKTREE-BRANCH-GUARD-OPTIN-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **Behavior change for existing users (AC-REQ-7):** the Main-Checkout Branch-State Guard introduced by SPEC-WORKTREE-BRANCH-GUARD-001 now ships **default-OFF (opt-in)**. Users who today receive the guard ON (every distributed install) will see it go INERT after this lands — routine git commands (`git switch`, `git stash`, `git reset --hard`, `git merge`, etc.) in a single-developer primary checkout are no longer denied. Maintainers of shared multi-session checkouts (the guard's actual intent) opt back in by setting `workflow.branch_guard.enabled: true` in local-only config (see `CLAUDE.local.md §22.9`); the template default stays `enabled: false` (§25 template neutrality). Independently of the default flip, the `branchStatePatterns` regex set is refined to exclude read-only forms that were false positives — `git stash list`, `git stash show`, and `git merge-base` no longer match even when the guard is enabled, while the genuinely dangerous forms (`git switch`, `git checkout <branch>`/`-b`, `git branch`, `git reset --hard`, `git rebase`, bare/mutating `git stash` push/pop/apply/drop, actual `git merge <branch>`) remain matched. **M1** (`internal/config/types.go` + `internal/config/defaults.go`, `### Changed`): new `BranchGuardConfig{ Enabled bool }` schema gate, default `false`, wired additively BELOW the existing exemption logic. **M2** (`internal/hook/branch_guard.go`, `### Fixed` semantically): `git merge` pattern anchors on trailing whitespace (`\bgit\s+merge\s`) so `git merge-base` no longer matches; `git stash` pattern requires bare end-of-input OR a mutating subcommand (push/pop/apply/drop), excluding `stash list`/`stash show`. **M3** (`internal/hook/pre_tool.go`, `### Changed`): `checkBranchState` call-site is gated by `preToolHandler.branchGuardEnabled()` reading `Workflow.BranchGuard.Enabled` via the existing `ConfigProvider` — when disabled, NO `git rev-parse` subprocess runs (the entire branch-state path is skipped). `checkBranchState` signature is unchanged (no cascade into the parent SPEC deny-origin test). **M4** (`internal/hook/pre_tool_test.go` + `pre_tool_branch_guard_integration_test.go`, `### Tests`): parent SPEC deny-origin and fail-open tests updated to opt in via a `cfgWithBranchGuard(true)` helper; config-default-false is explicitly asserted. **M5** (`.claude/rules/moai/workflow/main-checkout-branch-guard.md` v1.1.0 → v1.2.0 on BOTH source and template sides, `### Changed`): rule mirror documents the opt-in default + the pattern refinement; source retains SPEC/REQ tokens, template side is §25-sanitized (no SPEC-ID/REQ/AC tokens). **M6** (`CLAUDE.local.md §22.9`, `### Docs`): maintainer opt-in intent + local-config procedure documented. **M7** (verification): full `go test ./...` + `GOOS=windows GOARCH=amd64 go build ./...` + `golangci-lint run` + C-HRA-008 subagent-boundary grep + template-neutrality grep all clean. REQ-6 backward compatibility preserved: `MOAI_BRANCH_GUARD_EXEMPT=1` env var and `AgentType == "manager-git"` identity exemption continue to bypass the guard regardless of the config flag; fail-open norm (uncertainty → allow) is unchanged. The run-phase code + doctrine + SPEC artifacts landed in commit `214b6f6fb` (M1-M7 single commit, Tier M behavior-coherent unit); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. **Known follow-up (not in scope):** `.moai/config/local/` is not yet gitignored — the gitignore addition is a separate chore (tracked in run-phase §E.3 blocker note + `CLAUDE.local.md §22.9`); the maintainer opt-in file is NOT created in the committed tree (would violate §25 template neutrality). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-CLIFIX-HYGIENE-001](.moai/specs/SPEC-CLIFIX-HYGIENE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). CLI structure and hygiene remediation across the `internal/cli` update cluster: a per-file line ceiling, a verified dead-code sweep, hardcoding single-sourcing, English UI strings, and three genuine defect fixes (Tier L, P4, characterization-first DDD with reproduction-first TDD where the defect *was* the behavior). **M1 — PRESERVE net + ANALYZE inventories**: `internal/cli/update_hygiene_characterization_test.go` pins `cleanLegacyHooks` (the exact 10-pattern legacy set, per-pattern removal, non-legacy preservation, mixed-group surgical delete, no-hooks fast path) and `runAgencyMigrationAdapter` (`ErrMigrateNoSource → nil` suppression, `dryRun=true` read-only guarantee); four frozen inventories land under `.moai/specs/SPEC-CLIFIX-HYGIENE-001/inventories/` (dead-code caller graph, GLM env sites, Hangul strings, threshold literals). Zero production edits in M1. **M2 — defect corrections (reproduction-first)**: a single shared rune-boundary helper `internal/cli/truncate.go` (`truncateRunes` / `truncateRunesSuffix`) replaces byte-slicing at 5 user-content truncation sites (`constitution.go` clause prefix + file-path suffix, `tool_policy.go` audit column + `truncateArg`, `github.go` issue body), so CJK content is never split mid-rune; the setup wizard masks PAT entry via a new `IsSecretInputID` predicate gating `huh.EchoModePassword` on the `github_token` / `gitlab_token` questions (previously plaintext echo); the worktree package parses `workflow.yaml` through `yaml.v3` for **both** `team_mode` (`worktree/tmux_integration.go`) and `tmux_preferred` (`worktree/new.go`) instead of `strings.Contains` line matching — a commented-out `# team_mode: cg` no longer activates CG mode and a commented `# tmux_preferred:` no longer flips launcher selection — and `buildTmuxInitialCommand` quotes the worktree path (`%q`) so paths with spaces or shell metacharacters survive. **M3 — hardcoding single-source**: the three GLM inject/clear env-var NAMEs move to `internal/config/envkeys.go` constants behind `config.GLMEnvVarSet()`, consumed by every inject and clear site so the two sets are identical by construction (`TestGLMEnvSetParity`), with the AST-based `TestNoBareGLMEnvVarLiteralsInCLIProduction` gate blocking re-entry of bare literals; `config.DefaultTierThresholds` collapses the `[]int{1, 3, 5, 10}` tier vector duplicated at THREE sites (`harness.go` status fallback, `harness.go` learning-config initializer, `hook.go` `defaultTierThresholds`) to one, and `config.DefaultHookDispatcherTimeout` collapses the two `30 * time.Second` dispatcher literals in `hook.go`. **M4 — English UI strings**: 73 Hangul lines across the six audited files (`clean.go`, `migration.go`, `web_port.go`, `web_port_posix.go`, `web_port_windows.go`; `doctor.go` was already clean — recorded as a drift finding, not a defect) rewritten to English per `error_messages: en`, each message reviewed individually so diagnostic accuracy (PIDs, ports, counts, versions, `%w` wrapping) survives — no blind `sed`. **M5 — structure + dead-code deletion**: `internal/cli/update.go` goes **1,905 → 1,005 lines** by mechanical move only (function bodies byte-identical to the pre-split ranges; the diff on `update.go` is pure deletion plus a goimports import trim) into `update_template_sync.go` (590) and `update_wizard.go` (352), bringing every update-cluster production file under the 1,200-line ceiling; `buildGLMEnvVars`, `ttyConfirmer`, and `internal/cli/worktree_validation.go` (plus `launcher_worktree_validation_test.go`) are deleted after per-symbol caller-graph re-verification, for a **net −478 lines** (the plan's −150..−250 estimate is reported as measured rather than reconciled — the gap is the 291 lines of exclusive test files the deleted symbols carried with them). **Two deferred deletions resolved by evidence, not assumption**: `ttyConfirmer`'s `//nolint:unused` tag cited `SPEC-V3R6-CI-BASELINE-DRIFT-001 §D.1`, but that SPEC is `status: implemented`, its `progress.md` records "No follow-up SPEC stubs created", and its `plan.md` deferred the *inspection* rather than reserving the symbol — nothing was waiting on it; `worktree_validation.go` self-attested a follow-up wire-up SPEC that was never created, and its originating `SPEC-V3R3-RETIRED-AGENT-001` / `-DDD-001` are both `completed`. **PRESERVE-KEEP** (verified LIVE, untouched): `acquireUpdateLock` / `cleanStaleLock` (P0-wired), `scanDeprecatedPaths`, `backup.CleanupOldBackups`, `yesConfirmer`. **In-flight AC correction (v0.2.1, recorded honestly)**: two acceptance rows asserted more than the tree satisfies, so the *written verification* was narrowed to the shipped scope rather than widening the implementation late in run-phase — AC-HYG-001-003 previously expected "env-name literals outside `envkeys.go` reduced to 0", but M3 deliberately scoped to the three GLM names and the broad grep still returns 137 lines (57 raw literals), with the `ANTHROPIC_*` family and `CLAUDE_PROJECT_DIR` reads explicitly out of scope; AC-HYG-001-005's `web_port*.go` glob also matched `web_port_test.go` (Korean test comments and `t.Fatalf` strings), returning 1 file against an expected 0 — all four production files are clean, and test-file language is governed by `code_comments`, not `error_messages: en`. The run-phase code + tests landed in PR #1245 (merge commit `1d6b33526`, 8/8 ACs PASS, 19 CI checks SUCCESS / 7 skipped / 0 failures, darwin+linux+windows builds exit 0, `golangci-lint` 0 issues with 0 new findings, `internal/cli` coverage 75.6% unchanged); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. **Known follow-ups (not in scope):** single-sourcing the `ANTHROPIC_*` env-var family, which additionally has no `envkeys.go` constant at all for `ANTHROPIC_DEFAULT_FABLE_MODEL`; the broader i18n sweep across the ~24 remaining Hangul-bearing production files (already declared in `spec.md` §C `Out of Scope — Broader i18n sweep`); and correcting AC-HYG-001-001's `-run 'UpdateCharacterization'` selector, which matches zero tests and would therefore pass on any tree (the substantive `-run 'Characterization'` run — 17 tests, green pre- and post-split — is the real evidence, and the defect is named in `progress.md` §E.2 Gaps rather than silently passed). §25 template neutrality N/A (`internal/cli` + `internal/config` Go sources only, no template mirror). Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

- **[SPEC-KANBAN-BOARD-001](.moai/specs/SPEC-KANBAN-BOARD-001/spec.md)** — sync-phase close (3-phase plan→run→sync). **The six-column kanban board now has a single-origin board state store at `.moai/state/kanban-board/` — column assignments live in exactly one file that only the `lead` role may write, replacing the rejected `column:`-in-SPEC-frontmatter design of the superseded `SPEC-KANBAN-MULTISESSION-001` (59 REQ / 61 AC, plan-audit FAIL at 0.87 for structural reasons; this SPEC carries the board model of the three-way split).** Single-origin means the store is the only writer surface: the board NEVER writes a `column`, `status`, or any other frontmatter field into a SPEC file (REQ-KB-007), so SPEC bodies stay owned by their phase agents and there is no second datum for the frontmatter-vs-board drift that killed the predecessor's design.
  - **Store + resolution extraction (M1).** The board state directory resolves as the parent of the absolute git common dir through a newly extracted `internal/core/git` checkout discriminant (ported per `SPEC-KANBAN-WORKTREE-001` REQ-KW-018's extraction disposition), and `internal/hook`'s branch_guard now consumes the extracted function instead of its private copy. The path deliberately does NOT collide with the session-record dir (`SPEC-KANBAN-RENAME-001` REQ-KR-009 — one directory name, two occupants, two resolution rules, so the board got its own `.moai/state/kanban-board/`). An absent state file IS the empty board (created on demand, REQ-KB-021 — neither "partially written" nor "unreadable" owned the absent case before); every write is atomic through `internal/atomicfile` (same-directory temp + rename), and `LoadBoard` reads through `atomicfile.ReadFile` so a racing reader never misreports `ErrBoardUnknown` from two valid states (the M3-fix4 reader half).
  - **Sole-writer enforcement through the role carrier (REQ-KB-017 / REQ-KB-025).** Only the `lead` role may write board state; a refusing reader reads the caller's DECLARED role through the REQ-KB-025 carrier — adopted ahead of `SPEC-KANBAN-BOOTSTRAP-001` so exactly one declaration contract exists in the system, consumed by name rather than re-derived (the carrier carries the key `REQ-KS-019` dispatch routing and `REQ-KS-012` quorum accounting read, while defining no part of those mechanisms).
  - **Board-wide cross-process advisory lock with stale-lock clearing (REQ-KB-013 / REQ-KB-023).** The lock refuses to clear a lock whose owner is live, and an indeterminate liveness result is treated as LIVE — the fail-safe direction, because guessing "dead" is what unlinks a live holder's lock. The Windows liveness probe uses `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` + `GetExitCodeProcess` with STILL_ACTIVE (259) = live (the old `proc.Signal(0)` shape is EWINDOWS on everything but Kill — constant-false, found by sync-audit); an artifact EMPTY at the first read and still empty after a 100ms grace is clearable as interrupted acquisition, closing the killed-between-create-and-write brick.
  - **Bounded recovery (REQ-KB-022).** Recovery from a partial write is one invocation, one verdict, no retry — the sanctioned recovery can never itself produce the empty board it exists to repair, and can never wedge the board on a retry loop.
  - **Card model, branch-side status read, compatibility table, admission (M2).** The six columns are a closed enumeration (`backlog → plan → run → review → sync → done`). A card's `status` is read from its worktree-observed branch by blob read — never a checkout — with the primary checkout as fallback where no branch exists and `unresolved` when no single tree resolves (REQ-KB-020, closing the every-card-inconsistent gap where the primary's copy still read `draft` while the card sat in `run`). The (column,status) compatibility table governs dispatch refusals, and WIP-2 admission runs beneath the board-wide lock.
  - **Four fix passes: three review rounds + one sync-audit round.** Review rounds: the SpecID traversal sweep moved to the anchor so a rename-in-place stays covered (M3-fix); `LC_ALL=C` pinned the containment boundary (M3-fix2); the empty-id wedge was removed by the minimal-fix operator decision — shape-conditional only, traversal sweep unchanged (M3-fix3). The sync-audit round first FAILed 0.786 on four Windows-substrate defects (constant-false liveness probe; a Windows test layer `go build` could never see was uncompilable; the interrupted-acquisition empty-lock brick; the reader half of the atomicity guarantee) and re-audited PASS 0.882 after M3-fix4 (`431ce5071`). The process change adopted from that arc: **`GOOS=windows go vet ./...` is the cross-compile gate for the test layer** — `go build` never compiles `_test.go` files, so a bare cross-build is not compile verification of tests.
  - 25 requirements / 25 acceptance criteria (both at the Tier L ceiling), all PASS, verified against the run-phase implementation at HEAD `81e9fd41f` of worktree `~/.moai/worktrees/kanban-board` (per-milestone commits M1 `dc03459bf` → `5c2a942d1` → `f0786d16f` → M2 `4ae37e617` → `dba5af978` → M3 `b12e891ad` → fix passes `92065a1e4` → `f0ca483d0` → `640321877` → `431ce5071` + SHA backfill `81e9fd41f`; progress evidence §E.2/§E.3). Verification (kanban env unset): full suite `go test ./... -count=1` rc=0 / 0 fails; `golangci-lint run` 0 issues; host `go vet` 0; `GOOS=windows go vet ./...` rc=0; `GOOS=windows GOARCH=amd64 go build ./...` rc=0; coverage `internal/kanban` 88.0% / `internal/core/git` 86.7% (≥85% threshold). Sync-audit arc: FAIL 0.786 → re-audit PASS 0.882 (Functionality 88 / Security 88 / Craft 87 / Consistency 90, harmonic mean, Tier L threshold 0.85; report `.moai/reports/sync-audit-SPEC-KANBAN-BOARD-001.md`, local-only). Known recorded gap G1: the Windows test layer compiles under the vet gate and asserts its intended shapes but has NOT executed on a real Windows host — the first runtime execution is the release multi-os gate. README / docs-site / project-docs unchanged — measured: no user-facing surface changed (0 files under `internal/template/templates/`, no CLI, no rule/skill). The four inherited debts (kanban-dispatch.md disagreement, backlog column-vs-queue, binary lag, BOOTSTRAP adoption) and the BOOTSTRAP-bounded sole-writer seam stay open, recorded in §E.3/§E.4. This sync commit carries only the frontmatter `in-progress → implemented → completed` transition on all four SPEC artifacts + the `progress.md` §E.4 audit-ready signal + this CHANGELOG entry — zero code changes (sync is markdown-only). `sync_commit_sha` populated as `pending-backfill-sync` placeholder (self-referential-hazard workaround per `spec-frontmatter-schema.md` D3) and backfilled in a follow-up commit. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI

## [3.0.2] - 2026-07-29

### Added

- **[SPEC-PREEDIT-PARALLEL-SESSION-GUARD-001](.moai/specs/SPEC-PREEDIT-PARALLEL-SESSION-GUARD-001/spec.md)** — sync-phase close (3-phase plan→run→sync). Extended parallel-session detection from the write-agent **spawn** boundary to the **direct-edit** boundary (Edit/Write/Bash in the main session), closing the spawn-gate bypass that left foreign active sessions undetected mid-task. Added a Pre-Edit Sync Check doctrine section (`.claude/rules/moai/core/agent-common-protocol.md` § Pre-Edit Sync Check) mirroring the proven Pre-Spawn Sync Check (`moai session list` + `git fetch` + `git rev-list --left-right` before non-trivial direct edits to shared paths), broadened the `worktree-integration.md` § Parallel-Session Branch Conflict Auto-Isolation trigger to cover direct-edit work (not only "worktree entry chosen"), named the bypass failure mode inline, and shipped a read-only **advisory** PreToolUse-on-Edit hook (`internal/hook/session_guard.go` `checkForeignSessionAdvisory` — registry-read-only, fail-open, never blocks; surfaces foreign-session awareness to stderr + `.moai/logs/preedit-session-guard.log`). REQ-PES-005 (ambient signal) is satisfied by the existing SessionStart hook. The run-phase code + doctrine + SPEC artifacts landed in PR #1213 (merge commit `44484e3d7`, squash); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population + this CHANGELOG entry — no additional code changes. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI
- **[SPEC-CLI-TUI-MODERNIZE-001](.moai/specs/SPEC-CLI-TUI-MODERNIZE-001/spec.md)** — sync-phase close (3-phase plan→run→sync). The interactive TUI modernization (M1 Bubble Tea preview redesign + M2 dead-confirm removal + M3 huh theme unification) already landed in PR #1214 (mergeCommit `f81d9e890`, verified reachable on `origin/main`); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population. No code changes. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override). 🗿 MoAI
- **[SPEC-INVOCATION-MODEL-002](.moai/specs/SPEC-INVOCATION-MODEL-002/spec.md)** — sync-phase close (3-phase plan→run→sync). The invocation-model divergence reconciliation + Axis-A alignment (review↔`/code-review` compose; clean↔`/simplify` scoped out) already landed in commit `4285803ee` (M1, verified reachable: 1186-commit ancestry-path to `origin/main` tip); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population. Tier S doc-only. No code changes. Route B. 🗿 MoAI
- **[SPEC-WORKFLOW-CACHE-OPT-001](.moai/specs/SPEC-WORKFLOW-CACHE-OPT-001/spec.md)** — sync-phase close (3-phase plan→run→sync). The Workflow Bottleneck Phase 2 work (shared diagnostic snapshot contract + gate merging + delegation relaxation + audit defect-lists + bookkeeping batching, M1-M6, 36 AC) already landed in merge commit `ea5572edb` (verified reachable; directly contains the prior `run_commit_sha` `5b7691927` M6 verification-sweep commit as an ancestor); this sync commit carries only the frontmatter `in-progress → implemented → completed` transition + `progress.md` §E.4 `sync_commit_sha` population. No code changes. Route B. 🗿 MoAI
- **[SPEC-WORKTREE-ENTRY-STRATEGY-001](.moai/specs/SPEC-WORKTREE-ENTRY-STRATEGY-001/spec.md)** — codified the EnterWorktree-First worktree entry strategy across the three worktree-layer mechanisms (L1 runtime `Agent(isolation: "worktree")`, L2 `moai cc -w`/EnterWorktree, L3 `--worktree` persistent), extended the launcher `-w` flag to accept L2 absolute paths, defaulted the web console's worktree auto-toggles to OFF (explicit-user-opt-in intent), and added a parallel-session branch-conflict auto-isolation procedure (Tier L, 5-artifact set, 8 milestones incl. M3a launcher extension, 15/15 MUST ACs PASS + 1 SHOULD PASS). **M1** (`internal/config/defaults.go`, `### Changed` semantically): the web worktree automation toggles `AutoCleanup: true → false` and `AutoMerge: true → false` (AutoCreate was already `false`, unchanged) — web-console worktree automation now defaults OFF so a web-mode session never silently creates/destroys branches without explicit opt-in, aligning with the explicit-user-opt-in policy that the v3.0 main-checkout branch-guard already enforces on the CLI side. **M3a** (`internal/cli/launcher.go`, `### Added`): the `-w` / `--worktree` flag on `moai cc` / `moai glm` / `moai cg` is extended to accept `~/.moai/worktrees/<project>/...` absolute paths (L2 persistent worktree re-entry), additively — the short-name token-normalization behavior of `normalizeWorktreeFlag` is preserved byte-identical (11 existing subtests + 4 new ShortNamePreserved subtests green) and a new sibling `resolveWorktreeL2Path` resolver handles the absolute-path form; out-of-prefix absolute paths are rejected with an error naming both accepted prefixes. **M2–M6** (doc-rule alignment, `### Changed` semantically): `worktree-integration.md` EnterWorktree/ExitWorktree section expanded (L1-vs-L2 distinction, EnterWorktree-First policy, bare-`cd` deprecated, parallel-session auto-isolation procedure with conservative predicate + `auto-<session-short>-<spec-id>` naming); `session-handoff.md` + `session-handoff-examples.md` carry Block 0 canonical forms (`moai cc -w` Form A, `EnterWorktree` Form B); `CLAUDE.local.md` §22.8 addendum records the local-only dev intent; `init.go`/`update.go` Long help text carries an explicit opt-in note (no auto/enter/switch claim); each of the 3 rule docs is byte-identical to its `internal/template/templates/` mirror (`TestRuleTemplateMirrorDrift` PASS — sanitized-pair obligation, no SPEC-ID/REQ/AC tokens leak into templates per §25). Evidence: 15/15 MUST ACs PASS (AC-WES-001a/b/002/003/004a/b/c/005a/b/006/007/008/010a/b/c; AC-009 SHOULD also PASS); `go test ./internal/config/... ./internal/cli/...` 0 FAIL; cross-platform build (`go build ./...` darwin + `GOOS=windows GOARCH=amd64 go build ./...`) exit 0; `golangci-lint run` 0 issues; C-HRA-008 subagent-boundary grep 0 matches in touched `internal/cli` non-test files; `make build` regenerated catalog clean. Route B (PR-mandatory, this repo's `enforce_admins: true` branch-protection override); single sync commit carries the merged `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-HARNESS-LOOP-REPAIR-001](.moai/specs/SPEC-HARNESS-LOOP-REPAIR-001/spec.md)** — repaired the harness self-learning loop's four recurring defects (producer/consumer layout mismatch, species-barrier mis-wiring, undrained lesson inbox, missing CLI reporting) under a single falsifiability-disciplined SPEC. Tier L, 6 milestones, 17/17 AC PASS. **M1** (layout contract): introduced a shared accessor (`internal/harness/proposalgen/layout.go`) routing all three consumers through one path; the nested producer layout (52 live drafts) was adopted over flat. **M2** (route drafts to their designed consumer): de-wired `proposalgen` drafts from `Applier.Apply()` (a species barrier — discovery drafts are not apply inputs), added the `promote` path to manager-spec SPEC authoring with provenance round-trip + audit record, retired the `execute`→draft wiring with an honest diagnostic (AC-HLR-014), and placed the applicability guard in-`Apply` pre-flight before snapshot creation (defense-in-depth per §A.7). **M3** (dispatch observation): wired the routing-ledger `record`→`evidence`→`Stop` finalize lifecycle (opt-in gated). **M4** (generator quality): excluded bare-tool-name `agent_invocation` promotions and made draft IDs stable across dates (one `pattern_key` → one draft). **M5** (lesson channel): reconciled the constitution § Lessons Protocol to name the topic-file convention (`feedback_*.md` + `MEMORY.md`) as the single designated store (the stale `lessons.md` marked `[SUPERSEDED]`), and named the drain actor/trigger; drained the inbox 870→149. **M6** (falsifiability + CLI reporting): made `moai harness --help` enumerate every registered verb by deriving the Long from `cmd.Commands()` (round-trip, not a static token list — AddCommand is the single SSOT); aligned `list` and `doctor` to agree that a command-only thin harness is an expected INFO state, not a defect; and recorded `prediction:`/`verified:` on this SPEC's own lesson entries — the SPEC is the first test of the obligation it codifies. Every falsifiable AC carries an executed falsification (revert → observe failure → restore → green). Full `go test ./...` (105 packages, 0 FAIL) + cross-platform (`GOOS=windows`/`GOOS=linux`) builds + `golangci-lint` 0 issues; C-HRA-008 subagent-boundary grep 0 matches. §G open questions q1-q5 all resolved (q1 M1, q2 M4, q3 M5, q4/q5 Implementation Kickoff gate). Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the merged `in-progress → completed` 3-phase close. 🗿 MoAI

- **[SPEC-WORKTREE-BRANCH-GUARD-001](.moai/specs/SPEC-WORKTREE-BRANCH-GUARD-001/spec.md)** — added a PreToolUse conditional-deny hook that mechanically enforces the main-checkout branch-state isolation doctrine, closing the gap between the v1.0.0 doctrine-only `main-checkout-branch-guard.md` rule and actual runtime enforcement (Tier M, 13 ACs: 11 PASS + AC-WBG-003 branch-state-layer PASS with documented Gap-G1 + AC-WBG-007 sanitized-pair PASS-WITH-DEBT). **(1) Conditional-deny hook** (`internal/hook/branch_guard.go`, new, + `internal/hook/pre_tool.go` Handle integration) — denies branch-state-changing git commands (`git switch`, `git checkout <branch>`/`-b`, `git branch -d`, `git stash`, `git rebase`, `git merge` onto the checked-out branch) when ALL three hold: primary checkout (absolute git-dir equals absolute git-common-dir), command matches the `branchStatePatterns` regex set, and invoking agent is not exempt. Sentinel deny reason prefix `BRANCH_GUARD_VIOLATION:`. The deny rides the new fast `checkBranchState` regex path (microseconds, structurally distinct from `checkBashCommand`'s `DangerousBashPatterns`), NOT the 10.58s census-P1-B quality-gate path — measured wall-time ≤ 500ms per invocation against the 5s PreToolUse budget. **(2) Discriminant** (`isPrimaryCheckout`) — `git rev-parse --path-format=absolute --git-dir` vs `--git-common-dir` on git 2.31+, falling back to `--absolute-git-dir` + cwd-normalized `--git-common-dir` on older / Apple Git. **(3) manager-git exemption** (`isExemptAgent`, identity-based NOT phase-scoped) — `HookInput.AgentType == "manager-git"` (main-thread `claude --agent manager-git`) OR `MOAI_BRANCH_GUARD_EXEMPT=1` env var (closes the sub-agent-spawn gap where Claude Code does not reliably populate `agent_type`). **(4) Fail-open with advisory** (REQ-WBG-012) — uncertainty (not a git repo, git binary missing, non-zero `git rev-parse`, absent agent identity) falls through to `allow` + stderr advisory + `.moai/logs/branch-guard-audit.log` entry, aligning with the Bash Risk-Amplifier FAIL-OPEN doctrine. **(5) Worktree allow** — git-dir ≠ git-common-dir short-circuits to `allow` regardless of command pattern (legitimate worktree branch operations never denied). **(6) CLI worktree advisory** (`internal/cli/{init,update,web}.go`) — `moai init`/`update`/`web` surface advisory wording recommending worktree isolation for branch-changing work (`moai cc -w` / `moai cg -w`). **(7) `.worktreeinclude` reconciliation** — repo-root and template copies reconciled to byte-identity; redundant `teammateMode` mention dropped from the local copy. **(8) Doctrine rule bump** (`.claude/rules/moai/workflow/main-checkout-branch-guard.md`, v1.0.0 → v1.1.0) — documents the mechanical enforcer that supersedes the doctrine-only text. **(9) Template mirror §25 sanitized-pair** — `internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard.md` is intentionally NOT byte-identical to the source rule: §25 forbids a SPEC-ID in the template mirror, so the mirror is §25-sanitized (SPEC-ID → generic prose) and enrolled in the `sanitizedPairPaths` registry (`internal/template/sanitized_pair_parity_test.go`) so `TestSanitizedPairParity` enforces doctrine parity while `TestTemplateNoInternalContentLeak` enforces mirror §25 cleanliness. The source retains the SPEC-ID + REQ tokens for traceability; the rule is intentionally excluded from the `workflowOptMirroredPaths` byte-parity allowlist (NOTE comment in `internal/template/rule_template_mirror_test.go`). `branch_guard.go` coverage 94.1% floor / 91.7% statement (≥85% AC §C gate); full `go test ./internal/hook/... ./internal/template/...` exit 0; `GOOS=windows` cross-build exit 0; `golangci-lint run` 0 issues. **Known debt (transparent, both documented in acceptance.md F-1 + spec.md §E)**: (a) AC-WBG-003 Gap-G1 — end-to-end `git reset --hard` allow is unsatisfiable through Handle because `git reset --hard` is pre-gated by the pre-existing `AskBashPatterns` (returns DecisionAsk before the branch-state check); the exemption is proven at the branch-state layer + via the non-ask command `git switch main`, and this is the documented Phase-D interim break (acceptance.md F-1, pending a follow-up SPEC for the orchestrator-side `MOAI_BRANCH_GUARD_EXEMPT=1` spawn contract for `Agent(manager-git, ...)` Phase D invocations); (b) AC-WBG-007 sanitized-pair (REQ-WBG-007 amended from byte-parity in commit `4764e2579` after the orchestrator's M3 premise was corrected — §25 forbids a SPEC-ID in the template mirror, making byte-parity impossible without violating `TestTemplateNoInternalContentLeak`). Route B (PR-mandatory, this repo's `enforce_admins: true` branch protection); single sync commit carries the merged `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-TEMPLATE-DATE-NEUTRALITY-002](.moai/specs/SPEC-TEMPLATE-DATE-NEUTRALITY-002/spec.md)** — widened the `S1-internal-date` template-neutrality guard from `202[6-9]` to `202[5-9]`, remediated 32 `2025` date-stamp leaks across 25 template files, and added 24 content-anchored allowlist carve-out entries for the predecessor's preserved dates (version-history records, distinguishable `Created:` stamps, deadlines). Tier L, 6 milestones, 33/33 AC PASS (independently verified by orchestrator read-only batch). The committed `classify.sh` instrument reproduces the triage baseline (74 occurrence-class rows / 48 distinct findings / 34 distinct files); the widened strict tier reports zero findings. M2 adjudication split the 14 `HIST` version-history rows: 2 bullet-form rows removed (resolving the predecessor SPEC's flagged "Known cosmetic residue"), 12 table-form rows preserved as factual release notes. CI workflow test-target invocations normalized to the canonical quoted form. No user-facing behavior change — template-internal neutrality-guard infrastructure (README/docs-site intentionally untouched).

- **[SPEC-GOAL-DOCS-RETIRE-001](.moai/specs/SPEC-GOAL-DOCS-RETIRE-001/spec.md)** — retired native `/goal` emission references from public and internal documentation across four locales (en/ja/ko/zh), the public-documentation consequence of `SPEC-GOAL-SURFACE-UNIFY-001`. Tier M, 5 milestones, 12/12 AC PASS (11 clean + 1 PASS-WITH-DEBT, AC-GDR-007 off-by-one internal contradiction resolved during run-phase). Sweep targets: 12 locale files (3 pages × 4 locales) carrying 24 emission markers across `autonomous-loops.md`, `self-evolving.md`, `handoff.md`; retention surfaces: 4 files (native Claude Code factual statements, the H3/comparison-row in `autonomous-loops.md`, the strategy record superseding note). Every emission detector is locale-invariant (backticked code literal or structural marker; no translatable-prose anchors). All 12 baselines re-measured at run-phase entry against the squash-merge commit `24c84c56e` (the run-phase commit SHA) reproduced the acceptance.md §B recorded values exactly (12/12). 4-locale parity verified: every swept marker reached its target in all four locales simultaneously; no detector shows a locale split. Docs-site build (`hugo --minify --gc`) exit 0, warning-free, with sitemap present; URL blacklist and Mermaid TD-only greps clean. AC-GDR-007 debt resolved during run-phase via manager-spec correction: the sentinel-line exclusion form was adopted, keeping the pin at `25` (25 historical occurrences survive; the sentinel line itself is filtered from the count). The `docs-site` surface now cleanly separates MoAI's programmatic `/moai goal` from Claude Code's native HUMAN-ONLY `/goal` TUI command — four locales document `/moai goal` as the orchestrator-armed primitive and `native /goal` as a retired emission path, with the retained H3/comparison-row factual statement preserved. Single sync commit carries the merged `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-GOAL-SURFACE-UNIFY-001](.moai/specs/SPEC-GOAL-SURFACE-UNIFY-001/spec.md)** — unified the goal-arming surface on `/moai goal` (the orchestrator-armed, programmatic reimplementation) and retired native Claude Code `/goal` as an emission target, because native `/goal` is a HUMAN-ONLY TUI command the model cannot invoke on the user's behalf — a doctrine surface instructing its emission named an unreachable path. Tier L, 7 milestones, 30/30 AC PASS. **(1) Doctrine rewrite** (M1-M4, 15 local files) — `goal-directive.md` rewritten in place onto the `/moai goal` surface with a retained `## Native \`/goal\` Prohibition` section; the obsolete Post-Paste `/goal` Follow-up Block two-step delivery mechanism (made obsolete because `/moai goal` is orchestrator-armed, not user-typed) was removed in full from `session-handoff.md` / `session-handoff-examples.md` / `output-styles/moai/moai.md` §8, including its localization-table row, pre-emit self-check item, and cross-references; `workflows/goal.md` and `native-invocation-model.md` gained bidirectional wiring for the retained HUMAN-ONLY classification. **(2) Slash-command wrapper** (M5) — added the `/moai:goal` thin slash-command wrapper (`internal/template/templates/.claude/commands/moai/goal.md.tmpl`), the one `/moai` subcommand that previously lacked one, embedded via `make build`. **(3) Template mirror parity** (M6, 15 files) — restored byte-identical parity across all 14 `.claude/**` local/template pairs plus the intentionally divergent root `CLAUDE.md` pair. **(4) Go emission paths** (M7, TDD `cycle_type`) — the auto-injected resume renderer (`internal/hook/handoff_inject_render.go`, previously untested) now emits `/moai goal` in all four locale blocks (ko/ja/zh/en); a RED-first regression test (`handoff_inject_render_test.go`, new) was authored and observed failing before the change; the `PrimitiveGoal` v4manifest token was verified to have zero existing occupancy before alignment; the `moai handoff save --goal` CLI flag name is preserved (only its help string changed); the native-`/goal` yield invariant in `internal/goal/evaluate.go` is retained (interoperation with a human-typed native `/goal`, not an emission path). Three retention surfaces are preserved by design: `goal-directive.md` § Native `/goal` Prohibition, `native-invocation-model.md`'s HUMAN-ONLY classification, and the `evaluate.go` yield invariant. The public-documentation layer (13 files, `docs-site/**` + `autonomous-workflow-strategy.md`) was split to a dependent follow-up SPEC (`SPEC-GOAL-DOCS-RETIRE-001`, not yet started) after a plan-audit iteration returned STOP on scope size; that split is recorded in `spec.md` §B.7. Full `go test ./...` + cross-platform (`GOOS=windows`) builds exit 0; `golangci-lint` 0 issues; template neutrality/leak-guard tests pass. Single sync commit carries the merged `in-progress → implemented → completed` 3-phase close. 🗿 MoAI
- **[SPEC-ASTGREP-EDIT-001](.moai/specs/SPEC-ASTGREP-EDIT-001/spec.md)** — ast-grep wiring repair and the new `moai ast-edit` write-side command (Tier L, tdd; AC matrix AC-001/002/003, 010/011/012, 020/021, 030/031, 040/041, 050/051, 060/061/062, 070). The first independent sync audit returned **FAIL (0.770 against the Tier L 0.85 threshold)** — against the certification layer, not the implementation. Five acceptance criteria named `go test -run` selectors matching **zero** tests; because `go test -run` exits 0 on a zero-match selector and prints `[no tests to run]`, those five would have certified a fully reverted implementation as passing. They were repaired (real test names, `-count=1`, PASS restated as the presence of a `--- PASS:` line) together with two further must-fix findings, each carrying falsification evidence. A re-audit then scored **0.843**, still 0.007 short, and surfaced a sixth criterion (AC-012) carrying the same unfilled-placeholder defect plus a stale "safe default" heading on REQ-AGE-002 — both repaired in a third amendment. `progress.md` § Sync-Audit Remediation records the findings with their falsification evidence, and § G.1 names every residual item the close deliberately carries rather than dropping it from the record: the `--dry` default, a pre-existing template-mirror divergence that cannot be closed without violating template neutrality, the unparsed nested `rule:` block form, and a `FilesModified` double-count. **(0) The rewrite guards never ran in CI** — `.github/workflows/` provisioned no ast-grep, so every fixture-rewrite guard, including the falsification guards added here, was skipped by `requireSG`. Investigating it found the reported "absent `sg` → silent skip" was not Ubuntu's behaviour: Ubuntu and Debian ship `/usr/bin/sg` as a newgrp alternative, which satisfies `exec.LookPath` while being an unrelated program, so the guards do not skip — they run and **fail** (reproduced by putting a non-ast-grep `sg` first on `PATH`: three FAILs). `requireSG` now confirms `sg --version` reports ast-grep, reusing the check `internal/astgrep/rule_seed_test.go` already carried for the same reason, and `ci.yml` installs `@ast-grep/cli` (pinned, because the rewrite path parses sg output) on all three runners and then asserts the resolved binary is ast-grep, failing the job rather than skipping silently. **(1) `moai ast-edit`** (new command, `internal/cli/astedit.go`) — a distinct write-side entry point registered separately from the read-only `moai ast-grep`, so a `Bash(moai ast-grep:*)` permission grant never widens into write capability; `--dry` computes and prints the `ReplaceResult` without touching any file. **`--dry` is opt-in, not the default** — a run without it rewrites files in place, which the help text states explicitly (`This command MODIFIES FILES IN PLACE. Run it with --dry first to preview the changes`). Inverting that default, or gating the write path behind an explicit `--write`, is deliberately left to a follow-up SPEC rather than decided here. Pattern mode (`--pattern`/`--rewrite`) applies via `SGAnalyzer.PatternReplace`; rule mode (no `--pattern`) applies every loaded rule's `fix:` field, with `--rule <id>` narrowing to one rule. Skipped rules are counted under two distinct notices: a rule with no `fix:` is detection-only by its author's choice, whereas a rule whose matcher the loader cannot read (a nested `rule:` block) is a loader limitation the author neither chose nor can see — reporting both as "no fix: field" would misattribute the second to the first. **(2) `sg run --rewrite` no-op fix** (`internal/astgrep/analyzer.go` `PatternReplace`) — the non-dry rewrite path was missing `--update-all`, so `sg run --rewrite` printed a diff and exited 0 without ever touching disk; the existing unit test had pinned the buggy argument string, so the gap was undetected. Fixed by adding `--update-all` and re-pinning the mock expectation to the write flag, with a fixture-rewrite assertion (not mock-arg-only) as the falsification guard. **(3) `FindRulesConfig` search-path repair** (`internal/hook/security/rules.go`) — added `.moai/config/astgrep-rules/sgconfig.{yml,yaml}` to the resolver's search list (the shipped ruleset location every default MoAI project actually has) and removed the two retired `moai-tool-ast-grep` skill paths, which could never resolve since that skill no longer exists; previously the resolver returned the empty string for every default project and the security hook's rule load silently found nothing. **(4) Shipped rule `fix:` assessment** — all 4 shipped rule files (`go-no-hardcoded-api-url`, `go-no-duplicate-coverage-threshold`, `sec-hardcoded-credential`, `sec-weak-hash-md5`, `sec-command-injection-{shell,exec}`) remain deliberately detection-only; the disposition table in `acceptance.md` § REQ-AGE-006 records an evidence-backed reason per rule (constant-name invention, compile-breaking import mismatch, secret-exposure masking, etc.) — zero `fix:` fields were added, which is a verdict, not a deferral. A known gap is recorded (not fixed): the shipped rules use ast-grep's nested `rule:` block form, which `astgrep.Rule` does not parse (only the flat `pattern:` field), so rule mode conservatively skips them rather than passing an empty pattern to `sg`. **(5) Dead skill-reference cleanup** — replaced `moai-tool-ast-grep` mentions with the surviving `moai ast-grep` / `moai ast-edit` surface across `moai-foundation-cc/SKILL.md`, `moai-workflow-ddd/SKILL.md` (including its `related-skills` frontmatter), `moai-workflow-loop/SKILL.md`, and their template mirrors. **(6) Docs** — `moai ast-grep` / `moai ast-edit` documented across all 4 docs-site locales (`docs-site/content/{en,ja,ko,zh}/cli-reference/ast-grep.md` + `_meta.yaml`); the README set does not enumerate individual CLI commands, so no README change applies. Full `go test ./...` (105 packages, 0 FAIL) + `golangci-lint run` (0 issues) + `go test ./internal/template/ -run 'Neutrality|InternalContent|Leak|Namespace'` all clean. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the merged `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CC2219-UPSTREAM-ALIGN-001](.moai/specs/SPEC-CC2219-UPSTREAM-ALIGN-001/spec.md)** — Claude Code 2.1.208..2.1.219 upstream alignment umbrella covering 8 genuine-drift clusters (GD-1/2/4/5/6/7/8/9; GD-3 excluded, already landed as PR #1146) in one Tier L effort, 27/27 AC PASS. **(1) Nesting + permission-mode doctrine re-correction** (GD-1/GD-2, 7 doctrine surfaces live+template) — corrected the stale "subagent nesting defaults to off" claim to the v2.1.219 reality (nesting is enabled by default, depth 3, `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` disables it); removed the now-false "double guarantee" framing; retired the `sync-auditor` read-only nesting pilot (`Agent` tool removed from its `tools:` list, restoring flat-by-omission) since its safety rationale no longer holds once nesting defaults on; corrected every doctrine surface asserting `mode: "plan"` as a read-only guarantee to ground read-only enforcement in tool restriction instead, since the Task/Agent spawn-time `mode` parameter has been deprecated and ignored since v2.1.213. **(2) Opus 5 default-Opus migration** (GD-4, `internal/template/model_policy.go` + `internal/web`) — the `opus` alias now resolves to `claude-opus-5`, with `claude-opus-4-8` demoted to a deprecated-canonical row; the `internal/web` appbar default and its test were updated to the Opus 5 id; the doctrine naming sweep across ~10 files replaced the stale "opus = Opus 4.8" claim and added a disambiguated "Opus 5 (1M)" row to the context-window threshold table. **(3) Doc-only sync** (GD-5/6/7/8/9) — `native-invocation-model.md` re-marked `/code-review` and `/deep-research` as manual-invocation-only (no longer auto-invocable per 2.1.215/2.1.218); `dynamic-workflows.md` gained the `unrestricted` workflow-size enum value, an explicit `medium` default, and the `workflowSizeGuideline` settings key; `hooks-system.md` gained the `DirectoryAdded` event entry; `agent-authoring.md` fork-subagent section now documents `/subtask` (in-session fork) vs `/fork` (background session copy); `skill-authoring.md` documents `context: fork` skills running in the background by default with a `background: false` opt-out. **(4) docs-site 4-locale sync** — a grep sweep across `docs-site/content/{en,ko,ja,zh}` + both READMEs found and fixed one stale-nesting-default-off drift site (`claude-code/agentic/sub-agents.md`, 4-locale parity edit); the other three stale-claim families (mode:"plan", Opus-4.8-as-default, auto-`/deep-research`) returned 0 matches, recorded as evidence rather than edited speculatively. Every `.claude/` edit carries its `internal/template/templates/` mirror (Template-First); `make build` + `go test ./internal/template/... -run 'Leak|Neutrality'` exit 0; full `go test ./...` (107 packages) + cross-platform (`GOOS=windows`) builds exit 0; `golangci-lint` 0 issues; `moai spec lint` 0 findings for this SPEC. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **Epic SKILLPORT — [SPEC-SKILLPORT-CLAIM-CHECK-001](.moai/specs/SPEC-SKILLPORT-CLAIM-CHECK-001/spec.md) + [SPEC-SKILLPORT-SVG-INFOGRAPHIC-001](.moai/specs/SPEC-SKILLPORT-SVG-INFOGRAPHIC-001/spec.md) + [SPEC-SKILLPORT-HUMANIZE-LEDGER-001](.moai/specs/SPEC-SKILLPORT-HUMANIZE-LEDGER-001/spec.md)** — three clean-room-authored skill capabilities (Tier M/M/S, all 3 SPECs PASS — 17/17, 19/19, and 18/18 AC respectively, 0 FAIL). Each SPEC borrows only a functional capability description and re-authors it entirely from moai's own SPEC prose — no third-party wording, section structure, code, or file layout was consulted, and the shipped files carry zero vendor attribution (mechanically verified: `grep -ril 'skillstead'` → 0 across all three skill directories, no `NOTICE` file, and the house-convention `license: Apache-2.0` field is either newly added — the two new-skill SPECs — or preserved byte-unchanged — the enhancement SPEC). **(1) `moai-workflow-docs-claim-check`** (NEW skill) — a read-only check of whether a public-facing document's claims (README, release notes, install/usage guides) are supported by user-supplied evidence, running Preflight → Claim Triage → Validation, splitting composite claims into atomic ones and labeling each; it runs no commands and writes no fixes. **(2) `moai-domain-svg-infographic`** (NEW skill) — authors editable SVG technical infographics (architecture / flow / comparison / hierarchy) by computing the layout numerically before writing markup, then renders a 2x PNG via headless Chromium; ships a CJK-first font stack, a deterministic source lint (`scripts/check-svg.mjs`), a render script (`scripts/render.mjs`), and explicit mermaid-vs-SVG selection rules — positioned as additive/complementary to the existing mermaid pipeline, never a replacement. **(3) `moai-domain-humanize` v1.2.0 → v1.3.0** (enhancement) — grafts an Invariant Ledger (pre-edit boundary: a 4-category taxonomy of facts/figures/comparisons/uncertainty, each item marked supplied or inferred, unmarked defaulting to supplied as a fail-safe) and a Delta Audit (post-edit verification across claim-and-intent parity, survival check, and audience/tone/purpose fit) into the skill's existing workflow (steps 2 and 6), strictly additive — the pre-existing S1/S2/S3 severity model, A/B/C/D grading, 30%/50% over-editing guardrails, and 4-language routing are unchanged (regression-verified). All three skills are registered in `internal/template/catalog.yaml` (40 total skills) with byte-identical template↔local mirrors (`make build` clean); `internal/template` neutrality/leak-guard tests pass (0 internal SPEC-ID/REQ/AC tokens, 0 vendor tokens in any shipped file); full `go test ./...` exits 0. PR-mandatory regime (branch `feat/skillport-epic`); single sync commit carries the merged `in-progress → implemented → completed` 3-phase close for all three SPECs. 🗿 MoAI

- **[SPEC-SEC-DEEPSCAN-001](.moai/specs/SPEC-SEC-DEEPSCAN-001/spec.md) + [SPEC-SEC-GUARDIAN-001](.moai/specs/SPEC-SEC-GUARDIAN-001/spec.md)** — Epic SECURITY-ABSORB: two complementary, natively-reimplemented security layers absorbed from the Anthropic official security plugin architecture (Tier L, both PASS — 27/27 and 25/25 AC). **(1) On-demand deep scan** (`/moai review --deep`, SPEC-1) — a heavy, explicit multi-agent vulnerability scan built on Dynamic Workflows: a 6-phase read-only recon → hunt → verify pipeline runs a 3-voter adversarial verification panel (REACHABILITY / IMPACT / DEFENSES, 2-of-3 quorum) over each candidate finding, generates fix patches in an isolated scratch clone (never applies them directly — one finding = one patch = one PR), and writes timestamped JSONL results under `.moai/reports/security-deepscan/` (gitignored). Degrades gracefully on runtimes below Dynamic-Workflows availability (`v2.1.154+`). No shipped script, no new agent — a markdown playbook addition to the existing `review` workflow skill plus a command `argument-hint` update (`--deep`/`--patch`/`--commit <SHA>`). **(2) Always-on in-session guardian** (SPEC-2) — a light, 3-layer inline guardian complementing the on-demand scan: **Layer 1** (`internal/hook/security/`, new `patterns.go`/`scan.go`) is a PostToolUse regex pattern engine (~28 patterns across 10 vulnerability classes, 16-language-neutral) that surfaces advisory warnings on Write/Edit, never blocking; **Layer 2** (`diff.go`) is a Stop-hook turn-end diff review that scans only the turn's added lines and surfaces high-severity findings in the stop message; **Layer 3** (`guardian.go`, dormant by default, opt-in) is a commit-time cross-file review reading changed-plus-related files for cross-file vulnerability classes (e.g. IDOR). All three layers are advisory-first (fail-open on non-git directories, empty stdin, or malformed JSON) with blocking gated behind an explicit `MOAI_SECURITY_BLOCKING` opt-in and a `--skip-hook` audit trail. Shipped via three Template-First shell wrappers (`handle-security-{scan,turn,commit}.sh`) + `moai hook security-{scan,turn,commit}` Go subcommands + `settings.json` wiring (Layer 1 async); `internal/hook/security` coverage 90.0%. Both SPECs preserve the strict subagent-user-interaction boundary (0 `AskUserQuestion`/`mcp__askuser` references in any hook or scanner path). Full `go test ./...` + cross-platform (`GOOS=windows`) builds exit 0; `golangci-lint` 0 new issues. 🗿 MoAI

### Removed

- **[SPEC-DB-RETIRE-001](.moai/specs/SPEC-DB-RETIRE-001/spec.md)** — removed the DB documentation subsystem in full after an independent static analysis found it to be unwired vaporware (dormant `db-schema-sync` hook with no wrapper/settings wiring, zero `db.enabled` consumers, a missing `internal/db/parser/` package, no writer, and guidance/code mismatch). Deleted the `internal/hook/dbsync/` package (3 files) and its CLI wiring in `internal/cli/hook.go` (the `db-schema-sync` subcommand, `runDBSchemaSync`, `loadMigrationPatterns`, `defaultMigrationPatterns`, and the now-orphaned `splitLines`/`trimSpace` helpers); removed the `.moai/project/db/` scaffold (7 files) and `.moai/config/sections/db.yaml` from both the template tree and this repo; and stripped the DB Detection phase from the `project` doc-generation workflow and the DB Schema Doc Check phase from the `sync` quality-gates workflow (local + template mirrors kept byte-identical). **Deployed-user impact:** the orphaned `.moai/config/sections/db.yaml` is **auto-removed** on the next `moai update` (registered in `defs.DeprecatedPaths` — no manual action needed). The `.moai/project/db/` documentation scaffold sits under the `.moai/project` preserve root, so `moai update` cannot delete it — **existing projects should manually delete `.moai/project/db/`** if the generated DB docs are no longer wanted. `go build ./...` + `go test ./...` + `make build` exit 0. 🗿 MoAI

### Fixed

- **[SPEC-PRETOOL-GATE-MOVE-001](.moai/specs/SPEC-PRETOOL-GATE-MOVE-001/spec.md)** — relocated the heavy commit-quality gate (vet/lint/test) off the PreToolUse 5s hook budget to a native git pre-commit hook, closing census C-2 (CRITICAL): the gate's deny verdict was silently dropped after 5s, and under maintainer `defaultMode: bypassPermissions` that PreToolUse deny was the sole blocking mechanism between Claude Code and a bad commit (Tier M, 15/15 AC PASS). **(1) `moai gate` verb** (`internal/cli/gate.go`, NEW) — thin cobra wrapper exposing `QualityGate.Run` as a standalone CLI command, invoked by the pre-commit hook so the full step budget (vet 30s + lint 60s + test 120s per `gate.yaml`) is available outside Claude Code's 5s PreToolUse budget. **(2) `preCommitHookContent` extension** (`internal/cli/hook_install_precommit.go`) — the SPEC-PRECOMMIT-001 installer's hook body now invokes `moai gate` after the existing fast subset (`gofmt -l` + `go vet`), guarded by `command -v moai` (16-language neutrality: non-moai projects pass silently) and `SKIP_MOAI_PRECOMMIT=1` (bypass parity with `SKIP_MOAI_PREPUSH`); byte-identical mirror shipped at `internal/template/templates/.git_hooks/pre-commit` (closing a pre-existing gap where the template file was absent). **(3) `--no-verify` bypass defense** (`internal/hook/pre_tool.go`, F5 mechanical) — a fast sub-millisecond regex guard denies `git commit --no-verify` (standalone or compound) via the existing PreToolUse path, because under `bypassPermissions` the PreToolUse deny is the sole blocking mechanism and `--no-verify` would otherwise bypass the relocated gate; `.claude/rules/moai/development/coding-standards.md` §Bash Risk-Amplifier Doctrine names `git commit --no-verify` as a destructive primitive. AC-PGM-003 proves budget independence at the integration boundary (`TestPreCommitRelocation_BudgetIndependence`, 9.91s > 5s); AC-PGM-004 proves the mechanical guard (4 subtests + negative case). Full `go test ./...` exit 0; `golangci-lint` 0 issues; cross-platform (`GOOS=windows GOARCH=amd64`) build exit 0; subagent boundary grep 0; template neutrality 0 (no SPEC/REQ tokens, `#!/bin/sh` POSIX, no macOS-bias paths). PRESERVE verified: ast-grep scanner (SPEC-FALSE-ALLCLEAR-GUARD-001), `IsGitCommit`/`detectToolchain`/16-language table, `PrePushInstaller`, frozen-zone guard all untouched. **Known debt (transparent)**: package-aggregate coverage `internal/cli` 75.2% / `internal/hook` 83.4% (acceptance.md §F target ≥85%) — touched files are well-covered, the package aggregate lags on legacy code; pre-existing ast-grep findings (`pkg/version/version.go`, `test/integration/harness/it02_tier3_test.go`) now surface via `moai gate` exit 1 rather than being silently dropped (the documented `SKIP_MOAI_PRECOMMIT=1` escape hatch applies). Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the merged `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-FALSE-ALLCLEAR-GUARD-001](.moai/specs/SPEC-FALSE-ALLCLEAR-GUARD-001/spec.md)** — closed two false all-clear defects found by a codebase census: unconditional `slog` suppression across every CLI subcommand, and `moai ast-grep` reporting a clean scan when the `sg` scanner binary was absent (Tier M, 2 milestones, 18/18 AC PASS; independent sync-audit CONDITIONAL PASS 89.9/100, both must-pass dimensions — Functionality 90, Security 95 — clearing independently). **(1) M1 — logging scope** (`internal/cli/logging.go`, new) — `InitDependencies`/`initLightDeps` had each installed a discarding `slog` handler unconditionally for every subcommand, silencing all 218 warn-and-above call sites (198 `Warn` + 20 `Error`) and leaving `MOAI_LOG_LEVEL` wired to config but reaching no handler. Logging is now decided once, in `Execute()`, before the trivial/full-init branch: `moai hook <event>` keeps the discarding handler (stdout carries the hook's JSON contract, stderr belongs to the Claude Code runtime), every other subcommand writes warn-and-above to stderr, and `MOAI_LOG_LEVEL` (read via the existing `config.EnvLogLevel` constant) raises or lowers that bar, falling back to `warn` on an unparseable value. `InitDependencies` now inherits the process default instead of re-installing one (measured `slog.SetDefault` site count 2 → 1). **(2) M2 — ast-grep scanner sentinel** (`internal/astgrep/scanner.go`) — `Scanner.Scan` returned `([]Finding{}, nil)` when `sg` could not be resolved, indistinguishable from a clean scan; it now returns `ErrScannerUnavailable`, wrapped so the message names the binary and carries install guidance while `errors.Is` still matches through the wrap (`ValidateBinary` still runs first, so a rejected binary path stays a distinct, non-sentinel error). `moai ast-grep` writes the guidance to stderr and exits non-zero (previously exit 0 + `no findings` + 0 stderr bytes) — a breaking change for any scripted caller relying on the old exit-0 behavior; `moai ast-edit` deliberately keeps exit 0 (a mutator's "nothing to apply" is a genuine no-op). The commit-time PreToolUse quality gate still never blocks on `sg` being absent, but the pass now carries a reason — two named constants distinguish "scanner absent" from "scan failed", propagated through `QualityGate.Run` and emitted on `HookOutput.SystemMessage` (not `slog`, since the `moai hook` path discards records under M1). `moai doctor` gained an `sg` check (warn-not-fail, mirroring the existing `checkGitHubCLI` version-override seam so golden snapshots stay host-independent), and all four docs-site locales were corrected — each previously claimed both commands exit without an error, which is now false for `ast-grep`. Six pre-existing tests that asserted the silent pass were inverted (not dropped) so they fail again if the false all-clear returns; none was weakened. Verified: 18/18 AC PASS; `go test -count=1 ./...` exit 0 (105 packages, 0 FAIL); both `AC-FAG-011` falsification forms (call-deleted in `pre_tool.go`, body-neutered in `astgrep_gate.go`) fail in detached probe worktrees, plus a third auditor-added form; `go vet`, Windows and Linux cross-builds exit 0; scoped `gofmt` clean (the 106 pre-existing repo-wide unclean files are untouched, explicitly out of scope); coverage improved or held flat across all five touched packages (`internal/astgrep` 88.7→89.0, `internal/hook/quality` 87.5→87.6, `internal/hook` 83.3→83.4, `internal/cli` 75.3→75.4, `internal/cli/uikit` 98.8 flat). **Known debt (transparent, deferred to a follow-up)**: `AC-FAG-017`'s skip-count sub-assertion greps `^--- SKIP` on non-verbose `go test` output, which never emits that line and is therefore structurally pinned at 0 — the underlying requirement (no new test skips) was verified directly instead (56 skips at both merge-base and HEAD, empty named-skip diff in both directions); `AC-FAG-008`'s `install`-guidance sub-assertion passes with M1 alone (zero M2 change), though its other two sub-assertions (exit code, absence of `no findings`) correctly discriminate; `AC-FAG-015`'s prose mislabels which of its two commands is the actual registration guard. The largest residual risk (sync-audit Gap 3): whether Claude Code's runtime actually surfaces `HookOutput.SystemMessage` to the user on a PreToolUse `allow` decision was confirmed on the wire (the field is emitted correctly) but not against a live Claude Code session. `MOAI_LOG_FORMAT` remains unwired — explicitly out of scope per `spec.md` §C — and is now a visibly misleading knob rather than an invisibly inert one, a candidate for a follow-up SPEC. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CI-FLAKY-STABILIZE-001](.moai/specs/SPEC-CI-FLAKY-STABILIZE-001/spec.md)** — root-caused and fixed two independently-confirmed CI flaky defects (Tier M, 28/28 AC PASS — 0 FAIL, 0 PASS-WITH-DEBT). **(1) cobra lazy-sort data race** (`internal/cli/main_test.go`, `internal/cli/preference/main_test.go` — new) — cobra v1.10.2 sorts `c.commands` and writes `commandsAreSorted` *inside* the `Commands()` accessor itself, so concurrent first-call `t.Parallel()` tests racing on `PreferenceCmd.Commands()` / `rootCmd.Commands()` triggered `WARNING: DATA RACE` (reproduced 20/20 with an isolated, real-global repro test — see progress.md §E.2 M1 — then eliminated 0/20 after the fix, and 0 races across 50 fresh-process `-race` runs each for both packages). The fix adds a `TestMain`-driven recursive warm-up (`warmUpCommandTree`) that walks the full command tree from `rootCmd`/`PreferenceCmd` before `m.Run()`, forcing the lazy sort to complete before any parallel test starts. A reachability-signal guard test (`warmUpDone`, asserted only inside the warm-up function body — never in `TestMain` — so the guard's FAIL path is provably reachable, verified by round-trip: comment out the warm-up call → guard FAILs deterministically, restore → PASSes) replaces the original 0.1.0 sort-state assertion, which was structurally unfalsifiable (cobra always leaves the slice sorted once `Commands()` is called, warm-up or not). An independent sync-audit later found `warmUpDone` incomplete: it proves warm-up was *called*, not that it *traversed* — gutting only the recursive loop body while keeping the `TestMain` call left `warmUpDone == true` yet still reproduced the race (1/20). The guard was strengthened (0.4.0, AC-CFS-004c) with a second, additive signal `warmUpVisited` (incremented once per node the warm-up recursion actually visits) compared against `warmUpTreeSize`, a node count produced by a **deliberately separate** traversal (`countCommandTree`) captured in `TestMain` immediately after warm-up and before `m.Run()`. The separate-traversal + `TestMain`-timing placement surfaced a non-obvious pitfall: the global command tree **grows during the run** — `internal/cli/help_order_test.go` calls `runFang(ctx, rootCmd)`, and cobra's Execute path appends `help` + `completion` + 4 shell children (6 nodes total) via `InitDefaultHelpCmd`/`InitDefaultCompletionCmd`; because that test file sorts before `main_test.go` and runs non-parallel, it executes first. Recomputing the node count *inside* the guard test (rather than capturing it once in `TestMain`) measures the tree *after* this growth and produced a false `visited 182` vs `holds 188` FAIL against a correct implementation — a structural defect, not a tuning issue, resolved by taking both counts at the same point in time. Round-trip verified in both packages: body-gutting → deterministic FAIL (`visited 1` vs `tree 182` in `internal/cli`, vs `tree 3` in `preference`); call-removal → still FAILs on `warmUpDone`; restored → `ok`. A new stdlib-only `go/ast` inventory guard (`internal/cli/inventory_guard_test.go`) statically derives the warm-up reachable-set from `<parent>.AddCommand(<ident>)` edges plus transitive closure and fails if any `t.Parallel()` test calling `.Commands()` on a package-level global receiver falls outside it (round-trip-verified: excluding `githubCmd`'s edge reproduces the exact FAIL this guard is meant to catch). **(2) session registry flock starvation** (`internal/session/registry.go`) — the `withLock` retry loop's fixed 20ms sleep let the same losing goroutine repeatedly lose the race under contention, producing tail-latency spikes (run-phase measurement: baseline max 168.6–539.2ms across 3 runs, 3.2x run-to-run variance) that could exceed the CI `ErrLockTimeout` budget. Replaced with full-jitter exponential backoff (base 5ms, ×2, cap 50ms, `math/rand/v2`, remaining-deadline-clamped). An independent sync-audit re-measured the improvement with 8 pre-fix/post-fix binary pairs run alternately (controlling for machine drift): the direction reproduced — post-fix won 7 of 8 pairs, median max `147.6ms → 132.1ms` (≈ −10%) — but **run-to-run variance did not narrow; it widened** (pre-fix 145.7–169.1ms, 1.16x; post-fix 110.6–155.9ms, 1.41x), which is also what the mechanism predicts: full jitter *adds* randomness, so narrower spread was never a reasonable expectation. The original 3-run-vs-3-run `3.2x → 1.1x` figures (and the `539.2ms` baseline maximum) are a small-sample artifact that did not reproduce under the paired re-measurement. p50 stays effectively unchanged (no added latency on the uncontended path) and the `ErrLockTimeout`/`errors.Is` contract, the unchanged `LockTimeout = 2 * time.Second` default, and the `unix.LOCK_EX|LOCK_NB` flock flags are all verified byte-for-byte unchanged. A new characterization test (`internal/session/registry_starvation_test.go`) records p50/p95/max under contention (asserting only a max-ceiling, never p50/p95, to avoid introducing a second flaky test) and is `-short`-skipped. Full `go test ./...` exit 0 (105 packages); `go vet ./...` clean; `golangci-lint run` 0 new issues (baseline was already 0); cross-platform builds (`darwin_arm64`/`windows_amd64`/`linux_amd64`) all exit 0; coverage delta `internal/session` +0.5pp, `internal/cli`/`internal/cli/preference` unchanged (only `_test.go` files added). The two temporary real-global repro tests used to prove the RED state (M1) are not present in the final diff — their full source and the observed race output are preserved verbatim in `progress.md` §E.2. **Transparent debt (sync-audit F3):** the inventory guard's `warmUpRoots` map is fixed to two directories (`.` and `preference`); other `internal/cli` subpackages (`worktree`, `harness`, `agentlint`) also hold package-level cobra globals but currently have **0 violations** (verified repo-wide — no test file in those packages combines `t.Parallel()` with `.Commands()` today), so the two roots exactly cover today's risk surface — a future parallel `Commands()` test added to an uncovered subpackage would go undetected until the map is updated. **Residual limitation:** `countCommandTree`'s placement in `TestMain` (rather than inside the guard test body) is protected only by a source comment (`DO NOT "simplify" this back into the guard test body`), not by a mechanical check — a "simplifying" refactor that moves the count back into the guard would reintroduce the `182` vs `188` false positive documented above, and a red guard naturally reads as "the guard is wrong," which invites deleting the depth assertion and silently reopening the gap the two-signal design (F1) was built to close. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CONFIG-AUDIT-REPAIR-001](.moai/specs/SPEC-CONFIG-AUDIT-REPAIR-001/spec.md)** — repaired every finding of the `.moai/config` full-tree audit (`.moai/reports/moai-config-audit-20260725.md`, 19 findings: HIGH 4 / MEDIUM 7 incl. M7 / LOW 8) in one Tier L effort (21/21 AC PASS — 20 clean + 1 PASS-WITH-DEBT). **(1) ast-grep gate RESTORE, default OFF** (`internal/config/loader_gate.go`, REQ-CAR-011/019/020/021) — a `gate.yaml` config loader makes `AstGrepGate.Enabled` configurable (previously unreachable dead config), with the default-OFF invariant explicitly proven (`NewDefaultGateConfig` + a characterization test); the phantom `utils` ruleDir was removed from `sgconfig.yml` and the go/security rule sets were converted to sg-native `rule:` nested format so config-mode scanning is actually loadable, and the enabled gate degrades gracefully when the `sg` binary is absent; the dead V1 `RunAstGrepGate` code path was deleted, leaving V2 as the sole gate entry. **(2) Distribution-status codification** (REQ-CAR-013/014) — `tool-policy.yaml` is declared dev-only (CLAUDE.local.md §2) with the CLI hard error on a missing file replaced by a graceful one-line notice (`internal/cli/tool_policy.go`); the dangling `mcp-matrix.yaml` template-skill reference was removed/reworded. **(3) M7 test repair + registry reconciliation** (REQ-CAR-009/010, `internal/config/audit_test.go`) — `TestAuditParity` silently `SKIP`ped under plain `go test` because it resolved `.moai/config/sections` relative to the package cwd; it now resolves the repo root via `runtime.Caller` (same pattern as the sibling completeness test) and always PASSes or FAILs. The repaired test exposed 7 previously-unguarded orphan config sections; each was reconciled (`feedback` registered to its existing loader, `runtime` removed as a non-`sections/` struct, the remaining 6 registered as documented exceptions). **(4) Mechanical doc/rule sweep** (REQ-CAR-001..008, 10+ files across `.claude/rules` + `.claude/skills`, both trees) — removed retired `team.role_profiles` references; replaced every doc reference to the nonexistent `.moai/config/config.yaml` with the sections-SSOT wording (`.moai/config/sections/*.yaml`), including the `harness.md` project-initialized abort gate which previously always failed; corrected 3 stale `github.git_workflow` reads to the real key `spec_git_workflow`; corrected a nonexistent `design.yaml adaptation.phase_weights` citation and 5 bare `quality.yaml development_mode` path citations to `constitution.development_mode`; fixed a stale test comment, a mislabeled loader-registry entry, a retired `haiku` reference, a scalar/map description mismatch, and a dead config leaf. All template-mirrored corrections were applied template-first (`make build` clean, template neutrality/leak guards green). **Debt (transparent)**: **AC-CAR-020(b) PASS-WITH-DEBT** — config-mode scanning loads the curated go/security rule set (functionally equivalent, sg-native format) rather than the literal unconverted root `go-hardcoding.yml`; 2 sg-unparseable multi-node demo rules were dropped (deferred to the backlog `SPEC-ASTGREP-DOGFOOD-CLEANUP-001`, which continues to own 16-language ruleset productization, the 10/17 empty language stubs, and message-language unification). The M1 `db.yaml` dead-key cleanup is intentionally NOT implemented here — it is scoped to the parallel DB-subsystem-removal track per REQ-CAR-015, with a delete-3-keys fallback armed if that track stalls. Full `go test ./...` exit 0 (107 packages); `GOOS=windows GOARCH=amd64 go build ./...` exit 0; `golangci-lint run` 0 issues. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

### Changed

- **[SPEC-GOAL-DOCS-RETIRE-001](.moai/specs/SPEC-GOAL-DOCS-RETIRE-001/spec.md)** — amendment v1.5.0 (D2 aggregate liveness/aptness guard). AC-GDR-012 refactored to use single-p= source discipline, closing regression risk. Added liveness and aptness guards. Baseline preserved (total=24 pre-sweep, total=0 post-sweep). Tier S. Single sync commit carries 3-phase close. 🗿 MoAI

- **[SPEC-GOAL-DOCS-RETIRE-001](.moai/specs/SPEC-GOAL-DOCS-RETIRE-001/spec.md)** — amendment v1.6.0 (B2-2/B2-3 base-current + exemption-list hardening). AC-GDR-010 hardened with two new structural guards closing the plan-audit iteration 2 SHOULD-FIX findings (B2-2, B2-3): component (b) gains a frozen-base structural-currency assertion (heading-set equality between `e306e21a9:<scoped page>` and the working-tree page — `heading_set_match=12/12` PASS non-vacuous, both sides non-empty and the diff-logic discriminates a real mismatch); component (c) gains a mechanical exemption-list count check (structured `exempt_detectors: []` declaration replacing prose — `exempt_detectors_decl=1/1 entries=0/0 coexist_siblings=0/0` PASS non-vacuous, the `entries` regex discriminates 0-vs-1 and the coexist-guard regex correctly excludes the `exempt_detectors:` header line itself). Component (a) symmetry, component (d) aptness, and AC-GDR-012 (the v1.5.0 single-`p=`-source aggregate guard) are untouched — `total=0` preserved. The recorded baseline (`distinct=1,live_min>=1,apt=1` across all 5 detectors; `paired_se:live_min=2`; exemption list empty today) is unaffected and preserved verbatim — this is future-risk hardening, NOT a current-defect fix (`feedback_hypothesis_as_defect` applied). Run-phase scope: `acceptance.md` only (commit `d0dc7b530`, M1'); plan-audit verdict PASS 0.86 (re-audit). Tier S. Single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-AGENT-PARALLEL-OPT-001](.moai/specs/SPEC-AGENT-PARALLEL-OPT-001/spec.md)** — agent instruction diet + plan/run/sync parallelization maximization across the 10-agent catalog (Tier L, 5-file plan set, 54/56 AC PASS — 2 PASS-WITH-DEBT — sync-audit harmonic mean 0.908 against the Tier L threshold 0.85). **(1) Three orphaned dynamic-workflow fan-out scripts wired in** (`plan-research-fanout.js`, `sync-audit-4dim.js`, `codemaps-extract.js`) — added to the plan / run / sync / codemaps workflow docs behind a two-condition capability gate (script exists on disk AND runtime supports dynamic workflows), with an explicit no-error / no-warning fallback to the pre-existing serial path when either condition fails. All three scripts are shipped to `internal/template/templates/.claude/workflows/` so deployed users receive them, and the internal-content leak scanner is extended to scan `.js` files (`internal/template/internal_content_leak_test.go`, `leakTextExtensions[".js"] = true`) so the newly-shipped scripts fall under the neutrality guard rather than passing vacuously. **(2) Plan / run / sync quality stages restructured to read-only parallel evidence plus a single writer** — `sync/doc-execution.md` gains a 5-row D1-D5 drafter table with one applier, and run Phases 13/16/17 share one evidence-gathering pass while each phase keeps its own iteration ceiling verbatim; binding PASS/FAIL verdict ownership stays with `plan-auditor` / `sync-auditor` (the disjoint-writer parallel-write variant was explicitly rejected in `spec.md`). **(3) Ten core agent bodies cut from 2,417 to 1,974 lines** (−443, −18.3% vs the plan-phase `origin/main` baseline; −238, −10.8% vs the immediately-prior branch baseline), replacing in-body restatement with SSOT cross-references (the 12-field frontmatter schema → `spec-frontmatter-schema.md`, the GEARS/EARS pattern table → `moai-workflow-spec`, Tier PASS thresholds → `spec-workflow.md`, the DDD/TDD dual-workflow description → a single unified Implementation Cycle in `manager-develop.md`); every one of the 10 files satisfies its per-file line ceiling (`spec.md` §D.2), and parallel-batching cross-reference coverage across the catalog rises from 1 of 10 agent bodies to 10 of 10. **(4) Non-host-OS E2E recipes extracted** out of the `e2e-tester` agent body into an on-demand skill reference (`.claude/skills/moai-workflow-testing/references/e2e-desktop-native-recipes.md`), mirrored 0-diff into the template tree. **(5) `dynamic-workflows.md` corrected** — it previously asserted MoAI ships no saved workflows by default and described `.claude/workflows/` as universally not template-managed, both now false once the three scripts shipped; the doc now distinguishes MoAI-shipped template-managed generic fan-out scripts (overwritten by `moai update`) from user-owned `hns-*` / `harness-*` Runner Workflows (preserved). This SPEC carries `partially_supersedes: [SPEC-DWF-CODEMAPS-PILOT-001]`, citing `AC-DCP-010` / `REQ-DCP-009` / `REQ-DCP-010` by ID. Frontmatter invariant held across the SPEC's whole commit range: a diff of the 10 agent files (local + template mirror) for any `effort|model|tools|color|name|description|permissionMode|memory|hooks|skills:` line produces zero output. No Go production code changed (explicit Out of Scope); `go test ./...` exit 0 across 107 packages, `make build` exit 0 leaving the tree clean (committed `catalog.yaml` byte-identical to generator output), `golangci-lint run` 0 issues. **Debt (transparent)**: **AC-APO-072 / AC-APO-072b PASS-WITH-DEBT** — the primary neutrality-guard and namespace-guard conditions are verified mechanically (`".js": true` present + both guard tests green), but each AC's RED/GREEN round-trip / blocking-validity sub-clause (plant a non-neutral or misnamed file into the template tree and confirm the guard fails, then remove it and confirm the guard passes) requires deliberately mutating the template tree and was not re-executed by the independent sync audit; the run-phase record covers it. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CLI-WIZARD-RESTRUCTURE-001](.moai/specs/SPEC-CLI-WIZARD-RESTRUCTURE-001/spec.md)** — restructured `moai init`'s wizard from a retired Quick/Standard/Advanced 3-mode gate into a single always-shown 3-page flow, recalibrated the model-policy default, and repaired the Page-3 answer→disk persistence chain (Tier L, 19/19 AC PASS, 0 FAIL). **(1) Gate removal + 3-page restructure** (`internal/cli/wizard/questions.go`, REQ-WIZ-001..006) — the former `--standard`/`--advanced` mode gate is gone; `InitQuestions()` now assembles a fixed Page 1 "Basic" (conversation_language, user_name, project_name) + Page 2 "Model & Report" (model_policy, report_format) + Page 3 "Quality & Workflow" (lsp_enabled, enforce_quality, project_mode, design_enabled, claude_design_enabled) shown to every user unconditionally; `claude_design_enabled` remains the sole nested/conditional question (on `design_enabled`). `GitQuestions()` (7 questions) and the 3-way `DefaultQuestions`/`Page3Questions`/`InitQuestions` split are preserved so `moai update --reconfigure` keeps its pre-restructure Git question set unaffected. **(2) Default recalibration to Medium** (`internal/template/model_policy.go`, REQ-WIZ-008/009) — `DefaultModelPolicy` moves from `high` ("Max") to `medium`; `high` remains a fully selectable tier, only the default moved. `lsp_enabled` also flips its default to `true` (opt-out, REQ-WIZ-010), and both the `--model-policy`/`--profile` CLI flags and the reconfigure path continue to take precedence over the wizard answer (AC-WIZ-016, C30/C41 flag-vs-wizard precedence order unchanged). **(3) Page-3 persistence chain repair** (`internal/core/project/initializer_expansion.go`, REQ-WIZ-015/021/022) — Page-3 wizard answers (lsp.enabled, quality.constitution.enforce_quality, project.mode, design.enabled, design.claude_design.enabled) previously were computed but never reliably reached the deployed `.moai/config/sections/*.yaml` files on the deployer path; a new depth-aware `patchYAMLPathValue` line-walking helper patches an existing multi-KB deployed YAML file at an exact `a.b.c` key path in place (preserving every other line, including a second same-named `enabled:` key nested elsewhere in `lsp.yaml`) rather than overwriting the file, and the patch is proven non-destructive (AC-WIZ-010a) against the ~22 KB deployed config set. **(4) Advanced-path full retirement** (REQ-WIZ-018/019) — the dormant Phase-2/advanced-bridge code path (`--advanced` flag, `advanced_bridge` question, `coverage_exemptions_enabled` question, the `harness_profile` wizard question) is removed from `internal/cli/**` and `internal/core/project/**`; `harness_profile` is no longer a wizard question; the pre-existing `--harness-profile` CLI flag is still accepted and parsed, but it currently has **no persisted effect** — its only writer (`writeHarnessProfileYAML`) lost its last production caller in this SPEC (C36, deliberately, so `harness.yaml` is left byte-identical per AC-WIZ-010a) and survives as declared dead code (debt (a) below). **(5) Test reconciliation** (M7) — `TestInit_QualityYAMLContent` and 4 test files' removal-tombstone comments were updated to match the new default/removal set. **19/19 AC PASS** (0 FAIL, 0 PASS-WITH-DEBT at the AC level); coverage `internal/cli/wizard` 93.9%, `internal/core/project` 88.7%, `internal/template` 86.0% (the last figure re-measured on the post-merge tree; run-phase §E.3 recorded 84.9% before the base merge, and each figure is exactly EQUAL to the package's baseline at its own tree point — unchanged, not improved); full `go test ./...` exits 0 (107 packages at run-phase; 105 after the sync-phase merge of `origin/main`, which removed the DB and prompt-cache subsystems); `GOOS=windows GOARCH=amd64 go build ./...` exits 0; `golangci-lint run` 0 issues. **This sync phase additionally rewrote the 4-locale docs-site wizard documentation** (`docs-site/content/{en,ko,ja,zh}/{getting-started/init-wizard.md, getting-started/cli.md, cli-reference/init.md}`, 12 files) to describe the shipped 3-page flow in place of the retired 3-mode narrative, removing every `--standard`/`--advanced` reference (baseline 44 matches across 12 files → 0); `hugo --gc --minify` builds warning-free with 4-locale section/table-row parity confirmed. **Debt (transparent, inherited from run-phase §E.3.1, all zero AC impact)**: (a) `writeHarnessProfileYAML` and its 2 tests remain as dead code pending a plan.md §G amendment authorizing their deletion; (b) `patchYAMLPathValue` is a line-walker, not a full YAML parser — block-scalar (`|`/`>`) values that happen to resemble a matching key path are an untested edge case, with a real YAML round-trip parser recorded as the documented future fix; (c) an absent target key in an existing deployed file is a silent no-op by design (protects hand-edited config) but means a user who deletes `lsp.enabled` from `lsp.yaml` silently stops receiving the wizard answer; (d) `TestInit_QualityYAMLContent` behavior changed for direct-API callers that leave `EnforceQuality` unset (now `false` instead of the old template default `true`) — no production path is exposed to this, since `init.go` always sets the flag explicitly; (e) 4 test files retain removal-tombstone comments naming the now-nonexistent `advanced_bridge`/`harness_profile`/`coverage_exemptions_enabled` literals for audit-trail purposes. **Sync-phase audit (independent, 4-dimension) returned FAIL 0.745 against the Tier L 0.85 threshold — score-driven, with no must-pass criterion failing.** Four documentation/metadata findings were fixed in-place before close: the `sync_commit_sha` marker was bold-and-backtick-wrapped and therefore unreadable by `internal/spec/era.go`, which had misclassified this SPEC as `era: V3R5` and would have exempted it from lifecycle drift detection permanently (now `H-4` / `V3R6`, verified via `moai spec audit --json`); `design.md`/`research.md` were left at `status: in-progress`; the `internal/template` coverage figure was stale; and the `--harness-profile` CHANGELOG sentence overstated the flag's effect. One security finding was also fixed: `--project-mode` reached the project-mode YAML writer unvalidated — an exposure this SPEC introduced, since C32 is what made that write path reachable from `moai init` — so `validateInitFlags` now enforces the `personal`/`team` enum like every sibling flag, with an injection-reproduction test.

**Independent re-audit returned FAIL 0.834** (Functionality 0.88 / Security 0.82 / Craft 0.84 / Consistency 0.80 — harmonic mean, still below the 0.85 Tier L bar) and surfaced a second cluster of findings the first round's fixes had not reached: the docs-site pages this SPEC's own sync phase rewrote still taught the inert `--harness-profile` flag in the non-interactive example, `init.go`'s Page-3 override comment was stale, the `enterprise` project-mode value the S1 validator fix (`0e34ac7a3`) newly rejected was still documented in the deployed `project.yaml.tmpl` template, the S2 debt row misstated `internal/atomicfile.Replace` as unused, the coverage sentence claimed "above baseline" when the run-phase and post-merge figures were in fact identical, the cited sync-audit report path was untracked, and three live findings (a stale `init.go:382` comment, the docs-site LSP-default contradiction, and an unbounded `os.ReadFile` in the patch path) were absent from this section's resolution tables.

This repair pass closes that second cluster. **(6) `--enable-lsp` non-interactive default now matches the wizard** — `internal/cli/init.go`'s `LSPEnabled` field reads `getBoolFlagWithDefault(cmd, "enable-lsp", true)` instead of the bare `getBoolFlag`, so `moai init --non-interactive` (no flags) now persists `lsp.enabled: true`, matching the wizard's interactive default; an explicit `--enable-lsp=false` still overrides it unchanged. This closes the `--enable-lsp` non-interactive-default finding, which the previous revision of this entry listed as deferred debt (f); that debt item is removed rather than renumbered, so no `(f)` remains in the list below. As the same one-line fix, it also restores the truth of the `init.go` Page-3 comment and removes the docs-site contradiction between `init-wizard.md`'s "enabled (Yes)" and `cli.md`/`cli-reference/init.md`'s "(default: false)" (both now read "(default: true)"). **(7) docs-site `--harness-profile` disclosure** — the non-interactive CI example in all 4 locales of `init-wizard.md` no longer includes the inert `--harness-profile default \` line, and the 8 flag-table rows across `cli.md`/`cli-reference/init.md` now state the flag is accepted but currently has no persisted effect, matching the CHANGELOG (debt (a), listed in the debt paragraph above). **(8) `project.yaml.tmpl` regression fix** — the `personal`/`team`/`enterprise` project-mode enum documented in the deployed config template regressed with the S1 validator (`0e34ac7a3`, which rejects `enterprise`, a value the template still documented and the CLI previously accepted); `580d49959` drops `enterprise` from the template's own comments so the documented enum matches the validator, since no code path consumes that value. **(9) Audit-record corrections** (this bullet + `progress.md` §G) — the S2 debt claim now states `internal/atomicfile.Replace` has 10 production call sites across 7 files (`internal/cli/preference/filestore.go`, `internal/harness/tier/tier.go`, `internal/migration/version.go`, `internal/hook/handoff/persist.go`, `internal/goal/state.go`, `internal/session/registry.go`, `internal/session/store.go` ×4) rather than "unused" — it is the *config writers* in `initializer_expansion.go` that do not route through it; the coverage sentence now reads "unchanged from the package's own baseline" (both the run-phase 84.9% and the post-merge 86.0% figures were measured at exactly their respective baselines, not above them); and the two sync-audit reports (round 1 + re-audit) are committed to `.moai/reports/sync-audit/` so the `progress.md` §G citation resolves to a real path (siblings in that directory are already tracked; it is not gitignored).

**Remaining debt (transparent, all minor, zero AC impact)**: (g) the config writers use non-atomic `os.WriteFile` on the ~22 KB deployed config set — a crash mid-write could truncate a config file that AC-WIZ-010a otherwise protects; this is deferred and currently unscheduled (no follow-up SPEC exists yet — the debt is not routed to one until it is scoped). (h) `initializer_expansion.go`'s `os.ReadFile` of the patch target is unbounded, bounded in practice by deployed-template size, with no explicit limit. (i) `writeProjectModeYAML`/`patchYAMLKey` interpolate their value raw — the injection invariant currently lives in the two callers (the now-validated `--project-mode` flag and the wizard's constrained select), not in the writer itself; there is no live injection path today, but restoring `--harness-profile`'s writer (debt (a)) without also validating it would reopen the identical hole `validateInitFlags` closed for `--project-mode`. Route B (PR-mandatory, this repo's branch-protection override); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-SUBAGENT-NESTING-DOCTRINE-001](.moai/specs/SPEC-SUBAGENT-NESTING-DOCTRINE-001/spec.md)** — corrected the subagent-nesting doctrine across the harness to the Claude Code v2.1.217 reality and added an opt-in read-only nesting pilot to `sync-auditor` (Tier M, 16/16 AC — 15 clean + 1 PASS-WITH-DEBT). **(1) Doctrine correction** (REQ-SND-002..011, 7 surfaces: `CLAUDE.md` §4/§14, `agent-authoring.md` §Agent(agent_type) Restrictions/§Fork Subagents/§Tool Permissions, `agent-patterns.md` §Deprecated, `orchestration-mode-selection.md` §Mode 6) — the prior doctrine stated nesting was gated solely by the `Agent` tool being present in a subagent's `tools` list; this understated the actual v2.1.217 double guarantee — nesting requires BOTH the `Agent` tool AND `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` > 0, and the runtime default is OFF since v2.1.217, with concurrency caps of 20 concurrent / 200 per-session when enabled. All 7 surfaces and their `internal/template/templates/` mirrors were corrected (Template-First), verified stale-phrasing-gone AND new-facts-present (delta, not presence-only). **(2) sync-auditor read-only nesting pilot** (REQ-SND-013/014/017/019/021, `.claude/agents/moai/sync-auditor.md` + template mirror) — `sync-auditor` gains the `Agent` tool and an opt-in per-dimension child-verifier pattern (Functionality/Security/Craft/Consistency), gated entirely by the `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` env var (absent from the distributed `settings.json.tmpl` — shipped default stays flat, single-level); spawned children are read-only verifiers and never own the binding PASS/FAIL verdict (verdict ownership stays with `sync-auditor` itself), and the other 10 retained agents carry no `Agent` in `tools` (unaffected). **Debt (transparent)**: **AC-SND-016 PASS-WITH-DEBT** — a literal whole-tree grep for the env-var name across `internal/template/templates/` returns 8 (the token is required in doctrine-mirror prose by AC-SND-001/002/003), which is incompatible with a strict "0 occurrences anywhere" reading of REQ-SND-019; the requirement's actual load-bearing invariant — the env var is absent from the distributed `settings.json.tmpl` so the shipped default remains flat — is fully satisfied (0 occurrences there). `make build` regenerated `catalog.yaml` (agent-hash cascade only); `go test ./internal/template/...` exit 0 (mirror-parity + neutrality guards green); no Go production code touched. 🗿 MoAI

- **[SPEC-CLI-TUX-INIT-UPDATE-001](.moai/specs/SPEC-CLI-TUX-INIT-UPDATE-001/spec.md)** — modernized `moai init`/`moai update` terminal presentation by connecting the already-existing `internal/tui` design-system primitives (Box/Pill/StatusIcon/Progress) that both commands under-used, and restored the large 6-line "MoAI-ADK" ASCII-art logo with a vertical coral gradient (Tier M, presentation-only — no data or behaviour change, `go.mod` unchanged). **(1) Glyph SSOT** (`internal/tui` new `Glyph*` const block, REQ-TUXIU-001..004) — the check/dot/circle/cross status glyphs, previously redeclared as raw-rune literals in 4+ sites (`printer.go`, `uikit/styles.go`, `uikit/status.go`, `uikit/render.go`), now resolve from one exported `tui` source; zero raw-rune status-glyph literals remain outside it. **(2) `moai update` card-style presentation** (`internal/cli/update_tux.go`, REQ-TUXIU-010..016) — the merge-classification summary renders as an accent-bordered `tui.Box` with "+N add"/"~N update"/"!N conflict" pills (zero-count pills omitted), deploy steps use the unified glyph set with a `tui.Progress` block bar, and the identity header/outcome banner render as pills. **(3) Spinner-residue fix** (REQ-TUXIU-020..022) — removed a redundant reporter Step-wrapper that double-rendered each deploy step on stderr alongside the stdout progress line, eliminating the stale duplicated two-part spinner residue left after a step completes. **(4) `moai init` alignment** (REQ-TUXIU-030/031) — the init banner and success card (`buildInitSuccessCard`) adopt the same `tui.Box`/`tui.Pill` visual language as `update`. **(5) Restored logo** (`internal/tui/logo.go`, new `tui.Logo`/`tui.CoralRamp`, REQ-TUXIU-050..056) — the 6-line art retired by SPEC-CLI-TUX-V3-004 (commit `77893579e`) is restored byte-identical, with a per-row top-light-to-bottom-deep coral gradient (`lipgloss.Blend1D` over the existing `Theme.Accent`/`Theme.AccentDeep` tokens — zero new dependency), stacked above the retained compact identity band on all 3 `uikit.PrintBanner` surfaces (root no-args, `init`, `update`), plus a root-help predicate (`internal/cli/fang.go` `isRootHelpArgs`) that prints the logo before `fang.Execute` on explicit `moai --help`/`-h`/`help` (excluding the empty arg vector and all subcommand-help shapes, so no-args prints the logo exactly once). The logo's decorative block/box-drawing runes are carved out of the pre-existing status-glyph whitelist (REQ-TUXIU-056) as a separate decorative category. All colour decisions stay inside the `internal/tui` Theme SSOT (zero new hex literals); NO_COLOR and non-TTY degrade cleanly. 24/24 gating AC PASS (AC-TUXIU-001..024, 1 N/A — no JSON output surface to preserve); coverage: new/touched render functions 100% (one at 91.7%, binary-exercised), `internal/cli` 74.9% (>= 74.6% baseline, no regression), `internal/tui` 93.6% (>= 93.0% baseline); full `go test ./...` + `GOOS=windows GOARCH=amd64 go build ./...` exit 0; `golangci-lint` 0 new issues; `git diff -- go.mod go.sum` empty. PR-mandatory regime; single sync commit carries the `in-progress -> completed` close. 🗿 MoAI

- **[SPEC-TEMPLATE-DATE-NEUTRALITY-001](.moai/specs/SPEC-TEMPLATE-DATE-NEUTRALITY-001/spec.md)** — remediated the strict-tier internal-date leak across `internal/template/templates/` and replaced the report-cap silent-truncation hazard with a named constant plus a full-listing escape path (Tier L, 7 milestones, 23/23 AC PASS, 0 FAIL). **(1) 180-row occurrence-class inventory** (`triage.tsv`, M2) — every `(file, date, line_shape, line_no)` tuple under the S1-internal-date strict tier was catalogued and adjudicated: 92 REMOVE (80 DC-2a standalone/prose authoring-date stamps + 12 dual-shape DC-5 rows) and 88 PRESERVE (48 DC-1 frontmatter `updated:` fields, 13 DC-3 deadline references, 11 DC-2b mirror-capture rows, 6 DC-4 whole-file attribution records, 10 adjudicated DC-5 rows). **(2) M3 remediation** — the 92 REMOVE-dispositioned occurrences were deleted across 67 template files with no placeholder token substituted, driving the strict-tier finding count from 135 → 78. **(3) M4 hybrid carve-out** (`internal/template/internal_content_leak_test.go`) — a structural gate for the mechanically-decidable recurring shapes (DC-1 `updated:` frontmatter lines, DC-4 whole-file attribution) plus a 27-entry content-anchored allowlist for the remaining judgement-call categories drove the strict tier from 78 → 0. **(4) Report-cap fix** (M5) — the previous unnamed truncation magic number is now a named constant, and the full (untruncated) finding listing is written to a discoverable path rather than silently dropped past the cap. **(5) CI enforcement** (M6) — `.github/workflows/template-neutrality-check.yaml` gained a strict-tier step (`MOAI_TEMPLATE_LEAK_STRICT=1 go test -run TestTemplateNoInternalContentLeak ./internal/template/`) so the 0-finding state is enforced on every future template-path change, not just locally. Full `go test -count=1 ./...` exit 0 (105 packages); `golangci-lint run` 0 issues; `go vet ./...` exit 0; cross-platform build (darwin/amd64 native + `GOOS=windows GOARCH=amd64`) exit 0. **Debt (transparent, deferred, zero AC impact)**: 41 `Last Updated: 2025-*` stamps across 29 template files are semantically the same DC-2a authoring-stamp pattern as the 80 remediated here, but the strict-tier guard's regex matches `202[6-9]` only, so these carry no triage row and no acceptance criterion — deferred to a follow-up SPEC that should both remediate the remaining 2025 stamps and widen the guard's year class to `202[5-9]`. No docs-site or README update — this SPEC is an internal CI-guard and template-hygiene change with no user-visible feature, CLI flag, or API surface. PR-mandatory regime (branch protection `enforce_admins: true`); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

## [3.0.1] - 2026-07-21

### Added

- **init wizard: language, user name, and advanced-bridge steps** (`a408c80cf`, #1111) — `moai init` gains a `conversation_language` select (en/ko/ja/zh) with live locale re-render of subsequent wizard questions, a `user_name` input pre-filled from the existing profile (wizard answer wins over profile for `language.yaml`/`user.yaml` persistence), and a final advanced-settings bridge confirm that reveals the existing Phase 1 questions without requiring the `--standard`/`--advanced` flag.

- **[SPEC-DESIGN-MOAIWEBV2-002](.moai/specs/SPEC-DESIGN-MOAIWEBV2-002/spec.md)** — moai web console aligned to the docs-site v2 design system (Tier M, AC-MWA-001..014 +003a/003b PASS; plan-audit iter-1 PASS-WITH-DEBT 0.86 with D1-D7 folded in; sync-audit delta resolved). **(1) Dark-theme retirement** (`internal/web/`, REQ-MWA-001..004) — all `[data-theme]` CSS overrides, the themeToggle UI, sun/moon icons, the FOUC theme branch, and the 4-locale `theme.aria` i18n keys are removed; the console is light-only, matching docs-site policy. The FOUC **language** branch (`moai-console-lang` → `<html lang>`, CJK font activation) is preserved verbatim and machine-guarded (AC-MWA-003b); `board.templ`'s server-rendered `data-theme` attribute is removed with `lang="en"` kept. `TestDarkModeAndThemeToggle` is inverted to `TestDarkThemeAbsence` (refutation-proven non-vacuous). **(2) Status tokens byte-equal to docs-site** (REQ-MWA-005..007) — success `#5db872` / warning `#d4a017` / danger `#c64545` / info `#5db8a6` adopted at token level; WCAG AA text failures are compensated at usage sites via `--status-text-*` `color-mix` carve-outs toward `--color-ink` (measured 5.01–5.68:1 post-fix), never by mutating the FROZEN token bytes. **(3) Goorm Sans Code self-host** (REQ-MWA-008..011) — OFL-verified woff2 subset committed with `OFL-GoormSansCode.txt`, relative `/static/fonts/` `@font-face`, code/mono family unified with docs-site; the offline invariant holds (`grep -c 'http' console.css` = 0). **(4) Tone/layout alignment** (REQ-MWA-012..013) — compact hero band with mascot (docs-site `.doc-hero--compact` direction), card/spacing rhythm on the v2 neutral canvas; zero HTMX/server-contract change (handlers/server untouched, filter-diff clean). docs-site itself is byte-unchanged (verification-only baseline, AC-MWA-007a). Supersession bookkeeping: SPEC-WEB-CONSOLE-004 REQ-WC4-006/AC-WC4-006 (dark mode) is partially superseded by this SPEC. Coverage debt: `internal/web` 59.4% (baseline 59.5% — pre-existing, not a regression). Full `go test ./...` exit 0; `go vet` + `golangci-lint` clean. PR-mandatory regime; single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-MODEL-PROFILE-MATRIX-001](.moai/specs/SPEC-MODEL-PROFILE-MATRIX-001/spec.md)** — replaced the two-axis `plan_type × performance_tier` model-routing design with a single per-agent-group `profile` matrix (`max`/`medium`/`low`), moving consumption from shipped-agent frontmatter mutation to runtime-arg spawn injection (Tier L, 5-file plan set, 23/25 AC PASS — 21 clean + 2 PASS-WITH-DEBT — all 7 must-pass AC PASS). **(1) Config schema** (`internal/config/profile.go`, REQ-MPM-001..008) — `llm.profile` closed-set enum (`max`/`medium`/`low`), `EffectiveProfile()` resolving `llm.profile` → legacy `performance_tier` read-time alias (`high`→`max`) → default `medium` (DECISION-002), and `llm.agent_overrides` (`{model, effort}` per agent) with closed-set validation. **(2) Matrix A Go SSOT** (`internal/template/profile_matrix.go`) — the user-confirmed 6-group × 3-profile matrix (spec/auditors, develop, advisor, design/harness/e2e, docs, git) plus `ResolveAgentModelEffort` (override → profile-cell → default precedence) and the `Explore`/unmatched-agent `inherit` sentinel. **(3) `moai model profile [--json]` resolver** (`internal/cli/model.go`, REQ-MPM-025) — a new read-only CLI accessor: MODEL is emitted as the `[1m]`-safe per-spawn runtime arg (the super-advisor pattern, DECISION-001); EFFORT is emitted as documented intent (frontmatter default / GLM overlay / Workflow-prompt steering) since the Agent tool does not accept a per-spawn effort override for named subagents. Applies the GLM overlay (model-alias remap via `llm.glm.models` + `CollapseClaudeEffortToGLM`) when the session backend is GLM. **(4) Selection surfaces** — `moai init --profile <max|medium|low>` flag (persists `llm.profile`, `--plan-type` retired) and the `moai web` Model Policy console profile selector (persists `llm.profile`; the `plan_type` UI surface and read-only display are removed). **(5) `ApplyTierProfile` retirement** — the 66-cell `tierProfiles` map and all 4 production frontmatter-mutation call sites (`initializer.go`, `update.go` ×2, `web/agentfm.go`'s `applyPerfTierEdits`) are retired; the 8 shipped `.claude/agents/moai/*.md` files are restored to lint-canonical `model: inherit` + doc-canonical effort. **(6) Migration** — a legacy `llm.yaml` (`plan_type` + `claude_models` + `performance_tier`) loads without error and resolves to the correct profile; write-time (`template.ApplyProfile`) strips the retired `plan_type`/`claude_models` keys on the next save. **Debt (transparent)**: **AC-MPM-013/014 PASS-WITH-DEBT** — the config-layer `agent_overrides` support and the `moai model profile` matrix render are in place, but the pre-existing `moai web` agentfm surface (SPEC-WEB-CONSOLE-011) still renders/edits per-agent frontmatter rather than the profile matrix — UI-wiring only, neither AC is must-pass. `go build ./...` + `GOOS=windows GOARCH=amd64 go build ./...` exit 0; full `go test ./...` 0 failures; golangci-lint 0 new issues; `HaikuResidualRule` and all existing config CI guards green. Documentation-impact follow-up (README 4-locale + docs-site 11-page × 4-locale + `--plan-type`→`--profile` CLI-reference updates, tracked in progress.md §E.2 per REQ-MPM-036) landed in this release (`f1a692971`). Run-phase Route A main-direct (Hybrid Trunk, no PR); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CLI-TUX-V3-004](.moai/specs/SPEC-CLI-TUX-V3-004/spec.md)** — `doctor`/`status`/`spec view` surface polish + fmt.Print ratchet series-final gate (CLI TUX modernization M4, Tier M, 14/14 AC PASS — 13 clean + 1 PASS-WITH-DEBT). **(1) glamour markdown rendering** (`internal/cli/glamour_style.go`, REQ-TUX4-004/005) — `github.com/charmbracelet/glamour@v1.0.0` introduced as the shared render gateway (`renderMarkdown`/`renderTreeMarkdown`/`glamourRender`/`markdownRichEnabled`), styled entirely from `tui.Theme` tokens (zero new hex literals); rich glamour render fires only on a character-device TTY with `NO_COLOR` unset, all other destinations (pipes, CI, golden-test buffers) get byte-stable plain markdown passthrough at a fixed 100-col word-wrap. `status.go` and `spec_view.go` both re-wired through this gateway, preserving every data field (project name, ADK version, config path, SPEC count, tree structure). **(2) doctor live progress + result tables** (`internal/cli/doctor_render.go`, REQ-TUX4-001/002/003) — a non-intrusive `checkObserver` seam (`runGroupedChecksObserved`) emits one `printer.Step` per check on stderr during `moai doctor` (TTY: in-place erase-line; non-TTY/`NO_COLOR`: plain line-per-event), followed by a per-section pass/fail result table (rich path: `charm.land/bubbles/v2/table`; plain path: aligned columns) with `N ok, N warn, N fail` counts; the observer wrapper is verdict-preserving (`checkGit`/`checkGoRuntime`/etc. signatures and pass/fail criteria are byte-unchanged, pinned by `TestDoctorLiveProgress_SeamPreservesVerdicts` and a scoped `doctor.go` diff review). **(3) compact banner** (`internal/cli/uikit/banner.go`, REQ-TUX4-006) — the large ASCII logo is retired in favor of a 2-line identity (`◆ MoAI-ADK <tagline>` + a `[vX] [go X] [claude X]` pill row), fully routed through the `printer.Printer` gateway (`grep -c 'fmt\.Print' banner.go` 12 → 0). **(4) help group frequency reorder** (`internal/cli/help_order.go`, REQ-TUX4-007) — after a recorded render-path verdict (keep the fang-rendered help surface; reviving the shadowed custom 4-group renderer or extending fang's undocumented layout API were both rejected with evidence), `reorderRootHelpCommands` re-sorts commands within each fang cobra Group (`LAUNCH COMMANDS:` / `PROJECT COMMANDS:` / `TOOLS:`) by observed-usage frequency while leaving group membership byte-unchanged. **(5) fmt.Print ratchet series-final gate** (REQ-TUX4-009, AC-TUX4-010) — the non-test `internal/cli` `fmt.Printf`/`fmt.Println`/`fmt.Print(` inventory drops from the SPEC-005 baseline of 14 to **0** on this SPEC's §E surface set (doctor/status/spec-view/banner/root), closing the ratchet series (46 → 40 → 14 → 0) that began with SPEC-CLI-TUX-V3-003. **12 golden files regenerated** (doctor/status/banner/welcome × light/dark/nocolor) with per-file rationale recorded in progress.md §E.2; cross-platform build (darwin native + `GOOS=windows`/`GOOS=linux` amd64) exit 0; `golangci-lint` 0 issues (NEW = 0); full suite `go test ./...` exit 0. **Debt (transparent)**: **AC-TUX4-010 PASS-WITH-DEBT** — 1 residual call site (`internal/cli/branch_protection.go:44`, an interactive y/N prompt) is explicitly OUT OF this SPEC's scope, owned by the deferred `SPEC-V3R6-CI-BASELINE-DRIFT-001` §D.1, and is not modified here. `internal/cli` coverage 74.5% (non-regression vs the 74.3% pre-flight baseline; both below the 90% target, so REQ-TUX4-011's built-in relief downgrades the gate to strict non-regression for `internal/cli`/`internal/template`/`internal/hook`, all three PASS on that basis). Run-phase Route A main-direct (Hybrid Trunk, no PR); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

### Changed

- **init wizard: auto-detect git mode, drop redundant Git questions** (`d74a2aaff`, #1113) — `init.go` never read 3 of the 7 Git-question answers (`GitHubToken`/`GitLabToken`/`GitLabUsername`, persisted only by `update.go`) and never checked `IsGhAuthenticated`, prompting for a token even when `gh auth` was already set up. The question set now splits into `DefaultQuestions` (init, Git group removed), `GitQuestions` (the 7 Git questions), and `ReconfigureQuestions` (both, in the original order for `moai update`). `git_strategy.mode` is now resolved from the repository instead of asked (no remote → manual, ≥1 remote → personal, provider from the origin host); explicit `--git-mode`/`--git-provider` flags still take precedence, and new team projects now need `--git-mode team` or `moai web` to opt into the team profile.

- **docs-site and README content refresh** — README restructured around a single tokenomics thesis across all four locales with regenerated infographics (`dd0a6ce72`, `e2c8ec625`); docs-site gained 6 section concept infographics × 4 locales (`ee89a19a1`) and restored 4-locale table-of-contents parity (`670d2665d`). Visual/structural content only — no functional change.

### Fixed

- **Windows portability — 71 test failures across 17 packages, 3 production defects repaired** (#1114, #1115, #1116) — a three-round sweep closed every Windows-only CI failure, clearing failures that had long been carried as non-blocking debt (`Test (windows-latest)` was pinned to `continue-on-error`). Two of the failure archetypes were genuine production defects, not test hygiene: **(1)** `projectSlug` (`internal/hook/session_end.go`) mapped `/`, `\`, and `.` to `-` but not `:`, so a Windows absolute path (`C:\Users\...`) produced an illegal path component (`C:-Users-...`) and every derived `~/.claude/projects/{slug}` `mkdir` failed, taking the whole preference/memory subsystem down on Windows; **(2)** `os.Rename` cannot atomically replace an open or existing destination on Windows (unlike POSIX `rename(2)`), breaking the write-temp-then-rename idiom duplicated across 9 unshared helpers — a new `internal/atomicfile` package provides a build-tagged Windows-safe `Replace` (plain `os.Rename` passthrough on non-Windows), migrated at 6 call sites (harness/tier, session/registry, migration/version, cli/preference/filestore, hook/handoff/persist, goal/state); **(3)** handoff consume relied on `os.Rename` failing for the losing side to enforce single-winner claim semantics — a guarantee POSIX provides and Windows does not — so two concurrent sessions could both consume the same pending handoff record, fixed with an explicit `O_CREATE|O_EXCL` claim gate (`atomicfile.Claim`) plus an in-gate re-check. An earlier round (`d4af3fc5e`) fixed a related rename-retry (`writeFileAtomic` sharing-violation) and a port-probe `EADDRINUSE` matching gap that fed into the same effort. The release gate no longer tolerates a Windows regression: `release-pr-multi-os.yml` drops the `continue-on-error` pin previously applied to the windows leg, so every OS now blocks the release. Verified on darwin: `go test ./...` exit 0 (107 packages), `-race` clean on `internal/hook`/`internal/cli`, native and `GOOS=windows GOARCH=amd64` builds exit 0 — Windows runtime behavior itself remains CI-only verified. 🗿 MoAI

- **[SPEC-HOOK-FAILURE-CLASSIFY-001](.moai/specs/SPEC-HOOK-FAILURE-CLASSIFY-001/spec.md)** — `PostToolUseFailure` handler now classifies nested `tool_response` errors and resolves `session_id` from `transcript_path` when absent (fixes #1089; Tier S, 8/8 AC PASS). **(1) Nested-error classification** (`internal/hook/post_tool_failure.go`, REQ-HFC-001/002/003) — `classifyError` previously read only the top-level `input.Error` field; it now also inspects nested `ToolResponse` payloads (`error` / `stderr` keys) so a `PermissionDenied`/`TimeoutError`/etc. category is still assigned when the failure signal is nested one level deeper. The pre-existing top-level-`Error` classification path and the ordered category matcher are unchanged (aggregation widens the haystack only; no new `ErrorCategory` added). **(2) `session_id` resolution from `transcript_path`** (`internal/hook/trace_session.go`, REQ-HFC-004) — `resolveTraceSessionID` derives the session UUID from `filepath.Base(transcript_path)` (stripping `.jsonl`, validated against the RFC 4122 UUID regex) when `session_id` is absent or the pre-existing `"unknown"` bug-#541 substitution fired, wired at the trace-filename binding site in `internal/hook/registry.go` `Dispatch`; unresolvable cases keep the original `"unknown"` fallback unchanged. **(3) Malformed/absent `ToolResponse` resilience** (REQ-HFC-005/006) — malformed nested payloads (bad JSON, bare string, array, absent, empty object) no longer error or panic; the handler falls back to a content-free but non-empty excerpt (500-char error text truncated at 200 runes + `...`). Full suite `go test ./...` exit 0 (106 packages); `go vet`/`golangci-lint` 0 issues; subagent-boundary grep 0 matches; touched functions 100% covered (package-wide `internal/hook` 83.6% baseline unchanged, pre-existing unrelated handlers). Run-phase Route A main-direct (Hybrid Trunk, PR #1102); single sync commit carries the `in-progress → implemented → completed` 3-phase close. 🗿 MoAI

- **cli: strip retired v2 permission deny rules on v2→v3 upgrade** (`ea1b29a68`, fixes #1101) — the v2→v3 clean-reinstall preserved `settings.json` wholesale (no embedded base exists for a 3-way merge), so 12 retired v2-era `deny` entries (`Write`/`Grep`/`Glob` × `./secrets/**`, `~/.ssh/**`, `~/.aws/**`, `~/.config/gcloud/**`) survived every upgrade and produced Claude Code startup warnings each session. A one-shot migration now removes deny entries exactly matching the known retired v2 template strings (exact-string match only, so genuine user customizations are untouched; unknown keys preserved via a `map[string]any` round-trip); a v3-clean file and repeat runs are byte-for-byte no-ops.

- **template: quote `statusLine` path on non-Windows platforms** (`c29c8f5e7`, fixes #1096) — the macOS/Linux branch of `settings.json.tmpl`'s `statusLine` command left `$CLAUDE_PROJECT_DIR` unquoted, so a project whose absolute path contains spaces word-split the command and the statusbar silently rendered nothing. Quoted to match the Windows branch.

- **template: ship `labels.yml` referenced by the label-sync workflow** (`58527b3e6`, refs #1094) — the distributed `label-sync.yml` workflow declared `.github/labels.yml` as its config source, but the template set never shipped that file, so the workflow was inert and errored on dispatch in every user project. Adds a neutral 3-axis label set (`type:*` / `priority:*` / `status:*` + GitHub-standard functional labels).

- **cli: dedupe banner v-prefix and persist language/name on `update` reconfigure** (`01675965e`) — the banner tolerated only an unprefixed version string, so `version.GetVersion()` (`"v3.0.0"`) rendered as `"vv3.0.0"` in the CLI pill; `applyWizardConfig` now also persists `ConversationLang` to `language.yaml` and `UserName` to `user.yaml` during `moai update` reconfigure (previously only Git fields were saved).

- **spec: era-aware lint demotion for grandfathered/terminal SPECs** (`dd644b5e0`) — `moai spec lint --strict` failed CI with 38 errors, nearly all on SPECs predating the structural rules it enforces. The era classifier (same SSOT as `moai spec audit`) now demotes `MissingExclusions`/`FrontmatterInvalid` findings to advisory on grandfather-era SPECs (V2.x/V3R2-R4/V3R5) and on terminal-status SPECs (`completed`/`superseded`/`archived`/`rejected`) — still visible, no longer failing; active modern-era SPECs keep full enforcement. `moai spec lint --strict` now exits 0 (0 errors, 62 advisory warnings).

- **rules: narrow `paths` gates to `SKILL.md` to stop per-command rule loads** (`bbec41b3a`, fixes #1099) — `prompting-best-practices.md` (`**/.claude/skills/**`) and `native-invocation-model.md` (`**/.claude/skills/moai/**`) gated on the skills directory rather than `SKILL.md`, so every `/moai` command reading its own workflow body loaded 9.3KB/13.5KB of authoring rules per invocation it never needed.

- **[SPEC-V3R6-LINK-FIX-001](.moai/specs/SPEC-V3R6-LINK-FIX-001/spec.md)** — fixed broken repo links, `modu-ai/moai-adk-go` → `modu-ai/moai-adk` (`CONTRIBUTING.md`, `CONTRIBUTING.ko.md`, docs-site) and a stale Anthropic docs URL, `docs.anthropic.com/en/docs/claude-code` → `code.claude.com/docs/en`, across all four README locales (`30694b913`).

- **docs-site: repair single-page layout collapse** (`fa4152261`, `98471f0be`) — four defects found via live browser inspection on the doc-detail layout: a duplicate table-of-contents rail (the legacy right-rail partial rendered alongside the page's own left rail), invisible hero text (brand CSS `h1`/link colors overrode the hero's white text on its green gradient band), a grid track that could not shrink and pushed the rail past the viewport, and a zero-date (`0001.01.01`) render on pages lacking a frontmatter date.

- **[SPEC-DOCS-WTRGO-001](.moai/specs/SPEC-DOCS-WTRGO-001/spec.md)** — corrected the worktree docs across all four locales (en/ko/ja/zh) where `moai worktree go` was wrongly presented as a navigation command; `go` prints the worktree path to stdout and does not change directory, so bare Pattern A/B/C occurrences and Mermaid diagram labels are rewritten to the correct shell idiom `cd "$(moai worktree go SPEC-ID)"`. Also documented `moai worktree switch` in the command-reference table (it prints the worktree location; does not `cd`) and corrected that command's misleading CLI help text in `internal/cli/worktree/switch.go`. 🗿 MoAI

- Internal/CI-only (no user-visible behavior change): dependency bumps (Go modules `5bfd274f9`/`53c440f8b`, GitHub Actions `783fa0bcc`/`c223db587`/`013123d21`); flaky-test repairs (LSP singleflight barrier `409c7738f`, harness observe-stop time-bomb seed `ca879fb0d`, IT04 frozen-guard/brand-dir `f1d3f1946`); a non-reproduced characterization probe for #1094 gitignore-merge (`3c03d5061`); a `paths-filter` dead-zone fix for workflow-only PRs (`851206f25`) and a `scripts/release.sh` CHANGELOG-extraction regex fix (`ede5ad20d`), both maintainer-tooling only; `moai agent lint` local-copy effort-drift cleanup (`6b4937eca`, template mirrors were already correct); and this repository's own branch-protection/PR-mandatory policy tightening (`6fc5dd2dc`).

## [3.0.0] - 2026-07-20

### Summary

v3.0.0 is the first stable (GA) release of the MoAI-ADK v3 line, closing the arc that began after **v2.14.0** (2026-04-24) and stabilized through the `rc1`–`rc12` pre-releases. The headline of v3 is a **tokenomics-first agentic workflow**: MoAI now reasons about token cost as an explicit budget rather than an afterthought. The major stacks that landed and matured across the window:

- **Tokenomics stack** — per-agent/per-phase token accounting, Tier×Phase model routing, `plan_type` profiles that map a SPEC's scope to a model/effort tier, and a **Token Circuit Breaker** (a Stop-hook token-budget guard that halts a runaway autonomous loop before it burns the budget).
- **Self-evolving harness + goal engine** — the `/moai goal` autonomous-continuation loop (a fresh model evaluates a completion condition each turn-end) and the `/moai loop` diagnostic sweep, together with the recursive harness-learning ladder that proposes delegation-map improvements from observed routing.
- **Consolidated 11-agent catalog** — 10 MoAI-custom managers/auditors/builders plus the Anthropic built-in `Explore`; references to archived agent names are rejected at spawn, replaced by per-spawn `Agent(general-purpose)` with a domain whitelist.
- **CG Mode** — a Claude-leader + GLM-teammate tmux topology for cost-optimized, implementation-heavy work.
- **TUX v3 CLI modernization** — `moai init` / `moai update` redesign on bubbletea/huh v2, a change-preview TUI, and the `internal/cli/printer.Printer` stdout=data / stderr=status migration.
- **Documentation + i18n rebuild** — the `adk.mo.ai.kr` docs-site "Claude Warm Editorial" redesign, 4-locale (en/ko/ja/zh) content, and a value-centered README restructure across all four locales.
- **Security hardening** — the `security.yaml` surface, the Bash risk-amplifier doctrine (destructive-primitive confirmation, compound-command soft cap), and the verification-claim integrity invariant (no unobserved verification-or-defect claim).

Per-pre-release detail for each theme lives in the `v3.0.0-rc1`…`v3.0.0-rc12` entries below and in the referenced SPEC files.

### Breaking Changes

- **Major version bump** — `v2.x` → `v3.0.0` (SemVer major). Existing projects should run `moai update` to reconcile templates before upgrading the binary.
- **Agent catalog consolidated to 11 retained agents** — references to archived agent names (e.g. `manager-strategy`, `expert-backend`, `researcher`) are now **rejected at spawn**; the retained-agent replacement pattern (per-spawn `Agent(general-purpose)` with a domain whitelist, or one of the 11 retained agents) is documented in `archived-agent-rejection.md`.
- **Agent Teams static-orchestration layer retired** — a forced `--team` / `--mode team` emits `MODE_TEAM_UNAVAILABLE` and falls back to sub-agent mode. The native Claude Code teammate runtime (`moai cg` GLM panes, `worktree --team`) is unaffected.
- **Context7 MCP dependency retired** — `mcp__context7__*` removed from every `allowed-tools` and the settings ask-list; library-documentation lookups now use the WebSearch/WebFetch fallback strategy.
- **`/moai e2e` replaced** — the web-only E2E subcommand was retired and revived as a multi-platform (web / mobile / desktop / desktop-native) subsystem driven by the new `e2e-tester` agent.

### Added

- **[SPEC-DESIGN-DOCSV2-001](.moai/specs/SPEC-DESIGN-DOCSV2-001/spec.md)** — docs-site (`adk.mo.ai.kr`) migrated to the 모두의AI Design System v2 (Epic DESIGN-V2, SPEC-1 of 2; Tier L, 30/30 AC PASS — 25 MUST + 5 SHOULD). **(1) Token unification (M1-M2)** — the prior 3-layer warm-cream token stack (`moai-brand.css` FROZEN + `moai-docs-tokens.css` Clay/Cream/Ink + `moai-docs-theme.css` remap) is collapsed into a single v2-native SSOT: achromatic neutral scale `#f4f4f4 → #060606`, single signature green `#3d7d5f`/`#316750`/`#265240`, solid de-emphasized `--gradient-signature` (no `135deg` gradient), Pretendard-only Korean typography (MaruBuri `@font-face` removed), and a two-token mono split (`--font-mono` JetBrains Mono + `--font-code` Goorm Sans Code). **(2) Layout (M3)** — home/listing adopts the round3 docs-index prototype (hero + sticky filter pills + featured + 4:5 grid) and doc-detail adopts the round3 docs-detail prototype (3-column + sticky TOC + read-progress + next-doc CTA), with the existing i18n/menu/⌘K-search infrastructure fully preserved. **(3) Mermaid v2 palette (M4)** — `foot.html` themeVariables repointed to the v2 palette (`primaryColor #eef4f0`, `lineColor #9fa0a0`), retaining mermaid v10. **(4) Mascot expansion (M5)** — 6 MoAI-Mascot PNGs installed to `static/mascots/`, surfaced on 3 emotional surfaces (home hero, 404, empty-section) via the promoted `mascot.html` shortcode; data/form/checkout surfaces remain mascot-free. **(5) Component v2 recipes (M6)** — buttons migrate to a solid-fill pill recipe (`border-radius: 9999px`, no neo-brutalist offset shadow) and cards migrate to a hairline-border + soft-shadow surface recipe (`border: 1px solid #d1d1d1`, `box-shadow: 0 2px 4px rgba(6,6,6,0.06)`), replacing the rounded-border + left-color-accent pattern; v2 shadow/motion tokens (`--shadow-sm/md/lg`, `--dur-*`, `--ease-*`) are consumed throughout. **(6) 4-locale parity + build gate (M7)** — the last hardcoded-Korean UI strings (10 `rail_*` doc-rail keys) are promoted to i18n across ko/en/ja/zh, a `.prose-kr` Korean-readability class is added, and `hugo --minify --gc` closes warning-free (KO 153 / EN 150 / JA 139 / ZH 150 pages, held identical across every milestone). **Sync-phase**: `docs-site/static/moai-brand.css` is re-stamped FROZEN with the v2 token vocabulary as the new frozen baseline (comment-only; no token values changed). **Known doc inconsistency** (flagged, not corrected — forbidden ownership crossing): acceptance.md §A headline states "20 MUST / 10 SHOULD"; the actual Severity-column tally is 25 MUST + 5 SHOULD. Run-phase Route A main-direct (Hybrid Trunk, no PR); single sync commit carries the `in-progress → completed` 3-phase close. 🗿 MoAI

- **[SPEC-DESIGN-MOAIWEBV2-001](.moai/specs/SPEC-DESIGN-MOAIWEBV2-001/spec.md)** — `moai web` console (`internal/web/`) migrated to the 모두의AI Design System v2 (Epic DESIGN-V2, SPEC-2 of 2; Tier M, 15/15 AC PASS — 14 clean + 1 PASS-WITH-DEBT AC-MWV2-032). **(1) Orphan `project` panel removal (M1)** — the unreachable `fieldsetProject` + `detectionField` and the `data-panel="project"` block are removed from `fieldsets.templ`/`root.templ`; the server-side parse/validate/persist seam (`parseProjectNestedForm` in `projectconfig.go`) and `pageView.Cur*` fields are PRESERVED so `development_mode`/`git_convention`/`quality.*` remain editable via yaml config/CLI (REQ-MWV2-031, zero server-contract change). **(2) Mascot 6-pose expansion (M2)** — the v2 Coffee/Explaining/Pointing/Searching/Teaching/Thinking poses are copied into `assets/mascots/` as `mascot-<pose>.png`, embedded via the existing `//go:embed assets/mascots` directive and served at `/static/mascots/`; the header brand badge is repointed to `mascot-thinking.png` and the retired `mascot-coding.png`/`mascot-talking.png` are removed. **(3) v2 token realignment (M3)** — `console.css` `:root` values are rewritten to the v2 achromatic canon (ink `#060606`, bg `#f4f4f4`, achromatic neutral ramp, success `#2e8a63`, borders `#d1d1d1`/`#e6e6e6`/`#b5b5b5`, focus-ring `rgba(61,125,95,0.16)`, primary hover/active `#316750`/`#265240`, solid signature accent `#3d7d5f` replacing the prior `linear-gradient` form including the inert `[data-theme="dark"]` override); the offline-safe `@font-face` + `--font-latin`/`--font-mono` system fallbacks are preserved verbatim (REQ-MWV2-030). **(4) Build + verification gate (M4)** — `make build`, `go build ./...`, `GOOS=windows GOARCH=amd64 go build ./...`, `go test ./internal/web/...`, `go vet ./...`, and `golangci-lint run ./internal/web/...` all clean (0 issues). **Debt (transparent)**: **AC-MWV2-032 PASS-WITH-DEBT** — the console's light-only INTENT is preserved (this SPEC adds zero new dark-mode wiring), but the AC's verification grep is over-broad and matches 16 pre-existing hits from a functional dark toggle shipped by `SPEC-WEB-CONSOLE-004/005` (`app.js` `wireThemeToggle`/`applyTheme` + the `themeToggle` button) that this SPEC neither added nor is scoped to remove; the AC's "no dark toggle wired" premise did not match pre-existing reality. Run-phase Route A main-direct (Hybrid Trunk, no PR); single sync commit carries the `in-progress → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CLI-TUX-V3-005](.moai/specs/SPEC-CLI-TUX-V3-005/spec.md)** — AC-TUX3-020 Printer migration: `fmt.Print*` ratchet succession in non-test `internal/cli` sources (Tier M, 5/5 AC PASS — 4 MUST + AC-TUX3-023 SHOULD). The 24 migratable call sites move to the `internal/cli/printer.Printer` interface — `state.go` 11 (M2), `migration.go` 8 (M3), `worktree/tmux_integration.go` 5 (M4) — dropping the global ratchet from the SPEC-003 baseline **38 → 14** (AC-TUX3-020 PASS). Mapping: JSON output + human-format display → `p.Data` (stdout, byte-identical to the prior sequential `fmt.Print*`); status/success messages → `p.Info`/`p.Success` (re-routed stdout→stderr per the stdout=machine-data / stderr=human-status convention); the multi-line human-format blocks (`printPhaseStateHuman`, migration status display) compose into a single `p.Data(strings.Join(lines, "\n"))` string so scripted consumers are unaffected (DECISION 2026-07-14). 3 gap-sites EXCLUDED per the M1 architecture gate: `uikit/banner.go` 12 (TUI lipgloss render — Printer interface not extended), `branch_protection.go` 1 (ttyConfirmer dead code, `nolint:unused`), `worktree/new.go` 1 (commented-out godoc example). Behavior preservation pinned by 3 new characterization-test files (`state_m2_test.go`, `migration_m3_test.go`, `tmux_integration_m4_test.go`) with golden-master stdout/stderr assertions; `internal/cli` coverage 74.1% (AC-TUX3-023 SHOULD-PASS). Run-phase Route A main-direct (no PR); single sync commit carries the `in-progress → completed` 3-phase close. 🗿 MoAI

- **[SPEC-CLI-TUX-V3-003](.moai/specs/SPEC-CLI-TUX-V3-003/spec.md)** — `moai update` decomposed into 5 subpackages + change-preview TUI (CLI TUX modernization M3, Tier L, 18/20 AC PASS-WITH-DEBT: 18 clean + AC-TUX3-018 partial + AC-TUX3-020 debt). **(1) 5-subpackage decomposition** (`internal/cli/update/{plan,backup,deploy,merge,report}`, AC-TUX3-004) — the 3,276-LOC `update.go` is refactored so analysis/classification lives in `plan` (8 funcs incl. the `isUserOwnedNamespace` ANCHOR predicate), config backup/restore + YAML 3-way merge helpers in `backup` (8 funcs + 5-func merge cluster, with a nil-safe `MergeFallbackRecorder` callback seam to avoid a circular import), clean leaf deploy ops in `deploy` (3 funcs: `CleanMoaiManagedPaths`, `MigrateLegacyMemoryDir`, `ScaffoldEvolutionDir`), merge orchestration in `merge` (4 funcs), and hook-review guidance emission in `report`; `update.go` drops to ~1,730 LOC retaining cobra wiring + the 3 template-sync/agency-migration funcs that are entangled with root-cli helpers (callback-seam extraction documented as deferred debt). 6 guard-test files across the namespace-protection family take ONLY import-path/qualifier adjustments — assertion bodies UNMODIFIED (REQ-TUX3-005); `internal/merge/confirm.go` is untouched by the decomposition. **(2) Shared classification model** (`internal/cli/update/class.go`, AC-TUX3-001/002/014) — a single `ChangeClass` enum (4 values: Add / Update / PreserveUserOwned / Conflict) + `Classify(relPath, exists, conflict, isUserOwned)` is the one shared type consumed by both the TUI preview and the text fallback; the preserve class delegates entirely to the injected `UserOwnedPredicate` (no parallel heuristic), and renders the `"preserved (user-owned)"` label in both surfaces. **(3) Change-preview TUI** (`preview.go` + `preview_tui.go` + `preview_fallback.go`, AC-TUX3-008/009/010) — built on `charm.land/bubbletea/v2` (table + viewport) and `charm.land/bubbles/v2` (landed by SPEC-CLI-TUX-V3-002); a `confirmViaPreview` single-entry convergence at `update.go:1609` routes the prior call sites through it, behavior-preserving (non-TTY confirm path mirrors the old `tea.Run` failure; TTY preview + `--yes`/non-TTY fallback produce the SAME classification decision). The TUI shows per-class counts, an every-file row, a row-select → diff-viewport, and Esc-returns-to-table; the text fallback carries zero ANSI under `NO_COLOR` or when piped, and a non-TTY summary form. **(4) confirm.go v2 promotion** (M3e, AC-TUX3-011/012/013) — the prior `confirm.go` v1 (huh v1 + bubbles v1) is promoted to a v2 bubbles-list form with the unified outcome card; the 3 `@MX:DEBT`/`@MX:CEILING`/`@MX:UPGRADE` annotations at lines 871-873 are RESOLVED (removed). **(5) M3b characterization safety net** (`update_characterization_test.go`, 8 tests / 14 subtests) — pins observable behavior of the UNDECOMPOSED `update.go` that the M3d decomposition MUST preserve (flag surface, mutual exclusion, golden classify values, namespace predicate superset, moai-managed classification, fast-path version comparison); remained green across every decomposition milestone. **(6) Coherence integration test** (`coherence_test.go`, AC-TUX3-016) — bridges `update.Classify` to `isUserOwnedNamespace` (REQ-TUX3-002 reachability) + asserts the `preserved (user-owned)` label. Full suite (`go test ./... -count=1`) exit 0; cross-platform build (darwin/windows amd64) exit 0; `golangci-lint` 0 issues on SPEC-003 scope (AC-TUX3-019); namespace guard 5-family (TestSplitHarnessNamespaceNoLeak + cli `Namespace|SecurityM2` + `internal/merge/...`) green UNMODIFIED across all milestones. **Debt (transparent)**: **AC-TUX3-018 partial** — 3/5 subpackages ≥85% coverage (report 92.9% · plan 95.0% · merge 90.3% ✓); `deploy` 74.7% and `backup` 75.2% sit below the 85% target on error-handling paths (manager-develop judged the gap reasonable for flaky-risk paths; ratchet to 85% is follow-up scope). **AC-TUX3-020 debt** — the `fmt.Print*` ratchet in non-test `internal/cli` sources holds at the baseline 38 (no regression); migrating the remaining call sites to the `internal/cli/printer` abstraction is a larger follow-up (Printer migration). acceptance.md AC-TUX3-009/010 `-run` regexes use `\|` (RE2 literal-pipe escape, not alternation) and AC-TUX3-015 names a `UserAssetPreservation` test absent from the suite — both are acceptance.md wording fixes owed to manager-spec (forbidden ownership crossing; NOT edited in sync-phase). 🗿 MoAI

### Changed

- **[SPEC-CLI-TUX-V3-003](.moai/specs/SPEC-CLI-TUX-V3-003/spec.md)** — amendment re-close (2nd close). The SPEC was amended (commit `313482ca4`, completed → in-progress) to complete AC-TUX3-018 coverage. **AC-TUX3-018 NOW DONE**: `internal/cli/update/deploy` 97.5% (commit `6fb84a280`) + `internal/cli/update/backup` 88.6% (commit `8d7106c59`); report 92.9% · plan 95.0% · merge 90.3% unchanged (all ≥85% target). **D1 resolved**: acceptance.md §D.5 reconciled (commit `36eeb3280`) — 19 AC PASS + AC-TUX3-020 split (debt). **AC-TUX3-020 (Printer migration) EXCLUDED** — split to a separate SPEC (not yet authored; debt_register carries it); `fmt.Print*` ratchet baseline 38 unchanged (no regression). 19/20 AC clean. Single sync commit carries the `in-progress → implemented → completed` amendment re-close transition (spec.md frontmatter `status` + `updated` ONLY; SPEC body, plan.md, acceptance.md body NOT modified). 🗿 MoAI

### Fixed

- **[SPEC-V3R6-V2-V3-CLEAN-REINSTALL-002](.moai/specs/SPEC-V3R6-V2-V3-CLEAN-REINSTALL-002/spec.md)** — repaired two `moai update` v2→v3 clean-reinstall regressions introduced by the parent SPEC (`SPEC-V3R6-V2-V3-CLEAN-REINSTALL-001`): **#1084** (silent data-loss infinite loop) and **#1086** (arbitrary-directory template pollution). Tier M, 10/10 AC PASS (9 MUST + 1 SHOULD cross-platform). **(1) v3-version negative-override** (`internal/cli/v2_detection.go`, REQ-CRR-001/AC-CRR-001) — `probeVersionSignal` now returns a third `v3Confirmed` flag; when `system.yaml` carries a populated `v3.*` version, the new `V2Fingerprint.V3VersionConfirmed` field short-circuits `IsV2` to `false` regardless of Signal 2 (`.agency/`) or Signal 3 (DeprecatedPaths residue) — breaking the loop that reset user-modified `.moai/config/sections/language.yaml`/`design.yaml` to template defaults on every invocation (REQ-CRR-002/003, AC-CRR-002/003). **(2) Positive project-marker precondition** (`internal/cli/update.go`, REQ-CRR-004/005, AC-CRR-004/005) — `moai update` (non-`--binary`) now refuses to run in a directory lacking `.moai/config/sections/system.yaml`, returning a structured `not a moai project` error before `acquireUpdateLock` (the lock itself `MkdirAll`s `.moai/`, so a post-lock gate would still leave an empty directory behind) — an M2-introduced `isMoAIProject` conjunct on the clean-reinstall branch alone was insufficient because Option α's "missing system.yaml ⇒ positive Signal 1" heuristic let a non-project cwd fall through to the v3 file-level sync path, which still deployed the full embedded template tree. **(3) Actual-removal-count log gating** (`internal/cli/update_clean_install.go`, REQ-CRR-006/AC-CRR-006) — the REMOVE-phase log now derives `removedCount` from a pre/post-REMOVE filesystem re-scan diff instead of the 43-entry planned-list length, emitting `Removed N deprecated paths` only when `N > 0` (else `No deprecated paths found to remove`), eliminating the phantom "Removed 10 deprecated paths" log on runs that removed nothing. **(4) `.agency/` migration decoupling** (REQ-CRR-007/AC-CRR-007) — `runMigrateAgency` fires independently of the v2 fingerprint verdict, gated solely on `.agency/` presence, so legacy-asset migration keeps working on v3 projects without re-activating clean-reinstall. Two Reproduction-First tests (`TestReproduction_FingerprintNonConvergence_Issue1084`, `TestReproduction_NonProjectDirectoryPollution_Issue1086`) were confirmed RED against the true pre-fix baseline (`0cdf18e07`) before the fix landed, plus a 3-run idempotency test (`TestRunUpdate_ThreeRunIdempotency_V3Project`, AC-CRR-009) and a cross-platform parity test (`TestFingerprintPredicate_CrossPlatformParity`, AC-CRR-010, S2). The parent SPEC's PRESERVE-inventory enumeration, SHA-256 hash-diff detection, backup-before-removal scheme, and the frozen 43-entry DeprecatedPaths list are all unweakened (`internal/cli/update_preserve_inventory.go` 0 lines changed, AC-CRR-008). 🗿 MoAI

- **[SPEC-V3R6-ZONE-REGISTRY-PACKAGING-001](.moai/specs/SPEC-V3R6-ZONE-REGISTRY-PACKAGING-001/spec.md)** — `.claude/rules/moai/core/zone-registry.md` (115 `CONST-*` entries, consumed at runtime by `moai constitution list/guard/amend/validate`, `moai doctor`, and `moai spec lint`) is now packaged into the embedded template tree, fixing the lone-omission gap where every deployed project (`moai init`/`moai update`) lacked the registry entirely — `moai constitution` failed to load, `moai doctor` downgraded to Warn, and the zone-registry-backed `moai spec lint` dimension was silently skipped (Tier S, GitHub #1090, 6/7 AC PASS + 1 PASS-WITH-DEBT). A neutralized sanitized-pair mirror is added at `internal/template/templates/.claude/rules/moai/core/zone-registry.md` (source file untouched; 2 SPEC-IDs + 2 dates neutralized in the mirror only), preserving all 119 `CONST-V3R*` governance tokens under the default template-neutrality tier (`go test ./internal/template/...` exit 0). The pre-existing strict-tier leak baseline (132 unrelated internal-date occurrences across ~40 other template files) is unchanged with or without the mirror (evidence: 132/132) — out of scope for this fix. 🗿 MoAI

- **[SPEC-UPDATE-REINSTALL-LOOP-001](.moai/specs/SPEC-UPDATE-REINSTALL-LOOP-001/spec.md)** — broke the `moai update` clean-reinstall infinite loop and restored config preservation on the clean-reinstall path (GitHub #1084, Critical, Tier M, 8/8 gating AC PASS — AC-RIL-001..007 + 010; AC-RIL-008/009 SPLIT to follow-up `SPEC-UPDATE-PREFLIGHT-SAFETY-001`). **Root cause** — `.claude/rules/moai/design` was simultaneously a `defs.DeprecatedPaths` entry AND shipped by the v3 embedded template, so the v2-detection "deprecated path present" signal fired perpetually on every v3 project: the clean-reinstall removed the path, the template redeployed it, re-arming the next `moai update` (zero-net-change infinite loop) — a version-format-independent regression the existing `v3.`-prefixed system.yaml override could not suppress. **M2 (commit `94eb1b58a`)** — removed the stale `.claude/rules/moai/design` entry from `defs.DeprecatedPaths` (Category B, total 41→40, Category B 29→28, counts updated atomically in `dirs_test.go` guards + `dirs.go` + `v2_detection.go` header comments); added a build-time regression guard `TestDeprecatedPaths_NoTemplateCollision` asserting `DeprecatedPaths ∩ embedded-template-FS = ∅` (with a negative-path proof that re-inserting a colliding entry FAILs the guard). **M1 (commit `59f52dc8e`)** — restored config preservation on the clean-reinstall path: `.claude/settings.json` user customizations (effortLevel, teammateMode, permissions, non-MoAI hooks) and ALL `.moai/config/sections/*.yaml` files (not only `user.yaml` — issue #1084 explicitly reported `language.yaml`/`design.yaml` loss) now survive a clean-reinstall via the same merge/preserve protection (`backup.RestoreMoaiConfig` + `updatemerge.MergeUserFiles`) the normal `moai update` path already applies, closing the clean-reinstall's lower-protection bypass; a user-configured higher-capability `model` (e.g. `opus`) survives as a by-product of the merge-preserving `settings.json` handling (no template pin removal needed). **Scope note**: pre-flight safety hardening (fail-closed abort on symlink / nested-`.git` before destruction, no-half-migrated-state invariant — AC-RIL-008/009) is SPLIT to the follow-up `SPEC-UPDATE-PREFLIGHT-SAFETY-001` per plan-audit iter-1 decision; this SPEC's scope is the loop-break + config-preservation fix only. Full suite (`go test ./...`) green aside from a pre-existing unrelated `internal/config` failure (stash-verified pre-existing on clean tree); cross-platform build (darwin native + `GOOS=windows GOARCH=amd64`) exit 0. 🗿 MoAI

- **[SPEC-V3R6-DOCTOR-FALSE-SIGNAL-001](.moai/specs/SPEC-V3R6-DOCTOR-FALSE-SIGNAL-001/spec.md)** — repaired a `moai doctor` false-signal cluster sharing one root theme: the doctor conflated mere on-disk PRESENCE of a runtime-generated artifact, or a hand-maintained STALE reference list, with a genuine "configured / known" state (Tier M, 9/9 AC PASS — 8 clean + 1 PASS-WITH-DEBT). **Defect A (GitHub #1087)** — the Harness 5-Layer check (`internal/cli/doctor_harness.go` `runHarnessCheck`) previously decided "harness configured?" from `.moai/harness/` directory EXISTENCE alone, so the FIRST learning-telemetry write (`usage-log.jsonl`, appended by the PostToolUse/Stop/SubagentStop observe hooks) flipped a project that never configured a v4 harness from `CheckOK` to a red `CheckFail` (L2-L5 FAIL, missing baseline files). The check now excludes runtime telemetry artifacts from the "configured" determination — a `.moai/harness/` directory containing ONLY `usage-log.jsonl` still reports `CheckOK` "no harness configured", while a directory containing at least one of the 7 baseline harness files (`main.md`/`plan-extension.md`/`run-extension.md`/`sync-extension.md`/`chaining-rules.yaml`/`interview-results.md`/`README.md`) still runs the full L1-L6 battery unchanged (partial-baseline misconfiguration still correctly FAILs). **Defect B (GitHub #1088)** — the Skills Allowlist check (`internal/cli/doctor_skills.go` `classifySkill`/`checkSkillsAllowlist`) validated `.claude/skills/moai-*` directories against a hand-maintained 22-entry static Go slice that had drifted from the embedded template manifest (27 `moai-*` skills), reporting "10 unknown moai- skill(s) detected (run 'moai update' to sync)" on a project whose skills were JUST installed by that same binary's embedded templates — a misleading hint, since `moai update` cannot fix a drift in the doctor's own hardcoded list. The allowlist is now DERIVED from the authoritative embedded template skills manifest (`internal/template/skills_manifest.go`, new), so a template-fresh project reports zero unknown skills by construction, while a genuinely unrecognized `moai-*` directory (stale or third-party) still classifies `CheckWarn`. **Preservation guard** — both fixes are read-only diagnostics confined to `internal/cli/doctor_harness.go` / `internal/cli/doctor_skills.go` / `internal/cli/doctor.go` plus the new manifest-read helper and tests; `internal/cli/update.go`, `internal/cli/update_clean_install.go`, and `internal/harness/applier.go` are un-diffed, and the `SPEC-V3R6-V2-V3-CLEAN-REINSTALL-002` preservation test suites (namespace / clean-install / preserve families) pass unchanged. Reproduction-first TDD: both defects were RED-confirmed (byte-matching the exact issue reports) on the pre-fix tree, then GREEN post-fix. Full suite (`go test ./...`) and cross-platform build (darwin + windows/amd64) green; per-function coverage on changed funcs 80.8%-98.2%. **PASS-WITH-DEBT (AC-DFS-009)**: 2 pre-existing/flaky test failures outside this SPEC's scope were observed during the full-suite run and are not attributable to either fix. Run-phase Route A main-direct (Hybrid Trunk, no PR); single sync commit carries the `in-progress → completed` 3-phase close. 🗿 MoAI

- Per-pre-release fixes (e.g. the profile-display bug fix) are recorded in the `v3.0.0-rc1`…`v3.0.0-rc12` entries below.

### Installation & Update

- **New install** — `go install github.com/modu-ai/moai-adk/cmd/moai@v3.0.0`, or download a prebuilt binary from the GitHub release assets (asset names carry no `v` prefix per the update checker contract).
- **Existing projects** — run `moai update` to sync the v3.0.0 templates (user-owned files under `.claude/`, `.moai/project/`, `.moai/specs/` are preserved), then `moai update --binary` to upgrade the CLI.
- **Verify** — `moai version` reports `v3.0.0`.

---

## [3.0.0] - 2026-07-20 (한국어)

### 요약

v3.0.0은 MoAI-ADK v3 라인의 첫 정식(GA) 릴리스로, **v2.14.0**(2026-04-24) 이후 시작되어 `rc1`–`rc12` 사전 릴리스를 거쳐 안정화된 개발 구간을 마무리한다. v3의 핵심은 **토크노믹스 우선 에이전트 워크플로우** — MoAI가 토큰 비용을 부차적 요소가 아니라 명시적 예산으로 다룬다는 점이다. 구간 전반에 걸쳐 안착·성숙한 주요 스택은 다음과 같다.

- **토크노믹스 스택** — 에이전트/페이즈별 토큰 회계, Tier×Phase 모델 라우팅, SPEC 범위를 모델·effort 티어에 매핑하는 `plan_type` 프로파일, 그리고 자율 루프가 예산을 소진하기 전에 중단시키는 Stop-훅 토큰 예산 가드 **Token Circuit Breaker**.
- **자기진화 하네스 + goal 엔진** — 매 턴 종료 시 별도 모델이 완료 조건을 평가하는 `/moai goal` 자율 지속 루프와 `/moai loop` 진단 스윕, 그리고 관찰된 라우팅으로부터 위임 맵 개선안을 제안하는 재귀적 하네스 학습 사다리.
- **11-에이전트 카탈로그 통합** — MoAI 커스텀 매니저/감사/빌더 10종 + Anthropic 내장 `Explore`; 아카이브된 에이전트 이름 참조는 spawn 시 거부되고, 도메인 화이트리스트를 가진 per-spawn `Agent(general-purpose)`로 대체된다.
- **CG 모드** — 구현 위주 작업의 비용 최적화를 위한 Claude 리더 + GLM 팀메이트 tmux 토폴로지.
- **TUX v3 CLI 현대화** — bubbletea/huh v2 기반 `moai init` / `moai update` 재설계, 변경 미리보기 TUI, `internal/cli/printer.Printer`의 stdout=데이터 / stderr=상태 마이그레이션.
- **문서 + i18n 재구축** — `adk.mo.ai.kr` docs-site "Claude Warm Editorial" 재설계, 4개 로케일(en/ko/ja/zh) 콘텐츠, 4개 로케일 전반의 가치 중심 README 재구성.
- **보안 강화** — `security.yaml` 표면, Bash 위험 증폭기 독트린(파괴적 프리미티브 확인·복합 명령 소프트 캡), 검증-주장 무결성 불변식(관찰되지 않은 검증-또는-결함 주장 금지).

각 테마의 사전 릴리스별 세부 내용은 아래 `v3.0.0-rc1`…`v3.0.0-rc12` 항목과 참조된 SPEC 파일에 있다.

### 주요 변경 사항

- **메이저 버전 승격** — `v2.x` → `v3.0.0` (SemVer major). 기존 프로젝트는 바이너리 업그레이드 전에 `moai update`로 템플릿을 정합화해야 한다.
- **에이전트 카탈로그 11종으로 통합** — 아카이브된 에이전트 이름 참조는 이제 spawn 시 **거부**된다. 대체 패턴(도메인 화이트리스트를 가진 per-spawn `Agent(general-purpose)` 또는 11종 유지 에이전트 중 하나)은 `archived-agent-rejection.md`에 문서화되어 있다.
- **Agent Teams 정적 오케스트레이션 계층 폐기** — 강제 `--team` / `--mode team`은 `MODE_TEAM_UNAVAILABLE`을 내고 sub-agent 모드로 폴백한다. 네이티브 Claude Code 팀메이트 런타임(`moai cg` GLM 페인, `worktree --team`)은 영향받지 않는다.
- **Context7 MCP 의존성 폐기** — 모든 `allowed-tools`와 settings ask 목록에서 `mcp__context7__*` 제거; 라이브러리 문서 조회는 WebSearch/WebFetch 폴백 전략을 사용한다.
- **`/moai e2e` 교체** — 웹 전용 E2E 서브커맨드를 폐기하고, 새 `e2e-tester` 에이전트가 구동하는 멀티플랫폼(web / mobile / desktop / desktop-native) 서브시스템으로 부활시켰다.

### 추가됨

- **[SPEC-CLI-TUX-V3-005]** — 비테스트 `internal/cli` 소스의 `fmt.Print*` → `internal/cli/printer.Printer` 인터페이스 마이그레이션(24개 호출부, ratchet 38 → 14). 상세는 위 영어 항목 참조.

### 변경됨

- **[SPEC-CLI-TUX-V3-003]** — `moai update`의 5개 서브패키지 분해 + 변경 미리보기 TUI, 이후 AC-TUX3-018 커버리지 완결을 위한 수정 재클로즈. 상세는 위 영어 항목 참조.

### 수정됨

- **[SPEC-V3R6-V2-V3-CLEAN-REINSTALL-002]** — `moai update` v2→v3 클린 재설치 회귀 2건 수정(#1084 무한 루프·데이터 손실, #1086 임의 디렉터리 템플릿 오염). Tier M, 10/10 AC. 상세는 위 영어 항목 참조.
- **[SPEC-V3R6-ZONE-REGISTRY-PACKAGING-001]** — `zone-registry.md`(115 CONST 엔트리)를 임베디드 템플릿 트리에 패키징, 배포 프로젝트의 `moai constitution`·`moai doctor`·`moai spec lint` 로드 실패 수정(Tier S, #1090, 6/7 AC). 상세는 위 영어 항목 참조.
- 프로파일 표시 버그 수정 등 구간별 수정 내역은 아래 `v3.0.0-rc1`…`v3.0.0-rc12` 항목 참조.

### 설치 및 업데이트

- **신규 설치** — `go install github.com/modu-ai/moai-adk/cmd/moai@v3.0.0`, 또는 GitHub 릴리스 자산에서 사전 빌드 바이너리 다운로드(자산 이름은 업데이트 체커 규약에 따라 `v` 접두어 없음).
- **기존 프로젝트** — `moai update`로 v3.0.0 템플릿 동기화(`.claude/`, `.moai/project/`, `.moai/specs/` 하위 사용자 소유 파일은 보존), 이후 `moai update --binary`로 CLI 업그레이드.
- **검증** — `moai version`이 `v3.0.0`을 보고.

## [v3.0.0-rc12] - 2026-07-14

### Added

- **[SPEC-DESKTOP-NATIVE-E2E-001](.moai/specs/SPEC-DESKTOP-NATIVE-E2E-001/spec.md)** — `/moai e2e` gains a `desktop-native` OS-accessibility automation lane, replacing the deferral notice from SPEC-E2E-REVIVAL-001 (Tier M, 26/26 gating AC PASS — 25 clean + 1 PASS-WITH-DEBT; e2e-revival deferred item 2/2). **(1) Detection + routing** (workflow skill pair, byte-identical local/template) — the `desktop-native` Detection Matrix row gains positive per-toolkit markers (AppKit `.xcodeproj`/`Package.swift`, WinUI/Win32 `.vcxproj`, Qt `CMakeLists.txt`/`.pro`, GTK meson/CMake), preserving most-specific-first ordering (Electron/Tauri rows stay checked first); the verbatim deferral sentence "There is no opt-in automation path for `desktop-native`" is removed while the genuine no-target graceful exit (pure libraries) is preserved; `--platform`/`--tool` flags, the Tool Matrix, and the Toolchain Probe table all gain desktop-native rows/tokens; the Phase 5 report-template platform enum gains `desktop-native`. **(2) Per-OS agent recipes** (`e2e-tester.md` pair) — the `### desktop-native` stub is replaced with three OS recipe subsections: macOS default `axcli` (cargo install, pinned version, AXUIElement tree snapshot) with appium-mac2-driver+WebdriverIO fallback and a TCC/Accessibility-permission blocker-report path (never a user prompt); Windows default FlaUI.WebDriver+WebdriverIO (W3C WebDriver2/UIA3, EXPERIMENTAL caveat, `/status` smoke probe) with pywinauto fallback; Linux default dogtail 2.x (AT-SPI2, `QT_LINUX_ACCESSIBILITY_ALWAYS_ON=1` for Qt, Wayland/ponytail caveat) with ydotool/xdotool-plus-screenshot-verification fallback; a documented universal last resort (AX-tree text snapshot loop, screenshot loop explicitly NOT CI-repeatable evidence) and the token-cost ordering doctrine (AX-tree text ≪ full tree JSON < screenshot < screenshot loop). WinAppDriver/appium-windows-driver are explicitly excluded (abandoned since 2020, zero references). Windows/Linux recipes are declarative documentation verified by structure/grep ACs; only the macOS path is locally probe-able and that probe is non-gating. **(3) Command surfaces** — the template `e2e.md.tmpl` and local `.claude/commands/moai/e2e.md` `argument-hint` gain the `desktop-native` `--platform` value; template command body stays at 6 non-empty LOC (well under the 20-LOC thin-command ceiling). **(4) Integrity** — no new agent file, no `catalog.yaml` entry/count/pin change (CI pins `expectedAgentCount=10`/`expectedTotal=38` untouched) aside from the mechanical `hash:`-line content-hash regeneration of the existing `e2e-tester` entry produced by the required `make build` (an explicit carve-out added to REQ-DNE-302 at run-phase, since `TestManifestHashFormat` enforces the regen); skill and agent pairs remain byte-identical across local/template trees (`diff` exit 0); the deferral-wording family regex (baseline: skill 3 + agent 1 matching lines per tree) returns 0 matches across all 4 files in both trees. `make build` and `go test ./internal/template/...` exit 0; `moai spec lint --strict` reports 0 errors. docs-site 4-locale documentation of the desktop-native lane is deferred to a future docs-site sync, matching the SPEC-DOCSITE-E2E-001 precedent. 🗿 MoAI

- **[SPEC-DOCSITE-E2E-001](.moai/specs/SPEC-DOCSITE-E2E-001/spec.md)** — docs-site (`adk.mo.ai.kr`) 4-locale documentation for the revived `/moai e2e` subcommand, deferred from SPEC-E2E-REVIVAL-001 (Tier M, 20/20 AC PASS). **(1) New 4-locale page** — `content/{en,ko,ja,zh}/utility-commands/moai-e2e.md`, authored ko-canonical then derived to en/ja/zh with identical 11-H2 structure and per-platform (web/mobile/desktop) toolchain tables. **(2) Navigation + menu registration** — `data/menu/main.yaml` sub-entry (4-locale name map) plus a `_meta.yaml` entry in all 4 locale content trees, ordered after `moai-feedback` at weight 90. **(3) Count-literal normalization** — the docs-site-wide stale "10 retained agents / 9 MoAI-custom / 10-agent catalog" literal family (English + ko/ja/zh locale forms, including a widened digit-boundary pattern catching spaced ja/zh phrasing) corrected to the 11/10 catalog across 41 files (40 markdown + `layouts/index.html` stat row), with `e2e-tester` inserted into six enumeration-class catalog surfaces (`advanced/agent-guide.md`, `claude-code/agentic/sub-agents.md`, `getting-started/introduction.md`, `getting-started/faq.md`, `advanced/claude-md-guide.md`, `core-concepts/what-is-moai-adk.md`) and left absent from five count-only surfaces where the literal update alone is correct. Hugo build stays warning-free (0 new WARN/ERROR vs. pre-flight baseline); the 16 pre-existing "no H1 heading" script-check errors on untouched files (decision-memory.md, harness-profiles.md, moai-goal.md, moai-harness.md × 4 locales) are unrelated to this change. `moai spec lint` reports 0 findings.

- **[SPEC-DOCSITE-ADVANCED-001](.moai/specs/SPEC-DOCSITE-ADVANCED-001/spec.md)** — docs-site v3.0 Advanced Guides content expansion (Tier L, 6 pages × 4-locale = 24 new markdown files + advanced/_meta.yaml parity debt fix + navigation registration). **6 new Advanced pages**: tokenomics-overview, token-budget, no-haiku-3tier, plan-type-profiles, self-evolving, autonomous-loops — authored in canonical KO and derived to EN/JA/ZH per oss-docs i18n rules. **M1 parity debt fix** (`49b3fda60`) — closed pre-existing advanced/_meta.yaml parity gap: 14 existing entries (catalog-system, decision-memory, harness-profiles, harness-v4-builder, hooks-reference, ultracode-workflows) now registered in all 4 locales (KO was missing 3, EN/JA/ZH were missing 6 each). **M5 navigation registration** (`e16aa5ee0`) — 6 new sub-entries added to data/menu/main.yaml Advanced section, per-locale _meta.yaml updated with 6 new entries each (24/24 parity verified). **Design regime verification**: 0 body-emoji violations (after token-budget inline-code emoji fix), 0 flowchart LR/RL matches (TD-only Mermaid), 0 blacklisted URLs (adk.mo.ai.kr only), H2 section-count parity 0 gaps across all 6 slugs × 4 locales, 132 icon shortcode invocations across 24 files. **Hugo build**: exit 0, 0 warnings, 24 new HTML pages generated (KO 126, EN/JA/ZH 124 each). **Known debt** (AC-DA-071 PASS-WITH-DEBT): sitemap includes section-level URL only (/ko/advanced/), not individual page URLs — pre-existing geekdoc sitemap configuration affecting ALL advanced pages equally (not specific to this SPEC's 6 new pages). 33/33 AC PASS (32 clean + 1 PASS-WITH-DEBT). 🗿 MoAI

- **[SPEC-DOCTOR-PROMOTION-001](.moai/specs/SPEC-DOCTOR-PROMOTION-001/spec.md)** — `moai doctor` gains a new "Plugin Deployment" check (`internal/cli/doctor.go` `checkPluginDeployment`, registered in the `moaiChecks` group) that detects a `plugin-deployed vX.Y.Z` marker in `.moai/config/sections/system.yaml` — recognizing both the `moai:`-rooted `version:` key shape and a top-level `version:` key shape — and reports a Warn with the deployed-vs-binary version comparison and a Detail-text-only promotion suggestion naming `moai init` as the path to the binary-managed template tree (4/4 AC PASS, Tier S). The check performs no automatic migration and no file writes (REQ-DP-002), returns OK with zero noise when no marker is present — plain semver or absent `system.yaml` (REQ-DP-003) — and degrades gracefully (OK/Warn with a parse note, never Fail) on an unreadable or malformed `system.yaml` (REQ-DP-004). Origin: REQ-BD-007, deferred from SPEC-MOC-BOOTSTRAP-DESKTOP-001 (sibling repo `claude.mo.ai.kr`).

- **[SPEC-E2E-REVIVAL-001](.moai/specs/SPEC-E2E-REVIVAL-001/spec.md)** — `/moai e2e` revived as a multi-platform, token-minimized E2E testing subsystem (Tier L, 28/28 AC PASS), superseding the web-only version retired by SPEC-SUBCOMMAND-RETIRE-001. **(1) Platform expansion** — web (Playwright CLI) + mobile (Maestro default, Appium/Detox alternatives) + desktop (Playwright-Electron for Electron apps, WebdriverIO + `@wdio/tauri-service` for Tauri apps), selected via project-type auto-detection (`web`/`mobile`/`desktop`/`mixed`/`desktop-native`/`none`); native-desktop (non-Electron/non-Tauri) automation is explicitly deferred to a follow-up SPEC. **(2) Dedicated specialist agent** — a new retained agent `e2e-tester` (`.claude/agents/moai/e2e-tester.md`, both template and local trees) drives detection, journey mapping, script creation, execution, and recording; it never prompts the user directly (structured `## Missing Inputs` blocker reports only) and all toolchain/journey selection routes through the orchestrator's `AskUserQuestion` channel. **(3) Token-minimization as a first-class requirement** — CLI-output-first execution (MCP only when the selected CLI cannot do the job), bounded in-context output (≤50 lines OR ≤2KB, file-redirect for verbose logs), native trace/recording facilities over MCP screenshot loops, and no MCP server as a hard dependency for any default platform path. **(4) Router + catalog reachability** — the `/moai` SKILL.md Priority 1 router and semantic (Priority 3) classification both regain an `e2e` entry; `CLAUDE.md` §3/§4 and the retained-agent catalog move from 10 to 11 agents (9→10 MoAI-custom); `internal/template/catalog.yaml` gains an `e2e-tester` entry; the Go tier-profile display surface (`model_policy.go`) gains a matching data row so the agent renders in the `moai web` model-policy preview. CI-guard count constants reconciled in the same milestones (`expectedAgentCount` 9→10, `expectedTotal` 37→38); `expectedSkillCount` unchanged at 28 (the workflow skill lives inside the existing `moai` skill directory). Template-First throughout: all three new artifacts (thin command, workflow skill, agent) authored in `internal/template/templates/` first, then synced to the local tree, byte-identical across both. Full suite (`go test ./...`), cross-platform build (darwin/windows amd64), `golangci-lint` (0 issues), and template-neutrality CI guards all green. docs-site 4-locale documentation for the revived subcommand is explicitly deferred to a follow-up SPEC.

- **[SPEC-CLI-TUX-V3-002](.moai/specs/SPEC-CLI-TUX-V3-002/spec.md)** — `moai init` redesign (CLI TUX modernization M2, Tier M, 18/18 AC PASS). **(1) Deferred self-update ordering** — the pre-wizard blocking `runBinaryUpdateStep` network call is removed; a CHECK-ONLY goroutine starts after wizard completion / first phase output and flushes a non-blocking stderr notice (with a `moai update` hint) under a bounded grace, never re-execs, never installs; existing skip conditions (`templates-only` flag, `EnvSkipBinaryUpdate`, dev-build detection) are preserved. **(2) huh v2 spike + unified wizard** — the huh v2.0.3 + bubbletea v2 pair spike confirmed the v1 YOffset scroll defect (`updateViewportHeight()` unconditional snap-to-top) is resolved by v2's `ensureVisible()` clamp-only scrolling; the wizard migrated to `charm.land/huh/v2`, consolidating unconditional questions into multi-field group pages while each conditional question keeps its own `WithHideFunc`-wrapped group, and the `wizardTotalSteps = 6` hardcoded stepper denominator is replaced by a single dynamic source (`TotalVisibleQuestions`, measured 5/8/9/12 across modes). **(3) Live progress feedback** — `internal/cli/printer`'s Spinner handle now animates via bubbles v2 `spinner.MiniDot` frame-cycling (gated on motion-allowed real TTY, goroutine-joined before the final line, `-race` clean), with a zero-ANSI/zero-animation plain fallback for non-TTY / `NO_COLOR` / `MOAI_REDUCED_MOTION`; `charm.land/bubbletea/v2` and `charm.land/bubbles/v2` land as direct go.mod dependencies (M1 residual-debt resolution), while huh v1.0.0 is retained for `update.go`/`profile_setup.go` (dual-major coexistence, M3 scope). **(4) Warning collector** — a `warnCollector` records every `Warn` and emits a single consolidated stderr summary panel exactly once at exit (zero panel when no warnings); stdout never carries warning text. **(5) Completion card + redirect hint** — the success completion card shows the `cd <project>` → `moai cc` → `/moai plan` next-action sequence with a warning-summary pointer when applicable, and re-running `moai init` against an already-initialized project without `--force` now surfaces a `moai update` redirect hint. **(6) Quality gate** — full suite (`go test ./... -count=1`) and `golangci-lint` (0 issues) green, `GOOS=windows`/`GOOS=linux` builds exit 0, and the `fmt.Printf`/`Println`/`Print(` ratchet in non-test `internal/cli` sources drops from the re-measured baseline 40 to 38 as two wizard call sites migrate to the unified form's Note-rendered stepper. 🗿 MoAI

- **[SPEC-WEBCONF-SIMPLIFY-001](.moai/specs/SPEC-WEBCONF-SIMPLIFY-001/spec.md)** — `moai web` configuration console simplified from 17 tabs to 6 surviving tabs (identity, language, launch, git_strategy, llm, agentfm). **(1) Tab consolidation** — the 11 removed tabs (`project`, `quality_extras`, `workflow`, `harness`, `ralph`/MoAI-Loop, `feedback`, `observability`, `security`, `mx`, `handoff`, `cache`) have their current values baked as template defaults in `internal/template/templates/.moai/config/sections/*.yaml` (ship to all users via `moai init`/`moai update`); the config keys persist for runtime consumption, only the web UI tab + write-route are removed (reclassified to `RouteExcluded`). **(2) 4-color sub-agent token-cost tier** — a net-new name-keyed lookup table (`internal/harness/v4manifest`) maps each of the 20 catalog agents to a display-only color tier (🔴 xhigh / 🟠 high / 🔵 medium / 🩵 low; distribution 🔴×4·🟠×4·🔵×5·🩵×7) INDEPENDENT of the agent's `effort` frontmatter (Option A: effort files UNTOUCHED, 0 `tier:` keys written); the agentfm UI renders the badge + tier-suggested (model, effort) defaults, individually overridable. **(3) Simplified surviving-tab surfaces** — git_strategy surfaces only `mode` + `merge_method` + `hooks.pre_push`; llm surfaces only the GLM tier mapping (`glm.models.{high,medium,low,fable}`); quality_extras is reduced to a single enable/disable toggle on the `launch` tab. **(4) Per-option description rendering** — a `FieldDef.Description` i18n-key field + a `fieldsets.templ` `fieldDescription` helper render a localized description below each field; per-option descriptions use the native `<option title>` via `data-i18n-title` (resolved by `app.js`). **(5) i18n 4-locale descriptions** — 24 `fieldDesc.<sectionID>.<fieldID>` keys × 4 locales (en/ko/ja/zh) = 96 strings; `TestI18nKeySetParity` passes with zero exemptions (REQ-WC-015/C-9). **(6) Template-default deltas** — 8 of 9 §D.Δ deliberate default-changes applied (workflow.execution_mode→auto, security.strict_mode→true, cache.enabled→true, git-strategy pre_push→enforce ×2, harness.effort_mapping→{low,medium,high}); the 9th (merge_method `squash`→`Squash`) was NOT applied — `Squash` is outside the `validMergeMethods` closed set (lowercase), so `merge_method` stays valid lowercase `squash` (documented debt). 24/24 AC PASS. Run-phase Route A main-direct, 9 milestones M1-M9 (`7b11d68bc`, `e0061ad34`, `cca120c70`, `5f97d32fe`, `0f13b4684`, `d0ea1c84e`, `7db2192db`) + run_commit_sha backfills. 🗿 MoAI

### Changed

- **[SPEC-GLM-EFFORT-TUNE-001](.moai/specs/SPEC-GLM-EFFORT-TUNE-001/spec.md)** — GLM effort overlay configuration tune-up: removed `builder-harness` from the coding-max override set (P1), added comments-only GLM reasoning-effort mapping exposure to both template and local `llm.yaml` (P2), and tightened overlay file comments to 3-state framing (P4) (Tier M, 18 AC: 16 PASS + 2 PASS-WITH-DEBT). **P1 Override set**: `glmCodingMaxOverrideAgents` is now singleton `{manager-develop}`; `builder-harness` removed from the override set and now reaches `reasoning-high` via the standard collapse (CG-mode cost-reduction alignment). **P2 Documentation exposure**: both `internal/template/templates/.moai/config/sections/llm.yaml` (template source) and `.moai/config/sections/llm.yaml` (local mirror) gained a comments-only mapping block describing the 3 reachable GLM reasoning states (`thinking-off` / `reasoning-high` / `reasoning-max`) and the collapse from Claude's 5-level effort vocabulary; NO runtime behavior change, NO new struct field (deferred to future user-override SPEC). **P4 Comment framing**: `glm_effort_overlay.go` comments corrected from the former "2-tier high/max" framing to the actual 3-state reality; docs-site grep found zero instances of the legacy 2-tier framing (M4 no-op). **Honesty caveat**: wire state is described as "implemented and wired; live validation of z.ai wire-effectiveness is pending" — NOT claimed as "validated" or "guaranteed" (verification-claim-integrity; carries forward MODEL-TIER-PLANTYPE-001 caveat). **Out-of-scope**: P3 (GLM wire-effectiveness live validation) is owned by SPEC-MODEL-TIER-PLANTYPE-001 follow-up #1.

- **[SPEC-HNS-PREFIX-RENAME-001](.moai/specs/SPEC-HNS-PREFIX-RENAME-001/spec.md)** — `/moai:harness` v4 Builder user-owned artifact prefix renamed from `harness-<name>` to lowercase `hns-<name>` (Tier M, 16/16 AC PASS). The rename spans four layers: **(1) Template Builder contract** — `harness-builder.md`, `harness-build-entry.md`, `moai/SKILL.md`, `moai-meta-harness/SKILL.md` (+ 5 secondary docs) now emit `hns-<name>-*` names for all six Builder artifact types. **(2) Go recognition logic** — `moai update` preservation, `moai harness list/edit/remove/doctor`, `moai doctor` skill-classification, frozen-guard, and prefix-conflict detection recognize `hns-*` as the new canonical user-owned prefix **in addition to** the two legacy generations (`harness-*`, `my-harness-*`) — tri-generation backward compatibility, verified end-to-end via `TestUpdateNamespaceHNS_TriGenerationPreservation` (7 artifacts across 3 generations survive `moai update` byte-identical, SHA-256 verified). **(3) CI guards** — the namespace-leak (`TestSplitHarnessNamespaceNoLeak`) and split-harness guards now cover both `harness-*` and `hns-*` patterns; a red-team probe (`hns-probe`) confirms the leak detector still fires on the new prefix. **(4) Local dev artifacts** — this repo's 3 dev-only specialists, 3 `harness-moaiadk-*` skills, and 1 Runner JS renamed to `hns-*` form with all cross-references updated. Directory structure is unchanged (`.claude/agents/harness/`, `.claude/commands/harness/`, `/harness:` command namespace keep their names — only artifact-name prefixes change). Zero test files deleted; full suite + cross-platform build (darwin/windows) green; template neutrality and language-neutrality guards pass with 0 leaked internal SPEC-ID references.
- **[SPEC-HARNESS-CADENCE-BUILD-001](.moai/specs/SPEC-HARNESS-CADENCE-BUILD-001/spec.md)** — `/moai:harness` v4 Builder gains cadence integration: build-time recurrence question, discovery-queue scheduling, ANALYZE-phase external research, and retroactive schedule attachment (Tier M, 35/36 AC PASS + 1 PASS-WITH-DEBT). **(1) Manifest schema** — additive-only `schedule` field (`interval`/`mechanism`/`mode`) on the harness manifest, validated in `internal/harness/v4manifest` (loop/cron fixtures, discovery-only mode enforcement, no silent mode default); zero behavior change for schedule-less manifests. **(2) Builder doctrine** — a recurrence question is asked at the PLAN→GENERATE gate round (`harness-builder.md`), and a new Recipe 4 ("Scheduled Harness Discovery") in `cadence-bridge.md` composes native `/loop` with read-only `/moai` discovery under the unchanged HARD invariant that scheduled runs never commit, never push, and never enter run-phase — findings land in a persisted `.moai/reports/cadence/<date>.md` queue for later review, not autonomous action. **(3) ANALYZE research sub-step** — the Builder's ANALYZE phase now checks official docs and Context7 before generating a harness, with MCP-unavailable fallback to WebFetch and a recorded skip rationale when omitted. **(4) Schedule Retrofit** — `harness-build-entry.md` detects scheduling intent on an existing harness command file and offers `moai harness edit` to attach a schedule after the fact, evaluated before normal re-entry. **(5) CLI schedule awareness** — `moai harness list` surfaces the declared schedule (text row + `--json` field, omitted when absent), `moai harness remove` unregisters cron/loop schedules before deletion, and `moai harness doctor` flags an invalid schedule mode as an ERROR finding. Template/live mirrors byte-identical across all 4 doctrine files; full suite (`go test ./...`, 99 packages) and `golangci-lint` (0 issues) green; cross-platform build (darwin/windows) verified. The 1 PASS-WITH-DEBT row is a pre-existing transient `StatusGitConsistency` lint warning unrelated to this change, self-resolving at sync close.

### Removed

- **Context7 MCP + gateguard advisory hook retirement** (`c745f4ea8`, doctrine cleanup, 191 files, +923/-4975) — the Context7 MCP dependency is retired across the entire doctrine/skill/rules/agent/docs surface: `mcp__context7__*` removed from every `allowed-tools` and the `settings.json` ask list, all "Context7 MCP" prose references replaced with the WebSearch/WebFetch fallback (the MoAI-native MCP-fallback strategy in `agent-common-protocol.md`), the `ddd-context7` and `context7-integration` modules dropped from `moai-workflow-testing`, and the MCP docs-site pages (`advanced/mcp-servers`, `claude-code/extensibility/mcp`; 4-locale) removed. The `gateguard-fact-force.sh` PreToolUse first-edit advisory hook (SPEC-HOOK-OFFICIAL-COMPLIANCE-001 / SPEC-OBSERVE-HYGIENE-001) is also retired — hook file + template mirror + `settings.json` registration deleted in the same sweep. Residual decimal-phase references in the docs-site command docs and `clean.md` cleaned up in the same commit. No SPEC (doctrine cleanup); run-phase Route A main-direct (Hybrid Trunk, 1-person OSS). docs-site 4-locale `settings-json.md` MCP-section consistency is tracked as a follow-up (auto-sync divergence). 🗿 MoAI

### Fixed

- **Profile display bug fix** (`f61073fe8`) — `internal/profile` GetCurrentName now consults launch.yaml ledger before defaulting to "default". The web UI previously showed "default" regardless of the active profile; now shows the actual profile name from the ledger. 🗿 MoAI

## [v3.0.0-rc11] - 2026-07-13
