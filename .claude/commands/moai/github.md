---
description: "GitHub issue fixing and PR code review automation"
argument-hint: "issues [--all | --label LABEL | NUMBER] | pr [--all | NUMBER | --merge]"
---

Skill("moai-foundation-core")

## Execution

Route to the `/moai` skill with `github` subcommand.

Raw User Input: github $ARGUMENTS
