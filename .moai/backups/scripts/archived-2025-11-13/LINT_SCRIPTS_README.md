# 문서 검증 스크립트 가이드

온라인 문서의 품질 검증 및 최적화를 위한 자동화 스크립트 모음입니다.

## 📋 스크립트 목록

### 1. `lint_korean_docs.py`

**용도**: 한국어 마크다운 문서의 포괄적인 린트 검증

**검증 항목**:
- 헤더 구조 (중복, 계층 검증)
- 코드블록 쌍 검증
- Mermaid 다이어그램 추출
- 링크 유효성 검사
- 리스트 형식 검증
- 테이블 구조 검증
- 한글 특화 검증 (전각 공백, 전각 괄호)
- 후행 공백 감지

**실행**:
```bash
python3 .moai/scripts/lint_korean_docs.py
```

**출력**:
- `.moai/reports/lint_report_ko.txt` (124K)
  - 1,241개 에러 (거짓양성: HTML 스팬 영향)
  - 351개 경고 (상대경로 링크)
  - 3,045개 정보 항목

---

### 2. `validate_mermaid_diagrams.py`

**용도**: 모든 Mermaid 다이어그램의 타입 및 문법 검증

**검증 항목**:
- 다이어그램 타입 인식 (graph, state, sequence 등)
- 설정 블록 처리 (`%%{init: ...}%%`)
- 기본 노드/엣지 문법 검증

**실행**:
```bash
python3 .moai/scripts/validate_mermaid_diagrams.py
```

**출력**:
- `.moai/reports/mermaid_validation_report.txt` (2.9K)
  - 16개 다이어그램 모두 100% 유효 ✅
  - 타입별 분류 (graph 10개, state 2개, sequence 1개)

---

### 3. `extract_mermaid_details.py`

**용도**: 모든 Mermaid 다이어그램의 상세 코드 추출

**추출 내용**:
- 파일별 다이어그램 목록
- 라인 번호 및 위치
- 전체 다이어그램 코드
- 렌더링 테스트 가이드

**실행**:
```bash
python3 .moai/scripts/extract_mermaid_details.py
```

**출력**:
- `.moai/reports/mermaid_detail_report.txt` (15K)
  - 16개 다이어그램 전체 코드 포함
  - https://mermaid.live 테스트 안내

---

### 4. `validate_korean_typography.py`

**용도**: 한글 문서의 타이포그래피 및 인코딩 검증

**검증 항목**:
- UTF-8 인코딩 확인
- 전각 공백(U+3000) 감지
- 전각 괄호/따옴표 감지
- 한글 특화 요소 분석
- 문서별 통계 (한글/영문 문자 수)

**실행**:
```bash
python3 .moai/scripts/validate_korean_typography.py
```

**출력**:
- `.moai/reports/korean_typography_report.txt` (4.8K)
  - 53개 파일 검사
  - 43개 한글 콘텐츠 파일
  - 28,543개 라인 분석
  - UTF-8: 100% 완벽 ✅

**결과**:
- 모든 파일 UTF-8 정상
- 전각 문자 최소화
- 타이포그래피 우수

---

### 5. `generate_final_comprehensive_report.py`

**용도**: 모든 검증 단계의 결과를 통합한 최종 종합 리포트 생성

**생성 내용**:
- 종합 품질 점수 (8.5/10)
- 각 Phase별 상세 결과
- 우선순위별 권장사항
- 향후 실행 계획
- 최종 체크리스트

**실행**:
```bash
python3 .moai/scripts/generate_final_comprehensive_report.py
```

**출력**:
- `.moai/reports/korean_docs_comprehensive_review.txt` (6.0K)
  - Phase별 검증 결과 요약
  - 우선순위 3단계 분류
  - 액션 아이템 및 다음 단계

---

## 🚀 빠른 시작

### 개별 검증 실행

```bash
# Phase 1: 마크다운 린트
python3 .moai/scripts/lint_korean_docs.py

# Phase 2: Mermaid 검증
python3 .moai/scripts/validate_mermaid_diagrams.py

# Phase 3: 타이포그래피 검증
python3 .moai/scripts/validate_korean_typography.py
```

### 전체 검증 자동 실행 (권장)

```bash
# 모든 단계를 순차적으로 실행
python3 .moai/scripts/lint_korean_docs.py && \
python3 .moai/scripts/validate_mermaid_diagrams.py && \
python3 .moai/scripts/extract_mermaid_details.py && \
python3 .moai/scripts/validate_korean_typography.py && \
python3 .moai/scripts/generate_final_comprehensive_report.py
```

### 결과 확인

```bash
# 모든 리포트 목록 확인
ls -lh .moai/reports/

# 최종 리포트 조회
cat .moai/reports/korean_docs_comprehensive_review.txt
```

---

## 📊 최근 검증 결과 (2025-11-09)

**문서 통계**:
- 총 파일: 53개
- 한글 콘텐츠: 43개
- 총 라인 수: 28,543개
- Mermaid 다이어그램: 16개

**종합 점수: 8.5/10** ⭐⭐⭐⭐

**주요 발견사항**:
- ✅ UTF-8 인코딩: 100% 완벽
- ✅ Mermaid 다이어그램: 100% 유효
- ✅ 타이포그래피: 우수
- ⚠️ 거짓양성 오류: 1,241개 (HTML 스팬 영향)
- ⚠️ 상대경로 링크: 351개 (거짓양성)

---

## 🔧 커스터마이제이션

각 스크립트는 다음과 같이 수정 가능합니다:

### 검증 대상 문서 변경

```python
# lint_korean_docs.py 내
self.file_count = len(korean_files)
```

### 검증 규칙 추가

```python
def _check_custom_rule(self, content):
    # 새로운 검증 로직 추가
    pass
```

---

## 📝 권장사항

1. **정기 실행**: 주 1회 이상 검증 실행
2. **CI/CD 통합**: GitHub Actions에 통합
3. **자동화**: pre-commit hook으로 자동 검증
4. **모니터링**: 대시보드에서 메트릭 추적

---

## 🐛 알려진 문제

| 문제 | 원인 | 해결책 |
|------|------|--------|
| H1 중복 감지 1,241개 | Material Icons HTML 스팬 | 제목에서 HTML 제외 |
| 링크 경고 351개 | 상대경로 상대경로 | mkdocs 환경에서 정상 작동 |

---

## 📚 참고 자료

- [CLAUDE.md - 온라인 문서 작성 가이드](../../CLAUDE.md#-온라인-문서-작성-가이드라인-online-documentation-standards)
- [최종 종합 리포트](../reports/korean_docs_comprehensive_review.txt)
- [마크다운 린트 상세 결과](../reports/lint_report_ko.txt)

---

**마지막 업데이트**: 2025-11-09
**검증 버전**: Phase 1-4 완료
**품질 점수**: 8.5/10 ⭐⭐⭐⭐
