# Service Settings (CLAUDE.md Reference)
# This file is auto-loaded by CLAUDE.md for service configuration

service:
  # Service type: glm (GLM CodePlan recommended)
  type: "glm"

  # API key is stored in .env file
  # GLM_API_KEY for GLM CodePlan
