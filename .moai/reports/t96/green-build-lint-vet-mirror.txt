GREEN — make build / vet / lint / embed-presence / mirror parity
(all observed verbatim in this run, against this worktree at HEAD 400dde787
on branch WT-t96, with the new files in place)

1. make build (env-scrubbed one-compound form) — exit 0; tail:

  catalog.yaml updated successfully (12652 bytes)
  go build -ldflags "-s -w -X github.com/modu-ai/moai-adk/pkg/version.Version=v3.1.0 -X github.com/modu-ai/moai-adk/pkg/version.Commit=400dde787 -X github.com/modu-ai/moai-adk/pkg/version.Date=2026-08-16T20:02:38Z" -o bin/moai ./cmd/moai
  make-exit=0

   catalog.yaml entry regenerated by gen-catalog-hashes:
   moai-kanban-foreman hash fdae87cb7228172ebc29e289f8d6c71c2fd6d70a1a9ed6a14560c6de21e4c6a6
   (all other entry hashes unchanged by the regeneration)

2. go vet ./internal/template/ — exit 0 (VET-OK)

3. golangci-lint run ./internal/template/... — "0 issues." (exit 0)

4. Embedded-presence proof in the compiled binary:
   strings bin/moai | grep -c "Kanban foreman iteration"  -> 1
   strings bin/moai | grep -c "moai-kanban-foreman"       -> 6

5. Mirror parity (local <-> template source), cmp exit 0 both files:
   MIRRORS-BYTE-IDENTICAL (loop.md), MIRROR-PARITY-OK (after all edits)
   sizes: loop.md 1082 bytes; moai-kanban-foreman/SKILL.md 8219 bytes

6. Queue-watch primitive check (worktree-local probe file, plain commands):
   same content   -> cksum 1463931957 37 (twice, identical: no false change)
   mutated content-> cksum 3259350833 48 (inequality fires)
   missing file   -> "cksum: ...: No such file or directory" exit 1
                     (the snippet's [ -f "$f" ] branch handles this case)
   probe file removed after the check.
