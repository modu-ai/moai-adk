# v3.1.1 문서 4로케일 반영 계획

moai-adk-go · release/v3.1.1 · 2026-08-20

근거: `.moai/reports/v311-docs/{readme-ko-inventory, post-31-change-inventory, docs-4locale-surface-map}.md`

## 지표

| 값 | 항목 |
|---|---|
| 339 | v3.1.0..HEAD 커밋 |
| 33 | 사용자 노출 변경 (신규 19 / 변경 14) |
| 3 | CHANGELOG `[3.1.1]`이 실제 기록한 항목 |
| 778×4 | README 줄 수 (4로케일 완전 동형) |

## 릴리즈 상태 (블로커)

- PR #1585 (`release/v3.1.1` → main) 머지됨 — 2026-08-20 08:50
- `pkg/version/version.go:8` = `v3.1.1`; CHANGELOG `[3.1.1] - 2026-08-20` 존재
- **`v3.1.1` git 태그 없음** (`git tag -l 'v3.1*'` → v3.1.0까지)
- **GitHub 릴리즈 v3.1.1이 Draft** (08-15 생성 = 머지보다 5일 앞선 스테일 초안). Latest = v3.1.0

→ M0 게이트: 문서 머지 전까지 태그 + 게시 필요.

## ① 문서에 없는 신규 표면 (13)

| 표면 | 근거 | 비고 |
|---|---|---|
| `moai clean --home` | `internal/cli/clean.go:50`, `clean_home.go` | dry-run 기본, `--force` 실삭제, 허용목록 4범주 |
| `MOAI_HOME` | `internal/config/envkeys.go:22`, `internal/paths/paths.go` | 절대경로만. 셸 훅 미준수 |
| `moai doctor` → Home Disk Usage | `internal/cli/doctor.go:188`, `doctor_disk.go:67` | 권고 항목 |
| `state.home_retention_days` | `internal/cli/clean_home.go:91` | HOME 티어 전용, 기본 30일, `0`=비활성 |
| `moai memory doctor\|archive` | `internal/cli/memory.go:136,281` | CLI 표 누락 |
| `moai tokens record` | `internal/cli/tokens.go:405,420` | 풀별 토큰 원장 |
| `crosssession.yaml` 3키 | `templates/.moai/config/sections/crosssession.yaml` | `inbound`/`isolate_machines`/`dialog_expiry` |
| `cache.yaml` 4키 | `.moai/config/sections/cache.yaml` | 프롬프트 캐시 TTL |
| `statusline.forge` | `templates/.moai/config/sections/statusline.yaml`, `internal/statusline/forge.go` | GitLab 지원, 표시 변경 |
| 맨 `/loop` = 칸반 포먼 | `templates/.claude/loop.md`, `moai-kanban-foreman/SKILL.md` | 사용자 프로젝트로 배포 |
| `moai-domain-design-dna` 스킬 | `templates/.claude/skills/moai-domain-design-dna/` | Design DNA JSON |
| ast-grep 룰셋 6개 배포 | `templates/.moai/config/astgrep-rules/{go,security}/` | init/update가 배포 시작 |
| `MOAI_SESSION_PID` | `internal/cli/launch_session_pid.go` | POSIX 전용 |

## ② 실측과 다른 숫자 (5)

| README 주장 | 실측 | 위치 | 확인 |
|---|---|---|---|
| ref 스킬 10 | **11** | L384, L699 | `ls templates/.claude/skills/moai-ref-*` |
| domain 스킬 5(L384)/8(L701) | **7** | L384, L701 | 같은 방식. 양쪽 다 틀림 |
| 전체 CLI 36 | **49** | L663, L695 | 워크트리 트리 재빌드 바이너리의 `moai --help` 최상위 열거 (`help`/`completion`/자기참조 `moai` 제외) |
| 설정 탭 10(alt) vs 11(본문) | **11** | L377 vs L380·L693 | 본문이 11개 열거 |
| `.moai/config/sections/` 6개 | 배포 **33개** | L552-563 | "자주 쓰는 6개" 의도 → 문장 한정 필요 |

**충돌이나 오류 아님**: L429 "12-에이전트" vs L569 "11 에이전트" — 커스텀 11 + 내장 `Explore` 1 = 12. 둘 다 맞음. 관계 설명 한 문장 추가로 해소.

**정확 확인됨(수정 불필요)**: MCP 21개(실측 21), `moai graph`(L684 존재), GLM-5.3/1M(L636,L640-645), manager-lead(L438), 5열 보드(L46,L84).

## ③ 동작이 바뀐 것 (7)

| 변경 | 근거 | README 영향 |
|---|---|---|
| `ast_grep_gate.rules_dir` 기본값 `""` → `.moai/config/astgrep-rules` | `templates/.moai/config/sections/gate.yaml` | 미언급, 폴백 제거됨 |
| GLM-5.3 = 1M | `internal/config/envkeys.go:314` | 이미 정확 — 확인만 |
| `manager-kanban` → `manager-lead` | `.claude/agents/moai/manager-lead.md` | 이미 정확 — 확인만 |
| 보드 6열 → 5열 | `kanban-dispatch-detail.md:39` | 이미 정확 — 확인만 |
| `moai update` 삭제 전 백업 | 커밋 `aefaddb71`, `update_dryrun_preview.go` | 미언급 |
| 설치 스크립트 REST 레이트리밋 회피 | `install.sh`/`.ps1`/`.bat` | 미언급 |
| 웹 콘솔 Cross-Session 패널 | `internal/web/screens.templ` | L380 반영됨, L377 alt만 스테일 |

## 마일스톤

정본 = `README.ko.md`. ko 확정 후 en → ja/zh 파생 (`hns-oss-docs-readme-sync`). 4파일 778줄·72헤딩 동형성이 유일한 구조적 제약.

- **M0 (게이트)** — 릴리즈 마감: `v3.1.1` 태그 + Draft 재생성·게시 + CHANGELOG 보강. M6 머지 전까지. M1~M5와 병행 가능.
- **M1 (ko)** — ②표 5건 정정 + 12/11 에이전트 관계 한 문장. 줄 수 증가 없음.
- **M2 (ko)** — L40 아래 `### v3.1.1에서 더해진 것` 신설: 홈 정리 3종, 크로스세션, 캐시, forge, `/loop` 포먼. **H2 신설 금지**(동형성 뼈대).
- **M3 (ko)** — CLI 표(L676-696): `moai memory`/`moai tokens` 행 추가, `clean --home`·doctor 명시, 총계 36→49.
- **M4 (ko)** — 설정 절(L550-591): `crosssession`/`cache`/`state`/`statusline.forge` 추가. `rules_dir` 기본값 변경은 별도 한 줄.
- **M5 (ko)** — 스킬 절(L382-385, L697-702) 실측 재작성 + `design-dna` 소개. `moai update` 백업 개선 한 줄.
- **M6 (en/ja/zh)** — 파생 + 동형성 검증(줄 수·헤딩 수·이모지·URL). `hns-oss-docs-verify`.
- **M7 (docs-site)** — 신규: `cli-reference/{memory,tokens}`, `advanced/home-hygiene`, `core-concepts/cross-session`(잠정). 갱신: `advanced/statusline`, `utility-commands/moai-clean`, `cli-reference/doctor`, `advanced/kanban-mode`. 메뉴 누락 3건 동시 처리(`workflow-commands/moai-goal` 중복, `cli-reference/ast-grep`, `core-concepts/book`). 무경고 hugo 빌드로 검증.

## 범위 판단 (3)

1. **CHANGELOG도 수정** — `[3.1.1]`에 `Changed`/`Fixed` 하위절 부재, 33건 중 30건 미기록. README L703-706이 CHANGELOG를 가리키므로 링크가 헛돎. M0에 포함.
2. **기존 숫자 오류(②)도 동시 수정** — 엄밀히는 3.1 이전 오류지만 같은 문단을 어차피 수정하고, 4로케일 파생 2회가 훨씬 비쌈.
3. **내부 변경 제외** — always-loaded 다이어트, 룰 파일, `internal/paths` 리팩터, 모드 카탈로그 개명, 테스트 하네스. CHANGELOG가 "사용자 노출 표면 없음"으로 명시.

## 위험

| 위험 | 완화 |
|---|---|
| 4로케일 동형성 파손 (유일한 검증 가능 불변식) | M6 별도 마일스톤 + 줄 수/헤딩 수 대조를 종료 게이트로 |
| 태그 없이 문서 선행 → 릴리즈 링크 404 | M0을 M6 머지 선행 조건으로 고정 |
| 숫자가 또 낡음 (README가 코드 파생이 아님) | 이번은 수기 정정, "숫자 자동 검증"은 별도 카드(범위 밖) |
| docs-site 신규 페이지 과잉 (4×4=16파일) | 신규 최소화, 기존 페이지 갱신으로 흡수 (M7 이미 반영) |

## 완료 판정

- `git tag -l v3.1.1` 출력 있음, `gh release view v3.1.1`이 Draft 아님
- README 4파일 줄 수 동일, `grep -c '^#\{2,3\} '` 4파일 동일
- ②표 5개 수치가 실측 일치 — 세는 명령을 근거로 첨부
- ①표 13개 표면이 README 또는 docs-site에서 각 1회 이상 검색됨
- `hugo --printPathWarnings` 무경고 + sitemap 생성
- 본문 장식 이모지 0건, URL 블랙리스트 0건
- CHANGELOG `[3.1.1]`에 `Added`/`Changed`/`Fixed` 존재, 33건 귀속
