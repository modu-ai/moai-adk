--- FAIL: TestSyncSHASlot_SilentOnSHA (0.93s)
    lint_syncsha_test.go:128: sha-short: expected 0 findings on a well-formed SHA, got 1: [{testdata/syncsha/sha-short/progress.md 9 warning SyncSHASlotFormat `sync_commit_sha` holds "a6bbbf82b", which is neither a commit SHA nor a recognized backfill placeholder, so no close will be triggered to repair it and no reader can tell what the slot was meant to say. Write the commit SHA that carried the sync phase, or — while the backfill is genuinely still owed — the canonical placeholder `pending-backfill` (a `-`-suffixed member of that family, e.g. `pending-backfill-sync`, is equally admitted). A trailing annotation after the value is ignored and needs no removal. false}]
    lint_syncsha_test.go:128: sha-quoted: expected 0 findings on a well-formed SHA, got 1: [{testdata/syncsha/sha-quoted/progress.md 9 warning SyncSHASlotFormat `sync_commit_sha` holds "a6bbbf82b", which is neither a commit SHA nor a recognized backfill placeholder, so no close will be triggered to repair it and no reader can tell what the slot was meant to say. Write the commit SHA that carried the sync phase, or — while the backfill is genuinely still owed — the canonical placeholder `pending-backfill` (a `-`-suffixed member of that family, e.g. `pending-backfill-sync`, is equally admitted). A trailing annotation after the value is ignored and needs no removal. false}]
    lint_syncsha_test.go:128: sha-annotated: expected 0 findings on a well-formed SHA, got 1: [{testdata/syncsha/sha-annotated/progress.md 9 warning SyncSHASlotFormat `sync_commit_sha` holds "a6bbbf82b", which is neither a commit SHA nor a recognized backfill placeholder, so no close will be triggered to repair it and no reader can tell what the slot was meant to say. Write the commit SHA that carried the sync phase, or — while the backfill is genuinely still owed — the canonical placeholder `pending-backfill` (a `-`-suffixed member of that family, e.g. `pending-backfill-sync`, is equally admitted). A trailing annotation after the value is ignored and needs no removal. false}]
FAIL
FAIL	github.com/modu-ai/moai-adk/internal/spec	1.300s
FAIL
