--- FAIL: TestSyncSHASlot_SilentOnPlaceholder (0.52s)
    lint_syncsha_test.go:149: placeholder: expected 0 findings on a sanctioned backfill placeholder, got 1: [{testdata/syncsha/placeholder/progress.md 9 warning SyncSHASlotFormat `sync_commit_sha` holds "pending-backfill", which is neither a commit SHA nor a recognized backfill placeholder, so no close will be triggered to repair it and no reader can tell what the slot was meant to say. Write the commit SHA that carried the sync phase, or — while the backfill is genuinely still owed — the canonical placeholder `pending-backfill` (a `-`-suffixed member of that family, e.g. `pending-backfill-sync`, is equally admitted). A trailing annotation after the value is ignored and needs no removal. false}]
    lint_syncsha_test.go:149: placeholder-suffixed: expected 0 findings on a sanctioned backfill placeholder, got 1: [{testdata/syncsha/placeholder-suffixed/progress.md 9 warning SyncSHASlotFormat `sync_commit_sha` holds "pending-backfill-sync", which is neither a commit SHA nor a recognized backfill placeholder, so no close will be triggered to repair it and no reader can tell what the slot was meant to say. Write the commit SHA that carried the sync phase, or — while the backfill is genuinely still owed — the canonical placeholder `pending-backfill` (a `-`-suffixed member of that family, e.g. `pending-backfill-sync`, is equally admitted). A trailing annotation after the value is ignored and needs no removal. false}]
FAIL
FAIL	github.com/modu-ai/moai-adk/internal/spec	2.181s
FAIL
