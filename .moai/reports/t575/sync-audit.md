# sync-audit.md — SPEC-DOCS-TODO-TEMP-GUARD-001 (card t575)

Auditor: sync-auditor (independent) | Date: 2026-09-13
Tree: worktree `.claude/worktrees/t575`, branch `WT-docs-todo-locales`, HEAD `acd8b4b40`
Scope: `git diff e83b4ec20..HEAD` (8 files: 4 locale pages, CHANGELOG, census artifact, SPEC progress/spec)
Profile: `.moai/config/evaluator-profiles/default.md` (flat weighted; must-pass = Functionality + Security)

## Evaluation Report

SPEC: SPEC-DOCS-TODO-TEMP-GUARD-001
Overall Verdict: **PASS** (harmonic mean 94/100; no blocking findings; both must-pass dimensions pass)

### Dimension Scores

| Dimension | Score | Verdict | Evidence (verbatim, this run, this tree, HEAD acd8b4b40) |
|-----------|-------|---------|----------------------------------------------------------|
| Functionality (40%) | 95/100 | PASS | 7/7 ACs re-verified independently. AC-001: 4/4 files EXIST. AC-002: `grep -c '\.moai/state/todo/'` → ko:2 en:2 ja:2 zh:2. AC-003: old 248-forms → 0/0/0/0. AC-004: line-start anchors → 0/0/0/0. AC-005: `grep -c '^#'` → 29/29/29/29. AC-006: census at `.moai/reports/t575/docs-census.md`, 21 claims = 8 FIXED + 4 TRUE + 8 not-verified + 1 template (sums check), cited from progress.md §E.2(g)/§E.4 + CHANGELOG. AC-007: `git diff --stat e83b4ec20..HEAD` → no Go/test/template/README path (8 files: 4 docs + census + SPEC×2 + CHANGELOG). New sentences verified TRUE against code: `state_dir.go:26-41` (temp branch :29-31 keys on `TempOriginReason` + abs-`MOAI_HOME` escape; home branch :32-40; home-unresolvable :32-34 → local), `temp_origin.go:60-62` (`{os.TempDir(), "/tmp", "/var/folders"}` — matches the doc triple; /var/tmp excluded, doc does not claim it), `todo_root.go:135-144` (substitute root = launch base). All 8 new sentences (4 locales × L59/L248) carry the temp-origin condition + absolute-`MOAI_HOME` precedence + project-local path. Deeper sweep: `grep -n 'moai/db'` → exactly 1 hit per file (L59, fully conditioned); zero residual unconditional home-path claims. §E.2(a) pre-edit captures byte-verified against `git show e83b4ec20:` L59/L248. §E.2(d) two-probe disclosure honest: substring anchors do hit the new conditioned sentences (e.g. ko L59 contains `대기열은 \`~/.moai/db` mid-line); line-start re-anchors are sound. |
| Security (25%) | 100/100 | PASS | Docs-only diff reviewed in full — no executable content, no input surfaces, no auth/session/code paths touched. Secrets probe `grep -rn -iE 'api[_-]?key\|secret\|password\|token[[:space:]]*='` over the 4 changed pages + census → 0 hits. The change improves safety-documentation accuracy: the temporary-origin guard (a home-protection mechanism keeping test/temp projects out of the operator's home) is now described correctly, including the absolute-`MOAI_HOME` override boundary. No OWASP surface applies; no findings of any severity. |
| Craft (20%) | 90/100 | PASS | Coverage gate N/A per acceptance.md §D.3 (docs-only; no Go packages touched). Applicable craft gates pass: heading parity 29×4 (verbatim above); native register verified by reading all 4 new sentence pairs — ko/ja/zh read as natural native written prose, no translationese (ko「일반적인 프로젝트 디렉터리라면…다만」/ ja「通常の…であれば…ただし」/ zh「对于常规项目目录…不过」follow each file's existing voice); 4-locale same-change duty honored. Defects (non-blocking): census artifact Location cell names 2 phantom filenames (F1); hugo build not run locally (recorded gap — CI/Vercel owns it). |
| Consistency (15%) | 92/100 | PASS | Commit chain follows Conventional Commits with SPEC scope + card id in every message (5 commits e83b4ec20→acd8b4b40, all `(SPEC-DOCS-TODO-TEMP-GUARD-001)` + `(t575)`); close subject names the full SPEC-ID exactly once with the `3-phase close` infix. Frontmatter transitions verified on the correct commits: `draft`@e83b4ec20 → `in-progress`@55adec04f → `completed`@03d72705b; `sync_commit_sha: pending-backfill-sync` placeholder at 03d72705b (L118), backfilled to `03d72705b` by acd8b4b40 (progress.md only, 1 line) — D3 exemption honored. CHANGELOG entry format matches sibling entries (SPEC link + prose + 🗿 trailer). Minor: CHANGELOG follow-up attribution is set-level, not per-claim (F3). |

**Harmonic mean**: 4 / (1/95 + 1/100 + 1/90 + 1/92) = **94/100**

### Findings

- **F1** [Low] [optional] `.moai/reports/t575/docs-census.md:23` (rows 17-20) — Location cell names `README.en.md` and `README.zh-CN.md`; neither file exists in this tree (actual: `README.md`, `README.ko.md`, `README.ja.md`, `README.zh.md`). The row's substance is verified: `grep -n 'factory/factory.db'` hits line 80 in all four ACTUAL README files, so the claim count (4) and line number (80) are correct — only two filenames are wrong. Confidence: high. Required fix: correct the Location cell to the actual four filenames (evidence-artifact repair only; no shipped-doc impact; acceptance.md §D.5's own glob form `README*.md` is not affected).
- **F2** [Low] [optional] `.moai/specs/SPEC-DOCS-TODO-TEMP-GUARD-001/acceptance.md` AC-007 — the allowance list ("plus `.moai/reports/t575/` and the SPEC directory") omits `CHANGELOG.md`, which the realized diff contains. The sync-phase close legitimately adds the CHANGELOG entry (manager-docs B12 duty; transparently recorded in progress.md §E.4 `changelog_entry:`), and REQ-006's stated requirement ("no Go code, test, or template file") holds — plan.md §D's prohibition list (Go/test/template/README) also holds. AC-authoring omission, not a scope escape. Confidence: high. Required fix: none for the landed change; if the SPEC is ever amended, extend the AC-007 allowance to name CHANGELOG.md.
- **F3** [Low] [optional] `CHANGELOG.md:18` — the census clause binds "owned by follow-up cards t704/t705/t706" to the "8 factory/README claims" phrase, but the census's own per-row notes map t704→template doc (row 21) and t706→factory/README (rows 13-20), while t705 (verified in queue: StateDirForRoot vs TempOriginRefusal temp-git anomaly) corresponds to no census row. Set-level attribution is accurate against progress.md §E.4 `out_of_scope_followups: [t704, t705, t706]`; per-claim binding is imprecise. Confidence: medium-high (queue read `moai todo list --limit 0`). Required fix: reword to attribute per-subject, or leave as the set-level statement it factually is.
- **F4** [Info] [optional] `docs-site/content/*/utility-commands/moai-todo.md:59` — the `<base>` placeholder: for a git repository whose primary checkout is physically under a temp root, `StateDirForRoot` runs on the resolved canonical root, so the project-local queue hangs from the primary checkout, while `<base>` reads as the launch directory. Not false for the class the sentence primarily describes (non-git temp origins, where root = base exactly), and plan.md §B.4 explicitly chose origin-keyed wording; the underlying StateDirForRoot/TempOriginRefusal divergence is owned by t705 with a reproduce-first HARD step. Confidence: medium. Required fix: none now; revisit only if t705 classifies the anomaly as intended behavior worth documenting.

### Claim / Baseline-attribution / Gaps / Residual-risk

- **Claim**: all 7 ACs PASS on the landed tree; new doc sentences are true against the code truth table; 3-phase close integrity holds.
- **Baseline-attribution**: every evidence cell above was measured in this run, in this worktree, at HEAD `acd8b4b40` (diff base `e83b4ec20`). Pre-edit captures re-verified via `git show e83b4ec20:...`.
- **Gaps** (explicitly NOT observed): hugo build not run locally (CI/Vercel owns it — recorded in §E.3); census rows 9-12's TRUE classification spot-checked (line exists, describes a lookup candidate list) but the search-order semantics were not re-derived in depth (out of AC scope); factory-path truth status unverified by design (t706); t705's temp-git anomaly not reproduced here (its own HARD first step, owned by t705).
- **Residual-risk**: the untouched linked-worktree sentence ("resolves to the primary checkout's project key") is adjacent to t705's subject — under a temp-origin primary checkout the queue is project-local at the primary checkout rather than home-keyed; if t705 lands a behavior change, these pages may need one more pass. Follow-ups t704/t705/t706 remain open and queued.

### Recommendations

- Repair the census Location cell (F1) — one-line evidence-artifact fix; may ride any future commit touching `.moai/reports/t575/` or be left as-is with this finding as the record.
- No doc or code change required for the verdict. The landed wording satisfies plan.md §B.4's origin-keyed standard; treat t705's outcome as the trigger for any further wording pass.
