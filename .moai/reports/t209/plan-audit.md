# SPEC Review Report: SPEC-WORKTREE-REAPER-001

Iteration: 1/3
Verdict: **FAIL**
Overall Score: **0.55** (Tier L PASS threshold = 0.85, `spec-workflow.md` § SPEC Complexity Tier)

Reasoning context ignored per M1 Context Isolation. The dispatch prompt's
characterisations were treated as hypotheses to test, not as premises; every
judgment below is anchored to an artifact line or to a command run in
`/Users/goos/MoAI/moai-adk-go/.claude/worktrees/t209` (branch
`WT-worktree-reaper`, HEAD `cd0cee1b8`) on 2026-08-24.

---

## Must-Pass Results

- **[PASS] MP-1 REQ number consistency** — `grep -o 'REQ-WR-[0-9]*' spec.md | sort -u` returns `REQ-WR-001 … REQ-WR-017`, 17 unique, zero-padded, no gaps, no duplicates; `grep -c '^- \*\*REQ-WR-'` = 17. Within the Tier L ceiling of 25.
- **[PASS] MP-2 GEARS format compliance (requirement layer)** — judged against the 17 `REQ-WR-XXX` entries in `spec.md` §D only; the Given/When/Then entries in `acceptance.md` are the verification layer and are graded under Group 4. All 17 carry a `shall`/`shall not` response clause with a named subject. Two are mislabelled `(Event-detected)` (spec.md:L99, L118) — not a GEARS pattern name — but the sentences themselves are correct Event-driven form (`When …, the … shall …`). Independent mechanical confirmation: `moai spec lint .moai/specs/SPEC-WORKTREE-REAPER-001/spec.md` → exit 0, `✓ No findings`.
- **[PASS] MP-3 YAML frontmatter validity** — spec.md:L1-15 carries all 12 canonical fields (`id`, `title`, `version` `"0.1.0"`, `status: draft`, `created`/`updated` ISO, `author`, `priority: P1`, `phase: "v3.1.4 target"` (not a prohibited stage name), `module`, `lifecycle: spec-anchored`, `tags`), plus optional `tier: L`. No rejected snake_case alias. `moai spec lint` exit 0.
- **[N/A] MP-4 language neutrality** — single-language SPEC (Go, `module: internal/cli`); no template-bound or 16-language content.
- **[PASS] MP-5 D7 cross-SPEC reconciliation** — one external reference, `SPEC-SESSION-WORKTREE-001`; `.moai/specs/SPEC-SESSION-WORKTREE-001/spec.md` exists, `status: completed` (not retired/superseded/archived). No BLOCKING.
- **[PASS] MP-6 D8 cross-platform discipline** — `grep -rn 'syscall' .moai/specs/SPEC-WORKTREE-REAPER-001/` returns 0 matches → auto-PASS. (Note: the Windows liveness surface is still a real gap, reported as D7 below, but it is not a D8 `syscall` finding.)
- **[PASS] MP-7 clarification gate** — `grep -rn '\[NEEDS CLARIFICATION' .moai/specs/SPEC-WORKTREE-REAPER-001/` returns exactly one line, `progress.md:14` (`- No \`[NEEDS CLARIFICATION]\` markers remain.`), which is a negation, not a marker. `research.md` does not exist (see D16).

No must-pass criterion fails. **The FAIL is driven by the rubric scores**, principally Testability — see D1.

---

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|---|---|---|---|
| Clarity | 0.75 | 0.75 | Prose is unusually precise and the resolution order (design.md:L31-37) and fail-closed table (design.md:L91-99) are exhaustive. Deductions: REQ-WR-005 "behave as it does today" (spec.md:L105-108) is baseline-relative and unmeasurable in the artifact; "the anchor guard" (spec.md:L112-128) never names which call sites it binds (D3); "lock reason" is used for both git's reason and the full porcelain line (design.md:L80-87 vs acceptance.md:L96-99). |
| Completeness | 0.60 | 0.50-0.75 | All required sections present; §G Out of Scope is exemplary — five `### Out of Scope — <topic>` H3 sub-headings each with specific bullets (spec.md:L184-216). Deductions: the M3 options table (design.md:L149-153) omits the shipped capability that already does most of O3-b (D4); no requirement or edge case covers the removal class M1 newly creates (D2); Tier L is declared but `research.md`, a required Tier L artifact, is absent (D16). |
| Testability | **0.30** | 0.25-0.50 | **Measured**: `go test ./internal/cli/ -run TestPRMergeCleanup_GhNoAnswerConsultsGitFallback -count=1` on the pre-implementation tree → `ok github.com/modu-ai/moai-adk/internal/cli 0.830s [no tests to run]`, **exit 0** — while acceptance.md:L47-49 asserts "**This is the criterion that fails on the pre-implementation tree**". 14 of 18 criteria are `-run <test-that-does-not-yet-exist>` with expected observation "ok, exit 0", which is exactly what today's tree produces. See D1. |
| Traceability | 0.55 | 0.50 | `grep -o 'REQ-WR-[0-9]*' acceptance.md | sort -u` returns only `REQ-WR-004 REQ-WR-013` — 2 of 17 requirements are explicitly cited by any criterion. REQ-WR-009, -010, -011 (registry half), -014, -016, -017 have no criterion that exercises them. Mapping is otherwise implicit by section ordering only. |

Aggregate: (0.75 + 0.60 + 0.30 + 0.55) / 4 = **0.55** < 0.85.

---

## Defects Found (structured defect-list)

**D1.** AC-FALSIFIABILITY — `acceptance.md`:L23-241 (14 criteria) — Every criterion of the form `go test ./internal/cli/... -run <NewTestName> -count=1` **passes on the pre-implementation tree**, because Go exits 0 with `ok … [no tests to run]` when the `-run` pattern matches nothing. The stated expected observation ("`ok`, exit 0") is byte-identical to today's output, so the criterion set cannot fail. acceptance.md:L47-49 additionally makes a positive claim that AC-WR-002 "fails on the pre-implementation tree", which is contradicted by measurement — an unobserved verification claim (`verification-claim-integrity.md` §1). Ironically the file applies exactly the right discipline to grep tokens (acceptance.md:L6-11, `mergeStateUndetermined` measured at 0 hits) and then fails to apply the same reasoning to `-run`. — **Evidence**: `go test ./internal/cli/ -run TestPRMergeCleanup_GhNoAnswerConsultsGitFallback -count=1` → exit 0, `ok  github.com/modu-ai/moai-adk/internal/cli  0.830s [no tests to run]`. — Severity: **critical** — Class: **blocking** — **Required fix**: make every test-shaped criterion observe that the named test *ran and passed*, not merely that the package exited 0. Concretely, replace the command with a form that fails on a missing test, e.g. `go test ./internal/cli/ -run '^TestX$' -v -count=1 | tee /tmp/x.log; grep -c '^--- PASS: TestX' /tmp/x.log` with expected `1`, or `go test -json -run '^TestX$' … | jq -e '[.[]|select(.Action=="pass" and .Test=="TestX")]|length==1'`. Then re-measure each criterion on the pre-implementation tree and record the observed pre-implementation result per criterion (the acceptance.md grep-token baseline note is the template).

**D2.** MERGE-FALLBACK-WIDENS-REMOVAL — `spec.md` §D.1 / `design.md` §A / `acceptance.md` §E — `git branch --merged origin/main` reports **every branch with no commits of its own** as merged, not only deleted-branch merges. M1 therefore adds a removal class the SPEC never analyses: a freshly created `WT-*` branch with zero commits, clean tree, no PR → gh no-answer → git fallback says merged → removal candidate. This is not hypothetical: `WT-worktree-reaper`, the branch of the live session authoring this SPEC, is at line 95 of `git branch --merged origin/main` because `git rev-list --count origin/main..HEAD` = 0. It survives today only because the tree is locked and carries untracked files. The SPEC's safety argument (spec.md:L53-57, design.md:L39-42) establishes only that git never *overrides* gh; it never establishes what git *adds*. — **Evidence**: scratch repo — `git branch WT-fresh` at HEAD then `git branch --merged main` lists `WT-fresh`; in-tree — `grep -n 'WT-worktree-reaper' <(git branch --merged origin/main)` → `95:* WT-worktree-reaper`; `git rev-list --count origin/main..HEAD` → `0`; `git status --porcelain | wc -l` → 2 (untracked only). — Severity: **critical** — Class: **blocking** — **Required fix**: add a requirement and an edge case for the zero-unique-commit branch. Either exclude it explicitly (`git rev-list --count origin/main..<branch> == 0` ⇒ treat as *no answer*, not merged — matching the intent of the existing `moai worktree clean --stale` predicate, which pairs the merge check with a unique-commit check), or state the acceptance of the new removal class with its blast radius argued. Add `EC-8: branch with zero commits of its own` to the acceptance.md edge-case table.

**D3.** ANCHOR-REPAIR-SCOPED-TOO-NARROWLY — `spec.md` §D.2 (REQ-WR-006…011) vs `plan.md`:L106-108 — The requirements speak of "the anchor guard" generically, but the plan places the lock-based guard in a new `internal/cli/session_worktree_lock.go` wired only into `prMergeCleanup`. There is a **second, more dangerous consumer** of the same 1-of-5-blind `session.LiveAnchoredSessions`: `cleanStaleWorktrees` in `internal/cli/worktree/clean.go:L160-165`, reached by `moai worktree clean --stale --yes`. That path sweeps **every** registered worktree (its `WorktreeProvider.List()` is `git worktree list --porcelain`, `internal/core/git/worktree.go:L76-91` — all 154 trees, not just `WT-*`) and removes in bulk. Repairing only `prMergeCleanup` leaves the blind guard on the surface with the wider blast radius. — **Evidence**: `internal/cli/worktree/clean.go:162` `case len(session.LiveAnchoredSessions(wt.Path, time.Now())) > 0:`; `internal/core/git/worktree.go:85` `execGit(ctx, w.root, "worktree", "list", "--porcelain")`. — Severity: **critical** — Class: **blocking** — **Required fix**: place the lock-based anchor decision in `internal/session` beside `LiveAnchoredSessions` (or export it) so both call sites gain it, and add a requirement naming both consumers. If `worktree clean --stale` is deliberately left blind, say so in §G Out of Scope with the reason.

**D4.** M3-DECIDED-ON-AN-INCOMPLETE-OPTION-SET — `design.md` §C:L149-180 / `plan.md`:L110-128 — The options table considers O3-a (widen the prefix), O3-b (new report-only inventory), O3-c (doc only) and never mentions `moai worktree clean --stale`, which already ships and already does most of O3-b: it enumerates **all** worktrees including the 43 non-`WT-*` ones, prints a per-tree keep-reason covering dirty state, merge state and live-session anchor, **previews by default**, and requires `--yes` to remove. That last property is verbatim the "forward constraint" REQ-WR-014 proposes to invent (design.md:L175-180). Consequently design.md:L172-173's premise — that this is "exactly the work that is not being done today" — is an unverified recommendation premise (`verification-claim-integrity.md` §1.1 surface 4): the capability exists; what is unverified is only whether operators invoke it. Adding a parallel surface without that comparison also violates the Simplicity ladder step 2 (reuse an existing capability before writing new code). — **Evidence**: `internal/cli/worktree/clean.go:20-36` (`Use: "clean"`, `--stale`, `--yes`, `--base`), L139-215 (`cleanStaleWorktrees`: keep-reason per tree, preview unless `apply`), L217-235 (`staleKeepReason`: dirty check + `IsBranchMerged`). — Severity: **major** — Class: **blocking** — **Required fix**: add O3-d ("extend `worktree clean --stale`") to the options table and either select it or record the measured gaps that rule it out (candidates: no `--json`, base defaults to local `main` rather than `origin/main`, anchor source is the same blind registry, no explicit non-`WT-*` filter). Re-derive D3 from the completed table.

**D5.** AC-WR-011-IS-A-DESTRUCTIVE-ACT-WITH-NO-OBSERVATION — `acceptance.md`:L173-187 — The criterion runs `moai session list --json >/dev/null 2>&1` in a repository where `auto_cleanup: true`, *after M1 lands*. That is precisely the sweep that becomes able to see the 99 merged trees, so executing this criterion performs the bulk removal that `spec.md` §G declares out of scope ("performing the mass removal is an operator action", spec.md:L195-197). The command discards stdout, which is the only record of what was removed, and asserts nothing beyond t207's survival — so it passes identically whether 0 or 99 trees were deleted. It also invokes the installed `moai` binary, which may not contain the change under test (repo rule: binaries lag; `~/go/bin/moai` is a separate artifact from this tree). — **Evidence**: acceptance.md:L182-185; `.moai/config/sections/workflow.yaml:124` `auto_cleanup: true`; measured `git branch --merged origin/main | grep -c 'WT-'` → 99. — Severity: **major** — Class: **blocking** — **Required fix**: capture and assert the sweep output (`moai session list --json > /tmp/sweep.log 2>&1`, then assert the removal-notice count against an explicitly enumerated expected set), build and invoke the tree-local binary (`go build -o ./bin/moai ./cmd/moai && ./bin/moai session list --json`), and state whether the mass removal is intended at this point or must be deferred behind a temporarily-false `auto_cleanup`.

**D6.** SAFETY-INVERSION-MISCHARACTERISED-IN-BOTH-DIRECTIONS — `plan.md` §F:L61-65 / `design.md` §D:L188-191 — The claim is that landing M1 first "means the backlog sweep runs under the 1-of-5-blind guard", implying live sessions are at risk. Measured, that is **overstated for locked trees**: `git worktree remove` without force refuses a locked tree (`fatal: cannot remove a locked working tree …`, exit 128), and all 5 live anchors in this repository are locked — so the sweep cannot remove any of them today, with or without M2. It is simultaneously **understated for unlocked trees**: an unlocked anchored tree is a shape the repository can produce, because MoAI's own session-worktree materialiser creates `WT-<session>-<subcommand>` trees with a plain `git worktree add` and no lock (`internal/cli/session_worktree.go:218`, branch prefix `WT-` at :40). design.md:L108-113 waves at this as "a shape not measured but not excluded"; it is in fact produced by code in the package under repair, toggle-gated. M2's lock guard does **not** close that case — only the registry does, and the registry is the blind source. — **Evidence**: scratch repo — `git worktree lock --reason 'locked claude session tX (pid 999999 start Sun)'` then `git worktree remove` → `fatal: cannot remove a locked working tree, lock reason: …`, exit 128; `internal/cli/session_worktree.go:216-218`; live-anchor survey — 5 of 5 lock-carrying trees (`awk` over `git worktree list --porcelain`), `lsof -a -d cwd -p …` confirming claude cwds at t207/t209/t210/t212/t213. — Severity: **major** — Class: **blocking** — **Required fix**: restate the ordering caveat in terms of what is actually unguarded: locked anchors are already protected by git's non-forced removal; the residual risk is the **unlocked** anchor, which M2 does not eliminate. Either add a requirement covering unlocked anchors (e.g. a `git status`-independent cwd probe, or fail-closed on any tree whose branch matches the session-worktree naming), or record it explicitly as accepted residual risk with the toggle state that bounds it.

**D7.** PROBE-SEAM-CONTRADICTION — `spec.md` REQ-WR-008 (L118-120) / `acceptance.md` AC-WR-009a (L132-143) vs `plan.md`:L96-98 — The requirement and its criterion demand that "the liveness probe cannot determine an answer" yield *anchored*, and AC-WR-009a lists "a liveness probe returning an error" as one of four table cases. The plan mandates reusing `session.isProcessAlive`, which returns a bare `bool` and cannot express indeterminacy: on unix any non-`EPERM` error collapses to `false` (= "dead"), and on Windows it unconditionally returns `true`. As specified, the error case is either unimplementable or reachable only through a test-only seam that production can never enter. — **Evidence**: `internal/session/anchor_pid_unix.go:16-25`; `internal/session/anchor_pid_windows.go:14-18`. — Severity: **major** — Class: **blocking** — **Required fix**: specify the seam signature in design.md — `func(pid int) (alive bool, determined bool)` (or `(bool, error)`) — and state how the existing bool-only probe maps into it per platform; or drop the error case from AC-WR-009a and say why indeterminacy cannot arise.

**D8.** TRACEABILITY-MOSTLY-IMPLICIT — `acceptance.md` (whole file) — Only `REQ-WR-004` and `REQ-WR-013` are named by any criterion. Six requirements have no criterion that exercises them: REQ-WR-009 (the *either* half — no criterion covers registry-anchored-while-lock-absent), REQ-WR-010 (registry retained), REQ-WR-011 for the registry source, REQ-WR-014, REQ-WR-016, REQ-WR-017's dirty limb. — **Evidence**: `grep -o 'REQ-WR-[0-9]*' acceptance.md | sort -u` → `REQ-WR-004 REQ-WR-013`. — Severity: **major** — Class: **blocking** — **Required fix**: add a `Covers: REQ-WR-XXX` line to every criterion, add a criterion for the registry-only anchor path (the existing `TestPRMergeCleanup_AnchoredSessionSkipsRemoval` can anchor it), and either give REQ-WR-014 a criterion (e.g. a guard test asserting no removal flag exists on the inventory surface) or demote it from a requirement to a design constraint.

**D9.** AC-COUNT-WRONG — `acceptance.md`:L3 and `progress.md`:L8 — Both state "17 acceptance criteria (M1 five, M2 six, M3 four, cross-cutting two)". §B actually carries seven (AC-WR-006, -007, -008, -009a, -009b, -010, -011), for 18 total. — **Evidence**: `grep -o '^### AC-WR-[0-9a-b]*' acceptance.md` → 18 ids. — Severity: minor — Class: blocking — **Required fix**: correct both counts to 18 / "M2 seven", or renumber 009a/009b to 009/010 and shift.

**D10.** AC NAMES NON-EXISTENT "EXISTING" TESTS — `acceptance.md` AC-WR-001 (L32) and AC-WR-005 (L85) — Both are described as existing M8 regression guards, but `TestPRMergeCleanup_GhMergedRemoves` and `TestPRMergeCleanup_GhAbsentFallback` do not exist; the real names are `TestPRMergeCleanup_GhPresentMergedRemoves` and `TestPRMergeCleanup_GhAbsentBranchMergedRemoves` / `…_GhAbsentEmitsBlindnessNoticeOnce`. As written they match nothing and silently pass (compounding D1). — **Evidence**: `grep -n '^func Test' internal/cli/session_worktree_prmerge_test.go`. — Severity: minor — Class: blocking — **Required fix**: use the exact existing names, anchored (`-run '^TestPRMergeCleanup_GhPresentMergedRemoves$'`).

**D11.** AC-WR-016 OVERSTATES THE EXISTING TEST — `acceptance.md`:L250-256 — Claims "no git seam is invoked (asserted by call counters on all seams)" and that this is an existing test that must remain green unmodified. `TestPRMergeCleanup_ToggleOffNoOp` asserts only that `wtList` was not called, plus empty output. — **Evidence**: `internal/cli/session_worktree_prmerge_test.go:81-94`. — Severity: minor — Class: optional — **Required fix**: either weaken the criterion text to match the existing assertion, or state that the test is extended (which contradicts "unmodified").

**D12.** AC-WR-015 PASSES TODAY BY CONSTRUCTION — `acceptance.md`:L230-240 — `-run 'NoAskUserQuestion'` matches ten-plus existing tests across `internal/cli`, so the command is green on the pre-implementation tree and binds nothing about the new inventory file. — **Evidence**: `grep -rn 'func Test.*NoAskUserQuestion' internal/cli/` → 10+ matches including `TestNewInventory_NoAskUserQuestion`. — Severity: minor — Class: blocking — **Required fix**: name the new guard test exactly and assert it ran (per D1's fix form).

**D13.** MEASUREMENT DRIFT IN THE 111/43 SPLIT — `spec.md`:L71-76, `design.md`:L134, `investigation.md` measured-state table — Re-measured now: 154 worktree entries, **110** `branch refs/heads/WT-` lines → 44 non-`WT-*`, not 111/43. — **Evidence**: `grep -c '^worktree ' <porcelain>` → 154; `grep '^branch refs/heads/WT-' <porcelain> | wc -l` → 110. — Severity: minor — Class: optional — **Required fix**: re-measure and restate, or annotate the figures with their measurement timestamp so drift is not read as an error.

**D14.** DEAD-LOCK TREES CANNOT ACTUALLY BE REAPED — `design.md`:L95, L101-104 — The table permits the lock source to assert "not anchored" for a confirmed-dead PID, but git still refuses to remove a locked worktree regardless of PID liveness, so such a tree yields a `PR-merge cleanup failed (fatal: cannot remove a locked working tree …)` notice on **every** sweep, forever. The SPEC never discusses unlocking, and no criterion observes this path. — **Evidence**: scratch-repo `git worktree remove` on a locked tree → exit 128; the identical notice already appears in `investigation.md`:L20-25 for t212. — Severity: minor — Class: blocking — **Required fix**: state the policy — either the confirmed-dead branch is inert (and say so, so the design table stops implying a removal that cannot happen) or the sweep unlocks confirmed-dead locks first, in which case that is a new authority requiring its own requirement and criterion.

**D15.** STALE `origin/main` NOT ARGUED — `acceptance.md` §E edge-case table (L278-286) — EC-7 covers an *absent* `origin/main`; nothing covers a stale one (the sweep never fetches). The direction happens to be safe (a behind ref yields fewer ancestors, hence fewer removals; a false positive needs a history rewrite), but the SPEC asserts nothing about it, so the safety is unestablished rather than established. — **Evidence**: `internal/cli/session_worktree_prmerge.go:254-256` (`git branch --merged origin/main`, no fetch anywhere in `prMergeCleanup`). — Severity: minor — Class: optional — **Required fix**: add an edge case stating the direction argument and the one exception (force-pushed/rewritten `origin/main`).

**D16.** TIER L DECLARED WITHOUT ITS ARTIFACT SET — `spec.md`:L14, L19 / missing `research.md` — Tier L is justified as "3 milestones, cross-package surface, one behavioural decision"; the tier criteria are >1000 LOC **or** >15 files **or** constitutional, and the plan names 5-6 files. If Tier L stands, `research.md` is a required Tier L artifact (`spec-workflow.md` § SPEC Complexity Tier) and is absent; if Tier M is correct, the PASS threshold is 0.80 and design.md is a bonus artifact. — **Evidence**: `ls .moai/specs/SPEC-WORKTREE-REAPER-001/` → spec, plan, acceptance, design, progress (no research.md); `spec-workflow.md`:L138-142. — Severity: minor — Class: blocking — **Required fix**: either add `research.md` (the investigation report is most of it already) or re-tier to M and restate the justification against the LOC/file criteria.

**D17.** GEARS LABEL ERRORS — `spec.md`:L99 (REQ-WR-003), L118 (REQ-WR-008) — Both labelled `(Event-detected)`, which is not one of the five GEARS patterns; both sentences are correct Event-driven form. — Severity: minor — Class: optional — **Required fix**: relabel `(Event-driven)`.

**D18.** BASELINE-RELATIVE REQUIREMENT — `spec.md`:L105-108 (REQ-WR-005) — "shall behave as it does today" is not verifiable from the artifact; it depends on an unversioned external state. AC-WR-005 pins one observable (the notice fires once), which is narrower than the requirement's text. — Severity: minor — Class: optional — **Required fix**: state the behaviour ("the git fallback is the sole source and the squash-blindness notice is emitted exactly once per invocation") rather than referring to the current implementation.

**D19.** LOCK-REASON VS PORCELAIN-LINE CONFLATION — `design.md`:L80-87, `acceptance.md`:L96-99 — The "verbatim measured line" `locked claude session t207 (pid 36912 …)` is the porcelain **line**; git's stored reason is `claude session t207 (pid 36912 …)` and `locked ` is git's own prefix (confirmed: a lock created with reason `locked claude session tX (…)` renders as `locked locked claude session tX (…)`). AC-WR-006 asks that the entry "carries that reason string" without saying whether the `locked ` prefix is included, which the run phase will have to guess. — **Evidence**: scratch repo, `git worktree list --porcelain` → `locked locked claude session tX (pid 999999 start Sun)`; a reason-less lock renders as a bare `locked` line. — Severity: minor — Class: optional — **Required fix**: state the stored field's exact content, and confirm the bare-`locked` line maps to the "empty reason ⇒ anchored" row.

### Positive findings (recorded, not defects)

- **Scope discipline is sound.** The `RelocateSession` relocation defect is held out cleanly (`spec.md`:L206-211) with a stated composition argument, not smuggled in. Nothing in the SPEC builds a new reaper; the reframing from the dispatch's premise is evidence-backed and explicitly declared (spec.md:L23-38). §G Out of Scope is the strongest section in the document.
- **The reframing itself is verified.** `prMergeCleanup` exists, `auto_cleanup: true` at `.moai/config/sections/workflow.yaml:124`, and the two defect diagnoses are literally true of the code: `branchMergedForCleanup` gates the fallback on `gh` being absent (`session_worktree_prmerge.go:189-192`), `ghPRViewStateReal` returns `""` on any error (:230-236), `parseWorktreeList`'s switch has no `locked` case (:93-110), and the anchor guard is registry-only (:168).
- **The 99-tree figure is reproducible**: `git branch --merged origin/main | grep -c 'WT-'` → 99.

---

## Regression Check

Not applicable — iteration 1.

---

## Recommendation

FAIL. The document reads extremely well and its diagnosis of the shipped reaper is accurate and reproducible; the failure is not in the analysis but in the verification layer and in two unexamined safety consequences. Fix in this order:

1. **Rebuild the criterion set so it can fail** (D1). Every test-shaped criterion must observe that the named test ran and passed. Then re-measure each criterion against the pre-implementation tree and record the observed result per criterion, the way acceptance.md already does for grep tokens. Until this is done, nothing else in acceptance.md carries evidence weight.
2. **Close the removal class M1 creates** (D2). Add the zero-unique-commit case as a requirement plus an edge case; the existing `worktree clean --stale` predicate (dirty check **and** unique-commit check) is the pattern to copy.
3. **Re-scope the anchor repair to both consumers** (D3), or declare `worktree clean --stale` out of scope with the reason. Placing the lock guard in `internal/session` fixes both call sites for the same diff.
4. **Redo the M3 decision with the complete option set** (D4). `moai worktree clean --stale` already delivers the enumerate-and-report behaviour, previews by default, and requires `--yes` — the option table must engage with it before a new surface is justified.
5. **Restate the M2/M1 ordering caveat against what is actually unguarded** (D6): locked anchors are already protected by git; the unlocked anchor is the residual, and M2 does not remove it.
6. **Make AC-WR-011 non-destructive and self-evidencing** (D5), and resolve the probe-seam contradiction (D7).
7. Sweep the remaining items: traceability annotations (D8), counts and test names (D9, D10, D11, D12, D13), the dead-lock policy (D14), the stale-ref edge case (D15), the tier/artifact decision (D16), and the three wording fixes (D17, D18, D19).

Optional-class findings (D11, D13, D15, D17, D18, D19) are surfaced for the orchestrator's discretion and do not by themselves justify the FAIL; the verdict rests on D1-D8, D9, D10, D12, D14, D16.

---

## Evidence appendix — commands run in this audit

```
git rev-parse --show-toplevel                → /Users/goos/MoAI/moai-adk-go/.claude/worktrees/t209
git branch --show-current / rev-parse HEAD   → WT-worktree-reaper / cd0cee1b8
moai spec lint .../spec.md                   → exit 0, "✓ No findings"
grep -o 'REQ-WR-[0-9]*' spec.md | sort -u    → REQ-WR-001…017 (17, no gaps/dupes)
grep -o '^### AC-WR-[0-9a-b]*' acceptance.md → 18 ids
grep -o 'REQ-WR-[0-9]*' acceptance.md|sort -u→ REQ-WR-004 REQ-WR-013
grep -rn 'NEEDS CLARIFICATION' <spec dir>    → 1 (progress.md:14, a negation)
grep -rn 'syscall' <spec dir>                → 0
go test ./internal/cli/ -run TestPRMergeCleanup_GhNoAnswerConsultsGitFallback -count=1
                                             → exit 0, "ok … 0.830s [no tests to run]"
git worktree list --porcelain                → 154 worktree entries, 5 'locked' lines, 110 WT- branches
git branch --merged origin/main | grep -c WT-→ 99 ; grep -n WT-worktree-reaper → line 95
git rev-list --count origin/main..HEAD       → 0
git status --porcelain | wc -l               → 2 (untracked)
lsof -a -d cwd -p <claude pids>              → 5 claude cwds at worktrees/t207,209,210,212,213
scratch: worktree lock + worktree remove     → "fatal: cannot remove a locked working tree", exit 128
scratch: branch at HEAD + branch --merged    → the zero-commit branch IS listed as merged
scratch: worktree list --porcelain           → "locked <reason>"; reason-less lock → bare "locked"
```
