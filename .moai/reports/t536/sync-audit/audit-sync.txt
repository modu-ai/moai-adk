=== sync_commit_sha / status frontmatter ===
5:status: completed
7:updated: 2026-09-08
=== CHANGELOG entry ===
### Fixed

- **[SPEC-TODO-HOME-TEMP-GUARD-001](.moai/specs/SPEC-TODO-HOME-TEMP-GUARD-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t536, Tier M). **The production home fallback for the todo queue root now refuses a launch base whose origin is a temporary directory: no `~/.moai/todo/<key>` root is resolved to and none is created, the substitute root is the launch base itself, and the command path prints one guidance line naming both the matched temp root and the substitute before continuing at exit 0.** The gap this closes: `homeTodoQueueRoot` fails open for any base git cannot resolve, so a non-git run inside a temp directory keyed a home queue off a path that later disappears — an orphan nothing can reach or clean. Reachability was measured, not assumed: the one recovered production contamination `~/.moai/todo/t203-probe-d7a16ea2` re-derives exactly as `sha256("/tmp/t203-probe")[:4]` under `TodoQueueProjectKey`, and that inversion also settles two design points — the temp-root set `{os.TempDir(), "/tmp", "/var/folders"}` needs `/tmp` as a first-class member rather than a redundancy (on the reference machine `os.TempDir()` reports `/var/folders/…` and would not have caught it), and both sides of every comparison need normalizing, because the contamination arrived in the *unresolved* spelling. `/var/tmp` is deliberately excluded (it survives reboots, so the "the directory later disappears" premise is weak there while a long-lived real project under it is likelier) — an exclusion in the fail-open direction, which keeps today's behavior. The discriminant `TempOriginReason` (new file `internal/kanban/temp_origin.go`) normalizes with one rule — `filepath.EvalSymlinks` when the path exists, lexical `filepath.Clean` when it does not, adopted from `internal/cli/launcher.go`'s `resolveSymlinks` for its documented GOOS determinism — and compares by path **component** (`filepath.Rel`-based `pathWithin`), never by raw string prefix, so `/tmpfoo` is not read as living under `/tmp`. It fails open on an empty or un-absolutizable base: the two misclassification directions are not symmetric, since a false negative leaves today's behavior intact while a false positive withdraws a real project's queue. **Predicate placement is load-bearing, not stylistic:** the guard is evaluated before the home-resolution `ok` check in both resolvers, so on the intersection "temporary origin ∧ home unresolvable" — which is reachable, the two conditions are not exclusive — the return is fixed at the launch base rather than at `homeTodoQueueRoot`'s no-home value `resolveStateDir(base, false)`, the one layer-misaligned return in that file; placing the predicate after the `ok` check would have re-adopted that shape by ordering alone. The temp-root set is reached through an exported call-time seam `TempRootsFn`, which is a requirement rather than a convenience (REQ-THG-009): this repository's [HARD] isolation discipline puts every fixture under `t.TempDir()`, which is by definition inside `os.TempDir()`, so without the seam no "non-temporary non-git base" fixture is constructible at all and AC-THG-003 — the sole producing criterion for REQ-THG-005 — would have no fixture; `internal/cli` and `internal/web` tests need the same escape, hence exported, and it is read at call time because a stub is always installed after package initialization. On the command path `warnTempOriginQueueRefusal` (`internal/cli/todo.go`) runs from the todo command's `PersistentPreRun` and consults the read-only `TempOriginRefusal`, so the guidance exists without the resolvers acquiring an output channel — the pure `ResolveTodoQueueRoot` the web console imports still neither writes nor speaks on any branch. The exit code is unchanged deliberately: refusing the home queue is already the whole of the protection, and failing the command would withdraw working behavior from every script running `moai todo` inside a temp directory without preventing anything further. Preserved and asserted: `TodoQueueProjectKey` derivation, the git branch (a repository under a temp root resolves via `primaryCheckoutRoot` and never reaches the guard), adopt-not-shadow on non-temporary fallbacks, and the purity of `ResolveTodoQueueRoot`; nothing under `~/.moai/todo` is deleted, moved, or migrated (`find ~/.moai/todo -maxdepth 1 -type d | wc -l` → **344**, the plan's baseline, cleanup of the 343 existing orphans being card t542's scope). **Two sibling SPECs are narrowed, not withdrawn, and neither was modified by a character.** `SPEC-WEB-TODO-QUEUE-001` AC-WTQ-008 inherits its `Given` from AC-WTQ-006, whose subject is a directory "git cannot resolve to a primary checkout" — "temporary" appears nowhere in that premise — so this card shrinks the population reaching the home fallback while the guarantee stays true for every non-temporary non-git base; its producing test was preserved by moving to a non-temporary fixture. `SPEC-STATE-ANCHOR-001` AC-SA-011's producing test moves onto the **second branch REQ-SA-011 already documents** ("a mutant that produces no pollution shall be reported alongside the one that does, naming the guard boundary it reveals"), and this card is the implementation of the decision that SPEC's §5 explicitly deferred to the operator and predicted would ship as a follow-up card. All 14 impacted tests across 4 packages were dispositioned individually rather than in aggregate — 6 preserved by migration, 1 intentional update (the intersection assertion `dir/.moai/state/todo` → `dir`, on the requirement's authority rather than fixture convenience), 6 non-temporary copies each asserting the discriminant's verdict on its own base directly, and 1 **accepted loss**: `cli.TestAxisACanaryHomeSweep_TodoFamily` cannot have a copy because an `exec.Command` child process does not inherit a package-level seam, so it was not modified by a line and skips by default. **8 acceptance criteria (AC-THG-001..008): 8 PASS, 0 FAIL, 1 PASS-WITH-DEBT** — AC-THG-002 (symlink-spelling equivalence) passes on darwin/arm64 only; its linux cell belongs to CI and a local macOS PASS is not reused as a linux verdict. The mutant proof is reported with its own boundary, per the second branch above: mutant E (discriminant forced to constant not-temporary) fails all three AC-THG-001 branches, but **E alone cannot demonstrate the pollution** — against the `internal/cli` gate-bypass test it stops at the precondition guard, since an absence assertion says nothing unless the base actually classifies temporary — so the control is route (i), reverting only M2's two wiring points in this tree to reconstruct the pre-guard state and re-measuring here rather than citing an earlier run; that state reproduced **1** polluted entry under the canary HOME while the restored tree reproduces 0, and the two runs differ by the guard alone. **One finding is reported rather than repaired:** plan.md M3's literal fixed-SHA baseline `git diff 412c8cb14..HEAD -- internal/statusline/ internal/config/ internal/hook/ internal/session/ internal/stateanchor/` is **not** empty (18,650 bytes across 6 files) because `412c8cb14` predates this lane's absorb of develop at `6b71fdaa5`, so the range carries sibling cards' work (git log attributes all six files to t401 and t537); measured against this card's own commits, `6b71fdaa5..HEAD` over the same paths is **0 bytes**, so the property the criterion defends holds and it is the fixed-SHA baseline that went stale. The plan.md wording is left uncorrected — that body belongs to manager-spec — and the same shape is a recurrence risk for any no-change criterion pinned to a SHA older than an absorb. Coverage: `internal/kanban/temp_origin.go` (new), `internal/kanban/todo_root.go`, `internal/cli/todo.go`; the touch-forbidden packages (`internal/statusline`, `internal/config`, `internal/hook`, `internal/session`, `internal/stateanchor`) are card-attributed diff 0. `go test ./internal/kanban/ ./internal/web/ ./internal/cli/ -count=1` → `ok` on all three; `go vet` on the same three silent; `GOOS=windows GOARCH=amd64 go build ./...` exit 0. Gaps carried, not papered over: the linux and windows cells are unmeasured (darwin/arm64 only, and this lane does not push, so no CI result is citable here); `go build` does not compile test files, so the tests' windows compilability is unverified; only those three packages were run (`go test ./...` is deliberately not run locally per repository discipline) and `golangci-lint` was not run — both are CI's. No README or docs-site edit: the change is an internal CLI-behavior guard, and its runtime guidance line is the discoverability path that matters — but the four `utility-commands/moai-todo.md` locale pages carry one sentence ("a project without git metadata keeps its queue at `~/.moai/todo/<project-key>/`") that this guard now narrows for temp-root bases, recorded here as a doc-accuracy follow-up rather than edited inside a sync commit scoped to an internal guard. Evidence: `.moai/reports/t536/` (`guard-boundary.md` — the REQ-SA-011 boundary report; `m3/` — mutant, control, batch, and AC output; `m3/d8-literal.diff` and `m3/d8-card.diff` — both sides of the D8 finding). This sync commit carries the 3-phase close (`spec.md` frontmatter `in-progress → completed`, `status` + `updated` only), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` is written as the `pending-backfill` placeholder in this commit — a commit cannot cite its own hash — and the resolved value is backfilled in a following commit.
- **[SPEC-SEAM-GREENFIELD-001](.moai/specs/SPEC-SEAM-GREENFIELD-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t544, Tier S, Class B defect fix). **`moai web`'s settings console no longer fails the first Save on a tab whose section file does not exist with HTTP 500 — the first save now creates the file carrying the submitted edit, mode 0644.** The seam write path (`internal/settings/yamlpatch/yamlpatch.go` `atomicWrite`) called `os.Stat` on the target unconditionally and returned every stat error, `IsNotExist` included, so an absent section file failed the write — while the read layer (`PatchFile`) treats the same absence as greenfield and starts from an empty document. An absent section file is a normal state, not corruption: projects initialized from older templates legitimately lack the newer section keys (`mcp`, `gate`, `crosssession`, `report` were each added after 3.0), so every such project hit the 500 on its first MCP-tab save. The fix splits the pre-write stat: an error satisfying `os.IsNotExist` proceeds with the documented default mode `defaultFilePerm = 0644` — the measured convention of every file in `internal/template/templates/.moai/config/sections/`; the `os.CreateTemp` default 0600 was rejected in spec.md §4 because the first file's mode mismatch would then be preserved by every later edit's mode-preservation — while any other stat error (a non-directory path component, a permission failure) keeps its wrapped `yamlpatch: stat %s: %w` error, so absent tolerance does not widen into all-stat-error tolerance, and the same-directory temp-file + rename atomic-write convention is untouched. Two existing tests encoded the defect behavior as their expectation and were rewritten to the repaired contract: `TestYAMLPatchAtomicWriteErrors/"stat missing target"` (flagged at plan phase) and `TestYAMLPatchErrors/"missing file"` (found during the M2 regression run — the second defect-encoding test the plan-phase inventory missed). Guard evidence: the unit and web-level greenfield guards were captured RED on the pre-fix tree (`b91372794`, guards applied uncommitted at M1 — `yamlpatch: stat …: no such file or directory` at unit level, HTTP 500 with a `section config write failed: yamlpatch: stat …` banner at web level, which proves the submission reached `PatchFile` rather than being silently skipped by the unchanged-submission gate); five mutant probes ran against the committed fix, and mutant E (destroying present-file mode preservation) survived the entire existing suite — recorded per the SPEC's REQ-8 and closed with a new guard `TestAtomicWritePreservesPresentFileMode` whose re-test mutant E′ (destruction value 0600→0777) is caught. This closes the observation recorded as out-of-scope in the SPEC-WEB-WRITE-SAFETY-001 entry below ("a missing `mcp.yaml` still makes a full-form save return 500"). It is the second instance of the same defect class — one layer reads an absent file as defaults while another treats it as an error; t517's gate-vs-render divergence was the first — and a sweep of every absent read/write pair on the seam surface is a recorded follow-up, out of scope here.
  - Coverage: `internal/settings/yamlpatch/yamlpatch.go` (absent branch + `defaultFilePerm`), `internal/settings/yamlpatch/yamlpatch_test.go`, `internal/settings/yamlpatch/atomicwrite_mode_unix_test.go` (umask-independent 0644 assertion, `!windows`), `internal/web/write_safety_test.go` (web-level first-save guard). 6 acceptance criteria (AC-001..006) all PASS; `go test -count=1 ./internal/settings/... ./internal/web/...` → `ok` 4 packages, re-measured at the final run tree `ada4eda3b`; `golangci-lint run` on both packages 0 issues; `go vet` clean; `GOOS=windows GOARCH=amd64 go build ./internal/...` exit 0. Evidence: `.moai/specs/SPEC-SEAM-GREENFIELD-001/progress.md` §E.2 + `.moai/reports/t544/`. This sync commit carries the 3-phase close (`spec.md` frontmatter `in-progress → completed`, `status` + `updated` only), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` is written as the `pending-backfill-sync` placeholder in this commit — a commit cannot cite its own hash — and the resolved value is backfilled in a following commit.
- **[SPEC-STATE-ANCHOR-VALIDATE-001](.moai/specs/SPEC-STATE-ANCHOR-VALIDATE-001/spec.md)** — sync-phase close (3-phase plan→run→sync, card t537, Tier S; hardening follow-up to [SPEC-STATE-ANCHOR-001](.moai/specs/SPEC-STATE-ANCHOR-001/spec.md)). **The state-anchor resolver no longer trusts session-derived directory candidates blindly: a `project_dir` or `original_cwd` from the statusline stdin payload is returned only when it validates — an absolute path (`filepath.IsAbs`) naming an existing directory (`os.Stat` succeeding with directory mode) — and an invalid candidate now falls through to the next step of the precedence chain instead of being adopted.** The gap this closes: `Resolve` returned any non-empty session field verbatim, so a relative `project_dir`, a stale `project_dir` pointing at a deleted directory, or a `project_dir` naming a regular file would each have been adopted as the anchor — sending `.moai/state/` reads and writes to a path that is not a project root (relative candidates would anchor to whatever the process working directory happened to be at resolve time, re-opening the GH #1694 stray-directory shape through the resolver that repaired it). The predicate `isAnchorCandidate` (in `internal/stateanchor/stateanchor.go`) implements the check with three deliberate non-behaviors: an empty value still means "absent" and falls through trivially (REQ-SAV-003), the candidate is returned verbatim on success — no `EvalSymlinks` rewriting of the anchor value (REQ-SAV-006), and no git membership check is added — the git budget stays entirely with chain step 3, `FromDirectory` (REQ-SAV-004). Validation failure is a rejected candidate, not a process error: the chain falls through to `original_cwd`, then the git walk-up, preserving the REQ-SA-003 graceful degradation (a stale `project_dir` must not switch the state writes off). The git resolution path (`FromDirectory`) is byte-identical to the SPEC-STATE-ANCHOR-001 tree — the diff touches only `stateanchor.go` and `stateanchor_test.go`, with `FromDirectory` unchanged (`git diff 52f863f36..HEAD` shows zero `FromDirectory` lines). All 8 acceptance criteria (AC-SAV-001..008) PASS — RED-first for the two adopt-bad-candidate cases (`TestResolve_RelativeProjectDirFallsThrough`, `TestResolve_StaleProjectDirFallsThrough`) at commit `3b39eef8f`, all 13 package tests GREEN at `5df939476`; a file-candidate mutant (`TestResolve_ProjectDirFileCandidateRejected`) proves `os.Stat` success alone does not adopt a candidate — directory mode is required. `go test ./internal/stateanchor/ -count=1` exit 0; `go test -cover` reports 100.0% of statements (baseline ≥85%); `go vet` and `golangci-lint run` clean; `GOOS=windows GOARCH=amd64 go build ./internal/stateanchor/` exit 0. This sync commit carries the 3-phase close (`spec.md` frontmatter `in-progress → completed`, `status` + `updated` only), the `progress.md` §E.4 signal, and this entry; `sync_commit_sha` is written as the `pending-backfill-sync` placeholder — a commit cannot cite its own hash — and the resolved value is backfilled in a following commit.
=== docs-site claim (4 locales) ===
docs-site/content/en/utility-commands/moai-todo.md:215:Run it inside a linked worktree and the queue still **resolves to the one queue of the primary checkout** — the contract is one repository, one queue. A `moai todo add` from a card worktree lands in the same file the lead and the foreman loop read. Projects without git metadata keep the queue at `~/.moai/todo/<project-key>/backlog.db`.
docs-site/content/ja/utility-commands/moai-todo.md:215:リンクされたワークツリーの中で実行しても、キューは**プライマリチェックアウトのキューひとつに帰属**します — リポジトリ 1 つにキュー 1 つという契約です。カードのワークツリーで `moai todo add` をすると、リードとフォアマンループが読む同じファイルに追加されます。git メタデータのないプロジェクトは `~/.moai/todo/<project-key>/backlog.db` にキューを置きます。
docs-site/content/ko/utility-commands/moai-todo.md:215:연결된 워크트리 안에서 실행해도 대기열은 **프라이머리 체크아웃의 큐 하나로 귀속**됩니다 — 저장소 하나에 큐 하나라는 계약입니다. 카드 워크트리에서 `moai todo add`를 하면 리드와 포어맨 루프가 읽는 같은 파일에 추가됩니다. git 메타데이터가 없는 프로젝트는 `~/.moai/todo/<project-key>/backlog.db`에 큐를 둡니다.
docs-site/content/zh/utility-commands/moai-todo.md:215:在链接型 worktree 里执行时，队列也**归属为 primary 检出的那一个队列**——一个仓库一条队列的契约。在卡片 worktree 里执行 `moai todo add`，追加的就是主控和工头循环读取的同一份文件。没有 git 元数据的项目把队列放在 `~/.moai/todo/<project-key>/backlog.db`。
--- locale file inventory ---
-rw-r--r--@ 1 goos  staff  19365 Sep  7 23:27 docs-site/content/en/utility-commands/moai-todo.md
-rw-r--r--@ 1 goos  staff  24381 Sep  7 23:27 docs-site/content/ja/utility-commands/moai-todo.md
-rw-r--r--@ 1 goos  staff  22707 Sep  7 23:27 docs-site/content/ko/utility-commands/moai-todo.md
-rw-r--r--@ 1 goos  staff  18259 Sep  7 23:27 docs-site/content/zh/utility-commands/moai-todo.md
