# SPEC Review Report: SPEC-CODEMAPS-ACCURACY-001
Iteration: 1/2 (Tier M ceiling)
Verdict: PASS
Overall Score: 0.987 (harmonic mean; Tier M PASS threshold 0.80)
Skip-eligible: YES (verdict PASS and score ≥ 0.80 — Phase 1 verdict re-execution skip candidate only; Implementation Kickoff Approval remains mandatory and is never auto-bypassed by skip-eligibility)

Auditor: plan-auditor (iteration 1). Tree: `.claude/worktrees/t304` @ `65196a5a7` (= origin/develop, verified `git rev-parse HEAD` == `git rev-parse origin/develop`). Reasoning context ignored per M1 Context Isolation — only the four artifacts and the tree were audited.

## Must-Pass Results

- [PASS] MP-1 REQ number consistency — `REQ-CMA-001`…`REQ-CMA-010` sequential, no gaps, no duplicates (grep-sorted unique list = exactly 10, spec.md:L76–L85). `AC-CMA-001`…`AC-CMA-009` sequential (acceptance.md:L9–L17). Tier M ceilings respected (10 REQ ≤ 16; 9 AC ≤ 16).
- [PASS] MP-2 EARS/GEARS compliance (requirement layer only) — all 10 REQ-XXX entries match GEARS patterns: Ubiquitous (001, 005: "…shall name a path that exists…", "…shall cite the real `internal/session` API…"), Event-driven When (002, 003, 004, 007, 008), State-driven While (006, 009), negative forms (009 "shall not be written to", 010 "shall not treat freshness-gate green … as accuracy proof"). GEARS keywords kept as protocol tokens per spec.md:L74. The Given-When-Then entries are correctly placed in the verification layer (acceptance.md), never presented as REQs.
- [PASS] MP-3 YAML frontmatter validity — all 12 canonical fields present with correct types (spec.md:L1–L15): `id` string, `title` quoted, `version: "0.1.0"` quoted semver, `status: draft` (enum), `created/updated: 2026-09-02` ISO, `author`, `priority: P2` (enum), `phase: "v3.1.4 target"` (release target, not a prohibited lifecycle-stage value), `module` path-like, `lifecycle: spec-anchored` (enum), `tags` comma string; `tier: M` optional. No rejected snake_case aliases (`created_at`/`updated_at`/`labels`/`spec_id` absent).
- [PASS/N-A] MP-4 language neutrality — N/A: SPEC is scoped to this Go repository's own codemaps docs, `internal/graph`/`internal/cli`, and the codemaps skill. No language-specific tool name is hardcoded into template-bound content (REQ-CMA-007's skill edit names only the language-neutral `moai graph check` citations row and a markdown-notation convention).
- [PASS] MP-5 D7 cross-SPEC reconciliation — verification verb executed on spec.md: referenced SPEC-IDs = self + `SPEC-KANBAN-RENAME-001` (exists, `status: completed`) + `SPEC-CLEANUP-EVALUATOR-001` (exists, `status: completed`). None in {retired, superseded, archived}; no not-found references. No BLOCKING finding.
- [PASS] MP-6 D8 cross-platform discipline — `grep -c 'syscall' spec.md` → 0. Auto-pass (no syscall introduction; no build-tag obligation triggered).
- [PASS] MP-7 clarification gate — `grep -rn 'NEEDS CLARIFICATION' .moai/specs/SPEC-CODEMAPS-ACCURACY-001/` → 0 matches (rc=1). `research.md` absent, which is the normal Tier M artifact set (spec+plan+acceptance+progress); `plan.md` scanned clean.

Spec lifecycle audit (`mcp__moai__spec_audit`, project_root = this worktree, filter SPEC-CODEMAPS-ACCURACY-001): 1 finding — `EraAutoDetected` INFO, era V3R5, heuristic "H-3 (§E.2 present, sync_commit_sha missing)", grandfathered. This is the known/expected draft-stage state (§E.2 populates at run-phase, `sync_commit_sha` at sync close). No MUST-FIX. Does not affect the verdict.

## Factual spot-checks (adversarial — every claim re-measured on this tree)

| # | SPEC claim | My measurement | Result |
|---|---|---|---|
| 1 | `internal/factory` absent; `internal/kanban` real with record.go·revision.go·integration_lock.go | `ls` → factory/bodp "No such file or directory"; kanban listing carries all three files | CONFIRMED |
| 1b | modules.md:158–162 describes factory as live | sed 155–166: `### internal/factory` + role/core/state/entry-point lines; cited entry points `internal/cli/factory.go` (17,569 B) and `internal/cli/launcher_blockcap_infinite.go` (5,632 B) exist | CONFIRMED |
| 1c | kanban symbols: validateSessionID record.go:202; SuppressStep0551 revision.go:167-174 | `validateSessionID(rec.SessionID)` call at record.go:202 (def at :256 — spec's coordinate is the occurrence, true as written); SuppressStep0551 comment :167, func :174 | CONFIRMED |
| 2 | `internal/bodp` removed by `5792fc755` (#1278) | `git show 5792fc755 --name-status`: D internal/bodp/{audit_trail,audit_trail_test,relatedness,relatedness_test}.go; subject "refactor(worktree): redesign the worktree surface — 8 subcommands, launcher-owned entry (#1278)" | CONFIRMED |
| 2b | develop modules.md:95–97 positive bodp section | sed 90–100: `### internal/bodp` + role/function at 95–97 | CONFIRMED |
| 3 | data-flow.md ListActive at 197/214/351–359; real API per §1.2 | grep: 197 `E["ListActive"]`, 214 flow step, 356 `ListActive(spec string) ([]Session, error)` inside the Registry interface block (351–359). registry.go: Register(sessionID, specID, phase string) error @169, Heartbeat(sessionID string) error @215, Deregister(sessionID string) error @241, Query(optSpecID string) ([]Entry, error) @266, package func QueryActiveWork @261 — every name/param/return and line number exact | CONFIRMED (all five signatures byte-accurate) |
| 3b | no other identifier drift missed | `PurgeStale` exists (registry.go:293 `func PurgeStale(thresholdMinutes int) (int, error)`); `active-sessions.json` real (`DefaultRegistryPath` registry.go:39) | CONFIRMED (no uncovered phantom) |
| 4 | D1 extension point: CheckFreshness/Layers/LayerReport/metric-token/threshold-0; CLI consumes generically | internal/graph/check.go: `CheckFreshness` :142 → `CheckResult.Layers []LayerReport`; LayerReport{Layer,Metric,Value,Threshold,Verdict,…} :60; metric-token const block :32–38; layer-name const :24–28; `Failed()` :110 (any non-fresh → true); `OffendingLayers()` :121; threshold-0 precedent hardcoded in checkEdges :392. internal/cli/graph_check.go: exitStaleOrAbsent=1 / exitSystemError=2 (:20–26); render loop iterates `res.Layers` generically (:100–107); `Failed()`→exit 1 (:110–120); OffendingLayers→stderr (:114–118); `--json` marshals the full CheckResult (:92–97). A 4th layer row requires zero renderer wiring | CONFIRMED (D1's "기존 렌더러·Failed() 경로가 그대로 소비" holds) |
| 5 | D2 basis: five warning notes blockquote; bodp footnote form | modules.md:93, 147, 218, 232, 258 all `>`-prefixed negative notes (exact lines as claimed). Caveat found: two NON-negative blockquote annotation lines exist (modules.md:267 measurement note naming internal/binlag·guardliveness·guardstate; dependencies.md:149 guardstate→guardliveness note) — their paths all exist, so the check is unaffected today → defect D2 (MINOR) | CONFIRMED with caveat |
| 5b | absence set = exactly the enumerated 8, nothing else | My own census: 86 unique `(internal\|pkg\|cmd)/` tokens across all 6 .md files (incl. docs-truth.md); after t432 normalization (trailing-slash strip, `.go` restore, cmd/moai/main→main.go): ABSENT = internal/{bodp,design,evaluator,factory,migrate,research,state} exactly; cmd/moai/main = normalization artifact; zero other absent tokens. Independent reproduction of the SPEC's §1 numbers | CONFIRMED |
| 6 | R1: skill Phase 2 ingests existing codemaps; Phase 4 advisory-only | local `.claude/skills/moai/workflows/codemaps.md`:91–95 "Analysis inputs: … Existing .moai/project/codemaps/ content (if --force not set, for incremental updates)"; :127 "Verify all referenced files and modules actually exist" (bare advisory bullet — no command, no convention, no gate). `diff` local vs template mirror → IDENTICAL | CONFIRMED |
| 7 | M0 merge-order gate real | t432 (`7e1c4d94f`) verified NOT an ancestor of origin/develop (unmerged) — the constraint is live, not hypothetical. Plan §C step 1 is a mechanical predicate (`git merge-base --is-ancestor 7e1c4d94f origin/develop`); M0 completion = recorded re-measure (command+output in progress.md §E.2) + [HARD] §D edit-block. Real gate. Negative branch unstated → defect D1 (MINOR) | CONFIRMED with caveat |
| 8 | F1: t432 report §3.1 "26항목"(:194) vs 27-row table / "27항목"(:251) | sed of frozen t432 report: :194 "…전수 hit/miss 표 (26항목)", :251 "…전수(27항목)" — heading miscount real; ListActive = §3.1 #21 (:218) sole MISS | CONFIRMED |

## Category Scores (rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 1.0 | 1.0 | Every REQ has a single interpretation with mechanically checkable discriminators (D2: line starts with `>`; REQ-CMA-005: exact signatures verified against registry.go; REQ-CMA-002: named layer/metric/threshold). Extractor-scope ambiguity (code fences/mermaid) pre-answered in plan §B last row. Coordinates explicitly tree-pinned with post-absorb re-measure contract (acceptance.md:L5). |
| Completeness | 0.95 | 0.75–1.0 | All required sections present (HISTORY spec.md:L19; WHY §1; scope via §1+§6; REQUIREMENTS §2 L72–85; ACs in acceptance.md Tier M; Out of Scope with four `### Out of Scope — <topic>` H3 sub-headings each with `-` bullets, spec.md:L107–123). Frontmatter complete. One non-critical robustness gap: M0's negative branch (defect D1). |
| Testability | 1.0 | 1.0 | All 9 ACs binary-testable with runnable predicates (grep counts, signature diff, exit codes); mutant round-trip (acceptance.md §D.2 / AC-CMA-005 mutant) closes vacuous-green; empty-sweep hazard addressed (AC-CMA-001's set-equality formulation cannot pass on an empty extraction). Severity split MUST/SHOULD declared (§D.1). |
| Traceability | 1.0 | 1.0 | Full bidirectional coverage: REQ-001→AC-001, REQ-002→AC-005, REQ-003/004→AC-002, REQ-005→AC-003, REQ-006→AC-004, REQ-007→AC-006, REQ-008→AC-007, REQ-009→AC-008, REQ-010→AC-009. No orphaned ACs, no uncovered REQs (matrix at acceptance.md:L7–17). |

Harmonic mean = 4 / (1 + 1/0.95 + 1 + 1) = **0.987**.

## Defects Found (structured defect-list)

D1. M0-negative-branch — plan.md §C step 1 / §D [HARD] — If the `git merge-base --is-ancestor 7e1c4d94f origin/develop` check fails at run-phase start (t432 still unmerged), no disposition is stated, and §D's edit-block covers `internal/` as well as `.moai/project/codemaps/`, so even M1 (Go axis) stalls with no written exit. — Severity: minor — Class: optional — Required fix: add one line to plan §C step 1 ("check fails → blocker report to the lead; merge ordering is the lead's act"), and consider scoping the [HARD] pre-absorb edit-block to `.moai/project/codemaps/` only, since t432's regeneration cannot conflict with `internal/graph`/`internal/cli` files.
D2. blockquote-convention purity — spec.md §3 D2 / REQ-CMA-007 vs modules.md:267·dependencies.md:149 — Two existing blockquote lines are NOT negative citations (modules.md:267 measurement note naming internal/binlag·internal/guardliveness·internal/guardstate; dependencies.md:149 guardstate→guardliveness clarification). The skill convention REQ-CMA-007 will insert ("blockquote는 부정 인용 전용 표기") is contradicted by the corpus on day one. Harmless to the check today (all cited paths exist; AC-CMA-001's invariant is about the absence set and holds), and D2's stated rationale is one-directional — but the M3 wording should not claim exclusivity the corpus violates. — Severity: minor — Class: optional — Required fix: in the M3 skill edit, word the convention as "negative citations MUST use blockquote notation" without asserting blockquote lines are only negative citations (or explicitly grandfather annotation notes).
D3. count-provenance wrinkle — spec.md §1 "t432 트리 102개" — The SPEC cites the lane's remeasure (remeasure.md: "cited: 102") while the t432 report's own §1 headline says 100 unique paths. Divergence is a regex-method artifact (different trailing-punctuation stripping); the load-bearing fact — identical 8-member absence set on both trees — is unaffected and independently reproduced by this audit. — Severity: minor — Class: optional — Required fix: none required; optionally footnote the 100-vs-102 method divergence when recording the M0 re-measure in progress.md §E.2.

No blocking defects. All three findings are optional-class per M6 — they do not gate the verdict.

## Regression Check
Iteration 1 — not applicable (no prior iteration).

## Recommendation

PASS. Rationale per must-pass criterion: MP-1 through MP-7 all PASS or N/A with mechanical evidence above; the SPEC's factual substrate survived full adversarial re-measurement — every line-number coordinate, every API signature, the package census (86/8 exactly), the removal commit, the skill text in both copies, the graph-check extension point, and the t432 merge state were independently confirmed on this tree, and the draft-stage spec_audit emits only the expected INFO EraAutoDetected. The three optional findings (D1–D3) are surfaced for the lane's discretion and can be absorbed into the M0/M3 execution without a SPEC revision; none is score-gating. Implementation Kickoff Approval remains the mandatory plan→run human gate regardless of skip-eligibility.
