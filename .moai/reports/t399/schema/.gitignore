# Generated by:
#   codex app-server generate-json-schema --out .moai/reports/t399/schema
# on codex-cli 0.150.1, in this tree. 297 files / 3.8 MB, reproducible in one
# command.
#
# Only the file SPEC-CODEX-REVIEW-TARGET-001 actually cites as protocol ground
# truth is tracked (v2/ReviewStartParams.json). The rest stays on disk,
# untracked — a generated dump this size is regenerable, so tracking it buys
# nothing, and deleting an artifact a plan-phase measurement produced is not
# this phase's call to make.
*
!.gitignore
!v2
!v2/ReviewStartParams.json
