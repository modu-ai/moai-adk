# SPEC Review Report: SPEC-GATEWAY-WEDGE-REROOT-001 (card t700)

Iteration: 1/2 (Tier M ceiling per `harness.plan_audit_tier_ceilings`)
Verdict: **COND-FAIL** (formally FAIL — fix D1–D5, then scoped delta re-audit)
Overall Score: **0.6875** (Tier M PASS threshold 0.80)

Baseline-attribution: worktree `.claude/worktrees/t700`, branch `WT-wedge-reroot-policy`, base local develop `44e56d017` (this run, this tree). SPEC artifacts untracked at audit time. Cross-backend: codex adversarial review executed (verdict FAIL, 6 findings — 4 independently verified by this auditor, 1 overstated and corrected, 1 proportionality-scoped); glm fail-open unavailable ("no reviewable change: unknown review target"). Engine overall: FAIL. Reasoning context from the SPEC author was not supplied; audit performed on the artifacts only (M1 Context Isolation — nothing to ignore).

## Must-Pass Results

- **[PASS] MP-1 REQ number consistency**: REQ-WRR-001..009 (spec.md:62–85) — sequential, zero-padded, no gaps, no duplicates.
- **[PASS] MP-2 GEARS format compliance** (requirement layer graded): all 9 REQs match canonical patterns — Ubiquitous (001, 002, 007-core, 009), Where (003, 008), When (004, 006), shall-not (005). Two category-label nits are optional findings (O1), not pattern failures. Given-When-Then ACs in acceptance.md are the verification layer and were not penalized (M3 § Scope).
- **[PASS] MP-3 YAML frontmatter validity**: all 12 canonical fields present with correct types (spec.md:2–13; `created`/`updated` ISO dates, quoted semver `0.1.0`, `status: draft`, `priority: P1`, `phase: "v3.2.0 target"` — release label, not a stage name; no rejected snake_case aliases). Mechanical: `mcp__moai__spec_audit` (project_root=this worktree) → era V3R6, `modern_era_clean: 1`, 0 drift findings (only INFO `EraAutoDetected`, H-5).
- **[N/A] MP-4 Section 22 language neutrality**: single-language (Go) scoped SPEC — auto-passes.
- **[PASS] MP-5 D7 cross-SPEC reconciliation**: extracted SPEC-IDs = self + `SPEC-MOAI-GATEWAY-001` (grep, 3 artifacts). Target exists at `.moai/specs/SPEC-MOAI-GATEWAY-001/spec.md` with `status: draft` — not in {retired, superseded, archived}; no BLOCKING.
- **[PASS] MP-6 D8 cross-platform discipline**: `grep -c syscall` = 0 in spec.md / plan.md / acceptance.md → auto-PASS (D8-4).
- **[PASS] MP-7 clarification gate**: `grep -rn '\[NEEDS CLARIFICATION'` on plan.md → 0 matches; research.md absent (Tier M artifact set — not required). Non-blocking open questions in the SPEC tail (recovery verb naming, AC-WRR-013 optional live round trip) were judged: the WHAT is fully fixed by REQ-WRR-003/004/006 and AC-WRR-008..011; verb/flag naming is correctly deferred to run M3 (plan.md §F M3). Acceptable.

## Category Scores (0.0–1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.75 | 0.75 | REQ-WRR-007's blanket clause "The policy does not convert any rejected shape into an accepted one" (spec.md:80) has two readings, one false under the trailing-forged-tail case (D3); REQ-WRR-004's trigger "while the transcript's trailing assistant boundary was never published" (spec.md:74) is a condition the client cannot itself decide — resolvable, but a reasonable engineer could implement differently |
| Completeness | 0.75 | 0.75 | All required sections present (HISTORY :21, WHY §1, WHAT §2, security determination §3, Out of Scope with 6 H3 sub-headings + bullets :120–144); §3.4 disposition table (spec.md:110–116) is missing exactly the row the policy boundary turns on — trailing-forged/stripped tail (D3) |
| Testability | 0.75 | 0.75 | 12 of 13 ACs binary with named tests/commands; AC-WRR-012's second half ("zero diff inside the preserved symbol regions", acceptance.md:34/:50) is not executable via the cited `--name-only` command (D2); AC-WRR-006's verb names no test (D6); AC-WRR-013 is MAJOR by the legend yet "skippable" in its own row — self-contradictory gating semantics (D5) |
| Traceability | 0.50 | 0.50 | Mechanical: `go run ./cmd/moai spec lint --strict` → 3 × `CoverageIncomplete` (REQ-WRR-006 combined-token, REQ-WRR-008, REQ-WRR-009). REQ-WRR-008/009 have zero AC rows in the §B SSOT matrix (D1) |

## Verified Strengths (evidence, not courtesy)

1. **Constraint fidelity vs t672 — verified against `.moai/reports/t672/{verdict,investigation,matrix}.md`**: candidates A (item-level validation) and B (request-declared root seeding) are named as REJECTED and not reintroduced (spec.md §1.3, Out of Scope); the fixed-guidance contract matches the code byte-for-byte — `historyReplayGuidance` (`internal/gateway/translate/receipt_history.go:40`) + 3 reason clauses (:52/:54/:56) equal t672 matrix.md's verbatim C5/C6 bodies; "request metadata never selects a receipt root" (t672 investigation §2 quote from receipt_history.go:28-29) is preserved by construction (REQ-WRR-003-4, REQ-WRR-008(a)); C4b/C5 cells are cited accurately; the §0 isolation discipline is inherited verbatim into plan.md §D.
2. **All 16 symbol pins resolve on this tree**: `receiptHistory.Check`:172, `checkObserved`:233, `replayCause`:194, `Publish`:210, `HistoryReplayError`:45, `historyReplayGuidance`:40, `NewReceiptHistory`:69, `NewGPTSubscriptionReceiptHistory`:76; `Manifest.Check`:99, `Manifest.Fork`:127, `Candidate`:40, `Observation`:49 (`receipt/core.go`); `Manager.Fork`:257 (`family.go`); `authorizeGatewayNativeReceipt`:59, `newGatewayHandlerFactory`:80 (`gateway_factory.go`); launcher fork caller `gateway_session.go:204`.
3. **All 5 cited test names exist** in `receipt_history_cause_test.go` (incl. the C4b lock :106 and C5 lock :233); `TestAppServerSubprocessHTTPToolContinuation` exists (`appserver_integration_test.go:19`).
4. **Security determination §3.2 is argued, not asserted**: leg 1 is mechanical (validator untouched, enforced by AC-012's diff lock); leg 3 (deletion has no acceptance power) is grounded in the four-way binding — any deletion before a published boundary breaks that boundary's Prefix digest; the no-bypass consequence follows. Its mechanical ingredients (store-digest, diff-scope, shape-preservation tests) are mapped to ACs.
5. **t697 sibling hazard — measured, not assumed**: `git diff --name-only 44e56d017..WT-502-prestream-retry` = 7 files (`anthropic.go`, `openai.go`, `upstream.go` + tests + verdict) — **zero overlap** with the pinned files. The M1 absorb + re-verification gate (plan.md §A/§C) remains mandatory and is adequate. However, develop has moved UNDER the SPEC since authoring: local develop is now `e241da934` and **contains** the t697 merge `f45c2dddf`, while `origin/develop` (`0755cc7f5`) does **not** — making plan.md §A's "`origin/develop`/local `develop`" ambiguity a live hazard (D5).

## Defects Found

D1. **F1 — acceptance.md §B** — REQ-WRR-008 and REQ-WRR-009 have no AC row; REQ-WRR-006 is referenced only inside the combined token "REQ-WRR-003/006" (AC-WRR-008 row), which the lint tracker does not parse — mechanical proof: `go run ./cmd/moai spec lint --strict` → `CoverageIncomplete` ×3 (spec.md:62/66/69). §B is declared the SSOT (plan.md §E E1); DoD item 3 references REQ-WRR-009 but DoD is not an AC — Severity: major — Class: blocking — Required fix: write full ID tokens in AC-WRR-008's requirement cell; add AC-WRR-014 mapping REQ-WRR-008 (mechanical: card introduces no new gateway-state-changing recovery path; `Manager.Fork` remains the sole `.Fork(` / re-root call site — dual-purpose with AC-WRR-012's scope) and AC-WRR-015 mapping REQ-WRR-009 (binary: operator doc exists naming wedge class, recovery procedure, single-shot/non-destructive bounds, non-recoverable shapes, at a named path).

D2. **F2 — acceptance.md:34/:50 (AC-WRR-012) + plan.md §D PRESERVE** — two lock-completeness gaps in the BLOCKER-severity no-weakening assertion. (a) `git diff --name-only` cannot show "zero diff inside the preserved symbol regions"; the region-level escape hatch has no executable mechanism. (b) The preserve set omits the production authorization call site: `internal/gateway/translate/request.go` (`limits.History.Check` invocation + `NativeReceiptAuthorize` ordering) is in neither the PRESERVE list nor AC-WRR-012's zero-diff set, so the diff lock does not mirror what the behavioral suite protects. Corrective evidence against codex's strongest form: the existing suite DOES catch the "delete the Check call" mutant — `TestReceiptHistoryRejectsMidPairTruncationBeforeHistory` drives a rejection through the full `Request` entrypoint (receipt_history_cause_test.go:156→163), so codex's "all ACs stay green" claim is overstated — but lock and net should agree — Severity: major — Class: blocking — Required fix: tighten AC-WRR-012 to file-level zero-diff (the card's design touches none of the preserved files) for `core.go`, `receipt_history.go`, `family.go`, `gateway_factory.go` **and `request.go`**, plus zero paths under `internal/gateway/receipt/`; cite the wire-level mid-pair Request test in the M2/AC matrix as the behavioral net.

D3. **F3 — spec.md:80 (REQ-WRR-007) + spec.md §3.4** — the trailing-forged/stripped-tail case is undispositioned and contradicts the blanket clause under one reading. The client cannot distinguish a trailing never-published boundary from a trailing forged/stripped one (chain class alone; store reads forbidden by REQ-WRR-003-4); user-confirmed removal of either leaves a genuine published prefix, which the unchanged validator accepts — so the literal sentence "The policy does not convert any rejected shape into an accepted one" is false for the wedge itself and for the trailing-forged case. The sound property is §3.2's: no removed content ever validates; acceptance after recovery implies a published-chain prefix. Codex F2 independently identified this; verified sound — Severity: major — Class: blocking — Required fix: reword REQ-WRR-007's consequence sentence to the §3.2 prefix-property form and state explicitly that the procedure cannot distinguish the two trailing cases client-side (REQ-WRR-005's human confirmation + REQ-WRR-003-3's non-destructive preservation are the safeguards); add the trailing-forged/stripped-tail row to the §3.4 table; add an edge-case AC (recovery on a trailing-forged tail: the removed content is never accepted by any replay, the remaining prefix is accepted, content preserved aside).

D4. **F4 — spec.md:70/78 (REQ-WRR-003-2, REQ-WRR-006) + acceptance.md:30 (AC-WRR-008)** — single-shot is declared "the security property" (plan.md §G) but incident state is undefined across process restart: an in-memory boolean passes AC-WRR-008, and a fresh process peels another boundary per invocation (converging on an oracle-probe shape the SPEC itself bans). Authorization safety survives peeling (every accepted replay is still a genuine prefix — codex's finding is scoped accordingly); the durability gap is against the SPEC's own claim — Severity: major — Class: blocking — Required fix: require a durable attempt marker on the launcher conversation record (e.g. recovery-attempted flag + preimage digest); add AC: a second invocation — including from a fresh process reading the record — performs no removal; make aside-before-replace ordering explicit in REQ-WRR-003-3. Concurrent-invocation locking and the full CAS/fsync/symlink contract are OPTIONAL hardening at Tier M — do not add them to the blocking set.

D5. **F5 — plan.md §A/§C + acceptance.md:17/:35** — three absorb/gating precision defects. (a) §A says "absorb `origin/develop`/local `develop`" — measured divergence: local develop `e241da934` contains the t697 merge `f45c2dddf`, `origin/develop` `0755cc7f5` does not; absorbing the wrong ref completes M1 without t697. Per gitflow-lane-protocol §11 the absorb target is local develop. (b) §C pre-flight command `go doc ./internal/gateway/translate receiptHistory` is not executable — verified: exit 1, "doc: no symbol receiptHistory" (unexported symbol; the other two go doc forms work). (c) AC-WRR-013 is MAJOR ("must pass before sync" per the §A legend) yet "skippable with the gap recorded" in its own row and DoD 1 — self-contradiction — Severity: major — Class: blocking — Required fix: pin the absorb target to local `develop` and add the M1 ancestry gate `git merge-base --is-ancestor <t697-merge-SHA> HEAD` (resolve the SHA at M1 time, never pin a moving ref); replace the broken go doc form with an executable unexported-pin check (explicitly designate the characterization-suite compile as the net for unexported pins, or grep the declarations); downgrade AC-WRR-013 to MINOR (matches its skip clause and M5's Priority Low) or remove the skip clause.

D6. **F3-minor — acceptance.md:28 (AC-WRR-006)** — verification verb "existing lineage-classification test still green" names no test; measured: the `CauseLineage` assertion lives inside `TestReceiptHistoryRejectsForeignItemReplay` (receipt_history_cause_test.go:187–190), not a standalone test — Severity: minor — Class: blocking (same one-line fix round) — Required fix: name the test and its lineage section.

D7. **F5b — acceptance.md:32/:48 (AC-WRR-010)** — before/after digest comparison is blind to write-then-restore and to store reads; the design's real guarantee is structural (the recovery path never receives a receipt-store handle) — Severity: minor — Class: blocking (same fix round) — Required fix: add to AC-WRR-010 a structural assertion (the recovery implementation takes no receipt-store argument / does not open the store); keep the digest as the complement, not the sole proof.

O1. spec.md:78 — REQ-WRR-006 category label "Unwanted" vs Event-driven text. Optional: relabel.
O2. plan.md §C — only 2 of 3 go doc forms are executable (see D5b); 14 of 16 pins rely on the compile net implicitly. Optional: state the net explicitly.
O3. acceptance.md §D — first-turn wedge edge case has no AC row. Optional: fold into the M2 matrix.
O4. AC-WRR-012 execution discipline — re-derive `merge-base develop HEAD` at measurement time and record the SHA (gitflow-lane-protocol §8, t543 lesson); M4's pre-merge ordering is safe today; the verdict form is void post-merge. Optional note.

## Regression Check (Iteration 2+ only)

N/A — iteration 1.

## Recommendation

Numbered fixes for manager-spec (acceptance.md + plan.md + spec.md §3.4; one delta round):

1. D1: full REQ tokens + AC-WRR-014/015 rows (acceptance.md §B).
2. D2: AC-WRR-012 → file-level zero-diff incl. `request.go`; cite the wire-level mid-pair Request test (cause_test.go:156) as the behavioral net.
3. D3: reword REQ-WRR-007's consequence clause to the §3.2 prefix-property; add §3.4 row + trailing-forged edge-case AC.
4. D4: durable attempt marker requirement + fresh-process AC; aside-before-replace explicit.
5. D5: absorb target = local `develop` + t697 ancestry gate at M1; fix the broken go doc pre-flight line; AC-WRR-013 → MINOR.
6. D6/D7: name the lineage test; strengthen AC-WRR-010 with the structural no-store-handle assertion.

Then a scoped delta re-audit over D1–D7 only (Retry Loop Contract, iteration 2 of 2 for Tier M). Optional findings O1–O4 are the orchestrator's discretion. Implementation Kickoff Approval remains mandatory after any PASS; the M1 absorb + pin re-verification gate remains mandatory at run entry regardless of this audit's verdict.

Cross-model attribution: claude anchor = this report (COND-FAIL); codex adversarial = FAIL, findings 1/2/3/5(i)(ii)/6 verified by independent execution (one claim — "delete Check call passes all tests" — corrected by this auditor's measurement of cause_test.go:156; one — full CAS/locking contract demand — scoped down as disproportionate for Tier M); glm = unavailable (fail-open). Engine convergence: FAIL.
