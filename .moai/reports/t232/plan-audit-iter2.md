# SPEC Review Report: SPEC-ZONE-REGISTRY-RESYNC-001

Iteration: 2/2 (Tier M ceiling — 마지막 재감사)
Verdict: **PASS-WITH-DEBT**
Overall Score: **0.925** (Tier M 임계 0.80)

감사 트리: `.claude/worktrees/t232` @ `a13922293`, 브랜치 `WT-zone-registry-drift`, 작업 트리 클린.
델타: `880a43363..a13922293` 커밋 1개, 4파일 `+56 / -30`.
범위: **델타 한정 재감사**(iter1 권고에 따름) — 6개 blocking 결함의 폐쇄 여부 + 회귀 여부 2문항. 전면 재감사가 아니다.

M1 Context Isolation: 저자 추론 컨텍스트 없음. iter1 보고서(`plan-audit-iter1.md`)는 내가 쓴 것이므로 결함 목록의 출처로만 쓰고, 폐쇄 판정은 **현재 트리의 문안을 새로 읽어** 내린다.

---

## 판정 요약

blocking 6건 중 **5건 완전 폐쇄, 1건 부분 폐쇄**. AC 를 약화시킨 곳은 없고(오히려 AC-ZRR-008 은 OR → AND 로 강해졌다), 새로 생긴 결함은 문서 정합성 수준 2건뿐이다. must-pass 7건은 델타가 건드리지 않았고 여전히 전량 PASS.

부분 폐쇄 1건이 FAIL 이 아닌 이유와, 그럼에도 PASS 가 아닌 이유는 같은 곳에 있다 — **빈 clause 금지가 "빈 문자열"이라는 정확한 인스턴스에만 걸려 있어, 공백 한 칸짜리 clause 로 한 글자 옆으로 비켜설 수 있다.** 이건 내가 iter1 에서 지시한 문안(`빈 문자열이 아니다`)을 레인이 충실히 구현한 결과이기도 하다. 지시가 인스턴스 모양이었고 구현도 그 모양으로 나왔다.

이 잔여를 FAIL 로 올리지 않는 근거는 세 가지다. ① 수정이 AC 한 줄(유일 적중 단언)이고, ② 그 요구가 실제 데이터에서 이미 참임을 측정으로 확인했으며(현재 적중하는 25건 중 **24건이 정확히 1회 적중**, 2회 이상은 1건), ③ Tier M 의 재감사 기회가 여기서 끝나므로 한 줄짜리 문안 때문에 run-phase 를 막는 것은 비례하지 않는다. 대신 **run-phase 로 넘기는 부채로 명시하고 판정 주체를 지정**한다(§ 이월 부채).

---

## (a) blocking 6건 폐쇄 판정

### D-1 자기참조 `file:` — **폐쇄 (일반형은 사람 게이트로 이관, 잔여 1건 측정됨)**

추가된 문안: AC-ZRR-002 Then 에 "어떤 엔트리의 `file:` 도 레지스트리 파일 자신이 아니다" + "`file:` 값이 바뀐 엔트리의 목록(구 → 신)을 `progress.md` §E.2 에 인용하고, sync-phase 리뷰가 각 이동의 타당성을 판정한다". AC-ZRR-003 이 세 부가 조건을 템플릿 미러로 확장. `plan.md` §D 에 D13 신설. §G 에 합동 행 추가.

**적대적 재시도 — 리드가 지목한 근접 변종 2종을 실제로 측정했다.**

*변종 A — 템플릿 레지스트리 경로로 재지정.* `file:` 을 `internal/template/templates/.claude/rules/moai/core/zone-registry.md` 로 두면 문자열이 달라 금지 조항을 비켜간다. 그러나 AC-ZRR-003 이 `internal/template/templates/<file>` 로 결합하므로 경로가 두 번 겹쳐 존재하지 않는 파일이 되고 적중 실패로 걸린다. **AC-003 이 이미 잡는다.**

*변종 B — 패러프레이즈를 이미 담고 있는 다른 문서로 재지정.* 이게 실질적 잔여다. 두 트리에 모두 존재하는 파일(레지스트리 제외) **82개**를 후보로 잡고, 깨진 clause 68건이 그중 어디엔가 리터럴로 존재하는지 전수 검사했다.

```
dual-tree candidate files (excl. registry): 82
  RETARGETABLE CONST-V3R2-004 -> .claude/rules/moai/NOTICE.md
broken clauses literally present in some OTHER dual-tree file: 1 of 68
```

**68건 중 1건.** `CONST-V3R2-004` 의 짧은 라벨 `16-language neutrality` 만 `NOTICE.md` 로 옮겨 기계 판정을 통과시킬 수 있다. 나머지 67건은 옮겨 갈 곳이 없다 — 패러프레이즈가 레지스트리를 위해 쓰인 문장이라 다른 어떤 문서에도 그 형태로 존재하지 않기 때문이다.

즉 D-1 의 일반형("의미상 틀린 곳으로 옮기기")은 여전히 기계로 완전히 막히지 않지만, **오늘 데이터에서 실제로 도달 가능한 표면은 엔트리 1개**이고, 그 1개조차 `file:` 변경 목록 인용 의무 때문에 sync 리뷰어의 눈앞에 강제로 놓인다. iter1 이전에는 100줄짜리 diff 안에 묻혀 보이지 않았을 이동이다. **인스턴스 폐쇄 + 일반형의 가시화** — 이 SPEC 이 §D.4/§H 에서 이미 선언한 "의미 정합성은 사람이 판정한다"는 축과 일관되며, 새로 뚫린 구멍이 아니다.

우연이지만 기록해 둘 만한 것: 유일한 잔여 엔트리 `CONST-V3R2-004` 가 AC-ZRR-007 의 **고정 변이 대상**으로 지정된 바로 그 엔트리다. 방어는 아니지만 run-phase 가 이 엔트리를 눈여겨보게 되는 구조다.

### D-2 빈 clause — **부분 폐쇄 (이 보고서의 유일한 실질 잔여)**

추가된 문안: AC-ZRR-002 Then 에 "어떤 엔트리의 `clause:` 도 **빈 문자열이 아니다**. 빈 clause 는 적중이 아니라 **실패**로 센다". 판정 명령이 적중 수 + 빈 clause 수 + 자기참조 수 3개를 출력하도록 바뀌었고, §G 의 AC-006 행이 "이 방어는 plan 메모가 아니라 AC 본문에 있다"로 갱신됐다. 내가 iter1 에 요구한 것은 정확히 이대로 들어왔다.

**적대적 재시도 — 한 글자 옆에 변종이 살아 있다.** 금지 대상이 "빈 문자열"이라는 정확한 값이므로, 모든 clause 를 공백 한 칸(`" "`) 또는 아주 짧은 흔한 토큰(`"the"`, `"the "`)으로 바꾸면:

- 리터럴 체크: 공백은 모든 파일에 적중 → **101/101 통과**
- 빈 clause 카운트: 빈 문자열이 아니므로 **0 통과**
- 자기참조 카운트: `file:` 무관 → **0 통과**
- `Validate`: `normalizedClause != ""` 가드를 넘어 부분문자열 검사로 가지만 공백은 어디에나 있으므로 **DRIFT 0** → AC-ZRR-001 통과
- AC-ZRR-004(anchor) / AC-ZRR-006(엔트리 보존, clause 는 변경 허용 필드) 모두 무관하게 통과

즉 **빈 문자열 경로와 정확히 같은 비용으로 같은 결과**에 도달한다. 위협이 줄어든 게 아니라 형태만 한 칸 옮겨 갔다.

다른 AC 가 잡지 못하는 것도 확인했다. AC-ZRR-005 의 "clause 길이 임계 도입 금지"는 **검증 경로에 임계를 넣지 말라**는 뜻이지 데이터 쪽 clause 품질 요구가 아니다. D-1 이 새로 도입한 인용 의무는 `file:` **변경 목록**만 인용하게 하므로 clause 일괄 퇴화는 sync 리뷰어 앞에도 놓이지 않는다.

**원칙형 수정이 존재하고, 실측상 무료다.** 101건 clause 를 각자의 `file:` 안에서 몇 번 적중하는지 셌다.

```
0 occurrences : 76   (현재 깨진 것들)
exactly 1     : 24
2+            : 1
clause length: min 7 / median 93 / max 518, 20자 미만 3건
```

**현재 적중하는 25건 중 24건이 정확히 1회 적중한다.** 진짜 verbatim 인용은 원래 유일하다 — 즉 "clause 는 그 파일에서 **정확히 1회** 적중한다"는 단언은 정상 데이터를 하나도 배제하지 않으면서 공백·짧은 토큰 변종을 전부 죽인다. 2회 적중하는 1건은 run-phase 에서 더 긴 구간을 고르면 해소되며, 그조차 어려우면 최소 길이 하한이 대안이다(median 93 이므로 20자 하한도 3건만 건드린다).

폐쇄로 인정하지 않는다. **이월 부채 #1 로 넘긴다.**

### D-3 `|| true` / 스텝 단위 억제 — **폐쇄**

세 곳이 동시에 바뀌었다. `spec.md` `REQ-ZRR-011` 이 "and the step that runs it shall not be wrapped in `|| true`, a step-level `continue-on-error`, or any other exit-code suppression" 으로 확장됐고, AC-ZRR-008 Then 이 OR → **AND** 로 바뀌면서 스텝 단위 억제 금지 조항이 붙었으며, AC-ZRR-007 에 "변이를 담은 커밋을 푸시해 **해당 CI job 이 실제로 `fail` 로 결론나는 것**을 `gh pr checks` 출력으로 관측하고 인용한다"가 들어왔다.

**적대적 재시도 — 통과하는 구현을 구성할 수 없다.** 핵심은 마지막 조항이 **설정 읽기가 아니라 실측**이라는 점이다. 어떤 래핑을 하든 변이 커밋에서 job 이 `fail` 로 결론나야 하고, 그건 정확히 `REQ-ZRR-007`("가드는 그 편집을 담은 PR 을 실패시킨다")이 요구하는 바다. iter1 에서 "REQ-ZRR-007 을 검증하는 AC 가 하나도 없다"고 적었는데, 이제 있다.

운영 가능성도 확인했다. `gh pr checks` 는 PR 존재를 전제하는데 Tier M 의 기본 경로(Route A)는 main-direct 라 PR 이 없다 — 여기서 걸릴 뻔했으나, **이 리포는 `.claude/rules/local/repo-local-pr-policy.md` 로 전 티어 Route B(PR) 를 강제**한다(`main` 이 `enforce_admins: true` 로 보호돼 직접 push 가 거부됨). 따라서 이 AC 는 이 리포에서 실행 가능하다. 결함 아님.

### D-4 + D-8 평가 엔트리 수 단언 / 변이 대상 고정 — **폐쇄**

AC-ZRR-007 에 "그 통과 출력이 **가드가 평가한 엔트리 수를 보고하며 그 값이 `101` 이다 — 두 미러 각각**"이 들어왔고, 변이 대상이 "임의의 한 엔트리"에서 "**명시된 엔트리 `CONST-V3R2-004` 1건과 무작위로 고른 1건**"으로 고정됐다. 고정 이유("임의는 실행마다 다른 것을 골라도 둘 다 충족으로 읽혀 재현 가능한 판정이 아니다")까지 본문에 적혔다.

**리드의 질문 — 카운트 단언이 변이 위치와 독립적으로 제외 목록 mutant 를 닫는가: 그렇다.** 가드에 제외 목록·조기 반환이 있으면 평가 수가 101 미만으로 떨어지므로, 변이가 제외 안쪽에 떨어지든 바깥에 떨어지든 무관하게 걸린다. 이것이 변이 주입 방식이 갖지 못한 성질이고, §G 의 AC-005 행이 "변이 위치에 의존하지 않는 유일한 방어"라고 정정한 것도 정확하다 — iter1 에서 내가 틀렸다고 지적한 그 행이 올바르게 고쳐졌다.

잔여는 문안 한 곳: "평가한 엔트리 수"가 **로드한** 수인지 **실제로 두 검사를 수행한** 수인지 구분하지 않는다. 카운터만 101 로 보고하는 구현은 문자상 통과한다. 다만 그건 증거 조작 경로이지 AC 구멍이 아니며(`verification-claim-integrity.md` §1.1 이 덮는 영역), D10 의 "실제 출력 인용" 의무와 겹쳐 실무적으로 좁다. 부채 #3 으로 기록만 한다.

### D-5 이중 트리 파일 수 오기 — **폐쇄 (내 측정과 대조 확인)**

`spec.md` §5 가 "**2개 파일**이 로컬과 템플릿에서 서로 다르고, 그 2개가 **3개 엔트리**를 물고 있다" / "나머지 **15개** 파일(엔트리 98건)" 으로 정정됐고, `acceptance.md` AC-ZRR-003 의 RED 근거 문장도 함께 고쳐졌다. iter1 에서 내가 실측한 값(17개 인용 파일 중 발산 2개 / 엔트리 3개 / 나머지 15개 / 98건)과 정확히 일치한다.

덤으로 §5 에 내 실측 결과("발산 총 4줄, 전부 인용 대상 절 밖")가 반영돼 run-phase 가 같은 조사를 반복하지 않게 됐다.

### D-9 paths-filter 이탈 — **폐쇄**

`plan.md` §D 에 **D12** 가 신설됐다. `detect` job 의 `go_code` 필터가 덮는 경로 목록, `.claude/skills/**` 가 덮이지 않는다는 사실, `ci.yml:48-58` 이 기록한 #1557 실패 형태, 그리고 "밖으로 나가야 하면 **같은 변경에서 필터를 확장한다**"는 요구까지 들어왔다. 열거된 경로를 `ci.yml` 원본과 대조했고 정확하다. M1 본문도 "재지정은 paths-filter 안에서(D12)" 로 갱신됐다.

§D 는 "재논의 금지" 구속 조건 절이고 M1 이 이를 참조하므로, AC 가 아닌 구속 조건 형태로 들어간 것은 내가 iter1 에서 지시한 그대로다.

---

## (b) 회귀 — 약화되거나 새로 생긴 것

### 약화된 AC: **없음**

전량 대조했다. 삭제되거나 느슨해진 Then 절은 하나도 없고, 모든 변경은 Then 절 추가이거나 강화다.

- **AC-ZRR-008 은 오히려 강해졌다** — "job 이 `continue-on-error: true` 가 **아니거나**, 기존 테스트 job 에서 실행된다"(OR, 둘 중 하나만 만족하면 통과)에서 "**아니고** ... 실행된다"(AND) + 스텝 억제 금지 조항으로 바뀌었다. iter1 에서 지적한 "OR 의 두 번째 가지가 `|| true` 를 허용한다"가 구조적으로 닫혔다.
- AC-ZRR-002/003/007 은 And 절 추가만, 기존 절 무변경.
- `§D.2 심각도` 표와 `§D.5 Definition of Done` 은 델타가 건드리지 않았다. 새 조건들이 전부 BLOCKING 등급 AC(002/003/007) 안으로 들어갔으므로 "BLOCKING AC 9건 전부 GREEN" 이라는 DoD 가 새 조건까지 자동으로 덮는다 — 등급 희석 없음.
- §G 의 두 행(AC-005, AC-008)은 **거짓 방어 주장이 정정된** 것이다. iter1 에서 "007 이 종료 코드로 잡는다는 주장은 성립하지 않는다"고 지적한 그 자리가 올바른 방어를 가리키도록 고쳐졌고, AC-005 행은 변이 주입만으로는 부족하다는 사실을 명시적으로 적었다. 약화가 아니라 정직성 개선이다.

### 추적성 매트릭스 4건 — **3건 정확, 1건 과장**

리드가 지목한 4개 갱신을 각각 "그 AC 가 정말 그 REQ 를 검증하는가"로 확인했다.

| 갱신 | 판정 | 근거 |
|---|---|---|
| REQ-003 → 002, 003, 004 | **정확** | REQ-003 의 "clause 를 요약으로 다시 쓰지 말 것" 절반은 AC-002/003 의 verbatim 요구가 기계적으로 강제한다(요약은 리터럴 적중 불가). "이사한 곳으로 옮길 것" 절반은 새로 들어온 `file:` 변경 목록 인용 + sync 리뷰가 검증 경로를 만든다. iter1 의 "004 단독, 간접"보다 실질적으로 나아졌다 |
| REQ-008 → 007, 009 | **정확** | AC-007 의 평가 수 단언이 "**두 미러 각각** 101" 이므로 REQ-008("guard shall evaluate both registry mirrors")을 직접 검증한다. AC-009 는 템플릿 변이 검출로 보완 |
| REQ-011 → 007, 008 | **정확** | 확장된 REQ-011(스텝 억제 금지 포함)에 대해 AC-008 이 설정을 읽고 AC-007 이 CI 결론을 실측한다. 두 AC 가 각각 다른 축을 덮는다 |
| REQ-005 → 002, 006 | **과장 (경미)** | REQ-005 는 엔트리 추가·삭제·번호 재배치 금지 + `zone`/`zone_class`/`canary_gate` 불변이다. 이를 온전히 검증하는 것은 **AC-006 뿐**이다. AC-002 가 추가된 논리는 "빈 clause 로 엔트리를 속 빈 껍데기로 만드는 것은 사실상 삭제"라는 것인데, 성립은 하지만 REQ-005 의 문언이 말하는 바는 아니다. **커버리지 결손은 아니다** — AC-006 이 이미 완전히 덮으므로 AC-002 인용은 잉여 장식이다 |

### 새로 생긴 결함 2건 (둘 다 문서 정합성, minor)

**N-1 — `plan.md` §D8 의 참조가 붕 떴다.** D8 이 여전히 "규칙 변경은 구현 세부가 아니라 요구사항 변경이다(REQ-ZRR-015)" 라고 적는데, D-7 수정으로 `REQ-ZRR-015` 에서 바로 그 프로세스 절이 **삭제**됐다(현재 REQ-015 는 "shall declare, in code, the heading-to-slug rule ... with a comment" 뿐). 내용 자체는 D8 이라는 구속 조건으로 살아 있으므로 규범 손실은 없고, 인용만 존재하지 않는 문장을 가리킨다 — Severity: **minor** — Class: **optional** — 수정: D8 의 `(REQ-ZRR-015)` 를 떼거나 "(REQ-ZRR-015 는 선언 의무만 담고, 변경 취급 규율은 이 구속 조건이 진다)"로 바꿀 것.

**N-2 — REQ-ZRR-015 의 번호 순서가 여전히 어긋난다.** D-7 이 프로세스 절 분리는 해냈지만 재배치는 절반만 됐다 — `009`/`010` 사이에서 `011`/`012` 사이로 옮겨졌을 뿐이다. 실측 순서: `001 002 003 004 005 006 007 008 009 010 011 **015** 012 013 014`. "가드" 절 끝에 두려는 편집 의도는 읽히지만 번호는 여전히 비순차다. 집합은 완전하므로 MP-1 은 통과 — Severity: **minor** — Class: **optional** — 수정: `015` 를 `014` 뒤로 옮기거나(가드 그룹 밖이 되므로 소제목 조정 필요), 번호를 재배정할 것. 같은 성격의 사소한 것으로 `plan.md` §D 의 나열 순서도 `D10 → D12 → D13 → D11` 이 됐다.

### 스코프 보존 — **REQ 15 / AC 14, 압축 없음**

실측: `REQ-ZRR-*` 고유 15개, `AC-ZRR-*` 고유 14개, `### AC-ZRR-` 상세 표제 14개. Tier M 상한은 REQ 16 / AC 16 이므로 **REQ 1칸, AC 2칸이 비어 있다.**

리드가 지목한 "쥐어짜기" 여부를 이 여유가 판정해 준다. 네 개의 mutant 방어는 새 AC 를 만드는 대신 기존 AC 의 Then 절로 흡수됐는데, **상한에 2칸이 남아 있었으므로 그 흡수는 예산 때문일 수 없다.** 설계 선택이다. 그리고 그 선택은 타당하다 — 추가된 조건들이 전부 같은 체크(리터럴 적중 검사)가 내놓는 출력의 일부이고, 실제로 판정 명령이 "적중 수 + 빈 clause 수 + 자기참조 수" 3개를 함께 출력하도록 바뀌었다. 별도 AC 로 쪼갰다면 같은 명령을 세 번 인용하는 형태가 됐을 것이다.

검증력 손실도 없다. 흡수된 조건들이 BLOCKING 등급 AC 안에 들어갔으므로 DoD 의 "BLOCKING AC 9건 전부 GREEN" 이 그대로 적용된다. **Tier L 상향을 권고하지 않는다.**

---

## Category Scores

| 차원 | iter1 | iter2 | 밴드 | 변동 근거 |
|---|---|---|---|---|
| Clarity | 0.75 | **0.95** | 1.0 | iter1 의 감점 3건이 전부 해소 — §5 파일 수 오기 정정(내 측정과 대조 확인), REQ-015 복합문 분리, AC-007 변이 대상 고정. 남은 것은 N-1 붕 뜬 참조와 N-2 번호 순서로, 해석 모호성이 아니라 표기 결함이다 |
| Completeness | 0.75 | **0.95** | 1.0 | HISTORY 3행 추가로 마지막 결손 섹션이 채워졌다. §7 의 이중 트리 gap 은 측정 결과와 함께 닫혔고, slug 규칙 gap 은 정직하게 남았다. frontmatter 는 `0.3.0` / `updated: 2026-08-25` 로 갱신돼 여전히 12필드 적합 |
| Testability | 0.75 | **0.85** | 0.75↔1.0 | blocking mutant 4종 중 3종(자기참조·`\|\| true`·부분 순회)이 닫혔고, 특히 CI 결론 실측과 평가 수 단언은 **설정 읽기가 아닌 관측**이라 회피 경로가 없다. 1.0 이 아닌 이유는 단 하나 — 빈 clause 금지가 인스턴스 모양이라 공백 clause 변종이 살아 있다 |
| Traceability | 0.75 | **0.95** | 1.0 | 미커버 REQ 0, 고아 AC 0, 갱신 4건 중 3건이 실질 검증을 동반한다. REQ-003 의 매핑이 간접에서 실질로 올라선 것이 핵심 변동. REQ-005 → AC-002 한 건이 과장이나, AC-006 이 이미 완전히 덮으므로 커버리지 결손이 아니라 잉여 인용이다 |

**Aggregate = (0.95 + 0.95 + 0.85 + 0.95) / 4 = 0.925** — Tier M 임계 0.80 을 넉넉히 상회.

---

## Must-Pass Results (델타 영향 확인)

델타는 must-pass 축을 건드리지 않았고, 건드린 곳은 개선 방향이다.

- **MP-1 REQ 번호 일관성** — **PASS**. 15개, 결번 0, 중복 0 유지. 제시 순서 비순차는 N-2 로 기록(집합 완전성은 무관)
- **MP-2 GEARS** — **PASS**. `REQ-ZRR-015` 가 복합문에서 단일 Ubiquitous 로 정리돼 **개선**됐다. `REQ-ZRR-011` 은 Unwanted 형(`shall not … and the step … shall not …`)을 유지
- **MP-3 frontmatter** — **PASS**. `version: "0.3.0"`(quoted semver), `updated: 2026-08-25`(ISO) 로 갱신, 12필드 전량 유지, 거부 alias 0
- **MP-4 언어 중립성** — **N/A (auto-pass)**. 변동 없음
- **MP-5 D7 cross-SPEC** — **PASS**. `related_specs` 무변경, 참조 2건 모두 `completed`
- **MP-6 D8 cross-platform** — **PASS (auto)**. `syscall` 0건 유지
- **MP-7 clarification gate** — **PASS**. `[NEEDS CLARIFICATION]` 0건

---

## 이월 부채 (run-phase 로 넘어가는 것 — 판정 주체 지정)

**부채 #1 — 퇴화 clause (실질 부채, 반드시 처리)**

- **무엇**: AC-ZRR-002/003 이 금지하는 것은 "빈 문자열" 정확히 그 값뿐이다. 공백 한 칸 또는 아주 짧은 흔한 토큰으로 된 clause 는 모든 파일에 적중하면서 세 카운터를 전부 통과한다 — 빈 clause 경로와 동일한 비용으로 동일한 거짓 GREEN 에 도달한다.
- **run-phase 가 해야 할 것**: M1 의 리터럴 체크를 boolean 적중이 아니라 **적중 횟수**로 구현하고, **각 clause 가 자기 `file:` 안에서 정확히 1회 적중**함을 단언할 것. 유일 적중이 불가능한 엔트리가 나오면 더 긴 구간을 고르고, 그래도 안 되면 최소 길이 하한(20자 권고)으로 대체하되 그 사실을 `progress.md` §E.2 에 적을 것.
- **근거(이 감사에서 측정)**: 현재 적중하는 25건 중 **24건이 정확히 1회 적중**, 2회 이상 1건. clause 길이 median 93자, 20자 미만 3건. 즉 유일 적중 요구는 정상 데이터를 배제하지 않는다.
- **판정 주체**: 구현은 `manager-develop`. **확인은 sync-auditor** — "리터럴 체크가 적중 **횟수**를 출력하는가, boolean 인가"를 §E.2 인용 출력에서 직접 볼 것. boolean 이면 이 부채는 미해소다.

**부채 #2 — 근접 재지정 1건 (사람 판정으로 이관, 기계 판정 불가)**

- **무엇**: 깨진 clause 68건 중 `CONST-V3R2-004`(`16-language neutrality`) 하나만이 다른 이중 트리 파일(`.claude/rules/moai/NOTICE.md`)에 리터럴로 존재한다. 그쪽으로 옮기면 AC-002/003 을 기계적으로 통과한다.
- **defense**: AC-ZRR-002 가 새로 요구하는 `file:` 변경 목록 인용. 이 엔트리가 `NOTICE.md` 로 이동한 채 목록에 나타나면 **거부**해야 한다 — `16-language neutrality` 교리의 서식지는 `coding-standards.md` 의 `#language-policy` 절이다.
- **판정 주체**: **sync-phase 사람 리뷰어**. `plan.md` §H 가 이미 "레지스트리 diff 전체를 읽고 각 clause 가 그 anchor 절 안에 있는지" 를 리뷰어에게 명시 요청하도록 적어 뒀으므로, 그 요청에 이 엔트리를 이름으로 덧붙일 것.

**부채 #3 — 평가 수 단언의 문언 (경미)**

- **무엇**: "평가한 엔트리 수 101" 이 로드 수인지 실제 검사 수행 수인지 구분하지 않는다.
- **판정 주체**: `manager-develop` 이 구현 시 카운터를 **clause 검사와 anchor 검사 각각을 마친 엔트리**에서 증가시킬 것. sync-auditor 는 §E.2 인용 출력에 두 카운트가 따로 보이는지 확인.

**문서 정합성 2건 (N-1 붕 뜬 §D8 참조, N-2 REQ-015 번호 순서)** — run-phase 착수 전 아무 때나. verdict 무관.

---

## Recommendation

**PASS-WITH-DEBT — run-phase 진입을 승인한다.**

blocking 6건 중 5건이 완전히 닫혔고, 닫힌 방식이 대부분 **실측 기반**(CI job 결론 관측, 평가 수 단언)이라 문서상 선언보다 강하다. AC 를 약화시킨 곳이 없고, §G 의 거짓 방어 주장 2건이 정정된 것은 이 SPEC 의 자기 진단 능력이 실제로 작동한다는 신호다. Tier M 은 유지되며 상향 근거는 없다 — 상한에 REQ 1칸·AC 2칸이 남아 있어 흡수가 예산 압박의 결과일 수 없음을 확인했다.

부채 #1 은 run-phase M1 의 **첫 구현 결정**에 걸리는 사안이므로, 착수 시점에 이 한 줄을 반영하면 그대로 소멸한다. `acceptance.md` AC-ZRR-002/003 의 해당 And 절을 다음으로 갱신할 것을 권고한다 — SPEC 재감사 없이 문안만 조정해도 좋고, run-phase 구현이 유일 적중을 단언하는 것으로 갈음해도 좋다.

> 어떤 엔트리의 `clause:` 도 빈 문자열이 아니며, **각 clause 는 자기 `file:` 안에서 정확히 1회 적중한다**(측정: 현재 적중 25건 중 24건이 1회 적중 — 진짜 verbatim 인용은 유일하다). 0회는 미적중이고, 2회 이상은 clause 가 지나치게 짧다는 신호이므로 둘 다 실패로 센다.

이 문안이면 빈 문자열·공백·짧은 토큰이 한 조항으로 전부 닫히고, 부채 #1 은 이월되지 않는다.

---

## 이 감사가 하지 않은 것 (Gaps)

- **델타 한정이다.** `880a43363..a13922293` 이 건드리지 않은 부분은 iter1 판정을 그대로 승계했다 — 델타 밖 AC 본문을 처음부터 다시 읽지 않았다.
- **AC 를 실행해 보지 않았다.** 판정한 것은 문안이 mutant 를 막는지이며, 아직 존재하지 않는 구현의 거동이 아니다.
- **AC-ZRR-013(doctor Fail 0) 의 RED 는 iter1 과 마찬가지로 재현하지 않았다.** fresh init 을 돌리지 않았고, 레인 보고서 인용을 그대로 승계한다.
- **slug 규칙과 실제 markdown 렌더러의 바이트 일치는 여전히 미검증이다.** `spec.md` §7 이 이 항목을 자기 gap 으로 유지하고 있고, 나도 확인하지 않았다 — anchor 실패 17 은 `analyze.py` 재구현 기준의 수치다.
- **근접 재지정 후보 탐색은 "현재 clause 문자열이 다른 파일에 존재하는가"로만 했다.** run-phase 가 clause 를 새 문장으로 바꾸면 후보 집합도 달라지므로, 부채 #2 의 "1건"은 오늘 데이터 기준이다.
