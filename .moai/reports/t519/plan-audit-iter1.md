# SPEC Review Report: SPEC-CODEX-COVER-RESIDUAL-001

Card: t519 · Iteration: 1/3 · Tier M (PASS threshold 0.80)
Tree: `faa76fa7e` on `WT-codex-cover-residual` (worktree `/Users/goos/MoAI/moai-adk-go/.claude/worktrees/t519`)
Auditor: plan-auditor (Claude anchor only — `audit_model` absent from `.moai/config/**`, so no cross-model fan-out; `moai-ref-cross-model-audit` not loaded)

**Reasoning context ignored per M1 Context Isolation.** No author reasoning, prior draft, or conversation history was consulted. The audit reads the four Tier M artifacts, the production sources they name, and the sibling test files they claim to reuse.

**Verdict: FAIL — Overall Score 0.775** (threshold 0.80; blocking 4, advisory 5)

All seven must-pass criteria PASS. The FAIL is driven by the aggregate score: three of twelve acceptance criteria carry an adoption basis that cannot fire or that depends on the host environment, which is the precise defect class `.claude/rules/moai/development/verification-completeness.md` §2 (two-cell adoption) and §1.1 (observed-failure completion) exist to prevent — and which this SPEC's own acceptance.md §A.1 and REQ-CCR-008 declare they are guarding against. Every defect is cheaply repairable, and in all three cases the correct form already exists verbatim in a sibling test the SPEC names as its model.

---

## Claim

The SPEC is a well-constructed, evidence-disciplined plan whose baseline provenance, documented-skip record, coverage-ceiling derivation, out-of-scope discipline, traceability, and frontmatter are correct as written; and whose test-fixture specification is defective in three places such that AC-CCR-002 and AC-CCR-003 assert nothing about the work, and AC-CCR-004 returns a machine-dependent verdict.

## Evidence

### Must-Pass Results

- **[PASS] MP-1 REQ number consistency.** `grep -o 'REQ-CCR-[0-9]\{3\}' spec.md | sort -u` returned exactly `REQ-CCR-001` … `REQ-CCR-010` — ten ids, sequential, no gap, no duplicate, uniform three-digit padding.
- **[PASS] MP-2 GEARS format compliance (requirement layer).** Every `REQ-CCR-XXX` entry in `spec.md` §C carries a GEARS modality: event-driven `When` (001 spec.md:81, 003 spec.md:89, 004 spec.md:95, 005 spec.md:99, 008 spec.md:111, 009 spec.md:117), compound `Where`+`When` (002 spec.md:85), state-driven `While` (007 spec.md:107, 010 spec.md:123, and the second clause of 003 spec.md:91 and 008 spec.md:113), ubiquitous `shall` with a generalized subject (006 spec.md:103, subject "The SPEC artifact itself"). Judgment made against the **requirement layer only**: the twelve `Given … When … Then` entries in `acceptance.md` §C are verification-layer `AC-XXX` and are graded under Group 4, never here.
- **[PASS] MP-3 YAML frontmatter validity.** All 12 canonical fields present with correct types, field-by-field against `.claude/rules/moai/development/spec-frontmatter-schema.md`: `id` (spec.md:2), `title` (:3), `version: "0.1.0"` quoted semver (:4), `status: draft` in-enum (:5), `created: 2026-09-07` ISO (:6), `updated: 2026-09-07` ISO (:7), `author` (:8), `priority: P2` in-enum (:9), `phase: "v3.1.4 target"` (:10), `module: internal/cli` (:11), `lifecycle: spec-anchored` in-enum (:12), `tags` comma-separated string (:15). No rejected snake_case alias (`created_at` / `updated_at` / `labels` / `spec_id`) appears. `phase` is a release target, not one of the four prohibited lifecycle tokens. Optional `era` (:13), `tier: M` (:14), `related_specs` (:16) are permitted additions. Artifact statelessness holds: `plan.md` frontmatter carries no `status:`; `acceptance.md` and `progress.md` carry no frontmatter, which the schema explicitly permits. Independent confirmation: `moai spec lint .moai/specs/SPEC-CODEX-COVER-RESIDUAL-001/spec.md` → `✓ No findings — all SPEC documents are valid`.
- **[N/A] MP-4 language neutrality.** Single-language SPEC — scope is Go test files under `internal/cli/`. Auto-passes per the MP-4 N/A clause.
- **[PASS] MP-5 D7 cross-SPEC reconciliation.** Extraction over all three artifacts yielded two external references. `SPEC-CODEX-TEST-GAPS-001` → `status: completed`; `SPEC-MOAI-MCP-SERVER-001` → `status: completed`. Neither is `retired`, `superseded`, or `archived`, so no reconciliation clause is required and no BLOCKING finding is emitted. Both directories exist, so no D7-5 SHOULD finding either.
- **[PASS] MP-6 D8 cross-platform discipline.** `grep -c 'syscall'` → `spec.md:0`, `acceptance.md:0`, `plan.md:1`. The single occurrence is plan.md:74, a negation inside the Section-B category filter: *"Categories B1, B2, B7, B12 do not apply (no syscall use, …)"*. No syscall is introduced, so the D8-4 auto-pass condition is met in substance.
- **[PASS] MP-7 clarification gate.** `grep -rn '\[NEEDS CLARIFICATION' plan.md acceptance.md spec.md progress.md` returned one hit, progress.md:23, which is prose stating *"No `[NEEDS CLARIFICATION]` markers were emitted."* — not a marker. `research.md` does not exist, which is correct for Tier M. No unresolved marker blocks Implementation Kickoff Approval.

### Verified-correct claims (adversarial checks that did not find a defect)

These were attacked and held. Recording them so iteration 2 does not re-litigate settled ground.

- **The 12-of-13 coverage ceiling is right.** I recounted `runCodexReviewGate` (codex_review_gate.go:183-202) independently of the SPEC: coverage blocks of 2, 2, 4, 2, 1, 1, 1 statements = 13 total. The planned five RunE tests reach every block except the `out = &hook.HookOutput{}` arm at line 199, giving 12/13 = 92.3%. spec.md §D.1's derivation is reproduced exactly, and it is correctly labelled a derivation rather than a measurement.
- **S1 really is unreachable without a production seam.** `HandleCodexReviewGate` (codex_review_gate.go:66-110) returns a non-nil `*hook.HookOutput` on all six documented paths (lines 69, 72, 75, 80, 101, 104, 109); it never returns `(nil, nil)`. The documented skip is justified, not an evasion.
- **The AC-CCR-005 BLOCK path is hermetic.** `withCodexSession` (mcp_codex_test.go:108-117) swaps three seams, not one: `codexRunner`, `codexLookPath` (line 112, to `/fake/codex`), and `codexSession`. The step-4 binary-resolution fail-open at codex_review_gate.go:78 therefore cannot fire in that test. A single `- [P1] …` bullet does synthesize a BLOCK — precedent codex_review_gate_test.go:262-280 uses exactly one bullet and asserts `DecisionBlock`. `hook.DecisionBlock == "block"` (internal/hook/types.go:187) matches what `assertAllowJSON` inspects.
- **The gate-enable key path is right.** `readCodexReviewGateEnabled` exists (mcp_codex.go:2006), and `writeWorkflowYAML` (multi_review_gate_wiring_test.go:33-47) writes to `<dir>/.moai/config/sections/workflow.yaml`, the path that reader consults. The `workflow.codex.review_gate.enabled` spelling in AC-CCR-003 is corroborated by multi_review_gate_wiring_test.go:67, which uses that exact YAML as the codex-spelling control in the sibling reader's truth table.
- **No planned identifier collides.** `grep -rn 'func TestRunCodexReviewGate' internal/cli/` → no output, exit 1; `func TestCodexSessionHandlePid` → no output, exit 1; `func newCodexGateCmd` → no output. Control `func TestRunMultiReviewGate` → three rows from multi_review_gate_wiring_test.go:114/127/139, so the empty results are real absences, not a broken probe. `newGateCmd` is confirmed taken (multi_review_gate_wiring_test.go:102, bound to `runMultiReviewGate` at :104), and D2's rename to `newCodexGateCmd` correctly avoids it.
- **The AC-CCR-009 selector is non-vacuous and correctly shaped.** All six alternation members match the six function names the plan creates (plan.md:123-127, :135), one-for-one. The guidance to match `^--- PASS: Test` with no end anchor is correct — a Go PASS line ends in ` (0.00s)`, so a `$` anchor would silently report zero.
- **AC-CCR-007's union gate is currently green for the stated reason.** `git diff --name-only bf779ecf2..HEAD` lists four SPEC artifacts plus two evidence files; `git status --short` is empty. No `.go` path in either output. This confirms the criterion is GREEN at arrival, which is exactly why acceptance.md:117 adopts it by mutant M6 rather than by RED-now — the reasoning is correct and matches `verification-completeness.md` §2.1.
- **Scope creep is correctly refused.** `reviewableFromPorcelain` (83.3%) and `readHookInput` (85.7%) appear in the baseline extract and are explicitly declined in spec.md §B.2 (marked "adjacent, NOT named by the card") and §F, with the decision and its cost recorded at plan.md:70 (D6). The card named two surfaces; the SPEC delivers two.
- **Baseline provenance is honestly attributed.** spec.md §B.1 names the command, the verbatim output (`coverage: 80.9% of statements`, rc=0), and states plainly that manager-spec did not re-execute the measurement. I read both cited evidence files; the quoted rows match byte-for-byte. The 80.9% vs 81.1% divergence is disclosed and left unreconciled rather than silently averaged.

### Defects Found

D1. **F1 — AC-CCR-004's fixture omits the `codexLookPath` swap; the test's verdict depends on the host machine.** — `acceptance.md:74` and `plan.md:126` — Severity: major — Class: **blocking** — Both Given clauses name three things: gate enabled, `withChangeDetector(t, true)`, `codexSession = &fakeCodexSession{startErr: errFakeCodexCrash}`, `codexRunner = stubCodexRunner{}`. Neither names `codexLookPath`. The production default is `var codexLookPath = exec.LookPath` (mcp_codex.go:368), and `HandleCodexReviewGate` consults it at codex_review_gate.go:78 — **before** the session is ever started. Without the swap the test performs a real PATH lookup for `codex`. Measured on this machine: `command -v codex` → `/Users/goos/.local/bin/codex`, so the lookup succeeds here and the test passes. On a runner without codex installed, step 4 returns ALLOW with `err == nil` (line 80), `gateErr` is nil, the RunE takes the success path at lines 198-201, stderr carries no `codex-review-gate: error:` prefix, and the test fails. That is a local-green / CI-red split, and it also makes the M3a/M3b RED evidence recorded locally unreproducible in CI. It contradicts spec.md §E constraint 3 ("Reuse the existing hermetic seams"). — **Required fix**: add the `codexLookPath` swap to AC-CCR-004's Given and to plan.md §F M1 step 5, copying the four-line block from the precedent `TestReviewGate_FailOpenOnCodexError` (codex_review_gate_test.go:154-158), which swaps all three seams together under one `t.Cleanup`.

D2. **F2 — AC-CCR-003's fixture omits `withChangeDetector`, making its sole adoption mutant M2 vacuous.** — `acceptance.md:64` (Given) and `acceptance.md:187` (ledger row M2) — Severity: major — Class: **blocking** — M2 replaces `enabled := readCodexReviewGateEnabled(projectDir)` with `enabled := true` (codex_review_gate.go:191) and is claimed to fire the `t.Fatal` guard installed by `withCodexLookPath`. It does not. With `enabled` forced true, `HandleCodexReviewGate` advances to step 3, `!reviewGateChangeDetector(projectDir)` at codex_review_gate.go:74. The production detector is `hasReviewableChanges` (codex_review_gate.go:48), and the Given supplies a `writeWorkflowYAML` directory, which is a `t.TempDir()` — a non-git directory, so `git status --porcelain` errors and the function returns false (codex_review_gate.go:129-132). The handler therefore ALLOWs at line 75 and returns **before** `codexLookPath` is reached at line 78; the `t.Fatal` guard never fires and the mutated test still passes. In-repo evidence that a `t.TempDir()` yields false: codex_review_gate_test.go:312-314 asserts exactly that. AC-CCR-003's only adoption basis therefore cannot fire, leaving the criterion unadopted — the defect acceptance.md §A.1 and REQ-CCR-008 are written to prevent. The correct form already exists: the precedent `TestReviewGate_DisabledAllows` pairs the lookPath `t.Fatal` guard with `withChangeDetector(t, true)` at codex_review_gate_test.go:41, commented *"even with changes present…"*, for this exact reason. — **Required fix**: add `withChangeDetector(t, true)` to AC-CCR-003's Given and to plan.md §F M1 step 4. It is inert on the green path (with the gate disabled, step 1 returns at line 69 before step 3 is reached), so the unmutated test's behaviour is unchanged and only M2's detectability is restored.

D3. **F3 — AC-CCR-002's adoption basis is vacuous, and the artifact contradicts itself about it.** — `acceptance.md:26` vs `acceptance.md:186` — Severity: major — Class: **blocking** — The B matrix binds AC-CCR-002's adoption to mutant M1. The ledger's own Expected-effect column for M1 says the edit makes *"AC-CCR-001's stderr assertion fail"* — naming only AC-CCR-001. AC-CCR-002 asserts only that `Execute()` returns nil and that stdout decodes to an ALLOW (acceptance.md:56); deleting the `fmt.Fprintf` stderr diagnostic at codex_review_gate.go:186 changes neither, so AC-CCR-002 passes unchanged under M1. The two surfaces disagree, and the matrix is the wrong one. The ledger already names the detectable edit, but demotes it to a parenthetical *"Alternative if a stronger signal is wanted"*: replacing line 188 with `return err`, which makes `Execute()` return non-nil and flips both AC-CCR-001 and AC-CCR-002. That edit compiles (`err` is in scope inside the `if err != nil` block) and is correct. — **Required fix**: promote the alternative to its own ledger row (`M1b`) and rebind AC-CCR-002's matrix cell at acceptance.md:26 to M1b, so every per-test criterion retains a mutant that can actually fire.

D4. **F4 — plan.md §A.4 mis-attributes four of the eight reused seams to the wrong file.** — `plan.md:37-46` (and `spec.md:147`) — Severity: minor — Class: **blocking** — The table's Location column claims `withCodexLookPath`, `withCodexRunner`, `withCodexSession`, `codexSessionScript`, `fakeCodexSession`, and `stubCodexRunner` all live in `codex_review_gate_test.go`. Measured definitions: `withCodexRunner` mcp_codex_test.go:56, `withCodexLookPath` mcp_codex_test.go:63, `fakeCodexSession` mcp_codex_test.go:74, `withCodexSession` mcp_codex_test.go:108, `codexSessionScript` mcp_codex_test.go:123, `errFakeCodexCrash` mcp_codex_test.go:408, and `stubCodexRunner` **codex_rpc_error_test.go:29** — a third file neither plan.md §A.4 nor spec.md §E constraint 3 names at all. Only `withChangeDetector` (codex_review_gate_test.go:28), `writeWorkflowYAML` (:33), `assertAllowJSON` (:157), `fakeCodexConn` (mcp_codex_test.go:88) and `fakeCodexConnPID` (codex_jobs_test.go:31) are cited correctly. Every identifier is package-level, so nothing fails to compile; the cost is that the table is the implementer's map and it points at the wrong files, inviting a duplicate-helper definition that *would* fail to compile at package scope. — **Required fix**: correct the four Location cells in plan.md §A.4 and add `codex_rpc_error_test.go` to the file list in spec.md §E constraint 3.

### Advisory findings (surfaced, not routed — orchestrator's discretion per M6)

A1. **The "≥90.0%" threshold has no reachable slack; leave it alone.** — acceptance.md:123 — With 13 statements, coverage granularity is 1/13 ≈ 7.7 points, so the only attainable values at or above 90% are 12/13 = 92.3% and 13/13 = 100%. The threshold is therefore already exactly equivalent to "at most one uncovered statement", and the apparent 2.3 points of slack cannot be occupied by any measurement. This resolves the question in the SPEC's favour: do **not** tighten it to 92.3%, which would couple a release-blocking criterion to a derivation spec.md §D.1 deliberately declines to call a measurement.

A2. **None of the six new tests may call `t.Parallel()`.** — plan.md §F — They mutate package-level seams (`codexSession`, `codexLookPath`, `codexRunner`, `reviewGateChangeDetector`), so parallel execution races. The sibling files already observe this — `grep -rn 't.Parallel()'` across codex_review_gate_test.go, multi_review_gate_wiring_test.go and mcp_codex_test.go returned no output — but neither the plan nor any AC states the constraint, and CLAUDE.local.md §6 records a prior incident of exactly this class.

A3. **M5a's RED arrives as a panic, which does not match AC-CCR-010's stated evidence shape.** — acceptance.md:161, :192 — Deleting the nil guard at mcp_codex.go:702-704 makes `(*codexSessionHandle)(nil).pid()` dereference a nil receiver, so the RED is a runtime panic that also aborts the remaining tests in that binary. AC-CCR-010 requires "a verbatim `--- FAIL:` line" per row. Go does emit the `--- FAIL:` line ahead of the panic trace, so the criterion is satisfiable, but the evidence block for M5a will look different from the others — record the panic trace alongside it and run that mutant in isolation.

A4. **spec.md has no section headed WHAT / Scope / Overview.** Scope is carried implicitly by §B.2's target-row table (the Disposition column) and §F Out of Scope, both of which are precise. A reader scanning for a scope heading finds none.

A5. **`moai spec lint` on a directory argument is a linter defect, reproduced.** `moai spec lint .moai/specs/SPEC-CODEX-COVER-RESIDUAL-001` → `ERROR ParseFailure … is a directory`, while the same command against `spec.md` returns clean. progress.md:52 pre-recorded this as a candidate defect carried over from card t500; this run reproduces it. The defect is the linter's, not the SPEC's — worth a `/moai feedback` entry, not a SPEC change.

## Baseline-attribution

Every measurement in this report was taken in this run, against this tree, in this worktree.

- Tree: `git rev-parse --short HEAD` → `faa76fa7e`; `git branch --show-current` → `WT-codex-cover-residual`; `git rev-parse --show-toplevel` → `/Users/goos/MoAI/moai-adk-go/.claude/worktrees/t519`.
- Source facts (line numbers for `runCodexReviewGate`, `HandleCodexReviewGate`, `pid`, the seam `var` declarations, and every helper definition) were read directly from the files in this worktree, not inferred from the SPEC and not carried over from the SPEC's own citations.
- The coverage figures (80.9% package, 0.0% `runCodexReviewGate`, 66.7% `(codexSessionHandle).pid`) are **the lane's measurement on tree `bf779ecf2`**, read from `.moai/reports/t519/coverage-baseline-run.log` and `coverage-baseline-targets.txt`. I did **not** re-execute the coverage run — a ~474s package suite is outside an audit's scope and would measure this loaded machine rather than the code. The SPEC cites them with the same provenance, which is the correct handling.
- The 13-statement count and the 12/13 ceiling are my own derivation from reading codex_review_gate.go:183-202, arrived at independently and only then compared with spec.md §D.1. Both are derivations, not measurements; neither is presented as one.

## Gaps

Explicitly NOT observed. This section is non-empty on purpose.

- **No test was executed.** No `go test` invocation was run against `internal/cli/`, so every statement in this report about what a planned test *would* do under a mutant is a reasoned prediction from reading the control flow, not an observed RED. The predictions for F2 and F3 are strong (each rests on an in-repo assertion that already pins the relevant behaviour — codex_review_gate_test.go:312-314 for F2, and the ledger's own Expected-effect text for F3); the prediction for F1 rests on `var codexLookPath = exec.LookPath` plus a measured `command -v codex`. Run-phase execution is what converts them to observations.
- **Coverage was not re-measured.** The claim that the five planned tests reach exactly 12 of 13 statements is a block-by-block derivation, not a profile. REQ-CCR-009's run-phase re-measurement is what establishes it.
- **The other nine mutants were not applied.** M1, M3a, M3b, M4, M5a, M5b, M6 were reasoned about against the source; none was executed.
- **`golangci-lint`, `go vet`, and `gofmt` were not run.** AC-CCR-011 is a run-phase gate and was not exercised here.
- **The 76 package-wide 0.0% functions were not enumerated or examined.** spec.md §F places them out of scope and this audit accepted that boundary rather than testing it.
- **No cross-model second opinion was obtained.** `audit_model` is absent from `.moai/config/**`, so this is a Claude-only verdict with no codex or GLM corroboration. A defect visible only to a differently-biased reader would not have been caught.

## Residual-risk

- **The three blocking fixture defects share one root cause, so a fourth instance may exist where I did not look.** Each of F1, F2, F3 is the same failure: the AC's Given clause was written from the *intent* of a sibling test rather than from its *text*, dropping a seam or a mutant that the sibling carries. I checked all six planned test items against their precedents, but only against the precedents the SPEC names. If a seventh interaction exists that no sibling test covers, this audit would not have seen it.
- **Fixing F2 by adding `withChangeDetector(t, true)` restores M2's detectability but does not prove it.** The claim that M2 then fires rests on the same control-flow reading as the claim that it currently does not. Run phase must observe the RED, not assume it.
- **A1's granularity argument depends on the 13-statement count being exact.** If Go's cover instrumentation splits the blocks differently than I counted, the effective threshold shifts. The argument's conclusion (leave the threshold at ≥90.0%) is robust either way, since a wrong count only moves where the slack sits, never creates a value between 90% and 92.3% that a correct implementation would land on.
- **F4 is the least certain of the four in its blocking classification.** Nothing fails to compile because of it, and a competent implementer greps for a helper it cannot find. I classified it blocking because a factually wrong location in the artifact that exists to direct the implementer is an incorrectness, not a preference — but an orchestrator that treats it as advisory and fixes it during run phase would not be making an unreasonable call.
- **This SPEC's greatest strength is also its exposure.** It builds nearly all of its release-blocking weight on the mutant-adoption discipline. That discipline is the right one, and it is why three of these findings were findable at all — but it means a defective mutant costs a whole criterion rather than degrading it. Iteration 2 should re-check the ledger as a whole, not only the three rows named here.

---

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.75 | 0.75 — minor ambiguity in one or two requirements | Requirements and criteria are single-interpretation with line-level citations throughout; deduction is for the two under-specified Given clauses at acceptance.md:64 and :74, where a reasonable engineer implements a fixture the SPEC did not describe (F1, F2). |
| Completeness | 0.75 | 0.75 — one non-critical area sparse; frontmatter complete | All required sections present (History §A, baseline §B, requirements §C, skip record §D, constraints §E, out-of-scope §F with five `### Out of Scope — <topic>` H3s each carrying `-` bullets, cross-refs §G); frontmatter complete (MP-3). Deductions: F4's four wrong seam locations and the missing third file, plus A4's absent scope heading. |
| Testability | 0.60 | between the 0.50 and 0.75 anchors — see note | Every AC is binary and mechanically checkable, names a command and an expected observation, and contains no weasel word ("appropriate", "adequate", "reasonable") — 1.0 behaviour on the anchor's literal terms. But three of twelve criteria have a defective adoption basis: AC-CCR-002 and AC-CCR-003 are adopted on mutants that cannot fire (F3, F2), and AC-CCR-004's verdict is host-dependent (F1). The rubric's 0.50 anchor language describes judgment-call ACs, which these are not; the *level* is what applies, since a criterion whose mutant cannot fire asserts nothing at all. Scored between the anchors rather than forced into a band, with the reason stated. |
| Traceability | 1.00 | 1.0 — complete bidirectional coverage | Every REQ has at least one AC and every AC names a valid REQ, per the explicit matrix at acceptance.md:23-36 and the prose summary at :38: 001→AC-001/002, 002→AC-003, 003→AC-004, 004→AC-005, 005→AC-006, 007→AC-007 and AC-012, 008→AC-009/010, 009→AC-008, 010→AC-011. REQ-CCR-006 is deliberately and explicitly routed to the §F Definition of Done rather than a runtime criterion. No orphaned AC, no uncovered REQ. |

**Aggregate**: (0.75 + 0.75 + 0.60 + 1.00) / 4 = **0.775**. Tier M threshold is 0.80 (`.claude/rules/moai/workflow/spec-workflow.md` § SPEC Complexity Tier). 0.775 < 0.80 → FAIL.

## Regression Check

Not applicable — iteration 1.

## Recommendation

FAIL, with a short and entirely mechanical route to PASS. Four fixes, all confined to `acceptance.md` and `plan.md`, none touching scope, requirements, or the coverage threshold:

1. **acceptance.md:74 and plan.md:126** — add the `codexLookPath` swap to AC-CCR-004's fixture. Copy the three-seam block from codex_review_gate_test.go:154-158 verbatim; it already swaps `codexRunner`, `codexLookPath`, and `codexSession` together under one `t.Cleanup`.
2. **acceptance.md:64 and plan.md:125** — add `withChangeDetector(t, true)` to AC-CCR-003's fixture, matching the precedent at codex_review_gate_test.go:41. Inert on the green path; restores mutant M2's ability to fire.
3. **acceptance.md:26 and :186** — split the M1 ledger row: keep M1 (delete the `fmt.Fprintf` at codex_review_gate.go:186) bound to AC-CCR-001, add row M1b (replace codex_review_gate.go:188 with `return err`) and rebind AC-CCR-002's matrix cell to M1b.
4. **plan.md:37-46 and spec.md:147** — correct the four Location cells to `mcp_codex_test.go` at lines 56, 63, 108 and 123, move `stubCodexRunner` to `codex_rpc_error_test.go:29`, and add that third file to spec.md §E constraint 3's file list.

Advisories A1-A5 are surfaced for the orchestrator's discretion and are not routed. A1 in particular is a recommendation to change nothing: the ≥90.0% threshold is correct and should not be tightened.

Per `.claude/rules/moai/development/verification-completeness.md` §7, this audit's decisions rest on that rule's §1.1 (observed-failure completion, empty-sweep hazard), §2 (two-cell adoption and the mutant probe), and §2.1 (RED-now cell content, the undecidable disposition), and on `.claude/rules/moai/development/spec-frontmatter-schema.md` § Canonical 12 Required Fields and § Artifact Statelessness. This SPEC is, on the evidence, an unusually disciplined piece of work whose one weak seam is the gap between a fixture's intent and the sibling test's actual text — and iteration 2 should close on that gap alone.

VERDICT: FAIL score=0.775 blocking=4 advisory=5
