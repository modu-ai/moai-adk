[
  {
    "id": "M-D2-loglstat",
    "desc": "Delta-audit D2 completion: only the log half of the G-B condition replaced by an inline os.Lstat identity check (Lstat both, os.SameFile, fall back to filepath.Abs equality on error) - catches hard links and case variants, misses symbolic links; run only the symlink_log subtests",
    "edits": [
      ["internal/constitution/pipeline.go", "if sameFile(rulePath, registryPath) || sameFile(rulePath, evolutionLogPath) {", "if sameFile(rulePath, registryPath) || func() bool { ia, errA := os.Lstat(rulePath); ib, errB := os.Lstat(evolutionLogPath); if errA == nil && errB == nil { return os.SameFile(ia, ib) }; a, errA := filepath.Abs(rulePath); b, errB := filepath.Abs(evolutionLogPath); return errA == nil && errB == nil && a == b }() {"]
    ],
    "run": ["test", "./internal/constitution/", "-run", "^TestExecute_RuleFileAliasOfRegistryOrLog_Rejected$/^symlink_log$", "-count=1", "-v"],
    "expect_runs": 1,
    "out": ".moai/reports/t659/run/guards/d2b-red-loglstat.txt"
  },
  {
    "id": "M-D2-logabs-case",
    "desc": "Delta-audit D2 completion: only the log half of the G-B condition reverted to plain filepath.Abs string equality; run only the case_variant_log subtests",
    "edits": [
      ["internal/constitution/pipeline.go", "if sameFile(rulePath, registryPath) || sameFile(rulePath, evolutionLogPath) {", "if sameFile(rulePath, registryPath) || func() bool { a, errA := filepath.Abs(rulePath); b, errB := filepath.Abs(evolutionLogPath); return errA == nil && errB == nil && a == b }() {"]
    ],
    "run": ["test", "./internal/constitution/", "-run", "^TestExecute_RuleFileAliasOfRegistryOrLog_Rejected$/^case_variant_log$", "-count=1", "-v"],
    "expect_runs": 1,
    "out": ".moai/reports/t659/run/guards/d2b-red-logabs-case.txt"
  }
]
