# Card t657(큐 병합) M4 실행 판정 — 검증 FAIL 1건과 운영자 유지 결정

- Date: 2026-09-13 · Lane: lane-4 · Branch: `WT-todo-queue-merge` @ dae7e6219 (7a7a08f20 흡수)
- SPEC: `SPEC-TODO-QUEUE-HOME-MERGE-001` (in-progress, M4 executed, M5 보류)
- 주의: `.moai/reports/t657/verdict.md`은 **동일 번호의 구 web 콘솔 카드 판정서**로, 본 문서와 무관(리드 경유 분리 지시). 도구의 `id-mapping.tsv`·`backup-hashes.json` 등 데이터 산출물이 구 디렉터리에 기록된 것은 도구 기본 경로 때문이며 내용상 혼동 없음.

## Claim

M4 라이브 병합을 4중 게이트(창·운영자 승인·§F0 기록·신선도 브래킷) 하에 실행, 무손성(791/791) 통과 — 실행 후 검증이 참조 1건(t204→t538 표기)으로 FAIL. 운영자 판정(리드 채널 2026-09-13): **(A) 병합 유지 + 결함 추적**, REQ-TQM-016 복구는 예외 처리. 레인 쓰기 정지 불필요.

## 검증 FAIL 판정 근거 — 검증기 결함 vs 경계 케이스

1. **매핑 대조**: `id-mapping.tsv`에는 `t204→t718` 행만 존재하고 **`t538` 행은 부재** — t538은 재번호 대상이 아니었다. 재번호되지 않은 id에 대한 참조는 재작성 대상이 아니므로, "t204→t538 not rewritten"은 **매핑 장부와 모순**된다.
2. **무결성 대조**: ZeroLoss=true, TotalPre=TotalPost=791, Renumbered=126=MappingRows — 참조 1건 누락이 레코드 손실을 뜻하지 않음(텍스트 수준).
3. **가설 판정**: (i) 검증기의 참조 장부가 재번호 집합을 잘못 캐시/확장한 **검증기 결함** — 매핑 부재와 모순되는 점에서 유력. (ii) duplicate-origin(원본 유지) 카드 내부 참조는 재작성 스킵이 의도인데 검증기가 그것을 미재작성으로 셈 — **경계 케이스** 가능성. 둘 중 무엇이든 **데이터 결함이 아니라 판독기 결함**이라는 점은 동일하며, 실참조 위치·원문 확인은 후속 카드(리드 발행 예정)의 소관이다.

## 리드 관측 판정 — t204/t718 동일 본문 쌍은 의도된 결과

- 실행 전 백업(`store-1`, 해시 검증본)에 홈 원본 `t204`(queued, "v3.1.3 배포 게이트") 존재를 sqlite 조회로 실측.
- 프로젝트 `t204`(live, 동일 본문)은 REQ-TQM-006 v2 판별식 — "생존(queued/picked/dropped) 카드는 내용 동일과 무관하게 재번호" — 에 따라 t718로 이관. **정상 작동**이며, 홈 원본 t204은 유지.
- 부작용: 내용 동일 생존 쌍이 중복으로 남음(이 판별식의 알려진 비용). 수동 중복 정리 여부는 후속 판단 사항.

## 수치 (baseline-attribution: 이번 세션 관측)

| 단계 | 값 |
|---|---|
| dry-run 새 기준선 | duplicates 246 / renumbered 126 / migrated 0 / high-water 834 / 791=791 ZeroLoss |
| 브래킷 P1 | dry-run과 완전 일치(홈 59/360/708·프로젝트 105/267/661) |
| 실행 후 홈 | live 164 / archived 381 / seq 834 (21:56, 이후 신규 쓰기 0 관측) |
| 병합 트리 재측정 | kanban 37/0 · cli 22/0 |
| 커밋 트리 | dae7e6219 (흡수 후) — develop 병합은 아직 미실행(아래 참조) |

## Gaps

- 검증기 결함의 정확한 위치(장부 캐시 vs 스킵 경계) 미규명 — 후속 카드 소관
- t538 실참조 원문 미확인(어느 카드의 어떤 finding인지) — 후속 카드 소관
- M5(프로젝트 스토어 정리) 미실행 — 별도 승인 대기
- develop --no-ff 병합은 운영자 유지 결정 수령 후 같은 창 안에서 수행 예정(본 문서 작성 시점 기준 미실행 — 창 보유 중)

## Residual-risk

- 병합 상태의 홈 스토어로 레인들이 계속 쓰는 중 — 백업(option (b) 복구 경로)은 실행 시점 스냅샷이므로, 이후 복구를 선택하면 그 사이 쓰기 손실이 발생함이 운영자에게 명시됨(유지 결정으로 무효화)
- 검증기 결함이 다른 참조에도 영향을 줬을 가능성은 배제 못 함 — 후속 카드가 전수 대조해야 함
