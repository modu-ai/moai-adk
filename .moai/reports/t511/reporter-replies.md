# Reporter Reply Drafts — SPEC-REMOVAL-GUARD-EXTRAS-001 (card t511)

> **게시 금지 안내**: 이 파일은 초안이다. GitHub 게시는 리드의 배치 push가 develop에 착지한 뒤 별도 소관으로 수행한다. 두 이슈 모두 리포터가 릴리스에서 직접 확인하기 전까지는 열어 둔다.

---

## 초안 1 — #1658 (jjjh7401 님께)

jjjh7401님, 보고 주셔서 감사합니다. 말씀하신 두 방향이 모두 수리되어 develop 라인에 반영됐고, 다음 릴리스 v3.2.0에 포함될 예정입니다.

1. **플래그 순서 우회(너무 좁은 방향)** — 기존 정규식은 플래그 묶음을 문자 그대로 비교했기 때문에 `rm -fr /`처럼 두 글자만 뒤바꿔도 통과했습니다. 이제 정규식 매칭 대신 명령어를 토크나이즈한 뒤 실제 삭제 대상 경로를 판정하는 구조적 체크로 보호 대상을 판단합니다. `rm -rf /`, `rm -fr /`, `rm -r -f /`, `rm --force --recursive /` 등 셸에서 동등한 모든 형태가 동일하게 차단됩니다.

2. **데이터 언급 오탐(너무 넓은 방향)** — 기존 방식은 명령어 전체 텍스트를 스캔했기 때문에 `echo "rm -rf /"`처럼 위험한 문자열을 **출력하거나 저장만 하는** 명령까지 거부했습니다. 이것이 바로 이 결함 자체를 문서화·테스트·보고하기 어려웠던 원인이었습니다. 이제 명령어 실행이 실제로 위험한 지만 판정하므로, 데이터로서의 언급(heredoc 본문 포함)은 허용됩니다.

한 가지 남았던 잔여 문제 — 빌트인 정규식을 걷어낸 뒤에도 배포되는 `security.yaml` 템플릿의 extras 목록에 같은 결함의 잔여 사본이 남아 같은 오탐을 냈던 부분 — 도 이번 정리에서 함께 제거했습니다. 이 이슈는 리포터님께서 v3.2.0 릴리스에서 직접 확인해 주신 뒤에 닫겠습니다.

---

## 초안 2 — #1686 (hansooha 님께)

hansooha님, 재현과 보고 정확했습니다. 지적해 주신 오탐의 마지막 잔여 경로가 제거되어 develop 라인에 반영됐고, v3.2.0 릴리스에 포함될 예정입니다.

원인은 배포되는 보안 설정 템플릿(`security.yaml`)의 `extra_dangerous_bash_patterns`에 남아 있던 정규식 한 줄이었습니다. 빌트인 가드가 이미 구조적 판정(명령어 토크나이즈 + 실제 삭제 대상 경로 판정)으로 바뀐 뒤에도, 이 extras 정규식은 텍스트 매칭으로 명령어 전체를 훑었기 때문에 heredoc 본문이나 문서 텍스트 안에서 위험한 제거 명령 형태의 문자열을 **언급만 해도** 거부했습니다. 이번 변경으로 이 잔여 사본 3곳(배포 템플릿·로컬 설정·테스트 픽스처)이 같은 커밋에서 제거됐고, 배포 정책이 실제로 로드되는 양방향 회귀 테스트가 추가되어 재발을 잡습니다. 보호 대상에 대한 실제 삭제는 이전과 동일하게 차단됩니다.

이 이슈도 리포터님께서 v3.2.0 릴리스에서 직접 확인해 주신 뒤에 닫겠습니다. 확인에 문제가 있으면 이 이슈에 다시 알려 주세요.

