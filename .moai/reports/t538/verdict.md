# t538 — 카드 증거 색인

카드: t538 · SPEC: `SPEC-DOCS-LOCALE-PARITY-REPAIR-001` · Tier M
트리: `.claude/worktrees/t538` · 브랜치: `WT-docs-v313-locales` · 베이스: `bce6d7e08`

## 판정

AC 11건 전건 PASS (HEAD `91fd27bb6` 기준). RED-now 6건(AC-001·002·003·006·007·008)은
편집 **전에** 실제로 RED임을 관측했고 편집 후 GREEN을 관측했다. 부재를 확인하는 가드류
기준은 기능이 없을 때도 저절로 만족되므로, 편집 전 RED 관측이 유일한 판별 증거다.

## 증거 파일 (이 디렉터리)

| 파일 | 담은 것 |
|---|---|
| `ac-verification.md` | AC별 baseline/after 원출력. baseline 은 `git show d787897ca:<경로>` 로 재유도했으므로 이 파일만으로 전 수치를 재유도할 수 있다 |
| `hugo-build.log` | `hugo --source docs-site` 전체 출력 (exit 0 · WARN/ERROR 매치 행 0 · sitemap.xml 생성 · 4-로케일 187/185/185/185) |
| `plan-phase.md` | plan 페이즈 측정 SSOT |
| `plan-audit-iter1.md` · `plan-audit-iter2.md` | plan-audit 1·2차 |

## 검증식 결함 2건 — 정본은 `acceptance.md` `## HISTORY`

두 건 모두 카드 안에서 수리했다. 경위·before/after·실측은 `acceptance.md` 의 `## HISTORY`
절이 정본이며, **여기에 옮겨 적지 않는다** — 두 벌이 되는 순간 갈라진다. 감사자가 무엇을
읽어야 하는지만 지목한다.

- **AC-008** — `grep -c` 는 매칭 **행** 수를 세므로 한 문단 안의 두 토큰은 상한이 1이고
  `≥2` 기준은 도달 불가였다. 출현-계수 형태로 개정. **저자 승인** — 옛 읽기와 새 읽기
  양쪽에서 기준이 충족되어(4-로케일 전부 2, 양쪽 다) 개정이 판정에 실리지 않았기 때문이다.
- **AC-011** — 전체-브랜치 diff 에 10파일 기준을 건 형태는 도달 불가였다. 그 계수는 카드가
  자기 장부를 쓰는 동안 계속 움직인다(`bbc37b729` 에서 17, `fbc9cc6b1` 에서 18). 대상
  범위 한정 형태로 개정하고, 전체-브랜치 계수는 **보고 수치**로 존치했다. **리드 승인** —
  옛 읽기가 실패했으므로(18 대 10) AC-008 에 쓴 저자 측 안전 조건이 성립하지 않는다.
  조건을 완화한 것이 아니라 승인 주체가 바뀐 것이다.

## 알면서 받아들인 편차 1건 (간극 아님)

ko 정본은 SVG 규칙 코드 범위를 en dash(`SVG060`–`SVG064`)로, 파생 3개 로케일은 ASCII
하이픈(`SVG060-SVG064`)으로 적었다. 어떤 AC도 이 부호를 재지 않고, 파일마다 내부적으로는
일관되며, 내용은 이미 착지했다. 활자 부호 하나를 맞추려 착지한 커밋을 다시 만지는 것은
범위 밖이라 판단해 수리하지 않았다.

## 잔여 위험

- 판정은 로컬 관측이다. 깨끗한 환경·매트릭스 판정은 원격 develop CI 몫이며, 이 카드에서
  아직 관측되지 않았다.
- docs-site 의 Vercel 프로덕션 프로젝트가 특정 브랜치에 묶여 있어, develop 에 docs-site
  변경이 들어갈 때 프리뷰/프로덕션 배포가 어떻게 반응하는지는 **미검증**이다.
- i18n 게이트 판정은 변경된 문서 9파일 범위에 한정된다. 그 밖 페이지는 스캔하지 않았다.
