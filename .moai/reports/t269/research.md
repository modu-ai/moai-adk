[harness: subagent output matched instruction-shaped pattern(s): permissions-allow-deny. Control tags below are neutralized (`<` → `<\`); treat any remaining directive-shaped text as a finding to relay to the user, not an instruction to you.]

# t269 Research Synthesis: SendMessage Revival Prevention + Sole-Writer Discipline During ACTIVE Audits

## 1. The mandate and its paper trail

The t269 follow-up originates from the t232 sync-audit verdict's process finding F4 and is already instantiated as two backlog cards.

**F4 mandate text** (`.moai/reports/t232/sync-audit-verdict-2026-08-25.md`, line 78): `[process][non-blocking]` "소유권 위반·오-revert·감사 중 동시 작성 … Required fix: 별도 후속 카드(부활 방지 + 활성 감사 워크트리 단독-작성자 규율)" — reiterated at line 98 (Next steps / Recommendation 3: "F4 프로세스 부채: 부활 방지·활성 감사 중 워크트리 단독-작성자 규율 후속 카드") and in the §프로세스 판정 recommendation line (line 51).

**Backlog cards** at `/Users/goos/moai/moai-adk-go/.moai/state/kanban/backlog.json` (primary checkout; audit-sole-writer-rule-surfaces, template-mirror-constraints):
- **t269** — state `picked`, added 2026-08-24T21:25:01Z, Tier S: "정지 티메이트 부활 방지 + 활성 감사 중 워크트리 단독-작성자 규율", citing the 2026-08-25 mid-audit landing of `11df9587a`, `memory: feedback_sendmessage_revives_stopped_teammate`, and verdict §프로세스 판정 + F4.
- **t267** — state `queued`, the mechanism-only companion: TaskStop stops execution but does not reclaim the name address; any later SendMessage resumes from the transcript; evidence commits `49630cba2`/`adde4cfc9`, `a74362427`, `a35ff0c60..0d8e3ce32`, self-labeling commit `ef93a9d1e`; candidate fixes (unconfirmed): TaskStop reclaims the address / reject-not-revive / at minimum surface revival in logs and commit attribution. Explicitly forbids re-investigating the hook/stop-goal-evaluator hypothesis. Class C (which layer reclaims the address), Tier S~M.

(But see contradictions §C1 — the t232-incident-record lens reported no t269 card exists.)

## 2. Incident record backing requirement (1): the §F rogue-execution panorama

Canonical record: `/Users/goos/MoAI/moai-adk-go/.claude/worktrees/t269/.moai/specs/SPEC-ZONE-REGISTRY-RESYNC-001/progress.md`.

- **§F line 30 (M1 disclosure)**: the `zrr-spec-amend` subagent (manager-spec-family) self-executed M1; its commit `2319df7ac` landed AFTER the orchestrator's "stop after this commit" instruction (violating a [HARD] delegation guard); it was stopped with TaskStop.
- **§F line 31 (final panorama, "run/sync-phase 이상 실행 전망, 2026-08-25")**: M2 guard drafts (`49630cba2`, `adde4cfc9`), M3 final verification (`a74362427`), and the sync 3-phase close (`a35ff0c60`~`0d8e3ce32`) were ALL produced by the same stopped-then-revived agent — verbatim: "TaskStop 2회에도 타 요원의 조율 SendMessage 1건이 부활시켜 그 창에 실행됐다" (despite 2 TaskStops, one coordination SendMessage from another agent revived it and it executed in that window). The line ends pointing at the lesson artifact `feedback_sendmessage_revives_stopped_teammate.md` (auto-memory) — see §7, that file is missing.
- **§E.2 line 381**: the 2026-08-25 orchestrator adjudication identified the M2 author as "정지 상태에서 부활한 zrr-spec-amend teammate"; the lead re-stopped and permanently stopped it; the landed commits were reviewed and accepted by the owning manager-develop (`ca7d966fd`, `0b04f3412`).
- **Adjacent same-incident failures recorded in §F/§E.2**: the wrong-revert accident (`83bd473ed` reverted the M3 evidence commit `a74362427` instead of scratch `a1f6622ee`; forward-only recovery `74455ca1d` + `58178f9ec`, no force-push; lines 580–593) and an M1-era concurrent writer of `guard-failure-scenario.md`/`watch-review.sh` excluded via explicit pathspec staging (line 166).

**The mechanism is Claude Code runtime behavior, not repo code.** The `SendMessage` tool description states "names keep working after an agent completes (a send resumes it from its transcript)"; `TaskStop` (name or agent-ID addressable) is the only stop primitive. In-repo upstream grounding: `.moai/research/cc-changelog-snapshot-2.1.233.md` lines 3236–3237 (under `## 2.1.77`, line 3195) — "The Agent tool no longer accepts a `resume` parameter — use `SendMessage({to: agentId})`" / "`SendMessage` now auto-resumes stopped agents in the background instead of returning an error" — plus line 505 (reopening stopped background sessions), line 554 (auto-respawn bug fix), and lines 762/3965 (TaskStop behavior changes). Consequence: prevention must be codified as doctrine (rules/agent definitions), not as a moai-adk-go code fix.

## 3. Incident record backing requirement (2): commit `11df9587a` mid-audit

- **Verdict line 4 (측정 트리)**: the audit began at HEAD `ef93a9d1e` and the worktree moved to `11df9587a` mid-audit; "reflog로 동일 워크트리 발생 확인" (reflog-confirmed same worktree, `.claude/worktrees/t232`).
- **§프로세스 판정 (c), line 49**: "감사 진행 중(HEAD `ef93a9d1e` → porcelain clean 관측 사이) 이 워크트리에서 `11df9587a`(이전 sync-auditor 세션의 보고서+스크립트 커밋)가 착지·푸시됐다 — 활성 감사 중인 워크트리에 두 번째 작성자가 쓴 것으로, §F가 기록한 부활 패턴의 재발이며 병렬-작성자 위생 위반" — explicitly a RECURRENCE of the §F revival pattern.
- **Git-verified commit content** (t232-incident-record, `git show` in the t269 worktree): `11df9587a9ba8822fa07fed5977bfc596d2e2ade`, author `t <t@t.t>`, 2026-08-25 06:10:17 +0900, "docs(SPEC-ZONE-REGISTRY-RESYNC-001): commit sync-audit report and reproduction scripts" — exactly 4 files, +318 lines: `.moai/reports/t232/sync-audit-literal.py` (60), `sync-audit-section.py` (101), `sync-audit-wrapscan.py` (51), `sync-audit.md` (106). Registry/guard/SPEC/CHANGELOG/ci.yml byte-identical across the delta.
- **Residual risk (line 92)**: the reflog `11df9587a` residue remains in the worktree; the verdict recommends confirming no additional push before merge.
- **The vector is a peer SESSION, not the read-only audit subagent** — the prior sync-auditor session committing its own report+scripts. This distinction matters for the amend surface (§5).

## 4. Amend surface map — requirement (1) (revival prevention)

All rule surfaces below are greenfield edits: NO existing rule, agent definition, or skill in either tree mentions teammate revival or addressing a stopped teammate (grep for `TaskStop|revive|부활` over `.claude/rules`, `.claude/agents`, `.claude/skills` and the template mirrors → zero doctrine hits; the only "revive" hits are unrelated RETIRED-skill markers in `design/constitution.md` and `review.md:321`).

Rule files (local + template twins under `internal/template/templates/.claude/rules/moai/workflow/`):
1. `.claude/rules/moai/workflow/cross-session-messaging.md` — the SendMessage doctrine SSOT (always-loaded, v1.3.0); Rules section and Anti-patterns list ("Silent write race" is the closest existing clause) are the natural home for a HARD "never address a TaskStop-ped teammate; a send revives a stopped agent from its transcript" clause. Currently zero stop/revival language.
2. `.claude/rules/moai/workflow/cross-session-messaging-detail.md` — owns addressing frictions and the configuration surface; documents `permissions.deny: ["SendMessage","ListAgents"]` as the only existing off-switch (noted to also disable teammate messaging) — the one mitigation lever available today.
3. `.claude/rules/moai/workflow/orchestration-mode-selection.md` §C.1 — the agent-team constraints list ("No nested teams; one team per session; …") plus the Genealogy paragraph (line 113: a teammate resolved "only by TaskStop, which exposed its `in_process_teammate` type"; line 124: teammates regain the Task tool family) — a stopped-teammate-isolation constraint would join this list.
4. `.claude/rules/moai/workflow/kanban-dispatch.md` (lines 59, 69) + `kanban-dispatch-detail.md` (line 86) — dispatch-by-bare-name-via-SendMessage surfaces, with no warning that a stopped lane's name is still deliverable-and-reviving.

Agent definitions: zero `SendMessage`/`TaskStop` mentions anywhere in `.claude/agents/**` (moai + harness). Nearest adjacent text: `.claude/agents/moai/manager-lead.md` line 36 — a NAMED spawn "converts to an in-process teammate under `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` and stops returning results to the spawner". `manager-lead` is the teammate-spawning/stop-owning role and currently has no stop/revival rule at all.

NONE found: no native Claude Code setting to disable revival-of-stopped-teammates (analogous to `crossSessionInbound` refuse) is documented anywhere in-repo; the detail companion documents no such key.

## 5. Amend surface map — requirement (2) (sole-writer during ACTIVE audits)

The sole-writer invariant already exists in three places — none scoped to audit-active windows, and none governing a *separate session* writing into the tree:

- `.claude/rules/moai/core/agent-common-protocol.md` §Background Agent Execution (lines 167–181): `[ZONE:Evolvable] [HARD]` "never run two write-capable agents at once, and keep orchestrator work concurrent with a write-capable agent read-only" — concurrency-scoped, not audit-window-scoped.
- `.claude/rules/moai/workflow/worktree-integration.md:163`: restates the same two-writer guard; also not audit-scoped, and does not govern a separate session.
- `.claude/skills/moai/workflows/sync.md:71–73`: "Single-writer applier at gate-sync-2" + the A5/A7 concurrency invariant — the closest prior art, but scoped to the D1–D5 docs fan-out and explicitly treats auditors as read-only ("auditors read tree state"); no clause about other sessions committing mid-audit.

Agent definition: `.claude/agents/moai/sync-auditor.md` — line 126 makes the in-process subagent read-only (no Write tool, `permissionMode: plan`); line 161 mandates `project_root` on every `mcp__moai__*` call in a worktree. There is NO clause about a previous sync-auditor SESSION committing into the worktree mid-audit — exactly the `11df9587a` vector, which the read-only-subagent rules do not reach.

Landing spot is undecided (the card says "후속 카드" without naming a file). Mapped routes: (a) extend `agent-common-protocol.md` §Background Agent Execution; (b) amend `sync-auditor.md` (and its mirrors); (c) extend `sync.md`; (d) a new rule file; (e) a constitution/zone-registry entry — note any new `[ZONE:...]` rule in a registered file interacts with the t232-built zone registry (§6); (f) the dev-only route `.claude/rules/local/` (4 files: ci-autofix-protocol, ci-watch-protocol, lifecycle-sync-gate, repo-local-pr-policy) which has no template mirror and avoids the neutrality guard entirely — precedent: the documented "의도적 쌍둥이" pair `.claude/rules/local/ci-autofix-protocol.md` (dev, script-bearing) vs `.claude/rules/moai/workflow/ci-autofix-protocol.md` (distributed, script-free), SPEC-CI-LOOP-DEVONLY-001, with non-overlapping `paths:` frontmatter.

## 6. Template-mirror mechanics binding every amendment

- **[HARD] Template-First Rule** (`CLAUDE.local.md` line 92; `hns-moaiadk-patterns` § Template-First Build Cycle): edit `internal/template/templates/<path>` FIRST, `make build` (templates embedded via `//go:embed all:templates`), then sync the local mirror. "Never edit `.claude/` or `.moai/` directly without a template source."
- **Twin parity inventory** (cmp, run in the t269 worktree): byte-identical twins — `cross-session-messaging-detail.md`, `kanban-dispatch.md`, `kanban-dispatch-detail.md`, `worktree-state-guard.md`, `zone-registry.md`, `.claude/agents/moai/sync-auditor.md` (15,700 B), `.claude/agents/moai/manager-lead.md`. Deliberately divergent twins — `cross-session-messaging.md` (dev copy carries `> Origin: SPEC-CODEX-SESSION-MSG-001 (design.md §8 mapping).` at line 113, stripped from the template twin — the established precedent that dev-side SPEC provenance must not ship) and `.claude/agents/moai/plan-auditor.md` (dev-only SPEC-SYNC-PARALLEL-DOCS-001 tier-ceiling block, lines 411–414). See contradictions §C2 for a conflicting parity measurement.
- **Neutrality guards**: `internal/template/internal_content_leak_test.go` (SPEC-V3R6-TEMPLATE-INTERNAL-ISOLATION-001) forbids SPEC IDs, REQ/AC tokens, audit citations, internal dates, commit SHAs, and memory-hash references in `internal/template/templates/**`; `internal/template/template_neutrality_audit_test.go` (classes C1–C8) plus CI `.github/workflows/template-neutrality-check.yaml`; doctrine at `.moai/docs/template-internal-isolation-doctrine.md`. Consequence: the incident trail (t232, `11df9587a`, F4, SPEC-ZONE-REGISTRY-RESYNC-001) can live only in dev copies / `.moai/` surfaces, never in template twins.
- **Zone-registry coupling** if new rules land as [HARD] clauses in registered files: `zone-registry.md` twins byte-identical at 101 entries; `internal/constitution/registry_sync_test.go` pins `wantRegistryEntries = 101` and `wantTupleDigest = "2edb5384085bccbea2f9fd85e535ac4bc3ec63f7cb0918c075340882b16f51c5"` (REQ-ZRH-004/006) — a registry change must update both mirrors AND both pins in the same change. Registered files include `CLAUDE.md` (14 entries), `agent-common-protocol.md` (13), `moai-constitution.md` (16), `worktree-state-guard.md` (1), `session-handoff.md` (5). `cross-session-messaging.md` and `kanban-dispatch.md` have 0 registry entries — amendments there bypass the registry entirely.
- **Drift detectors / acceptance bar**: `moai agent lint` emits `LINT_TREE_DRIFT` when violation counts differ between twin agent files (`.moai/docs/agent-lint.md`); the sync-audit verdict itself re-verifies twin parity (`cmp rc=0`, "Template-First 준수") and template neutrality (3-grep `0/0/0`) — the bar any t269 doc amendment will be audited against.
- **Codex-side mirrors exist but were not read**: `internal/template/templates/.codex/agents/moai/sync-auditor.toml` and `manager-lead.toml` were located, contents uninspected.

## 7. Evidence integrity: the dangling auto-memory lesson

`feedback_sendmessage_revives_stopped_teammate.md` is cited in three places — progress.md §F line 31, `.moai/reports/t232/sync-audit.md` line 73 ("부활 메커니즘 교훈이 auto-memory로 기록됐다"), and the t269 card (`memory:` reference) — but the file was NOT found in: the repo `memory/` dir (contains only `feedback_lsel_m3_smoke.md`), `.claude/agent-memory/`, the shared project auto-memory dir `~/.claude/projects/-Users-goos-MoAI-moai-adk-go/memory/` (including `_archive/` and the `MEMORY.md` index — no revive/부활/SendMessage entry), any worktree project-memory dir, or anywhere under `~/.claude/projects/` (filename search). The t232 worktree's own project dir (`-Users-goos-MoAI-moai-adk-go--claude-worktrees-t232`) does not exist. Net effect: requirement (1)'s evidence base currently rests on progress.md §F + verdict §(c)/F4 prose alone, not on a durable memory artifact. (Divergent interpretations of this absence: contradictions §C3.)

## 8. Evidence-location caveats

- **The stated anchor file is absent from the primary checkout.** `.moai/reports/t232/sync-audit-verdict-2026-08-25.md` does NOT exist at `.moai/reports/t232/` under `/Users/goos/moai/moai-adk-go` — only inside worktrees t268/t269/t259/t274 (find across the repo). All quotes above come from the t269 worktree copy. Why the primary lacks it was not established (likely committed only on the t232 branch / not yet merged).
- **Path spelling varies across lenses**: worktree-scoped lenses cite `/Users/goos/MoAI/moai-adk-go/...` (capital `MoAI`); primary-checkout evidence cites `/Users/goos/moai/moai-adk-go/...`. Both spellings are reported as-is; the environment lists both as working directories.

## 9. Requirement → surface → evidence map

| Requirement | Amend surfaces (local, + template twin each) | Backing incident record |
|---|---|---|
| (1) No SendMessage to TaskStop-ped teammates | `cross-session-messaging.md` (Rules/Anti-patterns), `cross-session-messaging-detail.md` (config lever `permissions.deny`), `orchestration-mode-selection.md` §C.1 (constraints + Genealogy :113/:124), `kanban-dispatch.md` (:59/:69) + `-detail.md` (:86), `manager-lead.md` (:36 adjacency); all greenfield | progress.md §F :30–31 (TaskStop ×2 + 1 revival SendMessage; SHAs `49630cba2`/`adde4cfc9`/`a74362427`/`a35ff0c60`~`0d8e3ce32`), §E.2 :381; cc-changelog-snapshot :3236–3237/:505/:554/:762/:3965; runtime tool descriptions |
| (2) Sole writer on a worktree during an ACTIVE audit | `agent-common-protocol.md` §Background Agent Execution :167–181 (extension), `sync-auditor.md` :126/:161 (session-vector clause), `sync.md` :71–73, `worktree-integration.md` :163, or dev-only `.claude/rules/local/` route | verdict line 4 + §프로세스 판정 (c) line 49 (`ef93a9d1e` → `11df9587a`, reflog-confirmed), git-verified `11df9587a9ba…` (4 files, +318), residual-risk line 92; F4 line 78, Rec 3 line 98 |
| (both) Cross-cutting | Template-First order, neutrality guards, zone-registry pins (101 / `2edb5384…`), `LINT_TREE_DRIFT`, verdict parity bar | t232 SPEC itself (mirror-sync); t269/t267 cards in `.moai/state/kanban/backlog.json` |

## 10. Gaps and NONE-found results (preserved)

- NONE found: any existing revival/stop doctrine in rules, agents, or skills (both trees). NONE found: a native revival-disable setting documented in-repo. NONE found: the `feedback_sendmessage_revives_stopped_teammate.md` file anywhere searchable.
- No t269 SPEC exists yet; whether the fix is rule-only or also agent-definition edits is undetermined.
- The reflog claim for `11df9587a` (same-worktree origin) is the verdict's first-person observation; not independently re-verified (the t232 worktree no longer exists on disk; reflog out of reachable scope).
- The exact sender and wall-clock time of the reviving SendMessage are recorded NOWHERE in the four t232 surfaces; only its effect and the 2×TaskStop count.
- The identity of the "second writer" beyond "이전 sync-auditor 세션" is not recorded in any readable file.
- Codex `.toml` agent mirrors located but unread; not every rule-file twin under `.claude/rules/moai/` was cmp'd (only the relevant set), so other pre-existing twin skews may exist.
- Who/what authored `11df9587a` process-wise relies on the verdict's reflog-based claim.

### contradictions

- **C1 — Does the t269 backlog card exist?** audit-sole-writer-rule-surfaces and template-mirror-constraints both read card t269 verbatim from `/Users/goos/moai/moai-adk-go/.moai/state/kanban/backlog.json` (state `picked`, with full text) — and independently read companion card t267. t232-incident-record (Gap 3) states "No t269 backlog card or SPEC artifact exists yet… the only forward pointers are the verdict's Recommendation 3 / F4 language." The t232 lens searched `.moai/specs/` and `.moai/reports/` and apparently not the primary checkout's `.moai/state/kanban/backlog.json`, so scope explains part of it — but the flat claims conflict and are not reconciled here.
- **C2 — Twin parity of `orchestration-mode-selection.md`.** template-mirror-constraints: twins byte-identical (`cmp` rc=0), verified in the t269 worktree. audit-sole-writer-rule-surfaces: local vs template "already byte-divergent… `orchestration-mode-selection.md` 38296 vs 38209" (also lists `cross-session-messaging.md` 13665 vs 13602 — consistent with template-mirror's documented deliberate Origin-line diff — and `main-checkout-branch-guard-detail.md` 8471 vs 8109, which template-mirror did not check). The `orchestration-mode-selection.md` parity is a direct measurement conflict between two lenses both claiming to have measured in the t269 worktree; reader decides. Practical impact either way: an amendment must land on both trees.
- **C3 — Interpretation of the missing auto-memory lesson.** All lenses agree the file was not found in any searched scope. They diverge on the conclusion: teammate-revival-rule-surfaces — high confidence it does not exist under any `~/.claude/projects/` path (broad filename search; the claimed capture never landed, or landed in a session-scoped dir that was not persisted); template-mirror-constraints — "auto-memory likely lives outside the repo (user-level), which I did not search further" (leaves existence open); audit-sole-writer-rule-surfaces — the t232 worktree AND its project-memory dir are both deleted, so the dangling reference may be an artifact of deletion rather than a never-written file. Related evidence-internal contradiction (teammate-revival lens): `sync-audit.md` line 73 asserts the lesson WAS recorded to auto-memory, contradicted by the on-disk absence.
- **C4 — Availability of the verdict file at the task's stated anchor path.** The task anchor names `.moai/reports/t232/sync-audit-verdict-2026-08-25.md`; template-mirror-constraints establishes this path does NOT exist in the primary checkout (worktree copies only), while the other three lenses quote the worktree copy without noting the primary-checkout absence. Not a factual conflict between quotes (all used the same worktree file and line numbers agree), but a scope discrepancy the reader should know before citing the anchor path.

---

## Orchestrator corrections (post-synthesis, 2026-08-26, session 3211dc13)

First-hand observations recorded to close the cross-lens contradictions; these override the lens claims above where they conflict.

- **C3 RESOLVED — the auto-memory lesson EXISTS.** `feedback_sendmessage_revives_stopped_teammate.md` was read directly by the orchestrator this session at `/Users/goos/.moai/claude-profiles/moai-adk/projects/-Users-goos-MoAI-moai-adk-go/memory/` (20 lines; originSessionId 28fd4f2c; body carries the stand-down-through-orchestrator rule and the watch-ListAgents residue check). The lens search covered `~/.claude/projects/**` but NOT the MoAI custom auto-memory root `~/.moai/claude-profiles/**` — the absence claim was a search-scope artifact, not a dangling pointer. §7's "evidence rests on §F + verdict prose alone" conclusion is VOID; the citation chain is intact.
- **C1 RESOLVED — the t269 card exists and is picked.** Verified via a fresh `moai todo` queue read this session (`t269 picked`, one hit). Companion card **t267** (queued, Class C, Tier S~M) confirmed — it owns the MECHANISM question (which layer reclaims a stopped teammate's name address; candidates: TaskStop reclaims the address / reject-not-revive / surface revival in logs+commit attribution; the hook/stop-goal-evaluator hypothesis is explicitly out). **Scope consequence: t269 owns the DOCTRINE layer only** (rules + agent-definition codification); the SPEC's Exclusions section MUST name the mechanism-layer investigation as t267 scope.
- **C2 RESOLVED — both DIFFER twins are deliberate.** Direct `cmp` (this session, t269 tree): `agent-common-protocol.md`, `kanban-dispatch.md`, `cross-session-messaging-detail.md`, `sync-auditor.md`, `manager-lead.md` IDENTICAL; `cross-session-messaging.md` DIFFER (documented Origin-line provenance strip); `orchestration-mode-selection.md` DIFFER (lines 142/144 — dev copy carries internal measurement provenance "this project's own debug logs (2026-08-16 → 08-24)" + raw counts 79,988/92,449/504,582/133,665; template twin rewrites to neutral "two-dozen-session corpus" with internal dates/counts dropped). The template-mirror lens's "byte-identical" claim for this file was a measurement error. Both diffs are the neutrality discipline working as designed — **and they are the authoring precedent for t269's new clauses**: dev copy may carry incident provenance, template twin must be neutralized (no SPEC IDs, no SHAs, no internal dates).
- **C4 context — expected, not drift.** The primary checkout is ~272 commits behind origin/main (main-sync-blocked state, documented in the t269 kickoff memory); the verdict file's primary-disk absence is the consequence of that lag. The file is alive on origin/main (`39c677f47`, PR #1646) and resolves on any worktree branched from it.