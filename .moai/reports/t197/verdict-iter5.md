# t197 — SPEC-CODEX-LAUNCHER-001 iteration 5 판정 (분리 후 첫 감사)

| 항목 | 값 |
|---|---|
| 대상 | `SPEC-CODEX-LAUNCHER-001` (분리 후 14 REQ / 16 AC) |
| 내용 핀 | `8d1618ed4`, 트리 깨끗함 — 감사 중 이동 없음 |
| 실행 | `mcp__moai__audit_multi`, `target=.moai/specs/SPEC-CODEX-LAUNCHER-001` |
| 일자 | 2026-08-24 |

## VERDICT — **FAIL** (점수는 통과선 초과, 그러나 차단 4건)

**점수 0.81 / 1.00 — Tier M 통과선 0.80 을 처음으로 넘겼다.** 그런데 백엔드 본문 판정은 `FAIL — merge-blocking` 이고 차단 4건이 남아 있다. **점수와 판정이 갈리는 첫 사례** 다.

| 차원 | iter4 | **iter5** | 변화 |
|---|---:|---:|---|
| 명확성 | 0.75 | 0.75 | — |
| 완전성 | 0.75 | 0.75 | — |
| 시험 가능성 | 0.50 | **0.75** | **+0.25 — 3회 정체 후 처음 움직임** |
| 추적성 | 0.50 | **1.00** | **+0.50** |
| **총점** | **0.63** | **0.81** | **+0.18** |

**분리가 효과가 있었다.** 시험 가능성이 3회 연속 0.50 에 머물다 처음 올랐고, 추적성이 1.00 으로 회복됐다. 4차 판정서에서 "예산 압박이 커버리지를 깎고 있다" 고 읽은 진단이 점수로 확인된 셈이다.

## 백엔드별 판정

| 백엔드 | 게이트 | verdict 필드 | 본문 판정 | 채택 |
|---|---|---|---|---|
| claude | required | pass | pass | pass |
| codex | required | fail | **FAIL 0.81 · 차단 4건** | **FAIL** (관례대로 본문 우선) |
| glm | advisory | inconclusive | — | fail-open (5회 연속 타깃 인식 실패) |

누적 필드-본문 일치: 5회 중 3회 일치 (불일치 2회, 둘 다 필드가 관대한 방향).

## 요청한 5개 확인 결과

| # | 확인 항목 | 결과 |
|---|---|---|
| 1 | `tokens` 키 의미 검증 | **통과** — 무관 키·`account_id` 전용이 음성 fixture 이고 인정 키 집합이 명시됐다. 키를 안 보는 구현은 표에서 떨어진다 |
| 2 | 배선 6상태 행렬 | **핵심 통과** — 빈 디렉터리 행이 디렉터리-존재 판정을 떨어뜨리고, 맨몸/`status` 일치도 요구한다. 다만 F4 잔존 |
| 3 | REQ→AC 전수 참조 | **통과** — 기계 측정: requirements 14 / referenced 14 / unreferenced 0 / extra 0. AC-CL-015·016 복원 확인 |
| 4 | 산문-전사본 드리프트 | **실패** — F1 잔존 |
| 5 | 배선 판정 단일 정의 / 초기화 분리 | **부분** — 소유권 서술은 맞으나 F3 잔재 |

## 차단 4건 — 전부 직접 확인함, 4건 다 유효

### F1 (Medium) — 드리프트가 **부분만** 해소됨

내가 "정정했다" 고 보고한 항목인데 절반만 고쳤다. 실측:

| 위치 | 적힌 값 | 전사본 |
|---|---|---|
| `measurement.md:53` | **18초** | `probe-output.txt:20` 은 **17초** |
| `spec.md:45`, `plan.md:41` | `17~46초` 범위 | 전사본에 **46초 관측이 없다** |

낡은 핀(`6bfb076bc`)과 롤아웃 수치는 제거됐지만, 숫자 두 개가 남았다. 46초는 **이전 실행** 의 값이고 그 전사본은 커밋돼 있지 않다 — 즉 현재 근거 집합으로는 뒷받침되지 않는 수치를 활성 산문이 계속 주장한다.

### F2 (Medium) — `probe.sh` 가 여전히 실패를 가린다

**4차 판정서에서 내가 직접 지적하고 고치지 않은 항목이다.** `set -e` 가 없고 `run()` 이 `eval` 의 상태를 출력만 한 뒤 마지막 `echo` 의 상태를 반환한다. 감사 재현: `inner_rc=1 / continued=yes`. 조상 확인이나 측정 명령이 실패해도 전사본은 전체 성공으로 끝난다. `mktemp` 미검사도 그대로다.

### F3 (Medium) — 초기화 잔재가 런처에 남아 **참조가 끊겼다**

**이건 내가 분리 작업에서 만든 breakage 다.** 실측:

```
acceptance.md:180  "수락된 초기화의 쓰기는 AC-CL-015 가 판정한다."
acceptance.md:200  "## AC-CL-015 — 공유 러너 무회귀"     ← 전혀 다른 항목
measurement.md:96  "(REQ-CL-015/016 은 run-phase 에서 관측)"  ← 런처에서 삭제된 REQ
```

AC-CL-012(쓰기 없음)가 "수락된 초기화" 를 예외로 빼면서 존재하지 않는 대상에 위임한다. 초기화가 이 SPEC 밖으로 나갔으므로 그 예외 자체가 무의미하고, 남아 있으면 REQ-CL-013 의 "어느 동사에서도 쓰지 않는다" 계약과 모순된다.

### F4 (Medium) — 불완전 배선 상태에 조치 문구가 요구되지 않음

`acceptance.md:38` 은 `.codex/` **없음** 행에만 `moai init --agent codex` 를 요구하고, 빈 디렉터리·한쪽 파일만 행(39-41)은 상태값만 요구한다. AC-CL-006 도 부재 케이스만 시험한다. 그런데 `spec.md:114` 의 REQ-CL-006 은 **모든 불완전 상태** 에 조치를 요구한다. 따라서 빈/부분 배선에서 조치를 빼먹는 구현이 통과한다.

## 감사가 확인한 비발견

- 커밋은 문서 전용, 코드 변경·범위 확장 0
- 자격 증명 패턴 스캔에서 프로덕션성 비밀 0
- `git diff --check` rc 0, `spec lint --strict --json` → `[]`

## 상태 — 정지

**지적 4건 중 어느 것도 고치지 않았다.** 자체 반복 금지 규율에 따른다.

다만 성격이 갈린다는 점은 기록해 둔다:

| 지적 | 성격 | 판단 |
|---|---|---|
| F3 | **내가 분리 작업에서 만든 끊긴 참조** | 설계 판단이 아니라 명백한 breakage. 고치는 데 두 줄 |
| F1 | 내가 "정정했다" 고 보고한 것의 미완 | 숫자 두 개 |
| F2 | 4차에서 내가 지적하고 방치한 것 | `run()` 반환값 수정 |
| F4 | 새 지적 | REQ 와 AC 의 범위 불일치 |

넷 다 기계적이고 설계 재검토를 요구하지 않는다. 점수(0.81)는 이미 통과선을 넘겼고, 본문 FAIL 은 이 잔여 4건 때문이다. 처리 방향은 리드·운영자 판단.
