# t197 — SPEC-CODEX-LAUNCHER-001 iteration 6 (확정 감사) 판정

| 항목 | 값 |
|---|---|
| 대상 | `SPEC-CODEX-LAUNCHER-001` (14 REQ / 16 AC) |
| 내용 핀 | `064f9147e` (부모 `82c46c24c`), 트리 깨끗함 |
| 실행 | `mcp__moai__audit_multi`, `target=.moai/specs/SPEC-CODEX-LAUNCHER-001` |
| 일자 | 2026-08-24 |

## VERDICT — **FAIL** (차단 1건), 점수 **0.88 / 1.00**

통과선 0.80 을 크게 넘겼고 지난 라운드 4건 중 **3건이 닫혔다**. 그러나 본문 판정은 여전히 `FAIL — merge-blocking` 이고, 남은 1건은 **내가 세 번째로 부분만 닫은 같은 항목** 이다.

| 라운드 | 총점 | 차단 |
|---|---:|---:|
| iter4 | 0.63 | 5 |
| iter5 | 0.81 | 4 |
| **iter6** | **0.88** | **1** |

## 백엔드별 판정

| 백엔드 | 게이트 | verdict 필드 | 본문 판정 | 채택 |
|---|---|---|---|---|
| claude | required | pass | pass | pass |
| codex | required | fail | FAIL 0.88 · 차단 1건 | **FAIL** (본문 우선) |
| glm | advisory | inconclusive | — | fail-open (6회 연속) |

## 닫힌 3건

| # | 항목 | 감사 확인 |
|---|---|---|
| F3 | 분리로 끊긴 참조 | **Pass** — AC-CL-012 가 네 형태 전부를 예외 없이 덮고, 낡은 REQ 참조는 INIT-001 로 재조준됨 |
| F2 | `probe.sh` 실패 은폐 | **Pass** — `mktemp` 검사, `FAILURES` 집계, 비영 종료 확인. 감사가 직접 재현: `UNEXPECTED: rc=1` → `PROBE FAILED` → 최종 rc 1. 증거가 되는 rc 1 세 지점은 `run_rc 1` 로 선언 (probe.sh:72·101·104) |
| F4 | 조치 문구 범위 | **Pass** — AC-CL-004 가 불완전 다섯 상태 전부에 요구하고 `wired` 에서는 금지, AC-CL-006 이 5상태 × 2형태 10칸 |

## 남은 차단 1건 — F1 (Medium), **세 번째 부분 종료**

기준선 핀이 문서마다 다르다. 실측:

| 위치 | 적힌 값 |
|---|---|
| 전사본 (실제 실행 시점) | **`82c46c24c`** |
| `spec.md:36` | `01f5c531b` |
| `measurement.md:20` | `8d1618ed4+` |

`plan.md:7` 은 분기점 `9280c96b3` 을 전사본 줄 범위에 귀속시키는데 **전사본에는 분기점을 출력하는 명령이 없다**. 그리고 17~46초 범위가 "근거 아님" 이라 명시된 채로 활성 산문에 남아 있다.

**이 항목의 이력**: iter4 에서 처음 지적 → iter5 에서 "정정했다" 보고했으나 절반 → iter6 에서 다시 절반. 31초 수치는 이번에 전사본과 맞췄지만 **핀 자체를 맞추지 않았다.** 전사본을 재생성할 때마다 실행 시점 커밋이 바뀌는데 그것을 인용하는 문서를 따라 고치지 않는 것이 원인이고, 같은 원인이 세 번 반복됐다.

→ 필요: 핀을 `82c46c24c` 하나로 통일, 17~46초 괄호 삭제, 분기점 귀속은 전사본에 명령을 추가하거나 귀속 자체를 제거.

## 감사가 확인한 비발견

- 초기화 로직 잔재 0 — 조치 문구·이력 서술·소유권 경계·INIT-001 의존만 남음
- 커버리지 14 REQ / 14 참조 / 16 AC (Tier M 상한 정확히)
- `spec lint --strict --json` → `[]`, `git diff --check` rc 0
- 자격 증명 형태 문자열 0건, 프로브의 `rm -rf` 는 검증된 임시 디렉터리로 한정됨

## 상태 — 정지

**남은 1건을 고치지 않았다.** 자체 반복 금지 규율에 따른다.

성격 기록: 순수 문서 정합 문제이고 SPEC 본문의 게이트 품질과는 무관하다. 다만 **같은 항목을 세 번 연속 부분만 닫았다** 는 사실 자체가 내 작업 방식의 문제를 가리킨다 — 전사본을 재생성하면 그것을 인용하는 문서 전부를 같은 커밋에서 훑어야 하는데, 매번 눈에 띄는 것만 고쳤다.
