# SPEC 감사 판정서 (4회차, 확인 감사) — SPEC-PREMERGE-SETTINGS-DRIFT-001

카드 t488 · 반복 4 · Tier M(PASS 임계 0.80)
감사 트리: `/Users/goos/MoAI/moai-adk-go/.claude/worktrees/t488` · 브랜치 `WT-premerge-drift-assert` · HEAD `615d18c1f`
대상: **v0.5.0** (브리프는 v0.4.0으로 안내 — §0 참조)
기준선: `plan-audit.md`(0.725) · `plan-audit-iter2.md`(0.875) · `plan-audit-iter3.md`(0.895) — 셋 다 미변경

**판정: PASS** · 종합 **0.930** (임계 0.80, 3회차 대비 **+0.035**)

MP-7이 더 이상 적용되지 않는다(미해결 마커 0건). Must-pass 전 항목이 PASS 또는 N/A이고 점수가 임계를 크게 넘긴다. **이 카드의 첫 PASS다.**

남은 것은 blocking 1건(문구 하나)과 optional 2건이며, 어느 것도 승인을 막지 않는다. **Implementation Kickoff Approval 준비가 됐다** — §7에 조건 없이 적었다.

---

## §0 사전 확인 — 디스크는 v0.5.0이다

```
$ git rev-parse --show-toplevel   →  /Users/goos/MoAI/moai-adk-go/.claude/worktrees/t488
$ git branch --show-current       →  WT-premerge-drift-assert
$ git rev-parse --short HEAD      →  615d18c1f
$ moai spec lint .moai/specs/SPEC-PREMERGE-SETTINGS-DRIFT-001/spec.md
✓ No findings — all SPEC documents are valid          rc=0
$ grep -c "NEEDS CLARIFICATION:" spec.md plan.md acceptance.md   →  0 / 0 / 0
REQ 16 · AC 13
```

**브리프는 v0.4.0이라 했으나 디스크는 `version: "0.5.0"`이다.** HISTORY가 그 사이를 설명한다(`spec.md:25`): 리드가 이 회차를 marker 1 적용 회차로 지시했는데 marker 1은 v0.4.0에서 이미 종결돼 있었고(리드 측정이 v0.4.0 편집 이전 시점), 수리 측이 디스크를 재측정해 확인한 뒤 **중복 적용하지 않았다**. v0.5.0의 실제 델타는 M3 픽스처 문단 하나다 — "bare 원격은 만들되 브랜치 push를 잊는" 실수 모양과 구성 직후 확인(`plan.md:172-174`).

지시받은 대로 하지 않고 **먼저 재측정한 뒤 하지 않기로 판단한 것**이 옳다. 지시를 그대로 이행했다면 이미 닫힌 마커를 두 번 적용해 문서가 어긋났을 것이다. 감사자로서 이 판단에 이견이 없다.

내가 감사한 것은 디스크의 v0.5.0이다. 리드가 보고한 네 수치(마커 0 / REQ 16 / AC 13 / `plan.md:40` 기본값 `false`)는 v0.5.0에서도 전부 그대로다.

---

## §1 Must-Pass

| 항목 | 판정 | 근거 |
|---|---|---|
| MP-1 REQ 번호 | PASS | 001-016 연속, 결번·중복 0 |
| MP-2 GEARS | PASS | 16개 전부 패턴 대응. REQ-PSD-016은 Unwanted("게이트하지 않는다"가 아니라 "거절 층만 게이트한다 / 생략되지 않는다") 형식에 맞는다 |
| MP-3 프론트매터 | PASS | 12필드, `version: "0.5.0"`, 거부 별칭 0 |
| MP-4 언어 중립성 | N/A | 변화 없음 |
| MP-5 D7 교차 SPEC | PASS | 참조 SPEC 2건 status 불변 |
| MP-6 D8 syscall | PASS | 3개 아티팩트 0건 |
| **MP-7 clarification** | **PASS** | 미해결 마커 **0건**. 운영자가 계열 (가)를 골라 marker 1이 닫혔고, marker 2는 v0.4.0 이전에 닫혔다 |

예산: 요구 **16 / 16**(포화), 수용 13 / 16.

---

## §2 차원별 델타

| 차원 | 1회차 | 2회차 | 3회차 | **4회차** | 근거 |
|---|---|---|---|---|---|
| Clarity | 0.75 | 0.90 | 0.92 | **0.93** | (d-3)이 두 저장소를 이름으로 지목하고 그 이유까지 적는다. 부재 단정 규율이 3문단으로 늘며 실측 근거와 정직한 예외(정상 상태에서 빈 모집단)를 함께 담는다. 감점: 잔류 문구 3건(§4) |
| Completeness | 0.75 | 0.90 | 0.90 | **0.94** | N5-N8 종결 + REQ-016/AC-013 신설(아무도 지목하지 않은 구멍) + marker 종결. 요구 예산 포화지만 결손 없음 |
| Testability | 0.70 | 0.80 | 0.88 | **0.94** | 이번 회차의 최대 상승. AC-013이 "킬 스위치를 게이트 전체에 건" 구현을 실제로 반증하고, (d-3)의 내용 기반 대조가 **실측된** 퇴화 경로를 닫으며, 여섯 번째 뮤턴트가 표와 DoD 양쪽에 들어왔다 |
| Traceability | 0.70 | 0.90 | 0.88 | **0.91** | 요구 16개 전부 반증 AC 보유, 뮤턴트 표 ↔ DoD 일치 회복, AC-013 ↔ REQ-016. 감점: `plan.md:180`이 `AC-PSD-012`와 어긋난다(R1) |

종합 = (0.93 + 0.94 + 0.94 + 0.91) / 4 = **0.930** (3회차 0.895, +0.035).

궤적: 0.725 → 0.875 → 0.895 → **0.930**. 정체도 퇴행도 없다.

---

## §3 지목된 7문항

### 2 — REQ-PSD-016 / AC-PSD-013: 추론도 옳고 AC도 실제로 반증한다

**추론이 옳다.** 운영자가 (가)를 고를 수 있었던 근거는 "기본값에서 막지는 않지만 **검출·보존·원장은 매 `acquire`마다 돈다**"였다. 킬 스위치를 거절 층이 아니라 게이트 전체에 걸면 기본값에서 **검출조차 하지 않고**, 그러면 t334가 9일간 아무에게도 걸리지 않은 상태가 그대로 재현된다. 즉 그 구현은 단순히 요구 하나를 어기는 것이 아니라 **선택을 정당화한 전제를 무너뜨린다.**

**그리고 그 구현은 기존 AC를 전부 통과했을 것이다.** AC-001~006은 술어층이라 플래그를 거치지 않고, AC-007/008의 Given은 설정을 지정하지 않아 픽스처가 거절 층을 켜 두면(AC-009가 이제 그러듯) 관찰층이 도는 상태에서 측정된다. AC-009/010은 명시적으로 켠 설정이고, AC-011/012는 `preflight` 판정층이다. **기본 설정에서 `acquire`를 돌려보는 AC가 하나도 없었다.**

**AC-PSD-013이 그것을 반증한다.** 기본 설정(미지정 → `false`) + 적중 픽스처 + 우회 플래그 없음에서 (a) lock이 생기고(거절 안 됨) (b) 보존 사본이 바이트 일치로 생기고 (c) 원장 한 줄 (d) 출력에 drift와 보존 경로. 게이트 전체를 게이트한 구현은 플래그가 꺼져 있으므로 (b)(c)(d)가 전부 없다 → FAIL. (a)는 이 AC를 AC-009(켠 설정)와 갈라 주는 성분이다. **반증한다.**

자발적으로 추가한 것이 실제 구멍을 닫은 첫 사례다.

### 3 — 세 상태 carrier: 설계는 옳고, **잔류 1건이 있다**

carrier 설계 자체는 정확하다. 두 경로를 각각 막는다 — 불리언은 "측정 못 함"을 `false`로 접어 오탐으로 만들고, 필드 생략은 소비자 기본값(`.drift // false`)이 통과로 읽는다. 기본 OFF 게이트에서는 **아무것도 멈추지 않으므로** 후자가 곧 9일 실명의 재현이다. `plan.md:107`이 이 논리를 그대로 적어 뒀다.

`status`가 상시 필드이고(REQ-PSD-012), `undetermined`에서 `match_count`를 **생략**하며(0은 그 자체로 통과 신호라서), AC-PSD-011이 `status == "undetermined"`를 **적극 단정**한다("`clean`이 아니다"로 대신하지 않는다) — 셋 다 맞다.

**그러나 두 상태 잔류가 하나 남았다.** `plan.md:180`(M4의 AC-012 청구 항목):

> 보존 실패가 판정을 뒤집지 않는 것 — REQ-PSD-015 / `AC-PSD-012`. … **`drift`가 여전히 `true`**이고 `match_count`가 1이다

불리언 형태다. 같은 목록의 바로 위 항목은 `status`가 `"undetermined"`로 옳게 쓰였고, `AC-PSD-012` 본문(`acceptance.md:38`)도 `status`는 여전히 `"drift"`로 옳다. `plan.md:106`의 JSON 모양에는 불리언 `drift` 필드가 **아예 없다**. 즉 이 한 줄만 v0.3.0 표현이 남았다 → R1(§4).

### 4 — 내 N6 메커니즘은 틀렸다. 처방이 rc로 만족됐겠는가: **부분적으로 그렇다**

수리 측이 재현한 두 경우를 **내가 직접 다시 재현했다**(3회차 §9에서 안 했다고 밝힌 그 측정이다).

```
$ git init -q --bare bare.git
$ git ls-remote --heads bare.git > a.out 2> a.err
healthy_bare_no_branch rc=0
stdout_bytes=0  stderr_bytes=0            ← 오류가 전혀 없다

$ git ls-remote --heads nope-does-not-exist.git > b.out 2> b.err
nonexistent rc=128
stdout_bytes=0  stderr_bytes=198
fatal: 'nope-does-not-exist.git' does not appear to be a git repository
```

**수리 측 주장이 정확하다.** 설정 오류는 `rc=128` + stderr라 **잡힌다**. 진짜 위험한 것은 **정상 bare 원격에 브랜치를 push하지 않은 상태** — `rc=0`, stdout 빈 문자열, stderr 빈 문자열. **오류가 아니라 성공이다.** 어떤 종료 코드 검사로도, 어떤 error 체크로도 잡히지 않는다.

내 3회차 N6은 이 공허 경로를 "**측정 실패**"라고 이름 붙였다. 틀렸다 — 위험한 경우는 실패가 아니라 **성공인데 결과가 퇴화한 것**이다. 메커니즘을 잘못 짚었다.

**"처방을 문자 그대로 이행했다면 rc로 만족되는 구현이 나왔겠는가" — 두 부분으로 나뉜다.**

- **구체 처방은 아니다.** 3회차 §8 P1에서 나는 "사전 `ls-remote` 출력이 **비어 있지 않으며 픽스처의 브랜치를 담는다**"라고 적었다. 이것은 내용 기반이고, 그대로 이행하면 퇴화 경로가 막힌다.
- **일반 조항은 그렇다.** 같은 항목에서 상시 조항에 넣으라고 제안한 문구는 "**두 측정이 모두 실제로 성공했음**을 보인 뒤에만 유효하다"였다. 이것은 자연스럽게 `if err != nil` / `rc == 0` 검사로 읽히고, 조항은 **앞으로의 AC에 일반화되는 물건**이다. 새 부재 단정에 이 조항을 적용하는 구현자는 종료 코드 검사를 쓰고 조항을 만족했다고 여길 것이다 — 그리고 위 실측대로 그것은 아무것도 막지 못한다.

즉 **두 지점은 우연히 맞혔고, 그 지점들을 일반화하는 규칙은 틀렸다.** 규칙이 더 오래 살아남는 물건이므로 이쪽이 더 나쁜 오류다. 수리 측이 "성공 확인"을 "**사전 출력의 내용**"으로 좁힌 것이 정확한 교정이고, `acceptance.md:9`가 실측을 근거로 함께 적어 뒀다.

부수로: 3회차 §9에 "이 실패 케이스를 재현해 보지 않았다"를 명시해 둔 것이 이번에 작동했다. 미검증을 미검증이라 적어 두면 다음 사람이 잰다.

### 5 — (가) 자세 재작성: 뮤턴트 여전히 포착 가능, **경계 사례 산문에 잔류 1건**

`AC-PSD-009`/`AC-PSD-010`의 Given이 `workflow.settings_drift_gate.enabled: true`를 **픽스처가 명시적으로 켠다**로 바뀌었고(기본값이 `false`임을 괄호로 밝힌다), `plan.md:165`가 M2 뮤턴트 2의 포착 픽스처를 같은 설정에 못박았다 — "기본 설정으로 구성하면 거절 경로 자체가 돌지 않아 뮤턴트가 잡히지 않는다". **판정 반전 뮤턴트는 여전히 잡힌다.**

거절을 기본으로 가정하는 AC가 남았는지 훑었다 — AC-009/010은 명시적으로 켜고, AC-011/012는 판정층(자세 무관, 본문이 그렇게 적고 있다), AC-013은 정반대(기본 설정), AC-001~008은 술어·게이트층이다. **AC 층에는 잔류가 없다.**

**산문에는 있다.** `acceptance.md:46` 경계 사례:

> 보존 디렉터리 쓰기 실패 → **거절 판정**은 그대로 유지하고, 보존 실패를 별도로 보고한다

(가) 자세에서 기본값은 거절하지 않으므로 "거절 판정"이 유지될 것이 없다. 정본인 REQ-PSD-015는 "**drift 판정**을 통과로 뒤집지 않는다"이고 AC-PSD-012도 `status`는 여전히 `"drift"`다. 이 줄만 v0.3.0 표현이 남았다 → R3(§4).

### 6 — 형제 2건: 둘 다 같은 모양이고 범위 이탈이 아니다

- **`AC-PSD-008`의 파일 증감 단정** — "대상 트리 안에 새로 생기거나 사라진 파일이 없으며"는 모집단 위의 부재 단정이다. 사전 파일 목록이 통째로 비면 "증감 0"이 아무것도 주장하지 않는다. 내가 3회차에 sha256 쪽만 이름 붙이고 이쪽을 놓쳤다. **같은 모양이 맞다.**
- **`plan.md` M2 서두 "아래 넷을"** — 표는 v0.2.0부터 다섯 행이었다. 즉 **내가 v0.2.0·v0.3.0을 두 번 감사하면서 두 번 놓친** 불일치다. 그리고 이것은 N7(뮤턴트 수가 DoD와 어긋남)과 **정확히 같은 결함 계열**이다 — 열거와 그 개수가 갈리는 것. 이번에 "아래 **여섯**"으로 고쳐지며 표·서두·DoD 셋이 맞았다. **범위 이탈이 아니라 내 누락의 보정이다.**

### 7 — 요구 예산 16/16 포화: 내 발견 중 17번째를 요구하는 것은 **없다**

R1/R2/R3은 전부 기존 문장의 문구 교정이다. 새 요구를 만들지 않는다.

**포화에 대한 처방을 묻는다면: 지금은 아무것도 하지 않는 것이 옳고, 필요해지면 통합이지 tier 상향이 아니다.**

- 이 카드의 실제 규모는 Tier M이 맞다 — 술어 하나, 감시 파일 하나, CLI 표면 둘. Tier L로 올리면 `design.md` + `research.md`가 의무가 되는데, 그 둘이 답할 질문이 이 카드에 없다. 형식 비용만 늘고 품질은 그대로다.
- 17번째가 정말 필요해지면 통합 후보가 이미 있다. 1회차에 optional(D12)로 지목한 두 곳이다 — `REQ-PSD-003`의 둘째 문장("어떤 수용 기준도 종료 코드에 의존하지 않는다")은 검증층을 구속하는 절이라 `acceptance.md:5` 규율과 중복이고, `REQ-PSD-013`은 뮤턴트 반증·argv 단일 원천·게이트 경로 확장 셋을 한 요구에 담고 있어 쪼갤 수도 합칠 수도 있다. 어느 쪽이든 요구 하나를 벌 수 있다.
- 수용 기준은 13/16이라 여유가 있다. 두 상한이 독립이므로 AC를 늘리는 방향의 강화는 지금도 막히지 않는다.

### 1 — 새 결함: **3건, 전부 잔류 문구** (§4)

---

## §4 이번 회차의 발견

세 건 모두 **같은 원인**이다 — carrier 교체와 자세 재작성이 요구층과 AC층에는 닿았는데 연결 산문 세 조각에 닿지 않았다.

### R1. `plan.md:180`이 두 상태 표현을 유지해 `AC-PSD-012`와 어긋난다

- 위치: `plan.md:180`
- 심각도: **minor** · 분류: **blocking**

M4가 청구하는 AC-012의 기대값을 "`drift`가 여전히 `true`"로 적는다. 그런데 `plan.md:106`의 `--json` 모양에 불리언 `drift` 필드는 존재하지 않고, `AC-PSD-012` 본문은 `status`는 여전히 `"drift"`다.

blocking으로 두는 이유는 이 줄이 **구체적인 JSON 필드 모양을 지목**하기 때문이다. 구현자가 이 줄을 따르면 존재하지 않는 필드를 단정하거나(즉시 빨강 — 그나마 낫다), 명세에 없는 불리언 필드를 추가해 carrier가 둘이 되는(세 상태 설계가 조용히 무너지는) 쪽으로 간다. 다만 같은 문서 안에 옳은 출처가 둘(`:106`, AC-012 본문) 있어 오래 살아남기는 어렵다 — 그래서 major가 아니다.

**필요한 수정**: `` `drift`가 여전히 `true`이고 `` → `` `status`가 여전히 `"drift"`이고 ``. 한 구절이다.

### R2. `acceptance.md:5` 판정 규율이 `status`를 판정 근거 목록에서 빠뜨린다

- 위치: `acceptance.md:5`
- 심각도: **minor** · 분류: **optional**

"판정은 일치 줄 수(`match_count`), 파일의 존재/바이트, 구성된 argv 문자열로 내린다" — carrier가 바뀌기 전 목록이다. AC-011/012/013이 이제 `status`로 판정하므로 열거가 실물보다 좁다.

금지 조항이 아니라 열거라 실질 피해가 없어 optional로 둔다. 고친다면 목록에 `판정 상태(status)`를 더하면 된다.

### R3. `acceptance.md:46` 경계 사례가 거절을 기본으로 전제한다

- 위치: `acceptance.md:46`
- 심각도: **minor** · 분류: **optional**

"거절 판정은 그대로 유지하고" — (가) 자세에서 기본값은 거절하지 않는다. 같은 줄이 가리키는 정본(REQ-PSD-015 / AC-PSD-012)은 둘 다 "drift 판정"으로 옳게 쓰여 있어 오독이 오래 가지 않는다. optional.

**필요한 수정**: "거절 판정" → "drift 판정".

---

## §5 패턴이 깨졌는가 — 리드가 판단을 맡긴 질문

**깨졌다. 다만 그렇게만 말하면 부정확하므로 갈라서 적는다.**

1-3회차가 심은 것과 4회차가 남긴 것은 **종류가 다르다.**

| | 1-3회차가 심은 것 | 4회차가 남긴 것 |
|---|---|---|
| 성질 | **검증을 무력화하는** 결함 — 반증 불가 AC, 공허한 부재 단정, 잘못된 포착기 귀속 | **낡은 연결 산문** — 옳은 정본과 어긋나는 요약 문구 |
| 결과 | 깨진 구현이 초록으로 통과할 수 있다 | 읽는 사람이 헷갈린다. 공허한 초록은 만들지 않는다 |
| 발견 시점 | 다음 회차 감사 | 같은 문서 안의 다른 두 출처가 즉시 반박 |

**공허한 초록을 만드는 결함을 심는 패턴은 이번에 멈췄다.** R1-R3 중 어느 것도 잘못된 구현을 통과시키지 않는다.

무엇이 달라졌는지도 이번 회차 안에 증거가 있다.

- **내가 미검증이라 밝힌 것을 실제로 쟀고, 그 결과 내 메커니즘이 틀렸음을 찾아냈다**(§3-4). 앞선 세 회차에서 수리 측은 내 처방을 충실히 이행했고, 그래서 내 처방이 틀린 만큼 결함이 들어왔다. 이번에는 처방을 검증했다.
- **아무도 지목하지 않은 구멍을 스스로 찾아 닫았다**(REQ-016 / AC-013). 그 구멍은 운영자의 결정 근거를 무너뜨리는 것이었고, 기존 AC 전부를 통과했을 것이다.
- **지시를 재측정한 뒤 이행하지 않기로 판단했다**(§0). 이미 닫힌 marker를 두 번 적용하지 않았다.

셋 다 "받은 것을 그대로 반영한다"에서 "받은 것을 검증한 뒤 반영한다"로의 이동이고, 1-3회차의 결함이 정확히 그 검증의 부재에서 나왔다.

남은 패턴은 **큰 편집 뒤 연결 산문이 뒤처지는 것**이다. 이것은 이번 회차에도 재발했다(R1-R3). 다만 계열이 다르고, 비용도 다르다.

---

## §6 4회차 동안 미뤄 온 템플릿 미러 대조 — 이번에 실행했다

리드가 지목했으므로 더 미루지 않았다.

```
$ diff .claude/rules/moai/workflow/kanban-dispatch.md \
       internal/template/templates/.claude/rules/moai/workflow/kanban-dispatch.md
diff_rc=0
34268  .claude/rules/moai/workflow/kanban-dispatch.md
34268  internal/template/templates/.claude/rules/moai/workflow/kanban-dispatch.md

$ diff <같은 파일 자기 자신>                    control_rc=0
$ diff kanban-dispatch.md spec-workflow.md      negative_control_rc=1
```

**로컬과 템플릿 미러는 바이트 동일하다.** 음성 대조가 1을 돌려주므로 `rc=0`은 진짜 동일성이지 `diff`가 침묵한 것이 아니다.

이것이 M6의 전제를 확인해 준다. 둘이 지금 동일하므로, 중립 문구를 양쪽에 같이 넣으면 동일성이 유지되고 `plan.md:165`가 적어 둔 "`diff` 한 줄로 미러 동일성 확인"이 실제로 성립한다. M6 계획은 건전하다.

3회차까지 이 대조를 미검사 목록에만 올려 둔 것은 내 태만이었다. 명령 한 줄이었다.

---

## §7 Implementation Kickoff Approval 준비가 됐는가

**됐다.**

Must-pass 7개가 전부 PASS 또는 N/A이고(MP-7 포함 — 미해결 마커 0), 종합 0.930이 Tier M 임계 0.80을 크게 넘는다. 요구 16개 전부가 반증 가능한 AC를 갖고, 뮤턴트 6종이 표·DoD 양쪽에 열거돼 있으며, 운영자 결정이 산문이 아니라 `AC-PSD-013`으로 고정돼 있다.

R1은 **run 단계 M4 착수 전에** 고치면 된다(한 구절). 승인을 미룰 사유가 아니다 — 설계 결정을 다시 열지 않고, 같은 문서 안에 옳은 출처가 둘 있으며, 고치는 데 편집 하나가 든다. R2/R3는 재량이다.

권고 순서: 승인 → run 착수 → M4 전에 R1 반영(원한다면 R2/R3 함께).

---

## §8 검사하지 않은 것 (Gaps)

- **구현 코드는 존재하지 않는다.** 이 판정은 설계 문서에 대한 것이다. R1-R3이 "문서가 어긋난다"는 판정이지 잘못 구현된 것을 관측한 것이 아니듯, 반대로 AC들이 실제로 RED를 내는지도 관측하지 않았다. 뮤턴트 6종의 RED 실측은 전부 run 단계 몫이며, 이 감사가 대신하지 못한다.
- **테스트·빌드·린트를 돌리지 않았다.** `go test` / `go vet` / `golangci-lint` 0회.
- **`git rev-parse HEAD`의 퇴화 경로는 재현하지 않았다.** `ls-remote` 두 경우만 실측했다. (d-3)의 커밋 반증 쪽 대조("잘 형성된 40자 SHA")가 충분한지는 명령 의미론에 근거한 판단이다.
- **`git push`가 `refs/remotes/`를 갱신한다는 3회차의 정밀도 교정도 여전히 실행으로 확인하지 않았다.** 이번 결론에 영향을 주지 않는다.
- **v0.4.0과 v0.5.0의 기계적 diff를 뜨지 않았다** — SPEC 디렉터리가 untracked라 커밋된 기준선이 없다. v0.5.0 델타에 대한 판단은 HISTORY 서술과 `plan.md:172-174` 본문을 읽은 것에 근거한다.
- **`t334` 워크트리를 이번에도 건드리지 않았다.**
- **`CLAUDE.local.md` §4.1의 현재 문구를 읽지 않았다** — M6이 그 절을 고친다고만 확인했고, 넣을 문구가 기존 서술과 충돌하는지는 보지 않았다.
- **cross-model 2차 의견 없음.** 네 회차 모두 Claude 단독 판정이다.

## §9 잔여 위험

- **REQ-PSD-013 일반형이 검증보다 넓다** — 3회차에서 밝힌 그대로다. 결과가 남지 않는 명령의 우회는 어떤 AC로도 잡히지 않는다. 이번에 뮤턴트로 열거되며 포착기((d-3))가 붙었지만, 그것이 덮는 것은 커밋·push라는 결과가 남는 부분집합이다.
- **요구 예산이 포화(16/16)라 다음 변경이 통합을 강제한다.** 지금은 결손이 없지만, run 단계에서 요구를 하나 더 필요로 하는 발견이 나오면 그 시점에 §3-7의 통합 후보를 먼저 검토해야 한다.
- **AC-013이 고정하는 것은 "기본 설정에서 관찰층이 돈다"이지 "킬 스위치가 거절 층만 게이트한다"의 전부가 아니다.** 플래그를 켠 상태에서 관찰층이 도는 것은 AC-009가 간접적으로 보이지만(거절 출력에 보존 경로가 담긴다), 켠 상태에서 원장 한 줄이 남는지는 어느 AC도 직접 단정하지 않는다. 실질 위험은 낮다 — 관찰층은 플래그 분기 밖에 있는 것이 설계이므로 한쪽에서 돌면 양쪽에서 돈다 — 다만 요구(REQ-016)가 말하는 "플래그 값과 무관하게"의 절반만 직접 반증된다.

---

**반복 4 종료. 판정 PASS · 0.930.**

궤적 0.725 → 0.875 → 0.895 → 0.930. D1-D8 / N1-N4 / N5-N8 전부 종결. 이번 회차 발견 R1(blocking, 문구 하나) + R2·R3(optional).

**Implementation Kickoff Approval로 진행해도 좋다.** R1은 run 단계 M4 전에 반영한다.

---

## §10 부록 — 판정서 제출 후 추가된 두 판정

리드가 4회차 브리프의 버전 안내를 정정하면서(v0.4.0 → 실제 v0.5.0) 두 가지를 추가로 판정해 달라고 했다. 본문(§0-§9)은 지우지 않고 그대로 두고 여기에 덧붙인다. **판정과 점수는 바뀌지 않는다 — PASS · 0.930.**

### 10.1 "내 실수로 아티팩트를 감점하지 말라" — 감점하지 않았다

확인해 두는 편이 좋겠다. §2의 감점 사유는 Clarity·Traceability 양쪽 모두 **R1-R3(carrier·자세 잔류 문구)**이고, 버전 불일치나 HISTORY 기록은 어느 차원의 감점 근거에도 들어 있지 않다. §0에서 그 건을 다룰 때도 "지시받은 대로 하지 않고 먼저 재측정한 뒤 하지 않기로 판단한 것이 옳다"고 적었다 — 감점이 아니라 지지였다.

덧붙이자면 감점할 성질도 아니다. 감사 대상은 아티팩트의 품질이고, 리드가 어느 시점에 트리를 읽었는지는 아티팩트의 성질이 아니다.

### 10.2 v0.5.0의 M3 문단 — **닫는다. 서술에 그치지 않는다**

`plan.md:172-174`를 두 성분으로 갈라 보면 판정이 분명해진다.

| 성분 | 내용 | 성질 |
|---|---|---|
| 구성 지시 | "브랜치를 **실제로 push해 둔다**" | 픽스처가 갖춰야 할 상태 |
| 확인 지시 | "픽스처 구성 **직후** `ls-remote` 출력이 비어 있지 않음을 한 번 확인하고 넘어간다" | 그 상태가 실제로 갖춰졌는지 재는 단계 |

두 번째가 있느냐로 갈린다. 구현자가 push를 잊었을 때 무슨 일이 벌어지는가 —

- **확인 없이**: 픽스처가 조용히 만들어지고, `ls-remote`가 `rc=0` + 빈 출력을 내고, (d-3)이 빈 값과 빈 값을 비교해 "동일"로 통과한다. **아무 신호도 없다.**
- **확인 있으면**: 픽스처 구성 단계에서 즉시 실패한다. 단정에 도달하기 전이고, 실패 지점이 곧 원인 지점이다.

조용한 공허를 **시끄러운 셋업 실패로 바꾼다.** 이것이 닫는다는 것의 정의다.

**AC-PSD-007(d-3)의 대조와 중복 아닌가** — 아니다. (d-3)도 "사전 출력이 비어 있지 않고 픽스처의 브랜치를 담는다"를 요구하므로 결국 잡히기는 한다. 그러나 두 지점은 잡는 시점과 잡히는 모양이 다르다. (d-3)의 대조는 **단정 시점**에 "이 AC의 전제가 성립하지 않는다"로 터지고, 그것은 구현자가 대조를 약화시켜 통과시킬 수 있는 자리다. M3의 확인은 **구성 시점**에 픽스처 자체의 실패로 터지고, 거기엔 약화시킬 단정이 없다. 방어가 두 겹이고 바깥쪽이 원인에 더 가깝다.

**근거의 정직성도 확인했다.** 문단은 `rc=0` + 빈 출력을 "(실측)"으로 표시하는데, 나도 §3-4에서 독립적으로 재현했다 — `rc=0`, stdout 0B, stderr 0B. 참이다. 그리고 "픽스처가 사전 출력을 비어 있지 않게 만드는 것이 **유일한 방어**다"라는 단정도 과하지 않다: 다른 형태(예: 특정 ref 존재 단정)를 쓰더라도 브랜치가 push돼 있어야 성립하므로 같은 필요조건에 귀착한다. 3회차에서 내가 교정했던 "다른 관측 지점이 없다"류의 과한 표현과 대비된다 — 이번 것은 정확하다.

**작은 권고(optional)**: M3의 "비어 있지 않음을 한 번 확인" 은 (d-3)의 대조("비어 있지 않고 **픽스처의 브랜치를 담는다**")보다 느슨하다. M3이 형태를 다시 쓰는 대신 (d-3)의 대조를 가리키게 하면 두 지점이 갈릴 여지가 없어진다. 모순이 아니라 요약이라 결함으로 올리지 않는다.

### 10.3 불일치 기록 — 정확한가, 남기는 것이 옳은가

**남기는 것이 옳다.** 근거는 이 SPEC이 다른 모든 곳에서 지키는 규율과 같다 — 설명되지 않은 부재를 남기지 않는다.

아티팩트에는 설명 없이는 결함으로 읽히는 사실이 하나 있다. **marker 1 편집이 v0.4.0에 있는데 지시는 v0.5.0을 지목했다**는 것이다. 기록이 없으면 다음 독자는 그것을 버전 관리 오류로 본다 — 실제로 나는 §0에서 정확히 그것을 확인하러 갔고, 수리 측은 그럴 것을 예상해 미리 적어 뒀다. 기록은 **겉보기 불일치를 설명된 불일치로** 바꾼다.

"아티팩트를 깨끗하게 두는 것"과 저울질할 사안도 아니다. SPEC HISTORY는 변경 이력이고 "이 버전이 왜 이걸 담고 있는가"가 바로 그 칸의 용도다. `§E.1`은 관문 상태가 사는 곳이므로 marker가 실제로 언제 닫혔는지도 그 자리가 맞다. 둘 다 외래 관심사가 아니다.

**정확한가 — 서술된 사실은 전부 정확하고, 한 군데가 불완전하다.**

정확한 부분부터. 리드가 방금 준 경위와 대조했다.

| 기록의 서술 | 대조 |
|---|---|
| 리드가 이 회차를 marker 1 적용 회차로 지시했다 | 일치 |
| 그 답은 v0.4.0에서 이미 적용돼 있었다 | 일치 (내가 디스크에서 확인) |
| 지시에 인용된 측정은 v0.4.0 편집 **이전** 시점 | 일치 (리드: 측정이 그 창에 떨어졌다) |
| 디스크 재측정으로 확인 — 마커 0건, `plan.md:40` 기본 `false` | 일치. 기록이 명령과 출력을 함께 싣는다 |
| 중복 적용하지 않았다 | 일치 |
| 이유: 이미 적용된 결정을 다시 쓰면 다섯 곳이 어긋날 위험만 생긴다 | 일치. 그 "다섯 곳"이 `plan.md:68-76` 표에 실재함을 확인했다 |

기록이 명령과 그 출력을 함께 싣는 점이 특히 옳다 — 주장이 아니라 증거다.

**불완전한 한 군데**: 리드의 경위에 따르면 이 연쇄를 시작한 것은 **수리 측 자신의 완료 보고**였다("emitted a completion report saying marker 1 was still open"). 그 보고가 먼저 낡은 값을 실었고, 리드는 그것을 받아 트리를 쟀다. 그런데 기록은 낡은 측정을 **지시 쪽에만** 귀속시키고("지시에 인용된 측정은 … 이전 시점의 것") 자기 보고가 같은 값을 실었다는 사실은 적지 않는다.

**이 대목은 내가 트리에서 확인할 수 없다** — 그 완료 보고는 아티팩트가 아니고 이 워크트리에 없다. 리드의 진술에 근거한 판단이며, 그렇게 표시해 둔다.

사실이라면 고쳐 둘 값이 있다. 지금 기록은 "리드의 측정이 낡았다"로 읽히는데, 실제 교훈은 그보다 넓고 더 유용하다 — **작성자가 살아 있는 트리에서는 순간 판독이 누구에게도 정착 상태가 아니다.** 수리 측 자신도 같은 창에서 같은 값을 실어 보냈다면, 그 사실이 들어갈 때 기록은 한쪽의 실수담이 아니라 이 저장소의 병렬 작업 규율(`AGENTS.md` §2의 staleness 규칙과 같은 것)에 대한 사례가 된다.

**권고(optional, minor)**: `progress.md:36`에 한 절을 더한다 — 낡은 판독이 지시와 직전 완료 보고 양쪽에 있었다는 것. 어조는 지금 그대로 사실 서술로 둔다. 지금 기록이 책임 소재를 따지지 않는 것은 옳고, **그 방향으로 확장해서는 안 된다.** 리드가 내게 보낸 경위에는 판단("My conclusion was wrong")이 담겨 있지만 아티팩트에는 담기지 않았고, 그 구분이 정확하다.

### 10.4 결론 — 변동 없음

두 추가 판정 어느 것도 결함을 새로 만들지 않는다. 10.2는 **닫힘** 판정이고, 10.3은 **정확·적절** 판정에 optional 보완 하나다.

- 판정: **PASS · 0.930** (변동 없음)
- blocking: **R1 하나** (`plan.md:180`의 `` `drift`가 여전히 `true` `` → `` `status`가 여전히 `"drift"` ``)
- optional: R2 · R3 · 10.2의 M3 문구 정밀화 · 10.3의 완료 보고 언급 추가

**Implementation Kickoff Approval — 예. 진행해도 좋다.** 조건은 §7과 같고 부록으로 바뀌지 않았다.

### 10.5 이 부록에서 검사하지 않은 것

- **수리 측의 완료 보고를 읽지 않았다.** 워크트리에 없다. 10.3의 불완전성 판정은 리드의 진술에 근거하며, 내가 재지 못한 유일한 대목이다.
- **v0.4.0과 v0.5.0의 기계적 diff는 여전히 뜨지 못했다.** SPEC 디렉터리가 untracked라 커밋된 기준선이 없다. v0.5.0 델타가 M3 문단 하나뿐이라는 판단은 HISTORY·`§E.1` 서술과 `plan.md:172-174` 본문을 읽은 것에 근거한다 — 다른 곳이 함께 바뀌지 않았음을 **기계적으로는** 확인하지 못했다.
- **M3 확인 단계가 실제로 셋업 실패를 일으키는지 실행해 보지 않았다.** 구현이 없다. 10.2는 설계 판정이다.
