# Pre-fix PTY capture — deploy phase (final screen, 120x30)
# Same run as evidence-prefix-preview.txt after confirming with 'y'.
# Capture: tmux capture-pane -p (verbatim)
# Observed defect: the completed step-4 bar stays on screen leading with the
# RUNNING glyph (● 4/5 steps) above the final ✓ 5/5 — completed snapshots
# rendered as never-advancing in-flight processes; the 62-file deploy itself
# completed in <1.2s so the ~30 summary lines appeared in one burst.

  ● ████████░░ 4/5 steps
  ✓ User settings restored
  ✓ 2 user settings key(s) preserved (run with --verbose to list)
  ✓ .gitignore user patterns preserved
  ✓ .claude/settings.json user customizations preserved
  ✓ .moai/status_line.sh user content preserved
  ✓ .mcp.json user customizations preserved
  ✓ Merged 3 file(s) with 3-way merge engine
  ✓ ██████████ 5/5 steps

 ✓ Updated 584 files
62 merged/added + 522 managed re-deployed · removed 385 under managed paths (all re-deployed)
Backup: .moai-backups/20260913_174541
Recover: moai update --restore .moai-backups/20260913_174541
Hooks: running Claude Code sessions need a /hooks review (or a Claude Code restart) for new or changed hooks to take effect.

To reconfigure project settings (development mode, git, model policy), run:
   moai update -c
  Pre-push hook installed (.git/hooks/pre-push)
  Pre-commit hook installed (.git/hooks/pre-commit)
  Warning: user-modified pre-commit hook was replaced; previous hook backed up at .git/hooks/pre-commit.bak.20260913T084542Z
Tip: this checkout is shared across concurrent sessions; for branch-changing work (switch/reset/rebase), use a worktree
for isolation — `moai cc -w` / `moai glm -w`, or `claude --worktree`. See .claude/rules/moai/workflow/main-checkout-br
anch-guard.md.

Post-sync steps ---
RUN_EXIT=0
