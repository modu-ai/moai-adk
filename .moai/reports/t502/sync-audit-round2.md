# t502 sync-audit 2차 판정서 — SPEC-CODEX-SKILL-DISABLE-001

- **판정**: **FAIL** · 가중합 **90.8** (1차 89.0)
- **감사 앵커**: 커밋 `f269edbc2` — 시작·종료 모두 `git status --short` 무출력, HEAD 불변
- **감사 주체**: sync-auditor (2차, 1차와 별개 인스턴스)
- **기록 주체**: 리드 세션 (1차와 같은 이유 — 판정 본문이 세션에만 있으면 나중 독자가 찾을 곳이 없다)
- **1차 판정서**: `.moai/reports/t502/sync-audit.md`

> **현재 상태.** 이 판정의 유일한 blocking(F0)은 `35d6c7404` 에서 수리됐다. 그 커밋 이후 코드·테스트 변경은 없고 문서 3줄만 바뀌었다.

## 4차원 점수

| 차원 | 가중 | 점수 | 판정 | 1차 대비 |
|---|---|---|---|---|
| Functionality | 40% | 94 | PASS | 92 → 94 |
| Security | 25% | 90 | PASS | 90 (불변 — 소스 diff 가 주석 전용, 실행문 0줄) |
| Craft | 20% | 86 | PASS | **78 → 86** |
| Consistency | 15% | 90 | PASS | 94 → 90 (advisory 개수 오산) |

must-pass 방화벽(Functionality·Security) 통과. FAIL 은 점수가 아니라 F0 에서 나온다.

**감사 방법 기록**: 뮤턴트 주입을 **감사 트리에서 하지 않고** `rsync` 스크래치 사본에서 수행했다(대상 3파일 sha256 동일 확인 후 — `codex_skills_disable.go` `8819ef6d…`, `_test.go` `c50a724d…`, `codexwiring/skills.go` `2be7579f…`). `internal/` 무수정.

## F0 [BLOCKING] 잔여 advisory 는 9 가 아니라 10 — **수리됨 (`35d6c7404`)**

1차 판정서가 스스로 `advisory 11건 (F2-F12)` 라 적고 표 행도 11개다. F2 가 `13ae49a05` 에서 닫혔으므로 열린 것은 `F3-F12` = **10건**. 그런데 닫기 산출물 3곳이 9라고 적었다(`progress.md` :159 `NINE`, :197 `아홉 건(F3-F12 중 F2 제외)`, :207 `아홉 건`).

**197행 괄호가 오류의 흔적이다** — 「F3-F12 **중** F2 제외」는 성립하지 않는다(F2 는 그 구간 밖). F2 를 두 번 뺐다: 구간 시작을 F3 으로 올려서 한 번, 괄호에서 또 한 번.

**출처는 리드의 인계 지시문이다.** 리드가 "nine remain open and unowned (F3-F12 minus F2)" 라 썼고 docs 에이전트가 그대로 물려받았다. 산출물의 결함이지만 저작은 리드에 있다.

blocking 으로 올린 근거: 이 절은 **미결 부채 열거가 존재 이유인 Gaps 절**이고, `verification-claim-integrity.md` §3 은 Gaps 절의 단언 자체가 참일 것을 요구한다. 1차가 런타임 영향 없는 거짓 주석으로 FAIL 을 냈으므로, 증거가 반박하는 수치에 같은 기준을 적용하는 것이 일관된다. 내용 손실은 0 — 열 건 전부가 커밋된 1차 판정서에 개별 열거돼 있다.

## 지목별 판정

### 1. F1 수리는 실제로 F1 을 닫는가 — **닫는다. 반사실까지 확인**

기준선 `EXIT=0 PASS=17 FAIL=0`. 세 발행 지점 각각에 뮤턴트:

| 뮤턴트 | 지운 것 | 결과 | 붉어진 테스트 |
|---|---|---|---|
| MUTANT-6 | `:324` 삽입 분기 | `EXIT=1 PASS=16 FAIL=1` | `…InsertsMissingEnabledKey` |
| MUTANT-R | `:305` 재작성 분기 | `EXIT=1 PASS=16 FAIL=1` | `…UpdatesExistingEntry` |
| MUTANT-A | `:358` append 조립 | `EXIT=1 PASS=15 FAIL=2` | `…AppendsOneEntryWithFalse`, `…PreservesSurroundings` |
| MUTANT-7 | `reshapeLike` CR 계승(`:333`) | `EXIT=1 PASS=16 FAIL=1` | `…InsertsMissingEnabledKey` |

**반사실 대조가 핵심이다.** `43e820663` 시점 테스트 파일을 되돌려 놓고 같은 MUTANT-6 을 넣으면 `EXIT=0 PASS=16 FAIL=0` — 수리 전에는 삽입 지점 발행을 지워도 초록이었다는 1차 주장이 실측 재현된다. 구현 커밋이 「관측 주체는 구현 에이전트」라 적어 둔 대조를 제3자가 재현했으므로 그 기록의 미검증 지위가 해소된다.

### 2. 정정된 주석은 참인가 — **핵심 두 곳 참, 한 곳은 논거 비약**

`:13-18`(헤더)과 `:202-229`(`upsertCodexSkillDisable` docstring)은 정확하다 — 세 지점을 열거하고, 각각 왜 안전한지 다르게 밝히고, 옛 주장이 거짓이었음을 기록으로 남긴다. `!ok` 경로도 `:277` 이 `skip` 으로 빠져 누락 경로가 실제로 없다.

### 3. F2 폐쇄는 실질인가 — **실질이다**

MUTANT-7 아래 새 테스트만 붉고 두 단언 모두 발화. 커버리지 독립 확인 `332.36,335.3 … 1`(1차의 `296.36` count=0 이 이동·폐쇄). **약화 없음** — CRLF 로 바꾼 뒤에도 MUTANT-6 이 잡히고, `reshapeLike` 비-CR 경로는 재작성 분기 테스트가 계속 덮는다.

### 4. 재닫기가 옳은 트리를 기술하는가 — **기술한다**

자기 판별식 성립: `git diff --name-only 13ae49a05..HEAD | grep '\.go$'` **무출력**, 대조군 `43e820663..HEAD` 는 같은 grep 이 2파일 출력 — 무출력이 진짜 0이지 침묵이 아니다. §G 수치 전부 재측정 일치(AC 17 / REQ 16 / CHANGELOG 1 · 부재토큰 대조 0 / DoD 8·0 / 인용 줄 203·222·230·233·544 실재). `spec.md` byte-unchanged, status `completed`.

### 5. 나빠진 advisory — **없다**

F3 `403.42`=0, F4 `245.48`=0, F10 `e2e-verb.sh:45` 의 `$?`→sed, F6·F9 코드 무변경. 1차의 0-커버리지 5곳 중 **2곳 폐쇄, 나머지 동일** — 엄격히 개선.

## 신규 advisory 3건

| id | 내용 |
|---|---|
| **F13** | `codex_skills_disable.go:339-341` `appendDisableEntry` docstring 이 수리에서 안 건드려졌고 「세 줄이 함께, 다른 곳에서는 발행되지 않으므로 **어떤 경로도 `enabled` 없는 엔트리를 만들 수 없다**」고 말한다. 결론은 참이나 **논거가 결론을 지지하지 않는다** — 이 함수의 응집성은 다른 두 지점에 대해 아무 말도 못 한다. 정정된 docstring 이 「단일 깔때기가 아니다」라 하는데 이 주석만 단일 깔때기 논법을 쓴다. 1차 F1 이 문제 삼은 「국소 사실에서 전역 안전 성질을 끌어내는 주석」의 잔재. **거짓 주장은 아니므로 advisory** |
| **F14** | 헤더는 「세 지점 각각 자기 테스트」라 하고 docstring 이 이름 붙인다고 가리키는데, 실제로 테스트 이름을 대는 것은 삽입·append **둘뿐**(재작성 분기 항목엔 없음). 실질은 참(MUTANT-R 이 확인) |
| **F15** | §E.4 가 인용하는 `.moai/state/verify/t502-resync/` 는 `.gitignore:315`(`.moai/state/`)로 무시돼 **추적되지 않는다** — 리드 독립 확인: `git ls-files .moai/state/verify/` → 0. 워크트리를 폐기하면 인용 경로가 해소되지 않는다. 판정서 본문은 커밋돼 있어 핵심 기록은 안전. **계통적 위험** — 이 카드 고유 결함이 아니다 |

## 이미 내려진 두 결정에 대한 견해 — **둘 다 동의**

- **`pathLine < 0` bail 커버리지 0 유지**: 코드를 직접 읽어 도달 불가를 확인했다(파서가 `path` 줄에서만 `Path` 를 세우고 스캔이 같은 접두로 매치). 커버리지 `319.18,321.3 … 0` 로 일치. 이유가 코드 위에 적혀 있어 다음 판독이 다시 깃발을 꽂지 않는다.
- **사후조건 가드는 후보이지 간극**: 세 지점이 이제 각각 뮤턴트로 고정됐으므로 기계적 커버의 한계이익이 줄었다.

## 감사관이 보지 않은 것

- **E2E 를 `f269edbc2` 에서 재실행하지 않았다.** 1차가 `43e820663` 에서, 재닫기가 `13ae49a05` 에서 각각 재현했고 그 사이 소스 diff 는 주석 전용이지만, **이 HEAD 에서의 E2E 관측은 없다.**
- `golangci-lint` 미실행 — 산출물도 carried-over 로 적고 있어 **이 HEAD 의 lint 판정은 어느 쪽도 갖고 있지 않다.**
- 전체 스위트(CI 몫) · 크로스플랫폼 빌드 · Windows·Linux 런타임 · 0.153.4 외 codex 버전 · `gate-path-shape.md` 19셀 재측정 · `probe.sh selftest` 재실행 · docs-site 4로케일 · README.
- **커버리지는 disable 셀렉터 17개 범위로만 쟀다** — 위 0-셀 목록은 「이 17개가 덮지 않는다」는 뜻이지 「패키지 전체가 덮지 않는다」는 뜻이 아니다.

## 처분

| 항목 | 처분 | 커밋 |
|---|---|---|
| **F0** (blocking) | **수리됨** — `progress.md` 3곳의 아홉 → 열, 197행 괄호에서 「중 F2 제외」 삭제 | `35d6c7404` |
| **F13·F14·F15** | advisory 유지, 소관 미배정 | — |
| **F3-F12** | advisory 유지(10건), 소관 미배정 | — |

---

분류: 판정 기록 (sync-phase 2차). 앵커 `f269edbc2`; F0 수리 후 트리는 `35d6c7404`.
