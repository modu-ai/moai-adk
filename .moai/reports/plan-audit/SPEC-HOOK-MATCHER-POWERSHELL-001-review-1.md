# SPEC Review Report: SPEC-HOOK-MATCHER-POWERSHELL-001
Iteration: 1/2 (Tier M ceiling per harness.plan_audit_tier_ceilings)
Verdict: FAIL
Overall Score: 0.77 (Tier M PASS threshold 0.80)

Card: t1224. Tree audited: worktree `.claude/worktrees/t1224`, branch `WT-hook-matcher-powershell`, HEAD `e609f57b0`, SPEC base `efc807b81`. `git diff --stat efc807b81 HEAD` shows only the four SPEC files, so code read from the working tree equals the base. Collision measurements taken against local `develop` = `7d886936b` at audit time.

Reasoning context ignored per M1 Context Isolation. The invocation carried audit focus items only; those were used to prioritise checks, not as evidence.

Cross-model second opinion: not run. No `audit_model` key exists under `.moai/config/sections/` (grep 0 hits), so this is a Claude-only audit. `mcp__moai__spec_audit` (project_root = this worktree) returned `drift_findings: []`, `modern_era_clean: 1`. `moai spec lint SPEC-HOOK-MATCHER-POWERSHELL-001 --json` returned `[]`.

## Must-Pass Results

- [PASS] MP-1 REQ number consistency: REQ-HMP-001..016 appear once each, in order, zero-padded, at spec.md:L75-L105. 16 requirements = Tier M ceiling (16), not over.
- [PASS] MP-2 GEARS compliance (requirement layer only): every REQ-HMP-xxx in spec.md §B uses a GEARS form. Ubiquitous: L75, L76, L81, L98, L99, L100 ("The ... shall"). Unwanted: L77, L82 ("shall not"). Event-driven: L86, L89, L90, L94, L105 ("When ..., ... shall"). Compound Where+When: L87, L88. L104 is ubiquitous with an embedded When clause. ACs in acceptance.md are Given-When-Then and were graded under Group 4, not here.
- [PASS] MP-3 YAML frontmatter validity: spec.md:L2-L13 carry all 12 canonical fields with correct types: `version: "0.1.0"` quoted, `created`/`updated` ISO dates, `priority: P1`, `lifecycle: spec-anchored`, and `tags` as a comma string. No rejected aliases. `phase: "v3.2.0 target"` is a release label, not a lifecycle token. The extra fields `era`, `tier`, and `related_specs` are permitted.
- [N/A] MP-4 language neutrality: the SPEC concerns the PowerShell/Bash host tools, not the 16 programming-language toolchains. The template edit it prescribes (a matcher string) carries no language-specific tool name.
- [PASS] MP-5 D7 cross-SPEC reconciliation: the SPECs referenced are SPEC-POWERSHELL-DENY-PARITY-001 (`status: completed`), SPEC-HOOK-STDIN-FAILCLOSED-001 (`status: completed` on develop; absent from this tree), and SPEC-DUAL-HARNESS-HOOK-PARITY-001 (`status: in-progress` on `WT-dual-harness-parity-rebuild`; absent from this tree). None is retired, superseded, or archived, so there is no BLOCKING finding. The two absent-from-tree SPECs are a SHOULD (D14).
- [PASS] MP-6 D8 cross-platform: `grep -c syscall` = 0 in spec.md, plan.md, and acceptance.md. The auto-PASS applies.
- [PASS] MP-7 clarification gate: `grep -rn 'NEEDS CLARIFICATION' plan.md` exited 1 (no match). research.md is absent (Tier M, not required).

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.72 | 0.75 band, lower edge | Several requirements need interpretation: REQ-HMP-012 (spec.md:L98) does not define "hook command" when every registration is `"command": "bash"` plus args (D4). REQ-HMP-009 (L89) says "not parseable JSON" but does not say whether that means the envelope or tool_input (D2). REQ-HMP-010 (L90) makes `-EncodedCommand` "name git" (D6). D2 option (A) cites an audit log that does not exist for one guard (D5). |
| Completeness | 0.75 | 0.75 | All sections are present (HISTORY L19, §A background/WHY, §B REQs, acceptance.md, and §D with five `### Out of Scope — …` H3s at L120-L137). But the site inventory the SPEC calls exhaustive (L23 "Per-site classification of every Bash tool-name branch") omits the pre-tool shell wrapper branch (D1). AC-HMP-002, 007, and 011 have no scenario (D10). |
| Testability | 0.70 | between 0.50 and 0.75 | AC-HMP-012 (acceptance.md:L77-L82) has no FAIL outcome (D3). AC-HMP-011 (L19) cannot go red under the §C command form (D8). AC-HMP-003 admits a mutant under the `command`-keyed reading (D4). AC-HMP-002 names an undefined `BASE` (D9). The remaining ACs are binary. |
| Traceability | 0.95 | 1.0 band, minus the scenario gap | The AC matrix (acceptance.md:L7-L20) maps all 16 REQs and cites no nonexistent REQ. Three matrix rows carry no Given-When-Then body. |

Aggregate (harmonic mean) ≈ 0.77, below the Tier M 0.80 threshold.

## Verification performed (Claim / Evidence)

1. **Site inventory (focus 1).** `grep -rn '"Bash"' --include='*.go' internal cmd pkg | grep -v _test.go` returns exactly the 10 Go comparisons the SPEC lists: pre_tool.go:451/534/552/569, post_tool.go:238/248, evidence_writer.go:184/469/650, and cli/hook.go:788. It also returns permission/stack.go:460, which is (c). All other hits are comments. No constant-indirected or case-folded Bash comparison exists. The (a)/(b)/(c) labels were checked against the code:
   - Site 1: extraction and deny list, pre_tool.go:833-840 and :995-1010. The `Remove-Item`/`Clear-Content`/`terraform destroy` patterns are at :213-222.
   - Sites 2-4: branch_guard.go:355, integration_lock_guard.go:70, and slot_lease_guard.go:63 all run `substituteQuotedArguments`.
   - Sites 5-10: logic read at post_tool.go:232-250, evidence_writer.go:183-195/468-475/650, and hook.go:775-790.
   - Site 11: permission/stack.go. Its only non-test consumer is internal/cli/doctor_permission.go, so (c) is correct.
   - The classification is accurate for the Go layer. **Missed: the shell wrapper (D1).**
2. **D1 matcher test (focus 3).** agent_model_matcher_test.go:130-135 asserts an exact-equality `b.Matcher == "Write|Edit|Bash"` with the error "must not be renamed or widened". :145-150 is a RED fixture built on the same literal. The SPEC's A.2 claim is accurate, so option (B) requires amending both the assertion and the fixture.
3. **Collision claims (focus 4).**
   - `git diff --name-only develop...WT-dual-harness-parity-rebuild` (git warned of multiple merge bases and used 4dcd4d8d) lists `internal/cli/hook.go` and `internal/codexadapter/**`. It lists no `internal/hook/*`, no `settings.json.tmpl`, and no `.claude/hooks/*`. Its hook.go hunks fall in `init`, `runHookEvent`, and `runHarnessObserveStop` only. The claim holds.
   - `git diff --name-only develop...WT-hook-stdin-failclosed` returned empty, and `git merge-base --is-ancestor WT-hook-stdin-failclosed develop` exited 0. **t1152 has already landed in develop.** Its SPEC reads `status: completed`, and develop's hook.go:280-293 carries the stdin fail-closed path. The SPEC's §C treats t1152 as a concurrent lane and misses the behavioural collision (D2).
   - `git diff --name-only efc807b81 develop` over the scope shows `internal/cli/hook.go` and `internal/codexadapter/*` changed on develop. Site 10 is now at develop hook.go:822. develop's translate.go maps decisions, not tool names (grep bash/shell/ToolName = 0). The M0 re-measure gate covers these moves.
4. **Premise (focus 2).** The hook payload for PowerShell is indeed unmeasured. However, local evidence already narrows it, and the SPEC does not cite it (D7):
   - `jq` over `.moai/reports/t1211/m1/A.jsonl` and `B.jsonl` gives PowerShell `tool_use.input` = `{"command":"git clean -fdx","description":"…"}`, which is the same shape as Bash.
   - `tool_use_result` keys are `["interrupted","isImage","stderr","stdout"]`.
   - The fail-safe design (REQ-HMP-009 plus the existing fall-through) is sound for missing or non-string `command`: `extractBashCommand` and `checkBashCommand` return "" on a type mismatch. The envelope-invalid case is not sound (D2).

## Defects Found (structured defect-list)

D1. SITE-MISSED — internal/template/templates/.claude/hooks/moai/handle-pre-tool.sh.tmpl:L43-L57 (local `.claude/hooks/moai/handle-pre-tool.sh:L39`), against spec.md:L23/L49/L55-L69 — The pre-tool wrapper itself branches on the tool name: `grep -q '"tool_name"[[:space:]]*:[[:space:]]*"Bash"'` gates the Bash Risk-Amplifier subcommand-count warning. Its comment says "matcher scope is Write|Edit|Bash". Once D1 routes PowerShell into this wrapper, the branch silently skips PowerShell. It is also class (b): the metacharacter counter counts `` ` `` (the PowerShell escape char), `$(` (a PowerShell subexpression), and `|`/`;`/`&&`, so PowerShell text would be miscounted. The §A.3 table claims to classify "every Bash tool-name branch" yet omits this one. REQ-HMP-005 and REQ-HMP-013 (L82, L99) scan Go only, so the source guard cannot see it. — Severity: major — Class: blocking — Required fix: add a site 14 row to §A.3 for the template wrapper and its local copy, with a class and a decision: keep Bash-only with a documented reason, or accept PowerShell while noting the backtick and `$(` miscount. Either extend REQ-HMP-013 to the wrapper's tool-name literal or record an explicit exclusion with its reason. Update the wrapper comment in the same delivery if the matcher changes.

D2. REQ-CONTRADICTION / STALE-COLLISION — spec.md:L89 (REQ-HMP-009), acceptance.md:L57-L61 (AC-HMP-008), spec.md:L112-L113 — REQ-HMP-009 demands "shall not deny" and "the same output as for a Bash input with the same defect" for PowerShell input that "is not parseable JSON". After M0 absorbs develop, SPEC-HOOK-STDIN-FAILCLOSED-001 (card t1152) is in the tree: branch is an ancestor of develop (exit 0), and develop hook.go:280-293 makes an unparseable PreToolUse stdin a fail-closed deny. The Bash output for that defect is therefore a deny, and the two clauses contradict each other. Conversely, an unparseable `tool_input` inside a parseable envelope is not reachable: `tool_input` is a `json.RawMessage` that must be valid JSON for the envelope to decode, so only a wrong-typed value such as a string or array can occur. §C still describes t1152 as a concurrent branch at `ed1759b65`. The branch head is now `3511e13c1` and already merged. — Severity: major — Class: blocking — Required fix: split REQ-HMP-009 into (i) envelope-unparseable stdin, where the output equals the Bash output under SPEC-HOOK-STDIN-FAILCLOSED-001 (fail-closed deny) and "shall not deny" is dropped, and (ii) parseable envelope with `tool_input` lacking a string `command` or of non-object type, which gets no deny, no panic, and Bash-equal output. Replace "invalid JSON" in AC-HMP-008 with the two concrete cases. Rewrite the t1152 bullet in §C as "landed in develop (completed); behavioural interaction with REQ-HMP-009", and add t1152's REQ-HSF-001 to the D7 reconciliation.

D3. LIVE-NO-FAIL-BRANCH / PROVENANCE — acceptance.md:L77-L82 (AC-HMP-012), plan.md:L62-L68 (M5 arm C, validity list) — AC-HMP-012 defines only PASS (all observables as expected) and INCONCLUSIVE (a validity condition fails). It gives no outcome for the case where every validity condition holds and an observable contradicts the expectation, for example arm A's hook.log is non-empty or arm C shows no denial. A measurement that cannot record FAIL cannot falsify the claim (verification-completeness.md §1.1). Arm C also invokes the "real wrapper → binary built from this branch". The real wrapper resolves `command -v moai` first (handle-pre-tool.sh.tmpl:L66), which is the installed binary, not the branch build. No validity condition establishes which binary the hook ran, so a no-denial result is unattributable. — Severity: major — Class: blocking — Required fix: add a FAIL outcome to AC-HMP-012: all validity conditions hold and any expected observable is absent or contradicted, recorded with the arm and the observable. Make arm C's hook command invoke the branch binary by absolute path, or prepend its directory to PATH. Add a validity condition that the hook's binary commit equals the branch HEAD, for example by recording `<binary> version` into the arm directory from the hook command.

D4. AMBIGUOUS-IDENTITY / MUTANT — spec.md:L98 (REQ-HMP-012), plan.md:L27, acceptance.md:L30-L36 — Every template hook entry has `"command": "bash"` with the script path in `args` (settings.json.tmpl, e.g. L133-L135). The "(event, hook command) pair" is undefined for this shape. Keyed on the `command` field, every PreToolUse registration is the same "bash". A mutant that registers `PowerShell` for a different script, such as the Agent observer, satisfies the parity test while `handle-pre-tool.sh` still lacks PowerShell. — Severity: major — Class: blocking — Required fix: define the hook identity in REQ-HMP-012 as the hook script path (the final `args` element, with `${CLAUDE_PROJECT_DIR}` normalised), or `command` when `args` is absent. Add the "PowerShell registered on a different script" mutant to AC-HMP-003 alongside the remove-registration mutant.

D5. FALSE-PREMISE IN DECISION OPTION — plan.md:L10 (D2 option A), acceptance.md:L65-L68 — D2 option (A) says "append one audit line … to the guard's existing audit log" for both the branch guard and the integration lock. Only the branch guard has one (branch_guard.go:L47, `.moai/logs/branch-guard-audit.log`). integration_lock_guard.go has no audit log (grep audit/.jsonl = 0), and no integration-lock audit exists elsewhere in internal/. The slot-lease guard, which is not in D2 scope, has `.moai/logs/slot-lease-audit.jsonl`. AC-HMP-009 tests only the branch guard. The operator would therefore be choosing option (A) on a premise that is half false (verification-claim-integrity.md §1.1 surface 4). — Severity: major — Class: blocking — Required fix: state per guard where the (A) audit line goes. Either name a new file for the integration lock or reuse an existing one with the path spelled out. Add an integration-lock indirection case to AC-HMP-009.

D6. UNDECIDABLE-TRIGGER — spec.md:L90 (REQ-HMP-010), plan.md:L45 — REQ-HMP-010 triggers "when a PowerShell command names `git` through … `-EncodedCommand`". An encoded command carries base64 UTF-16, so the text does not name `git` without decoding, and the SPEC does not say whether to decode. — Severity: minor — Class: blocking — Required fix: state the rule. Either treat any `-EncodedCommand`/`-enc`/`-e` argument as unclassifiable regardless of its payload, or require decoding. Pin the chosen behaviour in the M4 test.

D7. ORDERING vs RISK MITIGATION; UNCITED EVIDENCE — plan.md:L79 (Risks row 1) vs plan.md:L36/L47 (M3 before M5), spec.md:L31 — The risk mitigation says "if the key differs, stop and report before M3 claims", but the payload capture (M5 arm B) is scheduled after M3. Arms A and B need only a logger, not the branch binary, so they can run in M0. §A.1 also marks the input shape wholly UNVERIFIED without citing the local t1211 evidence: tool_use input `{command, description}` and tool_use_result keys `{interrupted, isImage, stderr, stdout}` in `.moai/reports/t1211/m1/A.jsonl`/`B.jsonl`. That evidence is transcript-level, not hook-payload-level, but it narrows the risk. — Severity: minor — Class: blocking (ordering); optional (citation) — Required fix: move arms A and B into M0, or add an M0 step that captures one PowerShell hook payload before M3. Cite the t1211 transcript evidence in §A.1 and label it transcript-level.

D8. VACUOUS AC — acceptance.md:L19 (AC-HMP-011), spec.md:L100/L116 — The §C command form already exports `MOAI_HOME="<scratch dir>"` at process level. A test that forgets its own `t.Setenv("MOAI_HOME", …)` therefore still cannot touch the real home, and the "real MoAI home lease DB mtime unchanged" check cannot go red. It is also noisy: concurrent lanes write the real lease records, and the AC does not name the path. `t.Setenv` panics under `t.Parallel`, which the SPEC does not mention. — Severity: minor — Class: blocking — Required fix: replace the mtime check with a static assertion that every added or modified test function in the touched `_test.go` files calls `t.Setenv("MOAI_HOME", …)` or sets `MoaiHome` through an injected path. Add a positive control: one fixture test without the Setenv must fail the static check. State that these tests are non-parallel.

D9. UNDEFINED RANGE — acceptance.md:L10 (AC-HMP-002) — "`git diff BASE..HEAD`": `BASE` is undefined once M0 absorbs develop. A literal `efc807b81..HEAD` would sweep in other cards' settings or permission edits. — Severity: minor — Class: blocking — Required fix: define `BASE` as `$(git merge-base develop HEAD)`, evaluated before merge, per gitflow-lane-protocol.md §8. Include the non-empty control (`git diff --name-only $BASE..HEAD | wc -l` ≥ 1).

D10. MISSING SCENARIOS — acceptance.md:L10, L15, L19 — AC-HMP-002, AC-HMP-007, and AC-HMP-011 exist only as matrix rows with no Given-When-Then body (Group 4 AC-1). — Severity: minor — Class: optional — Required fix: add scenarios. AC-HMP-007 in particular should state the live-holder fixture and the expected reason prefix for both the integration lock and the slot lease.

D11. SOURCE-GUARD EVASION — spec.md:L82/L99, plan.md:L34 — The planned guard patterns (`== "Bash"`, `!= "Bash"`, `case "Bash"`) miss `const x = "Bash"` followed by `== x`, and `strings.EqualFold(name, "bash")`. REQ-HMP-005 forbids comparing against "the literal", which a constant sidesteps. — Severity: minor — Class: optional — Required fix: have the guard flag any `"Bash"` string literal (case-insensitive) in the scanned non-test files outside the predicate file, excluding comments.

D12. SCOPE GAP (SITE 4) — spec.md:L60, L90 — The slot-lease guard also runs `substituteQuotedArguments` (slot_lease_guard.go:L63), so the quote-collapse and indirection residual risk applies to it. REQ-HMP-010 and D2 cover only the branch guard and the integration lock, and the SPEC does not state site 4's behaviour on indirection. — Severity: minor — Class: optional — Required fix: add one sentence stating that site 4 keeps its existing fail-open, match-on-scrubbed-text behaviour under indirection, so no D2 policy applies.

D13. LIVE ISOLATION DETAILS — plan.md:L49-L68 — Arm C's real wrapper writes to the real `$HOME/.moai/logs/hook-stderr.log`, which is the wrapper's default. Arm B expects "one payload", but under `--max-turns 3` the model may issue more than one PowerShell call, and the SPEC does not say whether that counts as PASS or INCONCLUSIVE. `MOAI_BRANCH_GUARD_EXEMPT` is not required to be unset for arm C. — Severity: minor — Class: optional — Required fix: set `MOAI_HOOK_STDERR_LOG=<arm>/hook-stderr.log` for arm C. Change arm B's expectation to "≥1 payload with tool_name PowerShell". Add "MOAI_BRANCH_GUARD_EXEMPT unset" to the validity list.

D14. D7 SHOULD — spec.md:L16/L111-L112 — SPEC-DUAL-HARNESS-HOOK-PARITY-001 and SPEC-HOOK-STDIN-FAILCLOSED-001 do not exist in this tree at the base. They exist on `WT-dual-harness-parity-rebuild` (in-progress) and on develop (completed) respectively. — Severity: minor — Class: optional — Required fix: none beyond D2. The references resolve after M0 absorption. Note the locations in §C.

## Regression Check (Iteration 2+ only)

N/A — iteration 1. The previous attempt aborted on a rate limit and left no report.

## Recommendation

FAIL. Every must-pass criterion passes; the verdict comes from the aggregate score (0.77, below the Tier M threshold of 0.80) and five blocking major defects. Fix order for manager-spec, highest change-likelihood first:

1. D2: rewrite REQ-HMP-009 and AC-HMP-008 against the landed fail-closed dispatcher, and refresh the t1152 bullet in §C.
2. D1: add the wrapper site (template `.sh.tmpl` L44 and local `.sh` L39) to §A.3 with a class and a decision, and extend REQ-HMP-013 or declare the exclusion.
3. D4: define hook identity as the script path in REQ-HMP-012, and add the wrong-script mutant to AC-HMP-003.
4. D3: add a FAIL outcome to AC-HMP-012, and add binary-provenance handling plus a validity condition for arm C.
5. D5 and D6: correct D2 option (A)'s audit-log target per guard, and decide the `-EncodedCommand` rule.
6. D7, D8, D9: move payload capture ahead of M3, replace AC-HMP-011's mtime check with a static check plus a positive control, and define `BASE` as a merge-base.
7. D10 to D14: optional.

Kickoff decisions:

- **Operator:** D1 (matcher form), D2 (unclassifiable policy, to be re-presented after the D5 fix), and D4 (sibling cards). Also a new decision from defect D1: what the wrapper's risk-amplifier does for PowerShell. It changes distributed template behaviour, so route it to the operator.
- **Lead (plan's owner labels are appropriate):** D3 (PostToolUse sites) and D5 (LIVE arm C). D5 must be revisited after the D3 defect fix, because arm C as written cannot attribute its result to the branch binary.
