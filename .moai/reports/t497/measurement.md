# t497 — 재측정 기록 (plan 전 baseline)

card: t497 · tree: `.claude/worktrees/t497` · branch `WT-codex-neutrality` · base `ace1c5440` (= origin/develop)
측정 시각의 HEAD: `ace1c5440` (이 트리, 이 실행)

리드 지시에 따라 카드가 인용한 수치를 재사용하지 않고 전부 다시 쟀다.

## Claim 1 — codex 에이전트 TOML 11본

Evidence:
```
$ find internal/template/templates/.codex/agents -name '*.toml' | wc -l
11
```
Baseline-attribution: 이 트리, `ace1c5440`. **카드와 일치.**

## Claim 2 — `tools:` 키 선언 0본 = 결함이 아니라 문서화된 결정

Evidence:
```
$ grep -l '^tools' <11개 toml> | wc -l
0
```
그러나 `internal/template/agentemit/agents-codex.yaml:88-` `classes:` 가
`file-write` / `web` / `task-list` / `skill-loader` / `subagent-spawn` / `design-sync`
를 각각 `disposition: documented-drop` 으로 근거와 함께 기록하고 있다.
→ **0본은 emitter 의 의도된 산출이며 미해결 결함이 아니다.** 카드의 이 항목은
   기준선 정정 대상.

## Claim 3 — TOML 은 생성물이다 (카드에 없던 사실)

Evidence — 모든 TOML 1-2행:
```
# Generated by the MoAI agent dual-publication emitter; regenerate, do not edit.
# Neutral source: .claude/agents/moai/<name>.md (body carried verbatim).
```
`internal/template/agentemit/writer.go:104` renderTOML 이
`developer_instructions = <body>` 를 **verbatim** 으로 싣는다.
갱신 경로: `AGENTEMIT_UPDATE=1 go test ./internal/template/agentemit/...`
(`golden_test.go:8,82,97`).
→ **수정은 TOML 이 아니라 중립 소스 `.claude/agents/moai/*.md` 본문에 들어가고,
   골든 재생성으로 TOML 에 반영된다.** TOML 직접 편집은 다음 골든 실행에서 되돌아간다.

## Claim 4 — 배포되는 TOML body 의 Claude 전용 토큰 계수

Evidence (`grep -rhoE '<pat>' .codex/agents/moai/*.toml | wc -l`):

| 패턴 | 수 |
|---|---|
| `invoke Skill(` | 41 |
| `Skill(` 전체 | 45 |
| `Task(Create\|Update\|List\|Get)` | 4 (3줄) |
| `Agent(` | 25 |
| `AskUserQuestion` | 4 |
| `DesignSync` | 6 |

파일별 합계(`AskUserQuestion|TaskCreate|TaskUpdate|TaskList|TaskGet|DesignSync|Skill(|Agent(`):
`14 manager-develop · 13 manager-spec · 13 manager-lead · 10 manager-design ·
8 manager-docs · 5 sync-auditor · 5 super-advisor · 5 manager-git ·
4 plan-auditor · 4 e2e-tester · 3 builder-harness` = 74

Baseline-attribution: 이 트리, `ace1c5440`, 위 명령.
Gap/정정: 카드의 `14/13/13/7/5/5/5/4/3/3/3`(=75)과 4번째 이후가 다르다.
토큰 집합이 서로 달라 생긴 차이로 보이며, **위 표의 토큰 집합이 이 카드의 기준선**이다.

## Claim 5 — `.claude/agents/moai/*.md` 계수는 body 계수와 다르다

Evidence: 같은 패턴을 `.md` 에 돌리면 `Task*` 가 4 가 아니라 **48**.
차이는 `.md` frontmatter 의 `tools:` CSV 이며, emitter 가 frontmatter 를 싣지 않는다.
→ **중립화 대상 판정은 반드시 body(=TOML) 기준으로 한다.** `.md` 전체 grep 은 과다계상.

## Claim 6 — 미러 스킬은 실제로 codex 에 도달한다

Evidence:
```
$ grep -rlE 'AskUserQuestion|Agent\(|TaskCreate|TaskUpdate|TaskList|TaskGet|DesignSync' \
    internal/template/templates/.claude/skills/ | wc -l   → 77 파일
$ ... | cut -d/ -f1 | sort -u | wc -l                      → 15 디렉터리 (전체 34 중)
```
15본: moai · moai-domain-html-report · moai-foundation-{cc,core,thinking} ·
moai-harness-learner · moai-kanban-foreman · moai-meta-harness ·
moai-ref-{api-patterns,owasp-checklist,react-patterns,seo,ui-polish} ·
moai-workflow-{project,spec}

도달 경로: `internal/template/skill_mirror.go:4-5,52,171` 이 배포 시
`.agents/skills/<name>` 미러를 만들어 "every skill this run deployed reachable
from BOTH paths". codex 세션의 skill roots 표가 `.agents/skills` 를 싣는 것은
`agents-codex.yaml:133-135` 에 측정 기록이 있다.
→ **범위 안이다.** (템플릿에 `.agents/` 디렉터리가 없다는 이유로 범위 밖이라 판단하면 오답 —
   미러는 템플릿 산출물이 아니라 배포 시점 생성물이다.)

## Gaps — 이 문서가 관측하지 않은 것

- 74건 각각의 「산문 언급 vs 행위 지시」 분류는 아직 하지 않았다(기계적 grep 으로는 불가).
  분류는 plan 단계 산출물이다.
- 77개 스킬 파일 안의 매치 분류도 하지 않았다.
- codex 런타임이 `Skill("...")` 지시를 만났을 때의 실제 거동은 측정하지 않았다.
- `AGENTS.md:21` 결속표에 `skill-loader` / `subagent-spawn` 행이 없는 것이
  의도인지 누락인지는 판단하지 않았다(설계 결정 대상).

## Residual-risk

- emitter 의 golden 재생성이 이 카드 밖의 `.md` 변경까지 함께 실어 나를 수 있다.
  재생성 직전 `git status` 로 반경을 확인해야 한다.
