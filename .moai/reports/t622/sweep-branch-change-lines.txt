HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:42:- Rollback: `git reset --hard [checkpoint-tag]`
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:96:git checkout main && git pull origin main
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:110:git switch -c feat/SPEC-XXX
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:119:git checkout main
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:121:git reset --hard origin/main   # align local main with squashed remote
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:125:[HARD] Caveat: `git push origin main` is BLOCKED in Phase A/B even with `auto_push: true` — the orchestrator MUST hold push until Phase C branch creation, and the agent MUST NOT attempt a direct push during Phase A/B. Branch protection enforces this server-side and rejects with `! [remote rejected]`; recovery is `git switch -c feat/SPEC-*` to enter Phase C.
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:127:Failure mode — skipping Phase D leaves local main with un-squashed history → next `git pull` produces merge conflict against squashed remote. Recovery: `git fetch origin && git reset --hard origin/main`.
HEAD:internal/template/templates/.claude/agents/moai/manager-git.md:137:- **main_late_branch**: main commit + late `git switch -c feat/SPEC-*` at PR time, PR squash + delete-branch, local main `reset --hard origin/main` cleanup (4-phase procedure — see Late-Branch Invocation Pattern above)
HEAD:internal/template/templates/.claude/rules/moai/core/verification-claim-integrity-detail.md:161:- **L4 — A documented command is indistinguishable from an asserted claim.** Doctrine that *quotes* `git reset --hard origin/main` will be flagged. This is why the exemption is a marker rather than a cleverer regex.
HEAD:internal/template/templates/.claude/rules/moai/development/coding-standards.md:141:- `git reset --hard` — discards uncommitted working-tree state
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard-detail.md:120:  invoked rather than text carried as data. `moai todo add "… git switch …"` is
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard-detail.md:121:  allowed because the command being run is `moai todo add`; `git switch main`
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard-detail.md:122:  and `git checkout -b "feat/x"` both still deny, the latter because the
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard-detail.md:124:  shell wrapper (`bash -c "git switch main"`) is not matched — under-matching an
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard.md:20:| `git checkout <branch>` / `git switch` | relocates every concurrent session's tree |
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard.md:21:| `git checkout -b` / `git switch -c` / `git branch <name>` / any mutating `git branch` form (flag classification: § Mechanical Enforcement) | same, plus leaves a branch other sessions did not expect |
HEAD:internal/template/templates/.claude/rules/moai/workflow/main-checkout-branch-guard.md:22:| `git reset --hard` / `git checkout -- <path>` | discards work the orchestrator cannot see the provenance of |
HEAD:internal/template/templates/.claude/rules/moai/workflow/spec-workflow.md:50:- Step 1 (plan) MUST execute in main checkout on BOTH routes. NO L2/L3 worktree at this step. Plan artifacts are markdown only — no code conflict — and main-authored plans enable cross-SPEC reference for plan-auditor and parallel SPEC scoping. On **Route A** the plan-phase artifacts are committed + pushed directly to `main` (no branch). On **Route B**, the **Late-branch precondition (the Late-Branch closure contract)** applies: when `team.branch_creation.auto_enabled == false` in `git-strategy.yaml`, Step 1 entry requires `git rev-parse --abbrev-ref HEAD == main` (or the user's chosen `main_branch` if it differs). No `plan/SPEC-XXX` branch is created at Step 1; plan-phase commits land directly on `main` and are pushed only after Phase C `git switch -c plan/SPEC-XXX` at PR creation time.
HEAD:internal/template/templates/.claude/rules/moai/workflow/spec-workflow.md:56:  git checkout main
HEAD:internal/template/templates/.claude/rules/moai/workflow/spec-workflow.md:58:  git reset --hard origin/main
HEAD:internal/template/templates/.claude/settings.json.tmpl:564:      "Bash(git reset --hard:*)",
HEAD:internal/template/templates/.claude/skills/moai-ref-git-workflow/SKILL.md:144:| `git reset --hard` | Loses local changes | Confirm with user first |
HEAD:internal/template/templates/.claude/skills/moai-ref-git-workflow/SKILL.md:145:| `git checkout .` | Discards changes | Confirm with user first |
HEAD:internal/template/templates/.claude/skills/moai-workflow-worktree/modules/troubleshooting.md:176:4. If the worktree should track a specific branch, use git checkout branch-name
HEAD:internal/template/templates/.claude/skills/moai-workflow-worktree/modules/troubleshooting.md:179:- Avoid manual git checkout of commit hashes in worktrees
HEAD:internal/template/templates/.claude/skills/moai/workflows/plan/spec-assembly.md:336:- When `auto_enabled == false`: SKIP branch creation entirely. Cwd remains on the current branch (typically `main` — the default for Step 1 per SPEC Phase Discipline). SPEC files are committed to the current branch via the standard commit pipeline. Decision Point 3.5 mode-selection display MUST surface "Late-branch (main commit + late switch)" as the active mode to communicate the deferred-branch state to the user. Phase 13 BODP Gate STILL runs (with EntryPoint = `EntryPlanLateBranch`) — Late-branch does NOT bypass BODP, it only defers branch creation to Phase C (manual `git switch -c feat/SPEC-*` at PR time). Per the late-branch opt-in policy, no automated `git push origin main` is performed during this phase even if `team.automation.auto_push == true`.
HEAD:internal/template/templates/.claude/skills/moai/workflows/sync/delivery.md:323:**github-flow**: `git checkout {main_branch} && git pull origin {main_branch}`
HEAD:internal/template/templates/.claude/skills/moai/workflows/sync/delivery.md:324:**git-flow**: `git checkout develop && git pull origin develop` (for feature branches), `git checkout main && git pull origin main` (for release/hotfix). A `WT-*` branch needs no switch — its route already exits the integration worktree and returns the session to its own tree.
HEAD:internal/template/templates/AGENTS.md:64:**Never change branch state in the primary checkout.** Forbidden there: `git checkout <branch>` /
HEAD:internal/template/templates/AGENTS.md:65:`git switch` (relocates every concurrent session's tree); `git checkout -b` / `git switch -c` /
HEAD:internal/template/templates/AGENTS.md:66:`git branch` (same, plus an unexpected branch); `git reset --hard` / `git checkout -- <path>`
