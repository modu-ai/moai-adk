20:## Primary Mission
24:## Configuration Loading and Resolution
32:- `merge_method = git_strategy.{mode}.merge_method` (`squash` | `merge` | `rebase`; default `squash`) — every merge is executed as `gh pr merge --<merge_method> --delete-branch`, which under the squash default renders `gh pr merge --squash --delete-branch`
34:## Core Operational Principles
38:## Checkpoint System
44:## Commit Management
55:## Context Memory Section
60:## Context (AI-Developer Memory)
77:## Branch Management
92:Detection cue: `manager-git` recognizes Late-branch via `git rev-list main..HEAD --count > 0 && git branch --show-current matches feat/SPEC-* or worktree-*`. The `worktree-*` arm covers work done in a Claude Code worktree, where `moai cc -w <name>` names the branch `worktree-<name>`. The local branch name is not load-bearing either way — Phase C mints the conventional `feat/SPEC-*` name at PR time, so commits accumulated on a `worktree-*` branch flow through the same procedure.
113:# CI passes → merge with the resolved merge_method (§ Configuration Loading and Resolution);
114:gh pr merge <PR> --squash --delete-branch   # squash default
131:## Mode-Specific Git Strategy
133:### Personal Mode
142:### Team Mode
148:- Auto-merge: only with the `--auto-merge` flag, per § PR Auto-Merge (Team Mode)
154:## Synchronization
156:Pre-flight status reads (`git fetch`, `git status`, `git rev-list --count --left-right`, `gh pr checks --json`) are independent and read-only: issue them as ONE single-turn multi-Bash batch per `.claude/rules/moai/core/agent-common-protocol.md` § Parallel Execution (grouping rationale and batch-safety taxonomy: `.claude/rules/moai/workflow/verification-batch-pattern.md`).
164:## PR Auto-Merge (Team Mode)
166:Execute only with `--auto-merge` flag AND all approvals obtained:
170:4. `gh pr merge --<merge_method> --delete-branch` using the resolved merge_method
173:## Context Propagation
178:## Conditional Skill Loading
188:## Model/effort escalation
