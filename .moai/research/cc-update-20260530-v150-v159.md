# CC Upstream Update Research — 2.1.150 .. 2.1.159 (명시적 범위 재분석)

- 분석일: 2026-05-30
- 분석 도구: release-update-specialist agent (dev-only, /97-release-update 명시적 범위 모드)
- 분석 모드: **명시적 범위 강제** (baseline early-exit override). `.moai/state/last-cc-version.json`의 `last_analyzed_version`(2.1.158)과 무관하게 inclusive 범위 `2.1.150..2.1.159` 전체 재분석.
- canonical source: `https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md` (WebFetch 검증 완료)

---

## 1. 분석 범위 (Scope & Ground Truth)

### 현재 최신 버전 + 2.1.159 존재 여부 (Ground Truth)

| 항목 | 값 |
|------|----|
| **changelog 최상단(최신) 엔트리** | **2.1.158** |
| **2.1.159 존재 여부** | **존재하지 않음 (NOT PUBLISHED)** |

> **2.1.159는 changelog 어디에도 존재하지 않습니다.** 본 세션의 직전 baseline 분석이 최신을 2.1.158로 보고한 것과 일치합니다. 2.1.159는 날조하지 않았으며, 게시되지 않았음을 명시적으로 확인합니다.

### 요청 범위 10개 패치 버전 존재 매트릭스

요청된 inclusive 범위는 2.1.150, .151, .152, .153, .154, .155, .156, .157, .158, .159 (10개). 각 존재 여부:

| 버전 | 존재 | 비고 |
|------|------|------|
| 2.1.150 | ✅ 존재 | "Internal infrastructure improvements (no user-facing changes)" 단일 항목 |
| 2.1.151 | ❌ **미게시** | changelog에 없음 (gap) |
| 2.1.152 | ✅ 존재 | 다수 항목 (hook 이벤트 추가 포함) |
| 2.1.153 | ✅ 존재 | 다수 항목 (MCP/subagent/plugin 수정) |
| 2.1.154 | ✅ 존재 | 대량 항목 (Opus 4.8, dynamic workflows, lean system prompt 등) |
| 2.1.155 | ❌ **미게시** | changelog에 없음 (gap) |
| 2.1.156 | ✅ 존재 | 단일 항목 (Opus 4.8 thinking block API error fix) |
| 2.1.157 | ✅ 존재 | 대량 항목 (plugins in .claude/skills, agent field 등) |
| 2.1.158 | ✅ 존재 | 단일 항목 (Auto mode on Bedrock/Vertex/Foundry) |
| 2.1.159 | ❌ **미게시 (미래 버전)** | changelog에 없음 — 게시되지 않음 |

- **실제 존재 버전 (7개)**: 2.1.150, 2.1.152, 2.1.153, 2.1.154, 2.1.156, 2.1.157, 2.1.158
- **미게시 gap (3개)**: 2.1.151, 2.1.155, 2.1.159

---

## 2. Tier별 분류

분류 기준: moai-adk-go는 Claude Code 프로젝트 템플릿(.claude/ agents·skills·commands·hooks·settings.json·rules, .moai/ config, CLAUDE.md, settings.json.tmpl)을 배포하는 Go CLI. 따라서 분류는 **moai-adk 템플릿/Go 코드/문서에 미치는 영향** 관점.

- **Tier 1 (HIGH)**: hook 이벤트·settings.json 스키마·MCP/tool API·subagent/agent/Agent-Teams 동작·/command·skill·output-style 시스템·permission-mode 변경 등 템플릿 또는 Go 코드 변경을 유발할 수 있는 항목.
- **Tier 2 (MEDIUM)**: moai-adk가 문서화하거나 선택적으로 채택할 만한 non-breaking 신규 기능.
- **Tier 3 (LOW)**: 마이너 버그픽스, 외형/IDE/터미널 렌더링, 플랫폼별 항목 — moai-adk 조치 불필요.

### Tier 1 — HIGH (템플릿/코드/문서 영향 가능)

| 버전 | 변경 | moai-adk 영향 | 조치필요 |
|------|------|---------------|:--------:|
| 2.1.152 | `SessionStart` hooks가 `reloadSkills: true` 반환 → 동일 세션 내 skill 디렉토리 재스캔 | moai SessionStart hook은 GLM 자격증명/teammateMode/CLAUDE_ENV_FILE만 처리하며 skill 설치를 하지 않음. 신규 필드는 opt-in. **문서화 대상**: `.claude/rules/moai/development/` hook 스키마 참조 + docs-site hooks 문서. Go 코드/settings.json.tmpl 변경 불필요. | **Y (문서)** |
| 2.1.152 | `SessionStart` hooks가 `hookSpecificOutput.sessionTitle`로 세션 타이틀 설정 | 위와 동일. moai hook 핸들러는 현재 이 필드를 미사용. opt-in 신규 출력 필드. **문서화 대상** (hooks 스키마/docs-site). Go 코드 변경 불필요. | **Y (문서)** |
| 2.1.152 | 신규 `MessageDisplay` hook 이벤트 (assistant 메시지 텍스트 표시 시 변환/숨김) | **신규 hook 이벤트 vocabulary**. moai는 SessionStart/PostToolUse/Stop/SubagentStop/TaskCompleted 등을 문서화함. MessageDisplay는 신규. **문서화 대상**: hook 이벤트 참조 문서 + docs-site. moai가 이 hook을 배포할 필요는 없음(선택적). settings.json.tmpl 강제 변경 없음. | **Y (문서)** |
| 2.1.152 | Skill/slash command frontmatter에 `disallowed-tools` 설정 가능 (skill 활성 동안 도구 제거) | **skill/command frontmatter 스키마 확장**. moai skill 작성 규칙(`skill-authoring.md`, `agent-authoring.md`)에 `tools:`/`allowed-tools:` CSV 규칙 존재. `disallowed-tools`는 신규 선택 필드. **문서화 대상**: skill-authoring 규칙 + docs-site. 기존 moai skill에 강제 추가 불필요. | **Y (문서)** |
| 2.1.157 | `claude agents`: `settings.json`의 `agent` 필드가 dispatched 세션에 적용됨 (`--agent <name>` override) | **settings.json 스키마 관련**. moai의 settings.json.tmpl은 `agent` 필드를 렌더링하지 않음. 신규 필드는 사용자 opt-in. **문서화 대상**: settings 관리 규칙 + docs-site settings 문서에 `agent` 필드 언급 추가 검토. settings.json.tmpl 강제 변경 불필요. | **Y (문서, 선택)** |
| 2.1.153 | subagent(Agent tool) frontmatter MCP server가 `--strict-mcp-config`/`--bare`/managed MCP policy를 무시하던 버그 수정 + 차단된 subagent MCP server 경고 표시 | moai agent들은 frontmatter에 인라인 MCP server를 선언하지 않음(Context7 등은 별도 설정). 동작 정합성 개선이며 moai 템플릿 직접 영향 없음. **검토 후 조치 불필요** 가능성 높음. skeptical 판단: moai agent frontmatter에 `mcpServers` 인라인 선언이 없으므로 영향 없음. | N |
| 2.1.154 | subagent가 background 세션에서 worktree-isolation guard를 우회해 공유 checkout에 쓰던 버그 수정 | moai의 worktree isolation 정책(CLAUDE.md §14)과 정합. CC 런타임 버그 수정으로, moai 측 조치 불필요(런타임이 처리). 다만 worktree 정책 문서에 "런타임이 격리 보장 강화" 참고 가능. | N |
| 2.1.154 | dynamic workflows 정식 도입 (`/workflows`, tens-to-hundreds agents) | moai는 이미 `dynamic-workflows.md` 규칙으로 문서화 완료(v2.1.154 기준). **이미 반영됨**. 추가 조치 불필요. | N |

**Tier 1 판정 요약**: 8개 항목 중 **4개가 moai 조치(모두 문서/스키마-문서 레벨, Go 코드/template 강제 변경 없음) 필요** — MessageDisplay hook, SessionStart reloadSkills/sessionTitle, skill disallowed-tools frontmatter, settings.json agent 필드. 나머지 4개(2.1.153 MCP fix, 2.1.154 worktree fix, 2.1.154 dynamic workflows)는 이미 반영되었거나 런타임 버그 수정이라 조치 불필요.

> **중복 확인**: 본 4개 조치 항목은 직전 baseline 재분석(`cc-update-20260530.md`, range 2.1.150..2.1.158)에서 이미 식별됨. git log 최근 커밋 `038f6e793 docs(settings): Claude Code agent settings field (v2.1.157)` + `3dcd58677 docs(hooks): MessageDisplay 이벤트 + SessionStart reloadSkills/sessionTitle 필드 추가`가 이미 일부를 처리함. **skill disallowed-tools frontmatter 문서화는 아직 미확인** — 권장 조치 참조.

### Tier 2 — MEDIUM (문서화/선택적 채택)

| 버전 | 변경 | moai-adk 영향 | 조치필요 |
|------|------|---------------|:--------:|
| 2.1.152 | `/reload-skills` 명령 추가 (재시작 없이 skill 디렉토리 재스캔) | moai 워크플로우 문서에 신규 CC 명령 언급 가능. moai 명령 체계와 무관. | N (선택) |
| 2.1.152 | `--fallback-model`로 세션 전환 (primary model not found 시) | moai 모델 정책 문서에 참고 가능. 강제 조치 불필요. | N (선택) |
| 2.1.152 | Auto mode opt-in consent 제거 + OpenTelemetry `app.entrypoint` 속성 | moai telemetry 관련 문서에 참고 가능. 조치 불필요. | N |
| 2.1.153 | `skipLfs` plugin marketplace 옵션 + status line이 `COLUMNS`/`LINES` env 수신 | moai status_line.sh.tmpl이 COLUMNS/LINES를 활용할 수 있으나 현재 불필요. 조치 불필요. | N (선택) |
| 2.1.153 | `/model`이 선택을 기본값으로 저장 (`s`=this-session-only). keybinding `modelPicker:setAsDefault`→`thisSessionOnly` 리네임 | 사용자 keybindings.json 영향. moai 템플릿은 keybindings를 배포하지 않음. 조치 불필요. | N |
| 2.1.154 | lean system prompt가 기본값(Haiku/Sonnet/Opus 4.7 이하 제외) + Opus 4.8 high effort 기본 / `/effort xhigh` | moai의 effort 정책(ultrathink=xhigh, model-policy.md) 문서에 참고 가능. CLAUDE.md §12는 이미 Opus 4.8 Adaptive Thinking 문서화. 조치 불필요(이미 반영). | N |
| 2.1.154 | `claude agents`: `! <command>` background 세션, `claude --bg --exec` | moai background agent 정책(agent-common-protocol.md §Background Agent)과 무관한 CC 기능. 조치 불필요. | N |
| 2.1.157 | Plugins in `.claude/skills` 자동 로드 (marketplace 불필요) + `claude plugin init <name>` | moai는 `.claude/skills/`에 moai-* 스킬을 배포. plugin 자동 로드와 namespace 충돌 여부 검토 가치 있음(skeptical: moai 스킬은 plugin이 아닌 skill 형식이므로 직접 충돌 가능성 낮음). **모니터링 권장**, 즉시 조치 불필요. | N (모니터) |
| 2.1.157 | `EnterWorktree`가 Claude-managed worktree 간 mid-session 전환 가능 | moai worktree 정책(L1/L2/L3)과 정합. 런타임 기능. 조치 불필요. | N |

**Tier 2 판정 요약**: 9개 항목 모두 **선택적/모니터링 레벨, 강제 조치 불필요**. 일부는 이미 moai 규칙에 반영됨(lean prompt/effort, dynamic workflows).

### Tier 3 — LOW (조치 불필요)

| 버전 | 변경 (대표) | moai-adk 영향 | 조치필요 |
|------|------------|---------------|:--------:|
| 2.1.150 | Internal infrastructure improvements (no user-facing changes) | 없음 | N |
| 2.1.152 | 다수 UI/렌더링 fix (thinking summary, focus mode, markdown table border, effort dialog, cache token 보고 등) | 없음 (외형/CC 내부) | N |
| 2.1.153 | 다수 fix (Windows installer, npm update channel, memory usage, file:// links, background session, IME, tmux clipboard 등) | 없음 (CC 런타임/플랫폼) | N |
| 2.1.154 | 다수 fix (`rm -rf $HOME` trailing slash, `$TMPDIR`, terminal rendering, plan file `[Image #N]`, managed settings allowedMcpServers drop 등) | 없음 (CC 런타임/sandbox) | N |
| 2.1.156 | Opus 4.8 thinking block 수정 → API error fix | 없음 (CC 런타임) | N |
| 2.1.157 | 다수 fix (image paste, sandbox network prompt, `--resume`, `/model` picker, terminal freeze, WSL image, VS Code paste, IDE Stop, 워크플로우 키워드 trigger 설정 등) | 없음 (CC 런타임/IDE/터미널) | N |
| 2.1.158 | Auto mode on Bedrock/Vertex/Foundry for Opus 4.7/4.8 (`CLAUDE_CODE_ENABLE_AUTO_MODE=1`) | 없음 (CC 배포 플랫폼 기능) | N |

**Tier 3 판정 요약**: 대량의 버그픽스/렌더링/플랫폼 항목 — **moai-adk 조치 불필요**.

### Tier 카운트 요약

distinct change-row 기준 (per-bullet 아님):

| Tier | 항목 수 | 조치필요 수 |
|------|:-------:|:----------:|
| Tier 1 (HIGH) | 8 | 4 |
| Tier 2 (MEDIUM) | 9 | 0 (선택/모니터링) |
| Tier 3 (LOW) | 7 그룹 (수십 bullets) | 0 |

---

## 3. 권장 조치

verified Tier 1 중 조치 필요 4개 항목은 **전부 문서/스키마-문서 레벨**이며 **Go 코드 또는 internal/template/templates/ 강제 변경은 없습니다**. 직전 baseline 분석에서 이미 대부분 처리됨:

1. **MessageDisplay hook 이벤트** — git log `3dcd58677`에서 이미 hooks 문서에 추가됨. **추가 조치 불필요**.
2. **SessionStart reloadSkills / sessionTitle 필드** — git log `3dcd58677`에서 이미 추가됨. **추가 조치 불필요**.
3. **settings.json `agent` 필드 (v2.1.157)** — git log `038f6e793`에서 이미 docs(settings)로 문서화됨. **추가 조치 불필요**.
4. **skill/command frontmatter `disallowed-tools` (v2.1.152)** — 직전 분석 및 최근 git log에서 명시적 처리 흔적 미확인. **유일하게 미해결 가능성 있는 항목**.
   - 권장: `.claude/rules/moai/development/skill-authoring.md` + `agent-authoring.md`의 frontmatter 스키마 섹션에 `disallowed-tools` 선택 필드 1줄 추가 검토. docs-site 4-locale skill frontmatter 참조 문서에 동기화. (Tier S, 문서 전용, Go 코드 무관)
   - skeptical 판단: `disallowed-tools`는 CC 런타임이 처리하는 선택 필드이며 기존 moai 스킬에 강제 추가할 필요 없음. moai 스킬 작성자가 도구를 제거하고 싶을 때만 사용하는 advisory 문서화로 충분.

**종합 권장**: 신규 changelog 콘텐츠 없음(2.1.150..2.1.158은 직전 baseline에서 분석 완료, 2.1.159 미게시). 이 재분석은 직전 결과를 ground-truth로 재확인하는 성격. **유일한 잔여 후보는 skill `disallowed-tools` frontmatter 문서화(Tier S, 선택)**. 그 외는 **조치 불필요**.

---

## 4. Umbrella SPEC 제안

verified Tier 1 중 template/Go 코드 작업을 요하는 항목이 **0개**입니다 (4개 모두 문서/스키마-문서 레벨). 따라서:

> **Umbrella SPEC 제안 없음.** 잔여 후보(skill `disallowed-tools` 문서화)는 단일 docs Tier S 변경으로 충분하며 별도 umbrella SPEC 디렉토리가 불필요합니다. 만약 문서화를 진행한다면 기존 docs sync 워크플로우(`manager-docs`)로 처리 권장 — `/moai sync` 또는 직접 docs 편집.

(만약 향후 disallowed-tools를 정식 SPEC으로 다룰 경우 후보 ID: `SPEC-V3R6-CC2152-SKILL-DISALLOWED-TOOLS-001` (Tier S, docs only) — 단, 본 보고서는 SPEC 산출물을 작성하지 않으며 제안만 함.)

---

## 5. Sources (검증된 URL)

| URL | 검증 | 용도 |
|-----|------|------|
| `https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md` | ✅ WebFetch 검증 완료 (3회 호출, verbatim bullet 추출) | canonical CC changelog (Anthropic이 직접 작성하는 파일) |

추가 docs URL은 본 재분석에서 신규 조치가 없어 cross-reference 미수행. 직전 baseline 분석(`cc-update-20260530.md`)의 docs cross-reference 참조.

---

## 부록 A — 검증된 verbatim 변경 항목 (참조용)

### 2.1.158 (1)
- Auto mode is now available on Bedrock, Vertex, and Foundry for Opus 4.7 and Opus 4.8. Opt in by setting `CLAUDE_CODE_ENABLE_AUTO_MODE=1`

### 2.1.156 (1)
- Fixed an issue when using Opus 4.8 where thinking blocks were modified, leading to API errors.

### 2.1.150 (1)
- Internal infrastructure improvements (no user-facing changes)

### 2.1.152 — Tier 1 핵심 항목 (verbatim)
- Skills and slash commands can now set `disallowed-tools` in frontmatter to remove tools from the model while the skill is active
- Added `/reload-skills` command to re-scan skill directories without restarting the session
- `SessionStart` hooks can now return `reloadSkills: true` to re-scan skill directories, making skills installed by the hook available in the same session
- `SessionStart` hooks can now set the session title via `hookSpecificOutput.sessionTitle` on startup and resume
- Added a `MessageDisplay` hook event that lets hooks transform or hide assistant message text as it is displayed

### 2.1.153 — Tier 1 핵심 항목 (verbatim)
- Fixed subagent (Agent tool) frontmatter MCP servers ignoring `--strict-mcp-config`, `--bare`, remote mode, enterprise managed MCP config, and managed-settings MCP server allow/deny policies
- `--strict-mcp-config` no longer strips inline `mcpServers` from explicitly-passed agent definitions (`--agents` / SDK `agents`), and blocked subagent MCP servers now surface a visible warning

### 2.1.154 — Tier 1 핵심 항목 (verbatim)
- Introducing dynamic workflows: ask Claude to create a workflow and it orchestrates work across tens to hundreds of agents in the background, so you can take on larger, more complex tasks. Run `/workflows` to view your runs
- Fixed subagents in background sessions bypassing the worktree-isolation guard and writing to the shared checkout

### 2.1.157 — Tier 1 핵심 항목 (verbatim)
- Plugins in `.claude/skills` directories are now automatically loaded, no marketplace required
- Added `claude plugin init <name>` to scaffold a new plugin in `.claude/skills`
- `claude agents`: the `agent` field in `settings.json` is now honored for dispatched sessions, with `--agent <name>` to override it
- `EnterWorktree` can now switch between Claude-managed worktrees mid-session

---

분석 완료. 본 보고서는 `.moai/research/`에만 저장되며 `internal/template/templates/`를 건드리지 않습니다. `.moai/state/last-cc-version.json`은 변경하지 않았습니다 (orchestrator 판단 이월).
