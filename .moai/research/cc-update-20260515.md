# Claude Code Upstream Change Report — v2.1.140 ~ v2.1.142

> **[DEV-ONLY]** This report is for moai-adk-go maintainers (GOOS).
> Generated by `/97-release-update --since 2.1.140` workflow on 2026-05-15.

## 1. Metadata

| Field | Value |
|-------|-------|
| Report ID | `cc-update-20260515` |
| Analysis Range | `2.1.140` ~ `2.1.142` (3 consecutive versions) |
| Previous Analysis | `2.1.0` ~ `2.1.139` (`.moai/research/` no prior file; state file commit 2026-05-12) |
| Source | `https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md` (verified 2026-05-15) |
| Generated by | `/97-release-update` workflow (`.claude/skills/moai/workflows/release-update.md`) |
| Total Items Analyzed | 41 |
| Actionable Items | 11 (Tier 1: 5, Tier 2: 6) |
| Follow-up SPEC Candidates | 3 |

## 2. Versions

### 2.1 Version 2.1.142 (latest)

20 changes — heavy on `claude agents` CLI flags, plugin SKILL.md surface, daemon stability, and background-agent fixes.

### 2.1.2 Version 2.1.141

44 changes — broad UX and bugfix release. Most impactful for moai-adk-go: `terminalSequence` hook field, `AskUserQuestion popup` regression fix, `--cwd` flag for `claude agents`.

### 2.1.3 Version 2.1.140

13 changes — small release. Notable: Agent tool `subagent_type` case-insensitive matching, settings hot-reload symlink fix.

## 3. Tier-1 Critical (5 items)

| # | Item | Version | moai-adk-go impact | Action |
|---|------|---------|----|---|
| T1-1 | `claude agents` 8 new flags: `--add-dir`, `--settings`, `--mcp-config`, `--plugin-dir`, `--permission-mode`, `--model`, `--effort`, `--dangerously-skip-permissions` | 2.1.142 | HIGH — `moai cc / cg / glm` commands spawn Claude sessions; pass-through opportunity, especially for `--effort` mapping to `effortLevel` settings.json key | Follow-up SPEC candidate (T1-1-SPEC below) |
| T1-2 | Fast mode now uses Opus 4.7 by default (previously Opus 4.6) | 2.1.142 | MEDIUM — Aligns with moai-adk-go Opus 4.7 adoption (README §version table line 54). No code change; docs update only | docs-site `advanced/settings-json.md` reflect updated default |
| T1-3 | Plugins with root-level `SKILL.md` and no `skills/` subdirectory now surfaced as a skill | 2.1.142 | LOW — moai-adk-go body does not ship plugins; user-facing plugin guide may benefit | docs-site `advanced/agent-guide.md` mention root-level SKILL.md option |
| T1-4 | Added `terminalSequence` field to hook JSON output for notifications | 2.1.141 | MEDIUM — moai-adk-go hooks (`.claude/hooks/moai/handle-*.sh`) can emit terminal escape sequences for richer notifications | Follow-up SPEC candidate (T1-4-SPEC below) |
| T1-5 | Fixed AskUserQuestion popup hiding last line of preceding content | 2.1.141 | HIGH — moai-adk-go enforces AskUserQuestion-Only Interaction (CLAUDE.md §1, CLAUDE.local.md §19). Regression resolution improves orchestrator UX immediately | No action — auto-applies with CC ≥ 2.1.141 |

## 4. Tier-2 Important (6 items)

| # | Item | Version | moai-adk-go impact | Action |
|---|------|---------|----|---|
| T2-1 | `claude agents --cwd <path>` to scope session list | 2.1.141 | MEDIUM — Aligns with SPEC worktree mode (CLAUDE.local.md §18.12 BODP). Useful for narrowing agent session list per worktree | Document in §18.12 cross-reference; no code change required |
| T2-2 | Fixed background sessions not recognizing pre-existing git worktrees | 2.1.142 | HIGH — Resolves a class of issues related to lessons #12 (worktree isolation) and #13 (--team base mismatch). Auto-applies | Note in lessons archive that CC ≥ 2.1.142 mitigates the worktree race |
| T2-3 | `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` for GitHub plugin HTTPS cloning | 2.1.141 | LOW — Currently no GitHub plugin usage in moai-adk-go | Defer; revisit when plugin marketplace adoption increases |
| T2-4 | `ANTHROPIC_WORKSPACE_ID` for workload identity federation | 2.1.141 | LOW — Enterprise-only; moai-adk-go single-developer environment | Defer |
| T2-5 | `Rewind: Summarize up to here` option | 2.1.141 | LOW — User UX feature; complements CLAUDE.local.md §6 (context-window management) but no code path | Defer (informational only) |
| T2-6 | Auto mode permission dialog explains `permissions.ask` rules | 2.1.141 | LOW — moai-adk-go template uses `permissions.allow`; `permissions.ask` rarely exercised | Defer |

## 5. Tier-3 Minor / Informational (30 items)

Grouped by category for skim-read. All items in Tier 3 require **no maintainer action** beyond a CC upgrade. They auto-apply.

### 5.1 Background agent / daemon stability (v2.1.142)

- Background sessions disappearing after macOS sleep/wake
- Daemon not exiting cleanly after binary upgrade
- Background agents crash-looping with Claude-in-Chrome extension
- `claude --bg --dangerously-skip-permissions` not persisting
- `claude agents` opening crashed session with redundant dispatches
- `claude agents` launching no longer fails with unhealthy pre-warmed worker
- `claude agents` no longer shows empty placeholder sessions
- Empty idle background sessions auto-retired by daemon after 5 minutes

### 5.2 `claude agents` UX (v2.1.142, 2.1.141)

- Clicking links in attached `claude agents` session
- `claude agents` using daemon's default editor instead of shell's `$EDITOR`/`$VISUAL`
- `claude agents` deadlocking on Windows with network-drive working directories
- `claude agents` showing agent-type list instead of dashboard
- `claude agents` agents finishing work move to Completed status
- Pressing `x` on selected subagent typing into prompt

### 5.3 MCP / Plugin (v2.1.142, 2.1.141)

- `MCP_TOOL_TIMEOUT` not raising per-request fetch timeout for remote HTTP and SSE MCP servers
- Plugins using `skills: ["./"]` showing false path error
- Plugin cache cleanup deleting active plugin version
- `/plugin` browse pane showing "0 installs" for new plugins
- Plugin advisories not naming every shadowed `plugin.json` key
- `claude plugin install` failing when marketplace `ref` no longer exists
- Plugin details not showing MCP servers from `.mcp.json`
- Plugin MCP servers with unset config showing generic failure
- MCP HTTP/SSE servers with 403 on connect showing "failed"
- Remote MCP servers disconnecting when optional server-events failed
- Remote Control MCP connectors failing with 401 on token rotation
- Remote Control auto-enrolling trusted device on stale token

### 5.4 Hook system (v2.1.141, 2.1.140)

- Hook configuration error messages for prompt/agent-type hooks (improved)
- Hooks receiving non-existent `transcript_path` after `EnterWorktree` (fixed)
- `/goal` silently hanging with `disableAllHooks` or `allowManagedHooksOnly` (v2.1.140 fixed)

### 5.5 Misc UI / Terminal / Theme (v2.1.142, 2.1.141)

- Background-color bleed in 256-color-only terminals
- Markdown tables with cell wrapping falling back to vertical layout
- Light-ansi theme using invisible white for diff context
- Error overlay dumping minified bundle source
- Multi-line statusline output dropping or corrupting rows
- Welcome banner showing "API Usage Billing" on third-party providers

### 5.6 Settings / Config (v2.1.140, 2.1.141)

- Settings hot-reload with symlinked files causing spurious events (fixed)
- Remote managed settings not retrying on 401 (fixed)
- Managed `extraKnownMarketplaces` auto-update policy not persisting (fixed)
- `/model` silently changing autocompact threshold in other sessions (fixed)

### 5.7 Platform-specific (Windows / Bedrock / VSCode) (v2.1.141)

- `claude daemon status` and `/doctor` on Windows
- Background side-queries failing on Bedrock/Vertex/Foundry without model override
- Background jobs on custom gateway not getting auto-named
- Switching permission mode not auto-dismissing tool-permission prompts
- Bedrock: `awsCredentialExport` now always runs when configured
- VSCode: In-chat mic showing no feedback on silence
- VSCode: Voice mode WSL error suggests installing `sox libsox-fmt-pulse`
- Windows Alt+V image paste reporting "no image found"
- SDK "Claude Code native binary not found" on Linux with multiple packages

### 5.8 Other (v2.1.140 small release)

- Improved Agent tool `subagent_type` matching for case/separator-insensitive values
- Updated agent color palette
- `claude --bg` failing with "connection dropped mid-request"
- Background service startup failing on enterprise endpoint security
- `/loop` scheduling redundant wakeups
- Recurring event-loop stall on Windows with missing executables
- Read tool validation with padded/prefixed offset strings
- Native terminal cursor not staying at input caret
- Plugins now warn when default component folder silently ignored

## 6. Follow-up SPEC Candidates

### T1-1-SPEC — `claude agents` Flag Pass-through + `--effort` Mapping

- **Proposed SPEC ID**: `SPEC-V3R4-CC2X-ADOPT-002`
- **Scope**: `moai cc`, `moai cg`, `moai glm` commands accept new `claude agents` flags (`--effort`, `--model`, `--mcp-config`, `--plugin-dir`, `--add-dir`, `--settings`, `--permission-mode`, `--dangerously-skip-permissions`) and pass through to the spawned Claude session.
- **Special focus**: `--effort` should pull default from `settings.json` `effortLevel` field (per `.claude/rules/moai/development/coding-standards.md` Claude Code Version Compatibility table).
- **Priority**: P2 (UX improvement, no urgent breaking change)
- **Estimated complexity**: 2-3 files in `internal/cli/` + tests + docs-site `multi-llm/` updates (4 locale)

### T1-4-SPEC — Hook `terminalSequence` Field Adoption

- **Proposed SPEC ID**: `SPEC-V3R4-CC2X-ADOPT-003`
- **Scope**: Update moai-adk-go hook scripts (`.claude/hooks/moai/handle-*.sh`) to optionally emit `terminalSequence` JSON field in their stdout response, enabling richer terminal notifications for events like `Notification`, `SubagentStop`, etc.
- **Priority**: P3 (selective adoption, opt-in)
- **Estimated complexity**: Update 1-2 hook handlers + Go templating in `internal/template/templates/.claude/hooks/moai/`

### Tier 1-2 docs-only update (no SPEC needed)

- **Files**: `docs-site/content/{ko,en,ja,zh}/advanced/settings-json.md` — reflect Fast mode default = Opus 4.7
- **Files**: `docs-site/content/{ko,en,ja,zh}/advanced/agent-guide.md` — mention plugin root-level SKILL.md option (T1-3)
- **CHANGELOG.md** — single entry for CC v2.1.140-142 analysis
- **Handler**: manager-docs subagent (Phase 6 of this workflow)

## 7. moai-adk-go Code Impact Summary

| File / Path | Impact category | Reason |
|-------------|----|---|
| `internal/cli/cc.go` (or equivalent) | Tier 1 follow-up | `claude agents` flag pass-through (T1-1-SPEC) |
| `internal/cli/cg.go`, `internal/cli/glm.go` | Tier 1 follow-up | Same flag pass-through (T1-1-SPEC) |
| `internal/template/templates/.claude/hooks/moai/handle-*.sh` | Tier 1 follow-up | `terminalSequence` adoption (T1-4-SPEC) |
| `internal/template/templates/.claude/settings.json.tmpl` | No change | No new top-level fields required from CC 2.1.140-142 |
| `internal/template/templates/CLAUDE.md` | No change | Workflow rules unaffected |
| `docs-site/content/{4 locale}/advanced/settings-json.md` | docs sync | Opus 4.7 default + `--effort` flag mention |
| `docs-site/content/{4 locale}/advanced/agent-guide.md` | docs sync | Plugin root SKILL.md option |
| `docs-site/content/{4 locale}/guides/multi-llm-ci.md` | optional docs sync | `claude agents` flag table (deferred to T1-1-SPEC) |
| `README.md`, `README.ko.md` | optional version line bump | Note compatibility through 2.1.142 in compatibility table |
| `CHANGELOG.md` | docs sync | One entry summarizing analysis result |

## 8. Lessons / Auto-Memory Notes

- **Lesson observation candidate**: CC v2.1.142's worktree-recognition fix mitigates a worktree race recorded in lessons #12 + #13. Action: add a `[CC ≥ 2.1.142]` note inline in lessons #12 + #13 entries when next touched.
- **Lesson observation candidate**: AskUserQuestion popup regression (T1-5) historically caused user complaints; now resolved. Action: remove any "popup may hide content" caveats in operator-facing docs (none currently exist).

## 9. State File Update (after Phase 5)

`.moai/state/last-cc-version.json` will be updated to:

```json
{
  "last_analyzed_version": "2.1.142",
  "last_analyzed_date": "2026-05-15",
  "last_master_research": ".moai/research/cc-update-20260515.md",
  "analysis_history": [
    {
      "version_range": "2.1.140..2.1.142",
      "date": "2026-05-15",
      "spec_id": null,
      "items_found": 41,
      "tier1_count": 5,
      "tier2_count": 6,
      "tier3_count": 30,
      "followup_specs": ["SPEC-V3R4-CC2X-ADOPT-002 (candidate)", "SPEC-V3R4-CC2X-ADOPT-003 (candidate)"]
    }
  ]
}
```

## 10. Docs Sync Scope (Phase 6 delegation)

manager-docs subagent will be invoked with this scope:

- **Locales**: ko (canonical) → en (mirror) → ja, zh (parallel mirror)
- **Files to update**:
  - `docs-site/content/ko/advanced/settings-json.md` + 3 mirrors — Opus 4.7 default mention
  - `docs-site/content/ko/advanced/agent-guide.md` + 3 mirrors — root-level SKILL.md option
  - `CHANGELOG.md` — single "[Unreleased]" entry: "docs(cc-update): CC v2.1.140-142 analysis report + docs sync"
- **NOT updated** (intentionally):
  - `multi-llm-ci.md` — full `claude agents` flag table belongs to T1-1-SPEC, not this report
  - `installation.md` — CC version line is dynamic ("≥ v2.1.0"), no fixed bump needed
  - README.md — compatibility table line addition deferred until T1-1-SPEC merges

## 11. PR Scope (Phase 7 delegation)

manager-git subagent will be invoked to create a PR with:

- **Branch**: `chore/cc-update-2.1.140-142` (already created)
- **Base**: `main`
- **Title**: `chore(release-update): CC v2.1.140-142 분석 + docs-site sync`
- **Files in PR**:
  - `.claude/skills/moai/workflows/release-update.md` (new — workflow body restored)
  - `.moai/research/cc-update-20260515.md` (new — this report)
  - `.moai/state/last-cc-version.json` (modified — version bump to 2.1.142)
  - `CLAUDE.local.md` (modified — §21 added + §2 dev-only registry)
  - `docs-site/content/{ko,en,ja,zh}/advanced/settings-json.md` (modified — Opus 4.7 default)
  - `docs-site/content/{ko,en,ja,zh}/advanced/agent-guide.md` (modified — root-level SKILL.md option)
  - `CHANGELOG.md` (modified — one entry under `[Unreleased]`)
- **Excluded** (intentional):
  - `.moai/specs/SPEC-V3R4-HARNESS-001/spec.md` — independent in-flight work, git-stashed during this run
- **Merge strategy**: squash (chore category per CLAUDE.local.md §18.3)

## 12. Open Questions / Maintainer Decisions

- **Q1**: Should T1-1-SPEC (`claude agents` flag pass-through) be scheduled into Sprint 14 or later? — Out of scope for this report; raise in next planning session.
- **Q2**: Should the `terminalSequence` adoption (T1-4-SPEC) wait for user-side feedback first, or proactively adopt? — Out of scope; raise in next planning session.
- **Q3**: Is `docs.anthropic.com/*` URL family considered fully deprecated for moai-adk-go references? — README links to `docs.anthropic.com/en/docs/claude-code` still redirects (verified 2026-05-15); leave unchanged until next docs sweep.

---

**Status**: Phase 4 complete. Phase 5 (state file) → Phase 6 (docs-site sync, manager-docs) → Phase 7 (PR, manager-git) follow.

**Reproducibility**: Re-run with `/97-release-update --since 2.1.143` once a newer CC release is published.
