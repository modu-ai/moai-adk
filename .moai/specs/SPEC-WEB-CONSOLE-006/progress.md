# Progress — SPEC-WEB-CONSOLE-006

## Plan-phase

```yaml
plan_complete_at: 2026-06-05    # iter-2 patch (line-count + Class A/B/C re-classification + golangci correction)
plan_status: audit-ready
plan_audit_iterations: 2        # iter-1 FAIL 0.71 → patch → iter-2 PASS-WITH-DEBT 0.84 → iter-2 debt patch (5 mechanical fixes) → re-audit pending
tier: L
cycle_type: ddd
artifacts:
  - spec.md          # GEARS, 12-field frontmatter, 18 REQ-WC6-*, §2 characterization gate (Class A/B/C), §2.1.1 12-test Class C inventory, §4 exclusions (E.1-E.5 + E.5.8 carve-out)
  - plan.md          # §F Tier L justification (build-system pillar), §G M1-M6 port-first milestones, §H risks
  - acceptance.md    # 19 AC matrix (+AC-WC6-019 Class C) + 7 GWT scenarios + 7 edge cases + §D.3 markup-parity ledger + §D.6 DoD
  - design.md        # Templ component tree + §B markup-parity contract (§B.1 = §2.1.1 twelve Class C) + §D build flow + §F 007-seeding
  - research.md      # as-is render pipeline (page.html.tmpl = 339 lines) + A/B/C test classification + HTMX/Templ validation + cohort roadmap
  - progress.md      # this file
spec_id: SPEC-WEB-CONSOLE-006
module: internal/web
related_specs: [SPEC-WEB-CONSOLE-001, SPEC-WEB-CONSOLE-002, SPEC-WEB-CONSOLE-003, SPEC-WEB-CONSOLE-004, SPEC-WEB-CONSOLE-005]
deferred_to: SPEC-WEB-CONSOLE-007   # S2b — nested config-section editing (006 is the enabler)
```

### Iter-2 debt patch (2026-06-05) — 5 mechanical fixes (no new scope)

- **D3 line-count**: `page.html.tmpl` 672 → **339** (verified `wc -l`) across spec.md/plan.md/design.md/research.md/progress.md (10 sites). Tier L justification re-leaned on the build-system pillar (new `a-h/templ` dep + codegen + Makefile/CI), not LOC.
- **D1/D2 re-inventory**: Class C re-classification (3 misclassified Class B → Class C) + the **complete verified 12-test source-coupled inventory** (spec.md §2.1.1, verified by exhaustive grep: 35 `page.html.tmpl`/`pageTemplate`/`readEmbeddedAsset` sites; 12 distinct enclosing functions read the deleted source / retired symbol). A/B/C now rests on what the assertion OBSERVES (HTTP behavior = B unmodified; markup-structure or internal-source-read = A/C).
- **N1 dual-classification**: 9 tests dual-listed in §2.2 Class A table annotated inline "→ Class C, see §2.1.1" (one authoritative classification).
- **N1 stale "two" count**: replaced the wrong "two tests" count in spec.md §2.2 closing paragraph + design.md §B.1 + acceptance.md EC-7 → all reference the §2.1.1 twelve.
- **N2 Class C AC**: added **AC-WC6-019** (traces REQ-WC6-001) binding the §2.1.1 Class C mechanism-retarget discipline + the no-delete-except-symbol-existence rule.
- **N3 E.5.8 carve-out**: E.5.8 now carries the Class C carve-out text (retired-symbol-only tests MAY be retired with §D.3 justification) — the §2.1.1 reference no longer dangles.
- **D4 golangci correction**: no `.golangci.yml` exists in the repo; golangci-lint skips generated files by default via the `// Code generated by templ - DO NOT EDIT.` header. Corrected plan.md (3 sites) + design.md (1 site) — removed the ungrounded "configure/verify the config" claim.

### Plan-phase decisions recorded

- **Tier L** — build-system change (new `github.com/a-h/templ` dependency + `templ generate` codegen + Makefile/CI wiring) + whole-renderer rewrite (~339-line `page.html.tmpl` → Templ components, >1000 LOC source+generated) + ~15 brittle Class A markup-test ports. Cohort precedent was Tier M, but none touched the build system; this one does → L envelope justified (plan.md §F).
- **Characterization gate is the spine** — Class B server-contract tests (~55) stay green WITHOUT modification (= behavior preserved); Class A markup tests (~15) stay green with at most §2.2-justified parity adjustments. Full A/B inventory in spec.md §2.1/§2.2.
- **HTMX-depth resolved** — 006 lands the Templ render migration + the HTMX *foundation* (embed `htmx.min.js`, progressive-enhancement `hx-*`) while keeping POST /save observably full-page (REQ-WC6-014). Partial-swap section-nav = 007. Vanilla theme/i18n/segment stay vanilla (REQ-WC6-016).
- **Scope fence** — 006 ports the 5 fieldsets 1:1; ZERO new config section, ZERO settings.json field, ZERO observable behavior change. Section expansion = 007 (S2b); 006's component tree + HTMX foundation is the 007 enabler (E.1 + design.md §F).
- **D3 (commit generated files)** — RECOMMEND committing `*_templ.go` + CI drift-guard (preserves bare-clone `go build`); run-phase confirms.

### Run-phase milestone map (port-first)

| M | Subject | Key REQ | Gate |
|---|---------|---------|------|
| M1 | Templ scaffolding + build wiring | WC6-009/010/011 | `templ generate` + `go build` green; suite still green |
| M2 | Port langSelect/optSelect/icon helpers | WC6-002 | byte-comparable helper markup |
| M3 | Port chrome + 5 fieldsets + swap render call | WC6-003/018 | Class A bulk green; Class B green |
| M4 | HTMX foundation (embed htmx.min.js + hx-* PE) | WC6-006/014/015 | full-page /save preserved; zero CDN |
| M5 | Reconcile Class A + full characterization green | WC6-001/002/013 | closure gate exit 0 |
| M6 | Cleanup + final cross-platform verify | all | host + Windows build green |

### Closure gate (run-phase verifies)

```bash
templ generate ./internal/web/... \
  && go test ./internal/web/... ./internal/config/... ./internal/cli/... \
  && go build ./... \
  && GOOS=windows GOARCH=amd64 go build ./...   # all exit 0
git diff --exit-code internal/web/validate.go    # byte-unchanged
git diff --stat <Class B test files>             # empty (no Class B test modified)
```

### SPEC ID pre-write self-check (recorded)

decomposition: SPEC ✓ | WEB ✓ | CONSOLE ✓ | 006 ✓ → PASS
(canonical regex `^SPEC(-[A-Z][A-Z0-9]*)+-\d{3}$` matched; digit-only `006` end anchor, no alpha suffix.)

## Run-phase

_(empty — manager-develop fills §E.2 run-phase evidence: per-AC PASS matrix, D3 decision confirmation, any §2.2 Class-A markup-parity justifications, HTMX-depth confirmation.)_
