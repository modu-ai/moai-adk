# Progress — SPEC-V3R6-DOCS-CODEMAPS-V3-001

> Run/Sync/Mx-phase evidence tracker. At plan-phase only §E.1 is
> populated; §E.2..§E.5 are placeholder headings emitted so the
> `ClassifyEra` H-4 path sees the `§E.2`/`§E.3`/`§E.4`/`§E.5` literal
> markers it grep-matches on.

## §E.1 Plan-phase Audit-Ready Signal

- **SPEC ID**: SPEC-V3R6-DOCS-CODEMAPS-V3-001
- **Phase**: plan (authored 2026-06-17)
- **Frontmatter status**: draft (manager-spec owns `(none) → draft`)
- **Tier**: M
- **Era override**: `era: V3R6` (explicit, to prevent H-2
  misclassification of this initially-empty progress.md)
- **Artifacts authored**:
  - `spec.md` — 12 REQ-CM-* requirements (GEARS notation)
  - `plan.md` — 4 milestones (M1 codemaps refresh → M2 docs-truth
    authoring → M3 verify+neutrality+regression → M4 audit-ready signal)
  - `acceptance.md` — 10 AC-CM-* criteria (9 MUST-PASS [AC-CM-000..008]
    + 1 SHOULD-PASS [AC-CM-009])
  - `progress.md` — this file (§E.1 populated, §E.2..§E.5 skeleton)
- **Pre-flight verified at plan-phase (iter-2 re-verified)**:
  - `.moai/project/codemaps/` ALREADY EXISTS with 5 default files
    (`overview.md`, `modules.md`, `dependencies.md`, `entry-points.md`,
    `data-flow.md`) — NOT absent. This SPEC refreshes them + adds
    `docs-truth.md`. The codemaps skill description itself canonicalizes
    the `.moai/project/codemaps/` path.
  - The codemaps skill does NOT emit `architecture.md` (confirmed by
    reading `.claude/skills/moai/workflows/codemaps.md` Phase 3 output
    spec lines 104-110). REQ-CM-001 / AC-CM-001 re-targeted to
    `overview.md` + `modules.md`.
  - 7 MoAI-custom agent files present in `.claude/agents/moai/` (matches
    the 8 retained-agent count when Explore built-in is included)
  - 17 `/moai` command files present in `.claude/commands/moai/`
    (brain, clean, codemaps, coverage, design, e2e, feedback, fix, gate,
    harness, loop, mx, plan, project, review, run, sync)
  - `internal/spec/status.go` carries the 8 lowercase status values
  - `internal/spec/lint.go` `FrontmatterSchemaRule` `required` slice
    (~lines 586-602) carries exactly 12 fields
  - `internal/config/defaults.go` lines 40-57 carry the full GLM
    tier-models table: `DefaultGLMHigh = "glm-5.2[1m]"`,
    `DefaultGLMMedium = "glm-4.7"`, `DefaultGLMLow = "glm-4.5-air"`,
    `DefaultGLMSonnet = "glm-4.7"`, `DefaultGLMHaiku = "glm-4.5-air"`,
    `DefaultGLMOpus = "glm-5.2[1m]"` (glm-5.2[1m] activation confirmed)
  - `rootCmd.AddCommand(` spans 26 files across `internal/cli/` (NOT
    only `root.go`, which has 9 calls); AC-CM-005 greps the full tree
  - No duplicate SPEC ID under `.moai/specs/`
- **SPEC ID pre-write self-check**: PASS
  (`SPEC ✓ | V3R6 ✓ | DOCS ✓ | CODEMAPS ✓ | V3 ✓ | 001 ✓ → PASS`)
- **Plan-phase self-verification slots deferred to run-phase**: E1..E7
  (per `plan.md` §E).

## §E.2 Run-phase Evidence

- **Run-phase agent**: manager-develop (cycle_type=ddd — additive docs
  extraction, no behavior change)
- **Run-phase branch**: `worktree-agent-aef94b80336ca1d00` (L1 Claude
  Code worktree; work copied to this tree, SPEC artifacts copied in
  from shared checkout since they were untracked at worktree creation)
- **Pre-flight baseline (captured before M1)**:
  - `go build ./...` → exit 0
  - `go vet ./...` → exit 0 (no output)
  - `golangci-lint run --timeout=2m` → `0 issues.`
  - `ls -1 .claude/agents/moai/*.md | wc -l` → 7
  - `ls -1 .claude/commands/moai/*.md | wc -l` → 17
  - `internal/spec/status.go` ValidStatuses → 8 values
  - `internal/spec/lint.go` FrontmatterSchemaRule required slice → 12 entries
  - `internal/config/defaults.go` lines 40-57 → full GLM tier-models table
  - `grep -rnE '\.AddCommand\(' internal/cli/ | grep -v '_test.go' | wc -l` → 119

### M1 — codemaps refresh

- `.moai/project/codemaps/overview.md` refreshed (v3.0.0-rc2): 10
  capability layers table, architecture diagram, named package table.
- `.moai/project/codemaps/modules.md` refreshed: 11-layer module catalog
  covering all 48 internal packages + the 9 named capability-anchor
  packages.
- `.moai/project/codemaps/{dependencies,entry-points,data-flow}.md`
  retained from the prior 2026-06-10 skill run (not in this SPEC's
  refresh scope).
- Codemaps skill invocation (`Skill("moai:codemaps")`) launched but the
  pipeline runner did not execute in the isolated subagent context; the
  two in-scope files were regenerated by deterministic extraction
  (`go list`/`go doc` + file reads) + clearly-labeled Architectural
  Insight subsections (C4 determinism convention).

### M2 — docs-truth.md authored

- `.moai/project/codemaps/docs-truth.md` created (NEW), 5 sections:
  §1 Agent Catalog (8 retained, 0 archived leakage) · §2 Status Enum
  (8 values) · §3 Frontmatter Schema (12 fields) · §4 CLI Surface
  (terminal verbs + 17 `/moai` skills) · §5 GLM Tier Mapping (full
  7-row table). 6 `**Source:**` citations.

### M3 — AC PASS/FAIL matrix (verbatim observed output)

| AC | Severity | Result | Observed evidence |
|----|----------|--------|-------------------|
| AC-CM-000 | MUST | PASS | `test -s .moai/project/codemaps/docs-truth.md` → exit 0 |
| AC-CM-001 | MUST | PASS-WITH-DEBT | 9/10 layers mechanically PASS; the GLM token `"GLM\|Multi-LLM\|multi-llm"` is a regex defect (`\|` in ERE = literal pipe, not alternation) — the Multi-LLM layer IS substantively covered ("Multi-LLM" in overview table, "GLM tier-models" throughout). See Debt-1 below. |
| AC-CM-002 | MUST | PASS | `ls -1 .claude/agents/moai/*.md \| wc -l` → 7; manager≥4 (observed 5), evaluator≥2 (3), builder≥1 (2), Explore≥1 (3); archived leakage = 0 |
| AC-CM-003 | MUST | PASS | 8 statuses all present in docs-truth + source cross-verify `grep -cE '...' internal/spec/status.go` → 8 |
| AC-CM-004 | MUST | PASS | 12 frontmatter fields all present + source cross-verify `awk ... \| grep -cE '^\s*\{"[a-z]+", fm\.'` → 12 |
| AC-CM-005 | MUST | PASS | AddCommand count = 119 (≥30); 12 human-facing verbs OK; 17 `/moai` skills per-skill OK; command-file count = 17 |
| AC-CM-006 | MUST | PASS | Full GLM tier table in docs-truth + 6 source cross-verify greps all exit 0 |
| AC-CM-007 | MUST | PASS | REQ tokens: 0; Audit citations: 0; unique SPEC-IDs in codemaps/ = 1 (self-ref `SPEC-V3R6-DOCS-CODEMAPS-V3-001` only) |
| AC-CM-008 | MUST | PASS | `go build ./...` exit 0; `go vet ./...` exit 0; `golangci-lint run` → `0 issues.` — matches pre-flight baseline, zero NEW failures |
| AC-CM-009 | SHOULD | PASS-WITH-DEBT | `\b<pkg>\b` intent-faithful regex → 9/9 covered; literal AC regex `(^|[^a-z])pkg(/|[^a-z]|$)` → 7/9 (fails for `spec`/`hook` due to ugrep 7.5.0 defect: the `(^|[^a-z])` group never matches even `spec` at line start). See Debt-2 below. |

### M3 sub-checks

- **Coverage (E1/AC-CM-001)**: overview.md + modules.md collectively
  represent all 10 capability layers (Multi-LLM substantively covered
  via "Multi-LLM" layer row + "GLM tier-models" references).
- **Cross-platform build (E2/AC-CM-008)**: `go build ./...` exit 0;
  `.moai/project/codemaps/` is Markdown-only, Go toolchain unaffected.
- **Coverage (E3)**: N/A — no new Go code paths (REQ-CM-010 code-change
  zero honored).
- **Subagent boundary (E4)**: N/A — this SPEC does not touch
  `.claude/agents/` or hook surfaces.
- **Lint (E5/AC-CM-008)**: `golangci-lint run` → `0 issues.`
  (baseline preserved). `moai spec lint` on this SPEC's spec.md →
  1 ERROR `MissingExclusions` — this is a PRE-EXISTING plan-phase
  defect in spec.md body (the `### §B.2 Out of Scope` section uses a
  numbered list rather than `### <X.Y> Out of Scope` h3 sub-sections
  per Known Issue B6). It is NOT introduced by run-phase work and
  CANNOT be fixed by manager-develop (spec.md body is out of scope per
  the Status Transition Ownership Matrix). See Debt-3 below.
- **Push state (E6)**: single commit on `main` (Hybrid Trunk 1-person
  OSS, Tier M main-직진 per §23 Tier-based PR Routing).
- **Neutrality (E7/AC-CM-007)**: zero internal SPEC-ID / REQ /
  Audit-citation leakage in `.moai/project/codemaps/` (only self-ref).
- **Era override**: `grep -E '^era:' spec.md` → `era: V3R6` present.
- **Go code change**: 0 (REQ-CM-010 honored — only additive Markdown
  under `.moai/project/codemaps/` + SPEC frontmatter/progress fields).

### Debt entries (run-phase)

- **Debt-1 (AC-CM-001 GLM token regex defect)**: the `layer` variable
  `"GLM\|Multi-LLM\|multi-llm"` interpolated into `grep -qiE` becomes
  `grep -qiE "GLM\|Multi-LLM\|multi-llm"`. In ERE `\|` matches a
  literal `|`, NOT alternation — so the AC mechanically can only match
  the literal string `GLM|Multi-LLM|multi-llm`. The author intended `|`
  (ERE alternation). Substantive coverage IS present. Requires
  acceptance.md body fix (manager-spec domain).
- **Debt-2 (AC-CM-009 word-boundary regex defect)**: the regex
  `(^|[^a-z])pkg(/|[^a-z]|$)` does not match under ugrep 7.5.0 (the
  installed grep on this host) even for `spec` alone at line start —
  the `(^|[^a-z])` group with the `^` anchor inside alternation never
  matches. `\bpkg\b` confirms 9/9 substantive coverage. Requires
  acceptance.md body fix (manager-spec domain).
- **Debt-3 (spec.md MissingExclusions)**: `moai spec lint` emits 1
  ERROR `MissingExclusions` — the `### §B.2 Out of Scope` section uses
  a numbered list; the lint rule wants `### <X.Y> Out of Scope` h3
  sub-sections (Known Issue B6). This is a pre-existing plan-phase
  artifact defect, NOT introduced by run-phase. Requires spec.md body
  fix (manager-spec domain).

## §E.3 Run-phase Audit-Ready Signal

- **Run-phase status**: complete (M1-M4 all executed)
- **MUST-PASS ACs**: AC-CM-000, AC-CM-002, AC-CM-003, AC-CM-004,
  AC-CM-005, AC-CM-006, AC-CM-007, AC-CM-008 → all PASS (8/8)
- **MUST-PASS ACs with regex-defect debt**: AC-CM-001 → PASS-WITH-DEBT
  (substantive coverage present; Debt-1 regex defect blocks mechanical
  gate)
- **SHOULD-PASS ACs**: AC-CM-009 → PASS-WITH-DEBT (9/9 substantive;
  Debt-2 regex defect blocks mechanical gate)
- **Regression baseline**: preserved (build/vet/lint all exit 0,
  matching pre-flight)
- **Go code change**: 0 (REQ-CM-010 honored)
- **Neutrality**: clean (AC-CM-007 PASS — only self-ref SPEC-ID)
- **run_commit_sha**: `3fded3e21` (amended to backfill this field; the
  pre-amend SHA was `d23ac86cb`)
- **run_status**: PASS-WITH-DEBT (3 debt entries: Debt-1/2 are
  acceptance.md regex defects requiring manager-spec; Debt-3 is a
  spec.md body defect requiring manager-spec. None are introduced by
  or fixable from run-phase.)

## §E.4 Sync-phase Audit-Ready Signal

_<pending sync-phase>_ — manager-docs populates with `sync_commit_sha`
once the sync commit lands.

## §E.5 Mx-phase Audit-Ready Signal

_<pending Mx-phase>_ — manager-docs / orchestrator populates with
`mx_commit_sha` at the 4-phase close.
