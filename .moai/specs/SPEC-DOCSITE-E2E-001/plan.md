# SPEC-DOCSITE-E2E-001 — Implementation Plan

> Tier M (3 artifacts + progress.md). Milestones ordered by decision-reversibility: user-facing content decisions first (most likely to change under review), mechanical count sweeps and verification last.

## §A Context

- **Provenance**: deferred follow-up of SPEC-E2E-REVIVAL-001 (completed, sync push `4cef092b7`). Deferral clause: its spec.md §E "Out of Scope — docs-site 4-locale documentation and count-literal corrections".
- **Write scope**: `docs-site/**` + this SPEC directory ONLY. Zero Go, zero template tree, zero doctrine tree.
- **Content source of truth (run-phase re-read)**: `.claude/skills/moai/workflows/e2e.md` (workflow phases: Detection → Selection → Journey Mapping → Script Creation → Execution → Recording → Report), `.claude/agents/moai/e2e-specialist.md`, `.claude/commands/moai/e2e.md`. Do NOT author page content from this SPEC's summary prose — re-read the live artifacts.
- **Structure decisions already measured** (spec.md §A.2): section = `utility-commands`; path = `content/{locale}/utility-commands/moai-e2e.md`; menu = sub-entry in existing utility-commands section (no icon, no menu.html change); `_meta.yaml` entry × 4; frontmatter = `title: /moai e2e` + shared weight + `draft: false`; canonical authoring locale = **ko** (chain ko → en → ja/zh per `.moai/docs/docs-site-i18n-rules.md` §17.3).
- **Plan-phase measured baselines** (spec.md §A.1): Ring A 10 files/12 matches; Ring A′ 14 matches (≥1 false positive: `agent-view.md` parallelism prose); Ring B ko 19 / ja 19 / zh 21; Ring B′ (widened D1 any-gap pattern) ko 9 / ja 9 / zh 9. ALL baselines re-measured at run-phase pre-flight (REQ-DSE-104) — the surface moves.

## §B Known Issues / Risks

- **B1 — Shared-checkout parallel-session races** (highest operational risk): docs-site was actively edited by a parallel session on 2026-07-13 (`e7e8115a2`, `1fe9d5a2b`). Defense: run-phase entry precondition (§C P1), pathspec commits only (REQ-DSE-202), per-milestone commits, re-measure before edit (REQ-DSE-104).
- **B2 — Locale grep false positives/negatives**: digit-boundary `(^|[^0-9])` prefix is mandatory (a bare `10` matches `110`); the 10-family patterns anchor to agent nouns within 8 chars, but each match MUST be individually classified catalog-count vs unrelated before editing (confirmed false positive at plan: `content/en/claude-code/agentic/agent-view.md` "Running 10 agents in parallel"). Ring B′ 9-family is looser — classify each of its 27 matches (widened pattern, ko 9 / ja 9 / zh 9). The `[^0-9]{0,8}` any-gap in the 9-family pattern is REQUIRED: ja/zh write spaced forms ("9 個の MoAI", "9 个 MoAI 自定义") that a counter-only regex misses — audit iter-1 D1 BLOCKING escapees were exactly these (ja/zh advanced/agent-guide.md:111, ja core-concepts/what-is-moai-adk.md).
- **B3 — Stale generator header**: `data/menu/main.yaml` claims "Auto-generated by scripts/gen_menu.py" but `gen_menu.py` does NOT exist (verified 2026-07-13). Edit main.yaml MANUALLY; do not hunt for or attempt to run the generator.
- **B4 — `docs-i18n-check.sh` behavior unverified**: the script EXISTS (8,066 bytes) but its checks were not exercised at plan-phase. Treat as auxiliary (REQ-DSE-204 capability gate); the binding gates are `hugo --minify` zero-warnings + explicit greps. If the script fails on pre-existing tree state unrelated to this SPEC, record the failure as baseline and do not chase it (scope discipline).
- **B5 — Hugo warning baseline**: run `hugo --minify` ONCE at pre-flight to capture the pre-change warning baseline; the REQ-DSE-200 gate is "no NEW warnings and exit 0" against that baseline (expected baseline: zero warnings; if pre-existing warnings exist, pin them in progress.md §E.2).
- **B6 — Weight collision**: sibling weights in utility-commands were not read at plan-phase. Measure all sibling `weight:` values before choosing the new page's weight (identical across 4 locales, ordered last or after `moai-feedback`).
- **B7 — Enumeration-surface drift**: files like `advanced/agent-guide.md` are full catalog guides — the 11th agent needs a REAL entry (role, phase scope, model/effort if the page carries those columns), not a name dropped into a count sentence. Re-read each enumeration surface's local structure before inserting.
- **B8 — Template-tree reflex**: docs-site is NOT under `internal/template/` — do NOT run `make build`, do NOT mirror to templates. (Inverse of the usual Template-First rule; it does not apply to docs-site.)

## §C Pre-flight (run-phase entry preconditions)

- **P1 [HARD] Working-tree cleanliness (REQ-DSE-203)**: `git status --porcelain docs-site/` MUST be empty, or every residual line explicitly resolved by user decision, BEFORE the first edit. User decision 2026-07-13: "plan only now, clean before run". Plan-time residual (v0.1.1 re-measure): 2 files (`docs-site/content/{ja,zh}/advanced/settings-json.md`). If new residue appears (parallel sessions), STOP and return a blocker report.
- **P2 Pre-spawn sync check**: `git fetch origin main` + `git rev-list --count --left-right origin/main...HEAD` + `moai session list --json --filter-spec=SPEC-DOCSITE-E2E-001` per agent-common-protocol.md § Pre-Spawn Sync Check.
- **P3 Baseline re-measurement (REQ-DSE-104)**: run CMD-PRE-1..4 (acceptance.md § Executable Command Block); record the reconciled inventory (files + counts + per-match catalog-count/false-positive classification) in progress.md §E.2 BEFORE editing.
- **P4 Hugo baseline**: `cd docs-site && hugo --minify` — capture warning/exit baseline (B5).
- **P5 Content-source read**: Read `.claude/skills/moai/workflows/e2e.md`, `.claude/agents/moai/e2e-specialist.md`, `.claude/commands/moai/e2e.md` in full.
- **P6 Sibling weight scan**: `grep -H "^weight:" docs-site/content/en/utility-commands/*.md` (B6).

## §D Constraints (DO NOT VIOLATE)

- PRESERVE (read-only): `README*`, `CLAUDE.md`, `.claude/**`, `internal/**`, `docs-site/static/moai-brand.css` (FROZEN), `layouts/partials/menu.html` (unless REQ-DSE-007 gate fires — not expected), all docs-site files outside the measured inventory + the 4 new pages + `_meta.yaml` ×4 + `data/menu/main.yaml` + `layouts/index.html`.
- Forbidden commands: `git add -A`, `git add .`, `--no-verify`, force-push, `make build` (B8).
- Required: Conventional Commits (`feat(SPEC-DOCSITE-E2E-001): M{N} …` / `docs(…)`) + `🗿 MoAI` trailer; pathspec commits; 4-locale same-commit-set landing (REQ-DSE-003).
- Content hygiene: no body emoji (icon-shortcode rule), Mermaid TD only, emphasis-marker spacing, URL blacklist, no internal SPEC/REQ tokens or SHAs in page content (REQ-DSE-008).

## §E Self-Verification (run-phase completion deliverables)

Per verification-claim-integrity.md §3 (Claim / Evidence / Baseline-attribution / Gaps / Residual-risk):

- **E1**: AC-DSE-001..020 binary PASS/FAIL matrix with verbatim command outputs (CMD block IDs cited).
- **E2**: `hugo --minify` exit code + warning delta vs P4 baseline + `public/sitemap.xml` existence.
- **E3**: Ring invariance grep outputs (CMD-A, CMD-B family) — expect 0 outside documented false positives; false-positive list enumerated verbatim.
- **E4**: Reachability outputs (CMD-C e2e-specialist enumeration per file; CMD-E menu; CMD-G built-site nav).
- **E5**: 4-locale parity outputs (CMD-D existence + meta; H2 section-count parity per locale).
- **E6**: Commit SHA list + push state + confirmation that every commit was pathspec-scoped (`git show --stat` per commit).
- **E7**: `moai spec lint .moai/specs/SPEC-DOCSITE-E2E-001/spec.md` → 0 errors (a `StatusGitConsistency` WARNING is structural until sync close — expected, do not chase; do NOT add `lint.skip`).

## §F Milestones (decision-reversibility order)

### M1 — `/moai e2e` page content, ko canonical (HIGHEST review-change likelihood)
- Author `content/ko/utility-commands/moai-e2e.md` from the live workflow/agent artifacts (P5): one-line summary callout (sibling pattern), platform-toolchain matrix, auto-detection, journey/script/execution flow, token-minimization explanation, recording option, graceful/deferral branch, related-page links.
- Frontmatter per sibling convention; weight from P6.
- Priority: High.

### M2 — en/ja/zh derivation + 4-locale landing set
- Derive en from ko, then ja/zh (chain §17.3). H2 structure parity; Mermaid node labels translated (syntax/direction unchanged); anchors point at translated heading slugs; glossary terms (MoAI-ADK 등) untranslated.
- Priority: High.

### M3 — Navigation & meta registration
- `data/menu/main.yaml`: sub-entry (4-locale name map + ref) appended to the utility-commands section — manual edit (B3).
- `utility-commands/_meta.yaml` × 4 locales: `"moai-e2e": title: "/moai e2e"`.
- No menu.html change (REQ-DSE-007 gate not fired).
- Priority: High.

### M4 — Count-literal normalization sweep (mechanical)
- Apply the P3 reconciled inventory: Ring A (12 English ERE matches → 11/10 forms), Ring A′ (catalog-count matches only; extend `22 → 17 → 8 → 10` → `… → 11`; skip documented false positives), Ring B/B′ (locale-language forms per-locale), `layouts/index.html` stat row → 11.
- Enumeration reachability (REQ-DSE-103): insert a real `e2e-specialist` entry into each catalog-enumeration surface (B7) — table row / role prose / list item as the surface's local structure dictates, all 4 locales where the surface exists per-locale.
- Priority: Medium (mechanical; large file count but single transform intent).

### M5 — Verification & close
- Run the full CMD block (acceptance.md): invariance greps, reachability greps, parity checks, hugo build, sitemap, auxiliary docs-i18n-check.sh (REQ-DSE-204).
- E1-E7 self-verification report; pathspec commits + push.
- Priority: Medium.

## §G Anti-Patterns

- Bumping a count literal without naming `e2e-specialist` in the adjacent enumeration (token presence ≠ reachability — REQ-DSE-103).
- Blind `sed` across locales (per-file judgment mandated; ja/zh spacing conventions differ — e.g. ja `10 個の` with spaces).
- Authoring en first (ko is canonical — §17.3).
- Landing ko-only / partial locales (same-commit-set obligation).
- `git add -A` in the shared checkout.
- Editing `moai-brand.css`, menu.html, or any PRESERVE-listed file.
- Treating the plan-phase baselines as current at run-phase (mandatory re-measure, REQ-DSE-104).
- Chasing pre-existing docs-i18n-check.sh failures unrelated to this SPEC's surface (B4).

## §H Cross-References & Execution Vehicle

- **Execution-vehicle option (orchestrator decides at run)**: the user-owned oss-docs harness is an available run-phase vehicle — `hns-oss-docs-run` Runner with specialists hns-oss-docs content-author (M1/M2), locale-translator (M2), structure-curator (M3), and the `hns-oss-docs-verify` exit-gate recipe (M5). The skills `hns-oss-docs-i18n-rules` / `hns-oss-docs-structure-map` / `hns-oss-docs-readme-sync` carry the same doctrine this plan cites. Alternative: single `manager-develop` (cycle_type=ddd, docs-mode) delegation. Note: `hns-oss-docs-verify`'s claim that docs-i18n-check.sh "does not exist" is STALE — the script exists in this tree (spec.md §A.2); the recipe's inlined checks remain valid either way. Publishing (commit/push) stays orchestrator/human-gated per the Runner contract.
- SPEC-E2E-REVIVAL-001 spec.md §E (deferral provenance) + §B REQ-E2E-302 ring pattern (methodology precedent).
- `.moai/docs/docs-site-i18n-rules.md` (§17.1 URL blacklist, §17.2 markdown rules, §17.3 4-locale sync, §17.6 build checklist, Vercel binding — project ID immutable).
- CLAUDE.local.md §17.1 (icon shortcodes, light-theme-only, code-card/Mermaid rendering) + §20 (Vercel Elastic).
- Lessons applied: digit-boundary 4-parity locale grep; AC reachability not token presence; vacuous table-cell command prohibition (CMD block in acceptance.md); shared-checkout concurrent-commit race defense; worktree-scratch vs committed diagnostics.
