# Plan — SPEC-HUMANIZE-001

> **Run-phase nature**: this is a **documentation-authoring** SPEC, not a Go-code SPEC. The run phase RE-AUTHORS `modules/korean.md` in full (prose + copy, as an original work — REQ-HUM-001 v0.3.0), appends copy layers to the other 3 modules, edits SKILL.md (shared sections + attribution cleanup — NO NOTICE.md is created; the 5 dangling pointers are removed per REQ-HUM-015), edits one `catalog.yaml` entry, runs `make build`, and synchronizes the local `.claude/` copy. There is NO Go source change and NO test-first (RED/GREEN) cycle in the code sense; the DDD/TDD "test" surface is the acceptance-criteria grep + 8-sample verification in `acceptance.md`.

## §A Context

- Target skill: `internal/template/templates/.claude/skills/moai-domain-humanize/` (SKILL.md 155 lines + 4 modules), byte-identical to `.claude/skills/moai-domain-humanize/` at plan time.
- Catalog entry: `internal/template/catalog.yaml` line ~159, `moai-domain-humanize` under `optional-pack:design`, `version: 1.0.0`, `hash: 6a6ff534...`.
- Reference source (read-only; the **maintainer's own taxonomy** — grep-verified zero im-not-ai lineage, so re-authoring from it is unencumbered): the external `general-humanize-korean` skill — `references/ai-tell-taxonomy.md` (copy layer at A-20…A-25, L-1…L-8, M-1…M-3) and `SKILL.md` (4 철칙 with copy-mode variants + Phase 5 dual grading tables).
- Evidence base: `research.md` (this SPEC dir) consolidates three scratchpad research files (EN/JA/ZH) + the Korean port source.

## §B Known Issues / Watch-outs

- **B1 — Non-transfer discipline (highest risk).** The three modules (EN/JA/ZH) must NOT mirror-translate the Korean copy layer. The run-phase author MUST cross-read `research.md` §Non-Transfer Findings before authoring each module. Verification: AC-HUM-007a/b/c.
- **B2 — §25 neutrality leak.** The source taxonomy prose is dense with internal version annotations (`v2.2 신규`, `v2.6 확장`), `_source_anchor:` lines citing internal specs, and `.moai/reports/research-*` paths. These MUST be stripped from the ported Korean content. Verification: AC-HUM-008 grep.
- **B3 — Instruction-prose language.** The Korean source's instruction prose is Korean; REQ-HUM-010 requires English instruction prose. The Korean module's copy layer is an English **re-authoring** of the instruction prose, keeping only the before/after examples in Korean. Do NOT paste the Korean prose verbatim.
- **B4 — Byte-identity ordering.** Edit `templates/` first, `make build`, then sync the local copy. A reversed order (local first) is a Template-First violation. Verification: AC-HUM-007 `diff -rq`.
- **B5 — Hash recompute mechanism.** The catalog hash is regenerated by `make build` (`gen-catalog-hashes --all`), NOT hand-computed. Bump `version` to `1.1.0` in catalog.yaml AND SKILL.md `metadata.version` BEFORE `make build` so the recomputed hash covers the version change.
- **B6 — Attribution cleanup, NO NOTICE.md (rewritten v0.3.0 per user decision).** The v1.0.0 skill carries 5 dangling `See NOTICE.md` pointers + an inaccurate "faithful port of im-not-ai (MIT)" self-description; the actual content lineage is the maintainer's own taxonomy (zero im-not-ai references — grep-verified). The run phase MUST NOT create NOTICE.md. Instead: remove all 5 `See NOTICE.md` pointers and rewrite each attribution block into the courtesy credit "category-catalogue structure inspired by the im-not-ai (Humanize KR) project" — no license claim, no copyright line, no MIT token. `license: Apache-2.0` stays as-is. Verification: AC-HUM-016 (0 NOTICE.md refs), AC-HUM-017 (0 "MIT License" tokens + license unchanged).

## §C Pre-flight Checks (run-phase entry)

1. `git fetch origin main && git rev-list --count --left-right origin/main...HEAD` → confirm no parallel-session race before editing shared files.
2. `diff -rq internal/template/templates/.claude/skills/moai-domain-humanize/ .claude/skills/moai-domain-humanize/` → expect IDENTICAL (baseline).
3. Read `research.md` §Non-Transfer Findings + the three per-language catalogue tables.
4. Confirm `make build` runs clean on the current tree (baseline green) before any edit.

## §D Constraints

- English instruction prose; target-language before/after examples only (REQ-HUM-010).
- No internal SPEC IDs / REQ tokens / dates / SHAs / internal version annotations / `.moai/reports/` paths in skill content (REQ-HUM-009).
- SKILL.md frontmatter (per re-amended REQ-HUM-011, v0.3.0): PRESERVE `user-invocable: false`, `allowed-tools`, AND `license: Apache-2.0` (unchanged — no compound expression); DELIBERATELY CHANGE `metadata.version` (→ `"1.1.0"`) and `updated` (run-phase date). REWRITE the trailing attribution line + the 4 modules' "Source & License" blocks into the courtesy credit; remove all 5 `See NOTICE.md` pointers (REQ-HUM-015).
- Additive for EN/JA/ZH modules — do NOT rewrite their existing prose categories (§F Out of Scope). EXCEPTION: `korean.md` is fully RE-AUTHORED (prose A–J + copy layer) per REQ-HUM-001 v0.3.0.
- No Go code (§F Out of Scope).

## §E Self-Verification (plan-phase)

See `progress.md` §E.1 for the plan-phase audit-ready signal. Run-phase evidence (§E.2), run-phase audit signal (§E.3), and sync-phase audit signal (§E.4) are populated by the run/sync phases respectively — this agent emits placeholder headings only.

## §F Milestones (priority-ordered, no time estimates)

### M1 — Korean module full re-authoring (original work) [Priority: High]
- Owner: manager-develop (run-phase).
- RE-AUTHOR `modules/korean.md` in full as an original work from the maintainer's own taxonomy: prose layer A–J AND copy layer A-20…A-25 (copy translationese), L-1…L-8 (storytelling), M-1…M-3 (slides). Pattern IDs/severities may align with the maintainer's taxonomy freely (it is the maintainer's IP).
- Instruction prose in English; before/after examples in Korean; internal annotations stripped (§25); NO "faithful port" / "ported from" claim anywhere in the module.
- Satisfies: REQ-HUM-001, REQ-HUM-009, REQ-HUM-010. Verified by AC-HUM-001 (incl. no-port-claim negative), AC-HUM-008.

### M2 — English copy layer (ENC-1…ENC-9) [Priority: High]
- Owner: manager-develop (run-phase).
- Append an "English Copy Layer" section to `modules/english.md` with the `ENC-` prefix, authored from `research.md` §English.
- Encode the M-2 non-transfer: NO standalone fragment-headline removable category; em-dash stays a modifier.
- Satisfies: REQ-HUM-002, REQ-HUM-007(a), REQ-HUM-012. Verified by AC-HUM-002, AC-HUM-006b, AC-HUM-011.

### M3 — Japanese copy layer (JA-10…JA-14) [Priority: High]
- Owner: manager-develop (run-phase).
- Append a "Japanese Copy Layer" section to `modules/japanese.md`.
- JA-10 (体言止め over-reliance) is frequency-gated S2, NOT presence S1; particle-ending tell NOT ported; 体言止め boundary analysis included.
- Satisfies: REQ-HUM-003, REQ-HUM-007(b), REQ-HUM-012. Verified by AC-HUM-003, AC-HUM-006a, AC-HUM-011.

### M4 — Chinese copy layer (CN-L…CN-Q) [Priority: High]
- Owner: manager-develop (run-phase).
- Append a "Chinese Copy Layer" section to `modules/chinese.md`.
- 对偶/排比 boundary = content-first vs template-first (not count); dash-contrast scoped to binary-contrast headline shape only; 破折号 —— explanation use is a false-positive guard.
- Satisfies: REQ-HUM-004, REQ-HUM-007(c), REQ-HUM-012. Verified by AC-HUM-004, AC-HUM-006c, AC-HUM-011.

### M5 — SKILL.md shared-section + attribution cleanup (courtesy credit, no NOTICE.md) [Priority: Medium]
- Owner: manager-develop (run-phase). (The `korean.md` re-authoring itself is owned by M1; M5 owns the cross-file attribution/license surface.)
- Promote the copy-mode fact-anchor guard (REQ-HUM-005) and the dual prose/copy grading tables (REQ-HUM-006) to the shared section.
- Update the language-routing table "Source basis" column (REQ-HUM-013).
- Add the retired-Python conservative-judgment instruction near the change-rate thresholds (REQ-HUM-014).
- Attribution cleanup (REQ-HUM-015): remove all 5 `See NOTICE.md` pointers; rewrite the SKILL.md trailing line + the 4 modules' "Source & License" blocks into the courtesy credit ("category-catalogue structure inspired by the im-not-ai (Humanize KR) project") — no license claim, no copyright line, no NOTICE.md pointer. Do NOT create NOTICE.md.
- License unchanged (REQ-HUM-016): `license: Apache-2.0` stays as-is; confirm zero "MIT License" tokens remain in the skill dir.
- Apply the re-amended frontmatter scope (REQ-HUM-011): preserve `user-invocable`/`allowed-tools`/`license`; deliberately change `metadata.version` → `"1.1.0"`, `updated`.
- Satisfies: REQ-HUM-005, REQ-HUM-006, REQ-HUM-011, REQ-HUM-013, REQ-HUM-014, REQ-HUM-015, REQ-HUM-016. Verified by AC-HUM-005, AC-HUM-010, AC-HUM-013(prose/copy grading), AC-HUM-015, AC-HUM-016, AC-HUM-017.

### M6 — catalog metadata + build + sync [Priority: Medium]
- Owner: manager-develop (run-phase).
- Bump `catalog.yaml` `moai-domain-humanize` `version: 1.0.0 → 1.1.0`.
- `make build` (regenerates hash + embeds templates), then sync local `.claude/` copy; confirm byte-identity.
- Satisfies: REQ-HUM-008, REQ-HUM-013. Verified by AC-HUM-007, AC-HUM-012.

### M7 — 8-sample verification + false-positive guards [Priority: Medium]
- Owner: manager-develop (run-phase).
- Run the 8-sample matrix (4 languages × {prose, copy}) through detect→rewrite→grade; confirm each yields a grade + no meaning drift.
- Run the three false-positive guards (EN slide fragment, JA one 体言止め, ZH one 排比) — each must NOT be flagged.
- Verified by AC-HUM-013, AC-HUM-014.

## §G Anti-Patterns (do NOT do)

- Mirror-translating the Korean copy layer into EN/JA/ZH (REQ-HUM-007 violation — systematic false positives).
- Porting Korean M-2 fragment-presence rule into English or Japanese.
- Making JA-10 a presence-based S1.
- Making Chinese 排比 a count-based rule.
- Editing the local `.claude/` copy before `templates/` (Template-First violation).
- Leaving internal version annotations / `_source_anchor:` internal-spec citations / `.moai/reports/` paths in ported content.
- Rewriting the EN/JA/ZH prose categories (the Korean module IS fully re-authored per REQ-HUM-001 v0.3.0 — that is required, not an anti-pattern).
- Creating NOTICE.md, leaving ANY `NOTICE.md` reference or `MIT License` claim in the skill dir, or re-introducing a "faithful port of im-not-ai" claim (v0.3.0: the MIT dependency is dissolved; the courtesy credit carries no license token — AC-HUM-016/017 grep for 0 matches).
- Changing the `license:` field (it stays `Apache-2.0` per REQ-HUM-016 v0.3.0).
- Adding any Go code.

## §H Cross-References

- `spec.md` §C (three non-transfer constraints), §D (REQ set), §F (Out of Scope).
- `research.md` (evidence, sources, quarantined hypotheses).
- `design.md` (structure + ID prefix + guard promotion decisions).
- `internal/template/CLAUDE.md` (Template-First, byte-embed, mirror parity).
- `CLAUDE.local.md` §2/§15/§25.
