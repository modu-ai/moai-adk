# ac_baseline_guard fixtures

Acceptance fixtures for ac_baseline_commit_guard_test.go. No counter carrier lives
here: tests copy the tracked .claude/agents/moai/manager-docs.md into each temp
repo at runtime, so there is no second copy of the counter.
