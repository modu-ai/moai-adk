# SPEC-ASTF-001 — Acceptance Criteria

This artifact opens with a heading, not a `---` fence, so it has no frontmatter
block at all. The line below therefore cannot be a frontmatter field however it
is spelled — the block opens only at line 1:

status: draft

A second shape guarded here: a `---` horizontal rule further down the body does
not open a block retroactively.

---

status: implemented
