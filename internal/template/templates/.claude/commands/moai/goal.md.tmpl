---
description: {{if eq .ConversationLanguage "ko"}}완료 조건을 등록하고 자율 진행 루프를 arm{{else if eq .ConversationLanguage "ja"}}完了条件を登録して自律継続ループをarm{{else if eq .ConversationLanguage "zh"}}注册完成条件并启动自主继续循环{{else}}Register a completion condition and arm the autonomous continuation loop{{end}}
argument-hint: "{\"<condition>\"|status [--all]|clear}"
allowed-tools: Skill
---

Use Skill("moai") with arguments: goal $ARGUMENTS
