# State Configuration
# State directory path is the hardcoded literal ".moai/state/" (de-facto SSOT —
# internal/cli/state.go, internal/worktree/state_guard.go); the state_dir YAML
# key had no runtime consumer and was removed (SPEC-CONFIG-DEAD-SWEEP-001).

state: {}
