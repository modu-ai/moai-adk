# moai-adkコマンド完全リファレンス

`moai-adk`コマンドラインツールのすべてのコマンドとオプションをカバーします。

## コマンド構造

```
moai-adk <command> [options] [arguments]
```

## グローバルオプション

すべての`moai-adk`コマンドで使用可能:

| オプション           | 説明           |
| -------------------- | -------------- |
| `--help`, `-h`       | ヘルプ表示     |
| `--version`, `-v`    | バージョン情報表示 |
| `--verbose`, `-vv`   | 詳細出力モード |
| `--no-color`         | 色なしで出力   |

## コマンドリスト

### 1. moai-adk init

**プロジェクト初期化およびテンプレート注入**

#### 構文

```bash
moai-adk init [path] [options]
```

#### 引数

| 引数   | 説明          | デフォルト        |
| ------ | ------------- | ----------------- |
| `path` | プロジェクトパス | 現在のディレクトリ |

#### オプション

```bash
--language LANG, -l LANG    プロジェクト言語選択（ko/en/ja/zh）
--mode MODE                 開発モード（solo/team/org）
--with-mcp SERVER          MCPサーバー追加（context7, figma, playwright）
--mcp-auto                 すべての推奨MCPサーバー自動インストール
--force, -f                既存設定上書き
--skip-git                 Git初期化スキップ
```

#### 例

```bash
# 新規プロジェクト作成
moai-adk init my-project

# 現在のディレクトリ初期化
moai-adk init .

# MCPサーバーを含める
moai-adk init . --with-mcp context7 --with-mcp figma

# すべてのMCP自動インストール
moai-adk init . --mcp-auto

# 上書きで再初期化
moai-adk init . --force
```

#### 生成されるファイル

```
project/
├── .moai/
│   ├── config.json        # プロジェクト設定
│   ├── specs/            # SPECドキュメント
│   ├── docs/             # 生成ドキュメント
│   ├── reports/          # 分析レポート
│   └── scripts/          # ユーティリティスクリプト
├── .claude/
│   ├── settings.json     # Claude Code設定
│   ├── commands/         # Alfredコマンド
│   ├── agents/          # サブエージェントテンプレート
│   └── mcp.json         # MCP設定
└── CLAUDE.md            # プロジェクトガイドライン
```

______________________________________________________________________

### 2. moai-adk doctor

**システム環境診断**

#### 構文

```bash
moai-adk doctor [options]
```

#### オプション

```bash
--verbose, -vv       詳細診断情報
--fix                自動修正試行
--export FILE       結果をファイルにエクスポート
```

#### 診断項目

- ✅ Pythonバージョン（3.13+必須）
- ✅ uvパッケージマネージャー
- ✅ Gitリポジトリ状態
- ✅ `.moai/`ディレクトリ構造
- ✅ `.claude/`リソース
- ✅ Claude Codeアクセシビリティ
- ✅ Python依存関係
- ✅ ディスク容量

#### 例

```bash
# 基本診断
moai-adk doctor

# 詳細診断
moai-adk doctor -vv

# 結果をファイルに保存
moai-adk doctor --export .moai/reports/doctor.txt

# 自動修正
moai-adk doctor --fix
```

______________________________________________________________________

### 3. moai-adk status

**プロジェクト状態照会**

#### 構文

```bash
moai-adk status [options]
```

#### オプション

```bash
--json                 JSON形式出力
--compact, -c         簡単なサマリーのみ表示
--spec ID             特定SPECの詳細照会
```

#### 表示情報

- 📋 SPEC進行状況（完了/進行中/待機）
- 🏷️ TAG統計（@SPEC/@TEST/@CODE/@DOC）
- 📝 最近のコミット
- 📅 最後の同期時間
- 🔄 Gitブランチ状態

#### 例

```bash
# 全体状態
moai-adk status

# JSON形式
moai-adk status --json

# 簡単なサマリー
moai-adk status --compact

# 特定SPEC詳細
moai-adk status --spec SPEC-001
```

______________________________________________________________________

### 4. moai-adk backup

**プロジェクトバックアップ作成**

#### 構文

```bash
moai-adk backup [options]
```

#### オプション

```bash
--target DIR           バックアップ場所（デフォルト: .moai-backups/）
--include-git          Git履歴を含める
--compress, -z        圧縮形式（tar.gz）
--restore FILE         バックアップ復元
```

#### バックアップ対象

- `.moai/`全体ディレクトリ
- `.claude/`リソース
- `CLAUDE.md`プロジェクトガイドライン
- `pyproject.toml` / `requirements.txt`

#### 例

```bash
# 基本バックアップ
moai-adk backup

# 圧縮バックアップ
moai-adk backup --compress

# Git履歴を含める
moai-adk backup --include-git

# バックアップ復元
moai-adk backup --restore .moai-backups/20250115_143000/

# カスタム場所
moai-adk backup --target ~/backups/moai/
```

______________________________________________________________________

### 5. moai-adk update

**パッケージおよびテンプレート同期（最も重要なコマンド）**

#### 構文

```bash
moai-adk update [options]
```

#### オプション

```bash
--check                更新可能性のみ確認
--dry-run              変更プレビュー
--skip-backup          バックアップスキップ（推奨されません）
--force                強制更新
--from VERSION         特定バージョンから更新
```

#### 動作プロセス

1. **バージョン確認**: PyPIから最新バージョン確認
2. **バックアップ作成**: 現在の状態を安全にバックアップ
3. **テンプレート同期**: 新しいテンプレートと既存設定をマージ
4. **検証**: 整合性確認
5. **完了**: 変更サマリー

#### 例

```bash
# 通常更新
moai-adk update

# プレビュー
moai-adk update --dry-run

# 強制更新
moai-adk update --force

# バージョン確認のみ
moai-adk update --check
```

______________________________________________________________________

## オプション組み合わせ

### 高度な使用ケース

```bash
# 詳細ログと共に初期化
moai-adk init . --verbose --with-mcp context7

# 自動修正と詳細レポート
moai-adk doctor --fix --export .moai/reports/doctor.md

# 全体状態をJSONでエクスポート
moai-adk status --json > status.json

# 圧縮バックアップ作成および復元テスト
moai-adk backup --compress
moai-adk backup --restore .moai-backups/latest.tar.gz
```

______________________________________________________________________

## 終了コード

| コード | 意味        |
| ----- | ----------- |
| `0`   | 成功        |
| `1`   | 一般エラー  |
| `2`   | 使用方法エラー |
| `127` | コマンドが見つからない |

______________________________________________________________________

## 環境変数

```bash
MOAI_HOME              MoAI-ADKインストールパス
MOAI_DEBUG             デバッグモード有効化（1）
MOAI_NO_COLOR          色出力無効化（1）
MOAI_CONFIG_PATH       .moai/config.jsonパス
```

______________________________________________________________________

## トラブルシューティング

### "Permission denied"

```bash
# 権限確認
ls -la .moai/
chmod -R u+w .moai/
```

### "Template conflict"

```bash
# バックアップ後強制更新
moai-adk backup
moai-adk update --force
```

### "Python version mismatch"

```bash
# Python 3.13+確認
python3 --version
uv python install 3.13
```

______________________________________________________________________

**次**: [Alfredサブコマンドガイド](subcommands.md)または[CLIリファレンス](index.md)



