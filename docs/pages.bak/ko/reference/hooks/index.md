# Hooks 시스템 참고서

Claude Code의 Hook 시스템을 통한 자동 가드레일 및 컨텍스트 관리입니다.

## 🎯 개요

**Hooks**는 특정 이벤트 발생 시 자동으로 실행되는 스크립트입니다. MoAI-ADK는 4개의 주요 Hook을 제공합니다.

### Hook 종류

| Hook                 | 시점           | 용도               | 타임아웃 |
| -------------------- | -------------- | ------------------ | -------- |
| **SessionStart**     | 세션 시작      | 프로젝트 상태 확인 | 5초      |
| **PreToolUse**       | 도구 실행 전   | 위험 명령 차단     | 5초      |
| **UserPromptSubmit** | 사용자 입력 후 | 입력 검증          | 5초      |
| **PostToolUse**      | 도구 실행 후   | 결과 분석          | 5초      |

## 📍 Hook 위치

```
.claude/
├── hooks/
│   ├── session_start.sh       # SessionStart Hook
│   ├── pre_tool_use.sh        # PreToolUse Hook
│   ├── post_tool_use.sh       # PostToolUse Hook
│   └── user_prompt_submit.sh  # UserPromptSubmit Hook
├── settings.json              # Hook 설정
└── permissions.json           # 권한 설정
```

## 🔄 Hook 실행 흐름

```
┌─────────────────────────────────────┐
│  Claude Code Session 시작            │
└────────────────┬────────────────────┘
                 │
            ┌────▼────────────┐
            │ SessionStart    │ (프로젝트 상태 확인)
            └────┬────────────┘
                 │
      ┌──────────▼──────────┐
      │ 사용자 명령어 입력   │
      └──────────┬──────────┘
                 │
            ┌────▼────────────┐
            │PreToolUse       │ (도구 실행 전 검증)
            └────┬────────────┘
                 │
          ┌──────▼──────────┐
          │ 도구 실행       │
          └──────┬──────────┘
                 │
            ┌────▼────────────┐
            │PostToolUse      │ (결과 분석)
            └────┬────────────┘
                 │
      ┌──────────▼──────────┐
      │  사용자에게 결과 전달│
      └─────────────────────┘
```

## ⚙️ Hook 설정

### .claude/settings.json

```json
{
  "hooks": {
    "enabled": true,
    "timeout": 5000,
    "session_start": ".claude/hooks/session_start.sh",
    "pre_tool_use": ".claude/hooks/pre_tool_use.sh",
    "post_tool_use": ".claude/hooks/post_tool_use.sh",
    "user_prompt_submit": ".claude/hooks/user_prompt_submit.sh"
  }
}
```

## 🆘 Hook 오류 처리

### Hook 실행 실패

```
❌ Hook 실패
│
├─ Timeout (5초 초과)
│  └─→ 도구 실행 차단
│
├─ Script 오류
│  └─→ 오류 로그 저장
│
└─ Permission 오류
   └─→ 권한 조정 필요
```

### 디버깅

```bash
# Hook 로그 확인
cat ~/.claude/projects/*/hook-logs/*.log

# Hook 수동 실행
bash .claude/hooks/session_start.sh

# Hook 비활성화
# settings.json의 "hooks.enabled" → false
```

## 📚 상세 가이드

- **[SessionStart Hook](session.md)** - 세션 시작 시 자동 실행
- **[Tool Hooks](tool.md)** - 도구 실행 전/후 처리

______________________________________________________________________

**다음**: [SessionStart Hook](session.md) 또는 [Tool Hooks](tool.md)
