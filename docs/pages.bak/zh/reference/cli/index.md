# CLI命令参考

`moai-adk` CLI是一个基于Click的命令行接口，负责项目管理和模板同步。它与Alfred命令（alfred:*）分开使用，用于本地环境设置和维护。

## 核心命令

| 命令                 | 说明                                              | 使用时机                          |
| -------------------- | ------------------------------------------------- | --------------------------------- |
| `moai-adk init [path]` | 创建新项目或将模板注入现有项目 | 首次引入Alfred时            |
| `moai-adk doctor`      | 环境检查（Python、uv、Git、目录结构） | 安装后、问题发生时            |
| `moai-adk status`      | TAG摘要、检查点、模板版本查询            | 工作前、审查前    |
| `moai-adk backup`      | 创建`.moai/`、`.claude/`、CLAUDE.md备份         | 模板更新前、重大更改前 |
| `moai-adk update`      | 包和模板同步（最重要的命令）         | 新版本发布后、定期检查     |

## 命令详情

### `moai-adk init`

**目的**: 初始化新项目并创建基本结构

**用法**:

```bash
# 创建新项目
moai-adk init my-project

# 初始化当前目录
moai-adk init .

# 将MoAI-ADK注入现有项目
moai-adk init .
```

**生成的结构**:

```
my-project/
├── .moai/        # 项目元数据
├── .claude/      # Alfred资源
└── CLAUDE.md     # 项目指南
```

**初始化过程**:

1. Python环境检查
2. Git存储库初始化（如果不存在）
3. 创建`.moai/`目录结构
4. 复制`.claude/`资源模板
5. 创建默认配置文件

### `moai-adk doctor`

**目的**: 系统环境诊断和故障排除

**用法**:

```bash
moai-adk doctor
```

**诊断项目**:

- ✅ Python版本（3.13+）
- ✅ uv包管理器
- ✅ Git存储库状态
- ✅ `.moai/`目录结构
- ✅ `.claude/`资源完整性
- ✅ Claude Code可访问性

**预期输出**:

```
🩺 MoAI-ADK System Check
✅ Python 3.13.0
✅ uv 0.5.1
✅ Git存储库已初始化
✅ .moai/目录结构正常
✅ .claude/资源74个已加载
✅ Claude Code可访问

系统正常。准备启动Alfred！
```

### `moai-adk status`

**目的**: 项目状态摘要和状态理解

**用法**:

```bash
moai-adk status
```

**显示信息**:

- SPEC进度状态（完成/进行中/待处理）
- TAG统计（@SPEC/@TEST/@CODE/@DOC）
- 最近的检查点
- 模板版本信息
- Git工作流状态

**预期输出**:

```
📊 MoAI-ADK Project Status
:bullseye: Project: MyProject
📅 Last sync: 2025-01-15 14:30

📋 SPEC Progress
- ✅ Completed: 12
- 🔄 In Progress: 3
- ⏳ Pending: 5

🏷️ TAG Statistics
- @SPEC: 20 tags
- @TEST: 18 tags
- @CODE: 17 tags
- @DOC: 16 tags
- 🚨 Orphan tags: 2

📝 Version Info
- Template: v0.15.2
- Last update: 2025-01-10
- Backup available: .moai-backups/20250110/

🔄 Git Status
- Current branch: feature/auth-system
- Ahead of main: 12 commits
- Draft PR: #23
```

### `moai-adk backup`

**目的**: 创建项目资源备份

**用法**:

```bash
moai-adk backup
```

**备份目标**:

- `.moai/`整个目录
- `.claude/`资源模板
- `CLAUDE.md`项目指南
- Git状态信息

**备份位置**:

```
.moai-backups/
└── 20250115_143000/
    ├── .moai/
    ├── .claude/
    ├── CLAUDE.md
    └── backup-info.json
```

### `moai-adk update`

**目的**: 包和模板同步（最重要的命令）

**用法**:

```bash
moai-adk update
```

**更新阶段**:

1. **阶段1**: 包版本检查
2. **阶段2**: 模板版本比较
3. **阶段3**: 备份创建和合并

**自动处理**:

- 从PyPI检查最新版本
- 将当前资源备份到`.moai-backups/`
- 合并新模板与现有设置
- 冲突时提供指导消息

**输出示例**:

```
🔄 MoAI-ADK Update Started
:package: Current version: v0.15.1
:package: Latest version: v0.15.2

📁 Creating backup...
✅ Backup created: .moai-backups/20250115_143000/

🔄 Updating templates...
🔧 Merging .moai/config.json
🔧 Updating Alfred agents
🔧 Syncing Skills (74 → 77)

✅ Update completed successfully!
📝 Changelog: Added moai-domain-ml Skill
⚠️  Please review .claude/settings.json changes
```

## 内部操作

### CLI架构

```
moai-adk
├── __main__.py           # Click入口点
├── cli/
│   ├── commands/
│   │   ├── init.py      # 项目初始化
│   │   ├── doctor.py    # 环境诊断
│   │   ├── status.py     # 状态查询
│   │   ├── backup.py    # 备份创建
│   │   └── update.py    # 模板同步
│   └── utils.py          # 通用实用程序
├── core/
│   ├── template.py      # 模板管理
│   ├── backup.py        # 备份/恢复
│   └── filesystem.py    # 文件系统操作
└── templates/           # 默认模板源
```

### Rich控制台输出

- **颜色编码**: 成功（绿色）、警告（黄色）、错误（红色）
- **进度条**: 显示长时间运行任务的进度
- **表格格式**: 有组织地显示状态信息
- **ASCII艺术**: 徽标和分隔符

### 错误处理

- **清晰消息**: 用户友好的错误描述
- **解决方案建议**: 解决问题的具体方法
- **错误代码**: 自动化脚本的退出代码
- **日志记录**: 用于问题跟踪的详细日志

## 最佳实践

### 定期维护

```bash
# 每月定期检查
moai-adk doctor
moai-adk status
moai-adk backup
moai-adk update
```

### 重大更改前

```bash
# 安全更改程序
moai-adk backup  # 1. 创建备份
# 执行更改...
moai-adk status  # 2. 检查状态
moai-adk doctor  # 3. 环境检查
```

### 新团队成员入职

```bash
# 标准入职程序
git clone <project>
cd <project>
moai-adk doctor  # 环境检查
moai-adk status  # 了解项目
claude           # 启动Alfred
/alfred:0-project  # 初始化项目
```

## 相关链接

- **[项目结构](project-structure)** - `.moai/`和`.claude/`目录详细信息
- **[Alfred命令](../alfred/commands)** - alfred:*工作流命令
- **[工作流](../workflow)** - CLI和Alfred的集成方式



