# Alfred → Moai 폴더 구조 마이그레이션 가이드

## 개요

MoAI-ADK v0.27.0부터 Claude Code 설정 폴더 구조가 **Alfred에서 Moai로 변경**됩니다.

이 변경은:
- 🎩 **Alfred** (MoAI Agentic Framework) → 🗿 **Moai** (새로운 아키텍처) 네이밍 변경
- `.claude/commands/alfred/` → `.claude/commands/moai/`
- `.claude/agents/alfred/` → `.claude/agents/moai/`
- `.claude/hooks/alfred/` → `.claude/hooks/moai/`

> **좋은 소식**: 이 마이그레이션은 **완전히 자동**입니다! 🚀

---

## 자동 마이그레이션

### 언제 실행되나요?

`moai-adk update` 명령어를 실행하면 자동으로 마이그레이션이 수행됩니다.

```bash
moai-adk update
```

**출력 예시**:
```
[1/5] 프로젝트 백업 중...
✅ 백업 완료: .moai/backups/alfred_to_moai_migration_20251118_...

[2/5] Alfred 폴더 감지됨: commands, agents, hooks

[3/5] Moai 템플릿 설치 확인 중...
✅ Moai 템플릿 설치됨

[4/5] 경로 업데이트 중...
✅ settings.json Hook 경로 업데이트됨

[5/5] 정리 작업 중...
✅ Alfred 폴더 삭제됨

[6/6] 마이그레이션 검증 중...
✅ 마이그레이션 검증 통과

마이그레이션 상태 기록 중...
✅ 마이그레이션 상태 기록됨

✅ Alfred → Moai 마이그레이션 완료!
```

---

## 마이그레이션 과정

### 1단계: 자동 백업 생성

마이그레이션을 시작하기 전에 `.moai/backups/` 디렉토리에 전체 프로젝트의 스냅샷을 생성합니다.

**백업 위치**: `.moai/backups/alfred_to_moai_migration_YYYYMMDD_HHMMSS`

**백업 내용**:
- `.claude/` 폴더 전체
- `.moai/` 폴더 전체
- 기타 중요 설정 파일

### 2단계: Alfred 폴더 감지

시스템이 다음 폴더들의 존재를 확인합니다:
- `.claude/commands/alfred/`
- `.claude/agents/alfred/`
- `.claude/hooks/alfred/`

감지된 폴더들만 처리됩니다. (없는 폴더는 자동으로 건너뜀)

### 3단계: Moai 템플릿 설치 확인

패키지에서 제공하는 새로운 Moai 템플릿 폴더가 존재하는지 확인합니다:
- `.claude/commands/moai/`
- `.claude/agents/moai/`
- `.claude/hooks/moai/`

> **중요**: Moai 템플릿이 없으면 마이그레이션이 중단되고 자동 롤백됩니다.

### 4단계: settings.json Hook 경로 업데이트

다음 경로들이 자동으로 변경됩니다:

| 이전 | 변경됨 |
|------|--------|
| `.claude/hooks/alfred/` | `.claude/hooks/moai/` |
| `.claude/commands/alfred/` | `.claude/commands/moai/` |
| `.claude/agents/alfred/` | `.claude/agents/moai/` |

**예시**:

**Before**:
```json
{
  "hooks": {
    "SessionStart": [
      {
        "command": "uv run {{PROJECT_DIR}}/.claude/hooks/alfred/session_start.py"
      }
    ]
  }
}
```

**After**:
```json
{
  "hooks": {
    "SessionStart": [
      {
        "command": "uv run {{PROJECT_DIR}}/.claude/hooks/moai/session_start.py"
      }
    ]
  }
}
```

### 5단계: Alfred 폴더 삭제

더 이상 필요하지 않은 Alfred 폴더들이 삭제됩니다:
- `.claude/commands/alfred/` ❌
- `.claude/agents/alfred/` ❌
- `.claude/hooks/alfred/` ❌

### 6단계: 마이그레이션 검증

마이그레이션이 성공적으로 완료되었는지 확인합니다:
- ✅ Moai 폴더들이 모두 존재하는가?
- ✅ Alfred 폴더들이 모두 삭제되었는가?
- ✅ settings.json에 Alfred 참조가 남아있지 않은가?
- ✅ config.json에 마이그레이션 상태가 기록되었는가?

### 7단계: 마이그레이션 상태 기록

마이그레이션 완료 정보가 `.moai/config/config.json`에 저장됩니다:

```json
{
  "migration": {
    "alfred_to_moai": {
      "migrated": true,
      "timestamp": "2025-11-18 14:30:45",
      "folders_installed": 3,
      "folders_removed": 3,
      "backup_path": ".moai/backups/alfred_to_moai_migration_20251118_143045",
      "package_version": "0.27.0"
    }
  }
}
```

이 정보를 통해 **중복 마이그레이션을 방지**합니다.

---

## 중복 실행 방지

같은 프로젝트에서 `moai-adk update`를 여러 번 실행해도 마이그레이션은 **한 번만 수행**됩니다.

**확인 메시지**:
```
ℹ️  Alfred → Moai 마이그레이션이 이미 완료되었습니다
타임스탬프: 2025-11-18 14:30:45
```

---

## 마이그레이션 실패 시 처리

### 자동 롤백

마이그레이션 중 **어떤 단계에서든 오류가 발생**하면:

1. ❌ 모든 변경 사항이 중단됩니다
2. 🔄 백업에서 자동으로 복원됩니다
3. 📋 마이그레이션 상태가 초기화됩니다
4. 💡 사용자에게 오류 메시지가 표시됩니다

**자동 롤백 예시**:
```
❌ 에러: settings.json 업데이트 실패
원인: JSON 형식 오류

🔄 자동 롤백 시작...
[1/3] 프로젝트 복원 중...
✅ 프로젝트 복원됨
[2/3] 마이그레이션 상태 초기화...
✅ 롤백 완료

💡 팁: 에러를 해결한 후 다시 `moai-adk update` 명령을 실행하세요
```

### 롤백 후 재시도

롤백 후 다음 단계를 따르세요:

1. **에러 메시지 확인**: 어느 단계에서 실패했는지 확인
2. **문제 해결**: 필요시 수동으로 문제 수정
3. **다시 실행**: `moai-adk update` 명령어 다시 실행

```bash
# 첫 번째 실패
moai-adk update
# ❌ 실패...

# 문제 해결 후 재시도
moai-adk update
# ✅ 성공!
```

---

## 수동 롤백 (필요시)

### 백업 파일 찾기

자동 롤백이 작동하지 않았거나 수동으로 복원하려면:

```bash
# 백업 목록 확인
ls -la .moai/backups/

# 출력 예시:
# alfred_to_moai_migration_20251118_143045/
# alfred_to_moai_migration_20251117_102030/
```

### 수동 복원

```bash
# 가장 최근의 백업으로 복원
cp -r .moai/backups/alfred_to_moai_migration_20251118_143045/* ./

# 특정 폴더만 복원 (필요시)
cp -r .moai/backups/alfred_to_moai_migration_20251118_143045/.claude .
```

---

## 문제 해결

### Q1: 마이그레이션이 완료되지 않았는데 계속 "이미 마이그레이션됨" 메시지가 나타납니다

**원인**: config.json에 마이그레이션 상태가 잘못 기록됨

**해결 방법**:
```bash
# config.json에서 마이그레이션 정보 제거
cat .moai/config/config.json | jq 'del(.migration.alfred_to_moai)' > .moai/config/config.json.tmp
mv .moai/config/config.json.tmp .moai/config/config.json

# 다시 실행
moai-adk update
```

### Q2: settings.json 파일이 손상되었습니다

**원인**: 마이그레이션 중 JSON 형식이 손상됨

**해결 방법**:
```bash
# 백업에서 settings.json만 복원
cp .moai/backups/alfred_to_moai_migration_YYYYMMDD_HHMMSS/.claude/settings.json .claude/

# 다시 실행
moai-adk update
```

### Q3: Alfred 폴더가 여전히 존재합니다

**원인**: 마이그레이션이 부분적으로만 완료됨

**해결 방법**:
```bash
# 현재 상태 확인
ls -la .claude/

# Alfred 폴더 확인
ls -la .claude/commands/alfred/
ls -la .claude/agents/alfred/
ls -la .claude/hooks/alfred/

# 백업에서 전체 복원
cp -r .moai/backups/alfred_to_moai_migration_YYYYMMDD_HHMMSS/* ./

# 마이그레이션 상태 재설정
cat .moai/config/config.json | jq 'del(.migration.alfred_to_moai)' > .moai/config/config.json.tmp
mv .moai/config/config.json.tmp .moai/config/config.json

# 다시 실행
moai-adk update
```

### Q4: 마이그레이션 후 Hook이 작동하지 않습니다

**원인**: settings.json 경로가 올바르지 않거나, Moai Hook 파일이 누락됨

**해결 방법**:
```bash
# settings.json에서 경로 확인
grep -r "moai" .claude/settings.json

# Moai 폴더 내용 확인
ls -la .claude/hooks/moai/

# 모두 없으면 백업에서 복원
cp -r .moai/backups/alfred_to_moai_migration_YYYYMMDD_HHMMSS/.claude/hooks/moai .claude/hooks/

# 다시 실행
moai-adk update
```

---

## 주요 변경 사항

### 폴더 구조

| 항목 | Before (v0.26.x) | After (v0.27.0+) |
|------|------------------|------------------|
| **Commands** | `.claude/commands/alfred/` | `.claude/commands/moai/` |
| **Agents** | `.claude/agents/alfred/` | `.claude/agents/moai/` |
| **Hooks** | `.claude/hooks/alfred/` | `.claude/hooks/moai/` |

### 설정 파일

| 파일 | 변경 사항 |
|------|---------|
| **settings.json** | Alfred 경로 → Moai 경로 자동 변경 |
| **config.json** | 마이그레이션 상태 기록 (`migration.alfred_to_moai`) |

### 역호환성

✅ **완벽한 역호환성 유지**

- 마이그레이션은 **선택이 아닌 자동 프로세스**
- 기존 Custom Hook/Agent/Command는 **모두 Moai 폴더로 이동**
- 새로운 커스터마이제이션은 **Moai 폴더에서만** 작동

---

## FAQ

**Q: 마이그레이션 후 기존 Custom Hook이 작동하지 않습니다**

A: Hook 파일이 Moai 폴더로 이동되었는지 확인하세요:
```bash
ls -la .claude/hooks/moai/
# 자신의 custom hook이 있어야 함
```

---

**Q: 마이그레이션을 건너뛰고 싶습니다**

A: 불가능합니다. 다음 버전(v0.28.0)부터 Alfred 폴더가 완전히 제거되므로 마이그레이션이 필수입니다.

---

**Q: 여러 프로젝트가 있습니다. 마이그레이션을 한 번에 할 수 있나요?**

A: 각 프로젝트에서 `moai-adk update`를 개별적으로 실행해야 합니다.

```bash
# 프로젝트 1
cd project1
moai-adk update

# 프로젝트 2
cd project2
moai-adk update
```

---

**Q: 마이그레이션 후 이전 버전으로 돌릴 수 있나요?**

A: 가능합니다. 백업에서 복원하면 됩니다:
```bash
cp -r .moai/backups/alfred_to_moai_migration_YYYYMMDD_HHMMSS/* ./
```

---

## 성능 및 시간

| 작업 | 예상 시간 |
|------|----------|
| 백업 생성 | < 5초 |
| Alfred 폴더 감지 | < 1초 |
| Moai 템플릿 확인 | < 1초 |
| settings.json 업데이트 | < 1초 |
| Alfred 폴더 삭제 | < 5초 |
| 마이그레이션 검증 | < 1초 |
| **총 소요 시간** | **< 20초** |

---

## 더 알아보기

### 기술 문서

- [SPEC-MIGRATION-001](../../.moai/specs/SPEC-MIGRATION-001/) - 마이그레이션 명세
- [AlfredToMoaiMigrator](../../src/moai_adk/core/migration/alfred_to_moai_migrator.py) - 마이그레이션 구현

### 관련 명령어

```bash
# 버전 확인
moai-adk --version

# 도움말
moai-adk update --help

# 스테이터스 확인
moai-adk status
```

---

## 피드백 및 문제 보고

마이그레이션 중 문제가 발생하면:

```bash
moai-adk feedback
```

또는 GitHub에 이슈를 등록하세요:
- https://github.com/modu-ai/moai-adk/issues

---

## 버전 정보

| 버전 | 릴리스 날짜 | 내용 |
|------|-----------|------|
| v0.26.x | 2025-11-15 | Alfred 폴더 구조 (legacy) |
| **v0.27.0** | **2025-11-18** | **자동 마이그레이션 추가** ← 현재 |
| v0.28.0 | 2025-12-15 (예정) | Alfred 폴더 완전 제거 |

---

**마지막 업데이트**: 2025-11-18
**버전**: 1.0.0
**상태**: 안정적 (Stable)
