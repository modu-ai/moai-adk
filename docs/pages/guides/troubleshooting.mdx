---
title: "문제 해결
description: "MoAI-ADK 사용 중 발생하는 일반적인 문제와 해결 방법
---
"
MoAI-ADK를 사용하면서 발생할 수 있는 일반적인 문제들을 체계적으로 해결하는 방법을 안내합니다. 문제를 카테고리별로 분류하고 단계별 해결 전략을 제공합니다.
"
## 🚨 응급 처치
"
### Alfred가 응답하지 않을 때
"
**증상**: alfred:* 명령어를 입력해도 반응이 없거나 타임아웃 발생
"
**즉시 조치**:
```bash
# 1. 현재 세션 상태 확인
moai-adk status
"
# 2. Claude Code 재시작
# Claude Code에서 Ctrl+C 후 다시 시작
"
# 3. 환경 진단
moai-adk doctor
"
# 4. 설정 확인
cat .moai/config.json
```
"
**근본 원인**:
- Hook 타임아웃 (5초 제한)
- Claude Code API 연결 문제
- 파일 권한 문제
"
### Git 충돌 발생 시
"
**증상**: alfred:3-sync 실행 중 merge conflict 발생
"
**즉시 조치**:
```bash
# 1. 충돌 상태 확인
git status
"
# 2. 체크포인트 복원
moai-adk backup  # 백업 생성
git merge --abort  # 충돌 취소
"
# 3. Alfred에게 도움 요청
claude
/alfred:?  # 충돌 해결 안내 요청
```
"
## 🔧 카테고리별 문제 해결
"
### 설치 및 초기화 문제
"
#### 문제: `moai-adk: command not found`
"
**원인**:
1. uv가 PATH에 없음
2. 패키지 설치 실패
3. Python 환경 문제
"
**해결 단계**:
```bash
# 1. uv 설치 확인
which uv
uv --version
"
# 2. uv 재설치 (macOS/Linux)
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc  # 또는 ~/.zshrc
"
# 3. 패키지 재설치
uv tool install moai-adk --force
"
# 4. PATH 확인
echo $PATH | grep -o '[^:]*uv[^:]*'
```
"
#### 문제: `moai-adk doctor`에서 권한 오류
"
**해결 방안**:
```bash
# 1. 파일 권한 확인
ls -la .moai/
ls -la .claude/
"
# 2. 권한 수정
chmod 755 .moai .claude
chmod -R 644 .moai/*
chmod -R 644 .claude/*
"
# 3. 소유자 확인 (macOS/Linux)
sudo chown -R $(whoami) .moai .claude
```
"
### SPEC 작성 문제
"
#### 문제: alfred:1-plan이 계속 질문만 함
"
**원인**: 요구사항이 모호하거나 비즈니스 목표 불명확
"
**해결 전략**:
1. **명확한 제공**: 구체적인 기능 명세
2. **범위 한정**: MVP 기능부터 시작
3. **예시 제공**: 유사 시스템이나 기능 참고
"
**올바른 요청 예시**:
```
# ❌ 나쁜 예시
"사용자 기능 만들어줘"
"
# ✅ 좋은 예시
"React TypeScript로 이메일/비밀번호 로그인 기능 개발
- JWT 토큰 인증
- 15분 세션 타임아웃
- 비밀번호 복구 기능 제외"
```
"
#### 문제: SPEC 파일이 생성되지 않음
"
**진단 단계**:
```bash
# 1. 디렉터리 확인
ls -la .moai/specs/
"
# 2. 권한 확인
ls -la .moai/
"
# 3. Alfred 로그 확인
tail -f ~/.claude/logs/alfred.log
"
# 4. 수동 SPEC 생성 테스트
claude
"EARS 형식으로 로그인 기능 SPEC 작성해줘"
```
"
### TDD 실행 문제
"
#### 문제: alfred:2-run에서 테스트 실패 반복
"
**원인 분석**:
1. 테스트 환경 설정 문제
2. 의존성 충돌
3. 잘못된 테스트 로직
"
**해결 순서**:
```bash
# 1. 테스트 환경 점검
python -m pytest --version
python -c "import fastapi; print('FastAPI OK')"
"
# 2. 의존성 재설치
pip install -r requirements.txt --force-reinstall
"
# 3. 간단한 테스트로 확인
echo "def test_basic():
    assert 1 + 1 == 2" > test_basic.py
python -m pytest test_basic.py -v
```
"
#### 문제: 코드 커버리지가 85% 미만
"
**커버리지 향상 전략**:
1. **누락된 테스트 케이스 확인**
   ```bash
   python -m pytest --cov=src --cov-report=html
   # htmlcov/index.html에서 브라우저로 확인
   ```
"
2. **Edge case 테스트 추가**
   ```bash
   # Alfred에게 요청
   claude
   "/alfred:2-run에 테스트 케이스 누락된 부분 분석해줘"
   ```
"
3. **통합 테스트 강화**
   ```python
   # API 엔드포인트 통합 테스트 예시
   def test_user_login_integration():
       response = client.post("/auth/login", json={
           "email": "test@example.com",
           "password": "testpass123"
       })
       assert response.status_code == 200
       assert "access_token" in response.json()
   ```
"
### 문서 동기화 문제
"
#### 문제: alfred:3-sync에서 TAG 오류 발생
"
**TAG 무결성 검증**:
```bash
# 1. TAG 인벤토리 확인
find . -name "*.py" -o -name "*.md" | xargs grep -l "@SPEC\|@TEST\|@CODE\|@DOC"
"
# 2. 고아 TAG 탐지
python3 -c "
import re, os
tags = {'SPEC': set(), 'TEST': set(), 'CODE': set(), 'DOC': set()}
for root, dirs, files in os.walk('.'):
    for file in files:
        if file.endswith(('.py', '.md')):
            path = os.path.join(root, file)
            with open(path, 'r') as f:
                content = f.read()
                for tag_type in tags:
                    matches = re.findall(fr'@{tag_type}:(\w+)', content)
                    tags[tag_type].update(matches)
"
for tag_type in tags:
    print(f'{tag_type}: {len(tags[tag_type])} tags')
"
"
# 3. Alfred에 TAG 수정 요청
claude
"@TAG 무결성 검증 및 고아 TAG 정리해줘"
```
"
#### 문제: 문서 생성이 안 됨
"
**확인 사항**:
```bash
# 1. 권한 확인
ls -la .moai/reports/
"
# 2. 템플릿 확인
ls -la .claude/output-styles/
"
# 3. 메모리 공간 확인
df -h
du -sh .moai/
```
"
### 성능 문제
"
#### 문제: 명령어 실행이 너무 느림
"
**성능 최적화**:
```bash
# 1. Disk I/O 확인
iotop -o  # 또는 macOS: iostat 1
"
# 2. 메모리 사용량 확인
free -h  # 또는 macOS: vm_stat
"
# 3. 캐시 정리
rm -rf .moai/memory/cache/
rm -rf .claude/cache/
"
# 4. 병렬 처리 제한
# .claude/settings.json에서 병렬 처리 제한
```
"
#### 문제: 메모리 부족 오류
"
**메모리 관리**:
```bash
# 1. 사용량 확인
ps aux | grep claude
du -sh .moai/memory/
"
# 2. 로그 정리
find .moai/logs/ -name "*.log" -mtime +7 -delete
"
# 3. 백업 정리
ls -la .moai-backups/
# 오래된 백업 삭제
```
"
## 🔍 고급 디버깅
"
### 로그 분석
"
**Alfred 로그 확인**:
```bash
# 실시간 로그 모니터링
tail -f ~/.claude/logs/alfred.log
"
# Hook 로그 확인
tail -f ~/.claude/logs/hooks.log
"
# 에러 로그 필터링
grep -i error ~/.claude/logs/alfred.log | tail -20
```
"
### CLI 디버그 모드
"
```bash
# 상세 정보 출력
moai-adk --verbose status
moai-adk --debug update
"
# 환경 변수 설정
export DEBUG_MOAI=1
export CLAUDE_LOG_LEVEL=DEBUG
claude
```
"
### 설정 파일 검증
"
```bash
# JSON 유효성 검증
python3 -c "
import json
with open('.moai/config.json') as f:
    config = json.load(f)
print('✅ .moai/config.json 유효함')
"
with open('.claude/settings.json') as f:
    settings = json.load(f)
print('✅ .claude/settings.json 유효함')
"
```
"
## 🆘 도움 요청 방법
"
### Alfred 내부 도움말
"
```bash
claude
/alfred:?                    # 명령어 요약
/alfred:9-feedback           # 문제 회고
"debug-helper 도와줘"       # 디버깅 전문가 호출
```
"
### 커뮤니티 지원
"
1. **GitHub Issues**:
   - 버그 리포트: `bug` 레이블
   - 기능 요청: `enhancement` 레이블
   - 질문: `question` 레이블
"
2. **문제 보고서 작성**:
```markdown
## 환경 정보
- OS: macOS 14.2
- Python: 3.13.0
- moai-adk: v0.15.2
- Claude Code: latest
"
## 재현 단계
1. alfred:1-plan "로그인 기능" 실행
2. SPEC 작성 중간에 멈춤
3. 30초 후 타임아웃
"
## 예상 동작
SPEC이 정상적으로 생성됨
"
## 실제 동작
타임아웃 발생 및 오류 메시지 없음
"
## 로그
[관련 로그 첨부]
```
"
## 📋 예방 조치 체크리스트
"
### 일상적인 유지보수
- [ ] 매일 `moai-adk status` 실행
- [ ] 주 1회 `moai-adk backup` 실행
- [ ] 월 1회 `moai-adk update` 실행
- [ ] 커밋 전 `git status` 확인
"
### 안전한 작업 습관
- [ ] 큰 변경 전 백업 생성
- [ ] 브랜치에서 작업하고 PR 사용
- [ ] 정기적으로 TAG 무결성 검증
- [ ] 충분한 테스트 커버리지 유지
"
### 모니터링 설정
- [ ] 디스크 공간 주기적 확인
- [ ] 로그 파일 크기 모니터링
- [ ] 성능 메트릭 수집
- [ ] 오류 알림 설정
"
---
"
**관련 링크**:
- **[모범 사례](best-practices)** - 문제 예방을 위한 가이드
- **[CLI 레퍼런스](../reference/cli)** - 명령어 상세 설명
- **[GitHub Issues](https://github.com/modu-ai/moai-adk/issues)** - 커뮤니티 지원