# 코드 예제 작성 템플릿

> **목적**: 독자가 이해하기 쉽고 실행 가능한 코드 예제를 작성하기 위한 템플릿

---

## 📋 코드 예제 체크리스트

작성 전에 다음을 확인하세요:

- [ ] **실행 가능**: 복사-붙여넣기로 바로 실행 가능
- [ ] **최소성**: 핵심 개념만 포함 (불필요한 코드 제거)
- [ ] **명확성**: 변수명과 함수명이 의도를 명확히 표현
- [ ] **주석**: "왜"를 설명하는 주석 포함 (코드가 말하는 것 반복 금지)
- [ ] **출력**: 예상 출력을 명시
- [ ] **테스트**: 실제로 실행하여 검증
- [ ] **버전**: Python 버전 명시 (필요시)

---

## 패턴 1: 최소 실행 가능 예제 (Minimal Viable Example)

**용도**: 개념을 처음 소개할 때

**구조**:
```python
# 1. 간단한 설명 (1-2줄)
# 2. 가장 단순한 형태의 코드 (3-10줄)
# 3. 주석으로 핵심 포인트 강조
```

**예시**:

```python
# 리스트 컴프리헨션: 간결한 리스트 생성 문법
numbers = [1, 2, 3, 4, 5]
squares = [x ** 2 for x in numbers]

print(squares)  # 출력: [1, 4, 9, 16, 25]
```

---

## 패턴 2: Before/After 비교

**용도**: 새로운 방법의 장점을 보여줄 때

**구조**:
```python
# Before: 기존 방법 (비효율적/복잡)
[기존 코드]

# After: 개선된 방법 (효율적/간결)
[개선된 코드]
```

**예시**:

```python
# Before: 전통적인 반복문
squares = []
for x in range(1, 6):
    squares.append(x ** 2)
print(squares)  # [1, 4, 9, 16, 25]

# After: 리스트 컴프리헨션 (3줄 → 1줄)
squares = [x ** 2 for x in range(1, 6)]
print(squares)  # [1, 4, 9, 16, 25]
```

---

## 패턴 3: 점진적 확장 (Progressive Enhancement)

**용도**: 기본 → 응용 → 고급으로 단계적 학습

**구조**:
1. **Step 1**: 최소 기능
2. **Step 2**: 에러 처리 추가
3. **Step 3**: 프로덕션 수준 (타입 힌트, 독스트링, 로깅)

**예시**:

```python
# Step 1: 기본 함수
def greet(name):
    return f"안녕하세요, {name}님!"

print(greet("철수"))  # 안녕하세요, 철수님!


# Step 2: 에러 처리 추가
def greet(name):
    if not name:
        raise ValueError("이름은 비어있을 수 없습니다")
    return f"안녕하세요, {name}님!"

try:
    print(greet(""))
except ValueError as e:
    print(f"오류: {e}")  # 오류: 이름은 비어있을 수 없습니다


# Step 3: 프로덕션 수준
import logging
from typing import Optional

def greet(name: str, title: Optional[str] = None) -> str:
    """
    사용자에게 인사 메시지를 생성합니다.
    
    Args:
        name: 사용자 이름 (필수)
        title: 호칭 (선택, 예: "개발자", "학생")
    
    Returns:
        인사 메시지 문자열
    
    Raises:
        ValueError: name이 빈 문자열인 경우
    
    Examples:
        >>> greet("철수")
        '안녕하세요, 철수님!'
        >>> greet("영희", "개발자")
        '안녕하세요, 영희님! (개발자)'
    """
    if not name:
        logging.error("빈 이름으로 greet 호출됨")
        raise ValueError("이름은 비어있을 수 없습니다")
    
    greeting = f"안녕하세요, {name}님!"
    if title:
        greeting += f" ({title})"
    
    return greeting

# 사용 예시
print(greet("철수"))              # 안녕하세요, 철수님!
print(greet("영희", "개발자"))     # 안녕하세요, 영희님! (개발자)
```

---

## 패턴 4: 주석 달린 완전한 예제

**용도**: 복잡한 로직을 단계별로 설명

**구조**:
```python
# [전체 목적 설명]

# Step 1: [첫 번째 단계]
[코드]

# Step 2: [두 번째 단계]
[코드]

# Step 3: [세 번째 단계]
[코드]

# 결과
[출력]
```

**예시**:

```python
# 학생 성적 데이터에서 우수 학생 찾기

students = [
    {"name": "철수", "score": 85},
    {"name": "영희", "score": 92},
    {"name": "민수", "score": 78},
    {"name": "지훈", "score": 95},
]

# Step 1: 90점 이상 학생 필터링
high_scorers = [s for s in students if s["score"] >= 90]

# Step 2: 점수 기준 내림차순 정렬
high_scorers.sort(key=lambda s: s["score"], reverse=True)

# Step 3: 이름만 추출
names = [s["name"] for s in high_scorers]

# 결과 출력
print(f"우수 학생: {', '.join(names)}")  # 우수 학생: 지훈, 영희
```

---

## 패턴 5: 실전 시나리오

**용도**: 현업에서 마주칠 법한 완전한 예제

**구조**:
```python
"""
[파일명].py

[시나리오 설명]
[사용 방법]
"""

import [필요한 모듈]

# [메인 로직]
[코드]

# 사용 예시
if __name__ == "__main__":
    [테스트 코드]
```

**예시**:

```python
"""
word_counter.py

텍스트 파일에서 단어 빈도를 분석하는 프로그램

사용법:
    python word_counter.py input.txt
"""

import sys
from collections import Counter
import re

def count_words(text: str) -> dict:
    """
    텍스트에서 단어 빈도를 계산합니다.
    
    Args:
        text: 분석할 텍스트
    
    Returns:
        단어: 빈도 딕셔너리
    """
    # 소문자 변환 및 단어 추출 (알파벳, 숫자, 한글만)
    words = re.findall(r'[a-zA-Z가-힣0-9]+', text.lower())
    
    # Counter로 빈도 계산
    return dict(Counter(words))

def print_top_words(word_counts: dict, n: int = 10):
    """상위 N개 단어 출력"""
    sorted_words = sorted(
        word_counts.items(), 
        key=lambda x: x[1], 
        reverse=True
    )
    
    print(f"\n가장 많이 사용된 단어 Top {n}:")
    for rank, (word, count) in enumerate(sorted_words[:n], 1):
        print(f"  {rank}. {word}: {count}회")

# 메인 실행
if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("사용법: python word_counter.py <파일명>")
        sys.exit(1)
    
    filename = sys.argv[1]
    
    try:
        with open(filename, 'r', encoding='utf-8') as f:
            text = f.read()
        
        word_counts = count_words(text)
        print(f"총 {len(word_counts)}개의 고유 단어 발견")
        
        print_top_words(word_counts)
    
    except FileNotFoundError:
        print(f"오류: '{filename}' 파일을 찾을 수 없습니다")
    except Exception as e:
        print(f"오류 발생: {e}")
```

---

## 패턴 6: 대화형 예제 (REPL 스타일)

**용도**: Python 인터프리터에서 대화형으로 실행하는 모습

**구조**:
```python
>>> [입력 코드]
[출력]
>>> [다음 입력]
[출력]
```

**예시**:

```python
>>> # 리스트 생성
>>> numbers = [1, 2, 3, 4, 5]
>>> numbers
[1, 2, 3, 4, 5]

>>> # 요소 추가
>>> numbers.append(6)
>>> numbers
[1, 2, 3, 4, 5, 6]

>>> # 슬라이싱
>>> numbers[2:5]
[3, 4, 5]

>>> # 리스트 컴프리헨션
>>> [x * 2 for x in numbers]
[2, 4, 6, 8, 10, 12]
```

---

## 패턴 7: 테스트 케이스 포함

**용도**: 함수의 동작을 검증 가능하게 제시

**구조**:
```python
def [함수명]([매개변수]):
    """[독스트링]"""
    [구현]

# 테스트 케이스
assert [함수명]([입력]) == [예상출력]
assert [함수명]([입력]) == [예상출력]
print("✅ 모든 테스트 통과!")
```

**예시**:

```python
def is_palindrome(text: str) -> bool:
    """
    문자열이 회문(palindrome)인지 검사합니다.
    
    Args:
        text: 검사할 문자열 (공백 무시)
    
    Returns:
        회문이면 True, 아니면 False
    """
    # 공백 제거 및 소문자 변환
    cleaned = text.replace(" ", "").lower()
    return cleaned == cleaned[::-1]

# 테스트 케이스
assert is_palindrome("noon") == True
assert is_palindrome("hello") == False
assert is_palindrome("A man a plan a canal Panama") == True
assert is_palindrome("racecar") == True
assert is_palindrome("python") == False

print("✅ 모든 테스트 통과!")
```

---

## 주석 작성 가이드

### ✅ Good: "왜"를 설명하는 주석

```python
# 성능 향상을 위해 set 사용 (in 연산이 O(1))
unique_items = set(items)

# 역호환성 유지 (2026년 1월 제거 예정)
def legacy_format(date):
    return date.strftime("%d/%m/%Y")

# 외부 API 응답 시간이 느려 타임아웃 10초로 설정
response = requests.get(url, timeout=10)
```

### ❌ Bad: 코드를 그대로 반복

```python
# 변수 x에 10을 할당
x = 10

# 반복문으로 1부터 5까지 출력
for i in range(1, 6):
    print(i)

# 리스트에 요소 추가
items.append("new_item")
```

---

## 출력 표기 방법

### 패턴 A: 주석으로 표기

```python
numbers = [1, 2, 3]
print(numbers)  # 출력: [1, 2, 3]
```

### 패턴 B: 별도 블록

```python
numbers = [1, 2, 3]
print(numbers)
```

**출력**:
```
[1, 2, 3]
```

### 패턴 C: 콘솔 스타일

```bash
$ python example.py
[1, 2, 3]
```

---

## 코드 블록 메타 정보

````markdown
```python
# Python 3.12+ 필요
[코드]
```

```python
# ⚠️ 경고: 이 코드는 보안 취약점이 있습니다 (교육 목적)
[코드]
```

```python
# 💡 프로덕션 준비 완료
[코드]
```
````

---

## 파일 구조 표현

```
project/
├── main.py           # 메인 실행 파일
├── utils.py          # 유틸리티 함수
├── config.py         # 설정
├── data/
│   ├── input.txt     # 입력 데이터
│   └── output.txt    # 처리 결과
└── tests/
    └── test_main.py  # 단위 테스트
```

---

## 에러 처리 예제 패턴

```python
try:
    [정상 실행 코드]
except SpecificException as e:
    # 특정 예외 처리
    print(f"오류: {e}")
except AnotherException:
    # 다른 예외 처리
    print("다른 오류 발생")
else:
    # 예외가 없을 때 실행
    print("성공!")
finally:
    # 항상 실행 (정리 작업)
    print("완료")
```

---

## 체크리스트: 코드 예제 최종 검토

출판 전에 모든 코드 예제를 다음 기준으로 검토하세요:

- [ ] **실행 테스트**: 모든 예제를 실제로 실행하여 검증
- [ ] **버전 호환성**: Python 버전 명시 및 테스트
- [ ] **보안**: 하드코딩된 비밀번호, API 키 제거
- [ ] **경로**: 절대 경로 대신 상대 경로 사용
- [ ] **의존성**: 필요한 패키지 명시 (`pip install ...`)
- [ ] **플랫폼**: OS별 차이점 명시 (Windows/Mac/Linux)
- [ ] **출력**: 예상 출력 명시
- [ ] **주석**: "왜"를 설명하는 주석만 유지
- [ ] **가독성**: 80-100자 이내 줄 길이
- [ ] **일관성**: 프로젝트 전체에서 동일한 스타일

---

**도구 추천**:
- **포매팅**: Black (`pip install black`)
- **린팅**: Flake8 (`pip install flake8`)
- **타입 체크**: mypy (`pip install mypy`)
- **보안 스캔**: Bandit (`pip install bandit`)

**테스트 자동화**:
```bash
# 모든 코드 예제 자동 실행
pytest tests/code_examples/
```
