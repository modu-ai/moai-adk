# 장르 템플릿: api-reference (API 참조형)

## 📋 용도

API, 라이브러리, 프레임워크 기능 참조 문서

**구조**: Overview (10%) → Syntax (15%) → Parameters (20%) → Return Value (10%) → Examples (30%) → Notes (10%) → See Also (5%)

**특징**:
- 기술 문서 표준 형식
- 파라미터 테이블 필수
- 다양한 사용 예제
- 명확한 타입 정보

---

## 🏗️ 5가지 구성 요소

### 1. 문서 구조

```
├─ Overview (10%, 1-2문장 요약)
│  └─ 기능 한 줄 요약
│
├─ Syntax (15%, 함수 시그니처)
│  ├─ 함수/메서드 선언
│  └─ 파라미터 목록
│
├─ Parameters (20%, 파라미터 테이블)
│  ├─ 파라미터명
│  ├─ 타입
│  ├─ 필수/선택
│  ├─ 기본값
│  └─ 설명
│
├─ Return Value (10%, 반환값)
│  ├─ 반환 타입
│  └─ 반환값 설명
│
├─ Examples (30%, 코드 예제)
│  ├─ Basic Example (10-15줄)
│  ├─ Advanced Example (15-25줄)
│  └─ Error Handling (10줄)
│
├─ Notes & Warnings (10%, 주의사항)
│  ├─ 사용 시 주의사항
│  └─ 일반적인 실수
│
└─ See Also (5%, 관련 API)
   └─ 관련 함수/메서드 링크

총 섹션: 7개
비율 검증: 10+15+20+10+30+10+5 = 100%
```

### 2. 문체

**어조**: 간결하고 정확한 기술 문서

**종결어미**: "-다", "-니다" (설명), "-요" (예제)

**능동태**: 95% 이상

**문장 길이**: 평균 15-18단어 (간결)

**특징적 패턴**:
- "Returns X when..." (반환값)
- "Raises Y if..." (예외)
- "Defaults to Z" (기본값)

### 3. 내용 전개

**배치 순서**:
1. **Overview** (한 줄 요약)
   - 기능 핵심 설명 1-2문장

2. **Syntax** (함수 시그니처)
   - 정확한 함수 선언
   - 타입 힌트 포함

3. **Parameters** (파라미터 테이블)
   - 각 파라미터 상세 설명
   - 타입, 기본값, 필수 여부

4. **Return Value** (반환값)
   - 반환 타입 명시
   - 반환값 의미 설명

5. **Examples** (다양한 예제)
   - Basic: 가장 간단한 사용법
   - Advanced: 고급 옵션 활용
   - Error: 예외 처리 패턴

6. **Notes & Warnings** (주의사항)
   - 성능 고려사항
   - 일반적인 실수
   - 버전별 차이점

7. **See Also** (관련 API)
   - 유사 기능 링크
   - 대체 함수

### 4. 조건

**글자 수**: 1,500-2,000자

**코드 예제**: 3개 (Basic, Advanced, Error)

**파라미터 테이블**: 필수

**필수 요소**:
- 정확한 함수 시그니처
- 모든 파라미터 설명
- 반환 타입 명시
- 최소 3개 예제

**금지 사항**:
- 모호한 설명 ("일부", "몇 가지" 등)
- 파라미터 누락
- 타입 정보 생략

### 5. 형식

```markdown
# Task() - 서브 에이전트 실행

## Overview

서브 에이전트를 생성하고 실행하여 복잡한 작업을 독립적으로 처리합니다.

## Syntax

\`\`\`python
Task(
    subagent_type: str,
    description: str,
    prompt: str,
    model: Optional[str] = None,
    resume: Optional[str] = None
) -> Dict[str, Any]
\`\`\`

## Parameters

| 파라미터 | 타입 | 필수 | 기본값 | 설명 |
|---------|------|------|--------|------|
| `subagent_type` | str | ✅ | - | 실행할 에이전트 타입 (예: "Explore", "Plan") |
| `description` | str | ✅ | - | 작업 설명 (3-5 단어) |
| `prompt` | str | ✅ | - | 에이전트에게 전달할 상세 지시사항 |
| `model` | str | ❌ | None | 사용할 모델 (sonnet, opus, haiku) |
| `resume` | str | ❌ | None | 재개할 에이전트 ID |

## Return Value

**타입**: `Dict[str, Any]`

에이전트 실행 결과를 포함하는 딕셔너리:
- `output`: 에이전트가 반환한 최종 결과
- `agent_id`: 생성된 에이전트 ID
- `status`: 실행 상태 ("completed", "failed")

## Examples

### Basic Example

가장 간단한 사용법:

\`\`\`python
# 코드베이스 탐색 에이전트 실행
result = Task(
    subagent_type="Explore",
    description="Find API endpoints",
    prompt="Search for all API endpoint definitions in the codebase"
)

print(result["output"])
\`\`\`

### Advanced Example

모델 지정 및 복잡한 작업:

\`\`\`python
# 계획 에이전트를 haiku 모델로 실행
result = Task(
    subagent_type="Plan",
    description="Design database schema",
    prompt="""
    Design a database schema for a blog system including:
    - User authentication
    - Blog posts with tags
    - Comments with threading

    Provide SQL migrations and explain design decisions.
    """,
    model="haiku"  # 빠르고 비용 효율적
)

migrations = result["output"]
\`\`\`

### Error Handling

예외 처리 패턴:

\`\`\`python
try:
    result = Task(
        subagent_type="CustomAgent",
        description="Process data",
        prompt="..."
    )
except ValueError as e:
    # 잘못된 subagent_type
    print(f"Invalid agent type: {e}")
except TimeoutError as e:
    # 에이전트 실행 시간 초과
    print(f"Agent timeout: {e}")
\`\`\`

## Notes & Warnings

⚠️ **주의사항**:
- `subagent_type`은 사전에 정의된 에이전트만 사용 가능합니다
- `prompt`는 충분히 구체적이어야 합니다 (최소 1문장 이상)
- `model` 파라미터를 생략하면 부모 에이전트의 모델을 상속합니다

💡 **일반적인 실수**:
- ❌ `description`을 너무 길게 작성 (3-5 단어 권장)
- ❌ `prompt`에 실행 불가능한 지시사항 포함
- ❌ Haiku 모델로 복잡한 추론 작업 시도

📊 **성능 고려사항**:
- Explore 에이전트: 평균 10-30초
- Plan 에이전트: 평균 1-3분
- 병렬 실행 시 토큰 사용량 급증 주의

## See Also

- `ParallelAgent()` - 여러 에이전트 동시 실행
- `SequentialAgent()` - 에이전트 순차 실행
- `AskUserQuestion()` - 사용자 입력 요청
```

---

## 📝 작성 프롬프트 템플릿

```
API 참조형 섹션을 작성하세요.

**구조** (비율 준수):
1. Overview (10%) - 1-2문장 요약
2. Syntax (15%) - 함수 시그니처 (타입 힌트 포함)
3. Parameters (20%) - 파라미터 테이블 (필수)
4. Return Value (10%) - 반환 타입 및 설명
5. Examples (30%)
   - Basic Example (10-15줄)
   - Advanced Example (15-25줄)
   - Error Handling (10줄)
6. Notes & Warnings (10%) - 주의사항, 일반적 실수, 성능
7. See Also (5%) - 관련 API 링크

**글자 수**: 1,500-2,000자

**필수 요소**:
- 정확한 함수 시그니처
- 모든 파라미터 테이블 완성
- 반환 타입 명시
- 최소 3개 예제

API 이름: [API명]
기능: [기능 설명]
```

---

**마지막 수정**: 2025-11-25
**버전**: 1.0.0
**참조**: 기술 문서 표준 형식
